rust_library(
    name = "kernel-user-layout-kernel",
    srcs = glob(["src/**/*.rs"]),
    crate = "kernel_user_layout_kernel",
    crate_root = "src/lib.rs",
    edition = "2021",
    visibility = ["PUBLIC"],
    deps = [],
)

# The pure user-layout math tests (stack/auxv builder, descriptor builders,
# signal/timekeep/procinfo logic), run on the host via the same
# include!-the-production-sources pattern as the cargo tests-host harness.
rust_test(
    name = "host-tests",
    srcs = glob(["src/**/*.rs", "tests-buck/**/*.rs"]),
    crate = "kernel_user_layout_host_tests",
    crate_root = "tests-buck/lib.rs",
    edition = "2021",
    deps = [],
)
