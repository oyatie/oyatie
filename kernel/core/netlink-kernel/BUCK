rust_library(
    name = "kernel-netlink-kernel",
    srcs = glob(["src/**/*.rs"]),
    crate = "kernel_netlink_kernel",
    crate_root = "src/lib.rs",
    edition = "2021",
    visibility = ["PUBLIC"],
    deps = [],
)

# The pure netlink wire tests (RTM_GETLINK request parse, NLMSG_DONE response
# bytes, seq/port echo, truncation), run on the host via the same
# include!-the-production-sources pattern as the cargo tests-host harness.
rust_test(
    name = "host-tests",
    srcs = glob(["src/**/*.rs", "tests-buck/**/*.rs"]),
    crate = "kernel_netlink_host_tests",
    crate_root = "tests-buck/lib.rs",
    edition = "2021",
    deps = [],
)
