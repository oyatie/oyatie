diff --git b/third-party/BUCK a/third-party/BUCK
index 79f15545b..449459911 100644
--- b/third-party/BUCK
+++ a/third-party/BUCK
@@ -883,7 +883,7 @@ cargo.rust_library(
         "CARGO_PKG_VERSION_PRE": "",
     },
     proc_macro = True,
-    visibility = [],
+    visibility = ["PUBLIC"],
     deps = [
         ":proc-macro2-1",
         ":quote-1",
@@ -1129,6 +1129,11 @@ buildscript_run(
         "CARGO_PKG_VERSION_MINOR": "17",
         "CARGO_PKG_VERSION_PATCH": "0",
         "CARGO_PKG_VERSION_PRE": "",
+        # DEP_* propagated from aws-lc-sys (links = "aws_lc_0_41_0") — reindeer
+        # cannot emit the $(location) macro, so it is injected post-buckify.
+        "DEP_AWS_LC_0_41_0_INCLUDE": "$(location :aws-lc-sys-0.41-build-script-main-run[out_dir])/include",
+        "DEP_AWS_LC_0_41_0_LIBCRYPTO": "aws_lc_0_41_0_crypto",
+        "CARGO_FEATURE_AWS_LC_SYS": "1",
     },
     features = [
         "alloc",
@@ -1206,6 +1211,19 @@ buildscript_run(
         "CARGO_PKG_VERSION_PATCH": "0",
         "CARGO_PKG_VERSION_PRE": "",
         "DEBUG": "false",
+        # aws-lc-sys's cc_builder compiler feature-test (memcmp_invalid_stripped_check)
+        # LINKS an executable via $CC. Under buck2 the build-script $CC is the prelude
+        # cc-shim `clang --ld-path=__ld_shim.sh`, and __ld_shim re-invokes clang as the
+        # link driver — which RE-ADDS the C-runtime startfiles (Scrt1.o/crti.o/crtbeginS.o)
+        # on top of the complete ld line the outer clang already passed → `ld.lld: error:
+        # duplicate symbol: _start/_init/...` on aarch64-linux (darwin ld64 tolerates it).
+        # aws-lc-sys explicitly respects LDFLAGS for this probe (cc_builder.rs ~745, "brings
+        # us back to parity with CMake" for custom-linker setups), so -nostartfiles makes the
+        # OUTER clang omit its CRT; the inner __ld_shim clang adds exactly one set → links
+        # clean and the probe still runs. Scoped to this build script only (compile steps are
+        # -c, where -nostartfiles is ignored). Verified on the rust-ci image (clang 19.1.7).
+        # The durable class-wide fix is a prelude patch (-nostartfiles on the __ld_shim clang);
+        # this targeted fixup unblocks aws-lc-sys (and thus all aws-lc-rs Linux binaries) now.
         "LDFLAGS": "-nostartfiles",
         "OPT_LEVEL": "3",
         "PROFILE": "release",
@@ -9378,9 +9396,32 @@ buildscript_run(
         "CARGO_PKG_VERSION_MINOR": "10",
         "CARGO_PKG_VERSION_PATCH": "80",
         "CARGO_PKG_VERSION_PRE": "",
-        "DEP_OPENSSL_CONF": "",
-        "DEP_OPENSSL_INCLUDE": "/opt/homebrew/opt/openssl@3/include",
-        "DEP_OPENSSL_VERSION_NUMBER": "30600020",
+        # DEP_OPENSSL_* are normally propagated by Cargo's `links` mechanism from
+        # openssl-sys, which probes the ACTUAL platform OpenSSL. reindeer cannot express
+        # that dynamic propagation, so they were hardcoded to darwin/homebrew values —
+        # WRONG on the Linux gate (Debian libssl-dev OpenSSL 3.5.6, built OPENSSL_NO_IDEA):
+        # CONF="" made the openssl crate reference ffi::EVP_idea_* that openssl-sys (built
+        # with OPENSSL_NO_IDEA) never bound -> error[E0425] (#91). select() per platform:
+        #   linux  = the Debian gate image (the platform-hermetic pinned unit)
+        #   DEFAULT = darwin/homebrew (dev)
+        # Linux CONF = openssl-sys's emitted conf = its tracked OPENSSL_NO_* set
+        # (main.rs:132) INTERSECT Debian's defined macros = IDEA + SSL3_METHOD.
+        # Linux VERSION_NUMBER = 0x30500060 (3.5.6); INCLUDE = /usr/include.
+        # NOTE: `reindeer buckify` regenerates from the static fixup and WILL wipe these
+        # selects (same DEP-propagation durability gap as DEP_AWS_LC_*); re-apply until a
+        # durable links-DEP mechanism exists. See third-party/fixups/openssl/fixups.toml.
+        "DEP_OPENSSL_CONF": select({
+            "prelude//os:linux": "OPENSSL_NO_IDEA,OPENSSL_NO_SSL3_METHOD",
+            "DEFAULT": "",
+        }),
+        "DEP_OPENSSL_INCLUDE": select({
+            "prelude//os:linux": "/usr/include",
+            "DEFAULT": "/opt/homebrew/opt/openssl@3/include",
+        }),
+        "DEP_OPENSSL_VERSION_NUMBER": select({
+            "prelude//os:linux": "30500060",
+            "DEFAULT": "30600020",
+        }),
     },
     features = ["default"],
     version = "0.10.80",
@@ -11466,11 +11507,27 @@ cxx_library(
     ],
     compiler_flags = ["-xassembler-with-cpp"],
     preferred_linkage = "static",
-    preprocessor_flags = [
-        "-DCFG_TARGET_OS_darwin",
-        "-DCFG_TARGET_ARCH_aarch64",
-        "-DCFG_TARGET_ENV_",
-    ],
+    preprocessor_flags = select({
+        # psm.h decorates the stack-manipulation asm symbol PER OS: darwin/macos/ios ->
+        # Mach-O leading underscore (_rust_psm_stack_pointer); ELF -> undecorated. The
+        # reindeer fixup hardcoded -DCFG_TARGET_OS_darwin, so on the aarch64-linux gate the
+        # asm emitted the Mach-O symbol while linux-ELF rustc references the undecorated
+        # rust_psm_stack_pointer -> `ld.lld: undefined symbol: rust_psm_stack_pointer` at the
+        # rust_binary final link (#96/#78). Per-OS select fixes it. PROVEN: cloud-intelligence-app
+        # links green on aarch64-linux with -DCFG_TARGET_OS_linux. NOTE: `reindeer buckify`
+        # regenerates from the static fixup and WILL wipe this select (TOML can't express it) --
+        # re-apply post-buckify (see third-party/fixups/psm/fixups.toml).
+        "prelude//os:linux": [
+            "-DCFG_TARGET_OS_linux",
+            "-DCFG_TARGET_ARCH_aarch64",
+            "-DCFG_TARGET_ENV_",
+        ],
+        "DEFAULT": [
+            "-DCFG_TARGET_OS_darwin",
+            "-DCFG_TARGET_ARCH_aarch64",
+            "-DCFG_TARGET_ENV_",
+        ],
+    }),
     visibility = [],
 )

@@ -12943,7 +12943,7 @@ cargo.rust_library(
         "pki_types": ":rustls-pki-types-1",
     },
     rustc_flags = ["@$(location :rustls-0.23-build-script-run[rustc_flags])"],
-    visibility = [],
+    visibility = ["PUBLIC"],
     deps = [
         ":aws-lc-rs-1",
         ":log-0.4",

