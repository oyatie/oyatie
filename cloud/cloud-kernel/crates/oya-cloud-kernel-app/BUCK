# The kernel image: links exactly one arch Frame backend at a bare-metal target
# with a custom linker script. Bare-metal-only (see //cloud/cloud-kernel:BUCK).
rust_binary(
    name = "oya-cloud-kernel-app",
    srcs = glob(["src/**/*.rs"]),
    crate = "kernel",
    crate_root = "src/main.rs",
    edition = "2021",
    visibility = ["PUBLIC"],
    target_compatible_with = ["//cloud/cloud-kernel:bare-metal"],
    named_deps = {
        "hal": "//cloud/cloud-kernel/crates/oya-cloud-kernel-hal-kernel:oya-cloud-kernel-hal-kernel",
        "frame": "//cloud/cloud-kernel/crates/oya-cloud-kernel-frame-kernel:oya-cloud-kernel-frame-kernel",
    },
)
