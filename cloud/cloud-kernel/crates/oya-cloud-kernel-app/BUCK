# Intentionally no Buck2 target yet.
#
# This kernel image links an arch Frame backend with a custom bare-metal linker
# script. Exposing it as a placeholder Buck2 target before the real bare-metal
# Rust platform/toolchain exists makes direct builds fail while wildcard builds
# skip it, which is false-green CI coverage. See //cloud/cloud-kernel:BUCK.
