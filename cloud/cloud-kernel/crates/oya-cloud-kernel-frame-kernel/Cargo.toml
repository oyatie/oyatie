[package]
name = "oya-cloud-kernel-frame-kernel"
version.workspace = true
rust-version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false
description = "The Frame: safe abstractions (heap/memory/traps) built over the arch HAL."

[package.metadata.oya]
microservice = "cloud-kernel"
bounded_context = "cloud-kernel"
kind = "shared"
layer = "kernel"
owner_team = "axis-cloud-platform"
purpose = "Cutover-stable safe Frame abstraction layer over the architecture HAL for heap, memory, and trap surfaces."
adr_anchors = ["ADR-0512", "ADR-0520", "ADR-0536", "ADR-0562", "ADR-0547"]
design_non_claim = "scaffold_only_no_module_build_boot_artifact_or_runtime_readiness_claim"

[dependencies]
hal = { workspace = true }
# NOTE: the heap allocator (`linked_list_allocator`) is vetted in
# ALLOWED_CRATES.md and will be added here when the global allocator is wired
# up over a live RAM region. It is intentionally omitted until then so the
# skeleton builds warning-free.

[lints]
workspace = true
