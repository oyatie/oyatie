[package]
name = "oya-cloud-os-upgrade-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "OS upgrade orchestration and rollback for the operating-system Talos port."

[lib]
name = "oya_cloud_os_upgrade_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
oya-cloud-os-kernel = { path = "../oya-cloud-os-kernel" }

[lints]
workspace = true
