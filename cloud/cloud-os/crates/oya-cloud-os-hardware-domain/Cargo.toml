[package]
name = "oya-cloud-os-hardware-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Hardware inventory and SMBIOS/PCI discovery for the operating-system Talos port."

[lib]
name = "oya_cloud_os_hardware_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
oya-cloud-os-kernel = { path = "../oya-cloud-os-kernel" }

[lints]
workspace = true
