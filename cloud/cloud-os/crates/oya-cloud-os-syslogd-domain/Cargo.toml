[package]
name = "oya-cloud-os-syslogd-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Embedded syslog receiver and parser for the operating-system Talos port."

[lib]
name = "oya_cloud_os_syslogd_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
oya-cloud-os-kernel = { path = "../oya-cloud-os-kernel" }

[lints]
workspace = true
