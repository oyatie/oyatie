[package]
name = "oya-cloud-os-install-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "no_std port of the Talos installer and bootloader subsystem (GRUB, systemd-boot, UKI, A/B boot entries, image extraction, upgrades)."

[lib]
name = "oya_cloud_os_install_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
oya-cloud-os-kernel = { path = "../oya-cloud-os-kernel" }

[lints]
workspace = true
