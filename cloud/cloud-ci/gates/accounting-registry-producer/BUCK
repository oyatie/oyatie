rust_library(
    name = "accounting-registry-producer",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "accounting_registry_producer",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        # The fifth face (gate-baseline.generated.json) runs each gate's pure evaluate_keyed
        # over the live faces. The gate libs depend only on serde_json, so there is no cycle.
        "//cloud/cloud-ci/gates/total-accounting:total-accounting",
        "//cloud/cloud-ci/gates/cross-artifact-agreement:cross-artifact-agreement",
        "//cloud/cloud-ci/gates/staleness-reaper:staleness-reaper",
        "//cloud/cloud-ci/gates/automation-ratchet:automation-ratchet",
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "accounting-registry-producer-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "accounting_registry_producer",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//cloud/cloud-ci/gates/total-accounting:total-accounting",
        "//cloud/cloud-ci/gates/cross-artifact-agreement:cross-artifact-agreement",
        "//cloud/cloud-ci/gates/staleness-reaper:staleness-reaper",
        "//cloud/cloud-ci/gates/automation-ratchet:automation-ratchet",
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)

rust_binary(
    name = "accounting-registry-producer-bin",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//cloud/cloud-ci/gates/accounting-registry-producer:accounting-registry-producer",
        "//cloud/cloud-ci/gates/total-accounting:total-accounting",
        "//cloud/cloud-ci/gates/cross-artifact-agreement:cross-artifact-agreement",
        "//cloud/cloud-ci/gates/staleness-reaper:staleness-reaper",
        "//cloud/cloud-ci/gates/automation-ratchet:automation-ratchet",
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)
