{
  "$comment": "ALLOWLIST-as-DATA for the root-workspace-hygiene gate (ADR-0600). The gate engine is UNIVERSAL: it reads this allowlist as DATA and runs on ANY repo. Oyatie's allowlist is just the pack below. POSTURE: default-DENY. Every TRACKED file at the repository ROOT (a tracked path that carries no '/' separator) MUST match an allowlist rule; ANY tracked root file that matches NO rule fails the gate (root_workspace_unallowlisted_file). This is the complement of the existing scratch DENYLIST (cloud-ci-total-accounting `scratch_artifact`, which catches KNOWN scratch shapes): the allowlist catches EVERY new root file, including scratch shapes nobody has named yet. The two layers compose — denylist = zero-tolerance on known scratch, allowlist = born-blocking on any unrecognized root file. Adding a NEW legitimate root file is a reviewed DATA edit here (never a scanner branch). Top-level DIRECTORIES are governed by `allowed_root_dirs` (a tracked path's first segment must be a permitted capability/meta dir). Rules are evaluated against the BASENAME of each tracked root file; the FIRST matching rule wins (order is significant only for diagnostics).",
  "gate_id": "cloud-ci-root-workspace-hygiene",
  "match_kinds": {
    "exact": "the tracked root file basename equals `value`",
    "suffix": "the tracked root file basename ends with `value` (e.g. an extension or a family like `.buckconfig`)",
    "prefix": "the tracked root file basename starts with `value` (e.g. README*, LICENSE*)"
  },
  "allowed_root_files": [
    { "id": "cargo-manifest",        "kind": "exact",  "value": "Cargo.toml",          "reason": "workspace Cargo manifest" },
    { "id": "cargo-lock",            "kind": "exact",  "value": "Cargo.lock",          "reason": "generated lockfile (kept tracked)" },
    { "id": "buck-target",           "kind": "exact",  "value": "BUCK",                "reason": "root buck2 target file" },
    { "id": "buckconfig",            "kind": "suffix", "value": ".buckconfig",         "reason": "buck2 config (.buckconfig, .buckconfig.*)" },
    { "id": "buckroot",              "kind": "exact",  "value": ".buckroot",           "reason": "buck2 cell-root marker" },
    { "id": "rust-toolchain",        "kind": "exact",  "value": "rust-toolchain.toml", "reason": "pinned Rust toolchain" },
    { "id": "rust-project",          "kind": "exact",  "value": "rust-project.json",   "reason": "rust-analyzer project descriptor (if present)" },
    { "id": "rustfmt",               "kind": "exact",  "value": "rustfmt.toml",        "reason": "rustfmt config" },
    { "id": "reindeer",              "kind": "exact",  "value": "reindeer.toml",       "reason": "reindeer (buck2 third-party) config" },
    { "id": "deny",                  "kind": "exact",  "value": "deny.toml",           "reason": "cargo-deny supply-chain policy" },
    { "id": "readme",                "kind": "prefix", "value": "README",              "reason": "README, README.md, README.*" },
    { "id": "license",               "kind": "prefix", "value": "LICENSE",             "reason": "LICENSE, LICENSE-*, LICENSE.*" },
    { "id": "claude-md",             "kind": "exact",  "value": "CLAUDE.md",           "reason": "project agent-rules source (root authority)" },
    { "id": "agents-md",             "kind": "exact",  "value": "AGENTS.md",           "reason": "agent operating contract pointer" },
    { "id": "handoff-md",            "kind": "exact",  "value": "HANDOFF.md",          "reason": "session handoff anchor (masterplan companion)" },
    { "id": "adr-inventory",         "kind": "exact",  "value": "ADR-INVENTORY.tsv",   "reason": "ADR inventory index" },
    { "id": "goal-json",             "kind": "exact",  "value": "goal.json",           "reason": "root goal descriptor" },
    { "id": "oya-ci-toml",           "kind": "exact",  "value": "oya-ci.toml",         "reason": "oya-ci enforcement config (DATA)" },
    { "id": "makefile",              "kind": "exact",  "value": "Makefile",            "reason": "developer convenience targets" },
    { "id": "dockerfile-distroless", "kind": "exact",  "value": "Dockerfile.distroless", "reason": "release image build descriptor" },
    { "id": "gitignore",             "kind": "exact",  "value": ".gitignore",          "reason": "git ignore policy" },
    { "id": "gitattributes",         "kind": "exact",  "value": ".gitattributes",      "reason": "git attributes policy" },
    { "id": "editorconfig",          "kind": "exact",  "value": ".editorconfig",       "reason": "editor config (if present)" },
    { "id": "envrc",                 "kind": "exact",  "value": ".envrc",              "reason": "direnv environment loader" }
  ],
  "allowed_root_dirs": [
    ".cargo", ".claude", ".codex", ".config", ".github", ".omc",
    "audit", "benchmarks", "billing", "bin", "cell", "cloud", "comms",
    "compliance", "compute", "console", "contracts", "data", "docs",
    "evidence", "flags", "gateway", "governance", "iac", "iam", "infra",
    "intelligence", "k8s", "libs", "marketplace", "messaging", "network",
    "observability", "oya", "packs", "plan", "registry", "scripts",
    "secrets", "specs", "storage", "tasks", "templates", "tenancy",
    "third-party", "toolchains", "tools", "workflow"
  ],
  "product_contract": {
    "auto_fix": "Every finding carries a concrete remediation per offending path: relocate process scratch to `.omc/` (the gitignored scratch home) or `git rm` the file. A NEW legitimate root file is admitted by adding a reviewed allowlist rule here (a DATA edit), never a scanner change.",
    "portability": "The engine reads the allowlist + allowed-dir set as DATA. Another repository adopts the gate by replacing `allowed_root_files` / `allowed_root_dirs` with its own legitimate root surface; zero engine code changes.",
    "hermeticity": "Pure evaluator: the gate consumes the producer's git-ls-files snapshot (scm-facts) as DATA and a committed allowlist; no shell, no network, no clock, no rand, no filesystem access beyond declared inputs. Deterministic, buck2-built.",
    "relationship_to_scratch_denylist": "Complements (does not replace) cloud-ci-total-accounting `scratch_artifact`: the denylist is zero-tolerance on KNOWN scratch shapes; this allowlist is born-blocking on ANY unrecognized tracked root file (including unnamed scratch shapes), making committed root scratch structurally impossible."
  },
  "schema_version": "1.0.0"
}
