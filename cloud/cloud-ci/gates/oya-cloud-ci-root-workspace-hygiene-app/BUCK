rust_library(
    name = "oya-cloud-ci-root-workspace-hygiene-app",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_root_workspace_hygiene_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "oya-cloud-ci-root-workspace-hygiene-app-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_root_workspace_hygiene_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# ADR-0600 root-workspace-hygiene: the born-blocking self-test over the live tracked-path corpus.
# It loads the committed allowlist policy + the committed scm-facts.generated.json snapshot
# (a declared hermetic input — no git, no producer exec in the action), asserts the gate is GREEN
# on today's clean allowlisted root tree, and proves a synthetic tracked `foo.log` at root is
# born-blocking RED (the make-impossible guarantee). Runs in the cargo-matrix lane
# (`cargo test -p oya-cloud-ci-root-workspace-hygiene-app`) via the in-repo working tree.
# G-INTEGRITY track: no build-graph dep on the firewall.
rust_test(
    name = "oya-cloud-ci-root-workspace-hygiene-app-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "oya_cloud_ci_root_workspace_hygiene_app_corpus",
    crate_root = "tests/root_workspace_hygiene.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-root-workspace-hygiene-app",
        "third-party//:serde_json",
    ],
)
