[package]
name = "oya-cloud-ci-enforcement-liveness-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
bounded_context = "cloud-ci"
kind = "shared"
layer = "app"
purpose = "Cloud CI gate ensuring tracked hook enforcement remains live in both Claude and Codex project wiring."

[lib]
name = "oya_cloud_ci_enforcement_liveness_app"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
