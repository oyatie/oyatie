rust_library(
    name = "staleness-reaper",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "staleness_reaper",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "staleness-reaper-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "staleness_reaper",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# GATE-3 :staleness-reaper — the fixture-corpus + born-blocking self-test gate.
# Globs specs/fixtures/staleness-reaper/tc-*.json (the data-under-test) and asserts
# report.violations == fixture.expected_violations. G-INTEGRITY track: no build-graph dep.
# (Named -gate to avoid a buck2 in-package collision with the rust_library above; the
# package //cloud/cloud-ci/gates/staleness-reaper carries the §5.3 :staleness-reaper identity.)
rust_test(
    name = "staleness-reaper-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "staleness_reaper_corpus",
    crate_root = "tests/staleness_reaper.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":staleness-reaper",
        "//cloud/cloud-ci/gates/accounting-registry-producer:accounting-registry-producer",
        "third-party//:serde_json",
    ],
)
