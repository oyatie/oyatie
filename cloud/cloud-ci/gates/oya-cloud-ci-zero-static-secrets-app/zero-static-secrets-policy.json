{
  "$schema": "https://docs.oyatie.com/schemas/cloud-ci-zero-static-secrets-policy.schema.json",
  "gate_id": "cloud-ci-zero-static-secrets",
  "bootstrap_exceptions": [
    {
      "id": "g002-openbao-transitional-unseal-placeholder",
      "path": "bootstrap/root-of-trust/sealed-openbao-dev.unseal.env",
      "secret_kind": "openai_or_anthropic_key",
      "line_contains": "OPENBAO_TRANSITIONAL_UNSEAL_KEY=",
      "owner": "council-security",
      "reason": "G002 dogfood bootstrap step 2 permits a transitional sealed OpenBao handoff only through policy data while workload identity takes over.",
      "replacement_contract": "G002 step 2: workload identity plus dynamic secret leasing; delete this exception when the OpenBao bootstrap handoff is controller-issued."
    },
    {
      "id": "dev-cli-lint-ghp-fixture",
      "path": "oya/developer-sdk/crates/oya-dev-cli/src/commands/lint.rs",
      "secret_kind": "github_token",
      "line_contains": "secret_pattern_hit(\"token =",
      "owner": "developer-experience",
      "reason": "Existing lint heuristic regression fixture intentionally uses a credential-shaped GitHub token exemplar as test data, not a live credential.",
      "replacement_contract": "Replace the fixture with runtime-concatenated non-literal test data when the retired dev-cli lint path is next edited."
    },
    {
      "id": "dev-cli-lint-akia-fixture",
      "path": "oya/developer-sdk/crates/oya-dev-cli/src/commands/lint.rs",
      "secret_kind": "aws_access_key_id",
      "line_contains": "secret_pattern_hit(\"key =",
      "owner": "developer-experience",
      "reason": "Existing lint heuristic regression fixture intentionally uses a credential-shaped AWS access key exemplar as test data, not a live credential.",
      "replacement_contract": "Replace the fixture with runtime-concatenated non-literal test data when the retired dev-cli lint path is next edited."
    }
  ]
}
