rust_library(
    name = "oya-cloud-ci-manifest-hygiene-app",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_manifest_hygiene_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "oya-cloud-ci-manifest-hygiene-app-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_manifest_hygiene_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# §2.5#7 :manifest-hygiene — the born-blocking self-test over the live corpus. Runs the producer
# `--face manifest-hygiene` to resolve the per-crate manifest flags, then asserts the gate fires
# (some first-party oya-* crates miss a §2.5#7 field today) and reports the measured count.
# G-INTEGRITY track: no build-graph dep. (Named -gate to avoid an in-package buck2 collision.)
rust_test(
    name = "oya-cloud-ci-manifest-hygiene-app-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "oya_cloud_ci_manifest_hygiene_app_corpus",
    crate_root = "tests/manifest_hygiene.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-manifest-hygiene-app",
        "//cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app:oya-cloud-ci-accounting-registry-app",
        "third-party//:serde_json",
    ],
)
