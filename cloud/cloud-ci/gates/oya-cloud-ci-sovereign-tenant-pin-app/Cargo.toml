[package]
name = "oya-cloud-ci-sovereign-tenant-pin-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# sovereign-tenant-pin: pure readiness gate packet for tenant pin routing fixtures.

[lib]
name = "oya_cloud_ci_sovereign_tenant_pin_app"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
