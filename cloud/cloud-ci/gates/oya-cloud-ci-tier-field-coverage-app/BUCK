rust_library(
    name = "oya-cloud-ci-tier-field-coverage-app",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_tier_field_coverage_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_binary(
    name = "oya-cloud-ci-tier-field-coverage-app-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-tier-field-coverage-app",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "oya-cloud-ci-tier-field-coverage-app-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_tier_field_coverage_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# Phase-0 capability-first reorg (ADR-0562/0536/0245/0280): the born-blocking self-test over the
# real service manifests under cloud/ + oya/ plus on-disk RED/GREEN fixtures. It collects every
# governed service manifest from the live tree and asserts the corpus is born-blocking green (every
# service carries a valid tier/tier_subtype/dr_tier triple with no `tier` type-overload, every
# substrate carries a valid substrate_dag_position) while every violation class fails closed from
# disk. Also asserts the gate policy enums match specs/platform-architecture.json verbatim.
rust_test(
    name = "oya-cloud-ci-tier-field-coverage-app-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_tier_field_coverage_app_gate",
    crate_root = "tests/tier_field_coverage.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-tier-field-coverage-app",
        "third-party//:serde_json",
    ],
)
