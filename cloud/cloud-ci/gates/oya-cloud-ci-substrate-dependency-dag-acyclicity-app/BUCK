rust_library(
    name = "oya-cloud-ci-substrate-dependency-dag-acyclicity-app",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_substrate_dependency_dag_acyclicity_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# Gate binary (ADR-0280 §D-3). Loads the policy-declared substrate DAG and exits 0/1/2.
# LOCAL BRIDGE feedback only (founder CLI-retirement directive): merge authority is the gate test
# below + the check-substrates lane, never this binary.
rust_binary(
    name = "oya-cloud-ci-substrate-dependency-dag-acyclicity-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-substrate-dependency-dag-acyclicity-app",
        "third-party//:serde_json",
    ],
)

# Pure-kernel unit tests: Tarjan / Kahn / forbidden-edge / parse, plus the RED cycle classes
# (two-node, three-node, self-loop, six-node-buried) proven WITHOUT a filesystem.
rust_test(
    name = "oya-cloud-ci-substrate-dependency-dag-acyclicity-app-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_substrate_dependency_dag_acyclicity_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# ADR-0280 §D-3 acyclicity lane: Buck2-native live-corpus self-test over the REAL policy-declared
# substrate DAG (must be GREEN: acyclic, forbidden edges honoured,
# bootstrap_order == Kahn topo-sort), plus the on-disk RED cycle fixtures under
# tests/fixtures/dag-cycles/ (each MUST fail the validator).
rust_test(
    name = "oya-cloud-ci-substrate-dependency-dag-acyclicity-app-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_substrate_dependency_dag_acyclicity_app_gate",
    crate_root = "tests/acyclicity.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-substrate-dependency-dag-acyclicity-app",
        "third-party//:serde_json",
    ],
)
