rust_library(
    name = "total-accounting",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "total_accounting",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "total-accounting-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "total_accounting",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# GATE-2 :total-accounting-gate — the fixture-corpus + born-blocking self-test gate.
# Globs specs/fixtures/total-accounting/tc-*.json (the data-under-test) and asserts
# report.violations == fixture.expected_violations. G-INTEGRITY track: no build-graph dep.
# (Named -gate to avoid a buck2 in-package collision with the rust_library above; the
# package //cloud/cloud-ci/gates/total-accounting carries the §5.3 :total-accounting identity.)
rust_test(
    name = "total-accounting-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "total_accounting_corpus",
    crate_root = "tests/total_accounting.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":total-accounting",
        "//cloud/cloud-ci/gates/accounting-registry-producer:accounting-registry-producer",
        "third-party//:serde_json",
    ],
)
