rust_library(
    name = "oya-cloud-ci-gate-self-conformance-app",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_gate_self_conformance_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "oya-cloud-ci-gate-self-conformance-app-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_gate_self_conformance_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# Pipeline-as-product meta-gate: live-corpus scan over every gate crate's production Rust
# plus synthetic RED/GREEN fixtures in the library unit tests. The policy file carries repo-local
# roots and the declared transitional orchestrator exceptions; the Rust engine stays shape-neutral.
rust_test(
    name = "oya-cloud-ci-gate-self-conformance-app-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_gate_self_conformance_app_gate",
    crate_root = "tests/gate_self_conformance.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-gate-self-conformance-app",
        "third-party//:serde_json",
    ],
)
