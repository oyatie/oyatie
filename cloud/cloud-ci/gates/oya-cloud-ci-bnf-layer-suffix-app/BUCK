rust_library(
    name = "oya-cloud-ci-bnf-layer-suffix-app",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_bnf_layer_suffix_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//libs/oya-governance-predictable-naming-kernel:oya-governance-predictable-naming-kernel",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "oya-cloud-ci-bnf-layer-suffix-app-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_bnf_layer_suffix_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//libs/oya-governance-predictable-naming-kernel:oya-governance-predictable-naming-kernel",
        "third-party//:serde_json",
    ],
)

# §2.5#4 :bnf-layer-suffix — the born-blocking self-test over the live corpus. Runs the producer
# `--face bnf-layer-suffix` to resolve the live oya-* crate names, then asserts the gate fires
# (non-canonical trailing segments exist today) and reports the measured count. G-INTEGRITY
# track: no build-graph dep. (Named -gate to avoid an in-package buck2 collision with the
# rust_library above; the package identity carries the §5.3 :bnf-layer-suffix gate.)
rust_test(
    name = "oya-cloud-ci-bnf-layer-suffix-app-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "oya_cloud_ci_bnf_layer_suffix_app_corpus",
    crate_root = "tests/bnf_layer_suffix.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-bnf-layer-suffix-app",
        "//cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app:oya-cloud-ci-accounting-registry-app",
        "third-party//:serde_json",
    ],
)
