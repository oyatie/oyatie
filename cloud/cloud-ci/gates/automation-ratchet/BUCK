rust_library(
    name = "automation-ratchet",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "automation_ratchet",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "automation-ratchet-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "automation_ratchet",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# GATE-4 :automation-ratchet — the fixture-corpus + born-blocking self-test gate (polices
# Gates 1-3; test-time dep, not a build dep). Globs specs/fixtures/phase0-automation-ratchet/
# tc-*.json (the data-under-test) and asserts report.violations == fixture.expected_violations.
# G-INTEGRITY track: no build-graph dep.
# (Named -gate to avoid a buck2 in-package collision with the rust_library above; the
# package //cloud/cloud-ci/gates/automation-ratchet carries the §5.3 :automation-ratchet identity.)
rust_test(
    name = "automation-ratchet-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "automation_ratchet_corpus",
    crate_root = "tests/automation_ratchet.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":automation-ratchet",
        "//cloud/cloud-ci/gates/accounting-registry-producer:accounting-registry-producer",
        "third-party//:serde_json",
    ],
)
