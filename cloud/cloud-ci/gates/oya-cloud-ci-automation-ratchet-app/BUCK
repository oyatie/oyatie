rust_library(
    name = "oya-cloud-ci-automation-ratchet-app",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_automation_ratchet_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "oya-cloud-ci-automation-ratchet-app-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "oya_cloud_ci_automation_ratchet_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# GATE-4 :automation-ratchet — the fixture-corpus + born-blocking self-test gate (polices
# Gates 1-3; test-time dep, not a build dep). Globs specs/fixtures/phase0-automation-ratchet/
# tc-*.json (the data-under-test) and asserts report.violations == fixture.expected_violations.
# G-INTEGRITY track: no build-graph dep.
# (Named -gate to avoid a buck2 in-package collision with the rust_library above; the
# package //cloud/cloud-ci/gates/oya-cloud-ci-automation-ratchet-app carries the §5.3 :automation-ratchet identity.)
rust_test(
    name = "oya-cloud-ci-automation-ratchet-app-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "oya_cloud_ci_automation_ratchet_app_corpus",
    crate_root = "tests/automation_ratchet.rs",
    # HERMETIC: the test execs the buck2-BUILT producer binary (no `env!("CARGO")`, no `cargo
    # run`). `$(exe ...)` makes buck2 build the producer and substitute its sandbox path here;
    # the producer reads the committed scm-facts face (a declared input) — no action calls git.
    env = {
        "OYA_CI_PRODUCER_BIN": "$(exe //cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app:oya-cloud-ci-accounting-registry-app-bin)",
    },
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-automation-ratchet-app",
        "//cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app:oya-cloud-ci-accounting-registry-app",
        "third-party//:serde_json",
    ],
)
