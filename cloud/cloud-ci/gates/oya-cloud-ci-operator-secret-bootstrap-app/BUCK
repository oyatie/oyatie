rust_library(
    name = "oya-cloud-ci-operator-secret-bootstrap-app",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_operator_secret_bootstrap_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:serde_yaml",
    ],
)

rust_test(
    name = "oya-cloud-ci-operator-secret-bootstrap-app-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_operator_secret_bootstrap_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:serde_yaml",
    ],
)

# GH #980 cloud-ci-operator-secret-bootstrap: live-tree self-test plus hermetic RED/GREEN fixtures.
# The live leg runs the real read-only collector over the committed cloud-iam SVID-operator chart and
# asserts it is GREEN post-fix (secrets RBAC name-scoped + join token provisioned); the RED fixture
# proves the gate genuinely FAILS on the #980 defect (namespace-wide secrets get/update/patch + no
# join-token provisioning). No build-graph dep on the chart — the collector reads the tree directly.
rust_test(
    name = "oya-cloud-ci-operator-secret-bootstrap-app-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "oya_cloud_ci_operator_secret_bootstrap_app_gate",
    crate_root = "tests/operator_secret_bootstrap.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":oya-cloud-ci-operator-secret-bootstrap-app",
        "third-party//:serde_json",
        "third-party//:serde_yaml",
    ],
)
