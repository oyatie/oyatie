[package]
name = "oya-shared-hyperscaler-metrics-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure trait kernel (port-in-kernel per ADR-0056). No external deps; an
# adapter crate (oya-shared-hyperscaler-metrics-adapter-prometheus) wires
# this to the prometheus client library. Per ADR-0064 canonical-base-and-
# localization-packs: this kernel is the canonical base every µservice
# integrates against to satisfy
# observability/contracts/metric-naming-convention.md.

[dependencies]

[lints]
workspace = true
