[package]
name = "oya-shared-webhook-delivery-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Skeleton authored 2026-05-18 per ADR-0169 (webhook DLQ + retry).
# Canonical webhook-delivery substrate; every µservice with outbound
# webhook delivery integrates this trait surface per ADR-0169 §"Decision".
#
# Full impl (HTTP-1.1 adapter, DLQ Postgres tables, circuit breaker,
# secret rotation, Stripe-Signature parity) tracked under
# registry/placeholder-debt/adr-follow-ups.yaml#adr-0169-webhook-impl.

[dependencies]

[lib]
name = "oya_shared_webhook_delivery_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
