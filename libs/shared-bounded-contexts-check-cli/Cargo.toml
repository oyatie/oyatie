[package]
name = "shared-bounded-contexts-check-cli"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
vertical = "shared"
bounded_context = "bounded-contexts-check"
kind = "shared"
layer = "cli"
purpose = "LEAN-A2: BC registry validation, cross-vertical refusal, transitive walker, public_layers per-hop, overlap governance jaro-winkler."

[[bin]]
name = "shared-bounded-contexts-check-cli"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }

[lints]
workspace = true
