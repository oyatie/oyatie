[package]
name = "oya-shared-outbox-pattern-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Tier-A hyperscaler pattern (Stripe outbox + Uber Cadence).
# Authored 2026-05-18 per ADR-0153. The trait surface every µservice
# integrates to avoid the (DB, bus) dual-write hazard.

[dependencies]

[lib]
name = "oya_shared_outbox_pattern_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
