[package]
name = "oya-shared-object-store-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Object-store kernel per ADR-0196 (SeaweedFS primary, Ceph RGW scale-up).
# Pure trait + reference adapter; no external network deps in kernel scope.
# Authored 2026-05-18.

[dependencies]

[lib]
name = "oya_shared_object_store_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
