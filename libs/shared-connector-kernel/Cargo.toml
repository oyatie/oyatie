[package]
name = "shared-connector-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[lib]
name = "shared_connector_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
# Kernel is pure — no I/O, no network, no codegen deps.
# RustCrypto SHA-2 is used only for deterministic audit payload digests.
sha2 = { workspace = true }

[lints]
workspace = true
