[package]
name = "buck-syntax-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
bounded_context = "buck-syntax"
kind = "shared"
layer = "kernel"
purpose = "Sound lexer/parser + span-accurate safe-edit primitives and fixer self-validation harness for the Starlark subset BUCK gate crates consume."

# buck-syntax-kernel (ADR-0549): the single shared BUCK/Starlark parsing oracle for the cloud-ci
# gates (FRIC-1781131000-buck-syntax-kernel, FRIC-1781200001). Pure kernel: std-only, no filesystem,
# no policy, no repo specifics (R0 pack-shape). Consumers: cloud-ci-embedded-asset-hermeticity-app,
# cloud-ci-kernel-purity-app.

[lib]
name = "buck_syntax_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]

[lints]
workspace = true
