{
  "_comment": "De-brand strangler MOVE-1: home the messaging capability's substrate FLOOR from the libs/ junk-drawer into the messaging capability. The crate is an ADR-0083 kernel (pure-types + sync-traits, empty [dependencies]) and a dependency SINK (zero out-edges); its three consumers (bus/queue/stream boundary kernels) stay in libs/ and get their path-deps/BUCK-labels/Rust-idents rewired INTO messaging/core/substrate-kernel (libs->messaging is acyclic: the moved crate is a sink). Face = core per ADR-0570 (a cutover-stable kernel-tier port is PERMITTED in core; mirrors the sole existing leaf messaging/core/domain). De-brand per ADR-0532/0533: cargo name = de-branded path tail (oya-messaging-substrate-kernel -> messaging-substrate-kernel). ADR-0562 capability-first placement.",
  "capability": "messaging",
  "moves": [
    {
      "old_path": "libs/oya-messaging-substrate-kernel",
      "new_path": "messaging/core/substrate-kernel",
      "old_cargo_name": "oya-messaging-substrate-kernel",
      "new_cargo_name": "messaging-substrate-kernel"
    }
  ],
  "artifacts": []
}
