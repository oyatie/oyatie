{
  "_comment": "Cargo.lock graph mutations the ci/ keystone move introduces BEYOND the renames in ci-keystone-rename-map.json. Consumed with that plan's old->new bijection by `xtask-metadata-augment lockfile-move` (owned Rust, NO cargo) to maintain Cargo.lock: rename 46 crates + register these 2 new local members + add these edges + canonicalize. PROVEN byte-identical to the cargo-produced lock at 39e9c3a85. new_members are the PathResolver security core crates created by the keystone; add_dependencies are the edges from the (renamed) scm-facts snapshot facade that consumes them. This is a pure text/graph transform — no version resolution (every name is already locked); registry version bumps remain the ADR-0535 owned bump-bot's job.",
  "new_members": [
    {
      "name": "ci-path-resolver-ports",
      "version": "0.1.0",
      "dependencies": []
    },
    {
      "name": "ci-path-resolver-adapters",
      "version": "0.1.0",
      "dependencies": [
        "ci-path-resolver-ports",
        "serde_json"
      ]
    }
  ],
  "add_dependencies": [
    {
      "package": "ci-scm-facts-snapshot",
      "add": [
        "ci-path-resolver-adapters",
        "ci-path-resolver-ports"
      ]
    }
  ]
}
