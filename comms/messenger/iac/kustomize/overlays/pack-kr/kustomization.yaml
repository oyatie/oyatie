# Canonical-base: microservices/governance/iac/kustomize/components/{namespace-restricted-profile,pack-overlay-canonical/<pack>} (SWEEP-I Slice 3 per ADR-0064)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: messenger-kr

resources:
  - ../../base

# Pack-KR overlay per /specs/regulatory-packs.json#pack-kr
# Residency: KR data centers only; cross-pack federation forbidden.
# Retention floor: KR PIPA Art. 21 + Art. 28.
# eDiscovery: 전자문서법 (Electronic Document Act) compliance.

patches:
  - target: {kind: HelmRelease, name: messenger}
    patch: |-
      - op: replace
        path: /spec/values/global/pack
        value: kr
  - target: {kind: ConfigMap, name: messenger-retention-policy}
    patch: |-
      - op: replace
        path: /data/pack-kr-floor-days
        value: "1095"  # 3 years for work-context default per KR PIPA Art. 21
