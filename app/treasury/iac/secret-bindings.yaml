apiVersion: oyatie.dev/v1
kind: SecretBinding
metadata:
  name: oya-treasury
spec:
  openbaoPaths:
    - secret/{{ tenant_id }}/treasury/database
    - secret/{{ tenant_id }}/treasury/signing
  maxTtlSeconds: 60
  auditEventClass: EVT-TREASURY-CASH_POSITION-SECRET_LEASE
