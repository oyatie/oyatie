# Canonical-base: microservices/governance/iac/kustomize/components/{namespace-restricted-profile,pack-overlay-canonical/<pack>} (SWEEP-I Slice 3 per ADR-0064)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# pack-kr overlay: pack-specific residency + retention + replicas + signing keys.
# Per data-residency.md + multi-region.md (pack-kr: single-region OCI ap-seoul-1).

bases:
  - ../../base

patches:
  - patch: |-
      - op: replace
        path: /spec/values/citus/worker/replicas
        value: 2  # pack-kr trial tier
    target:
      kind: HelmRelease
      name: sheets-postgres
  - patch: |-
      - op: replace
        path: /spec/values/replicaCount
        value: 3
    target:
      kind: HelmRelease
      name: collab-crdt-worker
  - patch: |-
      - op: replace
        path: /spec/values/replicaCount
        value: 2
    target:
      kind: HelmRelease
      name: recalc-engine-worker
  - patch: |-
      - op: replace
        path: /spec/values/replicaCount
        value: 2
    target:
      kind: HelmRelease
      name: xlsx-export-worker

configMapGenerator:
  - name: pack-residency
    literals:
      - PACK=pack-kr
      - REGION=ap-seoul-1
      - JURISDICTION=kr
      - OCI_OBJECTSTORAGE_ENDPOINT=https://objectstorage.ap-seoul-1.oraclecloud.com
      - AI_FORMULA_PROVIDER_RESIDENCY=kr-resident
      - CDN_EDGE_REGION=ap-seoul-1
      - AUDIT_RETENTION_YEARS=3
