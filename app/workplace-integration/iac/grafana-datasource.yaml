apiVersion: oyatie.dev/v1
kind: GrafanaDatasource
metadata:
  name: workplace-integration-grafana-datasource
  labels:
    service: workplace-integration
    adr: ADR-0320
spec:
  tenantScope: required
  openBaoPath: secret/workplace-integration/grafana-datasource
  naming_justifications:
    bnf_v4: "workplace-integration.iac.grafana-datasource"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
