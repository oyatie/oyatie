apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: oya-crm-default-deny
spec:
  podSelector:
    matchLabels:
      app: oya-crm
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              oya-ingress: "true"
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              oya-substrate: "true"
