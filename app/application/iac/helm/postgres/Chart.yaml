# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: oya-application-postgres
description: Postgres + Citus shell-state DB for Application Shell (sessions ledger, audit, route registry, module manifests) per ADR-0131
type: application
version: 0.1.0
appVersion: "16.0"  # Postgres LTS
maintainers:
  - name: axis-application
dependencies:
  - name: citus
    version: "^12.0.0"
    repository: https://citusdata.github.io/citus
