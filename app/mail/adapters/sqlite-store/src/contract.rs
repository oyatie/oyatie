//! Test doubles for the contract suite: wrappers over SQLite `:memory:`.
//! Compiled into this crate's own tests through the `contract` feature and a
//! self dev-dependency, so the merge verdict runs the gates.
