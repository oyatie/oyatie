apiVersion: openslo/v1
kind: SLO
metadata:
  name: healthcare-integration-hipaa-access-review-latency
  labels:
    service: healthcare-integration
    suite: local-operational-primitive
    adr: ADR-0130
    promotion_gate: ADR-0139
spec:
  service: healthcare-integration
  description: HIPAA access review latency for HL7 ingestion, FHIR exchange, HIPAA access controls, PHI delivery, consent synchronization, and audit completeness operations.
  indicator:
    metadata:
      name: hipaa-access-review-latency
      labels:
        domain_object: ClinicalExchange
        operator_audience: HEALTHCARE_INTEGRATION_ADMIN
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(oya_healthcare_integration_hipaa_access_review_latency_good_total[5m])) by (tenant_id, cell_tier)
        total:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(oya_healthcare_integration_hipaa_access_review_latency_total[5m])) by (tenant_id, cell_tier)
  objectives:
    - displayName: HIPAA access review latency
      target: 0.99
  timeWindow:
    - duration: 30d
      isRolling: true
