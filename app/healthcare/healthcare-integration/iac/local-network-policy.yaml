apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: healthcare-integration-local-domain-egress
  labels:
    service: healthcare-integration
    suite: local-operational-primitive
    adr: ADR-0130
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: hl7-ingestor
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              oyatie.dev/plane: edge
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              oyatie.dev/plane: audit-chain
    - to:
        - namespaceSelector:
            matchLabels:
              oyatie.dev/plane: eventing
