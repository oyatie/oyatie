# app/calendar reorg drain notes

## Ownership (rule 3e)

- **Forever home:** `app/calendar/**`.
- **Writes:** only under `app/calendar/**` on this tip.
- Calendar **engine** crates stay in `comms/` (Interview D32). This rail is the product shell only; no crate move-plan.

## Completed

- Hygiene deepen: retargeted `oya/calendar/` + `microservices/calendar/` path cites → `app/calendar/` in README/manifest (forever-home authority).
- Wave-1 absorb: product scaffold reclaim `oya/calendar/**` → `app/calendar/**` (86 files + REORG-DRAIN).
- Path cites rewritten `oya/calendar` → `app/calendar` inside forever home.
- Judgment: envelopes **1.16.8** closes land/shrink relay after **1.16.7** OVERRULE (`delete_permanently` → `reorg_now`).
- Shrink: leftover `oya/calendar/**` husk deleted after byte-compare; 0 unique files to move, 64 duplicate files dropped (app copies were newer/richer). `app_products.current_dirs` now lists `app/calendar`.

## Out of envelope

- `oya/governance/**` stays `delete_permanently` (META collision) — not this rail.
- Do not absorb notes/slides/sites/translate. Do not touch HR/payroll branches.
