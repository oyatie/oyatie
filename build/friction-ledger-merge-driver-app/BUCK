rust_library(
    name = "friction-ledger-merge-driver-app",
    srcs = glob(["src/**/*.rs"]),
    crate = "friction_ledger_merge_driver_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//ci/facade/canonical-json:ci-canonical-json",
        "third-party//:serde_json",
    ],
)

rust_binary(
    name = "oya-friction-ledger-merge-driver",
    srcs = glob(["src/**/*.rs"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":friction-ledger-merge-driver-app",
    ],
)

rust_test(
    name = "friction-ledger-merge-driver-app-unittest",
    srcs = glob(["src/**/*.rs"]),
    crate = "friction_ledger_merge_driver_app",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//ci/facade/canonical-json:ci-canonical-json",
        "third-party//:serde_json",
    ],
)

# FRIC-1781370000 incident fixtures: each of the three merge-train leader incidents is reproduced
# RED under naive union semantics (cross-validated against the REAL ADR-0544 friction-accounting
# fold + policy) and GREEN under the driver. Plus idempotence, commutativity-modulo-append-order,
# fail-closed-on-garbage, canonical stability, and live-ledger fold-equivalence.
rust_test(
    name = "friction-ledger-merge-driver-app-fixtures",
    srcs = ["tests/merge_fixtures.rs"],
    crate = "friction_ledger_merge_driver_app_fixtures",
    crate_root = "tests/merge_fixtures.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":friction-ledger-merge-driver-app",
        "//ci/facade/action-item-accounting:ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "friction-ledger-merge-driver-app-cli-fixtures",
    srcs = ["tests/cli_fixtures.rs"],
    crate = "friction_ledger_merge_driver_app_cli_fixtures",
    crate_root = "tests/cli_fixtures.rs",
    env = {
        "OYA_FRICTION_LEDGER_MERGE_DRIVER": "$(location :oya-friction-ledger-merge-driver)",
    },
    visibility = ["PUBLIC"],
)
