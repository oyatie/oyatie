[package]
name = "port-engine-rust-ir"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Rust IR and deterministic renderer for the owned deterministic port engine (ADR-0637 D1).
# W0-B Slice 1 skeleton only — TargetIr rendering and syn/quote wiring land in Slice 5.

[dependencies]

[lib]
name = "port_engine_rust_ir"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
