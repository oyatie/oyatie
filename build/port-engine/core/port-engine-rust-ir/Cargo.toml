[package]
name = "port-engine-rust-ir"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Rust IR and deterministic renderer for the owned deterministic port engine (ADR-0637 D1).
# W0-B Slice 3 — empty renderer stub; syn/quote wiring and leakage-forbidden tests land in Slice 5.

[dependencies]
port-engine-api = { path = "../../ports/port-engine-api" }

[lib]
name = "port_engine_rust_ir"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
