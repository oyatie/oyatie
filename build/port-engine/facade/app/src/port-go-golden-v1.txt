// GENERATED by port-engine. Do not edit — regenerate from the rule pack.
pub mod basic {
    // region: oyatie_example_portengine_fixture_corpus_basic__go_alias__id
    /// ID is an alias for the underlying identity spelling.
    pub type ID = String;
    // region: oyatie_example_portengine_fixture_corpus_basic__go_const__defaultname
    /// DefaultName is the fallback identity for an unnamed record.
    pub const DEFAULT_NAME: &str = "anonymous";
    // region: oyatie_example_portengine_fixture_corpus_basic__go_const__maxretries
    /// MaxRetries bounds the retry loop.
    pub const MAX_RETRIES: i64 = 3;
    // region: oyatie_example_portengine_fixture_corpus_basic__go_defined_type__celsius
    /// Celsius is a temperature in degrees Celsius.
    pub struct Celsius(pub f64);
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__add
    /// Add returns the sum of a and b.
    pub fn add(a: i64, b: i64) -> i64 {
        a + b
    }
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__label
    /// Label renders a display name for the given identity.
    pub fn label(id: ID, fallback: String) -> String {
        if id == "" {
            return fallback;
        }
        id
    }
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__scale
    /// Scale multiplies value by factor.
    pub fn scale(value: f64, factor: f64) -> f64 {
        value * factor
    }
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__unexported
    /// unexported is not part of the fixture's public surface.
    fn unexported(n: i64) -> i64 {
        n
    }
}
pub mod geometry {
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_alias__origin
    /// Origin is the zero point, referred to across a package boundary.
    pub type Origin = crate::shapes::Point;
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_func__lookup
    /// Lookup returns a count for a label, falling back when the table has none.
    ///
    /// The map is in the SIGNATURE rather than the body on purpose: indexing is not in the translated
    /// statement subset yet and refuses by name, and this package's job is to prove type resolution
    /// rather than to smuggle an untranslated construct into the green corpus.
    pub fn lookup(table: std::collections::BTreeMap<String, i64>, fallback: i64) -> i64 {
        fallback
    }
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_func__widths
    /// Widths returns the horizontal extents of several rectangles.
    ///
    /// The slice is the point: a composite type resolves by CONSTRUCTOR now, so one `slice` entry in
    /// the pack answers this and every other slice, where a spelling table needed a row per element.
    pub fn widths(counts: Vec<i64>) -> Vec<i64> {
        counts
    }
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_struct__bounds
    /// Bounds is a rectangle described by two points from another package.
    pub struct Bounds {
        /// Min is the lower corner.
        pub min: crate::shapes::Point,
        /// Max is the upper corner.
        pub max: crate::shapes::Point,
    }
}
pub mod naming {
    // region: oyatie_example_portengine_fixture_corpus_naming__go_func__describe
    /// Describe renders anything that can name itself.
    ///
    /// The parameter is the one interface position with an unambiguous target form: the caller keeps
    /// the value and the callee only reads it, so a borrow is right and no ownership question arises.
    pub fn describe(named: &dyn crate::shapes::Named) -> String {
        named.name()
    }
    // region: oyatie_example_portengine_fixture_corpus_naming__go_struct_body__label
    /// Label is a display name that can rename itself.
    pub struct Label {
        /// text is the current display name.
        text: String,
    }
    impl Label {
        pub fn name(&self) -> String {
            self.text.clone()
        }
        pub fn rename(&mut self, next: String) {
            self.text = next;
        }
    }
    /// Ported from an implicit interface: the source was observed satisfying `crate::shapes::Named` at assertion.
    impl crate::shapes::Named for Label {
        fn name(&self) -> String {
            Label::name(self)
        }
        fn rename(&mut self, next: String) {
            Label::rename(self, next);
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_naming__go_struct_body__tag
    /// Tag is a display name that never changes.
    pub struct Tag {
        /// text is the fixed display name.
        text: String,
    }
    impl Tag {
        pub fn name(&self) -> String {
            self.text.clone()
        }
        pub fn rename(&self, next: String) {}
    }
    /// Ported from an implicit interface: the source was observed satisfying `crate::shapes::Named` at assertion.
    impl crate::shapes::Named for Tag {
        fn name(&self) -> String {
            Tag::name(self)
        }
        fn rename(&mut self, next: String) {
            Tag::rename(self, next);
        }
    }
}
pub mod pointers {
    // region: oyatie_example_portengine_fixture_corpus_pointers__go_struct_body__counter
    /// Counter accumulates a total.
    pub struct Counter {
        /// total is the running sum.
        total: i64,
        /// label names the counter.
        label: String,
    }
    impl Counter {
        pub fn add(&mut self, n: i64) {
            self.total = self.total + n;
        }
        pub fn label(&self) -> String {
            self.label.clone()
        }
        pub fn merge(&mut self, other: &Counter) {
            self.total = self.total + other.total;
        }
        pub fn total(&self) -> i64 {
            self.total
        }
    }
}
pub mod shapes {
    // region: oyatie_example_portengine_fixture_corpus_shapes__go_interface__named
    /// Named is anything that can render its own name.
    pub trait Named {
        fn name(&self) -> String;
        fn rename(&mut self, next: String);
    }
    // region: oyatie_example_portengine_fixture_corpus_shapes__go_struct_body__point
    /// Point is a location in two dimensions.
    pub struct Point {
        /// X is the horizontal coordinate.
        pub x: i64,
        /// Y is the vertical coordinate.
        pub y: i64,
        /// label is unexported and must survive extraction as such.
        label: String,
    }
    impl Point {
        pub fn area(&self) -> i64 {
            self.x * self.y
        }
        pub fn shift(&self, dx: i64, dy: i64) -> Point {
            Point {
                x: self.x + dx,
                y: self.y + dy,
                label: String::new(),
            }
        }
    }
}
