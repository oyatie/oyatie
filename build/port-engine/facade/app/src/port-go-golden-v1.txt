// GENERATED by port-engine. Do not edit — regenerate from the rule pack.
pub mod accumulate {
    // region: oyatie_example_portengine_fixture_corpus_accumulate__go_func__mix
    /// Folds the values together with the operators a hash uses.
    pub fn mix(values: &[i64]) -> i64 {
        let mut acc = 1i64;
        for i in 0..values.len() as i64 {
            acc ^= values[i as usize];
            acc = acc.wrapping_mul(3);
            acc |= 1;
        }
        acc
    }
    // region: oyatie_example_portengine_fixture_corpus_accumulate__go_func__total
    /// Sums the values, accumulating in place.
    pub fn total(values: &[i64]) -> i64 {
        let mut sum = 0i64;
        for i in 0..values.len() as i64 {
            sum = sum.wrapping_add(values[i as usize]);
        }
        sum
    }
}
pub mod basic {
    // region: oyatie_example_portengine_fixture_corpus_basic__go_alias__id
    /// An alias for the underlying identity spelling.
    pub type ID = String;
    // region: oyatie_example_portengine_fixture_corpus_basic__go_const__defaultname
    /// The fallback identity for an unnamed record.
    pub const DEFAULT_NAME: &str = "anonymous";
    // region: oyatie_example_portengine_fixture_corpus_basic__go_const__maxretries
    /// Bounds the retry loop.
    pub const MAX_RETRIES: i64 = 3;
    // region: oyatie_example_portengine_fixture_corpus_basic__go_defined_type__celsius
    /// A temperature in degrees Celsius.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Celsius(pub f64);
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__add
    /// Returns the sum of a and b.
    pub fn add(a: i64, b: i64) -> i64 {
        a.wrapping_add(b)
    }
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__scale
    /// Multiplies value by factor.
    pub fn scale(value: f64, factor: f64) -> f64 {
        value * factor
    }
    // region: oyatie_example_portengine_fixture_corpus_basic__go_func__unexported
    /// Returns n unchanged, and is not part of this package's public surface.
    fn unexported(n: i64) -> i64 {
        n
    }
}
pub mod composite {
    // region: oyatie_example_portengine_fixture_corpus_composite__go_func__newdriver
    impl Driver {
        /// Returns a driver labelled for display, driving the given engine.
        pub fn new(engine: Engine, label: &str) -> Self {
            Self {
                engine,
                label: label.to_owned(),
            }
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_composite__go_interface__describer
    /// Anything that can describe itself.
    pub trait Describer {
        /// Renders a description.
        fn describe(&self) -> String;
    }
    // region: oyatie_example_portengine_fixture_corpus_composite__go_interface__job
    /// Anything that runs and describes itself.
    pub trait Job: Runner + Describer {}
    // region: oyatie_example_portengine_fixture_corpus_composite__go_interface__runner
    /// Anything that can run.
    pub trait Runner {
        /// Performs one unit of work and reports the running count.
        fn run(&mut self) -> i64;
    }
    // region: oyatie_example_portengine_fixture_corpus_composite__go_struct_body__driver
    /// Embeds an Engine and gains its methods.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Driver {
        /// Embedded, so Driver's method set includes Run without declaring it.
        pub engine: Engine,
        /// Describes this driver.
        label: String,
    }
    /// Ported from an implicit interface: the source was observed satisfying `Describer` at assertion.
    impl Describer for Driver {
        fn describe(&self) -> String {
            self.label.clone()
        }
    }
    /// Ported from an implicit interface: the source was observed satisfying `Job` at assertion.
    impl Job for Driver {}
    /// Ported from an implicit interface: the source was observed satisfying `Runner` at assertion.
    impl Runner for Driver {
        fn run(&mut self) -> i64 {
            self.engine.run()
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_composite__go_struct_body__engine
    /// Counts the work it has done.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Engine {
        /// How many times Run has been called.
        calls: i64,
    }
    impl Engine {
        /// Performs one unit of work and reports the running count.
        pub fn run(&mut self) -> i64 {
            self.calls = self.calls.wrapping_add(1);
            self.calls
        }
    }
}
pub mod fallible {
    // region: oyatie_example_portengine_fixture_corpus_fallible__go_func__check
    /// Reports whether s is usable, returning only a failure.
    pub fn check(s: &str) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
        if s.is_empty() {
            return Err(Box::<dyn std::error::Error + Send + Sync>::from("empty"));
        }
        Ok(())
    }
    // region: oyatie_example_portengine_fixture_corpus_fallible__go_func__length
    /// Reports the length of s, failing on the empty string.
    pub fn length(s: &str) -> Result<i64, Box<dyn std::error::Error + Send + Sync>> {
        if s.is_empty() {
            return Err(Box::<dyn std::error::Error + Send + Sync>::from("empty"));
        }
        Ok(s.len() as i64)
    }
    // region: oyatie_example_portengine_fixture_corpus_fallible__go_func__twice
    /// Returns the length of s doubled.
    pub fn twice(s: &str) -> Result<i64, Box<dyn std::error::Error + Send + Sync>> {
        let n = length(s)?;
        Ok(n.wrapping_add(n))
    }
    // region: oyatie_example_portengine_fixture_corpus_fallible__go_func__validate
    /// Checks s and reports nothing else.
    pub fn validate(s: &str) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
        check(s)?;
        Ok(())
    }
}
pub mod geometry {
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_alias__origin
    /// Names the point type this package builds rectangles from.
    pub type Origin = crate::shapes::Point;
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_func__size
    /// Reports how many labelled counts the table holds.
    pub fn size(table: &std::collections::BTreeMap<String, i64>) -> i64 {
        table.len() as i64
    }
    // region: oyatie_example_portengine_fixture_corpus_geometry__go_struct__bounds
    /// A rectangle described by its two opposite corners.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Bounds {
        /// The lower corner.
        pub min: crate::shapes::Point,
        /// The upper corner.
        pub max: crate::shapes::Point,
    }
}
pub mod globals {
    // region: oyatie_example_portengine_fixture_corpus_globals__go_func__describe
    /// Returns the name of this package.
    pub fn describe() -> String {
        "globals".to_owned()
    }
}
pub mod handoff {
    // region: oyatie_example_portengine_fixture_corpus_handoff__go_func__bump
    /// Adds one through the pointer, so the parameter is an exclusive borrow and the argument
    /// lends exclusively.
    pub fn bump(c: &mut Counter) {
        c.total = c.total.wrapping_add(1);
    }
    // region: oyatie_example_portengine_fixture_corpus_handoff__go_func__read
    /// Returns the count without writing, so the parameter is a shared borrow.
    pub fn read(c: &Counter) -> i64 {
        c.total
    }
    // region: oyatie_example_portengine_fixture_corpus_handoff__go_func__twice
    /// Bumps the counter twice, handing the same local to a borrowing parameter each time.
    pub fn twice() -> i64 {
        let mut c = Counter { total: 0 };
        bump(&mut c);
        bump(&mut c);
        read(&c)
    }
    // region: oyatie_example_portengine_fixture_corpus_handoff__go_struct_body__counter
    /// Counts.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Counter {
        total: i64,
    }
}
pub mod invariant {
    // region: oyatie_example_portengine_fixture_corpus_invariant__go_func__half
    /// Returns n halved, and aborts on an odd input.
    pub fn half(n: i64) -> i64 {
        if n % 2 != 0 {
            panic!("Half requires an even argument");
        }
        n / 2
    }
}
pub mod naming {
    // region: oyatie_example_portengine_fixture_corpus_naming__go_func__describe
    /// Returns the display name of anything that can name itself.
    pub fn describe(named: &dyn crate::shapes::Named) -> String {
        named.name()
    }
    // region: oyatie_example_portengine_fixture_corpus_naming__go_func__newlabel
    impl Label {
        /// Returns a label whose display name is derived from prefix.
        pub fn new(prefix: &str) -> Self {
            Self {
                prefix: prefix.to_owned(),
                text: prefix.to_owned(),
            }
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_naming__go_func__newtag
    impl Tag {
        /// Returns a tag with the given fixed display name.
        pub fn new(text: &str) -> Self {
            Self { text: text.to_owned() }
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_naming__go_struct_body__label
    /// A display name derived from a prefix.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Label {
        /// The source the display name is derived from.
        prefix: String,
        /// The current display name.
        text: String,
    }
    /// Ported from an implicit interface: the source was observed satisfying `crate::shapes::Named` at assertion.
    impl crate::shapes::Named for Label {
        fn name(&self) -> String {
            self.text.clone()
        }
        fn refresh(&mut self) {
            self.text = self.prefix.clone();
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_naming__go_struct_body__tag
    /// A display name that never changes.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Tag {
        /// The fixed display name.
        text: String,
    }
    /// Ported from an implicit interface: the source was observed satisfying `crate::shapes::Named` at assertion.
    impl crate::shapes::Named for Tag {
        fn name(&self) -> String {
            self.text.clone()
        }
        fn refresh(&mut self) {}
    }
}
pub mod pointers {
    // region: oyatie_example_portengine_fixture_corpus_pointers__go_func__newcounter
    impl Counter {
        /// Returns a counter starting at zero, named for display.
        pub fn new(label: &str) -> Self {
            Self {
                total: 0,
                label: label.to_owned(),
            }
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_pointers__go_func__newtally
    impl Tally {
        /// Returns a tally named for display.
        pub fn new(label: &str) -> Option<Box<Tally>> {
            Some(Box::new(Tally { label: label.to_owned() }))
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_pointers__go_struct_body__counter
    /// Accumulates a total.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Counter {
        /// The running sum.
        total: i64,
        /// Names the counter.
        label: String,
    }
    impl Counter {
        /// Adds n to the counter.
        pub fn add(&mut self, n: i64) {
            self.total = self.total.wrapping_add(n);
        }
        /// Reports the counter's name, by value receiver.
        pub fn label(&self) -> String {
            self.label.clone()
        }
        /// Folds other into the receiver.
        pub fn merge(&mut self, other: &Counter) {
            self.total = self.total.wrapping_add(other.total);
        }
        /// Reports the accumulated value.
        pub fn total(&self) -> i64 {
            self.total
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_pointers__go_struct_body__tally
    /// A counter allocated away from the caller's frame.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Tally {
        /// Names the tally.
        label: String,
    }
}
pub mod scoped {
    // region: oyatie_example_portengine_fixture_corpus_scoped__go_func__span
    /// Reports the width of s, counting a short string as one wider than it is.
    pub fn span(s: &str) -> i64 {
        let mut result = 0;
        {
            let size = s.len() as i64;
            if size > 4 {
                result = size;
            } else {
                result = size.wrapping_add(1);
            }
        }
        result
    }
    // region: oyatie_example_portengine_fixture_corpus_scoped__go_func__width
    /// Reports the width of s once it clears the minimum, and zero otherwise.
    pub fn width(s: &str) -> i64 {
        {
            let size = s.len() as i64;
            if size > 4 {
                return size;
            }
        }
        0
    }
}
pub mod sequence {
    // region: oyatie_example_portengine_fixture_corpus_sequence__go_func__buffer
    /// Returns a zeroed buffer of the fixed width the protocol uses.
    pub fn buffer() -> [i64; 4] {
        [0; 4]
    }
    // region: oyatie_example_portengine_fixture_corpus_sequence__go_func__empty
    /// Returns no widths at all.
    pub fn empty() -> Vec<i64> {
        Vec::new()
    }
    // region: oyatie_example_portengine_fixture_corpus_sequence__go_func__seeds
    /// Returns the fixed seed table.
    pub fn seeds() -> [i64; 3] {
        [11, 13, 17]
    }
    // region: oyatie_example_portengine_fixture_corpus_sequence__go_func__widths
    /// Returns the configured widths.
    pub fn widths() -> Vec<i64> {
        vec![1, 2, 3]
    }
}
pub mod shapes {
    // region: oyatie_example_portengine_fixture_corpus_shapes__go_func__newpoint
    impl Point {
        /// Returns the point at the given coordinates, labelled for display.
        pub fn new(x: i64, y: i64, label: &str) -> Self {
            Self {
                x,
                y,
                label: label.to_owned(),
            }
        }
    }
    // region: oyatie_example_portengine_fixture_corpus_shapes__go_interface__named
    /// Anything that can render its own display name.
    pub trait Named {
        /// Returns the display name.
        fn name(&self) -> String;
        /// Recomputes the display name from the current state.
        fn refresh(&mut self);
    }
    // region: oyatie_example_portengine_fixture_corpus_shapes__go_struct_body__point
    /// A location in two dimensions.
    #[derive(Debug, Clone, Default, PartialEq)]
    pub struct Point {
        /// The horizontal coordinate.
        pub x: i64,
        /// The vertical coordinate.
        pub y: i64,
        /// Names the point for display.
        label: String,
    }
    impl Point {
        /// Reports the area of the rectangle between the origin and this point.
        pub fn area(&self) -> i64 {
            self.x.wrapping_mul(self.y)
        }
        /// Returns the point's display name.
        pub fn label(&self) -> String {
            self.label.clone()
        }
        /// Returns the point moved by the given deltas, keeping its label.
        pub fn shift(&self, dx: i64, dy: i64) -> Point {
            Point {
                x: self.x.wrapping_add(dx),
                y: self.y.wrapping_add(dy),
                label: self.label.clone(),
            }
        }
    }
}
