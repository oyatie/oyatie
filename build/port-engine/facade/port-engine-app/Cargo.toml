[package]
name = "port-engine-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Driver facade for the owned deterministic port engine: plan, render, verify, delta, receipt.
# W0-B Slice 1 skeleton only — CLI wiring and six-axis end-to-end receipts land in Slice 6.

[[bin]]
name = "port-engine-app"
path = "src/main.rs"

[dependencies]

[lib]
name = "port_engine_app"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
