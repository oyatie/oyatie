[package]
name = "port-engine-source-pin"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Upstream pin acquisition and license verification for the owned deterministic port engine.
# W0-B Slice 3 — pin loader present; bootstrap extractor admission lands in later slices.

[dependencies]
serde.workspace = true
serde_json.workspace = true

[lib]
name = "port_engine_source_pin"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
