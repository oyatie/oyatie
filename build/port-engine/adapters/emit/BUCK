rust_library(
    name = "port-engine-emit",
    srcs = glob(["src/**/*.rs", "src/**/*.txt"]),
    crate = "port_engine_emit",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//build/port-engine/adapters/hash:port-engine-hash",
        "//build/port-engine/ports/api:port-engine-api",
    ],
)

# Canary selection, golden compare, and every materialize destination refusal.
rust_test(
    name = "port-engine-emit-canary",
    srcs = glob(["tests/emit.rs", "src/**/*.txt"]),
    crate = "port_engine_emit_canary",
    crate_root = "tests/emit.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":port-engine-emit",
        "//build/port-engine/ports/api:port-engine-api",
    ],
)

# Architecture fence. `srcs` carries the library sources because the fence READS them with
# include_str! — they are inputs to compiling this test, never modules of it.
rust_test(
    name = "port-engine-emit-fences",
    srcs = glob(["tests/fences.rs", "src/**/*.rs"]),
    crate = "port_engine_emit_fences",
    crate_root = "tests/fences.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":port-engine-emit",
    ],
)

rust_test(
    name = "port-engine-emit-readiness",
    srcs = glob(["tests/readiness.rs"]),
    crate = "port_engine_emit_readiness",
    crate_root = "tests/readiness.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":port-engine-emit",
    ],
)
