[package]
name = "port-engine-emit"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Single-fixture canary emit seam (W0-B Slice 13). Neutral mini canary only —
# NEVER bulk k8s/ corpus emission. No Cargo.lock absorb this tip.

[dependencies]
port-engine-api = { path = "../../ports/port-engine-api" }
port-engine-hash = { path = "../port-engine-hash" }

[lib]
name = "port_engine_emit"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
