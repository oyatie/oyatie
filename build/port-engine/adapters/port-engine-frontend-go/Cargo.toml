[package]
name = "port-engine-frontend-go"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Go SourceModel snapshot consumer for the owned deterministic port engine (ADR-0637 D1).
# W0-B Slice 1 skeleton only — snapshot decode and the Go firewall land in Slice 4.

[dependencies]

[lib]
name = "port_engine_frontend_go"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
