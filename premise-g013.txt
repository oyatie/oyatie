CLAIM: The friction ledger (.omc/ultragoal/friction-ledger.jsonl, 68 rows at origin/dev) is an UNGOVERNED surface — no existing cloud-ci gate validates that every friction row terminates in a gate/automation/accepted-risk, so unconverted frictions accumulate as untracked debt; the FRIC-total-accounting meta-gate is net-new.
VERIFIED: (1) grep across cloud/cloud-ci/gates/* + oya-ci.toml + libs/oya-ci-config shows NO gate reads friction-ledger.jsonl (the existing "total-accounting" gate accounts tracked CODE PATHS via the accounting-registry, not friction rows; "automation-ratchet" classifies frictions by enforceable/automatable but does NOT enforce closed-loop termination/evidence). (2) ADR max on disk = ADR-0541; next free = ADR-0544 (ADR-0542 claimed by open PR #644, ADR-0543 by open PR #686 — HARD collision, avoided). (3) origin/dev ledger inventory: 68 rows, 50+ free-text status values (NOT an enum), 10 rows missing `status`, 10 missing `enforcement_fix`, `evidence` field already present in 11 rows — confirms the surface is ungoverned and heterogeneous, so the gate must baseline existing debt and only block NEW schema/disposition violations (constraint 3 ratchet semantics).
INVALIDATES: if a cloud-ci gate already enforces per-friction-row closed-loop accounting + evidence-on-closure against friction-ledger.jsonl (it does not — automation-ratchet is the nearest and stops at classification), OR if the founder closed-loop decision was reversed/superseded after 2026-06-10.
