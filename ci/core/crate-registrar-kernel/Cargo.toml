[package]
name = "crate-registrar-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
bounded_context = "crate-registrar"
kind = "shared"
layer = "kernel"
purpose = "Pure register_crate planner: a RegisterCrateRequest plus a current-state snapshot computes an ordered, typed RegistrationPlan (the born-accounting upsert diff) with no I/O — closed-set capability validation, role→suffix/BUCK mapping, and VERBATIM governed-path enumeration (the resolve_justifications tokenizer match, task #66)."

[lib]
name = "crate_registrar_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]

[lints]
workspace = true
