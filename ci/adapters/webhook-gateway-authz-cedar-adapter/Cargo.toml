[package]
name = "ci-webhook-gateway-authz-cedar-adapter"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Cedar authorization adapter for the CI webhook gateway (ADR-0387 D6).
# Implements the kernel WebhookAuthzGate trait via cedar-policy engine.
# Loads policy from oya/ci-webhook-gateway/policy/ci-webhook-gateway.cedar.
# Deny-by-default + forbid-wins semantics (ADR-0183 inheritance).
# Stage-5 GREEN: Cedar engine wired; dogfood tenant on same path.

[lib]
name = "ci_webhook_gateway_authz_cedar_adapter"
path = "src/lib.rs"
doctest = false

[dependencies]
oya-ci-webhook-gateway-kernel = { path = "../../../oya/ci-webhook-gateway/crates/oya-ci-webhook-gateway-kernel" }
cedar-policy = { workspace = true }

[lints]
workspace = true
