rust_library(
    name = "ci-dependency-graph-acyclicity",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_dependency_graph_acyclicity",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:sha2",
    ],
)

# Gate binary (ADR-0280 §D-3 + ADR-0631). Loads the policy-declared graph-v2 document and exits 0/1/2.
# LOCAL BRIDGE feedback only (founder CLI-retirement directive): merge authority is the gate test
# below + the check-substrates lane, never this binary.
rust_binary(
    name = "oya-cloud-ci-substrate-dependency-dag-acyclicity-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    resources = [
        "//specs:capability-registry.json",
        "//specs:substrate-dependency-dag.json",
        "//specs:substrate-dependency-dag.schema.json",
    ],
    deps = [
        ":ci-dependency-graph-acyclicity",
        "third-party//:serde_json",
    ],
)

# Pure-kernel tests include the max-min failure-impact composition rule and policy boundary.
rust_test(
    name = "ci-dependency-graph-acyclicity-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_dependency_graph_acyclicity",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:sha2",
    ],
)

# ADR-0631 graph-v2 live-corpus and mutation-fixture gate. It proves the exact five-kind set,
# runtime-face endpoints, graph-3-only acyclicity, and exact derived failure closure.
rust_test(
    name = "ci-dependency-graph-acyclicity-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "ci_dependency_graph_acyclicity_gate",
    crate_root = "tests/acyclicity.rs",
    visibility = ["PUBLIC"],
    resources = [
        "//specs:capability-registry.json",
        "//specs:substrate-dependency-dag.json",
        "//specs:substrate-dependency-dag.schema.json",
    ],
    deps = [
        ":ci-dependency-graph-acyclicity",
        "third-party//:serde_json",
    ],
)
