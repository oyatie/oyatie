rust_library(
    name = "ci-crate-reference-integrity",
    srcs = glob(["src/**/*.rs", "*.json"]),
    crate = "ci_crate_reference_integrity",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//libs/oya-workspace-members-kernel:oya-workspace-members-kernel",
        "third-party//:serde_json",
        "third-party//:serde_yaml",
    ],
)

# Pure-kernel unit tests: every violation class is proven from in-memory fixtures with ZERO
# filesystem access, so a broken collector can never present as a passing kernel.
rust_test(
    name = "ci-crate-reference-integrity-unittest",
    srcs = glob(["src/**/*.rs", "*.json"]),
    crate = "ci_crate_reference_integrity",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//libs/oya-workspace-members-kernel:oya-workspace-members-kernel",
        "third-party//:serde_json",
        "third-party//:serde_yaml",
    ],
)

# ADR-0630 cloud-ci-crate-reference-integrity: live-corpus self-test over the real governed
# artifacts (spec object keys, architecture-graph crate labels, vendor seam declarations, runnable
# `-p` commands in markdown, build-graph labels) resolved against the live workspace + lockfile
# census, plus the in-repo RED fixture the gate MUST fail on and the shrink-only baseline fidelity
# check that forbids tolerated debt outliving the defect.
rust_test(
    name = "ci-crate-reference-integrity-gate",
    srcs = glob(["tests/**/*.rs", "*.json", "fixtures/**"]),
    crate = "ci_crate_reference_integrity_gate",
    crate_root = "tests/crate_reference_integrity.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-crate-reference-integrity",
        "//libs/oya-workspace-members-kernel:oya-workspace-members-kernel",
        "third-party//:serde_json",
        "third-party//:serde_yaml",
    ],
)
