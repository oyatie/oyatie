rust_library(
    name = "ci-build-cache-policy",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_build_cache_policy",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:sha2",
    ],
)

# Resolver + canary toolkit binary (ADR-0560). LOCAL BRIDGE feedback only (founder
# CLI-retirement directive): merge authority is the conformance gate test below,
# never this binary. CI workflows invoke it via `buck2 run` as job steps (resolve /
# report / assert-cold / hash-outputs / canary-verdict); its successors are
# reconcilers per ADR-0556 D4 / ADR-0555 D4.
rust_binary(
    name = "oya-cloud-ci-cache-wiring-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-build-cache-policy",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "ci-build-cache-policy-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_build_cache_policy",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:sha2",
    ],
)

# ADR-0560 cache-wiring conformance: live-corpus self-test over the REAL policy +
# license + overlays + live workflows. Proves the dark-wiring guarantee (unlicensed
# => every class bypasses), the ADR-0556 one-way cold floor, the kill-switch, the
# overlay posture, the clean root .buckconfig, the canary workflow's from-empty
# shape, and ADR-0554 D10's ban on archiving runner-local `buck-out` in required CI.
# The slice-1 instalment of the ADR-0556-named cache-policy-conformance successor gate.
rust_test(
    name = "ci-build-cache-policy-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "ci_build_cache_policy_gate",
    crate_root = "tests/cache_conformance.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-build-cache-policy",
        "third-party//:serde_json",
    ],
)
