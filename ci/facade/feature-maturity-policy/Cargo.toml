[package]
name = "ci-feature-maturity-policy"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
bounded_context = "cloud-ci"
kind = "shared"
layer = "app"
purpose = "Cloud CI gate enforcing planned-feature maturity: product PRDs must carry acceptance and verification contracts, capability records must use rich claim-boundary shape, and retired plan paths must be classified as historical provenance with live gate inputs."

[lib]
name = "ci_feature_maturity_policy"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
