rust_library(
    name = "ci-service-tier-metadata",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_service_tier_metadata",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_binary(
    name = "oya-cloud-ci-tier-field-coverage-app-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-service-tier-metadata",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "ci-service-tier-metadata-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_service_tier_metadata",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# Phase-0 capability-first reorg (ADR-0562/0536/0245/0280/0348): the born-blocking self-test over the
# real service manifests under cloud/ + oya/ plus on-disk RED/GREEN fixtures. It collects every
# governed service manifest from the live tree and asserts the corpus is born-blocking green: valid
# tier/tier_subtype/dr_tier metadata, no `tier` type-overload, valid substrate_dag_position,
# ADR-0348 sharding_automation, and OpenSLO references or explicit live exemptions. Also asserts the
# gate policy enums match specs/platform-architecture.json verbatim.
rust_test(
    name = "ci-service-tier-metadata-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "ci_service_tier_metadata_gate",
    crate_root = "tests/tier_field_coverage.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-service-tier-metadata",
        "third-party//:serde_json",
    ],
)
