rust_library(
    name = "ci-supply-chain-audit",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_supply_chain_audit",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//libs/oya-advisory-mirror-kernel:oya-advisory-mirror-kernel",
        "third-party//:semver",
        "third-party//:serde_json",
        "third-party//:toml",
    ],
)

rust_binary(
    name = "oya-cloud-ci-supply-chain-audit-app-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-supply-chain-audit",
        "//libs/oya-advisory-mirror-kernel:oya-advisory-mirror-kernel",
        "third-party//:semver",
        "third-party//:serde_json",
        "third-party//:toml",
    ],
)

rust_test(
    name = "ci-supply-chain-audit-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_supply_chain_audit",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//libs/oya-advisory-mirror-kernel:oya-advisory-mirror-kernel",
        "third-party//:semver",
        "third-party//:serde_json",
        "third-party//:toml",
    ],
)

# Owned RustSec advisory gate live self-test: a pure-Rust born-blocking self-test (synthetic
# quinn-proto 0.11.14 + fixture advisory => SCA-VULN keyed to RUSTSEC-2026-0185; 0.11.15 => clean;
# unmaintained absent from ignore => SCA-UNMAINTAINED, present => clean; tampered manifest =>
# SCA-MIRROR-MALFORMED) PLUS a live_corpus_is_born_green leg over the real Cargo.lock + committed
# advisory mirror (green: quinn fixed + the 3 unmaintained ids in policy.ignore), PLUS the lockfile
# COVERAGE legs: the tree's Cargo.lock corpus is derived by walking it and asserted equal to
# policy.lockfile_paths (live), with synthetic RED proofs for an undeclared lockfile
# (SCA-LOCKFILE-UNCOVERED) and a declared-but-deleted one (SCA-LOCKFILE-ABSENT).
rust_test(
    name = "ci-supply-chain-audit-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "ci_supply_chain_audit_gate",
    crate_root = "tests/supply_chain_audit.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-supply-chain-audit",
        "//libs/oya-advisory-mirror-kernel:oya-advisory-mirror-kernel",
        "third-party//:semver",
        "third-party//:serde_json",
        "third-party//:toml",
    ],
)
