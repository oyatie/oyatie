[package]
name = "ci-parity-claim-evidence"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# cloud-ci-hyperscaler-parity-taxonomy: Rust/Buck2 replacement for the retired
# scripts/tests/cloud_hyperscaler_parity_taxonomy_check.py validator. The gate keeps the
# target taxonomy honest: official source coverage, dogfood-only evidence lanes, required
# category coverage, and no production/parity/readiness overclaims.

[lib]
name = "ci_parity_claim_evidence"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
