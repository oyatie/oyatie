rust_library(
    name = "ci-parity-claim-evidence",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "ci_parity_claim_evidence",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "ci-parity-claim-evidence-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "ci_parity_claim_evidence",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# Live-corpus gate for the cloud hyperscaler parity taxonomy target artifact.
rust_test(
    name = "ci-parity-claim-evidence-gate",
    srcs = glob(["tests/**/*.rs"]),
    crate = "ci_parity_claim_evidence_corpus",
    crate_root = "tests/hyperscaler_parity_taxonomy.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-parity-claim-evidence",
        "third-party//:serde_json",
    ],
)
