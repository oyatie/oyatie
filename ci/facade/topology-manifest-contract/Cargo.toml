[package]
name = "ci-topology-manifest-contract"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# CELL-001R cloud-ci Rust gate for the cell/residency/multi-region topology
# manifest contract. It deliberately does not extend the retired local gate CLI
# CLI authority; protected merge authority remains cloud-ci via oya-ci-required.

[lib]
name = "ci_topology_manifest_contract"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
