{
  "_comment": "Corpus index coverage ratchet (DATA, not code). ADR-0541 Corpus Liveness Graph. A buck2 package that OWNS at least one YAML file is INDEXED iff it declares a corpus-yaml-facts extraction target, which makes that YAML a build-graph input whose facts are a build output. Two ceilings, both shrink-only: baseline_uncovered_packages is the in-graph debt (packages that could be indexed today and are not), and baseline_unpackaged_yaml_files is the NORTHSTAR debt (YAML files belonging to no buck2 package, which cannot be indexed at all until they are pulled INTO the build graph). The second ceiling is deliberately the larger number: indexing an artifact through a side channel would improve a coverage score while leaving it outside the graph, so the gate counts unpackaged files in the DENOMINATOR and never lets a side channel look like progress. The two min_expected_* floors are the anti-vacuity backstop: a walk that silently sees nothing reports zero uncovered files, which reads as PERFECT coverage, so a collapsed census fails the gate closed. ALL repo-specifics are DATA — another repo adopts this gate by repointing these five numbers.",
  "gate_id": "cloud-ci-corpus-index-coverage",
  "schema_version": "1.0.0",
  "baseline_uncovered_packages": 24,
  "baseline_unpackaged_yaml_files": 5003,
  "min_expected_yaml_packages": 20,
  "min_expected_yaml_files": 4000,
  "measured": {
    "_comment": "Measured by the live gate at freeze time, recorded so a later reader can see whether the ratchet moved or the corpus did. yaml_files_total counts the walk's census, which skips dot-directories; a git ls-files census reports 8 more (.github/**) and is not the number the ceilings are frozen against.",
    "yaml_packages_total": 27,
    "yaml_packages_indexed": 3,
    "yaml_files_total": 5866,
    "yaml_files_indexed": 801,
    "yaml_files_unpackaged": 5003,
    "package_coverage_bps": 1111,
    "file_coverage_bps": 1365
  },
  "product_contract": {
    "born_advisory": "Born ADVISORY over today's debt and BLOCKING on regression. 24 YAML-owning packages declare no extraction target and 5003 YAML files sit outside every buck2 package; both are reported as advisory findings and frozen as ceilings. A NEW uncovered package, or NEW YAML outside the build graph, exceeds its ceiling and fails closed.",
    "shrink_only": "Both ceilings are lowered as extraction targets and service-root packages land. The gate emits a corpus_index_stale_ceiling advisory whenever a ceiling sits above the measured number, so slack cannot silently accumulate and absorb the next regression.",
    "pack_shaped": "The Rust kernel hardcodes no oyatie path. Package census, file census, and both ceilings are DATA.",
    "anti_vacuity": "min_expected_yaml_packages and min_expected_yaml_files fail the gate closed when the walk collapses. Without them a broken walk reports zero uncovered packages, which is indistinguishable from full coverage.",
    "known_limitations": "v1 detects an extraction target by the literal rule name in the package's BUCK file rather than by querying the configured build graph, so a target that is declared but unbuildable would still count as indexed. The buildability of every declared target is separately covered by the buck2 lane. Only the YAML artifact class is measured; markdown, JSON, TOML, and Cedar each need their own extractor and their own ceiling."
  },
  "purpose": "Make 'everything in the code graph and the build graph' a measured, shrink-only invariant instead of a slogan, by counting indexed vs total artifacts and letting the number move in only one direction."
}
