{
  "slice_id": "residency-001-fixture-signed-footprint",
  "spec_path": "specs/fixtures/residency/residency-001-signed-footprint.fixture.json",
  "required_fields": [
    "tier",
    "region",
    "regulatory_regimes",
    "compliance_packs",
    "residency_footprint.allowed_regions",
    "signatures.tenant_signature",
    "signatures.oyatie_compliance_signature",
    "claim_boundary.signed_footprint_only"
  ],
  "required_true_fields": [
    "claim_boundary.contract_schema_only",
    "claim_boundary.no_runtime_attestation_service_claim",
    "claim_boundary.no_runtime_cell_placement_claim",
    "claim_boundary.no_cross_border_data_plane_enforcement_claim",
    "claim_boundary.signed_footprint_only"
  ],
  "enum_constraints": [
    {
      "field": "tier",
      "allowed": [
        "multi_region",
        "single_region",
        "sovereign_cell",
        "airgapped_cell"
      ]
    }
  ],
  "field_patterns": [
    {
      "field": "region",
      "pattern": "^[a-z]{2}(_[a-z0-9]+)+$|^global_multi$"
    },
    {
      "field": "signatures.tenant_signature",
      "pattern": "^[A-Za-z0-9_-]{86}$"
    },
    {
      "field": "signatures.oyatie_compliance_signature",
      "pattern": "^[A-Za-z0-9_-]{86}$"
    },
    {
      "field": "residency_footprint.source_registry_hashes.regions_registry_sha256",
      "pattern": "^[0-9a-f]{64}$"
    },
    {
      "field": "residency_footprint.source_registry_hashes.regimes_registry_sha256",
      "pattern": "^[0-9a-f]{64}$"
    },
    {
      "field": "residency_footprint.source_registry_hashes.pack_matrix_sha256",
      "pattern": "^[0-9a-f]{64}$"
    }
  ]
}
