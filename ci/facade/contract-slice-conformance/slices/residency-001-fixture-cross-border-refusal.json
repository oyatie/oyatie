{
  "slice_id": "residency-001-fixture-cross-border-refusal",
  "spec_path": "specs/fixtures/residency/residency-001-cross-border-refusal.fixture.json",
  "required_fields": [
    "event_class",
    "audit_chain_attested",
    "requested_destination_region",
    "requested_destination_cell",
    "refusal_reason_codes"
  ],
  "required_true_fields": [
    "audit_chain_attested"
  ],
  "enum_constraints": [
    {
      "field": "event_class",
      "allowed": [
        "tenancy.residency.cross_border.refused"
      ]
    }
  ],
  "field_patterns": [
    {
      "field": "residency_footprint_hash",
      "pattern": "^[0-9a-f]{64}$"
    },
    {
      "field": "requested_destination_region",
      "pattern": "^[a-z]{2}(_[a-z0-9]+)+$|^global_multi$"
    }
  ]
}
