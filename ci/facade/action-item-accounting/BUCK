rust_library(
    name = "ci-action-item-accounting",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_action_item_accounting",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:sha2",
    ],
)

rust_test(
    name = "ci-action-item-accounting-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_action_item_accounting",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:sha2",
    ],
)

rust_test(
    name = "fixuptask-v2-admission",
    srcs = glob(["tests/fixuptask_v2_admission.rs", "src/**/*.rs", "fixuptask-v2-schema.json"]),
    crate = "ci_fixuptask_v2_admission",
    crate_root = "tests/fixuptask_v2_admission.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "legacy-friction-adapter",
    srcs = glob(["tests/friction_accounting.rs", "src/**/*.rs", "**/*.json"]),
    crate = "ci_legacy_friction_adapter",
    crate_root = "tests/friction_accounting.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)

# ADR-0544 FRIC-total-accounting: Buck2-native, cargo-free live-corpus meta-gate over the friction
# ledger. The test collects the policy-declared ledger, folds the event-sourced append rows, and
# asserts the live corpus is born-blocking green on closure-integrity codes while legacy schema debt
# stays under the reviewed shrink-only ceilings (FRIC-1781112000 anti-laundering).
rust_test(
    name = "ci-action-item-accounting-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "ci_action_item_accounting_gate",
    crate_root = "tests/friction_accounting.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)
