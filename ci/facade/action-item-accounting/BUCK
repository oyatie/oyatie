rust_library(
    name = "ci-action-item-accounting",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_action_item_accounting",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:serde",
        "third-party//:sha2",
    ],
)

rust_test(
    name = "ci-action-item-accounting-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_action_item_accounting",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:serde",
        "third-party//:sha2",
    ],
)

rust_test(
    name = "fixuptask-v2-admission",
    # Exact durable composition: no recursive source glob and no legacy crate.
    srcs = [
        "src/fixuptask_v2.rs",
        "fixuptask-v2-schema.json",
    ],
    crate = "ci_fixuptask_v2",
    crate_root = "src/fixuptask_v2.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
        "third-party//:serde",
        "third-party//:sha2",
    ],
)

# Executes the durable filesystem adapter against the registry and protected facts materialized in
# the CI workspace. The pure target above remains the independently removable kernel boundary.
rust_test(
    name = "fixuptask-v2-materialized-gate",
    srcs = ["tests/fixuptask_v2_admission.rs"],
    crate = "ci_fixuptask_v2_materialized_gate",
    crate_root = "tests/fixuptask_v2_admission.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)

# Regression guard for the target's explicit, independently removable source boundary.
rust_test(
    name = "fixuptask-v2-source-boundary",
    srcs = [
        "tests/fixuptask_v2_source_boundary.rs",
        "BUCK",
        "Cargo.toml",
    ],
    crate = "ci_fixuptask_v2_source_boundary",
    crate_root = "tests/fixuptask_v2_source_boundary.rs",
    visibility = ["PUBLIC"],
)

rust_test(
    name = "legacy-friction-adapter",
    srcs = glob(["tests/friction_accounting.rs", "src/**/*.rs", "**/*.json"]),
    crate = "ci_legacy_friction_adapter",
    crate_root = "tests/friction_accounting.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)

# Conventional gate identity required by the gate-self-conformance admission contract. The
# separately named target above remains explicit workflow evidence for the transitional adapter.
rust_test(
    name = "ci-action-item-accounting-gate",
    srcs = glob(["tests/friction_accounting.rs", "src/**/*.rs", "**/*.json"]),
    crate = "ci_action_item_accounting_gate",
    crate_root = "tests/friction_accounting.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-action-item-accounting",
        "third-party//:serde_json",
    ],
)
