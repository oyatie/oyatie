# ci/ports: webhook-gateway contracts the facade consumes (ADR-0562).
rust_library(
    name = "ci-webhook-gateway-ports",
    srcs = glob(["src/**/*.rs"]),
    crate = "ci_webhook_gateway_ports",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//ci/core/webhook-gateway-kernel:ci-webhook-gateway-kernel",
    ],
)
