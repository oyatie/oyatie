[package]
name = "secrets-kms-operator-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure KMS operator reconcile kernel. No kube-rs, no k8s-openapi, no tokio.

[lib]
name = "secrets_kms_operator_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
serde = { workspace = true }

[lints]
workspace = true
