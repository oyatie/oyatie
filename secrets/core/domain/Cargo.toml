[package]
name = "secrets-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
data-boundary-kernel = { path = "../../../libs/data-boundary-kernel" }
# G001 contract-lock identity types: leases bind to validated Workload
# principals (SPIFFE identities minted by the G05 lane), never free strings.
shared-platform-contracts-kernel = { path = "../../../libs/shared-platform-contracts-kernel" }
zeroize = { version = "1.8", features = ["zeroize_derive"] }

[lib]
name = "secrets_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
