[package]
name = "compute-oci-adapter"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[lib]
name = "compute_oci_adapter"
path = "src/lib.rs"
doctest = false

[dependencies]
compute-domain = { path = "../../core/domain" }

[dev-dependencies]
compute-resource = { path = "../../core/resource" }
data-boundary-kernel = { path = "../../../base/data-boundary-kernel" }
network-residency = { path = "../../../network/core/residency" }

[lints]
workspace = true
