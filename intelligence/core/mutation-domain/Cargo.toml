[package]
name = "intelligence-mutation-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
intelligence-capability-domain = { path = "../capability-domain" }
intelligence-policy-domain = { path = "../policy-domain" }
audit-chain-domain = { path = "../../../audit/core/chain-domain" }


[lib]
name = "intelligence_mutation_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
