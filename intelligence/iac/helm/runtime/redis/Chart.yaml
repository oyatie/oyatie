apiVersion: v2
name: oya-intelligence-runtime-redis
description: Valkey 8.1 (Redis wire-compat) OSS LTS cluster (6 shards × RF=2) for session-state hot tier per policy/runtime-isolation.md + capacity-model.md
type: application
version: 0.1.0
appVersion: "7.4.0"   # Valkey LTS pin per docs/standards
maintainers:
  - name: axis-foundry-runtime
    email: axis-foundry-runtime@oyatie.com
dependencies:
  - name: redis-cluster
    version: "^11.0.0"  # valkey/valkey-cluster chart version pin
    repository: https://charts.bitnami.com/bitnami
