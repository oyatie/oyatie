apiVersion: v1
kind: ConfigMap
metadata:
  name: kubewarden-image-verification-config
  namespace: kubewarden
  labels:
    app.kubernetes.io/name: kubewarden-image-verification-config
    app.kubernetes.io/part-of: iac-app
    oyatie.com/admission-default: kubewarden
  annotations:
    oyatie.com/change-id: CS-CLOUD-IAC-KUBEWARDEN-ADMISSION-POLICY-GATE-001
    oyatie.com/non-claim: local-verification-config-source-no-cosign-execution
data:
  verification-config.yaml: |
    allOf:
      - kind: githubAction
        owner: jason931225
        repo: oyatie
        issuer: https://token.actions.githubusercontent.com
        rekor: https://rekor.sigstore.dev
