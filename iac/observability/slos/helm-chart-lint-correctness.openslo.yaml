# Canonical-base: specs/openslo/canonical-envelope-schema.json + canonical-burn-rate-alert-template.openslo.yaml (SWEEP-I Slice 4 per ADR-0064)
apiVersion: openslo/v1
kind: SLO
metadata:
  name: iac-app-helm-chart-lint-correctness
  displayName: "iac-app: Helm chart lint correctness 100%"
  labels:
    microservice: iac-app
    bounded_context: iac-validator
    sli_class: correctness
    tier: load-bearing-invariant
    plane: control
    owner_team: axis-iac-app
    pack_scope: all
spec:
  service: iac-app
  description: |
    Helm chart lint correctness SLO. A chart is "good" when `helm lint`
    AND the canonical extended-lint suite (security-hardening rules,
    image-pinning, no-default-secret) both pass on every render. Per
    PRD §"Quality Gates" Helm-lint requirement. Target = 100%.
  indicator:
    metadata:
      name: iac-app-helm-lint-pass-ratio
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: Prometheus
            spec:
              query: |
                sum(rate(iac_app_helm_lint_total{outcome="pass"}[5m]))
        total:
          metricSource:
            type: Prometheus
            spec:
              query: |
                sum(rate(iac_app_helm_lint_total[5m]))
  objectives:
    - target: 1.0
      displayName: "100% Helm-lint pass (any miss blocks promotion)"
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  alertPolicies:
    - alertPolicyRef: canonical-burn-rate-multi-window
