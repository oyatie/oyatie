# Relying-party attestation scaffold (RATS/KBS shape).
# Extends ConfidentialPlatform story (SNP/TDX/CCA) — stub verifier only; no hardware quotes.

rust_library(
    name = "os-attestation-relying-party",
    srcs = glob(["src/**/*.rs"]),
    crate = "os_attestation_relying_party",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "os-attestation-relying-party-unittest",
    srcs = glob(["src/**/*.rs"]),
    crate = "os_attestation_relying_party",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "os-attestation-relying-party-scaffold",
    srcs = ["tests/relying_party_scaffold.rs"],
    crate_root = "tests/relying_party_scaffold.rs",
    visibility = ["PUBLIC"],
    deps = [":os-attestation-relying-party"],
)
