[package]
name = "os-attestation-relying-party"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Relying-party attestation scaffold (SNP/TDX/CCA) extending ConfidentialPlatform — no fake hardware claims."

[lib]
name = "os_attestation_relying_party"
path = "src/lib.rs"
doctest = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
