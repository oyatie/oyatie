# Differential-oracle harness for the owned OCI executor (scaffold).
# youki / runc / crun are ORACLES only — never shipped product (conformance-laundering ban).

rust_library(
    name = "os-oci-executor-oracle",
    srcs = glob(["src/**/*.rs", "fixtures/**/*.json"]),
    crate = "os_oci_executor_oracle",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "os-oci-executor-oracle-unittest",
    srcs = glob(["src/**/*.rs", "fixtures/**/*.json"]),
    crate = "os_oci_executor_oracle",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "os-oci-executor-oracle-scaffold",
    srcs = ["tests/oracle_scaffold.rs"],
    crate_root = "tests/oracle_scaffold.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":os-oci-executor-oracle",
        "third-party//:serde_json",
    ],
)
