[package]
name = "os-kernel-abi"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Kernel-ABI port for os/: the operations os/ crates require from a kernel, expressed as traits so the kernel substrate is swappable."

[lib]
name = "os_kernel_abi"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../../core/kernel" }

[lints]
workspace = true
