[package]
name = "os-meta-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Port of the Talos META partition: ADV key/value store, persisted machine-state keys, and partition layout. Mirrors internal/pkg/meta + pkg/machinery/meta."

[lib]
name = "os_meta_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
