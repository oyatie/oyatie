[package]
name = "os-machine-config-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Machine configuration schema, multi-document container, versioned loading/decoding, validation, patching, and provider accessors for the operating-system Talos migration. Mirrors pkg/machinery/config."

[lib]
name = "os_machine_config_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
