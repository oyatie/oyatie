[package]
name = "os-siderolink-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "SideroLink WireGuard management-plane link for the operating-system Talos port."

[lib]
name = "os_siderolink_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
