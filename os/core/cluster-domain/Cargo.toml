[package]
name = "os-cluster-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "no_std port of the Talos cluster discovery, membership, and KubeSpan/SideroLink subsystem."

[lib]
name = "os_cluster_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
