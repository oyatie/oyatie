[package]
name = "os-archiver-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Filesystem archive/extract streaming for the operating-system Talos port."

[lib]
name = "os_archiver_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
