[package]
name = "os-controllers-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Generic COSI controller runtime and reconcile traits for the operating-system Talos port."

[lib]
name = "os_controllers_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-cosi-domain = { path = "../cosi-domain" }
os-kernel = { path = "../kernel" }
os-machine-config-domain = { path = "../machine-config-domain" }
os-network-domain = { path = "../network-domain" }

[lints]
workspace = true
