[package]
name = "os-extensions-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "System extension manifest parsing and lifecycle for the operating-system Talos port."

[lib]
name = "os_extensions_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
