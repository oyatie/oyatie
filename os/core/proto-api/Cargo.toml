[package]
name = "os-proto-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Talos gRPC API message and service definitions for the operating-system Talos port."

[lib]
name = "os_proto_api"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
