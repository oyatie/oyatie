[package]
name = "os-perf-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "System performance and resource-usage statistics for the operating-system Talos port."

[lib]
name = "os_perf_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
