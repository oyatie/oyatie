[package]
name = "os-config-docs-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Machine configuration documentation and example generation for the operating-system Talos port."

[lib]
name = "os_config_docs_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
