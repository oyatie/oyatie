[package]
name = "os-install-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "no_std port of the Talos installer and bootloader subsystem (GRUB, systemd-boot, UKI, A/B boot entries, image extraction, upgrades)."

[lib]
name = "os_install_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
