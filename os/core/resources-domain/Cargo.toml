[package]
name = "os-resources-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "COSI resource type definitions and registry for the operating-system Talos port."

[lib]
name = "os_resources_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
