[package]
name = "os-maintenance-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Maintenance-mode service and config apply flow for the operating-system Talos port."

[lib]
name = "os_maintenance_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
