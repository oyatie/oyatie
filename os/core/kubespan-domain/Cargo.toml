[package]
name = "os-kubespan-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "KubeSpan WireGuard mesh peer management for the operating-system Talos port."

[lib]
name = "os_kubespan_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
