[package]
name = "os-kubelet-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Kubelet configuration, lifecycle, and node registration for the operating-system Talos port."

[lib]
name = "os_kubelet_domain"
path = "src/lib.rs"
doctest = false

[dependencies]
os-kernel = { path = "../kernel" }

[lints]
workspace = true
