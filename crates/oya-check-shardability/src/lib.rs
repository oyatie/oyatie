//! `oya-check-shardability` — M02 implementation pending.
//!
//! Verifies that all DB designs declare `tenant_id` as partition key
//! and enable Row-Level Security (RLS) per ADR-0062.
//!
//! Running in `--report-only` mode until M02 substrate phase completes.
