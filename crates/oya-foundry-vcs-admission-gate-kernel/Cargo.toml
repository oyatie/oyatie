[package]
name = "oya-foundry-vcs-admission-gate-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
vertical = "foundry"
bounded_context = "vcs-admission-gate"
kind = "shared"
layer = "kernel"
purpose = "I/O-free Oya VCS admission invariant checks: root-hub pointer shape, master-plan sequencing contract, multispectrum agentic-flow wiring, gitops-vcs admission gates + provider evidence/execution shape, branch-protection + workflow surface, cargo metadata coverage, and audit-chain coverage of multispectrum evidence."
lifecycle.stage = "scaffolded"

[lib]
name = "oya_foundry_vcs_admission_gate_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
