[package]
name = "oya-audit-chain-sealing-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Wave 15-IMPL-truth-up scaffold (2026-05-21):
# Merkle math home per IP-007. Wraps oya-audit-chain-domain::merkle_tree until
# the full extraction lands.

[dependencies]
oya-audit-chain-domain = { path = "../oya-audit-chain-domain" }
oya-audit-chain-sealing-kernel = { path = "../oya-audit-chain-sealing-kernel" }

[lib]
name = "oya_audit_chain_sealing_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
