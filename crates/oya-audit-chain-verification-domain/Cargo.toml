[package]
name = "oya-audit-chain-verification-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Wave 15-IMPL-truth-up scaffold (2026-05-21):
# Pure proof + signature verification per IP-011. Reuses
# oya-audit-chain-domain Ed25519 + Merkle behavior.

[dependencies]
oya-audit-chain-domain = { path = "../oya-audit-chain-domain" }
oya-audit-chain-verification-api = { path = "../oya-audit-chain-verification-api" }

[lib]
name = "oya_audit_chain_verification_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
