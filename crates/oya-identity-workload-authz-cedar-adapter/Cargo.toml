[package]
name = "oya-identity-workload-authz-cedar-adapter"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Cedar authorization gate for workload identities: evaluates PARC requests over the workload-identity domain with deny-by-default + forbid-wins semantics. Real cedar-policy (Apache-2.0) swap seam documented. Task T3."

[dependencies]
oya-identity-workload-domain = { path = "../oya-identity-workload-domain" }

[lib]
name = "oya_identity_workload_authz_cedar_adapter"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
