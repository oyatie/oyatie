[package]
name = "oya-shared-connector-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[lib]
name = "oya_shared_connector_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
# Kernel is pure — no I/O, no network, no codegen deps.
# Layer 1 (kernel) per ADR-0148 layered architecture discipline.

[lints]
workspace = true
