//! Payments payout-BC usecase — `SchedulePayoutUseCase`,
//! `InitiatePayoutUseCase`.
//!
//! Wave 15-IMPL-truth-up scaffold; full Cedar + cooling-period + step-up
//! auth + PSP routing pipeline in IP-008.
#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic))]

#[allow(dead_code)]
pub struct SchedulePayoutUseCase {
    _placeholder: (),
}

#[allow(dead_code)]
pub struct InitiatePayoutUseCase {
    _placeholder: (),
}
