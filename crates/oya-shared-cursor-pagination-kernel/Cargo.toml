[package]
name = "oya-shared-cursor-pagination-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Tier-A hyperscaler pattern (AWS NextToken + Stripe starting_after).
# Authored 2026-05-18 per ADR-0150. The trait surface every µservice
# integrates to expose opaque cursor pagination on list endpoints.

[dependencies]

[lib]
name = "oya_shared_cursor_pagination_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
