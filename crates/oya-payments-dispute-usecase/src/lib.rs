//! Payments dispute-BC usecase — `HandleDisputeUseCase`,
//! `SubmitEvidenceUseCase`, `DraftRepresentmentUseCase`.
//!
//! Wave 15-IMPL-truth-up scaffold; full Cedar + PSP webhook parse +
//! Intelligence network-opt-in draft pipeline in IP-010.
#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic))]

#[allow(dead_code)]
pub struct HandleDisputeUseCase {
    _placeholder: (),
}

#[allow(dead_code)]
pub struct SubmitEvidenceUseCase {
    _placeholder: (),
}

#[allow(dead_code)]
pub struct DraftRepresentmentUseCase {
    _placeholder: (),
}
