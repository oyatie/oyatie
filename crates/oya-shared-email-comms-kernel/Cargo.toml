[package]
name = "oya-shared-email-comms-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# ADR-0201 transactional email substrate. Trait surface + four real
# adapter shapes (SES, Postal, Mailgun, SMTP) — no Noop fallback.
# Real provider SDK integrations are wired by µservices behind
# feature flags; the default kernel build is dep-free so the trait
# is callable from any layer.

[dependencies]

[lib]
name = "oya_shared_email_comms_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
