[package]
name = "oya-shared-semver-check-cli"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
vertical = "shared"
bounded_context = "semver-check"
kind = "shared"
layer = "cli"
purpose = "LEAN-A4: wraps cargo-semver-checks with rename-baseline-reset classifier; pure-name deltas classified BASELINE-RESET."

[[bin]]
name = "oya-shared-semver-check-cli"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }

[lints]
workspace = true
