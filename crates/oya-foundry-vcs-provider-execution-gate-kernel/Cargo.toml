[package]
name = "oya-foundry-vcs-provider-execution-gate-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
vertical = "foundry"
bounded_context = "vcs-provider-execution-gate"
kind = "shared"
layer = "kernel"
purpose = "I/O-free credential-safe Oya VCS provider-execution invariant checks: validates the Argo desired-state Application manifest shape (apiVersion, kind, spec.source.repoURL/path, spec.destination.server, spec.syncPolicy.automated.prune+selfHeal) and builds the deterministic provider-execution evidence record consumed by the admission gate."
lifecycle.stage = "scaffolded"

[lib]
name = "oya_foundry_vcs_provider_execution_gate_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
