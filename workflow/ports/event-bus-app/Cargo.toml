[package]
name = "workflow-event-bus-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
workflow-event-bus-sdk = { path = "../event-bus-sdk" }
workflow-event-bus-worker = { path = "../event-bus-worker" }

[lib]
name = "workflow_event_bus_app"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
