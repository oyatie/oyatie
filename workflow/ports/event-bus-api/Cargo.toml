[package]
name = "workflow-event-bus-api"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Source-level workflow-engine event-bus API boundary foundation for preview-only tenant workload integration."

[dependencies]
workflow-event-bus-usecase = { path = "../../core/event-bus-usecase" }
