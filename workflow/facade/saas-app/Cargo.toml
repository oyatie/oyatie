[package]
name = "workflow-saas-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
workflow-saas-kernel = { path = "../saas-kernel" }
workflow-saas-domain = { path = "../saas-domain" }

[lib]
name = "workflow_saas_app"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
