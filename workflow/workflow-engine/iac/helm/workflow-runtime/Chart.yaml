# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: oya-workflow-engine-runtime
description: workflow-engine runtime — engine workers + REST + outbox-relay + spec-store + replay-debugger-backend deployments per ADR-0131
type: application
version: 0.1.0
appVersion: "0.1.0"  # bumped per workflow-engine µservice version (semver)
maintainers:
  - name: axis-workflow
    email: axis-workflow@oyatie.com
