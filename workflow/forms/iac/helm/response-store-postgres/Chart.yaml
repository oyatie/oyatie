# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: oya-forms-response-store-postgres
description: |
  Forms response-store Postgres + Citus 12.1 LTS per ADR-FORMS-0003.
  tenant_id shard key + RLS + column-level envelope encryption with per-tenant DEK
  rooted in pack-resident OpenBao.
type: application
version: 0.1.0
appVersion: "1.0.0"
maintainers:
  - name: axis-forms + ops-security + cloud-iac
    email: axis-forms@oyatie.com
