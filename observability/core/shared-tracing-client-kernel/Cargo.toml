[package]
name = "shared-tracing-client-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Skeleton authored 2026-05-18 per ADR-0145 Invariant 2.
# Each µservice integrates this trait to propagate OpenTelemetry trace
# context on every inter-µservice call. Full impl tracked under
# registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-tracing-client-impl.

[dependencies]

[lib]
name = "shared_tracing_client_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
