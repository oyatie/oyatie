[package]
name = "observability-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
observability-aggregate = { path = "../aggregate" }
audit-chain-domain = { path = "../../../audit/core/chain-domain" }

network-residency = { path = "../../../network/core/residency" }

[lints]
workspace = true
