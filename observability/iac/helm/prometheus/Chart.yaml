# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: observability-prometheus
description: Prometheus 3.x query path embedded by Mimir per ADR-0139 Layer-A; standalone Prometheus optional for ad-hoc per-cluster monitoring
type: application
version: 0.1.0
appVersion: "3.2.0"  # Prometheus LTS line per docs/standards/observability-slo.md
maintainers:
  - name: axis-observability
dependencies:
  - name: prometheus
    version: "^27.0.0"
    repository: https://prometheus-community.github.io/helm-charts
