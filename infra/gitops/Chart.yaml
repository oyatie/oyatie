apiVersion: v2
name: oya-platform
description: >-
  App-of-apps Helm chart — historical deployment-controller pattern for a cell's
  platform stack. Helm-first delivery remains as a pattern reference while
  native release-conveyor/control-plane seams replace retired external CD
  interim authority.
type: application
version: 0.1.0
appVersion: "0.1.0"
