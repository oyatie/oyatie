# ADR-0381 D3 — CI cell ingress boundary.
#
# Restricts ingress to pods in oya-ci (BuildKit daemon + ci-webhook-gateway +
# any future CI specialty workload):
#   - same-namespace pod-to-pod (BuildKit Job consumes buildkitd daemon).
#   - oya-ci-jenkins (Jenkins agents that drive builds + status posts).
#   - oya-forge (GitHub webhook delivery to ci-webhook-gateway).
#   - argocd (sync + drift detection).
#
# Anything else is denied. Egress unconstrained (the CI cell needs outbound
# CoreDNS + outbound to oya-storage bucket-api + outbound to oya-registry +
# outbound to oya-forge for status posts — restricting egress too aggressively
# breaks the loop).
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: oya-ci-ingress
  namespace: oya-ci
  labels:
    oya.adr/decided-by: ADR-0381-D3
    oya.cell-boundary/scope: "ingress-only"
    oya.cell-boundary/target-cell: "ci"
spec:
  podSelector: {}
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector: {}
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: oya-ci-jenkins
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: oya-forge
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: argocd
