load("//governance/corpus/extract:yaml_facts.bzl", "corpus_yaml_facts_shards")

# Corpus graph extraction (ADR-0541). Package root + non-subpackage YAML
# (nested packages such as local/patches keep their own BUCK ownership).
corpus_yaml_facts_shards(
    srcs = glob(["**/*.yaml", "**/*.yml"]),
    shard_size = 256,
)
