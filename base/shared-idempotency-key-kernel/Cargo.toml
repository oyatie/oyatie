[package]
name = "shared-idempotency-key-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Tier-A hyperscaler pattern (Stripe Idempotency-Key + AWS ClientToken).
# Authored 2026-05-18 per ADR-0149. The trait surface every µservice
# integrates to enforce duplicate-suppression on POST/PUT/PATCH/DELETE.

[dependencies]

[lib]
name = "shared_idempotency_key_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
