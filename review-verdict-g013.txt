PRE-OPEN ADVERSARIAL REVIEW FILTER VERDICT (Torvalds rubric, fresh-context fable subagent)
Lane: agent/g013-fric-accounting-gate — FRIC-total-accounting meta-gate (ADR-0544)

HEAD SHA (PR head): ed6e2f0d732b535cc27f1d56b5ee40c98afb319c
Re-review validated SHA: b4147cbeb2dacb8f1ae7df8e18696169d6845117
  (ed6e2f0d7 = b4147cbeb + a comment reword in src/lib.rs and an evidence-count string
   update in the multispectrum file; no behavior change, so the APPROVE carries forward.)

VERDICT: APPROVE

Round 1 (HEAD cc9e61293): VERDICT BLOCK.
  - CRITICAL: a friction logged ONLY as update-shaped rows (no anchoring primary) folded to a clean
    terminal-with-evidence state and evaded every check (silently unaccounted — the exact failure the
    gate exists to close). FIXED: added friction_orphan_update_row (primary_count==0), emitted as the
    sole finding for that id; 3 pre-existing orphans baselined shrink-only; friction_no_disposition
    is now born-blocking-clean. Unit + RED fixtures added.
  - MEDIUM (tautology test): replaced red_baseline_is_shrink_only with a test that drives
    evaluate_keyed and proves a NEW closed-without-evidence key breaks baseline set-equality. FIXED.
  - MEDIUM (overstated anti-laundering): corrected ADR/baseline/policy to "review-visible, not
    structural"; named FRIC-1781112000 merge-base meta-check as the follow-up. FIXED (doc).
  - MEDIUM (prefix-match weakening unknown-status; SRE owner-half + aging not enforced; undeclared
    buck2 input): documented as explicit trade-offs / named follow-ups in ADR-0544. FIXED (doc).
  - LOW (VIOLATION_CODES drift; born-pack row-schema caveat; audit-chain canonicalization): added a
    codes-coverage invariant test; clarified the row schema is the engine contract; rewrote the
    appended audit-chain line in compact JSON. FIXED.

Round 2 (HEAD b4147cbeb): VERDICT APPROVE.
  All five mandated fixes verified in code AND by execution; no new CRITICAL/HIGH introduced.
  Remaining MEDIUM/LOW are honestly documented with named follow-ups:
    - undeclared buck2 input (warm-cache staleness; merge authority unaffected — oya-ci-required
      runs cargo on a fresh runner). ADR-0544 known gap; follow-up named.
    - anti-laundering review-visible not structural (FRIC-1781112000 follow-up). Documented.
    - taxonomy is the unguarded pressure valve (same-PR edit can reclassify). Documented; follow-up.
    - LOW: states-ordering comment wording; orphan `continue` leaves frozen-debt ids' later updates
      unpoliced (bounded to already-frozen ids); stale evidence counts. Comment + counts FIXED in
      ed6e2f0d7; the bounded-frozen-debt item is inherent and noted.

VERIFICATION (binding buck2 signal, run from the lane worktree):
  buck2 build //cloud/cloud-ci/gates/oya-cloud-ci-friction-accounting-app/... -> BUILD SUCCEEDED
  buck2 test  //cloud/cloud-ci/gates/oya-cloud-ci-friction-accounting-app/... -> Pass 2
     unittest: 16 passed, 0 failed; gate (live-repo + RED): 10 passed, 0 failed
  buck2 test  //cloud/cloud-ci/... -> Pass 38, Fail 0
     (incl. registry-drift committed_faces_equal_regenerated + committed_scm_facts_equal_regenerated)

RESIDUAL RISK (from the reviewer): the gate's most likely production failure is silent staleness, not
evasion — the friction ledger is an undeclared buck2 input that changes on nearly every lane, so the
only fresh-read enforcement is the oya-ci-required cargo matrix leg; if that matrix is ever migrated
to buck2 without first landing the declared-input fix, the gate could go green-but-stale on the exact
input it polices. Tracked as an ADR-0544 known gap + named follow-up.
