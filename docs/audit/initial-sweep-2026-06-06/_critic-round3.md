# CRITIC REVIEW — AMENDMENT-PLAN round 3 (against Architect round-3)

> Reviewer: critic lane, separate from the authoring/planner lane and from the round-3 architect lane. Verdict basis: re-read the full round-3 AMENDMENT-PLAN end-to-end; re-read the round-3 Architect review (`_arch-round3.md`); re-read the round-2 Critic (`_critic-round2.md`) for the open re-review trigger; **and independently re-derived BOTH of the architect's new findings by grepping + line-reading the SSOT (`synthesis/decision-record-oyatie-canon.md`) myself**, not from the architect's self-report. Founder rule honored: every claim below cites a file+line I read or a grep I ran this round; no phantom findings; I certify no RULED/DERIVED tag I did not personally check against the SSOT.

**Bottom line up front: the architect's round-3 verdict (APPROVE-WITH-CONDITIONS) is honest and its two new findings are TRUE and independently re-derived — but as a CRITIC enforcing fair-alternatives + principle-option-consistency + verification adequacy, the plan as it currently stands FAILS two bars at the SAME directional blind spot round-2 fixed in the opposite direction. Finding 1 (RULED-deflation of L2-in-the-D11-sweep) is not a refinement: it means the §F founder sign-off currently presents Option 2 as a clean choice when Option 2 as literally worded collides with a RULED D11 batch-door, and the §A.6 provenance pass — the plan's own gating honesty mechanism — is structurally incapable of catching it. That is a fair-alternatives FAIL and a verification-completeness FAIL on the irreversible 831-file lane, the same severity class round-2 rated HIGH/gating. Finding 2 (consolidation-set knowability paradox) is a door:one-way-over-an-unfinalized-artifact defect that the plan asserts away. Both are localized, both are patchable by the architect's synthesis #1/#2, the wave order does not change — so this lands at ITERATE, not REJECT, and emphatically not APPROVE.**

---

## 1. INDEPENDENT RE-VERIFICATION (founder rule: trust no prior verdict — not the planner's, not the architect's)

I re-ran every load-bearing check the architect's round-3 findings rest on, against the SSOT directly:

| Claim under test | My grep/read this round | Verdict |
|---|---|---|
| `grep double-work` over SSOT | **ZERO** (re-confirmed) | round-2 relabel still correct; D-1 not SSOT-ruled |
| D11 `:52` content (d) names the foundry rename | **CONFIRMED verbatim**: `:52` "(d) foundry→cloud-intelligence/governance bulk-rename (0347 Proposed→Accept; fix 0363's false 'eradicated' claim)" inside "**authorize the FULL sweep (runs before masterplan backfill …) Door: one-way (batch)**" | **Finding 1 GROUNDED — the foundry rename IS a named content of the D11 batched one-way sweep** |
| D13 `:43` no-id-reuse + no-dangling invariant | confirmed verbatim | map-before-sweep RULED still grounded |
| census 831/43 at `:107` | confirmed verbatim | correct |
| D3 `:114-118` gives a *file-level* fold map | **FALSE** — `:114-118` give cluster resolution ("supersedes 0124", "phases 0369/0367/0366", "supersede/relate 0511", "seed ONE clean ratifying ADR (ADR-0000+ series)"); the file-level fold map is the **output** of authoring that ADR, which is Wave-1 work | **Finding 2 GROUNDED — the SSOT pins fold INTENT, not a frozen file map** |
| Plan already knows L2 ∈ D11 sweep | **CONFIRMED, plan line 125**: "Rulings: CC-1, **D11 (d) (foundry bulk-rename, a sweep content)**, WF2 mechanical sweep" | **the smoking gun: the plan has the fact and still draws the DERIVED conclusion** |

**Both architect findings are TRUE and I re-derived them independently. The architect honored the founder rule: no phantom findings, every claim resolves to a real SSOT line or plan line.** I push the round-2 open re-review trigger to closure below (§2), then enforce the two new findings as a critic (§3–§5).

---

## 2. ROUND-2 OPEN RE-REVIEW TRIGGER — closed, with one consequence the architect drew correctly

Round-2 critic §8 set three round-3 confirmation conditions. My verdict on each:

1. **"Fabricated quote deleted."** ✅ CONFIRMED. `grep double-work AMENDMENT-PLAN.md` → the string appears only in *meta-commentary about its own deletion* (round-2 revision note line 12, D-1 provenance line 39, §A.5 line 87, Failure-4 line 288) — never as a quoted SSOT ruling. The fabricated line-53 quotation is gone. The plan now states the opposite (the note does not exist) and treats `grep double-work = 0` as a standing exit check (§H.2 A.6 row, line 301).
2. **"Relabel is surgical (only D-1 → DERIVED; D-2 stays RULED)."** ✅ CONFIRMED for the direction round-2 looked. §A.5 (lines 80-87) keeps map-before-sweep `[RULED: D13:43]`, sweep-before-backfill `[RULED: D11:52]`, census `[RULED: :107]`, D14 drops `[RULED]`, D-EVENT `[RULED]`; only D-1 is `[DERIVED]`. The relabel did not collateralize D-2.
3. **"Provenance pass surfaced no further false-RULED tags."** ⚠️ **This is where round-3 breaks.** The §A.6 pass is RULED-*inflation*-only (lines 93-98: "for every `[RULED]` tag, confirm a citable SSOT line; downgrade if none"). It found no *false-positive* RULED — correct, there are none left in that direction. **But the architect found a false-NEGATIVE: a genuinely-RULED constraint the plan tagged DERIVED.** Round-2's trigger asked round-3 to confirm "no further false-RULED tags"; the honest answer is **the wrong-direction question was asked.** The trigger policed inflation; the residual defect is deflation. Round-2 critic §8 also said: *"It would become REJECT if a re-review found the provenance pass surfaced additional false-RULED tags."* Finding 1 is the mirror image — a RULED tag *missing*, not a DERIVED tag *inflated* — so it does not trip the round-2 REJECT clause verbatim, but it is the **same defect class, equal severity, opposite sign.** ITERATE is therefore the calibrated outcome; the round-2 REJECT clause is not literally tripped.

**So the round-2 trigger is closed (1 + 2 pass; 3 passes only for the question it asked) — and closing it surfaced exactly the new finding the architect escalated.** This is consistent, not contradictory: round-2's machinery did its one job and exposed its own directional limit.

---

## 3. FINDING 1 (RULED-deflation, L2-in-the-D11-sweep) — enforced as a FAIR-ALTERNATIVES + VERIFICATION-COMPLETENESS FAIL [HIGH, gating]

The architect's §1/§5#1 finding, re-derived and enforced against my mandate:

**The fact (verified, plan line 125 + SSOT line 52):** the foundry bulk-rename is named content (d) of the D11 "FULL sweep," which D11 rules "runs before masterplan backfill" under a "Door: one-way (batch)." Therefore **"the foundry rename runs inside the pre-backfill sweep" is RULED by D11(d)+:52** — the very same line the plan already cites as `[RULED]` for L3 (line 80, line 133, line 189). The plan cites D11:52 as RULED for L3's integrity sweep but, for L2 — the *same sweep, same line, same batch door* — re-derives Wave-0 placement through the weaker DERIVED D-1 driver and **omits D11 from the line-189 "Hard ordering constraints `[RULED]`" set entirely.**

**Why this fails my bars (not the architect's wording, my enforcement):**

- **FAIR-ALTERNATIVES — FAIL.** §A.3 / §E / §F now present Option 2 ("Refound-first, mechanical cleanup second") as a *clean, foreclosure-free founder choice* (line 56: "a legitimate founder choice … NOT excluded by fiat"; line 226 / §F: "the founder is asked to actually choose Option 1 … vs Option 2"). But Option 2 **as literally worded** — "then run the foundry rename + integrity sweep across the new series" (line 53) — runs the foundry bulk-rename *after* the ADR-0000+ re-foundation, and re-foundation feeds masterplan backfill. **D11 puts the bulk-rename in the sweep that runs before backfill.** So Option-2-as-worded mildly violates the D11 batched-sweep-before-backfill door. Round-2 fixed an *unfair exclusion* of Option 2 (killed by a fabricated ruling); round-3 reveals the over-correction created an *unfair inclusion* of Option 2 (presented as fully legal when a RULED constraint partially binds it). **Both are fair-alternatives failures — the founder is being shown a dishonest option menu in round-3, just inverted from round-2.** This is the precise enforcement target of my mandate.
- **VERIFICATION-COMPLETENESS — FAIL (PV-1).** The §A.6 pass (lines 93-98) is the plan's own gating honesty mechanism, and it is **one-directional by construction**: it can only ever *remove* a RULED tag, never *add* a missing one. Run as written against the round-3 plan, it would **PASS this plan while Finding 1 stands** — it never asks "is any SSOT-ruled action carrying a DERIVED/untagged label?" A verification pass that structurally cannot detect the defect on the irreversible 831-file lane is not a complete verification of the orderings; it is a complete verification of their *non-inflation*. The plan claims §A.6 makes the orderings "honest" (line 215, line 234); it actually makes them *non-over-claimed*. Different claims — and the gap sits on L2.
- **PRINCIPLE-OPTION-CONSISTENCY — held, with a caveat.** §A.1 principle-6 (provenance discipline) is itself sound; the failure is that the plan *applied* it one-directionally. The architect's synthesis #1 (make §A.6 bidirectional + re-tag L2's sweep-membership `[RULED: D11(d)+:52]`, keep rename-before-reauthor *internal sequencing* `[DERIVED: D-1]`) is the correct, surgical fix and I endorse it verbatim. The key distinction the founder actually needs: **the rename happens inside the pre-backfill sweep (RULED); whether it lands before or interleaved-with the re-authoring of the same files is the DERIVED choice.** Option 2's *real* legal shape is "author the ADR-0000+ shells first, then run the still-pre-backfill sweep over them" — NOT "rename after re-foundation." That honest Option-2 must be what the founder signs.

**Severity: HIGH, gating before §A.6 is declared complete and before any §F sign-off.** Rationale: it changes *what the founder signs* (the legality envelope of Option 2), it sits on the irreversible lane, and the plan's own gating mechanism cannot self-catch it. This is the same gating bar round-2's FLAG-A cleared.

---

## 4. FINDING 2 (consolidation-set knowability paradox) — enforced as a DOOR:ONE-WAY-OVER-UNFINALIZED-ARTIFACT FAIL [HIGH, gating]

The architect's §2 / PV-2, re-derived (SSOT D3 `:114-118`) and enforced:

**The paradox, verified:** A.0-2 / L1.0-CONS freezes "which old Accepted ADRs fold into which ADR-0000+ doc" as a **door:one-way primary source at the very START of Wave-0** (line 21, line 108), and the entire S1 parallelism recovery (L1-amend ∥ Wave-0, line 71/109) is load-bearing on that freeze being *final*. But the file-level fold set is the **output of authoring the re-foundation ADRs** — which is Wave-1 (L1.1, lines 112-119). The SSOT (`:114-118`) gives the *clusters* (D3 "supersedes 0124, phases 0369/0367/0366"; D5/D6/D7 likewise) but not a file-level fold map; that map is *produced by* the Wave-1 authoring. So the Wave-0 freeze is **necessarily provisional-until-Wave-1**.

**Why the plan's own defense fails (I checked it):** L1.0-VERIFY's SY3 completeness check (line 111, line 198) proves "every re-foundation-candidate is classified consolidate-or-amend, none unclassified." That proves the *classification is total at Wave-0*; it **cannot** prove the *classification won't change when the ADR is actually authored in Wave-1*. The closure criterion ("every Accepted ADR carrying a foundry-sense term that is also a re-foundation candidate is classified") is checkable only if "re-foundation candidate" is already pinned — and pinning it IS the Wave-1 work. The plan names this exact risk at line 111 ("without it, a Wave-1 re-foundation could discover a missed fold and an L1-amend rename would already have landed on a now-archived file") and claims completeness closes it. **It does not.** Completeness ≠ stability-under-authoring. This is a genuine ordering circularity, not a verification gap.

**Why it fails my bars:**

- **PRINCIPLE-OPTION-CONSISTENCY / PV-2 — FAIL.** Door:one-way is, by the plan's own §A.1 principle-5 and the SSOT's repeated "Door: one-way" semantics, an *irreversible commitment* — which presupposes a *finalized object*. Signing a one-way door over an artifact that downstream Wave-1 authoring may revise is internally inconsistent with the plan's own immutability/irreversibility discipline. Either (a) the freeze is genuinely final → then the re-foundation *design decisions* must actually live in Wave-0 (and the wave map is mislabeled, hiding L1-refound intellectual work in Wave-0 under L1.0-CONS), or (b) it is provisional → then it must not be door:one-way *yet*. The plan asserts (a)'s finality while scheduling (b)'s authoring later.
- **RISK-MITIGATION CLARITY — WEAK at this joint.** The S1 "recovery makes the narrowing minimal" claim (line 70/72) is *conditional on the freeze being final*; if provisional, L1-amend cannot safely run in Wave-0 (any file it amends could turn out to be a Wave-1 fold → wasted amend + dirtied immutability diff, the exact defect A.0-2 exists to prevent). The plan's parallelism recovery (S1) and its correctness gate (A.0-2 freeze) are in unacknowledged tension. The architect's synthesis #2 resolves it cleanly: **split L1.0-CONS into a Wave-0 design-freeze (the D3/D5/D6/D7 fold INTENT, which the SSOT genuinely rules) + a Wave-1 L1.1-CONS-CONFIRM checkpoint** (when each re-foundation ADR is authored, verify the actual fold set == the Wave-0 frozen set; **any delta = re-review trigger / re-open the door:one-way, NOT a silent amend**; L1-amend renames on a newly-discovered fold are reverted before archival). Two checks for two genuinely different claims (design-completeness at Wave-0, file-stability at Wave-1). I endorse it.

**Severity: HIGH, gating before A.0-2 is signed as door:one-way.** A one-way door over a guess is the exact "unverified verdict authorizes an irreversible act" pattern §A.1 principle-4 forbids — and it is the keystone every Wave-0 lane keys off.

---

## 5. FINDING 3 (terminal-barrier serialization) + PV-3 — enforced as TESTABLE-CRITERIA / HONESTY upgrades [MEDIUM + LOW]

- **Finding 3 (Tension, §3) — VERIFIED REAL, MEDIUM.** The trilemma is genuine: build-first-cutover-later (`:26`, keeps superseded bridges live → graph grows) ⊥ D-LANES maximal parallel authoring ⊥ no-dangling against a frozen snapshot (D13 `:43`). Round-2's SY2 (terminal cross-ref re-resolution on the *delivered* graph, line 207/293/312) is the correct resolution of (iii)-vs-(ii) — **but it implicitly re-serializes**: the terminal sweep cannot run until the *last* Wave-2 supersession lands, so Wave-3 is hard-blocked on all of Waves 1–2. The §A.4 "minimal narrowing" framing (line 70) does not account for this mandatory whole-corpus barrier. The architect's synthesis #3 (name the Wave-3 terminal barrier as a *second* acknowledged D-LANES serialization, both RULED-driven by D13 `:43`, S1 recovers within-Wave-0 parallelism but not the terminal barrier) is correct and cheap. This is a **testable-acceptance-criteria honesty fix** — the exit (§G) already requires the terminal re-resolution, so the barrier exists in fact; the plan must *name* it so a future reviewer does not re-discover it as a surprise (which is exactly what happened this round). Endorse.
- **PV-3 (aspirational-status staleness) — VERIFIED REAL, LOW.** The D1 supersede records `generator = worktree-only @ 2026-06-06` (line 146/284) — a *dated prose snapshot*, not a *queryable current-truth* field. A future reader sees a stale date, not live status; reachability-of-the-fact is satisfied, reachability-of-the-current-truth is not. The architect's synthesis #4 (`cutover_status:` front-matter field on the masterplan-wiring meta-ADR, mirroring the already-mandated `cutover_trigger:` pattern, updated when the generator lands on dev) closes it with the plan's own accepted mechanism. Endorse, LOW.

These three do not gate sign-off (they gate their own load-bearing points: Finding 3 before Wave-3, PV-3 is a tracking upgrade), but #3 should land in the same iteration as the two HIGHs since it is a one-line §A.4 amendment.

---

## 6. WHERE I PUSH BACK ON / SHARPEN THE ARCHITECT

1. **The architect under-states the §A.6 directionality fix as "synthesis," not a gating condition on the pass's own completeness.** The architect's condition #1 says "make §A.6 bidirectional … before §A.6 pass is declared complete." I sharpen: **the bidirectional mandate must be added to §A.6 itself (lines 93-98) AND to the §H.2 A.6 evidence row (line 301) AND to the §G/§E gating predicate (lines 215, 234)** — all four currently encode a one-directional pass. A patch that fixes only §A.5's L2 tag but leaves the §A.6 *mandate* one-directional would let the next ordering constraint slip through the same hole. The architect's own §103 closing makes this point ("close it structurally rather than patch per-instance") but condition #1 as listed reads as a per-instance re-tag. **The structural fix (bidirectional mandate in all four loci) is the gating item; the L2 re-tag is its first output.**
2. **Endorse the architect's refusal to change the wave order, and sharpen why this is NOT a REJECT.** A fabricated quotation (round-2) was arguably closer to REJECT than this; round-3's defects are *omissions of correct tags* and *an asserted-away circularity*, both with the wave order verified-correct and both patchable by named synthesis items. The architect's "two of these are the same defect class, opposite direction, second location → close structurally" is the right meta-read. But I register the calibration explicitly: **this is the second consecutive round a directional/one-artifact blind spot produced a HIGH finding on the load-bearing lane.** If round-4 surfaces a *third* instance of the same blind spot (a RULED/DERIVED mis-tag or a one-way-door-over-a-guess the bidirectional pass + design/confirm split did not prevent), that is evidence the structural fix was cosmetic, and round-4 escalates toward REJECT. I am stating the trip-wire so it is not re-litigated.
3. **One thing the architect did NOT flag that I checked and clear:** I re-verified that the architect's Finding 1 does not collaterally invalidate D-2 or the census. It does not — D11(d) RULED-tagging L2's *sweep membership* is orthogonal to D-2 (map-before-sweep, D13 `:43`) and to the 831/43 census (`:107`); both stay exactly as round-2 left them. The relabel-up (D-1-internal-sequencing stays DERIVED, L2-sweep-membership becomes RULED) is as surgical as round-2's relabel-down. No over-correction risk in the architect's remedy.

---

## 7. ENFORCEMENT-CHECKLIST SCORING (my mandate)

| Bar | Status | Note |
|---|---|---|
| Principle-option consistency | **WEAK (two joints)** | PV-1 (§A.6 one-directional, applied not just stated) + PV-2 (door:one-way over provisional consolidation-set). Both patchable (synthesis #1/#2). Rest consistent (round-2 relabel surgical, D-2/census intact). |
| Fair alternatives | **WEAK (one joint)** | Option 2 presented as fully-legal founder choice when D11(d) partially binds it; the round-2 *under*-exclusion is now a round-3 *over*-inclusion. Patch = synthesis #1 (state Option-2's real legal shape). |
| Risk-mitigation clarity | **STRONG, one WEAK joint** | §H.1 Failures 0–5 + 2b/2c are concrete and gate-mapped; the WEAK joint is the S1-recovery-vs-provisional-freeze tension (Finding 2), unacknowledged. Patch = synthesis #2 design/confirm split. |
| Testable acceptance criteria | **STRONG (one un-named barrier)** | §H.2 is greppable; R4 published-falsification-log exit is real. The Wave-3 terminal barrier exists in the exit (§G) but is un-named as a serialization (Finding 3). Patch = synthesis #3. |
| Concrete verification steps | **STRONG, one DIRECTIONAL hole** | §D per-lane gates + separate-verifier discipline are real; G3/G4 close round-1 holes. The hole: §A.6 cannot detect RULED-deflation (PV-1). Patch = bidirectional mandate. |
| Deliberate-mode: real pre-mortem + expanded verification | **PRESENT & STRONG** | §H.1 Failures 0–5 + §H.2 per-lane evidence table clear the deliberate bar. (Finding 2 argues for a Failure-6 entry: "Wave-0 freeze over a provisional fold map" — owed but the bar is already met.) |

Two bars WEAK (principle-option consistency at two joints; fair alternatives at one), one bar STRONG-with-a-directional-hole (verification). All three failures are the **same directional/one-artifact blind spot, found in the opposite direction and a second location** from round-2 — localized, identified, patchable, wave-order-preserving. Per my mandate ("REJECT if missing/weak"): WEAK here is localized + patchable, not pervasive or absent → **ITERATE.** APPROVE would ship a one-directional honesty pass and a one-way door over a guess on the irreversible lane; REJECT would be disproportionate to defects whose fix is a bidirectional mandate + a design/confirm split + three named amendments, with the wave order verified-correct.

---

## 8. REQUIRED ITERATION ITEMS (gating where noted, before founder sign-off)

1. **[HIGH — Finding 1 / PV-1, gating before §A.6 is declared complete + before any §F sign-off] Make §A.6 bidirectional and re-tag L2.** Add the second mandate to §A.6 (lines 93-98), the §H.2 A.6 row (line 301), and the §E/§G gating predicate (lines 215, 234): *"for every D-decision that names a concrete amendment action (D11(a)-(d), D12, D13, D14, D-EVENT, …), confirm the plan's corresponding lane carries the matching RULED tag; surface any ruled action left DERIVED/untagged as a re-review trigger."* Re-tag **L2's sweep-membership `[RULED: D11(d)+:52, foundry bulk-rename is named sweep content, batched one-way before backfill]`** (lines 125, 189); keep **rename-before-reauthor *internal sequencing* `[DERIVED: D-1]`**. Re-state **Option 2's actual legal shape** in §A.3/§E/§F: "author the ADR-0000+ shells first, then run the *still-pre-backfill* sweep over them" — NOT "rename after re-foundation." A §A.6 pass that surfaces this missing RULED tag is a re-review trigger, consistent with the plan's own round-2 rule.
2. **[HIGH — Finding 2 / PV-2, gating before A.0-2 is signed door:one-way] Split L1.0-CONS into a Wave-0 design-freeze + a Wave-1 confirm checkpoint.** Rename L1.0-CONS so Wave-0 freezes the **re-foundation cluster-design decision** (the D3/D5/D6/D7 fold INTENT the SSOT genuinely rules, `:114-118` / `:31` / `:34`), NOT a guaranteed-final file map. Add **Wave-1 L1.1-CONS-CONFIRM**: when each re-foundation ADR is authored, the verifier confirms actual fold set == Wave-0 frozen set; **any delta = re-review trigger (re-open the door:one-way), NOT a silent amend**; L1-amend renames on a newly-discovered fold are reverted before archival. SY3 completeness then proves *design-level classification* (Wave-0-checkable); the Wave-1 confirm proves *file-level stability* (Wave-1-checkable). This makes door:one-way meaningful (sign the design, confirm the file map) and gives S1 a real safety net.
3. **[MEDIUM — Finding 3, before Wave-3] Name the Wave-3 terminal barrier as a second acknowledged D-LANES serialization.** Amend §A.4 (line 70): "the wave-serialization narrows D-LANES at two points — the Wave-0 gate (buys map-before-sweep, D13 `:43`) and the Wave-3 terminal cross-ref barrier (buys delivered-graph no-dangling under build-first growing-graph, D13 `:43`). Both RULED-driven, both accepted; S1 recovers within-Wave-0 parallelism but not the terminal barrier." Land in the same iteration as #1/#2 (one-line edit).
4. **[LOW — PV-3] `cutover_status:` queryable field.** Replace the static `generator = worktree-only @ 2026-06-06` prose-date with a `cutover_status:` front-matter field on the masterplan-wiring meta-ADR (mirroring the mandated `cutover_trigger:` pattern), updated when the generator lands on dev.
5. **[tracking] Add Pre-mortem Failure-6** (§H.1): "Wave-0 consolidation-set freeze signed door:one-way over a provisional fold map that Wave-1 authoring revises → wasted L1-amend rename on a now-archived file + dirtied immutability diff." Mitigation = item #2's design-freeze/Wave-1-confirm split. (Owed for completeness; the deliberate bar is already met without it.)

---

## 9. SYNTHESIS — what is settled, what remains

**Settled (verified TRUE this round, no further iteration owed):** the round-2 absorptions are correctly wired (D-1 → DERIVED surgical; fabricated quote deleted, confirmed by grep; D-2/D11-sweep-before-backfill/census/D14/D-EVENT/`cutover_trigger:` genuinely RULED vs `:42-43`/`:52`/`:107`/`:46`/`:26`; SY2 terminal re-resolution; SY3 completeness; SY4 post-L2.0b census; SY5 standing caveat; R4 published-falsification-log exit). **The wave order (Option 1) is verified-correct and unchanged** — both architect findings reshape *provenance* and *the consolidation-set's finalization model*, not the recommended sequence. The architect review is evidence-grounded with **no phantom findings**, and honored the founder rule.

**Remaining (this round's iteration):** the plan polices RULED-inflation but is blind to RULED-*deflation* — and the one deflation instance (L2 ∈ D11 sweep) sits on the irreversible 831-file lane, with the plan's own §A.6 honesty mechanism structurally unable to catch it (Finding 1, HIGH); and the keystone consolidation-set freeze is door:one-way over a fold map that Wave-1 authoring produces, asserted-final but necessarily provisional (Finding 2, HIGH). Both are the **same directional/one-artifact blind spot round-2 fixed in the opposite direction** — which is itself the evidence the architect names (§103) for closing it *structurally* (bidirectional §A.6 mandate + design/confirm split) rather than per-instance. Plus the un-named Wave-3 terminal serialization (MEDIUM) and the stale-date aspirational-status field (LOW). The order does not change; its *honesty in both directions* does.

**The boulder does not stop here.** The plan must absorb the round-3 conditions — bidirectional §A.6 + L2 re-tag + honest Option-2 shape (gating before sign-off); the design-freeze/Wave-1-confirm split (gating before A.0-2 is signed); the named terminal barrier (MEDIUM); the `cutover_status:` field (LOW) — and return for a round-4 confirmation that **(a)** the §A.6 mandate is bidirectional in all four loci and surfaced no further RULED-deflation, **(b)** L1.0-CONS is split so no door:one-way is signed over a provisional artifact, and **(c)** the same directional blind spot did not produce a third instance (which would escalate toward REJECT).

---

VERDICT: ITERATE
