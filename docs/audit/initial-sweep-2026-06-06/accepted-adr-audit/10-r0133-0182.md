# Accepted-ADR audit — range ADR-0133 .. ADR-0182

> **Method:** READ-ONLY scan of the ACTUAL files in `/Users/jasonlee/Developer/source/docs/decisions/` (machine index `docs/machine-readable/decisions.json` is DRIFTED and was NOT used). Every finding cites `path:line` + a verbatim snippet. Audited through three lenses: (1) CONTRADICTIONS vs cited/related ADRs + the founder canon `decision-record-oyatie-canon.md`; (2) HYPERSCALER-LENS; (3) PATTERNS/ANTI-PATTERNS. Disposition per ADR: KEEP / AMEND / SUPERSEDE / ARCHIVE.
> **Date:** 2026-06-06.

## Coverage ledger (NO SILENT CAPS)

**In-range files: 50 (ADR-0133..0182).** Read in full: all 50 headers + full body of the 45 status:Accepted ones.

**SKIPPED (not status:Accepted) — 5:**
- ADR-0134 portfolio-hyperscaler-pattern-remediation-backlog — `status: Proposed`.
- ADR-0140 cross-cutting-carriers-adapter-exemption — `status: Superseded` (by ADR-0145).
- ADR-0141 workflow-ontology-read-path-direct — `status: Superseded` (by ADR-0145).
- ADR-0147 container-sandboxing-runtime-ladder — `status: Amended` (not Accepted).
- ADR-0170 developer-portal — `status: Superseded`.

**AUDITED (status:Accepted) — 45 ids:** 0133, 0135, 0136, 0137, 0138, 0139, 0142, 0143, 0144, 0145, 0146, 0148, 0149, 0150, 0151, 0152, 0153, 0154, 0155, 0156, 0157, 0158, 0159, 0160, 0161, 0162, 0163, 0164, 0165, 0166, 0167, 0168, 0169, 0171, 0172, 0173, 0174, 0175, 0176, 0177, 0178, 0179, 0180, 0181, 0182.

> Note on status formatting: 0146/0149/0150/0151/0152/0153/0154/0155/0156/0173 use a table/`- Status:` form instead of front-matter `status:` — each verified `Accepted` by reading the header. No file in range was left unread.

**Cross-reference verifications performed (read real files):**
- ADR-0044 (service-mesh-istio) = `status: proposed` — confirms it is a stale dangling-reference target (0176/0177/0178 cite it as if live).
- ADR-0150 title = `# ADR-0150: Cursor Pagination Canonical` — confirms the **phantom-0150** class: 0148 & 0182 cite "ADR-0150 — policy engine separation (Cedar app authz vs Kyverno)" which is FALSE; the real policy-engine ADR is **0183** (exists).
- ADR-0183/0184/0185/0186 all exist (0148/0182 forward-reference them).
- ADR-0124 (own-merge-queue) = `status: Superseded` — confirms 0160 cites a superseded ADR as live.

---

## Standing cross-cutting findings (apply to MANY ADRs in range)

These recur across the batch; per-ADR sections reference them by tag rather than repeat the full reasoning.

- **[X-FOUNDRY] CRITICAL — foundry is a DEAD context (canon D-FOUNDRY-CLARIFY / D-INTEL).** Founder ruling: "the foundry CONTEXT is dead" — the `foundry` name + platform routes to `intelligence` (framework→cloud-intelligence endpoint, current-home oya-intelligence), fitness→`oya-governance`, agentic-VCS→retired (canon `:204`, `:86`). ADRs that treat `microservices/foundry/` as a live first-class µservice/product directly contradict this. Affected: 0136, 0137, 0138, 0143 (whole-ADR), and as a service-list/reference defect in 0133, 0139, 0144, 0146, 0148, 0149, 0153, 0154, 0158, 0161, 0162, 0163, 0164, 0167, 0168, 0173, 0174, 0178.
- **[X-CEDAR] MED — Cedar = contract, owned PARC = engine (canon D6).** Canon: "Cedar = PERMANENT external-standard CONTRACT … own the EVALUATION ENGINE behind it (compile-to-Rust PARC, linux 0021)" (`:34`). ADRs that name "Cedar PDP / Cedar evaluator" as the engine without the owned-PARC framing need the contract-vs-engine split threaded. Affected: 0144, 0145, 0148, 0157, 0159, 0162, 0163, 0182.
- **[X-PURESPLIT] HIGH — `microservices/<ms>/` flat catalog contradicts pure-split (canon D-PURESPLIT/0512).** Canon: "Pure split: exactly two service trees — `oya/` (products) + `cloud/` (platform) — ERADICATE … microservices/ …" (`:172`). Nearly every ADR in range writes paths as `microservices/<ms>/…` and counts "33/34/35 µservices". Path-rename + tree-rehoming sweep required across the batch (mechanical).
- **[X-EVENT] MED — Kafka/NATS → Pulsar (canon D-EVENT).** Canon: "PULSAR is the canonical eventing/streaming bridge. Keep the Kafka→Pulsar ruling" (`:147`). ADRs naming Kafka/NATS as the eventing substrate need amendment. Affected: 0154, 0173.
- **[X-MESH44] MED — stale dual service-mesh reference (0044 Proposed vs 0148 Accepted).** ADR-0148 "REWRITES the prior … framing"; ADR-0044 is `status: proposed`. ADRs citing BOTH 0044 and 0148 as live mesh authority carry a stale edge. Affected: 0176, 0177, 0178.
- **[X-ENVTIER] LOW/MED — `tier` vocabulary namespacing (canon D12).** Canon: rename ADR-0163 "environment tiers"→"environment stages"; namespace `tier`→`autonomy_tier`/`eu_ai_act_risk_tier`/`dr_tier`/`storage_tier`/`tenant_class` (`:59`). Affected: 0144 (risk tier), 0152 (RTO tier), 0161 (storage tier), 0163 (environment tier), 0178 (tenant tier).

---

## Per-ADR audit

### ADR-0133 — Industry-best-practice + hyperscaler-grade conformance program — **AMEND**
- **Contradiction (MED):** lists piecemeal baselines incl. `ADR-0030 Argo Rollouts` (0133:28) as already-adopted — but the live progressive-delivery ADR in range is ADR-0160 **Flagger**, which canon D10 ALSO supersedes back to Argo Rollouts. The "ADR-0030 Argo Rollouts" citation is internally inconsistent with 0160 and needs reconciliation to the D10 Argo-Rollouts ruling.
- **Contradiction (MED):** `related: [… ADR-0131, ADR-0132 …]` + axis-foundry decider + `microservices/governance/IP-M01-AUDIT-<axis>-<NNN>.md` (0133:46) — bakes the `microservices/` flat-catalog (X-PURESPLIT) and foundry-as-axis (X-FOUNDRY) into the conformance program's own enforcement paths.
- **Hyperscaler-lens (KEEP-worthy core):** the 6-axis continuous-conformance-against-named-references program IS what a hyperscaler does (SLSA/SSDF/Well-Architected/SRE). Good pattern. But Axis-2 "Directory conformance … against AWS/Google/Stripe per-service folder shape" hard-codes `microservices/<ms>/` — wrong target post-D-PURESPLIT.
- **Anti-pattern caught (exemplary):** §"First-run amnesty" `severity: legacy-grandfathered` + BLOCKER-on-new (0133:181) is the correct ratchet (block new regressions, time-box legacy) — KEEP.
- **Disposition:** AMEND — keep the program; retarget Axis-2 to oya/cloud pure-split, fix the Argo/Flagger inconsistency, re-home enforcement out of `microservices/governance/`.

### ADR-0135 — Aspirational Enforcement Gate — **KEEP**
- **Pattern (exemplary, directly serves canon D-DOCTRINE/D-SEQUENCE):** blocks "active enforcement claims when the named check crate, workflow, or branch-protection context is absent" (0135:18). This is the honest-claim / no-false-green discipline the founder's FIREWALL-FIRST ruling demands ("enforcement is a FAÇADE … make enforcement REAL first", canon `:196`). One of the few ADRs that is itself a real, landed gate.
- **Hyperscaler-lens:** correct — fail-closed on unreadable corpus/workflow/crate/branch-protection (0135:62).
- **Minor (LOW):** scans `oya-governance-*` workflow names + `.github/branch-protection.yaml` — assumes the GitHub branch-protection model; under canon D-CICD the producer becomes `oya-ci-required`, so the known-surface list will need extension (additive, non-blocking).
- **Disposition:** KEEP (extend known-surfaces when oya-ci producer lands).

### ADR-0136 — Foundry as a single µservice (six internal BCs) — **SUPERSEDE**
- **Contradiction (CRITICAL) [X-FOUNDRY]:** entire ADR establishes "Foundry is one µservice … `microservices/foundry/`" with six BCs (0136:121-124) as live canon. Founder D-FOUNDRY-CLARIFY: the foundry context is dead; platform→intelligence, fitness→governance, vcs→retired (canon `:204`). The "one product perimeter" topology argument is sound IN ISOLATION but the NAME + HOME are superseded.
- **Salvage value (HIGH):** the six-concern decomposition (runtime/supervisor/eval/guardrails/providers/evidence) maps almost 1:1 to the cloud-intelligence framework primitives the canon names (D-INTEL: "model-routing, capability, MCP, eval, autonomy, guardrails, RAG", canon `:87`). The DDD/one-perimeter reasoning should be RE-HOMED into the cloud-intelligence framework ADR, not discarded.
- **Hyperscaler-lens (the topology call is RIGHT):** "Every reference platform … ships ONE product surface with internal BCs" (0136:59) — AWS Bedrock/Vertex/Azure-Foundry. Correct anti-pattern catch (the prior 6-µservice split). KEEP this reasoning, move it.
- **Disposition:** SUPERSEDE — by the cloud-intelligence/oya-intelligence re-home (canon D-INTEL FINAL); preserve the one-perimeter+six-BC reasoning in the successor.

### ADR-0137 — Foundry bounded contexts — **SUPERSEDE**
- **Contradiction (CRITICAL) [X-FOUNDRY]:** names "exactly six bounded contexts" inside `microservices/foundry/` with crate fan-out `oya-foundry-<bc>-…` (0137:46-146) — the `foundry` crate namespace itself is the 831-ref rename target (canon WF2 `:150`, D-FOUNDRY).
- **Salvage value (HIGH):** the inter-BC dependency table + forbidden/permitted rules (0137:155-194) are a clean event-driven BC-boundary contract — exemplary DDD discipline worth preserving in the intelligence-framework successor.
- **Stale-memory ref (MED):** `related_memory: [feedback_workflow_objectgraph_adapter_layer (retired per ADR-0145)…]` (0137:10) — cites a retired memory; the inter-BC rule at 0137:150 still depends on it. Dangling-rule, fix in successor.
- **Disposition:** SUPERSEDE — fold BC partition + inter-BC contract into the intelligence-framework ADR; retire the `foundry` crate namespace.

### ADR-0138 — Foundry six-path deprecation (Strangler) — **ARCHIVE**
- **Status (HIGH):** this ADR's whole job — migrate OFF `microservices/foundry-{runtime,…}/` INTO `microservices/foundry/` (0138:45-50) — is now moot: the destination (`microservices/foundry/`) is ITSELF being eradicated (X-FOUNDRY + X-PURESPLIT). The Strangler target is a dead address.
- **Pattern (exemplary, transferable):** the "atomic-consolidation + 6-month REPORT-ONLY→BLOCKER lane" variant (0138:217) is a genuinely good zero-current-consumer Strangler shape — cite it as the TEMPLATE for the foundry→intelligence rename sweep (which is the same shape at larger scale).
- **Anti-pattern caught (exemplary):** rejects adapter shims for zero callers as "Zombie-Code anti-pattern" (0138:213) — KEEP the principle.
- **Disposition:** ARCHIVE — superseded-by-refoundation; reuse its Strangler template in the foundry→intelligence sweep ADR.

### ADR-0139 — Agentic SLO-gated promotion — **AMEND**
- **Contradiction (MED) [X-FOUNDRY]:** decider `axis-foundry` + artifacts under `microservices/observability/` (0139:46, X-PURESPLIT). Otherwise the substance is independent of foundry.
- **Contradiction (MED) — internal ledger inconsistency:** the Decision §12 says "Mimir-native (recording rules ARE the ledger) … **No git-tracked JSONL ledger**" (0139:70) but the Consequences file-table writes `registry/promotion-eligibility.jsonl … (append-only ledger)` (0139:135) and the lane "Reads `registry/promotion-eligibility.jsonl`" (0139:172). Self-contradiction (Mimir-as-ledger vs JSONL-as-ledger) — must resolve in-doc.
- **Hyperscaler-lens (exemplary):** "adopt OSS leaders, own the differentiator" (Grafana LGTM substrate + owned SLO-engine/eligibility-gate, 0139:37) is the EXACT own-when-proven ratchet shape canon D-META endorses. Strong KEEP-the-pattern. Aligns with canon D10/D-CICD (SLO-gated promotion is a face of oya-ci/cd).
- **Disposition:** AMEND — resolve the ledger self-contradiction; pure-split paths; de-foundry the decider; thread into the oya-ci/cd promotion face (D-CICD).

### ADR-0142 — CRDT portability trait + alternate-adapter compile gate — **KEEP**
- **Pattern (exemplary, = canon D-META own-the-endpoint/vendor-the-bridge):** "oyatie owns a CRDT portability trait kernel; Loro is the primary adapter; Yjs and Automerge … INV-PORTABILITY-CI-COMPILE" (0142:71). Port-in-kernel + ≥2 impls + CI-compile-the-alternates is precisely the seam discipline ADR-0173 later codifies. Textbook.
- **Hyperscaler-lens:** correct — zero-cost generic (rejects `Box<dyn>` in the hot loop, 0142:148) + bus-factor-1 mitigation is real hyperscaler taste.
- **Minor (LOW):** `crates/oya-shared-crdt-*` path (root `crates/` not `microservices/`) — already closer to a flat shared-crate home; reconcile to the final oya/cloud shared-crate location (X-PURESPLIT, mild).
- **Minor (LOW):** `related: [… ADR-0135]` labeled "connect super-app expansion" in References (0142:223) but 0135 is the aspirational-enforcement gate — dangling/mislabeled related-edge (integrity sweep, D11).
- **Disposition:** KEEP (fix the mislabeled 0135 edge; reconcile crate home).

### ADR-0143 — Foundry per-BC release pointer — **SUPERSEDE**
- **Contradiction (CRITICAL) [X-FOUNDRY]:** depends entirely on `microservices/foundry/` + six BCs + `release/foundry-<bc>/<env>` (0143:67-73). Dies with 0136/0137.
- **Hyperscaler-lens (the pattern is RIGHT):** "umbrella chart + per-BC image tag" so a guardrails hot-fix is a single-pod rolling-restart (0143:80) — correct per-component-release shape (Bedrock/Vertex). Preserve in the intelligence-framework successor as per-BC release granularity.
- **Disposition:** SUPERSEDE — fold per-BC release-pointer mechanism into the intelligence-framework ADR; retire `release/foundry-*`.

### ADR-0144 — EU AI Act graduated 5-tier risk model — **AMEND**
- **Pattern (exemplary):** replaces a binary Annex-III gate with the regulation's actual 5-tier taxonomy + (archetype, context)→tier computation (0144:71-98). Correctly rejects 3-tier (loses GPAI) and 7-tier (over-engineering: "Annex III sections are sub-classifications WITHIN tier 3", 0144:196). Good Linus-taste "match the regulation, don't impose a model".
- **Contradiction (MED) [X-CEDAR]:** "the Cedar admission path computes the effective tier" (0144:98) + `ADR-0140 — Cedar policy enforcement substrate` (0144:271) — 0140 is the SUPERSEDED cross-cutting-carriers ADR, NOT a Cedar-substrate ADR; this is a **dangling/wrong related-edge** (real Cedar authz = ADR-0007; the engine = owned PARC per D6). Fix the citation + contract/engine split.
- **Vocabulary (LOW) [X-ENVTIER]:** `tier`/`risk_class` should namespace to `eu_ai_act_risk_tier` (canon D12).
- **Canon-fit (positive):** dovetails D16 autonomy-ceiling runtime gate (canon `:40`, "gates 0139/0144") and D-SAFETY — KEEP this hook.
- **Disposition:** AMEND — fix the 0140-Cedar dangling edge, contract/engine split, tier namespacing.

### ADR-0145 — Inter-microservice communication reform — **KEEP (amend refs)**
- **Pattern (exemplary, foundational):** kills the universal-mediator ESB-2.0 rule, replaces with 3 weaker invariants (decentralized audit seal, OTel trace propagation, Ontology-as-substrate-not-gateway) + opt-in Workflow (Step-Functions model) (0145:34-78). This is a genuinely correct hyperscaler reshape; explicitly cites the regret-probability that motivated it (0145:24). Strong KEEP.
- **Contradiction (MED) [X-CEDAR]:** "Cedar policy authorization (per-call)" + "Cedar v4.2 LTS" (0145:88, 185) — name Cedar as the engine; thread the owned-PARC-engine framing (D6).
- **Note (positive, but tension with D-D1-TOPOLOGY):** Invariant 3 keeps "Ontology IS the canonical READ substrate" but demotes it from gateway. Canon D-D1-TOPOLOGY makes Ontology the SINGLE transactional system-of-record + write-path and REMOVES Kafka outbox from the consistency path (`:190`). 0145's "direct gRPC permitted + Workflow opt-in" is compatible with D-D1's federated-execution half but the D1 meta-ADR must reconcile the consistency-token contract. Flag for D1 meta-ADR author.
- **Disposition:** KEEP — supersedes 0140/0141 correctly (verified front-matter `supersedes: [ADR-0140, ADR-0141]`); amend Cedar refs; reconcile with D1 meta-ADR.

### ADR-0146 — Container base image distroless static-debian12:nonroot — **AMEND**
- **Hyperscaler-lens (KEEP-worthy):** distroless-nonroot default + single-`scratch`-exception-needs-ADR (0146:42) is correct table-stakes (Google distroless, CIS restricted). Good.
- **Contradiction (LOW) [X-FOUNDRY/X-PURESPLIT]:** "`foundry-providers` calls the Anthropic / OpenAI / Google APIs" (0146:50) + "all 33 µservices" (0146:18) + `microservices/<ms>/iac/build/Dockerfile*` — foundry+flat-catalog references (mechanical sweep).
- **Anti-pattern caught (exemplary):** rejects `scratch` for concrete reasons (missing CA bundle / tzdata / `/etc/passwd` UID lookup, 0146:47-57) — evidence-based, not cargo-cult. KEEP.
- **Disposition:** AMEND — mechanical foundry/path/count sweep only; technical decision stands.

### ADR-0148 — Service-mesh canonical: Cilium L3/L4 + Istio Ambient L7 — **AMEND**
- **Integrity defect (HIGH) — phantom/wrong reference [D11 dangling-edge]:** References cite "**ADR-0150 — Kubernetes policy engine separation (Cedar app authz vs Kyverno admission)**" (0148:257) and §Decision points at it — but ADR-0150 is **Cursor Pagination Canonical** (verified). The real policy-engine ADR is **0183**. Same phantom-0150 class as canon C-2/D11. Also forward-references 0183/0184/0185/0186 in `related` (0148:9) — those exist, but the 0150 edge is flat wrong.
- **Hyperscaler-lens (exemplary):** "each layer owns exactly one concern; zero feature overlap" — Cilium CNI/L4/Hubble vs Istio-Ambient ztunnel-mTLS/waypoint-L7 (0148:49-58), matching GKE Dataplane-V2 + Solo.io reference. Correctly rejects Cilium-Service-Mesh-L7 (insufficient L7 for regulatory shaping), classic-sidecar (~2x CPU), Ambient-only (loses eBPF). Strong taste.
- **Version-currency (positive pattern):** `amendment_2026_05_26` fixes EOL "Cilium 1.16 LTS"→1.19.x with cited evidence (0148:15) — exactly the staleness-discipline canon D-DOCTRINE wants.
- **Contradiction (MED) [X-CEDAR/X-FOUNDRY]:** "ext_authz → Cedar PDP" engine framing (0148:116) + foundry in the 5 waypoint µservices (0148:112). De-foundry + PARC-engine split.
- **Disposition:** AMEND — FIX the phantom-0150 ref (→0183) as a HIGH integrity item; PARC/Cedar split; de-foundry. Technical layering decision is exemplary, KEEP.

### ADR-0149 — Idempotency Keys Canonical — **KEEP (amend refs)**
- **Hyperscaler-lens (table-stakes, correct):** mandatory `Idempotency-Key` on every state-changing op (Stripe/AWS ClientToken, 0149:24). Exactly right; under-engineering would be NOT having it.
- **Contradiction (LOW) [X-FOUNDRY/X-PURESPLIT]:** decider `axis-foundry, axis-all-microservices`; "33 microservices" (0149:5,20).
- **Disposition:** KEEP — mechanical de-foundry/pure-split sweep; substance stands. (Note: this is the genuine ADR-0149; not to be confused with anything in the phantom-0150 issue.)

### ADR-0150 — Cursor Pagination Canonical — **KEEP**
- **CRITICAL META-FINDING (the phantom-0150 root):** THIS file is the REAL ADR-0150 = Cursor Pagination. Multiple other ADRs (0148:257, 0182:165) AND canon (D6 `:34`, D11 `:56`) cite "ADR-0150" as a Cedar-engine / policy-engine ADR. That cited decision does NOT live here — 0150 is pagination. The Cedar-engine decision is a PHANTOM (needs a real id; canon: "assigned at Wave-1 L1.0 MAP", `:78`). This ADR is fine; the DANGLERS pointing at it are the defect.
- **Hyperscaler-lens (table-stakes, correct):** opaque cursor pagination mandatory, offset BANNED (0150:20) — AWS NextToken/Stripe starting_after. Correct; `scope_hash` to prevent cursor-reuse across mismatched filters (0150:53) is a nice rigor.
- **Contradiction (LOW) [X-FOUNDRY]:** decider `axis-foundry`.
- **Disposition:** KEEP — and the integrity sweep must FIX every other doc that cites "ADR-0150" for the Cedar/policy-engine decision (HIGH-priority dangling-edge cleanup, D11).

### ADR-0151 — X-Request-Id Propagation — **KEEP**
- **Hyperscaler-lens (correct):** ULID `X-Request-Id` distinct from OTel `traceparent`; `request_id` FORBIDDEN as a Prometheus label (0151:34) — the canonical high-cardinality anti-pattern, correctly foreclosed. Good taste.
- **Contradiction (LOW) [X-FOUNDRY]:** decider `axis-foundry`.
- **Disposition:** KEEP — de-foundry decider only.

### ADR-0152 — RPO/RTO Canonical (5-tier recovery) — **AMEND**
- **Hyperscaler-lens (correct table-stakes):** per-µservice RPO/RTO declaration + aggregate registry + coverage gate (0152:21, AWS Well-Architected Reliability). Right.
- **Vocabulary (LOW) [X-ENVTIER]:** `rto_tier` / R0-R4 should namespace to `dr_tier` (canon D12 explicitly lists `dr_tier`).
- **Contradiction (LOW) [X-PURESPLIT]:** `specs/microservices/rpo-rto-targets.json` + "33 µservices".
- **Disposition:** AMEND — `dr_tier` namespacing + pure-split path; substance stands.

### ADR-0153 — Outbox Pattern — **AMEND**
- **Contradiction (MED) [X-EVENT + D-D1-TOPOLOGY]:** "the ONLY canonical way for a µservice to emit an event accompanying a state change … Direct `event_bus.publish(...)` … FORBIDDEN" (0153:25-36). Two canon tensions: (a) the event bus is Pulsar not Kafka (X-EVENT, mild — ADR is bus-agnostic); (b) **canon D-D1-TOPOLOGY removes the outbox from the critical consistency path** ("Kafka outbox is REMOVED from the critical consistency path … notification / eventual fan-out only", `:190`). 0153's "outbox is the ONLY way" over-states; under D1 the outbox is for notification/eventual-fan-out, NOT for the consistency-token path. Must reconcile with the D1 meta-ADR.
- **Hyperscaler-lens (correct as far as it goes):** transactional outbox vs dual-write/2PC (0153:54, Pat Helland) is correct for the eventual-fan-out role.
- **Contradiction (LOW) [X-FOUNDRY/X-PURESPLIT]:** decider `axis-foundry` + "remaining 28 µservices".
- **Disposition:** AMEND — scope to notification/eventual-fan-out per D-D1-TOPOLOGY; Pulsar; de-foundry.

### ADR-0154 — Event Schema Versioning — **AMEND**
- **Contradiction (MED) [X-EVENT]:** "every event emitted across every channel (WebSocket, AMQP, **NATS, Kafka**)" (0154:24). Canon D-EVENT: Pulsar canonical, Kafka/NATS stale. Amend channel list to Pulsar.
- **Overlap (LOW) with 0166:** 0154 says "A schema registry µservice is DEFERRED; on-disk AsyncAPI is source of truth" (0154:36) while ADR-0166 (also 2026-05-18, Accepted) ADOPTS Apicurio schema registry. 0154's "deferred" is already superseded by 0166 — reconcile (0154 should reference 0166 as the now-landed registry).
- **Hyperscaler-lens (correct):** explicit per-event `version` + SemVer backward-compat + ≥30-day overlap (0154:31). Right.
- **Disposition:** AMEND — Pulsar channel list; reconcile the "registry deferred" claim against ADR-0166.

### ADR-0155 — Per-Tenant Resource Quotas — **KEEP (amend path)**
- **Hyperscaler-lens (correct table-stakes):** 5-axis per-tenant quotas (rate/concurrent/memory/storage/connections) beyond rate-limit-only (0155:23, AWS SaaS Lens). Correct noisy-neighbor isolation; rate-limit-only rejected for the right reason.
- **Contradiction (LOW) [X-PURESPLIT]:** "every µservice" + `crates/oya-shared-tenant-quota-kernel/`.
- **Note:** overlaps ADR-0178 (layered throttling) on the rate axis — complementary not contradictory (0178 = ingress rate tiers; 0155 = resource quotas). KEEP both; cross-link.
- **Disposition:** KEEP — pure-split path reconciliation only.

### ADR-0156 — PII Registry Canonical — **KEEP (amend path)**
- **Hyperscaler-lens (correct, compliance table-stakes):** cross-µservice `data_classes_processed` registry to drive the DSR delete/export cascade (0156:23, GDPR Art.30 / Macie / GCP DLP). Correct and load-bearing for D-RECOVER/D38 erasure-proof.
- **Contradiction (LOW) [X-PURESPLIT]:** per-µservice manifest + `specs/microservices/pii-registry.json`.
- **Disposition:** KEEP — pure-split path; substance is canon-aligned (feeds ADR-0175 delete_saga erasure-proof).

### ADR-0157 — Dedicated API Gateway Tier — **AMEND**
- **Overlap/precedence (MED):** 0157 picks the implementation ("Envoy Gateway 1.1", 0157:62) while ADR-0182 is the architectural authority for north-south/east-west separation AND pins "Envoy Gateway **1.8.0**" (0182:44). Version drift (1.1 vs 1.8.0) between two Accepted ADRs dated the same day. 0157 front-matter even says `architectural_authority: ADR-0182`. Reconcile the version + defer impl pin to 0182.
- **Hyperscaler-lens (correct):** dedicated edge tier (TLS/JWT/WAF-Coraza/DDoS/global-rate-limit) distinct from per-µservice shuffle-sharding (0157:44-57). Correctly rejects per-µservice-gateway (33 WAFs), cloud-managed-only (lock-in), Kong/Traefik. Strong.
- **Contradiction (MED) [X-CEDAR]:** "coarse Cedar tenant scoping" at gateway (0157:50) — engine framing; PARC split.
- **Disposition:** AMEND — reconcile Envoy-Gateway version with 0182; PARC/Cedar split; pure-split.

### ADR-0158 — Per-µservice Multi-Region Disposition + Sovereign Pin + Global Control Plane — **AMEND**
- **Contradiction (MED) [X-FOUNDRY]:** baseline disposition table sets `foundry | single_region per-cell | GPU pool pinned` (0158:88) — foundry as a live µservice with its own DR posture. Re-home to cloud-intelligence (GPU/runtime is exactly the framework engine being re-homed, D-INTEL FINAL `:90`).
- **Hyperscaler-lens (exemplary):** per-µservice active_active/active_passive/single_region disposition + sovereign-pin overlay + "control-plane global, data-plane regional, NO cross-region DB txns" (0158:53-57, Spanner/Stripe-tag-routing). Correctly rejects mandatory-active-active ("engineering malpractice", 0158:104) and Spanner-everywhere (portability). Strong, supports D9 maximal-sovereign-scope.
- **Contradiction (LOW) [X-PURESPLIT]:** entire per-µservice table uses bare service names under the flat catalog.
- **Disposition:** AMEND — re-home foundry→intelligence in the disposition table; pure-split; substance is canon-aligned (D9/D-KR regional packs).

### ADR-0159 — Dedicated Feature-Flag µservice (OpenFeature) — **AMEND**
- **Contradiction (HIGH) — vendor-pick clash with ADR-0173:** 0159 adopts a dedicated `feature-flags` µservice "implementing the OpenFeature spec" (0159:42) and rejects LaunchDarkly. BUT ADR-0173 Tier-III lists "LaunchDarkly (use **OpenFeature + Flipt**; ADR-0159)" (0173:199) — naming **Flipt** as the impl. 0159 itself never names Flipt (it says own-OpenFeature-server + Cedar predicates). Two Accepted ADRs disagree on whether the flag substrate is owned-OpenFeature-server vs Flipt-the-product. Reconcile (canon D-META own-when-proven would favor: vendor an OSS OpenFeature provider as bridge → own later; pick ONE).
- **Contradiction (MED) [X-CEDAR]:** flag predicates = "Cedar fragment … Same Cedar evaluator that the governance µservice uses" (0159:66,94) — engine framing; PARC split. Also correctly rejects "Cedar as the feature-flag substrate" (mixing authz with flagging, 0159:122) — that anti-pattern catch is good.
- **Hyperscaler-lens (exemplary):** crisp separation of the THREE orthogonal gates — code-deploy (ChangeSet) / traffic-shape (progressive delivery) / runtime (flag) (0159:44-51, Fowler taxonomy) + flag-lifecycle sunset gates to kill dead flags (0159:90). Strong taste.
- **Disposition:** AMEND — reconcile owned-OpenFeature-vs-Flipt with ADR-0173; PARC/Cedar split.

### ADR-0160 — Progressive Delivery via Flagger 1.x — **SUPERSEDE**
- **Contradiction (CRITICAL) — direct clash with founder D10:** 0160 adopts **Flagger** and "Why Flagger over Argo Rollouts" (0160:42,62). Founder D10: "**Argo Rollouts + Chaos Mesh** as vendored bridges … **Supersede Flagger (0160)**" (canon `:62`) AND D-CICD: "Argo Rollouts (progressive delivery — canary/blue-green/analysis) … reimplemented in Rust" (canon `:169`). 0160's tool pick is explicitly overruled. (D10 door:two-way — reversible — but the canon has ruled Argo Rollouts.)
- **Contradiction (MED):** cites `ADR-0124 — own merge queue` as live (0160:151) but 0124 is **Superseded** (verified). Dangling-to-superseded edge.
- **Hyperscaler-lens (the pattern is right, the tool is wrong):** SLO-gated canary/blue-green/A-B + auto-rollback (0160:54-61) is correct; the Flagger-vs-Argo-Rollouts reasoning (0160:62-67, "Flagger's first-class Istio") is a defensible but now-overruled tool pick.
- **Disposition:** SUPERSEDE — by the D-CICD Argo-Rollouts-pattern-reimplemented-in-Rust ADR (the progressive-delivery face of oya-ci/cd). Preserve the SLO-gate ladder + auto-rollback shape; swap Flagger→Argo-Rollouts-patterned engine. Fix the 0124-superseded edge.

### ADR-0161 — CSI Driver + StorageClass abstraction — **AMEND**
- **Hyperscaler-lens (exemplary):** canonical `oya-<kind>-<tier>` StorageClass names + per-pack CSI-driver matrix (0161:37-64) — workloads stay cloud-agnostic, packs bind. Correctly rejects per-µservice provider-volume-types (portability) and Crossplane (redundant indirection over CSI). Strong; supports D9/D-KR multi-pack + D4 storage-tier ownership.
- **Vocabulary (LOW) [X-ENVTIER]:** `hot/warm/cold` `<tier>` should namespace to `storage_tier` (canon D12 lists `storage_tier`).
- **Contradiction (LOW) [X-FOUNDRY/X-PURESPLIT]:** `related: [ADR-0143 foundry-per-BC]` (0161:9,165) + `microservices/<ms>/`.
- **Note (canon D4):** the per-pack vendored CSI drivers (EBS/Ceph/NHN/…) are correct as transitional vendored substrates behind the StorageClass port — aligns with D4 own-the-data-tier-via-ratchet. KEEP that framing.
- **Disposition:** AMEND — `storage_tier` namespacing; drop foundry edge; pure-split.

### ADR-0162 — Per-tenant Audit-Chain Slicing — **AMEND**
- **Hyperscaler-lens (exemplary):** partition audit-Merkle by tenant_id; per-pack-shared-shard + per-sovereign-tenant-dedicated-shard + O(log n) subtree retrieval (0162:41-52, CloudTrail-per-account). Correctly rejects single-global-tree (residency leak) and per-row-tree (unbounded key mgmt). Strong; load-bearing for D9 sovereign + D-RECOVER erasure.
- **Contradiction (MED) [X-CEDAR]:** "Cedar-gated … per-tenant" retrieval (0162:66). PARC split.
- **Contradiction (LOW) [X-FOUNDRY/X-PURESPLIT]:** `related: [ADR-0143]`; `audit-chain` µservice under flat catalog.
- **Disposition:** AMEND — PARC/Cedar split; drop foundry edge; pure-split. Substance is canon-aligned.

### ADR-0163 — Per-tenant Environment Tiers (test/staging/prod) — **AMEND**
- **Vocabulary (MED) [X-ENVTIER] — named in canon:** canon D12 explicitly: "rename ADR-0163 'environment tiers' → 'environment stages'" (`:59`). Direct, named amendment.
- **Hyperscaler-lens (correct):** Stripe `sk_test_`/`sk_live_` prefix + cell-isolated test/staging/prod + intercept/test-recipients/live outbound modes + prod-destructive-ack (0163:52-83). Right; correctly rejects separate-tenant-ids and separate-cells.
- **Contradiction (MED) [X-FOUNDRY/X-CEDAR]:** "Foundry isolation … per-tier GPU/sandbox pool" (0163:64) + per-tier Cedar key-issuance (0163:73). De-foundry + PARC split.
- **Disposition:** AMEND — rename tiers→stages (canon-named D12); de-foundry; PARC split.

### ADR-0164 — Sovereign Cloud / Air-Gapped Deployment — **AMEND**
- **Canon-fit (STRONG positive, D9/D-SEQ):** per-pack `air_gap: true` overlay (on-prem Harbor + OpenBao + on-prem vLLM + in-cell audit + no-egress) for KSA/EU-sovereign/KR-FSC/US-Gov/FedRAMP-High/ITAR (0164:44-99). This is the EXACT D9 "maximal vertical/sovereign scope" + D-SEQ infra-sovereignty ratchet endpoint, and the bominal far-future defense/govcloud scope (D-LINEAGE). KEEP the doctrine; it's net-positive for canon.
- **Contradiction (HIGH) [X-FOUNDRY] — but also a CANON SIGNAL:** the air-gap LLM fallback is framed entirely as "foundry µservice falls back to on-prem LLMs (vLLM/Ollama)" (0164:64-70). Under D-INTEL the framework/runtime that serves models IS cloud-intelligence — so the air-gap on-prem-inference capability is a cloud-intelligence requirement, not foundry. Re-home; this strengthens the cloud-intelligence ADR (air-gapped inference = a maturity dimension).
- **Hyperscaler-lens (correct):** rejects AWS-GovCloud-reseller ("US-only; no KSA/UAE/KR analog", 0164:121) and confidential-compute-as-air-gap-substitute ("regulator acceptance gap", 0164:127). Sound.
- **Disposition:** AMEND — re-home the air-gap inference path foundry→cloud-intelligence; pure-split; keep the sovereign-pack doctrine as a D9/D-SEQ anchor.

### ADR-0165 — Chaos Engineering Substrate (Chaos Mesh 2.x) — **KEEP (amend refs)**
- **Canon-fit (STRONG positive, D10):** canon D10 "Argo Rollouts + **Chaos Mesh** as vendored bridges behind ports, ratchet-to-owned later. … reconcile 0040/0165" (`:62`). 0165 IS the Chaos-Mesh ADR the founder endorsed. KEEP; thread the port + ratchet-to-owned framing.
- **Hyperscaler-lens (exemplary):** nightly SLO-gated chaos drills against staging + release-blocker-on-SLO-breach (0165:38, Netflix Simian Army / Google DiRT / SRE-WB ch.17). Correctly rejects AWS-FIS (portability), Gremlin (sovereign+cost). Notes Litmus is "CNCF graduated (higher than Chaos Mesh incubating)" yet picks Chaos Mesh on Workflow-CRD fit (0165:96) — honest tradeoff.
- **Contradiction (LOW) [X-PURESPLIT/X-FOUNDRY]:** `microservices/cloud-iac/iac/helm/chaos-mesh/` + production-chaos opt-in lists `foundry` (0165:56).
- **Note (MED):** depends on ADR-0160 (Flagger) "Workflow CRD composes" (0165:96) — since 0160→Argo-Rollouts, re-verify Chaos-Mesh/Argo-Rollouts composition.
- **Disposition:** KEEP — add port/ratchet-to-owned framing (D10/D-META); de-foundry; re-verify composition with Argo-Rollouts.

### ADR-0166 — Schema Registry (Apicurio 3.x) — **AMEND**
- **Hyperscaler-lens (correct):** one registry for AsyncAPI+proto3+OpenAPI+Avro+JSON-Schema, Confluent-compat, backward-compat release-blocking lane (0166:49-87). Correctly rejects Confluent (license+scope), Glue (AWS), Buf (proto-narrow).
- **Contradiction (MED) — own-vs-buy (D-META) under-applied:** 0166 ADOPTS Apicurio (Red-Hat product) as the canonical registry with no port + no own-when-proven ratchet + no second-impl — exactly the seam discipline ADR-0173/0142 require. Per D-META "own-the-endpoint/vendor-the-bridge/ratchet-when-proven", Apicurio should be behind a `SchemaRegistry` port as a vendored bridge, not a bare endpoint. (Contrast ADR-0142 which did this correctly.) Apicurio is also a JVM service — runtime-sprawl tension with Rust-first (ADR-0120).
- **Contradiction (LOW) [X-EVENT/X-FOUNDRY]:** Kafka-consumer framing; `microservices/governance/iac/helm/apicurio-registry/`.
- **Disposition:** AMEND — add the port + own-when-proven ratchet (D-META); flag JVM runtime-sprawl; pure-split.

### ADR-0167 — Tenant-facing CLI `oya` (vs internal `oya-dev-cli`) — **KEEP (amend refs)**
- **Hyperscaler-lens (exemplary):** separate tenant CLI from internal tooling (avoid leaking internal gate names + 200-crate closure + semver-tier collision), Tier-A semver, OAuth-2.1 device-code, static Rust binary (0167:40-91, aws/stripe/gh/claude parity). Strong; correctly rejects extend-dev-cli (confidentiality leak) and TS/Node (runtime weight + ADR-0120).
- **Contradiction (MED) [X-FOUNDRY]:** command surface includes `oya foundry capability invoke` (0167:65) — the tenant-facing capability-invoke verb should target the intelligence product surface, not `foundry`. Rename the verb namespace per D-INTEL (oya-intelligence is the tenant-facing servicing layer).
- **Contradiction (LOW) [X-PURESPLIT]:** `crates/oya-tenant-cli/`.
- **Disposition:** KEEP — rename `oya foundry …` verb → intelligence; otherwise exemplary.

### ADR-0168 — Public status page (SLO-derived) — **KEEP (amend refs)**
- **Hyperscaler-lens (correct):** SLO-auto-derived public status page, Statuspage.io-schema parity, runs on a dedicated cell to "outlive the platform during outages" (0168:42,132,149), 99.99% (one nine above platform). Correct; correctly rejects Statuspage.io-SaaS (residency+manual-toil) and internal-Grafana-only ("useless during auth outage", 0168:115).
- **Contradiction (LOW) [X-FOUNDRY]:** per-product status list includes `foundry` (0168:88) → intelligence product. Also `related: [… ADR-0167, ADR-0171]` fine.
- **Disposition:** KEEP — rename foundry→intelligence product component; substance stands.

### ADR-0169 — Webhook DLQ + exponential-backoff retry — **AMEND**
- **Hyperscaler-lens (exemplary):** shared webhook-delivery kernel (HMAC-SHA256 signing, 13-retry backoff, per-endpoint circuit-breaker for per-tenant isolation, DLQ, secret-rotation dual-sign window) (0169:46-83, Stripe/GitHub/Slack/EventBridge). Strong; the per-tenant circuit-breaker-isolation ("a slow tenant MUST NOT starve others", 0169:75) is the correct dominant-scale-issue catch.
- **Anti-pattern leak (LOW):** "Per-endpoint **goroutine**/task pool" (0169:79) — "goroutine" is Go vocabulary in a Rust-first platform (ADR-0120); cosmetic rebrand-arrow item (canon D-CONFORM retired-terms scan).
- **Contradiction (MED) [X-EVENT/X-FOUNDRY/X-PURESPLIT]:** sits on ADR-0005 outbox (Pulsar); webhook sources include "Foundry capability completions" (0169:23); `crates/oya-shared-webhook-delivery-kernel/`.
- **Disposition:** AMEND — Pulsar; de-foundry source; "goroutine"→Rust term; pure-split. Substance exemplary.

### ADR-0171 — Multi-cluster federation (ArgoCD ApplicationSets + Cluster API) — **AMEND**
- **Contradiction (MED) — repoURL drift:** ApplicationSet examples hard-code `repoURL: https://github.com/oyatie/oya.git` (0171:78) — canon D-FORGE-CLARIFY: GitHub-interim is the ONLY forge now (Forgejo dropped), bespoke Sapling-SCM later (`:206`). The literal `github.com/oyatie/oya` is correct-for-now but should be parameterized to the forge-pluggable port (it'll move to bespoke SCM). Minor vs the 0173 Forgejo issue.
- **Hyperscaler-lens (correct):** ArgoCD ApplicationSets (app deploy) + Cluster API (cluster lifecycle) + federation control-plane (cross-region routing) (0171:53). Correctly rejects single-global-cluster (violates 4 ADRs), Karmada (ecosystem maturity), GKE-MCI (lock-in). SPOF acknowledged honestly (0171:158).
- **Contradiction (LOW) [X-PURESPLIT]:** `microservices/<ms>/iac/helm/`.
- **Note (canon D-CICD):** ArgoCD is adopted here; canon D-CICD reimplements Argo-CD's GitOps face in Rust as part of oya-cd. ArgoCD-the-Go-binary is a vendored bridge → own-when-proven. Thread that framing.
- **Disposition:** AMEND — parameterize repoURL to forge-port; add the ArgoCD-as-bridge / oya-cd-own-when-proven framing (D-CICD/D-META); pure-split.

### ADR-0172 — Read replicas + CQRS (per-BC opt-in) — **KEEP (amend refs)**
- **Hyperscaler-lens (exemplary, anti-over-engineering):** NARROW CQRS (read-replica split) for exactly 3 high-read BCs, NOT event-sourced-everywhere — "the dominant cause of bounced 'we migrated off CQRS' retrospectives" (0172:99). Per-BC opt-in protocol to prevent CQRS-everywhere drift; Read-Your-Writes via per-tenant LSN-pinning (0172:74). Excellent Linus-taste: don't add what isn't load-bearing.
- **Contradiction (MED) [D-D1-TOPOLOGY]:** depends on ADR-0141 "workflow → ontology read path direct" (0172:27,213) — but 0141 is **Superseded** (by 0145). Dangling-to-superseded edge. Also `ontology.entity-query` CQRS interacts with D-D1's "Ontology = SINGLE transactional system-of-record" — read-replica staleness budget (1s) must respect D1's read-your-writes consistency-token. Reconcile with D1 meta-ADR.
- **Contradiction (LOW) [X-PURESPLIT]:** crate names under flat catalog.
- **Disposition:** KEEP — fix the 0141-superseded edge; reconcile ontology CQRS with D-D1-TOPOLOGY; substance exemplary.

### ADR-0173 — Vendor lock-in avoidance + stack ownership — **AMEND (HIGH — canon-defining but multiply stale)**
- **Canon-fit (STRONG positive, = D-META / D-DOCTRINE):** this ADR IS the own-the-stack ratchet doctrine the founder canon is built on — default-OWN, Tier-I OWNED / Tier-II VENDOR-SEAMED-with-phase-out / Tier-III FORBIDDEN, mandatory port + ≥2 impls, phase-out registry, CI lane (0173:70-273). Aligns precisely with D-META "own-the-endpoint/vendor-the-bridge/ratchet-when-proven". KEEP the doctrine — it should be elevated/cross-linked as a charter ADR.
- **Contradiction (HIGH) [D2/D-FORGE-CLARIFY] — Forgejo:** Tier-II lists "GitHub Actions → replacement target **Forgejo Actions or Woodpecker**" and "GitHub → replacement target self-hosted **Forgejo** … readiness gate: Foundry VCS substrate (ADR-0113)" (0173:163-171). Canon D-FORGE: "**Forgejo is DROPPED entirely**"; GitHub-interim→bespoke Sapling-SCM; CI replacement = **oya-ci** (D-CICD/0513), not Woodpecker/Forgejo-Actions. AND it cites "Foundry VCS substrate (ADR-0113)" — agentic-VCS is RETIRED (D-FOUNDRY `:204`). Three stale picks in one Tier-II block.
- **Contradiction (HIGH) [D-EVENT] — Kafka/NATS:** Tier-III "SQS/SNS (use **Kafka or NATS**)" (0173:184). Canon D-EVENT: Pulsar canonical, Kafka/NATS stale.
- **Contradiction (MED) [ADR-0159 clash + D6]:** Tier-III "LaunchDarkly (use OpenFeature + **Flipt**; ADR-0159)" (0173:199) — names Flipt; 0159 says owned-OpenFeature-server. AND Tier-I "Cedar (v4.2) … ADR-0007 cedar authorization" (0173:120) treats Cedar as an OWNED Tier-I substrate — but D6 says Cedar=external CONTRACT, the ENGINE=owned PARC. Cedar-the-language is Tier-I-adopt; the engine is Tier-I-OWN (PARC). Split.
- **Contradiction (MED) [X-FOUNDRY]:** decider `axis-foundry`; "foundry providers ship six adapter crates" worked-example (0173:215); replacement target "in-house foundry-runtime model substrate (ADR-0026)" (0173:151) → cloud-intelligence.
- **Hyperscaler-lens (exemplary):** the seam-and-multi-impl rule + grep-able phase-out registry + fail-closed CI lane (0173:255) is exactly right; cites the cost-of-exit-grows-superlinearly principle (0173:280). Strong.
- **Disposition:** AMEND (high priority) — keep the doctrine, FIX every stale pick: Forgejo→GitHub-interim/oya-ci, Kafka/NATS→Pulsar, Flipt-vs-OpenFeature reconcile, Cedar contract-vs-PARC-engine split, foundry→intelligence, ADR-0113-VCS-retired. This ADR's stale picks are high-leverage because it is the vendor-doctrine SSOT others cite.

### ADR-0174 — FinOps cost-attribution + chargeback — **AMEND**
- **Hyperscaler-lens (correct):** canonical cost-tag block + chargeback formula + MAD anomaly detector + quarterly regulator-evidence cadence (0174:67-139). Correctly rejects provider-native-cost-API-only (lock-in + lag + can't express capability/audit cost). `sustainability_class` / carbon attribution (0174:206) is forward-looking-good.
- **Contradiction (MED) [X-FOUNDRY]:** related ADR-0020 foundry-multi-provider; `per_invocation_cost(capability)` = "foundry capability registry cost field" (0174:108). → cloud-intelligence metering (canon D-INTEL dual-consumption: metered API + subscription, `:93`).
- **Contradiction (LOW) [X-PURESPLIT]:** `microservices/cloud-iac/`, `microservices/observability/`.
- **Self-ref note (LOW):** front-matter `enforcement_status: advisory-until-…` is honest (D-DOCTRINE no-false-green) — good.
- **Disposition:** AMEND — re-home capability-cost foundry→cloud-intelligence (ties D-INTEL dual-consumption + D16 cost/quota); pure-split.

### ADR-0175 — Tenant lifecycle workflow (6-state saga) — **AMEND**
- **Hyperscaler-lens (correct):** 6-state machine (Pending→Active→Suspended→Migrating→Offboarded→DeletionConfirmed), every transition a saga, delete_saga gated on collected erasure-receipts (0175:55-138, Stripe/AWS-Orgs). Correctly rejects free-form, 3-state (hides Migrating/Pending), and choreography ("operationally undebuggable", 0175:130). Strong; load-bearing for D-RECOVER erasure + D38.
- **Dangling-edge (MED):** `related: ADR-0222-saga-compensation-portfolio-policy.md` (0175:14) — 0222 is OUT of this range; not verified here but it is a Proposed-ledger candidate (canon D14). The saga substrate dependency is real; ensure 0222 is ratified (canon D14 batch) before 0175 enforcement flips from advisory.
- **Contradiction (LOW) [X-PURESPLIT/D-D1]:** `microservices/tenancy/`; lifecycle sagas relate to D-D1 workflow-engine-as-federated-durable-execution — reconcile saga home with D1.
- **Disposition:** AMEND — pure-split; confirm ADR-0222 ratification (D14); reconcile saga substrate with D-D1-TOPOLOGY. Substance canon-aligned.

### ADR-0176 — Brown-out + graceful-degradation signal API — **AMEND**
- **Hyperscaler-lens (exemplary):** explicit `oya-degradation-class: nominal|degraded|brownout|outage` response header + per-µservice gauge + mesh retry-budget biasing (0176:64-141, Google SRE ch.22 / Cloudflare brownout / Hystrix). Correctly rejects latency-inference-only (lagging+noisy), side-channel-gossip (complexity), and HTTP-503-only ("503 is a failure signal; brownout is succeeding-at-reduced-capacity", 0176:174). Excellent semantic precision.
- **Contradiction (MED) [X-MESH44]:** `related: [ADR-0044-service-mesh-istio-ambient…, ADR-0148-service-mesh-cilium]` (0176:14-16) — cites BOTH the Proposed 0044 and the Accepted 0148. Drop the stale 0044 edge.
- **Contradiction (LOW) [X-PURESPLIT]:** `crates/oya-observability-degradation-kernel/`.
- **Disposition:** AMEND — drop the stale ADR-0044 mesh edge; pure-split. Substance exemplary.

### ADR-0177 — Internal vs external API surface separation — **AMEND**
- **Hyperscaler-lens (correct):** two gateway tiers (`api.oyatie.com` public-semver-stable vs `internal-api.oyatie.com` mesh-mTLS-only, semver-waived, 10x budget) (0177:62-94, Stripe/AWS/Google). Mis-routed external→internal = 404 (route invisible) is the right structural enforcement. Correctly rejects single-gateway-annotations, hostname-per-µservice, hostname-per-bundle (cohesion thesis).
- **Contradiction (MED) [X-MESH44]:** related cites `ADR-0044-service-mesh-istio-ambient` (0177:14) as live mesh; should be ADR-0148.
- **Contradiction (LOW) [X-PURESPLIT]:** `oyatie.com` domain + `microservices/<ms>/contracts/internal/`. Note `internal-api.oyatie.com` (0174:144 cross-ref) is consistent here.
- **Disposition:** AMEND — drop the stale ADR-0044 edge; pure-split. Substance correct.

### ADR-0178 — Layered throttling (per-IP/key/user/tenant) — **AMEND**
- **Hyperscaler-lens (exemplary):** 4 throttle layers evaluated outermost-first + per-layer token-bucket + headroom gauges feeding the brownout classifier (0178:64-101, Cloudflare/AWS-APIGW/Stripe/Twilio/Shopify). Correctly rejects per-tenant-only (noisy-user starves tenant), per-user-only (no pre-auth coverage), and ML-adaptive ("brittle under traffic-shape changes … unobservable denial patterns", 0178:170). Strong.
- **Contradiction (MED) [X-MESH44]:** related `ADR-0044-service-mesh-istio-ambient` + ADR-0148 (0178:14-15). Drop 0044.
- **Vocabulary (LOW) [X-ENVTIER]:** per-tenant tier `Free/Pro/Enterprise` = `tenant_class` per canon D12 (`:59` retired tenant-tier→tenant_class).
- **Contradiction (LOW) [X-FOUNDRY]:** related ADR-0021 foundry-MCP-gateway (0178:13).
- **Disposition:** AMEND — drop 0044 edge; `tenant_class` vocab; de-foundry ref. Substance exemplary.

### ADR-0179 — Postgres connection pooling: pgcat — **KEEP (amend refs)**
- **Hyperscaler-lens (correct, D4-aligned):** pgcat (Rust pgbouncer-class) per-cell, transaction-mode default, shard-aware routing, per-µservice `pool_max` manifest declaration (0179:38-56, Notion/Stripe/Linear). Correctly rejects PgBouncer (C, single-threaded ceiling), Supavisor (Elixir runtime sprawl), RDS-Proxy (AWS lock-in). Rust-first alignment (ADR-0120). Good D4 data-tier substrate.
- **Pattern (exemplary):** the `renumber_note` (0179:13) honestly documents the multi-stage rebump (0173→0179) — exactly the no-id-reuse/no-dangling discipline canon D13 wants. KEEP as a positive example.
- **Contradiction (LOW) [X-PURESPLIT]:** "33 µservices"; `iac/helm/pgcat-cell/`.
- **Disposition:** KEEP — pure-split path only; substance is canon-aligned (D4 vendored-bridge-behind-port).

### ADR-0180 — SLO composition + inheritance arithmetic — **KEEP (amend refs)**
- **Hyperscaler-lens (exemplary):** serial/parallel/critical-path SLO composition arithmetic + design-time feasibility gate ("impossible parent 99.95% with blocking child 99.5% blocks promotion", 0180:71, Google SRE-WB Embedded-SLO-Hierarchy). Catches aggregate-impossibility + hidden-drift at gate-time not breach-time. Strong, OpenSLO-compatible.
- **Contradiction (MED):** integrates with `ADR-0160 progressive-delivery-flagger` for parent-level auto-rollback (0180:90,136). Since 0160→Argo-Rollouts (D10), re-target the rollback-signal integration.
- **Pattern (exemplary):** `renumber_note` (0180:13) — same honest D13 discipline as 0179.
- **Contradiction (LOW) [X-PURESPLIT]:** `specs/products/*.json`, `microservices/<ms>/slos/`.
- **Disposition:** KEEP — re-target the Flagger→Argo-Rollouts rollback integration; pure-split. Substance exemplary.

### ADR-0181 — Container image promotion pipeline (cosign-signed tiers) — **AMEND**
- **Hyperscaler-lens (exemplary):** 3-tier dev→staging→prod ladder, byte-copy (not rebuild) + per-tier Cosign keyless Fulcio signing + Kyverno admission refuses wrong-tier-signed images (0181:40-83, AWS-ECR-cross-account / GCP-Binary-Authorization). Correctly rejects rebuild-per-tier (drift), Notary-v1 (deprecated), long-lived-keys (ADR-0043). Strong supply-chain taste; aligns canon D-CICD (oya-ci SBOM/SLSA day-0, `:159`).
- **Contradiction (MED) [D2/D-FORGE + D-CICD]:** `related_specs: /specs/gitops-vcs-replacement.json` (0181:12) + OIDC identity = "GitHub Actions or Foundry-pipeline tier-bound workflow" (0181:48) + "Foundry pipeline's webhook-driven µservice promotion (ADR-0112)" (0181:135). Foundry-pipeline/agentic-VCS retired (D-FOUNDRY); the promotion CI = oya-ci (D-CICD). Re-target the signing-identity + promotion-driver to oya-ci.
- **Contradiction (LOW):** related ADR-0160 (Flagger→Argo-Rollouts), ADR-0124 (Superseded).
- **Disposition:** AMEND — re-target promotion driver foundry-pipeline/GH-Actions→oya-ci (D-CICD); Flagger→Argo-Rollouts; fix 0124-superseded edge. Substance exemplary.

### ADR-0182 — API Gateway (north-south) vs Service Mesh (east-west) separation — **AMEND**
- **Integrity defect (HIGH) — phantom-0150 [D11/D6]:** References: "**ADR-0150 — policy engine separation (Cedar app authz vs Kyverno admission)**" (0182:165) — ADR-0150 is Cursor Pagination (verified). Real policy-engine ADR = **0183** (also in `related`, 0182:9). Same phantom-0150 dangling-edge as 0148. FIX to 0183.
- **Hyperscaler-lens (exemplary):** crisp north-south (Envoy Gateway, Gateway-API-v1.0) vs east-west (Cilium+Istio-Ambient) with zero overlap (0182:40-61). Correctly rejects Istio-Gateway-for-N-S (couples to istiod), AWS-ALB (lock-in), Kong (Lua mismatch), Traefik/NGINX (weak WAF / F5-risk). The `In-house roadmap` table (0182:138) with KEEP-classification + "why no in-house gateway" reasoning is exactly the D-META own-when-proven honesty (KEEP standard engines, own policy/product assets). Strong.
- **Version reconcile (MED):** pins "Envoy Gateway **1.8.0**" (0182:44) vs ADR-0157's "Envoy Gateway **1.1**" (0157:62). Two Accepted same-day ADRs disagree; 0182 is the authority (0157 declares `architectural_authority: ADR-0182`) — make 0157 defer.
- **Contradiction (MED) [X-CEDAR/X-PURESPLIT]:** Cedar-PDP engine framing (0182:131); `microservices/api-gateway/`.
- **Disposition:** AMEND — FIX phantom-0150→0183 (HIGH integrity); reconcile Envoy-Gateway version with 0157; PARC/Cedar split; pure-split. Layering decision exemplary.

---

## Disposition summary (45 audited)

- **KEEP (substance sound; refs/path sweep only):** 0135, 0142, 0145, 0149, 0150, 0151, 0155, 0156, 0167, 0168, 0172, 0179, 0180 — and 0165 (KEEP, add port framing). [14]
- **AMEND (substance largely sound; named/integrity fixes):** 0133, 0139, 0144, 0146, 0148, 0152, 0153, 0154, 0157, 0158, 0159, 0161, 0162, 0163, 0164, 0166, 0169, 0171, 0173, 0174, 0175, 0176, 0177, 0178, 0181, 0182. [26]
- **SUPERSEDE (overruled by canon; salvage the pattern):** 0136, 0137, 0143 (foundry cluster → cloud-intelligence framework ADR); 0160 (Flagger → Argo-Rollouts D10/D-CICD). [4]
- **ARCHIVE (target/job moot):** 0138 (Strangler to a dead address; reuse template). [1]

(14 KEEP + 26 AMEND + 4 SUPERSEDE + 1 ARCHIVE = 45 ✓)
