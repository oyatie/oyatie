# Accepted-ADR audit — ID range ADR-0222 .. ADR-0335

> READ-ONLY audit. Source scanned: `/Users/jasonlee/Developer/source/docs/decisions/` (actual files, NOT the drifted `docs/machine-readable/decisions.json`).
> Founder canon checked against: `/Users/jasonlee/Developer/linux/docs/audit/initial-sweep-2026-06-06/synthesis/decision-record-oyatie-canon.md`.
> Every finding cites `path:line` + verbatim snippet. Severity: CRITICAL / HIGH / MED / LOW.
> Date: 2026-06-06.

## Scope accounting — NO SILENT CAPS

**Files in id-range 0222–0335 present on disk (ls-filtered):** 76 ADR files (0222, 0223, 0234–0258 sparse, 0263, 0272, 0273, 0276, 0280, 0284, 0292–0335). The numbering is sparse (many ids unused or live in other ranges).

**status:Accepted in range (AUDITED — 17 ADRs):**
ADR-0222, ADR-0223, ADR-0234, ADR-0235, ADR-0237, ADR-0238, **ADR-0239** (`Status: Accepted (amendment)` — no front-matter `status:` key, header-only at `ADR-0239:3`), ADR-0240, ADR-0241, ADR-0258, ADR-0329, ADR-0330, ADR-0331, ADR-0332, ADR-0333, ADR-0334, ADR-0335.

**SKIPPED (status:Proposed — NOT in scope) — 58 ADRs:**
0236, 0242, 0243, 0244, 0245, 0246, 0247, 0248, 0249, 0250, 0251, 0252, 0253, 0254, 0255, 0257, 0263, 0272, 0273, 0276, 0280, 0284, 0292, 0293, 0294, 0295, 0296, 0297, 0298, 0299, 0300, 0301, 0302, 0303, 0304, 0305, 0306, 0307, 0308, 0309, 0310, 0311, 0312, 0313, 0314, 0315, 0316, 0317, 0318, 0319, 0320, 0321, 0322, 0323, 0324, 0325, 0326, 0327, 0328. (Many of these are founder-ruled elsewhere — e.g. D14 DROP 0325/0316 — but they are Proposed so out-of-scope here.)

**Read-completeness caveats (honest):** ADR-0258 (1108 lines) read to line 794 — D-1..D-12 decision body, all alternatives + consequences + implementation surface covered; tail (lines 795–1108) is appendices/pattern-attribution, not re-read in full. ADR-0329 (2570 lines) read to line 1199 — full front-matter + Status + Context (A.1–A.7) + Decision (B2.001–B2.100) + Consequences (C.1–C.7.10); Section D implementation footprint (1200+) not fully read. ADR-0330 read lines 1–200 (front-matter + Status + Context A.1–A.4); decision clauses beyond 200 not fully read. ADR-0331 read lines 1–130; 0332 read lines 1–160; 0334 read lines 1–160. For these, findings are scoped to the read regions and clearly so-marked. All other Accepted ADRs (0222/0223/0234/0235/0237/0238/0239/0240/0241/0333/0335) read in full.

---

## Per-ADR audit

### ADR-0222 — Saga + compensating-transaction portfolio policy — **KEEP (amend refs)**

**(1) Contradictions:**
- vs D-PURESPLIT (pure-split {oya,cloud}, eradicate microservices/): `ADR-0222:106` `"µservices MUST NOT chain cross-µservice writes outside the engine"` and `ADR-0222:220` `"microservices/workflow-engine/policy/saga-compensation-policy.md"` assume the `microservices/` tree. MED — vocabulary/path drift, not a doctrine conflict.
- vs D-EVENT (Kafka→Pulsar): related ADR `ADR-0222:12` `"ADR-0005-eventing-backbone-outbox-pattern.md"` rides the outbox/eventing substrate; the saga policy itself is broker-agnostic (orchestration via workflow-engine, not choreography). No direct contradiction. LOW.
- vs D-FOUNDRY-CLARIFY: `ADR-0222:155` lists `"each axis (workspace, cloud, foundry)"` as a rejected alt — `foundry` is a dead context. LOW (historical example, not normative).

**(2) Hyperscaler-lens:** Strong. Sagas + compensation as the only cross-service write shape is exactly AWS DistributedSagas / Temporal / Azure Saga guidance (`ADR-0222:54-58`). Orchestration-over-choreography for observability (`ADR-0222:171-179`) is the right call. 2PC ban reaffirmed (`ADR-0222:142`). No over/under-engineering. Would Google/AWS do this? Yes.

**(3) Patterns/anti-patterns:** EXEMPLARY — declared `compensation_action` up-front (`ADR-0222:90`), `NoopWithEvidence` only for provably side-effect-free steps (`ADR-0222:98-100`), deferred-gate advisory→strict promotion (`ADR-0222:31-34`). Keep. Anti-pattern correctly killed: "best-effort compensate on error" (`ADR-0222:144`).

**Disposition: KEEP.** Amend only the `microservices/` paths → `oya/`|`cloud/` and drop the `foundry` axis example during the D-PURESPLIT/foundry sweeps.

---

### ADR-0223 — `oya git` drop-in surface with explicit policy verbs — **KEEP**

**(1) Contradictions:**
- vs D2/D-FORGE-CLARIFY (GitHub-interim → bespoke Sapling-inspired SCM): `ADR-0223:43` `"Adopt oya git <git-subcommand> as a drop-in wrapper around git"` is forge-agnostic (wraps local git, emits a local audit ledger) — compatible with both GitHub-now and bespoke-SCM-later. No contradiction. LOW.
- vs D-AEC-DECLINE / ADR-0116: `ADR-0223:74-79` rejects a cross-agent conflict-radar/file-claim service — consistent with the founder's "don't add what isn't load-bearing" Linus-taste. No contradiction; reinforces it.

**(2) Hyperscaler-lens:** Right-sized. Drop-in git wrapper that preserves git's observable contract (`ADR-0223:46-48`) + local audit side-channel (`ADR-0223:50`) is the minimal correct surface. Explicitly rejects scope-creep (auto-PR-on-push `:69-72`, implicit lifecycle inference `:61-66`). Google/Meta-style: thin, honest, Hyrum's-Law-aware (`ADR-0223:93`).

**(3) Patterns/anti-patterns:** EXEMPLARY doubt-driven-design checkpoint (`ADR-0223:36-39`); rejects brittle heuristics. Keep.

**Disposition: KEEP.** No amendment required (forge-agnostic by construction).

---

### ADR-0234 — Connect social-expansion planning contract — **AMEND**

**(1) Contradictions:**
- vs ADR-0334 (shorts merged into social) AND vs the in-file `network` retirement: `ADR-0234:38-40` lists `"connect-shorts"` and `"connect-network (retired by Wave 15K into community)"` as expansion sub-products. `connect-shorts` is now superseded by ADR-0334 (shorts→social). **HIGH** — this Accepted planning-contract enumerates a now-retired topology (`shorts` as its own surface) without a supersession edge; a fresh reader treats `connect-shorts` as live.
- vs D-PURESPLIT: `ADR-0234:34` `"product-platform role in the flat Oyatie catalog"` + the `connect-*` umbrella framing predate the oya/cloud pure-split. MED.

**(2) Hyperscaler-lens:** The "planning contract, not implemented production" discipline (`ADR-0234:23,47`) is GOOD — explicitly marks `hyperscaler_bar`/`production_readiness_gates` advisory until validators exist (`ADR-0234:50`). The `connect-anonymous` "requires a structured threat model before merge" gate (`ADR-0234:53`) is table-stakes-correct for an anonymity surface. No over-engineering.

**(3) Patterns/anti-patterns:** EXEMPLARY pattern — "advisory-until-validator-exists" prevents phantom-enforcement (the exact D-DOCTRINE "robust, not false" rule). Anti-pattern present: stale topology (shorts/network) left live.

**Disposition: AMEND.** Add supersession/relation edges to ADR-0334 (shorts→social) and the Wave-15K network→community retirement; reframe `connect-*` under the oya/ product tree.

---

### ADR-0235 — Connect core public contracts — **KEEP (amend refs)**

**(1) Contradictions:**
- vs D-PURESPLIT: `ADR-0235:48` `"All cross-product behavior must route through Workflow and Ontology mediation. Direct child-to-child calls are not allowed."` — sound, but the `connect-*` child framing predates pure-split. LOW.
- No founder-canon conflict; the six contracts (mail/calendar/messenger presence/reaction) are pillar-isolation-correct (`ADR-0235:49-54`).

**(2) Hyperscaler-lens:** GOOD. Public contracts as addressable ADR-bound artifacts rather than PRD prose (`ADR-0235:58`), pillar isolation for presence (`ADR-0235:53` `"work tenants cannot observe personal presence"`), reaction inherits parent-message retention/legal-hold (`ADR-0235:54`). Right-sized.

**(3) Patterns/anti-patterns:** EXEMPLARY "advisory until validators bind" (`ADR-0235:47,66`). Keep.

**Disposition: KEEP.** Minor ref refresh to oya/ tree during pure-split sweep.

---

### ADR-0237 — Connect dissolution Strangler migration — **KEEP (amend stale topology)**

**(1) Contradictions:**
- vs ADR-0334 (shorts→social): `ADR-0237:45` `"ADR-0238 dissolves into 8 first-class flat µservices (mail, messenger, calendar, community, social, shorts, network, anonymous)"` and `ADR-0237:214` lists `HG-SHORTS`, `HG-NETWORK`, `HG-ANONYMOUS` as retirement-trigger gates. Both `shorts` and `network` and standalone `anonymous` are now retired (0334 shorts→social; Wave-15K network→community; anonymous→community posting-mode per ADR-0238:306). **HIGH** — the Strangler exit-trigger (`ADR-0237:228` "all 8 HG-<MS> gates green") is now unreachable as written because 3 of the 8 services no longer exist as standalone surfaces.
- vs D-PURESPLIT: pervasive `microservices/` paths (`ADR-0237:88,193,218`). MED.

**(2) Hyperscaler-lens:** EXEMPLARY migration engineering. Strangler with adapter+removal (`ADR-0237:289`), per-canary SLO+error-rate gates (`ADR-0237:134-142`), zero-active-usage proof before removal (`ADR-0237:151-174`), terminal-state cleanliness (no zombie code, `ADR-0237:314`). This is precisely how AWS/Google run deprecations. Migration-cost quantified (`ADR-0237:342-355`).

**(3) Patterns/anti-patterns:** EXEMPLARY — explicitly kills "indefinite parallel maintenance" (`ADR-0237:252-268`) and "adapter-only without removal / zombie code" (`ADR-0237:270-287`). Keep the pattern. Anti-pattern present: the 8-gate trigger references retired services.

**Disposition: KEEP (pattern) + AMEND (topology).** Re-derive the umbrella-retirement trigger to the post-merge service set (shorts→social, network→community, anonymous-as-mode); fix `microservices/` paths.

---

### ADR-0238 — Connect super-app expansion into 8 flat µservices — **AMEND**

**(1) Contradictions:**
- vs ADR-0334 (shorts→social) — **HIGH.** `ADR-0238:112` table row `| `shorts` | Short-form video ... | `microservices/shorts/` | `oya-shorts-*` |` names `shorts` as a first-class µservice; ADR-0334 retires it. The `amends:` block of 0334 (`ADR-0334:18` `"ADR-0238"`) edits this, but 0238's body table is not annotated — a reader of 0238 alone gets the wrong topology.
- Internal contradiction (already partially self-healed): `ADR-0238:110` folds the LinkedIn/network subset into `community` while `ADR-0238:155` still lists `HG-NETWORK` as a retirement-trigger gate, and the verify block `ADR-0238:305` says `test -f microservices/network/RETIRED.md` + `ADR-0238:306` notes `anonymous/` was deleted. So the decision table (8 services) and the verification (network RETIRED, anonymous deleted) **disagree within the same file**. **HIGH** — self-inconsistent count.
- vs D-PURESPLIT: `oya-*` crate prefixes are correct-direction, but `microservices/<ms>/` homes (`ADR-0238:107-113`) predate pure-split. MED.

**(2) Hyperscaler-lens:** The decomposition rationale is STRONG — independent scaling dimensions (`ADR-0238:72-79`), per-concern SLO (`ADR-0238:80-85`), per-concern compliance overlay (`ADR-0238:86-92`). "AWS/Google/Microsoft/Stripe ship per-surface microservices, never per-suite bundles at architecture layer; brand-layer Connect survives in GTM" (`ADR-0238:215,249-251`) is exactly right. No over-engineering.

**(3) Patterns/anti-patterns:** EXEMPLARY brand-layer-vs-architecture-layer separation (`ADR-0238:249-251`). Anti-pattern present: stale 8-count table vs in-file retirements (network/anonymous) and downstream merge (shorts).

**Disposition: AMEND.** Reconcile the decision table to the actual post-Wave-15 set (drop shorts row → social flavor; network → community; anonymous → community mode); make the table self-consistent with its own verify block; pure-split path refresh.

---

### ADR-0239 — Foundry scope-clarification (internal-only amendment) — **SUPERSEDE (by ADR-0335) / mark historical**

**(1) Contradictions — the heaviest in this batch:**
- vs ADR-0335 (foundry µservice RETIRED, absorbed by intelligence) — **CRITICAL.** The entire premise of 0239 is `ADR-0239:21` `"microservices/foundry/ is INTERNAL only"` as a LIVE µservice with a consumer/internal split. ADR-0335 retires that µservice. ADR-0335 even lists 0239 in its `amends:` block (`ADR-0335:24`) and marks it historical (`ADR-0335:593-595` `"ADR-0239 amendment remains active as historical context"`), BUT 0239's own `Status: Accepted (amendment)` (`ADR-0239:3`) is NOT downgraded and carries no supersession banner. A reader of 0239 alone believes foundry is a live internal µservice.
- **phantom-0150 bug (founder D6 / D11(c)) — CRITICAL.** `ADR-0239:49` `"| Cedar (ADR-0150) | Internal agent permissions | Tenant-scoped consumer AI access |"` and `ADR-0239:92` `"ADR-0150 — Cedar policy engine."` cite ADR-0150 as the Cedar engine. Verified: `ADR-0150-cursor-pagination-canonical.md:1` is literally `"# ADR-0150: Cursor Pagination Canonical"`. This is exactly the dangling/phantom edge founder D11(c) orders fixed (`canon:56` `"0150-cedar phantom"`). The Cedar contract is D6 (Cedar-as-permanent-contract + owned PARC engine); 0239 mis-numbers the anchor.
- vs D-FOUNDRY-CLARIFY 3-way routing: `ADR-0239:21-28` splits foundry(internal) vs intelligence(consumer) — but founder canon routes foundry platform→intelligence, fitness→governance, vcs→retired (`canon:204`). 0239's 2-way split is now obsolete. HIGH.
- vs D5/identity: `ADR-0239:86` `"microservices/oyatie-intelligence/ (incorrect; legacy brand prefix)"` naming note is stale post-D-INTEL re-home (oya-intelligence vs cloud-intelligence). MED.

**(2) Hyperscaler-lens:** The 2026-05-18 internal-vs-consumer audience split (`ADR-0239:21-28`) was reasonable AT THE TIME (Palantir-Foundry-internal vs Apple-Intelligence-consumer brand, `ADR-0239:99-104`) but is OBE — the founder's two-layer model (cloud-intelligence framework + oya-intelligence tenant-servicing, `canon:81-99`) supersedes the binary. The audit-leakage-isolation rationale (`ADR-0239:60-61` `"Internal agents have access to source code + CI secrets... consumer agents... per-tenant data"`) survives as a principle but its µservice embodiment is gone.

**(3) Patterns/anti-patterns:** Anti-pattern: a structural ADR left Accepted with no supersession banner after the thing it governs is retired (the exact D11(b)/D-INTEGRITY hazard). The phantom-0150 citation is a textbook dangling-reference anti-pattern.

**Disposition: SUPERSEDE / mark historical.** Set status to Superseded-by-ADR-0335 with a banner; fix the phantom-0150→real-Cedar-anchor; this is one of the highest-priority integrity fixes in the range.

---

### ADR-0240 — Sovereign cloud per regional pack — **KEEP (amend refs + namespace tier)**

**(1) Contradictions:**
- vs D12 (namespace `tier`): `ADR-0240:88` data class `"HEALTH_RECORD_KR_TIER1"` uses an un-namespaced `TIER` suffix on a data-class name. Borderline — it's a data-class label, not the retired capability-tier, but D12 vocabulary-namespacing should confirm it's not swept by the bronze/silver/gold/platinum lane. LOW/MED.
- vs D-PURESPLIT: pervasive `microservices/cloud-iac/` (`ADR-0240:113,119-126`). MED.
- vs D4 (own-the-data-tier ratchet): `ADR-0240:119-126` pins per-provider managed services (EKS/RDS, GKE/CloudSQL, Naver Cloud DB, etc.) — these are the vendored bridges; sovereign-overlay is a residency concern orthogonal to the own-ratchet. No conflict, but the owned-engine endpoint (D4) should eventually satisfy sovereign placement too. LOW.

**(2) Hyperscaler-lens:** EXCELLENT and genuinely differentiated. Per-pack `sovereign_cloud_overlay.yaml` with primary/secondary certified providers (CSAP/K-ISMS-P/NDMO/SDAIA/GAIA-X, `ADR-0240:76-102`), policy-time cross-provider egress denial (`ADR-0240:133-141`), per-provider OpenTofu module catalog behind a canonical interface (`ADR-0240:128-130`). This is exactly what a sovereign-cloud-serious hyperscaler does (AWS GovCloud / Azure Government precedent, `ADR-0240:55,126`). Directly serves the D9 net-new sovereign/defense/air-gapped vertical requirement. NOT over-engineered — each piece maps to a real regulator rejection risk (`ADR-0240:57-60`).

**(3) Patterns/anti-patterns:** EXEMPLARY — canonical per-provider interface so the µservice runtime is provider-agnostic (`ADR-0240:128-130`), rejects storage/compute split (`ADR-0240:191-197` — correct: regulators bind compute too). Keep.

**Disposition: KEEP.** Path refresh (cloud-iac → cloud/ tree) + confirm `_TIER1` data-class labels are allow-listed against the tier-vocab zero-residue lane (ADR-0329).

---

### ADR-0241 — DR + business-continuity portfolio policy — **KEEP (amend refs)**

**(1) Contradictions:**
- vs D12 (`dr_tier` namespacing): GOOD — 0241 already uses the namespaced `dr_tier` field (`ADR-0241:24,77`), exactly what D12 prescribes (`canon:59` `"dr_tier"`), and ADR-0329 explicitly preserves it (`ADR-0329:644` allow-lists `"dr_tier: T1"`). No conflict. POSITIVE.
- vs D-FOUNDRY-CLARIFY: `ADR-0241:62` lists `"Foundry runtime"` as a T1 service and `ADR-0241:205` `"T1 list is intentionally short (foundry, audit chain, identity...)"`. foundry is retired (0335) → the T1 example is stale. MED.
- vs D-PURESPLIT: `microservices/<ms>/manifest.json` paths (`ADR-0241:73`). MED.

**(2) Hyperscaler-lens:** EXCELLENT and table-stakes-complete. Four DR tiers with RTO/RPO (`ADR-0241:60-65`), drill cadence + provable drill receipts to audit chain (`ADR-0241:88-99`), degraded-dependency cross-tier rule via brown-out signal (`ADR-0241:119-124`), tabletop BC scenarios (ransomware/pandemic/regulator-suspension, `ADR-0241:131-139`), regulator evidence packet (ISO 22301/SOC2/CSAP, `ADR-0241:153-163`). Matches AWS Well-Architected 4-tier + Google SRE DR tiering (`ADR-0241:240-243`). Right-sized (rejects active-active-everywhere as cost-prohibitive, `ADR-0241:180-184`).

**(3) Patterns/anti-patterns:** EXEMPLARY — drill-receipt-to-audit-chain makes DR provable not aspirational (`ADR-0241:97-99`); the degraded-dependency rule is sophisticated correct distributed-systems thinking. Keep.

**Disposition: KEEP.** Refresh foundry T1 example → intelligence/audit-chain; pure-split path refresh.

---

### ADR-0258 — API versioning model (Stripe-style header for public, URL for mesh) — **KEEP (amend refs)**

**(1) Contradictions:**
- vs D-FOUNDRY-CLARIFY (foundry dead context) — **HIGH.** `ADR-0258:99` `"oya.foundry.v1.CapabilityService"` and `ADR-0258:793` use `foundry` as a live gRPC mesh package, and `ADR-0258:32,58` list `"Foundry"` as a public-API surface. foundry the µservice is retired (0335); the AI surface is intelligence. The example package must move to `oya.intelligence.v1.*`.
- vs D6 (Cedar contract vs owned engine): `ADR-0258:235,248,735` cite `"Cedar v4.2 LTS"` + the Cedar `advice` extension for per-tenant pin. This rides the Cedar policy LANGUAGE (D6: permanent contract — fine), but the `advice`-block dependency couples to a specific upstream engine feature; under D6 (own the evaluation engine / PARC) the owned engine must implement `advice` or the pin mechanism re-homes. MED — design coupling, flag for the PARC engine spec.
- vs D-PURESPLIT: `microservices/<ms>/contracts/...` (`ADR-0258:133,756`) and `microservices/connector-events` (`ADR-0258:457`). MED.

**(2) Hyperscaler-lens:** OUTSTANDING — arguably the strongest hyperscaler-grade ADR in the range. Dual-mode (Stripe date-stamped header pinning for public `ADR-0258:56-72`; URL `/v1//v2/` for mesh L7-routing determinism `ADR-0258:86-110`) is exactly the empirically-correct split (Stripe/Square/Plaid for public; AWS Twirp/Google Stubby for internal mesh, `ADR-0258:48,790`). Per-µservice independent cadence (no monolithic "oyatie v3", `ADR-0258:112-116`), 12-month sunset + 6-month deprecation announce + RFC 8594 headers (`ADR-0258:135-183`), deterministic fail-loud negotiation algorithm (`ADR-0258:300-364`), SDK auto-gen from OpenAPI/AsyncAPI/proto with parity-gate (`ADR-0258:192-219`), webhook payload versioning (`ADR-0258:436-465`). Reversible-rollout analysis (`ADR-0258:596-615`). This is GA-parity Stripe-class. No over/under-engineering.

**(3) Patterns/anti-patterns:** EXEMPLARY — fail-loud-on-unknown/sunset generation (`ADR-0258:361-363`), contract-as-SSOT with no hand-edited SDK drift (`ADR-0258:219`), per-call audit emission surviving pin changes (`ADR-0258:431`). Keep verbatim as a model ADR.

**Disposition: KEEP.** Amend the `foundry` gRPC package examples → intelligence; flag the Cedar-`advice` coupling for the owned-PARC engine spec; pure-split path refresh.

---

### ADR-0329 — Tier system retired; replaced by tenant-class — **KEEP (amend refs)** *(read 1–1199 of 2570)*

**(1) Contradictions:**
- vs D12 (namespace `tier` → `tenant_class`, `dr_tier`, `eu_ai_act_risk_tier`, etc.): This ADR IS the canonical realization of D12 for the customer-facing axis (`canon:59` tenant_class retired tenant-tier 0329). It correctly PRESERVES the other tier vocabularies it must not sweep: ADR-0248 cellular `Tier 0..4`/`cell-tier-*`/`dr_tier` (`ADR-0329:639-646`), ADR-0037 public-API `preview/stable/GA` (`ADR-0329:647-650`), ADR-0083 Rust error-handling tiers (`ADR-0329:651-654`). EXCELLENT alignment, not contradiction. POSITIVE.
- vs D-FOUNDRY-CLARIFY: `ADR-0329:969-973` (B2.097) `"The Foundry continues to run as oyatie.foundry.* principals under Cedar"` and `axis-foundry` owner (`ADR-0329:16,933`). Post-0335 the µservice is retired (principal namespace survives, `ADR-0335:42`). The `axis-foundry` owner + "Foundry continues to run" framing is stale. MED.
- vs D-PURESPLIT: absolute `/Users/jasonlee/oyatie/microservices/...` paths throughout (`ADR-0329:153,174,455`) — these are machine-fragile absolute local paths AND predate pure-split. MED.

**(2) Hyperscaler-lens:** EXCELLENT doctrinal correction. Collapsing Bronze/Silver/Gold/Platinum feature-gating into binary `{demo_trial, paid}` + composable `billing_components` (`ADR-0329:418-420`) is the honest hyperscaler model — Stripe/AWS/Salesforce "tiers" are pricing collateral, not capability fragments (`ADR-0329:264-271`). Uniform quality bar across classes (`ADR-0329:712-717`) matches the industry-leader bar. The cellular-criticality-preservation allow-list lane (`ADR-0329:824-828`, BLOCKER continuously) prevents an over-broad sweep from deleting infra vocabulary — sophisticated.

**(3) Patterns/anti-patterns:** EXEMPLARY — allow-list (not exclude-list) zero-residue enforcement with RED-default (`ADR-0329:1192-1196`); authorized historical-mention escape so the retirement decision is itself findable (`ADR-0329:905-913`). Anti-pattern called out + killed: "intelligent-default trap" defaulting universal capabilities into paid tiers (`ADR-0329:273-279`). One concern: the ADR is 2570 lines with 100 numbered B2 clauses — borderline over-verbose (a maintainability cost; Linus-taste would trim), but the substance is correct.

**Disposition: KEEP.** Refresh foundry/axis-foundry references; replace absolute local paths with repo-relative; confirm tenant-class binding lands under oya/cloud trees not microservices/.

---

### ADR-0330 — Tenant Class (demo_trial vs paid) + composable billing — **KEEP** *(read 1–200 of full)*

**(1) Contradictions:** (within read region)
- vs D12: this is the positive replacement model for D12's `tenant_class` ruling — aligned. POSITIVE.
- vs founder canon billing (D-INTEL dual-consumption: metered API + subscription/seat): `ADR-0330:138-143,167-179` defines `billing_components ⊆ {revenue_share, per_seat, per_usage}` — directly satisfies the D-INTEL "DUAL CONSUMPTION ... metered API AND subscription/seat-based" requirement (`canon:93`). POSITIVE alignment.
- Path/owner refs: `companion_docs` use absolute `/Users/jasonlee/oyatie/...` paths (`ADR-0330:34-38`) and stale filenames (`ADR-0330:34` `"ADR-0329-tier-system-retirement.md"` vs the real `ADR-0329-tier-system-retired-replaced-by-tenant-class.md`). MED — dangling-citation hazard (D11(c)).

**(2) Hyperscaler-lens:** EXCELLENT. Composable billing matches how real customers buy (marketplace=rev-share, B2B=seats, dev=metering, reseller=all-three, `ADR-0330:169-179`); honest decomposition of vendor pricing (`ADR-0330:180-188`). cloud-billing as single source-of-truth, cloud-iam emits principal claims, Cedar gates, audit records transitions (`ADR-0330:78-81`) — clean separation.

**(3) Patterns/anti-patterns:** EXEMPLARY — separates commercial fact ("has the customer agreed to pay?") from engineering fact (`ADR-0330:164-166`); rejects accidental-vendor-mimicry (`ADR-0330:156-158`). Keep.

**Disposition: KEEP.** Fix the dangling companion-doc filenames + absolute paths (D11(c) integrity sweep).

---

### ADR-0331 — Cross-µservice tenant_class adoption template — **KEEP (amend refs)** *(read 1–130 of full)*

**(1) Contradictions:** (within read region)
- Dangling-citation hazard (D11(c)): `related:` uses non-existent filenames — `ADR-0331:14` `"ADR-0329-tier-system-retirement.md"`, `:15` `"ADR-0330-tenant-class-replacement-model.md"`, `:22` `"ADR-0255-byok-everywhere-credentials.md"`, `:24` `"ADR-0324-anti-script-authoring-doctrine.md"` — actual files are `ADR-0329-tier-system-retired-replaced-by-tenant-class.md`, `ADR-0330-tenant-class-demo-trial-vs-paid-composable-billing-components.md`, `ADR-0255-intelligence-as-two-layer-ai-substrate.md`, `ADR-0324-anti-script-anti-template-doctrine.md`. **HIGH** — multiple dangling related-edges; the index/regen lane (founder Phase-1 A-INTEGRITY) must fix.
- vs D-PURESPLIT / "77 µservices": `ADR-0331:58,91` `"every active µservice in the Oyatie corpus (77 µservices at audit time)"` + twelve `microservices/`-tree adoption surfaces. The 77-flat-microservices model is the pre-pure-split topology. MED.

**(2) Hyperscaler-lens:** GOOD — a canonical adoption template (twelve surfaces: manifest/PRD/ARCH/Cedar/YAML/SLO/cost/iac/SDK/onboarding/tests/observability, `ADR-0331:59-64`) prevents per-team plumbing drift; per-µservice advisory→BLOCKER ratchet (`ADR-0331:93`). This is how a large org rolls out a cross-cutting primitive.

**(3) Patterns/anti-patterns:** EXEMPLARY lockstep rule — retirement (0329) and adoption (0331) must land together per-µservice, "neither side may regress without the other progressing" (`ADR-0331:129`). Anti-pattern flagged + handled: silent tier→tenant_class swap (`ADR-0331:117-129`). One risk: `oya-governance-no-template-stamping` (`ADR-0331:69`) vs a "template" ADR — must ensure bespoke substance per µservice, not stamped scaffold (D-DOCTRINE "robust not false").

**Disposition: KEEP.** Fix the dangling related-filenames (HIGH integrity); confirm template homes under oya/cloud trees.

---

### ADR-0332 — Healthcare domain decomposition (8 new µservices) — **KEEP (amend refs)** *(read 1–160 of full)*

**(1) Contradictions:** (within read region)
- vs D9 (maximal vertical scope, sequenced) + D-DEPTH (owed-depth capture) + D-SAFETY (HITL clinical-Dx gate): STRONGLY ALIGNED. 0332 decomposes healthcare into emr/diagnostics/imaging/emergency/pharmacy/patient-monitoring/clinical-decision-support/care-management (`ADR-0332:64-66`) — the `clinical-decision-support` µservice is the home for the D-SAFETY HITL clinical-Dx invariant (`canon:130`). The MFDS/HITL owed-depth (D-DEPTH, `canon:136`) routes here. POSITIVE — but the cross-vertical D-SAFETY governance-owned safety-gate ADR is not yet referenced (it's net-new per founder). MED — add the edge when D-SAFETY ADR is authored.
- vs D-PURESPLIT: eight new `microservices/` homes (`ADR-0332:83`). MED.
- vs ADR-0316: `related:` `ADR-0332:29` `"ADR-0316-capability-tier-doctrine.md"` (and `ADR-0332:30` `"ADR-0321-b2b-saas-industry-leader-universe.md"`) — 0316 is the retired tier doctrine (superseded by 0329); citing it as a live related-doc is a stale edge. MED.

**(2) Hyperscaler-lens:** EXCELLENT. Domain-scoped decomposition mirrors the real healthcare-software market (Epic/Cerner EMR, Sunquest/Beaker labs, Sectra/Philips imaging, Pyxis/Omnicell pharmacy, Philips/GE monitoring, UpToDate/Lexicomp CDS — `ADR-0332:139-160`), each µservice competing shape-for-shape. Narrows healthcare-integration to the FHIR/HL7v2/DICOM broker (`ADR-0332:66`). HIPAA pack mandatory for paid tenants (`ADR-0332:69`). build-ahead-of-certification (ADR-0250) wired in. This is genuinely what a healthcare-serious hyperscaler does. Not over-engineered — 215 features across 14 domains in one µservice WAS the anti-pattern (`ADR-0332:100-131`).

**(3) Patterns/anti-patterns:** EXEMPLARY — single-concern decomposition with named-counterpart parity per domain; rejects the "one healthcare µservice = a suite" failure mode (`ADR-0332:126-131`). Keep.

**Disposition: KEEP.** Fix the stale ADR-0316 related-edge → 0329/0330; add the (forthcoming) D-SAFETY safety-gate ADR edge; pure-split path refresh.

---

### ADR-0333 — Cell µservice retired; cellular architecture is a pattern — **KEEP (amend refs)**

**(1) Contradictions:**
- vs D-PURESPLIT: pervasive `microservices/` (`ADR-0333:156,182-184,322-333`). MED.
- vs ADR-0263 dangling edge (D11(c)): `ADR-0333:34` `"ADR-0263-audit-event-class-registry.md"` — the real file is `ADR-0263-observability-emission-contract.md`. MED dangling-citation.
- Internal amendment churn: `ADR-0333:52` already carves cell-rebalancer + cell-lifecycle BACK OUT into dedicated µservices per ADR-0351 — i.e. "retire the cell service" then immediately re-create two cell-shaped services. Not a founder-canon conflict but a coherence smell (the "retire then re-add" oscillation). LOW/MED — verify 0351 actually nets out cleaner.

**(2) Hyperscaler-lens:** CORRECT and well-reasoned. "AWS does not run a general cell service that owns cell topology for every product; cellular architecture is a service-team topology pattern" (`ADR-0333:120-123`) is exactly right — a central cell meta-orchestrator would duplicate tenancy/cloud-iac/observability/audit ownership and add an api-gateway hot-path dependency (`ADR-0333:127-137`). Pure shuffle-sharding algorithm extracted to a runtime-free deterministic crate `oya-shuffle-sharding` (`ADR-0333:204-218`) is clean. OpenTofu state as the authoritative cell registry (`ADR-0333:188-190`). Preserves ADR-0248 doctrine in full (`ADR-0333:455-496`). Hits D7 isolation (Cloud Hypervisor + Kata direction, `ADR-0333:469`).

**(3) Patterns/anti-patterns:** EXEMPLARY — "doctrine lives centrally, runtime responsibility lives locally" (`ADR-0333:312-314`); distributed-ownership-with-common-doctrine over central-meta-service. Anti-pattern killed: meta-orchestrator (`ADR-0333:509`). The 82 single-sentence D-clauses are over-fragmented stylistically (each line a numbered sentence) — a readability/maintainability nit, Linus-taste would consolidate. LOW.

**Disposition: KEEP.** Fix the ADR-0263 dangling edge; pure-split path refresh; verify the 0351 cell-rebalancer/cell-lifecycle carve-out doesn't re-introduce the meta-service it just retired.

---

### ADR-0334 — Shorts µservice retired; absorbed into social — **KEEP (amend refs)** *(read 1–160 of full)*

**(1) Contradictions:**
- vs ADR-0238 / ADR-0237 (which still list shorts as a first-class µservice): 0334 `amends: ADR-0238` (`ADR-0334:18`) and `related: ADR-0238` — correct direction. But 0238's body table + 0237's 8-gate trigger are not yet updated (see 0238/0237 findings). This ADR is the FIX; the debt is on 0237/0238. POSITIVE for 0334.
- vs D-PURESPLIT: `microservices/shorts/`, `microservices/social/` (`ADR-0334:149-158`). MED.

**(2) Hyperscaler-lens:** CORRECT. "Instagram Reels is part of Instagram, YouTube Shorts is part of YouTube ... TikTok is itself a unified social product, not a multi-service decomposition" (`ADR-0334:123-131`) — accurate industry read. Short-form video as a media flavor on the same `Post` aggregate (`ADR-0334:160`), not a sibling service, is right. Preserves DRM/copyright/moderation/minor-protection (`ADR-0334:64-70`).

**(3) Patterns/anti-patterns:** EXEMPLARY single-concern enforcement (both shorts+social "produce posts, rank feeds, carry follow graphs, moderate, serve creators — bounded contexts are the same, only media flavor differs", `ADR-0334:136-139`). Zero-current-consumer Strangler variant (RETIRED.md marker, `ADR-0334:151`). Keep.

**Disposition: KEEP.** Pure-split path refresh. (The real debt this ADR exposes is on 0237/0238 — see those.)

---

### ADR-0335 — Foundry µservice retired; absorbed by intelligence; retired external agent harness dropped — **KEEP (amend per D-INTEL/D-FOUNDRY-CLARIFY)**

**(1) Contradictions — material vs founder canon:**
- vs D-INTEL endpoint (cloud-intelligence owns the framework) — **HIGH (scoping nuance).** 0335 routes ALL foundry AI-substrate ownership to `microservices/intelligence/` (`ADR-0335:60,150-183` — "intelligence is the canonical AI substrate µservice"). Founder D-INTEL FINAL re-homes the engine DOWN into `cloud/cloud-intelligence` (`canon:90` "RE-HOME the 96k-LOC engine DOWN from oya/intelligence into cloud/cloud-intelligence"). HOWEVER founder explicitly notes (`canon:99`) the Wave-0 rename targets the CURRENT home (oya-intelligence, where crates physically are) and the cloud-intelligence re-home is a SEPARATE later migration. So 0335's routing is CONSISTENT with Wave-0 reality but must be amended to NAME the cloud-intelligence endpoint (framework→cloud-intelligence; per-tenant servicing→oya-intelligence) so docs don't lock the oya-side home as the endpoint.
- vs D-FOUNDRY-CLARIFY 3-way routing — **HIGH.** 0335 does a 2-way absorb (foundry→intelligence + retired external agent harness-drop). Founder canon routes foundry **fitness→governance** (`canon:204` "Governance REMAINS its own service ... foundry-fitness→governance ~73% landed"). 0335 has no governance-routing clause; it folds eval/guardrails/evidence wholesale into intelligence (`ADR-0335:198-203,449,452`) without carving the fitness/governance slice. The governance service must own the gates that VALIDATE intelligence (layering: can't be part of it).
- vs D2/D-FORGE + ADR-0363 (agentic-VCS retired): `ADR-0335:224-227` (D-25) keeps `"End-to-end VCS orchestration per ADR-0113"` and `ADR-0335:459-462` keeps `vcs-orchestrator` as a live successor owner. Founder D-FORGE-CLARIFY/D3 retired the agentic-VCS (git+GitHub+oya-ci; `canon:204` "no vcs service ... retired"). 0335 preserving a `vcs-orchestrator` µservice contradicts the vcs-retired ruling. **HIGH.**
- vs D6 phantom-Cedar: 0335 cleanly uses `oyatie.foundry.*` as a Cedar PRINCIPAL namespace (`ADR-0335:295,463`) — correct, no phantom-0150 here. POSITIVE.

**(2) Hyperscaler-lens:** The absorb-the-duplicate-AI-substrate decision is CORRECT (two AI µservices duplicating model-router/adapters/eval/guardrails is waste, `ADR-0335:131-138`). Transition-debt handling (don't rename 122 crates across 43 deps mid-flight to keep `cargo check --workspace` green, `ADR-0335:280-283,647-652`) is pragmatic Linus-taste. retired external agent harness-name drop (inherited NousResearch terminology that never became canonical, `ADR-0335:108-110`) is correct hygiene. The ONLY hyperscaler concern is the routing destination (oya-intelligence vs cloud-intelligence) — Bedrock-analog framework belongs to the CLOUD (D-INTEL), and a vcs-orchestrator survivor contradicts the retired-vcs ruling.

**(3) Patterns/anti-patterns:** EXEMPLARY transition-debt-not-big-bang-rename (`ADR-0335:647-652`); zero-current-consumer Strangler (`ADR-0335:417-419`); doctrine-ADRs-stay-in-docs/decisions (`ADR-0335:664-668`). Anti-pattern risk: wholesale fold of eval/guardrails into intelligence without the governance carve-out re-creates the layering violation founder D-FOUNDRY-CLARIFY guards against.

**Disposition: AMEND.** (1) Add the cloud-intelligence endpoint statement (framework→cloud-intelligence later; oya-intelligence = current/Wave-0 home + tenant-servicing). (2) Carve foundry-fitness→governance (3-way, not 2-way). (3) Reconcile the surviving `vcs-orchestrator`/ADR-0113 successor with D-FORGE/D3/ADR-0363 vcs-retired (retire or reframe as oya-ci, build-first-cutover-later). (4) Correct ADR-0220/0239 to carry supersession banners (0335 already marks them historical in prose but their own status is not downgraded — see ADR-0239 finding).

---

## CRITICAL / HIGH findings roll-up (cited)

**CRITICAL**
1. **ADR-0239 — phantom-0150 Cedar citation.** `ADR-0239:49` `"Cedar (ADR-0150)"` + `ADR-0239:92` `"ADR-0150 — Cedar policy engine."` — but `ADR-0150-cursor-pagination-canonical.md:1` = `"# ADR-0150: Cursor Pagination Canonical"`. Exact founder-D11(c) phantom-0150 edge. Fix to the real Cedar contract/PARC-engine anchor (D6).
2. **ADR-0239 — left Accepted with no supersession banner after foundry µservice retired.** `ADR-0239:3` `"Status: Accepted (amendment)"` governs `ADR-0239:21` `"microservices/foundry/ is INTERNAL only"`; ADR-0335 retires that µservice and only marks 0239 historical in 0335's prose (`ADR-0335:593-595`), not in 0239's own status. A 0239-only reader gets a dead topology.

**HIGH**
3. **ADR-0335 — routes foundry-substrate to oya `intelligence`, omits the cloud-intelligence endpoint (D-INTEL).** `ADR-0335:60,150` "intelligence is the canonical AI substrate µservice" vs `canon:90` "RE-HOME ... into cloud/cloud-intelligence". Consistent with Wave-0 (`canon:99`) but must name the endpoint.
4. **ADR-0335 — no foundry-fitness→governance carve-out (D-FOUNDRY-CLARIFY 3-way).** 2-way absorb (`ADR-0335:198-203,449-452`) vs `canon:204` "Governance REMAINS its own service ... foundry-fitness→governance".
5. **ADR-0335 — keeps a `vcs-orchestrator` µservice / ADR-0113 successor, contradicting vcs-retired (D-FORGE/D3/ADR-0363).** `ADR-0335:224-227,459-462` vs `canon:204` "no vcs service ... retired".
6. **ADR-0234 / ADR-0237 / ADR-0238 — stale Connect topology vs ADR-0334 + Wave-15 retirements.** 0234:38-40 lists live `connect-shorts`/`connect-network`; 0237:214 + 0238:112 name `shorts`/`HG-SHORTS`/`HG-NETWORK` as live; shorts→social (0334), network→community (15K), anonymous→community-mode. 0238 is additionally self-inconsistent (8-row table vs its own verify block at `ADR-0238:305-306` showing network RETIRED + anonymous deleted).
7. **ADR-0331 — multiple dangling `related:` filenames (D11(c)).** `ADR-0331:14,15,22,24` cite `ADR-0329-tier-system-retirement.md` / `ADR-0330-tenant-class-replacement-model.md` / `ADR-0255-byok-everywhere-credentials.md` / `ADR-0324-anti-script-authoring-doctrine.md` — none exist by those names. Same dangling-edge class also in ADR-0330:34 + ADR-0333:34 (`ADR-0263-audit-event-class-registry.md`) + ADR-0332:29 (`ADR-0316-capability-tier-doctrine.md`, a retired doctrine).
8. **ADR-0258 — `foundry` live mesh package + public surface.** `ADR-0258:99,793` `"oya.foundry.v1.CapabilityService"`; `ADR-0258:32,58` list "Foundry" as a public-API surface. foundry retired (0335) → re-point to `oya.intelligence.v1.*`.

**Cross-cutting MED (systemic, not per-ADR-unique):**
- D-PURESPLIT `microservices/` path residue in EVERY Accepted ADR in range (0222/0235/0237/0238/0240/0241/0258/0329/0330/0331/0332/0333/0334/0335) — bulk path/home refresh to oya/ + cloud/ trees during the pure-split sweep.
- Absolute local paths (`/Users/jasonlee/oyatie/...`, `/Users/jasonlee/.claude/...`) baked into front-matter of 0329/0330/0331/0332/0335 — machine-fragile; repo-relative-ize in the integrity sweep.
