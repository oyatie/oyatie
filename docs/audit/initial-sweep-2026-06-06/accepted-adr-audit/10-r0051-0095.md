# Accepted-ADR Audit — Range ADR-0051 .. ADR-0095

> **Auditor lane:** read-only, evidence-based. Every finding = cited `file:line` + verbatim snippet.
> **Date:** 2026-06-06.
> **Canon reference:** `docs/audit/initial-sweep-2026-06-06/synthesis/decision-record-oyatie-canon.md` (founder rulings D1–D-FORGE-CLARIFY).
> **Source scanned (actual files):** `/Users/jasonlee/Developer/source/docs/decisions/` (the machine index `docs/machine-readable/decisions.json` is DRIFTED and was NOT trusted).

---

## Scope accounting (NO SILENT CAPS)

**ID range scanned:** ADR-0051 .. ADR-0095. Files present in range = 30. Files ABSENT in range (id gaps, verified via `ls`): ADR-0052(?see below), **0068, 0070, 0071, 0072, 0073, 0074, 0075, 0076, 0077, 0078, 0079, 0080, 0081, 0082, 0084, 0085, 0086, 0087, 0088, 0089** (no file on disk).

**Status filter (verified by `grep` of each file's `status:` field):**
- **SKIPPED — not Accepted (per instruction):**
  - **ADR-0052** `status: Superseded` (`ADR-0052-inventory-grit-cutover.md`) — SKIP.
  - **ADR-0054** `status: deprecated` (`ADR-0054-grit-scaffold-claim-pattern.md`) — SKIP.
- **AUDITED — status Accepted/accepted (23 ADRs):** 0051, 0053, 0055, 0056, 0057, 0058, 0059, 0060, 0061, 0062, 0063, 0064, 0065, 0066, 0067, 0069, 0083, 0090, 0091, 0092, 0093, 0094, 0095.

**Fully read:** all 23 audited ADRs were read end-to-end. **No ADR in scope was un-readable / truncated.** ADR-0092 was spot-read for status/title only (it is the seam-policy parent that 0093/0094/0095 cite); it is Accepted and is fully in this audit's disposition table.

**Audited count = 23. Audited ids = {0051, 0053, 0055, 0056, 0057, 0058, 0059, 0060, 0061, 0062, 0063, 0064, 0065, 0066, 0067, 0069, 0083, 0090, 0091, 0092, 0093, 0094, 0095}.**

---

## CRITICAL / HIGH findings (cited) — executive list

| # | Sev | ADR | Finding (one-line) |
|---|---|---|---|
| F1 | **CRITICAL** | 0057 | `supersedes:` points to a NON-EXISTENT file AND collides with the live ADR-0055 (Object→Ontology). Dangling + id-collision edge. |
| F2 | **CRITICAL** | 0058 / 0056 / 0059 / 0060 / 0061 / 0062 / 0063 / 0064 | Canonical architecture is `crates/` **flat-crate single-workspace** — directly contradicts founder **D-PURESPLIT** (exactly two trees `oya/`+`cloud/`, pure-split, no flat-crates) + **D-CONFORM**. |
| F3 | **HIGH** | 0053 | Whole ADR fixes `grit`+`icm` as the SOLE sanctioned agent primitives — **retired by ADR-0116 (Accepted) + ADR-0363**; founder **D-AEC-DECLINE** killed the adjacent layer. Live ADR still mandates a dead toolchain. |
| F4 | **HIGH** | 0059 / 0062 | Kafka is on the **critical cross-service path** ("outbox → Kafka KRaft", direct-call-requires-ADR). Contradicts **D-EVENT** (Kafka→Pulsar) AND **D-D1-TOPOLOGY** (Kafka REMOVED from the consistency path). |
| F5 | **HIGH** | 0053 / 0062 / 0065 / 0066 / 0067 / 0069 | Pervasive `foundry` / `axis-foundry` / `oya-foundry-*` naming + foundry-as-engineering-platform framing — contradicts **D-INTEL / D-FOUNDRY-CLARIFY** (foundry context dead → intelligence/governance/retired-vcs). |
| F6 | **HIGH** | 0067 / 0066 / 0063 / 0065 | CI/observability hard-wired to **GitHub Actions** as canonical (`.github/workflows/...`, "GH Actions API") — contradicts **D3 / D-CICD** (oya-ci is the canonical pipeline; GitHub is interim forge only). |
| F7 | **HIGH** | 0069 | `Related:` cites **ADR-0088** ("foundry microservice scaffolding") which does NOT exist on disk — dangling ref (matches founder D11(c) "0069→0088"). |
| F8 | **HIGH** | 0067 | `docs` µservice **renamed to `ops`** with ~20 BCs / "~100-140 crates" — single mega-microservice contradicts the flat-catalog one-concept rule AND D-PURESPLIT; massive premature scope. |
| F9 | **MED→HIGH** | 0062 | "100M+ users / hyperscaler scale / horizontally-scalable **from day one**, no single-instance designs" mandated **pre-revenue, pre-first-tenant** — premature-complexity anti-pattern vs the founder M0-gate / build-first-cutover-later ratchet. |

Full per-ADR detail below.

---

## Per-ADR audit

### ADR-0051 — Mobile and Native Client Strategy — **KEEP (minor AMEND)**

- **Lens 1 (contradictions):**
  - Wave vocabulary `W-Workspace-Stable / W-Vertical-Pilot / W-Foundry-Preview` (`ADR-0051:30-35`) uses `W-Foundry-Preview` — collides with **D-FOUNDRY-CLARIFY** (foundry is a dead context). MED. Snippet: `"- **W-Foundry-Preview / W-SaaS-Preview / W-Workspace-Preview**: web-only."` (`:32`).
  - References "14 verticals" (`:35` "not all 14 verticals get native") — stale vs **D9** maximal-vertical-scope (now B2B/healthcare/consumer/defense/power-grid/"many more"); a hardcoded 14 undercounts. LOW.
- **Lens 2 (hyperscaler):** Web-canonical + per-product-PRD-owns-tech-selection is exactly what Google/Apple/AWS do (web-first + native shells per surface). Sound. The quality bar (`crash-free ≥99.5%`, p99 cold-start, a11y, SBOM `:64-69`) is table-stakes-correct.
- **Lens 3 (patterns):** GOOD pattern — defer tech selection to per-product PRD, avoid one-mobile-stack-for-all over-prescription (`:91`). KEEP.
- **Disposition:** **KEEP** — amend `W-Foundry-Preview` wave label + "14 verticals" count only.

---

### ADR-0053 — grit + icm + oya-tooling-agent-read as sole sanctioned primitives — **SUPERSEDE / ARCHIVE**

- **Lens 1 (contradictions):** **HIGH (F3).** Decision (`ADR-0053:49`): `"The set of agent-callable coordination and state-transition primitives is fixed at exactly three: grit ... icm ... and oya-tooling-agent-read ... No agent execution path may invoke git or gh directly."` The ADR's own landing-evidence already concedes retirement: `ADR-0053:138` — `"superseded by ADR-0116 retirement so no further emissions expected against this entry"`. Founder **D-AEC-DECLINE** is explicit: `"ADR-0116 (retire external agent-coordination tooling) + ADR-0363 (retire agentic-VCS foundry) — both Accepted — deliberately killed the adjacent layer"` (canon `:193`). So a still-`Accepted` ADR mandates a toolchain the project retired.
  - Owner is `foundry` (`ADR-0053:8` `owners: - foundry`) → dead context (D-FOUNDRY-CLARIFY).
  - `planned_enforcement_ref: oya-governance-banned-primitives` (`:16`) — names a lane the canon flags as part of the un-wired `oya-governance-*` debt (D-DOCTRINE `:181`).
- **Lens 2 (hyperscaler):** No hyperscaler bans `git`/`gh` for agents and routes everything through a third-party `rtk-ai/grit` symbol-lock with a known upstream blocker (`:103` "grit session start bug ... is a real blocker"). That is reinventing-what-should-be-adopted in reverse — adopting a fragile niche tool as a hard mandate, a single-point-of-failure on an external repo.
- **Lens 3 (anti-patterns):** ANTI-PATTERN — mandate-a-dead-dependency; the steady-state "final shape from day one" (`:63`) locked a toolchain that was abandoned weeks later. Exemplary intent (audit-every-mutation) survives — re-home it onto oya-ci's signed-gate model (D3).
- **Disposition:** **SUPERSEDE** by the oya-ci canon (ADR-0116/0363 + ADR-0515). Mark superseded; do not silently leave Accepted.

---

### ADR-0055 — Object Graph renamed to Ontology — **KEEP (AMEND lineage note)**

- **Lens 1 (contradictions):** The rename itself is clean and aligns with the Ontology/Palantir vocabulary used pervasively in 0059/0006. **BUT** this ADR-0055 is the target of a **WRONG supersedes edge from ADR-0057** (see F1) — ADR-0057's front-matter `supersedes: docs/adr-archive/ADR-0055-object-graph-renamed-to-ontology.md` (`ADR-0057:13`) implies *this* 0055 is a v3 rename-cutover ADR, which it is NOT. Id-collision/dangling. CRITICAL (recorded under F1/0057).
  - Self-referential rename risk: title is fine here, but canon D11(b) flags `0006 "Ontology→Ontology"` tautology in the *related* 0006 — out of scope but noted.
- **Lens 2 (hyperscaler):** Adopting the established industry term (Palantir Ontology) over a bespoke "Object Graph" is correct adopt-the-standard-vocabulary behavior. GOOD.
- **Lens 3:** GOOD pattern — glossary-forbidden-token CI lane (`:97` `oya-check-glossary ... hard-fails on "Object Graph"`). KEEP.
- **Disposition:** **KEEP**; the AMEND work is on ADR-0057's edge, not here. Add a one-liner disambiguating 0055 (Ontology rename) from the phantom "0055-v3-cutover" that 0057 cites.

---

### ADR-0056 — Rust Clean Architecture BNF v4.1 (flat microservice grammar + 12-layer enum) — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (part of F2).** Whole grammar assumes the **single flat `crates/` workspace** with `[workspace.metadata.oya.microservices]` (`ADR-0056:111-157`). Founder **D-PURESPLIT** (`canon :171-172`): `"Pure split: exactly two service trees — oya/ (products) + cloud/ (platform) — ERADICATE everything else ... no oya→cloud internal dep"`. The BNF's `cloud` `public_layers=["sdk"]` cross-microservice exemption (`:242-256`) directly violates "no oya→cloud internal dep". The 12-layer enum + import-matrix is GOOD and survives; the **flat-crate topology + cloud-as-one-of-the-flat-microservices framing must be amended** to the two-tree split.
  - `foundry = { owner = "council-foundry" }` (`:143-144` "# Foundry (internal)") — dead context. MED (F5).
  - Layer enum here is **12 values** (`:69-72, :159-166`) but founder **D-DOCTRINE** + ADR-0105 reference a **"BNF 13-layer enum (0056/0105)"** (`canon :183`). 12 vs 13 mismatch between this ADR and the canon's stated invariant. **HIGH** — must reconcile which is canonical.
- **Lens 2 (hyperscaler):** Crate-name-encodes-layer + mechanically-enforced inward-only dep direction is exactly Google/Meta monorepo doctrine (visibility/layering enforced by tooling). GOOD. The `oya-check-architecture` matrix (`:198-217`) is the right shape.
- **Lens 3 (patterns):** EXEMPLARY — closed layer enum, "adding a layer = 1-ADR action" (`:167`), ports-in-kernel rule (`:184-187`). KEEP these. ANTI-PATTERN risk: the `public_layers` escape hatch is a backdoor around cross-product-refusal that conflicts with pure-split dogfood purity.
- **Disposition:** **AMEND** — retarget topology to `{oya,cloud}` two-tree (D-PURESPLIT), reconcile 12-vs-13 layer count vs ADR-0105/D-DOCTRINE, drop/rehome `foundry` registry row, reconcile `public_layers` against no-oya→cloud-dep.

---

### ADR-0057 — Cutover Mechanics Rename Plan v4 (Hybrid C) — **SUPERSEDE / ARCHIVE**

- **Lens 1 (contradictions):** **CRITICAL (F1).** Front-matter (`ADR-0057:14`): `supersedes: docs/adr-archive/ADR-0055-object-graph-renamed-to-ontology.md` and prose (`:32`): `"**Supersedes:** ADR-0055 (v3-era rename plan ADR ...)"`. Verified: **no file `ADR-0055-rename-plan-v3-cutover.md` exists**; the only ADR-0055 on disk is "Object Graph renamed to Ontology" (Accepted, live). This is BOTH a dangling supersedes (target file absent) AND an id-collision (the live 0055 is a different decision). Matches founder D11(c) `"0057→0055-v3"` dangling-edge ruling (`canon :56`).
  - Entire ADR is grit/icm-coordination cutover machinery (`:108-126`, `:154-172`) — depends on the **dead** ADR-0053 toolchain (F3). The whole "Shard 0/1/1.5" cutover is a one-time historical migration event already executed.
  - `reviewer-foundry` partition (`:103`) — dead context.
- **Lens 2 (hyperscaler):** A one-time rename-cutover plan is legitimately ephemeral; hyperscalers don't keep executed migration runbooks as live Accepted architecture decisions — they archive them.
- **Lens 3 (anti-patterns):** ANTI-PATTERN — keeping an executed one-shot migration plan `Accepted` indefinitely; the dangling/colliding supersedes edge is exactly the drift D-DOCTRINE targets.
- **Disposition:** **ARCHIVE** (executed historical migration) — and the supersedes edge MUST be fixed regardless (point at the real superseded ADR or delete the line) before/at archival, per D11(c).

---

### ADR-0058 — Flat microservice catalog (Product Groups retired) — **AMEND**

- **Lens 1 (contradictions):** **CRITICAL/HIGH (F2).**
  - Decision (`ADR-0058:33`): `"No vertical, arm, product group, or platform grouping exists in code, directories, or architecture. Every feature and product is an independent microservice registered in [workspace.metadata.oya.microservices]"`.
  - Directory rule (`:131-144`): `"crates/ oya-tenancy-kernel/ ... No crates/healthcare/, crates/enterprise/, crates/fintech/ directories. All crates flat under crates/."`
  - This single-flat-`crates/` topology directly contradicts founder **D-PURESPLIT** (two trees `oya/`+`cloud/`) and **D-CONFORM** decision 4 (collapse STD service trees into `kubernetes/`, `operating-system/`; per-service colocation). The "everything is shared, one flat catalog" thesis is the OPPOSITE of the pure oya/cloud split.
  - Catalog lists `foundry` first (`:38-40` "Foundry (internal-only...)") — dead context (F5).
  - Catalog hardcodes a fixed product list (`:36-65`) that omits the D9 net-new verticals (defense, power-grid) — stale scope. MED.
- **Lens 2 (hyperscaler):** Flat-catalog à-la-carte enablement ("like enabling services in an AWS console" `:69`) is the right *product* model. But hyperscalers DO separate platform substrate from product surfaces at the repo/tree level (the pure-split) — this ADR explicitly forbids that separation in code. Wrong structural call vs the founder ruling.
- **Lens 3 (patterns):** GOOD: GTM-segmentation-is-not-architecture (`:78-82`); cross-microservice-refusal lane (`:122`). ANTI-PATTERN: "platform retired as a name" (`:89`) is now itself stale since `cloud/` IS the platform tree.
- **Disposition:** **AMEND** — reconcile the flat-`crates/` topology to the `{oya,cloud}` pure-split (keep the à-la-carte product model + GTM-not-arch rule; change the directory/topology mandate + drop foundry + reconcile scope to D9).

---

### ADR-0059 — Workflow + Ontology = ecosystem adapter layer — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (F4).** `ADR-0059:112`: `"Event-driven design via outbox → Kafka KRaft (Bominal ADR-0116 inherited): sub-second event lag Workflow → subscriber."` Founder **D-EVENT** (`canon :147`): `"PULSAR is the canonical eventing/streaming bridge. Keep the Kafka→Pulsar ruling"`. AND **D-D1-TOPOLOGY** (`canon :190`): `"Kafka outbox is REMOVED from the critical consistency path (notification / eventual fan-out only)"`. This ADR puts Kafka/outbox as the cross-product action backbone — contradicts both.
  - `supersedes/related` cite `ADR-0055 (Ontology renamed to Ontology)` (`:147`) — self-referential tautology phrasing (D11(b) class). LOW.
  - "Microservices never call each other directly ... all cross-product integration flows through Workflow OR Ontology" (`:30`) is the right inversion-of-control, but the realization assumes the flat-catalog single-workspace (F2 inherits).
- **Lens 2 (hyperscaler):** Workflow=orchestration-adapter + Ontology=data-adapter as the ONLY cross-service channels is a clean, Palantir-shaped IoC plane. GOOD architecture. But making Workflow a mandatory hot path for ALL cross-product action flow (`:137` "Workflow becomes a hot path") is a scale-ceiling/SPOF risk unless the consistency-token model of D-D1 replaces the Kafka-outbox fan-out.
- **Lens 3 (patterns):** EXEMPLARY: typed-events + audit-chain + RLS on every cross-product hop. ANTI-PATTERN: Kafka-outbox-on-critical-path (the exact "SAP integration tax" D-D1 eliminates by construction).
- **Disposition:** **AMEND** — Kafka→Pulsar (eventual fan-out only); adopt the D-D1 ontology-typed-action + consistency-token model for the critical path; reconcile topology to pure-split.

---

### ADR-0060 — Bominal-inheritance precedence — **KEEP (AMEND lineage framing)**

- **Lens 1 (contradictions):**
  - The default-inherit-Bominal-then-override model is consistent with founder **D-LINEAGE** (`canon :113-114`: oyatie WAS bominal; lineage churned). GOOD — this ADR is the in-source artifact of that lineage.
  - Inherited-list row `ADR-0116 | Event streaming (outbox → Kafka KRaft at scale)` (`ADR-0060:75`) re-imports the Kafka decision → same D-EVENT/D-D1 conflict (F4). MED.
  - Override #2 row text: `"Object Graph naming ... Renamed to Ontology (matches Palantir directly)"` (`:50`) — fine.
  - Inherited `ADR-0140 (retired per ADR-0145)` shown as inherited-but-retired (`:87`) — confusing live edge; should be dropped not listed. LOW.
- **Lens 2 (hyperscaler):** An explicit precedence + glossary-translation table is good provenance hygiene (consolidates-provenance). GOOD.
- **Lens 3 (patterns):** GOOD pattern — explicit override ledger (`:47-58`), no implicit divergence. ANTI-PATTERN risk: the inherited-list duplicates decisions that have since been superseded in oyatie (Kafka), so it can re-introduce retired choices by reference.
- **Disposition:** **KEEP** — AMEND the inherited-rows that conflict with later founder rulings (Kafka row; the retired-0140 row), align to D-LINEAGE reconciliation workflow.

---

### ADR-0061 — Application — B2B unified shell — **KEEP (minor AMEND)**

- **Lens 1 (contradictions):**
  - Inherits flat-catalog assumptions (`ADR-0061:33` "registered as application in [workspace.metadata.oya.microservices]") → F2 topology inherits. MED.
  - `"state lives only in Postgres (enablement records) + Redis (session cache)"` (`:106`) — Redis named, but founder data-tier canon **D4** uses **Valkey (0336)** as the vendored cache bridge, not Redis. MED — naming drift (Redis→Valkey).
  - Scale claim `"100M+ tenant users via cell architecture"` (`:107`) — premature-scale framing (see F9) but cell-arch grounding is legit.
- **Lens 2 (hyperscaler):** AWS-Console-style à-la-carte product enablement + capability-registry dynamic surface discovery (`:67-70`) is exactly the hyperscaler control-plane pattern. GOOD; benchmark targets (AWS Console / Linear `:102`) appropriate.
- **Lens 3 (patterns):** EXEMPLARY — composition-root-only rule, no-business-logic-in-app (`:113`); capability-registry decoupling (adding a µservice needs no Application code change). ANTI-PATTERN: none material.
- **Disposition:** **KEEP** — AMEND Redis→Valkey naming + topology reconcile + soften day-1-100M scale claim to M0-gated.

---

### ADR-0062 — Quality/Performance/Scalability bar — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (F4).** `ADR-0062:71`: `"Event-driven | Outbox → Kafka KRaft. Direct synchronous cross-service calls require ADR justification"` and `:54` event-lag target sourced to "Bominal ADR-0116 LISTEN/NOTIFY". Contradicts D-EVENT (Pulsar) + D-D1 (Kafka off critical path).
  - **HIGH (F5).** `:40` benchmark row `"Operational telemetry | Palantir Foundry-grade observability"` + `:128-130` `"Foundry (internal engine) must be scalable ... oya-check-statelessness and oya-check-shardability apply to oya-foundry-* crates equally."` — dead foundry context.
  - `:42` `"Eventing | Confluent Kafka (KRaft) + Apache schema registry parity"` — sets the *benchmark* to Kafka; should be Pulsar per D-EVENT.
- **Lens 2 (hyperscaler):** **F9 — premature complexity.** `:18`/`:20`: `"hyperscalers (100M+ user scale). Horizontal scalability is mandatory from day one. No single-instance-only designs."` + `:65` "Mandatory from day one". A hyperscaler does NOT build for 100M users before its first paying tenant; AWS/GCP services ship regionally then scale on proven demand. Founder canon sequences via M0 evidence-gates + build-first-cutover-later (D-SEQ/D8), explicitly answering the "would a hyperscaler do all this?" concern by *sequencing not day-one-everything*. Mandating 100M-scale designs pre-revenue is gold-plating that taxes every crate. HIGH.
  - The benchmark-against-industry-leaders discipline itself (`:32`) is GOOD and matches ADR-0123 maturity-claim-gate.
- **Lens 3 (patterns):** EXEMPLARY: CI-enforced quality (statelessness/shardability/perf-budget/benchmark lanes `:88-99`), "quality is CI-enforced not slide-deck aspirational" (`:177`). ANTI-PATTERN: day-1-hyperscale mandate; Kafka-as-benchmark.
- **Disposition:** **AMEND** — Kafka→Pulsar (benchmark + outbox); drop foundry; re-frame "from day one" → "M0-gated, scale-on-proven-demand" per D8/D-SEQ; keep the CI-enforced-quality spine.

---

### ADR-0063 — Documentation set coverage (CI-enforced) — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (F6).** `ADR-0063:185` E2E lane: `".github/workflows/ci-fitness-lanes.yml job runs the binary"` — GitHub-Actions-as-canonical-CI contradicts D3/D-CICD (oya-ci is the canonical pipeline; GitHub interim forge only). MED-HIGH.
  - Algorithm reads `docs/MASTERPLAN.md §2.1 catalog` as the **planned set** and reconciles registered-vs-planned (`:95`). Founder **D1**: ADRs are SSOT; masterplan is GENERATED. Using MASTERPLAN as a *source* the lane reconciles against is the inverted-authority pattern (CC-3) — should read the ADR front-matter, not the generated masterplan. HIGH.
  - Owner `axis-foundry` for lane implementation (`:145`) — dead context (F5). MED.
- **Lens 2 (hyperscaler):** "code + docs + tests + evidence in the same commit, no stubs" (`:20`) + machine-enforced doc-coverage is good monorepo doctrine (Google's "docs-as-code, enforced"). GOOD. The deliberate-mode pre-mortem (`:151-177`) is exemplary process.
- **Lens 3 (patterns):** EXEMPLARY: pack.yaml-as-single-source-of-truth, orphan-scan, lane-is-source-of-truth-not-hand-table (`:128`). ANTI-PATTERN: masterplan-as-authority for the planned set (inverts D1).
- **Disposition:** **AMEND** — GitHub-Actions→oya-ci lane runner; read ADR front-matter (not masterplan) as the planned-set source; drop axis-foundry owner. Keep the doc-coverage contract spine.

---

### ADR-0064 — Canonical base + localization packs (Korea = pack #1) — **KEEP (AMEND)**

- **Lens 1 (contradictions):**
  - Already self-amended for pure-split: `ADR-0064:11-13` `"Amendment note — 2026-06-02 ... pack/catalog/evidence roots ... remain canonical during the {oya,cloud} pure-split migration."` GOOD — partially reconciled to D-PURESPLIT already.
  - Strongly aligns with founder **D-KR** (`canon :132-133`: global-canonical core + localization packs; KR first-to-market) — this ADR IS the in-source grounding D-KR cites (`canon :132` "aligns ADR-0064 canonical-base"). GOOD.
  - Pack crate naming `crates/oya-<microservice>-<pack>-*` (`:93`) inherits flat-`crates/` topology (F2). MED.
- **Lens 2 (hyperscaler):** Canonical-base + per-jurisdiction packs is exactly AWS's "global service + per-region compliance" model; the seam/adapter/pack trichotomy (`:36-40`) is a clean, correctly-scoped abstraction with a hard CI neutrality gate (`:71` `oya-check-architecture --canonical-base-neutrality`). EXEMPLARY.
- **Lens 3 (patterns):** EXEMPLARY: smallest-blast-radius "prefer seam over adapter over pack" (`:73`); hard-rule mechanical enforcement of canonical-base neutrality; thorough alternatives table (`:211-221`). KEEP.
- **Disposition:** **KEEP** — only topology-path reconcile (crates/ → oya/ tree). Strong, founder-aligned ADR; the model is canon (D-KR).

---

### ADR-0065 — Documentation as Leptos webapp + machine-readable co-emit — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (F5).** Owner `council-architecture + axis-foundry` (`:10`); `"per ADR-0063 + Foundry primitives"` (`:197`); generator owner `axis-foundry` (`:292`) — dead context.
  - `"Markdown is the source of truth"` (`:41`) for docs — distinct from D1 (which is about masterplan/ADR authority), so NOT a direct D1 conflict; but note D-DOCORG wants a unified machine-readable per-doc record (Diátaxis quadrant + axis + tier) generated-validated — this ADR's triple-output is compatible but predates the D-DOCORG unification. MED.
  - `frontmatter status enum: Proposed|Accepted|Rejected|Superseded|Retired` (`:66`) — should be the canonical status enum the integrity sweep (D11) standardizes; verify single source. LOW.
- **Lens 2 (hyperscaler):** Docs-as-product (Leptos) + machine-readable co-emission for agents is a strong dogfood pattern (the docs portal is itself a tenant product). GOOD. SvelteKit-vs-Leptos consistency with 0051 noted (0051 says SvelteKit for tenant UIs; 0065 says Leptos) — minor stack drift. LOW.
- **Lens 3 (patterns):** GOOD: deterministic generated derivations, "MUST NOT be hand-edited (CI lane enforces)" (`:41`). ANTI-PATTERN: adds a NEW `docs` microservice (`:44-54`) that 0067 immediately renames to `ops` — churn.
- **Disposition:** **AMEND** — drop foundry owner/refs; fold the `docs`-µservice into 0067's `ops` rename (or supersede-by-0067 per 0067's own claim); align frontmatter status enum + D-DOCORG unification.

---

### ADR-0066 — Live code-introspection docs portal — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (F6).** Extractor table `:53` `"GitHub Actions API | gh api / GitHub Actions GraphQL | Recent CI run status"` + `:62` on-PR mode "GitHub Actions PR check" — GitHub-Actions-as-canonical-CI vs D3/D-CICD oya-ci. MED-HIGH.
  - **F5.** Owner/extractors `axis-foundry` (`:10, :238`); `grit status --json` + ICM extractors (`:51-52`) — pulls the dead grit/icm coordination state (F3) into the live portal as a first-class surface. MED.
  - `:210` `"Inherits Bominal Foundry-grade observability posture"` — dead-context naming. LOW.
- **Lens 2 (hyperscaler):** Live-introspection / single-manifest / dead-code-zero-tolerance is genuinely hyperscaler-grade (AWS Config + Resource Explorer + asset graph analog). GOOD ambition. But ~15 extractors + daemon + rustdoc-JSON-nightly dependency (`:204` "Rustdoc JSON is unstable nightly-only") is high premature complexity for pre-tenant stage — over-engineering relative to M0 sequencing (D8). MED.
- **Lens 3 (patterns):** EXEMPLARY: extractor-as-canonical-source, "structured facts extracted never typed" (`:32`); dead-code BLOCKER lane (`:107-113`). ANTI-PATTERN: grit/ICM/GitHub-Actions as live data sources (couples portal to retired/interim tooling).
- **Disposition:** **AMEND** — GitHub-Actions→oya-ci data source; replace grit/ICM surfaces with oya-ci run/claim equivalents; drop foundry; sequence the 15-extractor build under M0 gates.

---

### ADR-0067 — `ops.oyatie.com` hyperscaler operations console — **AMEND (large)**

- **Lens 1 (contradictions):**
  - **HIGH (F8).** Decision (`:33-35`): renames `docs` µservice → `ops` with **20 BCs** and `:159` `"~20 BCs × 5-7 layer crates each = ~100-140 crates over time"`. A single microservice with 20 bounded contexts and 140 crates contradicts the flat-catalog single-concept rule (ADR-0056 `:96` "Omit BC when ... single concept") and D-PURESPLIT's flat-colocated-once rule. It is a mega-service. Also inverts the 0065/0066 work (`:139-140` "ADR-0065 + ADR-0066 are subsumed ... docs µservice ... is retired").
  - **F6.** Surfaces hard-wire GitHub Actions (`:64` `"ci-runs | GH Actions workflow runs"`, `:53` "GH Actions runs", `:185` lanes) + grit/ICM browsers (`:62-63`) — interim-forge + dead-tooling coupling vs D3/D-CICD/D-AEC-DECLINE.
  - **F5.** Pervasive foundry: `axis-foundry` owner (`:10, :188`), `internal-foundry` Cedar role (`:97`), "Palantir Foundry's ops view" (`:22`).
  - References its own predecessor as filename `ADR-0067-ops-oyatie-com-portal-foundation.md` (`ADR-0069:173`) — title/filename drift (actual file is `...-hyperscaler-operations-console.md`). LOW.
- **Lens 2 (hyperscaler):** "AWS Console + Datadog + Grafana + PagerDuty + Linear + Workday admin + GitHub Insights + Palantir Foundry ops" in ONE console (`:22`) is the right north-star surface, BUT building all 20 surfaces is the textbook over-scope; hyperscalers ship the console incrementally. The Cedar-gated audience-tier model (`:88-98`) and "cross-tenant scope leak rate = 0" (`:110`) are correct table-stakes. The concentration-risk is self-acknowledged (`:160` "ops.oyatie.com outage takes down the operations surface").
- **Lens 3 (patterns):** GOOD: single-pane ops, Cedar-gated tiers, no-silent-regression surface (`:114-136`). ANTI-PATTERN: mega-microservice (20 BCs/140 crates); GitHub-Actions/grit/ICM as live sources; rename-churn (docs→ops after 0065/0066 just created docs).
- **Disposition:** **AMEND** — re-shape `ops` from one 140-crate mega-service into properly-bounded services under the pure-split; swap GH-Actions/grit/ICM sources to oya-ci; drop foundry; fix the self-citation filename. Keep the ops-console north-star + Cedar-tier model, sequence under M0.

---

### ADR-0069 — Active machine-readable artifact contract (9-capability) — **AMEND**

- **Lens 1 (contradictions):**
  - **HIGH (F7).** `Related:` line (`ADR-0069:12`): `"ADR-0088 (foundry microservice scaffolding)"` and References (`:174`): `"docs/decisions/ADR-0709-general-live-apex.md (ADR scaffolding pattern precedent)"`. **No ADR-0088 exists on disk** — dangling ref (founder D11(c) "0069→0088", `canon :56`). Also `:172` cites `"ADR-0056-bnf-v4-1.md"` and `:173` `"ADR-0067-ops-oyatie-com-portal-foundation.md"` — both WRONG filenames (real files are `...-rust-clean-architecture-bnf.md` / `...-hyperscaler-operations-console.md`). Multiple dangling/wrong-filename edges. HIGH.
  - **F5.** Owner `axis-foundry` (`:10`); GitHub Actions YAML listed as an artifact format (`:45`) — interim coupling. MED.
  - `status: blocked-by-foundation` capability state (`:61`) honestly flags the un-wired reality — consistent with D-DOCTRINE's "robust not false" (the ADR even cites the honest-claim discipline `:74`). GOOD self-honesty.
- **Lens 2 (hyperscaler):** The contract explicitly models on AWS Config + AWS Resource Explorer + GCP Asset Inventory + K8s CRD/admission (`:37`) — correct adopt-the-pattern reasoning. The 9-capability + knowledge-graph + DRY-registry is genuinely hyperscaler-grade governance. GOOD ambition. Risk: "monolithic registries today; a graph-storage adapter ... needed before ... ~10k artifacts" (`:135`) — scale ceiling acknowledged, not yet solved. MED.
- **Lens 3 (patterns):** EXEMPLARY: honest-claim rule (`operational` requires resolvable evidence `:74`), profiles-to-reduce-authoring-burden (`:103`), mechanical-prevention-over-process. This ADR is the kind of enforcement D-DOCTRINE wants. ANTI-PATTERN: the dangling ADR-0088/wrong-filename refs are exactly the drift the contract itself is meant to prevent (ironic).
- **Disposition:** **AMEND** — fix the dangling ADR-0088 ref + the two wrong filenames (D11(c)); drop foundry owner; route GitHub-Actions artifact format under oya-ci. Strong contract; keep and wire it (it directly serves the Phase-0 accounting-registry the founder wants).

---

### ADR-0083 — Rust error-handling tier (thiserror/anyhow/no-panic) — **KEEP**

- **Lens 1 (contradictions):**
  - Lane name `oya-governance-error-boundary` (`:151`) — part of the un-wired `oya-governance-*` crate debt (D-DOCTRINE `:181` "22 oya-governance-* crates NOT wired into CI"); claims enforcement that may be convention-only. MED — verify it actually blocks.
  - `silent-failure-hunter` reviewer signs `## Code Review` (`:112`) — canon notes `## Code Review` is **convention-only, no `guard-pr-merge-review.mjs` on disk** (D-CONFORM `:110`). So the enforcement mechanism is partly a façade. MED.
  - Tier-1 layer list (`:76`) names `service` + `bindings` layers NOT in the ADR-0056 12-value enum (`api`, `service`, `bindings` are not among kernel/domain/application/app/adapter/infrastructure/cli/rest/grpc/graphql/worker/sdk) — layer-vocabulary drift vs 0056. MED.
- **Lens 2 (hyperscaler):** thiserror-at-libraries / anyhow-at-binaries / no-panic-in-lib / typed-matchable-errors is textbook idiomatic-Rust + exactly what a hyperscaler-grade Rust shop enforces. EXEMPLARY. The amendment (`:180-265`) correctly drives a real Tier-1 violation (audit-chain `.expect()`) to a versioned breaking fix.
- **Lens 3 (patterns):** EXEMPLARY: types-don't-lie, no-silent-fail-open, variant-stable-SDK-gen. The CONV-2 silent-failure prevention is load-bearing. KEEP.
- **Disposition:** **KEEP** — verify/wire the `oya-governance-error-boundary` lane for real (not advisory) per D-DOCTRINE; reconcile the Tier-1 layer list (`service`/`bindings`/`api`) against the 0056 enum.

---

### ADR-0090 — Hyper canonical HTTP backbone — **KEEP (AMEND superseded core)**

- **Lens 1 (contradictions):**
  - The 2026-05-14 absolute "hyper everywhere / axum banned" decision (`:40-41`) was REPLACED by the 2026-05-29 amendment (`:132-176`) to a strategic hyper/axum split — the body's main Decision section now contradicts its own Amendment. The Amendment is the live truth; the head Decision should be marked superseded-by-amendment. MED.
  - Amendment references `oya-cloud-intelligence-rest` (`:160`) — aligns with D-INTEL cloud-intelligence (GOOD, current naming), and `oya-ci-webhook-gateway-app` (`:162`) — aligns with D3 oya-ci (GOOD). This ADR is relatively well-reconciled to canon.
  - Cites ADR-0509 (`:14`) — verified EXISTS. GOOD (no dangling).
- **Lens 2 (hyperscaler):** "own the routing/middleware kernel, hyper at the protocol layer, axum where ergonomics pay" (`:144-162`) is a defensible own-the-core/use-the-tool-where-it-pays stance consistent with the ownership ratchet (D-META). The 0-to-minimal-dependency principle is sound but the original "ban axum, hand-roll everything" was over-engineering — the amendment correctly walks it back. GOOD outcome.
- **Lens 3 (patterns):** GOOD: layered building-blocks (std-only L1-4, hyper only at L5 `:58-65`); enforced by `oya-check-http-stack` reading `specs/http-stack-policy.json` (`:170`). ANTI-PATTERN (resolved): the original absolutism; the dual Decision-vs-Amendment now reads contradictorily.
- **Disposition:** **KEEP** — AMEND the head Decision to defer to the Amendment (mark the "axum banned" prose superseded-in-place) so the ADR reads consistently. Well-aligned to canon otherwise.

---

### ADR-0091 — Foundry write-gate foundations — **AMEND (rename + reframe)**

- **Lens 1 (contradictions):**
  - **F5.** Title + kernel `oya-foundry-write-gate-kernel` (`:7, :41`), owner `council-foundry` (`:12`) — dead foundry context; per D-FOUNDRY-CLARIFY/D-INTEL the write-gate governance belongs to **governance** (the gate authority) not foundry. HIGH for naming.
  - **F4.** Follow-ups bind a **Kafka** producer to the write-gate (`:114` "M02-P05 IP-003: bind Kafka producer to WriteGate") + Decision §"Contract surface" `:67` "gRPC/Webhook/Kafka adapters" — Kafka vs D-EVENT (Pulsar). MED.
  - Milestone vocabulary `M02-P04/P05` (`:24`) — pre-gate-wave framing; canon uses gate-waves not M0-M3 calendar milestones (CC-12). LOW.
- **Lens 2 (hyperscaler):** Four-eyes / separation-of-duties / default-deny write-gate state machine (`:43-65`) is exactly the Stripe/AWS-grade governance the ADR claims (`:79`). EXEMPLARY governance primitive — this is a keep-the-pattern.
- **Lens 3 (patterns):** EXEMPLARY: single canonical state machine across all transports, fail-closed default-deny (`:99`), one ApprovalEnvelope. ANTI-PATTERN: foundry naming; Kafka binding.
- **Disposition:** **AMEND** — rename `oya-foundry-write-gate-*` → governance-owned (`oya-governance-*` per D-INTEL/D-FOUNDRY-CLARIFY); Kafka→Pulsar; gate-wave vocab. Keep the write-gate state machine intact.

---

### ADR-0092 — Workspace dependency-seam policy — **KEEP (AMEND topology)**

- **Lens 1 (contradictions):** (Status `accepted` verified `ADR-0092:3`; title `# ADR-0092: Workspace dependency-seam policy`.) Seam policy presumes the single-workspace `[workspace.dependencies]` model — inherits the flat-`crates/`-vs-pure-split topology tension (F2). The policy *mechanism* (centralized version pinning, seam discipline) is correct and survives any topology; only the one-workspace assumption needs reconcile to the two-tree `{oya,cloud}` (D-CONFORM decision 4 collapses STD trees into one version root anyway, so this is largely compatible). MED.
- **Lens 2 (hyperscaler):** Workspace.dependencies-as-BOM (single pin, consumers reference) is exactly Cargo/Maven-BOM hyperscaler doctrine (echoed by ADR-0069 `:37`). GOOD.
- **Lens 3 (patterns):** EXEMPLARY: DRY version pinning / seam discipline. Parent of the 0093/0094/0095 seam-plan decisions. KEEP.
- **Disposition:** **KEEP** — reconcile the one-workspace assumption to the pure-split one-version-root (mostly already compatible per D-CONFORM).

---

### ADR-0093 — DeadlineMiddleware → LatencyBudgetReporter (honest naming) — **KEEP**

- **Lens 1 (contradictions):** None vs canon. Pure honest-naming rename within `oya-http-*` building blocks (`:33-46`). Crate rename `oya-http-deadline-middleware-domain → oya-http-latency-budget-middleware-infrastructure` (`:37-38`) is internal; no topology/eventing/identity/foundry conflict.
- **Lens 2 (hyperscaler):** Naming-must-not-lie-about-behavior is exactly the F5 multispectrum quality bar + Linus-taste the founder canon endorses (D-DOCTRINE "Linus-Torvalds taste ... no special cases"). GOOD.
- **Lens 3 (patterns):** EXEMPLARY: documentation-as-test (`slow_handler_runs_before_504...` proving post-hoc semantics `:44-46`); names match behavior. KEEP.
- **Disposition:** **KEEP** — no canon conflict. Model honest-naming ADR.

---

### ADR-0094 — `Handler` trait with associated `Error` type — **KEEP**

- **Lens 1 (contradictions):** None vs canon. Additive, non-breaking typed-handler trait in `oya-http-middleware-kernel` (`:34-49`); coherence-aware (no-blanket-impl rationale `:70-74`). Consistent with ADR-0083 typed-error tiers (errors as `Into<HttpResponse>`).
- **Lens 2 (hyperscaler):** Typed error vocabulary with type-driven rendering is idiomatic-Rust best practice; additive migration "at each author's pace" (`:65`) is the right low-blast-radius approach.
- **Lens 3 (patterns):** EXEMPLARY: additive evolution, adversarial fixtures (`:84-92`), explicit "why not blanket impl"/"why Into not Box<dyn Error>" reasoning. ANTI-PATTERN (minor, self-noted `:117`): two ways to define a handler — style guide must pick one.
- **Disposition:** **KEEP** — no canon conflict.

---

### ADR-0095 — `TenantSlug` centralized in `oya-tenancy-kernel` — **KEEP**

- **Lens 1 (contradictions):** None vs canon. Moves tenant-slug grammar from HTTP middleware into the tenancy kernel newtype (`:44-83`); clean inward dep (infrastructure→kernel `:134`) per ADR-0056. Note: `tenant slug` vs `tenant_class`/`tenant-tier` — this is the customer-facing identifier, NOT the tier vocabulary D12 namespaces, so no D12 conflict. The `TenantId`/`TenantSlug` split (`:86-94`) is correct defense-in-depth.
- **Lens 2 (hyperscaler):** Distinct internal-id vs customer-facing-slug types (compiler refuses confused-deputy) is exactly the type-safety hyperscalers enforce at trust boundaries. EXEMPLARY.
- **Lens 3 (patterns):** EXEMPLARY: single-source-of-truth grammar, defense-in-depth, homoglyph + path-traversal rejection fixtures (`:107-110`), parity test proving kernel==middleware (`:116`). KEEP.
- **Disposition:** **KEEP** — no canon conflict. Model boundary-type ADR.

---

## Disposition summary

| Disposition | ADRs | Count |
|---|---|---|
| **KEEP** (no/minor amend) | 0051, 0055, 0060, 0061, 0083, 0090, 0092, 0093, 0094, 0095 | 10 |
| **AMEND** | 0056, 0058, 0059, 0062, 0063, 0064, 0065, 0066, 0067, 0069, 0091 | 11 |
| **SUPERSEDE** | 0053 | 1 |
| **ARCHIVE** | 0057 | 1 |
| (SKIPPED — not Accepted) | 0052 Superseded, 0054 deprecated | 2 |

## Cross-cutting themes (drive the amendment lanes)

1. **Flat-`crates/` topology vs D-PURESPLIT `{oya,cloud}` two-tree** — the single biggest contradiction; touches 0056/0058/0059/0060/0061/0062/0063/0064/0065/0066/0067/0092. (A-STRUCT lane.)
2. **Kafka on the critical path vs D-EVENT Pulsar + D-D1 Kafka-off-critical-path** — 0059/0060/0062/0091. (A-CI/data-lane.)
3. **`foundry` dead-context naming vs D-INTEL/D-FOUNDRY-CLARIFY** — 0051(W-Foundry-Preview)/0053/0056/0058/0062/0065/0066/0067/0069/0091. (A-FOUNDRY lane.)
4. **GitHub-Actions-as-canonical-CI vs D3/D-CICD oya-ci** — 0063/0066/0067. (A-CI lane.)
5. **Dangling / wrong-filename / colliding ADR edges vs D11 integrity sweep** — 0057→absent-0055-v3 (CRITICAL), 0069→absent-0088 + wrong filenames for 0056/0067 (HIGH). (A-INTEGRITY lane.)
6. **Day-1-hyperscale premature complexity vs D8/D-SEQ M0-gated sequencing** — 0062 (mandate), echoed 0061/0066/0067. (Re-frame, not cut — D9 sequencing answer.)
7. **Dead/retired tooling mandated live** — 0053 (grit/icm, retired by 0116/0363/D-AEC-DECLINE). (A-FOUNDRY/coordination.)
8. **Un-wired `oya-governance-*` / convention-only enforcement claimed as enforced** — 0083 (`oya-governance-error-boundary`, `## Code Review`). (D-DOCTRINE robust-not-false.)
