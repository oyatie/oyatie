# Accepted-ADR audit — range ADR-0393 .. ADR-0515

**Scope:** every `status:Accepted` ADR with id in [ADR-0393 .. ADR-0515] in `/Users/jasonlee/Developer/source/docs/decisions/`.
**Method:** READ-ONLY scan of the ACTUAL files (the machine index `docs/machine-readable/decisions.json` is DRIFTED — it tops out at ADR-0392 and contains none of 0476/0515/etc., verified). Three lenses per ADR: (1) CONTRADICTIONS vs cited/related ADRs + the founder canon `decision-record-oyatie-canon.md`; (2) HYPERSCALER-LENS; (3) PATTERNS/ANTI-PATTERNS. Disposition: KEEP / AMEND / SUPERSEDE / ARCHIVE.
**Date:** 2026-06-06.

---

## Coverage ledger (NO SILENT CAPS)

**In-range files found (19):** 0393, 0394, 0408, 0476, 0478, 0479, 0480, 0481, 0482, 0506, 0507, 0508, 0509, 0510, 0511, 0512, 0513, 0514, 0515.

**Audited — status:Accepted (13, all FULLY read end-to-end):**
0393, 0476, 0478, 0479, 0480, 0481, 0482, 0506, 0507, 0508, 0509, 0512, 0515.

**SKIPPED (per scope = non-Accepted, listed not audited):**
- `status:Proposed` → **0394** (bespoke-rust-idp-central-hub), **0408** (buck2-driven-ci-cd), **0510** (scm-bespoke-hyperscaler-destination-cutover-trigger).
- `status:Superseded` → **0511** (argo-workflows; superseded_by 0515), **0513** (oya-ci-bespoke-rust-prow; superseded_by 0515), **0514** (build-ci-cd-hyperscaler-remediation; superseded_by 0515).

**Read-completeness:** all 13 Accepted files read in full (no truncation). Cross-reference existence checks run against the live directory (347 ADR files) + decisions.json.

---

## ★ CRITICAL SYSTEMIC FINDING (spans the whole 0476–0482 family) — phantom-ADR dangling-reference epidemic

This is the dominant finding of the range and is rated **CRITICAL**. The Accepted "bespoke substrate" family (0476/0478/0479/0480/0481/0482) and the crypto trio (0506/0507/0508) cite, as load-bearing `supersedes`/`related`/integration references, **a large set of ADR numbers that DO NOT EXIST in this source tree**. Verified by directory scan:

- The decisions dir jumps **0394 → 0408 → 0476** — there is a hard gap **ADR-0395..0407, 0409..0475** (only 0408 present in 04xx below 0476).
- **Confirmed-missing ADR ids that are cited as real:** `0409` (oya-vcs — cited by 0476 D4, 0479 related, 0481 D3/D5/integration, 0482 Tier-1), `0421` (Keycloak — `0476.supersedes:[ADR-0421]` + cited ~6×), `0411` (Crossplane — cited by 0478/0479/0480/0481), `0416` (Connect-RPC — 0476/0478), `0397` (Pulsar — 0476/0478/0479/0480), `0406` (Postgres — 0476/0478/0479/0481), `0403`/`0420`/`0423`/`0428`/`0429`/`0434`/`0441`/`0443`/`0449`/`0451`/`0457`/`0418` and more — **none exist on disk**. `0477`/`0474`/`0475`/`0483`/`0484` (cited by 0482) — **none exist**.
- These ids appear in **NEITHER the filesystem NOR `decisions.json`** (which itself stops at 0392). So they are not merely index-drift; they are authored against a numbering universe absent from this tree.

**Why CRITICAL:** this is a direct, wholesale violation of **D13** (no-dangling-ref invariant) and **D-DOCTRINE** ("Total accounting — every file/doc/folder accounted-for AND justified … Unaccounted/unjustified ⇒ blocks"). A reader cannot follow `0476.supersedes:[ADR-0421]` to verify what was superseded; the supersession graph is unverifiable. The most likely cause is the documented branch/worktree churn (the 0476-0482 family was authored on a branch with a different numbering universe and partially landed here). This must be resolved (re-point every dangling edge to the real id, or RENUMBER + author the missing decisions) **before** any masterplan generation or "total-accounting" gate can be honest. Per-ADR findings below tag each instance.

Evidence (verbatim):
- `0476.md:9` → `supersedes: [ADR-0421]`; `0476.md:18` → "Supersedes ADR-0421 (Keycloak)." — **ADR-0421 file does not exist** (`ls ADR-0421*` → no matches).
- `0476.md:70` → "**oya-vcs** (ADR-0409): Forgejo human browser auth" — **ADR-0409 does not exist**.
- `0482.md:54` → "| oya-vcs (ADR-0409) | Forgejo (ADR-0363) | …" — phantom 0409.

---

## Per-ADR findings

### ADR-0393 — Leptos canonical app-shell frontend — disposition: **AMEND**

- **LENS 1 (contradictions):**
  - `0393.md:46,129` cites **ADR-0394** ("bespoke-Rust IDP central hub — the portal whose shell this ADR makes canonical") as the consumer that this ADR unblocks. ADR-0394 is **status:Proposed** (verified) — an Accepted ADR is load-bearing-on / declares it "unblocks" a still-Proposed ADR. MED.
  - `0393.md:30` says "flip the `oya-ci-deck` reference (ADR-0513) to Leptos" — ADR-0513 is now **Superseded by ADR-0515**; the deck reference should re-point to 0515. LOW–MED.
  - No contradiction with the founder canon: Leptos/Rust-WASM end-to-end is consistent with D-DOCTRINE Rust doctrine and ADR-0394's Node/React-forbidden stance. Door is correctly `two-way`.
- **LENS 2 (hyperscaler):** Sound. The ADR's own honest accounting (`0393.md:111-114`) names the real costs (WASM cold-start floor, WASM↔DOM transcode tax, scarcer Rust-frontend talent) and accepts them for an *internal operator console* — this is a defensible, scoped bet, NOT over-engineering. A hyperscaler would not ship Rust/WASM for a *massive-scale consumer* surface, but the ADR explicitly scopes this to the internal IDP shell (`0393.md:100`), so the lens passes. Good discipline.
- **LENS 3 (patterns):** EXEMPLARY pattern to KEEP — clean-supersede-not-third-amendment rationale (`0393.md:74-76,102`), honest sunk-cost accounting, and the "code drifted, not the decision" framing. Anti-pattern it *fixes*: two parallel app-shell stacks (SolidJS prod + Leptos "mock"). One real residual anti-pattern: the actual file moves/deletions are deferred to "an implementation follow-up" (`0393.md:90,122-124`) — decision/code drift risk persists until that lands.
- **Disposition rationale:** KEEP the decision; AMEND the stale downstream pointers (0513→0515; flag 0394-is-Proposed dependency).

### ADR-0476 — oya-identity bespoke human identity — disposition: **AMEND (high-priority)**

- **LENS 1 (contradictions) — CRITICAL/HIGH:**
  - **CRITICAL:** `supersedes:[ADR-0421]` (`:9`) + "Supersedes ADR-0421 (Keycloak)" (`:18`) — **ADR-0421 does not exist**. The actual canonical OIDC IdP ADR is **ADR-0187 (Zitadel, Accepted)** (verified: `ADR-0187-canonical-oidc-idp-zitadel-primary.md`). This is exactly canon **D5 / C-4**: *"0476 `supersedes:[0187]` (fix the 0421 mis-number); 0187 status → superseded-as-endpoint/bridge-retained; resolve the hard contradiction (C-4)."* 0476's supersession edge points at a phantom.
  - **CRITICAL:** Zitadel is listed only as a **rejected alternative** — `0476.md:103` → "| **Zitadel** | Go-based; newer; smaller federation adoption; same Go-stack objection |". This **directly contradicts canon D5**: *"Zitadel = the vendored OIDC bridge (0187 demoted from canonical → Phase-1 bridge)."* The ADR rejects the very component the founder ruled IS the transition bridge. The ADR instead names **Keycloak** (a phantom 0421) as the Phase-1 bridge.
  - **CRITICAL (phantom refs):** `:70` oya-vcs **ADR-0409** (missing); related list `:11` cites 0411/0416/0406/0434 (all missing). D11(c)/D13 dangling edges.
  - **HIGH (Forgejo):** `:70` "oya-vcs (ADR-0409): **Forgejo** human browser auth" — contradicts **D2 / D-FORGE-CLARIFY** (Forgejo DROPPED entirely; GitHub-interim → bespoke Sapling-inspired SCM). Forgejo must be eradicated here.
  - MED: `:47,128` Cedar "evaluates every authorization decision" — consistent with canon **D6** (Cedar = permanent contract) but the ADR treats Cedar as the engine; per D6 the owned PARC engine evaluates behind the Cedar contract. Retarget engine language.
- **LENS 2 (hyperscaler):** Owning the human-identity plane IS the hyperscaler pattern (`:24-27,87-95` Google Identity Platform / AWS IAM Identity Center) — correct on substance. BUT the ratchet is mis-stated: the bridge should be **Zitadel** (already adopted, 0187), not Keycloak/JVM (a phantom regression). Hyperscalers do NOT re-platform onto a *new* OSS bridge (Keycloak) en route to bespoke when an OIDC bridge (Zitadel) is already chosen — that is churn, not a ratchet. UNDER-engineering risk: SAML 2.0 deferred to Phase-2 (`:57`) is fine, but tenant-IdP-federation as table-stakes is correctly Phase-1.
- **LENS 3 (patterns):** KEEP the bespoke-endpoint + parity-gated-cutover pattern (build-first-cutover-later, `:76-78`) — matches D-META. ANTI-PATTERN: the alternatives table actively argues *against* the founder-ruled bridge (Zitadel), i.e. the ADR's reasoning is stale relative to the ruling it must obey.
- **Disposition rationale:** Decision (bespoke oya-identity endpoint) is canon-aligned (D5) → AMEND not supersede: fix `supersedes:[0421]`→`[0187]`; flip Zitadel from rejected→adopted Phase-1 bridge; eradicate Forgejo/oya-vcs(0409); re-point all phantom related ids; retarget Cedar-engine language to PARC contract.

### ADR-0478 — oya-billing bespoke billing engine — disposition: **AMEND**

- **LENS 1:** `supersedes:[ADR-0457]` (Lago) — **ADR-0457 does not exist** (CRITICAL dangling). `related` cites 0411/0443/0451/0416 — all missing (`:11`). `:41,111` "Flat single-concern layout per **ADR-0131 + ADR-0132**" and `microservices/oya-billing/` home (`:37`) — contradicts **D-PURESPLIT / ADR-0512** (services live under `{oya,cloud}/<service>/crates/`, NOT `microservices/`; 0512 amends 0131). HIGH. `:84` cites ADR-0509 single-crate pattern — but 0509 is itself superseded by 0512 (see below) — stale anchor. MED.
- **LENS 2:** Owning billing IS the hyperscaler pattern (`:69-77` Stripe/Shopify/AWS) — substantively correct, and the AGPL-3.0-avoidance rationale (`:26-28`) is a real, hyperscaler-grade license concern (billing exposed as a product primitive ⇒ AGPL redistribution risk). Stripe-as-default-processor with `PaymentAdapter` trait (`:46-48`) is the right abstraction (multiple genuine backends justify the port, satisfying 0509 D4/D5). No over-engineering. GOOD.
- **LENS 3:** EXEMPLARY: license-risk-driven build-vs-buy reasoning; billing-as-a-product multi-tenant framing (`:64-67`) aligns D-LAYER (cloud dogfood). ANTI-PATTERN: home under `microservices/` + ADR-0131/0132 anchors are pre-pure-split.
- **Disposition:** AMEND — fix phantom `supersedes:[0457]`, re-home to `oya/` or `cloud/` per 0512, re-point 0509→0512.

### ADR-0479 — oya-meter bespoke usage metering — disposition: **AMEND**

- **LENS 1:** `supersedes:[ADR-0429]` (OpenMeter) — **0429 missing** (CRITICAL dangling). `related:[…0193,0397,0083,0411,0403,0420,0449,0131,0132,0509]` — 0397/0411/0403/0420/0449 all **missing** (`:11`). `microservices/oya-meter/` + ADR-0131/0132 flat layout (`:40-41`) — pre-pure-split, contradicts 0512/D-PURESPLIT. HIGH. ClickHouse (ADR-0193, exists) for time-series is consistent with canon D4 (ClickHouse = vendored OLAP bridge behind a port) — but the ADR treats it as endpoint, not transitional-vendored; tag per D4. MED.
- **LENS 2:** Owning metering IS the hyperscaler pattern (`:22-23,87` AWS Metering Service / GCP Usage Tracking) — correct. Bounded-scope claim (`:27-29` "~4-6 months", token/compute/storage/message/cache-hit) is honest right-sizing, NOT over-engineering. The meter table `:54-61` references `cloud-intelligence` token metering — consistent with canon D-INTEL (cloud-intelligence is the metered substrate). GOOD.
- **LENS 3:** KEEP the single-downstream-sink integration shape (`:107-113`). ANTI-PATTERN: same microservices/ home + 0131/0132 anchors; ClickHouse-as-endpoint vs canon's vendored-behind-port framing.
- **Disposition:** AMEND — phantom `supersedes:[0429]`, re-home per 0512, tag ClickHouse transitional-vendored (D4).

### ADR-0480 — oya-cost bespoke K8s cost allocation — disposition: **AMEND**

- **LENS 1:** `supersedes:[ADR-0443]` (OpenCost) — **0443 missing** (CRITICAL dangling). `related` 0407/0411/0423 + body `:36` OpenSLO ADR-0441, `:59` Mimir ADR-0383 (exists), Karpenter ADR-0423 (missing), Mimir ADR-0383 vs `:60` cites ADR-0383 ✓ but `:35` references "Mimir metrics" with no id and `:59` "(ADR-0383)" — internal inconsistency. `microservices/oya-cost/` home (`:28`) — pre-pure-split. HIGH on dangling, MED on home.
- **LENS 2:** Owning cost-allocation matches AWS Cost Explorer / GCP Cost Recommendations (`:22`). The "three primitives" right-sizing (`:24` node-price + pod-fraction + attribution labels) is honest and argues the bespoke surface is *smaller* than the OpenCost operator — credible. BUT: cost-allocation is the *least* differentiated of the 0478-0481 family; a hyperscaler-lens skeptic would ask whether oya-cost should be a *subsystem of oya-meter* rather than a separate service (the ADR itself rejects "extend oya-billing with metering" on single-concern grounds at 0479:95, yet oya-cost↔oya-meter coupling is tight: `:62-63` "oya-cost publishes per-request cost signals consumed by oya-meter"). Possible premature service-split. MED (over-engineering candidate).
- **LENS 3:** KEEP cost-as-SLI (`:36`) — exemplary (cost becomes a first-class objective). WATCH anti-pattern: service-proliferation where a subsystem would do (oya-cost is 3 primitives feeding oya-meter).
- **Disposition:** AMEND — phantom `supersedes:[0443]`; re-home per 0512; reviewer should pressure-test oya-cost-as-separate-service vs oya-meter-subsystem against D8 capacity budget.

### ADR-0481 — oya-flags bespoke feature flag server — disposition: **AMEND**

- **LENS 1:** `amends:[ADR-0428]` (flagd/OpenFeature) — **0428 missing** (CRITICAL dangling amend edge). Front-matter deliverables cite **oya-vcs ADR-0409** (`:23` D3, missing → Forgejo GitOps repo, also D2/D-FORGE violation), **Buck2 CI ADR-0408** (`:23,24` — 0408 is **Proposed**, an Accepted ADR's BLOCKER gate depends on a Proposed build ADR), **ADR-0434** Self-Service UI (`:31`, missing), ADR-0384 cloud-intelligence (exists). `microservices/oya-flags/` home (`:15,71`) — pre-pure-split. HIGH.
  - Internal good sign: `related` correctly cites ADR-0476 (oya-identity, exists) for auth.
- **LENS 2:** Bespoke flag server IS the Google Gatekeeper / Meta GateKeeper / Stripe pattern (`:50-60`) — correct, and OpenFeature-SDK-as-stable-contract while owning the server (`:132`) is precisely the right "adopt the contract, own the engine" posture (mirrors canon D6 Cedar-contract logic). Sub-ms in-process eval (`:76,112`) is a real hyperscaler concern (no per-request DaemonSet hop). GOOD. No over-engineering.
- **LENS 3:** EXEMPLARY: standard-protocol-contract + owned-server pattern; flag-as-code/GitOps audit-trail. ANTI-PATTERN: flag bundles homed in "oya-vcs (ADR-0409) Forgejo GitOps repo" (`:23,77`) — double violation (phantom id + dropped-Forgejo). Buck2-CI BLOCKER gate (`:24`) depends on Proposed 0408.
- **Disposition:** AMEND — fix phantom `amends:[0428]` + 0409/0434; eradicate Forgejo; resolve 0408-is-Proposed dependency; re-home per 0512.

### ADR-0482 — Bespoke Substrate Roadmap (doctrine meta) — disposition: **AMEND (keystone doctrine — handle carefully)**

- **LENS 1 — HIGH:**
  - The entire Tier-1 table (`:52-64`) is built on phantom ids: oya-vcs **0409**, Rust-portal **0434**, oya-notify **0451**, oya-errors **0474**, oya-status **0475**, oya-identity 0476 (exists), oya-code-search **0477**, oya-billing 0478 (exists), oya-meter 0479, oya-cost 0480, oya-flags 0481. Of 11 Tier-1 rows, **≈6 cite non-existent ADRs**. The roadmap's own scaffolding is unverifiable.
  - `:54` oya-vcs supersedes **Forgejo (ADR-0363)** "Parallel-run; tenant opt-in" — contradicts **D2 / D-FORGE-CLARIFY** (Forgejo DROPPED, not a parallel-run bridge). The roadmap encodes the stale Forgejo-as-bridge model the founder explicitly killed. HIGH.
  - `related:[…0388,0392,0409,0434,…]` mix of Proposed (0392/0408) and phantom (0409/0434/0451/0474/0475/0477/0483/0484). Dangling-edge epidemic.
  - The 2026-05-28 kubers amendment (`:141-162`) hard-codes absolute developer paths (`/Users/jasonlee/Developer/kubers`, `:143,158`) into a canon doctrine ADR — non-portable, machine-specific. MED.
- **LENS 2:** The *doctrine* (OSS-as-bridges, bespoke-as-destination, parallel-run + parity-gated cutover, no-hard-deadline) is exactly canon **D-META** (own-the-endpoint / vendor-the-bridge / ratchet-when-proven) and **D-DOCTRINE** — substantively the strongest, most hyperscaler-aligned ADR in the range. The Tier-1..4 sequencing (`:50-101`) correctly *defers* kernel/OS to Tier-4 and warns against premature Tier-4 (`:121-124`) — good. BUT: "Any upfront investment is acceptable … Timeline is the only constraint" (`:114-117`) is a UNDER-disciplined cost stance that **contradicts canon D8** (capacity budget — "own everything" ≠ "build everything at once"; oya-ci is the prioritized crown-jewel, builds are capacity-bounded). The roadmap lists ~30 parallel bespoke tracks with no capacity gate. HIGH (this is the over-engineering risk D8 was written to bound).
  - SPOF/scale: Tier-3/4 (oya-events, oya-search, oya-workflow, oya-graph, oya-clickhouse, oya-kernel, oya-hypervisor) is an ENORMOUS owned-everything surface; a real hyperscaler owns these *selectively over decades with revenue*, not as a declared 30-item roadmap. Canon D8/D-SEQ exists precisely to sequence this — the ADR must cite/obey it.
- **LENS 3:** EXEMPLARY pattern to KEEP: bridge-discipline checklist (`:104-112` parallel-run / parity-table / opt-in / rollback / no-hard-deadline) and the `bespoke_replacement_planned` frontmatter convention (`:131-137`). ANTI-PATTERN: unbounded-ambition without the capacity gate (D8); machine-specific absolute paths in canon; phantom-id roadmap rows.
- **Disposition:** AMEND — this is keystone doctrine that the founder canon *agrees with*; do NOT archive. Fix: re-point/renumber all phantom Tier-1 rows; replace Forgejo-parallel-run with GitHub-interim→bespoke-SCM (D2/D-FORGE); insert the D8 capacity-budget gate ("acceptable cost" → "capacity-bounded, oya-ci-prioritized, sequenced per D-SEQ"); strip machine-specific absolute paths.

### ADR-0506 — aws-lc-rs canonical crypto provider (Phase-1) + oya-crypto Tier-4 — disposition: **KEEP (minor AMEND)**

- **LENS 1:** Clean. `related:[ADR-0482]` (exists). The phasing (aws-lc-rs bridge → oya-crypto Tier-4 endpoint) matches canon D-META and the founder D5 note that crypto libs are "reused behind ports (foundational, not differentiators)." `door:two-way` is appropriate for a one-line provider swap. No phantom ids. Only minor: machine-specific path `/Users/jasonlee/Developer/kubers` (`:105,129`) in cutover-gate text. LOW.
- **LENS 2:** EXEMPLARY hyperscaler reasoning: aws-lc-rs is *literally what AWS uses internally* (`:53`), drop-in ring-compatible, FIPS-140-3-validatable — adopting it now is the *correct* "adopt-what-should-be-adopted" call, and owning oya-crypto later is correctly gated behind kubers Phase-B + FIPS validation (`:100-106`). Constant-time + formal-verification (Kani/Creusot) as the bespoke bar (`:94`) is appropriately ambitious-but-deferred. This is the own-when-proven ratchet done right. No over/under-engineering.
- **LENS 3:** KEEP — the mandatory feature-parity-target table (`:85-97`) is the exemplary artifact the whole bespoke doctrine (0482) requires; this ADR models it best (concrete per-primitive bars + post-quantum ML-KEM/ML-DSA migration path).
- **Disposition:** KEEP; trivial AMEND to de-hardcode the absolute kubers path.

### ADR-0507 — webauthn-rs canonical RP (Phase-1) + oya-webauthn Tier-2 — disposition: **KEEP (minor AMEND)**

- **LENS 1:** Clean. `related:[0476,0482,0508]` — all exist. Consistent with canon D5 (webauthn-rs reused behind ports). One stale anchor: `:59-62,119-120` prescribes consuming webauthn-rs via "oya-identity's `oya-identity-webauthn-*` **use-case crates** … `domain/usecase/api/adapter-postgres/rest/grpc/app`" per "PR #289 canonical clean-architecture pattern" — this is **exactly the per-use-case 7-crate sprawl that ADR-0509 (and then 0512) SUPERSEDED**. Internal contradiction within the same Accepted set. MED.
- **LENS 2:** Correct — webauthn-rs (Firstyear/Kanidm) is the Rust-ecosystem canonical RP; adopting it while owning oya-webauthn later (gated on parity-table-green + oya-identity Phase-2) is the right ratchet. MPL-2.0 is correctly flagged as file-level-copyleft-only (`:50`) — honest license diligence. No over-engineering.
- **LENS 3:** KEEP — closed-loop identity framing (RP 0507 + authenticator 0508) is a genuinely good architecture pattern; mandatory parity table (`:94-107`) present. ANTI-PATTERN: the 7-crate per-use-case scaffold prescription is stale (superseded by 0509/0512).
- **Disposition:** KEEP; AMEND the scaffold prescription to single-crate/bounded-context per 0512.

### ADR-0508 — OpenSK canonical authenticator reference (Phase-1) + oya-authn-device Tier-3 — disposition: **KEEP (AMEND for altitude)**

- **LENS 1:** Clean refs (`related:[0482,0507,0506,0483,0484]`) BUT **0483 and 0484 do not exist** on disk (cited `:172-173`) — dangling (MED). Otherwise consistent with D-META.
- **LENS 2 — over-engineering flag (MED):** Owning the RP + authenticator firmware (OpenSK fork) is defensible (Google Titan / Apple Secure Enclave / MS Pluton precedent, `:31-37`). BUT this ADR pushes to **owned SILICON** (OpenTitan SoC, custom secure element, a manufacturing line with per-device root-CA attestation injection, `:79-92,143-152,159-161`). For a pre-revenue platform this is the *furthest-out* ambition in the entire range (Tier-3→4, 24-60mo+). A hyperscaler reaches owned authenticator silicon *after* massive scale + revenue (Apple/Google did). Per canon D8/D-SEQ this is fine ONLY if explicitly sequenced behind an M0 evidence-gate — the ADR correctly defers it to Tier-3/4 with parity gates (`:114-123,129-153`), so it does not *violate* canon, but it is the prime candidate for "is this premature complexity?" scrutiny and must be bound to the D8 capacity budget.
- **LENS 3:** KEEP the phased silicon roadmap + parity table (exemplary deferral discipline). WATCH: scope creep toward hardware/manufacturing ADR families (`:159-161`) — ensure these stay DEFERRED, not pulled forward.
- **Disposition:** KEEP; AMEND to (a) fix dangling 0483/0484 refs, (b) explicitly bind the silicon ambition to the D8 capacity gate / D-SEQ M0 sequencing so it cannot be pulled forward.

### ADR-0509 — Hyperscaler service decomposition (single-crate-per-service) — disposition: **SUPERSEDE (mark Superseded — edge is dangling)**

- **LENS 1 — HIGH (status drift):** **ADR-0512 explicitly supersedes ADR-0509** — `0512.md:10-11` `supersedes: [ADR-0357, ADR-0509]`, `:22` "Supersedes ADR-0357 … and ADR-0509 (single-crate-per-service)." YET **0509 still carries `status: Accepted` and `superseded_by: []`** (verified `0509.md:4,10`). This is a **dangling/incorrect supersession edge** — a no-dangling-ref / status-enum invariant violation (D11(c), D13). Two live ADRs (0509 "single-crate-per-service" vs 0512 "crate = bounded context, possibly multiple crates per service") give *contradictory* granularity rules; 0509 must be flipped to `Superseded`, `superseded_by:[ADR-0512]`. Compounding: 0476/0478/0479/0480/0481 all cite "ADR-0509 alignment" as their layout authority (e.g. `0476.md:137-139`) — they anchor to a superseded ADR.
- **LENS 2:** The *analysis* in 0509 is strong and hyperscaler-grounded (`:57-85` Google Bazel one-target-per-binary, Meta Buck2, Amazon, Stripe, Tailscale/Vector/Pulumi — per-use-case 7-crate sprawl has no FAANG equivalent). 0512 KEEPS this verdict (rejects layer-per-crate) but REFINES it (crate=bounded-context, not rigidly one-crate-per-service) — so 0509's substance survives inside 0512; only its over-strict "exactly one crate" rule and its `microservices/` home were corrected.
- **LENS 3:** The survey-the-real-hyperscalers method is EXEMPLARY (keep it, now embodied in 0512). ANTI-PATTERN: leaving a superseded ADR marked Accepted while five other Accepted ADRs cite it as authority.
- **Disposition:** SUPERSEDE — set `0509.status:Superseded`, `superseded_by:[ADR-0512]`; re-point the 0476-0481 "ADR-0509 alignment" anchors to 0512.

### ADR-0512 — Canonical monorepo pattern (vertical-slice, one workspace, bounded-context crates, Buck2 graph) — disposition: **KEEP**

- **LENS 1:** STRONGLY canon-aligned. `supersedes:[0357,0509]`, `amends:[0131]` (`:9-13`) — all correct, and the **2026-06-02 pure-split amendment** (`:24-28`) already encodes canon **D-PURESPLIT**: "Canonical service homes are `{oya,cloud}/<service>/` … `microservices/` is legacy only." `:57` "A flat top-level `crates/` directory is **forbidden**. … The `foundry` name remains eradicated." — aligns D-PURESPLIT + D-FOUNDRY. The build-graph clauses cite Buck2 0392/0408 (`:22,61`) which are **Proposed** — an Accepted monorepo ADR depends on Proposed build ADRs (MED; same 0392/0408-not-ratified issue that pervades the range).
- **LENS 2:** EXEMPLARY hyperscaler doctrine: one-version monorepo, Buck2-is-rustc-direct (`:47`), crate=bounded-context as the build-parallelism dial, dependency-rule-as-visibility, no nested `[workspace]` tables (`:58`). This is Google/Meta monorepo taste applied correctly (canon D-DOCTRINE "Google/Meta monorepo doctrine + Linus taste"). The hard process rule (`:68` "a structural migration of this class MUST run as a dedicated, exclusive, post-acceptance change on a stable tree … never merged concurrently in a PR drain") is a real, hard-won operational lesson — KEEP.
- **LENS 3:** EXEMPLARY: clean-architecture-as-modules-not-crates, proportional-to-complexity (`:60` "a CRUD service does not earn five layers" — directly fixes the 0507 7-crate-scaffold anti-pattern), mechanically gate-enforced layout (`:62`). This is the best-engineered ADR in the range.
- **Disposition:** KEEP. (Only watch item: it depends on Proposed 0392/0408 — ratify those, see cross-cutting below.)

### ADR-0515 — oya-ci / oya-cd unified Rust-native CI/CD — disposition: **AMEND (Forgejo eradication) — otherwise canon-aligned**

- **LENS 1 — HIGH (Forgejo) + MED (deps):**
  - **HIGH:** Forgejo is baked into the design: `:76` Face A "**Forgejo-native** webhook → CloudEvent" and `:96` "`cloud/cloud-scm` — the bespoke VCS destination (**Forgejo transitory** → bespoke; the `ForgeAdapter` seam)." This is the **exact contradiction the founder canon self-flags in D-FORGE-CLARIFY**: *"ADR-0515's 'Forgejo-native'/`ForgeAdapter` prose … I propagated stale Forgejo framing into ADR-0515 without reconciling against D2 … FIX = a systematic Forgejo-eradication sweep: amend … ADR-0515."* Must become GitHub-interim → bespoke Sapling-inspired SCM.
  - MED: `depends_on:[ADR-0408, ADR-0392]` (`:11`) — both **Proposed**. An Accepted, door:one-way "firewall keystone" ADR depends on two un-ratified ADRs. The supersession map (`:111`) correctly keeps 0408 as depends_on-not-superseded, but 0408 must be ratified for the dependency to be honest.
  - Supersession edges verified CORRECT: 0124 (`status:Superseded, superseded_by:[ADR-0515]` ✓), 0349/0359/0361/0511/0513/0514 all `Superseded` ✓. This is the cleanest supersession hygiene in the range.
- **LENS 2:** STRONG. "Adopt the patterns (Prow/Tekton/Argo-WF/Argo-CD/Argo-Rollouts), reimplement in Rust, never run the Go binary; reuse non-moat substrate behind ports" (`:71-83`) is precisely the canon D-CICD/D3 ruling and is hyperscaler-grade (own the build-graph brain/moat, REAPI/CAS/event-bus/cosign reused behind ports). The honest enforcement-façade diagnosis (`:55-56` "Net: 0 gates block a merge today") + the trustless trunk-sourced producer contract (`:88-93`, signature-IS-the-check, gate-config-can't-be-weakened-by-the-PR-it-gates) is exactly right (canon D-SEQUENCE firewall-first). MVP cuts (single-Postgres RunStore, serial Tide, CD-face-reuse-Argo-behind-port) avoid premature complexity — GOOD. No over-engineering; if anything it is disciplined *against* it.
- **LENS 3:** EXEMPLARY: the RED/GREEN proof-it-blocks verification section (`:127-132`), the claim-ceiling/no-false-green discipline (`:132`), own-when-proven ratchet on every owned claim. ANTI-PATTERN (sole, but real): stale Forgejo framing the founder already flagged for eradication; depends-on-Proposed-0408.
- **Disposition:** AMEND — eradicate Forgejo (→ GitHub-interim/bespoke-SCM per D2/D-FORGE-CLARIFY); ratify or explicitly gate the 0408/0392 dependency. Decision itself is the ratified canon (D-CICD/D3) → KEEP the substance.

---

## Cross-cutting findings (range-wide)

1. **CRITICAL — phantom-ADR dangling-reference epidemic** (0476/0478/0479/0480/0481/0482/0508): dozens of cited ids (0397/0406/0409/0411/0416/0418/0420/0421/0423/0428/0429/0434/0441/0443/0449/0451/0457/0474/0475/0477/0483/0484) exist in **neither the filesystem nor decisions.json**. The 0476-0482 family was authored against a numbering universe absent from this tree (consistent with the documented branch/worktree churn). Blocks D-DOCTRINE total-accounting + D13 no-dangling invariant. Must re-point or renumber+author before masterplan generation can be honest.

2. **HIGH — pre-pure-split homes** (0478/0479/0480/0481, and 0476 scaffold): all home under `microservices/oya-*` and anchor to ADR-0131/0132 + ADR-0509, which are superseded/amended by **ADR-0512 / D-PURESPLIT** (`{oya,cloud}/<service>/crates/`). These five must be re-homed and re-anchored to 0512.

3. **HIGH — Forgejo not eradicated** (0476:70, 0481:23/77, 0482:54, 0515:76/96): four Accepted ADRs still encode Forgejo as bridge/native, contradicting **D2 / D-FORGE-CLARIFY** (Forgejo DROPPED; GitHub-interim → bespoke Sapling SCM). The canon explicitly names 0515 as a fix target.

4. **HIGH — ADR-0509 status drift:** 0512 supersedes 0509, but 0509 is still `Accepted, superseded_by:[]`, and five Accepted ADRs cite it as layout authority. Flip to Superseded, re-point anchors to 0512.

5. **CRITICAL/HIGH — identity supersession is wrong:** 0476 `supersedes:[ADR-0421]` (phantom Keycloak) instead of the real **ADR-0187 (Zitadel, Accepted)**, and rejects Zitadel as an alternative — inverting canon **D5** (Zitadel = the vendored bridge; 0187 demoted, superseded-as-endpoint by 0476).

6. **HIGH — depend-on-Proposed:** Accepted ADRs 0512 and 0515 (and 0481's BLOCKER gate) depend on **ADR-0392 / ADR-0408 (both Proposed)**. The build substrate the entire monorepo + CI canon rests on is un-ratified.

7. **HIGH — unbounded-ambition vs capacity budget:** 0482 "Any upfront investment is acceptable; timeline is the only constraint" (~30 parallel bespoke tracks) contradicts canon **D8 / D-SEQ** (capacity-bounded, oya-ci-prioritized, sequenced). 0508's owned-silicon ambition is the sharpest instance.

8. **MED — machine-specific absolute paths in canon ADRs:** 0482/0506/0508 hard-code `/Users/jasonlee/Developer/kubers` into doctrine text — non-portable.

9. **EXEMPLARY patterns to KEEP (range-wide):** mandatory feature-parity tables (best in 0506); clean-supersede-not-nth-amendment (0393); standard-contract + owned-engine (0481 OpenFeature, 0476/0515 Cedar/REAPI, mirrors D6); survey-real-hyperscalers method (0509); crate=bounded-context + clean-arch-as-modules-proportional-to-complexity (0512); RED/GREEN proof-it-blocks + claim-ceiling (0515); bridge-discipline checklist + `bespoke_replacement_planned` convention (0482); trustless trunk-sourced gate, signature-is-the-check (0515).

---

## Disposition summary

| ADR | Disposition | Top finding |
|---|---|---|
| 0393 | AMEND | stale 0513→0515 deck ref; declares it unblocks Proposed 0394 |
| 0476 | AMEND (high-pri) | CRITICAL: supersedes phantom 0421 (should be 0187); rejects Zitadel (canon D5 bridge); Forgejo; phantom 0409 |
| 0478 | AMEND | phantom supersedes 0457; microservices/ home (D-PURESPLIT) |
| 0479 | AMEND | phantom supersedes 0429; microservices/ home; ClickHouse-as-endpoint |
| 0480 | AMEND | phantom supersedes 0443; possible premature service-split vs oya-meter |
| 0481 | AMEND | phantom amends 0428; Forgejo GitOps repo; BLOCKER on Proposed 0408 |
| 0482 | AMEND (keystone) | phantom Tier-1 rows; Forgejo-parallel-run; unbounded cost vs D8 |
| 0506 | KEEP (minor) | clean; de-hardcode kubers path |
| 0507 | KEEP (minor) | stale 7-crate scaffold prescription (superseded by 0512) |
| 0508 | KEEP (amend altitude) | owned-silicon premature-complexity; dangling 0483/0484; bind to D8 |
| 0509 | SUPERSEDE | status drift — 0512 supersedes it but it's still Accepted; 5 ADRs cite it |
| 0512 | KEEP | best-engineered; canon-aligned (D-PURESPLIT/D-DOCTRINE); depends on Proposed 0392/0408 |
| 0515 | AMEND | Forgejo eradication (canon-flagged); depends on Proposed 0408; otherwise ratified canon |
