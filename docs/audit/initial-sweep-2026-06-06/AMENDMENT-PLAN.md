# AMENDMENT-PLAN — Oyatie canon consolidation, staged parallel-lanes execution

**STATUS: pending approval (door:one-way founder sign-off).** Nothing in this plan executes against `jason931225/oyatie` until the founder signs the RALPLAN-DR sequencing decision (§A) and the per-lane door:one-way gates (§F). This is the build plan for amendment-phase task #13 — applying the ~30 ruled founder decisions to the **company monorepo** (`jason931225/oyatie`), NOT the linux pilot.

- **SSOT for rulings:** `synthesis/decision-record-oyatie-canon.md` (the ~30 D-decisions).
- **Registers:** `synthesis/01-ADR-DISPOSITION-TABLE.md` (372 rows), `synthesis/03-PROPOSED-RESOLUTION-LEDGER.md` (132 Proposed), `synthesis/04-DOMAIN-TAXONOMY.md` (16-enum + 28-fine), `docs-sweep/00-REST-OF-DOCS-REGISTER.md` (CC-1..CC-13 + 831-file mechanical sweep), `bominal-reconciliation/00b-INTERVIEW-AGENDA-CORRECTED.md` (owed-depth), `legacy-recovery/00-RECOVERY-REGISTER.md` (.Trash recoveries).
- **Date:** 2026-06-06.
- **Tooling reality flag (carried per step):** `oya gen masterplan` + the drift/cohesion/safety gates exist **only in `.claude/worktrees` (in-flight, NOT on `dev`)**. Every step that depends on them is marked **[ASPIRATIONAL]** (run manually now, automate at cutover) vs **[ENFORCED]** (a real CI lane exists on `dev` today). Build-first-cutover-later: Jenkins/Argo stay operative; nothing retired before its owned replacement is proven.

> **Round-1 revision note (2026-06-06).** This revision absorbs the round-1 Architect review (`_arch-round1.md`, APPROVE-WITH-CONDITIONS / S1–S6) and Critic review (`_critic-round1.md`, ITERATE / GATING G1–G5 + RECOMMENDED R1–R6). Both reviewers independently re-verified the same four headline facts against primary sources (census 731-105-in-register vs 831-43-in-SSOT; D-LANES door:one-way "not a serial chain"; D1 asserts generator-as-real with no aspirational caveat; cutover-trigger ownerless). Those facts are settled. The additions were a **base-data consistency predicate (§A.0)**, an explicit **D-LANES-narrowing tradeoff (§A.4)**, four **correctness gates** (census reconciliation, consolidation-set freeze, MAP self-consistency, DROP-reference-safety), and verification hardening (adversarial cohesion stand-in, testable exit-evidence). Per the Critic, **G1 census-reconciliation and G2 consolidation-set-freeze are hard predicates the irreversible lanes run against and MUST land before Wave-0 starts.**

> **Round-2 revision note (2026-06-06).** This revision absorbs the round-2 Architect review (`_arch-round2.md`, APPROVE-WITH-CONDITIONS, one new HIGH) and Critic review (`_critic-round2.md`, ITERATE, HIGH independently re-derived by grepping the SSOT). **The HIGH finding (FLAG-A / SY1): the plan's load-bearing ordering edge "L2 foundry-rename must precede L1-refound (rename-before-reauthor, driver D-1)" was asserted as `RULED` in three places (old lines 53/143/159) and line 53 quoted a "dependency note … to avoid double-work" as if from the SSOT — but `grep double-work` over `decision-record-oyatie-canon.md` returns ZERO, and re-reading D11 (`:52`, sweep-before-backfill only) + D13 (`:42-43`, re-founding with NO rename-ordering) confirms the SSOT rules no such edge.** I re-verified this myself this round (grep + line-reads, evidence in §A.5). The edge is *engineering-correct* (the double-work argument is sound and sufficient) but it is **DERIVED, not founder-RULED** — so the order does NOT change, but its **status** changes and the Option-1-vs-Option-2 wave-shape moves into the §F founder sign-off set as a genuine (not pre-decided) choice. Applied this round: a **`[RULED:<SSOT-line>]` vs `[DERIVED:<driver>]` provenance discipline on every ordering constraint** (mirroring the plan's own ASPIRATIONAL/ENFORCED tool discipline); the **fabricated quotation deleted** (old line 53); a **one-time ruling-provenance verifier pass (§A.6)** that checks EVERY RULED tag corpus-wide (the slip "overlap is real ⇒ ordering is ruled" could recur); **D-2 (map-before-sweep) stays RULED** (grounded in D13's no-dangling-ref invariant — a sweep against a moving id-space is incoherent), only D-1 drops; plus **SY2** (Wave-3 terminal whole-corpus cross-ref re-resolution — the corpus grows through Waves 1–2 under build-first, so "zero dangling" must be proven of the *delivered* graph, not the Wave-0 snapshot), **SY3** (L1.0-VERIFY extended to prove consolidation *completeness*, not just surjection, so the S1 L1-amend partition is provably total), **SY4** (L2 exit reconciles to the *post-L2.0b* census, not "831" which has a sampled tail until L2.0b), **SY5** (standing §G caveat: amendment-complete ≠ gates-live). The wave *order* (Option 1) is confirmed correct by both round-2 lanes and is unchanged.

> **Round-3 revision note (2026-06-06).** This revision absorbs the round-3 Architect review (`_arch-round3.md`, APPROVE-WITH-CONDITIONS, two new HIGH) and Critic review (`_critic-round3.md`, ITERATE, both HIGHs independently re-derived by grepping the SSOT). The two findings are the **same directional/one-artifact blind spot round-2 fixed, in the OPPOSITE direction and a second location** — closed STRUCTURALLY, not per-instance. **Finding 1 (RULED-deflation, HIGH):** round-2's §A.6 pass policed RULED-*inflation* only (downgrade a DERIVED edge wearing a RULED tag) and was structurally blind to RULED-*deflation* (a genuinely-ruled SSOT action left DERIVED). The instance: **L2's membership in the pre-backfill sweep is RULED by D11(d)+`:52`** — the foundry bulk-rename is a NAMED CONTENT of "the FULL sweep (runs before masterplan backfill …) Door: one-way (batch)" (verified verbatim, D11`:52`(d)) — yet the plan re-derived L2's Wave-0 placement through the weaker DERIVED D-1 driver. Fix: **§A.6 made BIDIRECTIONAL** (direction-2: walk every SSOT ruled-action forward into the plan, surface any left DERIVED); **L2's sweep-membership re-tagged `[RULED: D11(d)+:52]`**; **rename-vs-reauthor *internal sequencing* stays `[DERIVED: D-1]`** (the rename happening *inside the pre-backfill sweep* is RULED; whether *before or interleaved-with* re-authoring is the DERIVED choice); **Option 2's bounded legal shape restated** ("author shells first, THEN the still-pre-backfill sweep" — NOT "rename after re-foundation/backfill", which violates D11). **Finding 2 (consolidation-set knowability paradox, HIGH):** A.0-2/L1.0-CONS was door:one-way over a file-level fold map that is the OUTPUT of Wave-1 authoring (D3`:114-118` pins fold INTENT at cluster level, not a frozen file map) — a one-way door over a guess. Fix: **split the freeze** into a Wave-0 PROVISIONAL design-freeze (the cluster-level intent the SSOT genuinely rules) + a Wave-1 **L1.1-CONS-CONFIRM** checkpoint (confirm actual file-fold == provisional; delta = re-review trigger + revert-on-fold, NOT silent amend); S1 parallelism is now conditional on the Wave-1 confirm, not the Wave-0 freeze. Plus **Finding 3 (MEDIUM):** the Wave-3 terminal cross-ref barrier (Wave-3 backfill hard-blocks on the last Wave-2 supersession resolving on the delivered graph) is NAMED as a second acknowledged D-LANES serialization (D13`:43`). Plus **Finding 4 (LOW):** aspirational-tooling liveness carried as a queryable **`cutover_status:` field** (not a stale dated prose snapshot). The wave *order* (Option 1) is confirmed correct by both round-3 lanes a THIRD time and is unchanged.

---

## Δ — REFRESH DELTA (2026-06-07): five founder rulings folded into the lanes
> This plan was authored 2026-06-06. Five founder rulings landed after, plus the personal ADR verification completed (full corpus). They do **not** change the wave-ordering (Option 1) or the §A RALPLAN-DR — they **sharpen the content** of L2/L3/L4/L5 and add one new lane. Each carries its decision-record (DR) anchor. The Wave assignment of each is stated; the §A.6 bidirectional provenance pass re-runs over these new edges before sign-off.

- **Δ1 — Eradication set is now FOUR terms, not one (DR D-CLOUD-NATIVE forbidden-vocab clause).** `forgejo` · `foundry` · `jenkins` · `oya-vcs`/agentic-VCS are ALL forbidden vocab → removed from code/infra/specs/registry/masterplan/docs **and superseded/historical ADR bodies**; all four into the brand-residue gate. Carve-out: external "Palantir Foundry". jenkins = dropped CI bridge (GitHub-Actions live, D-CICD-AUTHORITY); oya-vcs = retired agentic-VCS (ADR-0363) — generic VCS/SCM survives only as `cloud-scm`. **Lane impact:** **L2 widens** from "foundry sense-routed rename" to a **4-term eradication** — foundry stays per-file *sense-routed* (intelligence vs governance), but forgejo/jenkins/oya-vcs are simpler *fixed replacements* (forgejo→GitHub-now/cloud-scm-dest; jenkins→GitHub-Actions/oya-ci; oya-vcs→cloud-scm). L2.3 adds all four to `oya-check-brand-residue`. `[RULED: DR D-CLOUD-NATIVE]`; **Wave-0** (mechanical, in the pre-backfill sweep, D11(d)+`:52`).
- **Δ2 — `oya gate` + `oya cli` (+ `oya check`/`oya verify`/`oya-dev-cli`/`oya git`) = STALE → SUPERSEDE, not amend (DR D-CLOUD-NATIVE oya-gate/oya-cli clause).** This is a *different operation* from Δ1 eradication: the CLI mechanism's ADRs are marked Superseded and the preserved gate/governance/honest-claim LOGIC (0129 honest-claims, 0135 aspirational-enforcement, etc.) is **re-authored as cloud-native CaC/CaaS pipeline ADRs**. The foundational grammar/enum (0056 `oya-check-*` BNF, 0092/0105 `cli` layer) is amended to the pipeline-gate convention. **Lane impact:** **NEW lane L8** (below) owns the supersede+re-home; it is ADR re-authoring (Wave-1/2), NOT a mechanical rename, so it does NOT live in L2. `[RULED: DR D-CLOUD-NATIVE + D-GOVERNANCE-CENTRAL]`.
- **Δ3 — Governance/evidence consolidates to PaC/CaC/PaaS/CaaS, central + automated, not CLI (DR D-GOVERNANCE-CENTRAL); LLM = cloud-intelligence domain (DR D-LLM-DOMAIN).** **Lane impact:** L4 (CC-2 Cedar→contract+PARC) + the new L8 converge on **one governance-pipeline ratifier ADR** (PaC=Cedar policy-as-code / CaC=compliance-as-code / PaaS=owned PARC PDP / CaaS=compliance pipeline); every `verified_by: "oya gate validate …"` field (found live in **ADR-0481:25,:33**, Accepted) re-homes to that pipeline. LLM re-homes to cloud-intelligence (already in the <400 verification). `[RULED: DR D-GOVERNANCE-CENTRAL, D-LLM-DOMAIN]`; **Wave-2** (new/reshaped ADRs).
- **Δ4 — D-EVENT sharpened: PULSAR from day 0 (NO Kafka interim) → owned-in-Rust best-in-class destination (DR D-EVENT, 2026-06-07).** Owned destination = object-storage-native/diskless + ecosystem-compatible wire (symmetric with the owned SQL engine). **Lane impact:** **L4 CC-6 + CC-9 re-resolve** — drop the `Kafka→Pulsar` *migration* framing (it was "migrate to Pulsar"); the fix is now "Pulsar from day 0" + the owned-Rust diskless destination recorded on the L1.1 data-tier ADR (D4/D-LAYER). ADR-0005 amended to Pulsar-from-day-0 (not "reconciled"). `[RULED: DR D-EVENT]`; **Wave-2** (CC fixes ride the data-tier ADR).
- **Δ5 — Two NEW integrity defects found in the personal verification (DR / 00-PERSONAL-ADR-VERIFICATION Batch 8).** (a) **PHANTOM ADR-0409** — `oya-vcs` cited "(ADR-0409)" by 3 *Accepted* ADRs (0476, 0481×2, 0482×2) but no ADR-0409 file exists (0405–0415 empty except 0408) → dangling ref to a forbidden concept. (b) **ADR-0509 asymmetric supersession** — ADR-0512 declares `supersedes:[…0509]` but 0509 is still `status: Accepted` with empty `superseded_by` (the flat-`crates/` carve-out at 0509:115 reads live only because the back-edge is missing). **Lane impact:** **L3.3 adds both** (the no-dangling-ref / supersession-completeness invariant — same class as the existing dangling-edge fixes; G-XA/G1 catches them). `[RULED: DR D13 no-dangling invariant]`; **Wave-0**.

> **Net:** wave-order unchanged; **L2 widens to a 4-term eradication**, **L3.3 gains 2 integrity fixes**, **L4 CC-6/CC-9 re-resolve to Pulsar-from-day-0**, and a **new L8 lane** owns the oya-gate/oya-cli SUPERSEDE + the CaC/CaaS governance-pipeline re-homing. The personal verification (full corpus, both bands + dropped items) is the disposition basis; **only 0102 SettingsTemplate + 0138 strangler-pattern** were found worth preserving among dropped items (AEC + "Train" ADRs confirmed non-existent).

---

## A.0 BASE-DATA CONSISTENCY PREDICATE (pre-Wave-0, GATING) — the data the ordering operates on

> The §A ordering is correct and over-determined by three RULED dependencies (the Architect's strongest antithesis is right: §A proves *order* but did not prove the *base data the order runs against* is consistent). This section is the missing companion proof. **No lane may enter Wave-0 until A.0-1 and A.0-2 are green.** These are data-correctness predicates, not wording nits — the founder rule (verify-at-each-step, re-read primary sources each iteration, never trust a prior verdict) does not degrade gracefully on a self-contradictory source: on the 831-file irreversible rename lane it *guarantees* either a permanent verifier stall or a silent ~62-file miscount, and a mis-routed `oya-foundry-*→oya-intelligence-*` on a governance-sense file is a newly-authored CC-1-class contradiction — the amendment producing the exact defect it exists to kill.

- **A.0-1 (G1 — census reconciliation, FIRST Wave-0 unit, HIGH).** Primary sources disagree: `docs-sweep/00-REST-OF-DOCS-REGISTER.md` body says **731 files (ADR-excluded)** + Palantir carve-out **105 journey files** + table row `foundry | 731`; `synthesis/decision-record-oyatie-canon.md:107` (the SSOT) corrects to **total non-ADR = 831, Palantir-Foundry carve-out = 43**. **Resolution:** `decision-record:107` is the single **census-of-record (831/43)**. Land a one-doc-per-PR correction to `00-REST-OF-DOCS-REGISTER.md` (fix the 731 file-count row, the 105 carve-out figure, and the `foundry | 731` table row, with a dated correction note pointing at `decision-record:107`) as the **first Wave-0 unit, before L2.0 routing-rule freeze**. Re-point every L2 verifier reference (§D L2 gate, §H.2 L2 row) at the corrected 831/43. **No L2 verification may run against a self-contradictory source.**
- **A.0-2 (G2 — ADR-0000+ consolidation-set freeze, FIRST Wave-0 unit, door:one-way — SPLIT into a Wave-0 PROVISIONAL design-freeze + a Wave-1 CONFIRM checkpoint [R3/Finding-2]).** The set "which old Accepted ADRs fold into which ADR-0000+ doc (and are therefore archived-by-re-foundation)" is a *missing primary source*: L2.2 runs in Wave-0 but L1.1 only proves the consolidation map later, so L2 cannot today know which files it is about to archive — every L2 rename on a to-be-archived original is wasted work, and the wasted rename also dirties the immutability diff. **The knowability paradox (R3/Finding-2):** the file-level fold map is the *OUTPUT of designing the re-foundation ADRs* (Wave-1: L1.1) — the SSOT (`:42-43` D13, `:114-118`/D3, D5/D6/D7) pins the fold *INTENT at cluster level* ("0124 consolidated, 0369/0367/0366 phased, 0511 superseded/related") but NOT a guaranteed-final file map; that map is produced *by* authoring the ADR, which is Wave-1. **A door:one-way over a file map that Wave-1 authoring may revise is a one-way door over a guess (PV-2). Completeness ≠ stability-under-authoring.** **Resolution — split the freeze:** **(a) Wave-0 PROVISIONAL design-freeze** — freeze the *re-foundation cluster-design decision* (the D3/D5/D6/D7 fold INTENT the SSOT genuinely rules) as its own door:one-way primary source at the very start of Wave-0, establishing the explicit edge `consolidation-design-freeze → {L2.2, L3.3, L1-amend}`. The founder signs the *design-level classification*, NOT a guaranteed file map. **(b) Wave-1 CONFIRM-OR-AMEND checkpoint (L1.1-CONS-CONFIRM)** — when each re-foundation ADR is actually authored in Wave-1, the verifier confirms the *actual file-level fold set == the Wave-0 frozen design set*. **Any delta is a RE-REVIEW TRIGGER (re-open the door:one-way), NOT a silent amend**; any L1-amend rename already landed on a newly-discovered fold is REVERTED before the file is archived. L2.2 then **skips renaming any file in the provisional design set** and the rename lands once, on the new ADR-0000+ text (see S1 split, §A.4 / L1). **The S1 parallelism recovery is conditional on the Wave-1 CONFIRM, not the Wave-0 provisional freeze** — L1-amend runs in Wave-0 under the provisional partition, but its safety net is the Wave-1 confirm + revert-on-delta, so a discovered fold is recoverable, not a silent wasted-rename.
- **A.0-3 (census completeness, with L2.0b, MEDIUM/R6).** The routing split is a **sampled estimate, not a census** for the journeys/personas bulk (`00-REST-OF-DOCS-REGISTER.md:107` "journeys/personas residue = sampled estimate"; `:164` flags `intern-month-one.md` 138-hit fabricated-precision filler). `~274 intelligence + ~135 governance = 409` ≠ 831; the remainder is carve-outs + FP + the journeys/personas residue. **Resolution:** L2.0b completes the journeys/personas census **template-first** (regenerate the persona/journey boilerplate from its generator — O(1) — which collapses most of `intern-month-one.md`-class filler) *before* L2.2 claims per-file coverage. The plan does not promote a sampled estimate to a per-file execution target.

---

## A. RALPLAN-DR — sequencing-strategy decision record

### A.1 Principles (the invariants every option must honor)

1. **ADRs are the immutable SSOT; the masterplan is GENERATED (D1).** "Backfill the masterplan" ≡ author clean ADR front-matter (`planning_impact`/`milestone`/`deliverables`/`affected_surfaces`/`domain`) so `oya gen masterplan` emits it — never hand-author the masterplan. Therefore ADR work strictly precedes any masterplan regeneration.
2. **Supersede-never-edit immutability (D13).** No in-place mutation of a frozen ADR. Re-authoring is into the clean **`ADR-0000+` generative series** with `consolidates`/`supersedes` provenance; the old frozen ADRs are archived, not deleted.
3. **Build-first-cutover-later (D-META operational rule).** An ADR superseded by a not-yet-built owned thing is marked `superseded-on-cutover (pending build+proof)`, NOT archived now (Jenkins/Argo/data-bridges stay operative). The amendment never retires a bridge before its owned replacement exists and is proven.
4. **Verify-at-each-step, separate verifier lane (founder rule).** Every fix is read-only-verified against primary sources by a verifier lane that is NOT the authoring lane. No phantom findings; no self-approval; an unverified verdict never authorizes a delete/amend.
5. **One-doc-per-PR + door:one-way founder sign-off + signed commits + linear history.** Each ADR/doc lands as its own PR to `dev`; one-way doors require explicit founder sign-off; DOC-CATALOG/CHANGELOG rows and glossary cascade ride with the doc that triggers them.
6. **Ordering-constraint provenance discipline (NEW, R2/SY1).** Every ordering constraint carries a provenance tag, exactly as every tool carries [ASPIRATIONAL] vs [ENFORCED]: **`[RULED:<SSOT-line>]`** = a founder-ruled door:one-way edge with a citable `decision-record` line; **`[DERIVED:<driver>]`** = an engineering driver the planner inferred, NOT founder-ruled, which the founder MAY overrule. The inferential slip "overlap is real ⇒ ordering is ruled" is forbidden: the existence of foundry amendments in the disposition table proves overlap, not a ruled order. A `[DERIVED]` edge may still drive the recommended plan, but it does not foreclose a founder choice and it is surfaced for sign-off, not asserted as non-negotiable law.

### A.2 Top-3 decision drivers

- **D-1 Double-work avoidance — `[DERIVED: engineering double-work; NOT founder-ruled; founder-ratifiable]` (R2/SY1).** The 831-file foundry sense-routed rename (L2) and the ADR re-foundation (L1) touch overlapping text. If ADR-0000+ re-authoring runs before the foundry routing rule is fixed, every re-authored ADR re-introduces or hand-resolves `foundry` ad hoc → the rename should run (or its routing rule be frozen) *before/with* the re-author, not after. **Provenance (verified §A.5):** the SSOT rules NO rename-vs-reauthor ordering — D11 (`:52`) rules only sweep-before-backfill, D13 (`:42-43`) rules the re-founding with no ordering vs the rename, and `grep double-work` over the SSOT is empty. This is a sound engineering driver, sufficient on its own to prefer Option 1, but it is the planner's inference, not a founder ruling. It therefore enters the §F founder sign-off set as a genuine choice (Option-1 wave-shape vs Option-2 refound-first), NOT the non-negotiable layer. Recommended call: Option 1 (double-work is real overlap per the disposition table), but the founder may overrule.
- **D-2 Reference integrity under renumber — `[RULED: D13 no-dangling-ref invariant, decision-record:42-43]`.** Renumbering linux 0001–0026 → 0515+, dropping the 0377 forge-board collision, and the ADR-0000+ re-founding all rewrite cross-reference edges. Doing the integrity sweep (L3: dangling supersedes/amends) *before* the renumber means fixing edges twice; doing the renumber on top of broken edges means the sweep can't tell a real dangling ref from a renumber artifact. Order: freeze the renumber MAP first, sweep against the post-map id-space. **Provenance:** this IS grounded — D13's "strict no-id-reuse + no-dangling-ref invariant" (`:43`) forces map-before-sweep, because a sweep against a moving id-space cannot honor a no-dangling invariant. D-2 stays RULED; the SY1 relabel is surgical to D-1 only.
- **D-3 Gate availability.** The cohesion gate, drift gate, and `oya gen masterplan` are not on `dev`. The masterplan backfill (L1 terminal) and domain-cohesion-by-construction (D15) are **[ASPIRATIONAL]** — they either block on porting the generator to `dev`, or run manually with a verifier lane standing in for the gate. The plan must not assume the gate exists.
- **D-4 (NEW) Base-data consistency precedes ordering.** The §A.0 predicate (census 831/43, consolidation-set freeze) is an independent degree of freedom the original §A did not examine — "reconcile the census first" vs "run on the inherited number" is a real choice, and only the former is safe on an irreversible lane. Selected: reconcile first (G1/G2 gate Wave-0).

### A.3 Viable options for the OVERALL sequencing strategy (≥2)

**Option 1 — "Sweep-then-refound" (integrity & rename first, re-foundation second). ✅ SELECTED.**
Run L3 integrity sweep + L2 foundry rename + L6 vocabulary namespacing as the **first wave** (against the existing id-space, fixing the live corpus in place where the ADR is already Accepted and not being re-founded), freeze the L1 renumber MAP, THEN run L1 ADR-0000+ re-foundation on a clean, foundry-free, integrity-clean, namespaced base, THEN L4/L5/L7 author new/reshaped ADRs + owed-depth, THEN masterplan backfill last.
- *Pros:* Re-authoring happens once over clean text (kills D-1 double-work); the renumber MAP gives L3 a stable target id-space (kills D-2 fix-twice); new ADRs (L5) inherit the namespaced vocab + domain field for free; the masterplan regen runs over a fully-clean ADR set so its output is trustworthy on the first generate.
- *Cons:* The first wave touches many already-Accepted ADRs in place (allowed — they're not being re-founded, only de-foundried/de-stale'd via supersede-on-touch where frozen), so the verifier lane carries its heaviest load early; longer critical path before the first "new ADR" lands.
- *Invalidation rationale (why it wins):* It best satisfies all three drivers simultaneously. D-2 is a hard RULED ordering constraint (map-before-sweep, grounded in D13's no-dangling invariant); D-1 (rename-before-reauthor) is a strong DERIVED engineering driver (not a ruling) that Option 1 honors and Option 2 fights. Option 1 wins on D-2 (RULED) + the D-1 engineering merits; it is the recommended call, but because D-1 is DERIVED the wave-shape is a founder choice (§F), not a foreclosed law.

**Option 2 — "Refound-first" (ADR-0000+ shells first, the still-pre-backfill sweep second). [LEGAL SHAPE RESTATED — R3/Finding-1]**
Seed the ADR-0000+ generative series immediately (D3 oya-ci ratifier, D5/D6/D7 endpoints, etc.), then run the foundry rename + integrity sweep across the new series — **but the foundry rename MUST still sit INSIDE the pre-backfill sweep (D11(d)+`:52`, RULED).** **Option 2's ONLY legal shape is therefore "author the ADR-0000+ shells first, THEN run the (still-pre-backfill) sweep over them."** An Option-2 that runs the foundry rename *after* re-foundation **and** *after* masterplan backfill is **NOT a legal alternative** — re-foundation feeds backfill, D11 puts the bulk-rename in the sweep that runs *before* backfill, so a post-backfill rename violates the D11 batched-sweep-before-backfill one-way door. The founder may reorder re-foundation vs the other lanes; the founder may NOT move the foundry rename out of the pre-backfill sweep.
- *Pros:* Produces visible "new canon" artifacts fastest; founder sees ADR-0000+ shape early; good if the founder wants to validate the re-foundation template before committing to the bulk cleanup.
- *Cons:* Fights the D-1 *internal-sequencing* driver — every re-authored ADR shell still carries foundry/stale-term/dangling-ref text the still-pre-backfill sweep must then re-touch → double-work + merge churn on the freshly-authored series; the cohesion gate can't run cleanly (the base is mid-sweep) so contradictions risk leaking into the new series.
- *Invalidation rationale (why it is NOT recommended — on engineering merits for the part that is a genuine choice, NOT a ruling):* The double-work + merge-churn cost (D-1 internal sequencing) is real and sufficient to prefer Option 1: the disposition table shows dozens of Accepted ADRs carrying `foundry→intelligence/governance` amendments, so re-authoring shells before the sweep guarantees re-touching freshly-authored docs. **This is an engineering disqualifier on the DERIVED *sequencing* axis, not a founder ruling — the SSOT rules no rename-vs-reauthor order (verified §A.5).** Option 2 is therefore a *legitimate but BOUNDED founder choice* (refound-shells-first, accept the double-work to see ADR-0000+ canon sooner), bounded by the RULED constraint that the rename stays in the pre-backfill sweep (D11(d)+`:52`). It is NOT a clean free choice and NOT excluded by fiat. The plan recommends Option 1 on the merits and surfaces the *bounded* choice for sign-off (§F); it neither forecloses Option 2 with a non-existent ruling (round-2 over-exclusion fixed) nor presents it as fully-legal when D11(d) partially binds it (round-3 over-inclusion fixed).

**Option 3 — "Fully-parallel big-bang" (all lanes at once, reconcile at merge).**
Run L1–L7 concurrently and resolve conflicts at PR-merge time.
- *Pros:* Maximum wall-clock parallelism.
- *Cons:* one-doc-per-PR + linear-history + the supersede-never-edit immutability make concurrent edits to the same ADR/cross-ref graph unmergeable without constant rebase; the verifier lane can't establish a stable primary-source baseline; renumber + sweep race. Rejected as incompatible with the source regimen.

**SELECTED: Option 1.** Within it, D-LANES still applies — lanes run in parallel *within each wave*, and tasks within a lane are sequential or parallel as noted. Option 1 is a wave-ordering over parallel lanes, not a global serial chain.

### A.4 D-LANES narrowing — a deliberate, founder-acknowledged tradeoff (door:one-way, NAMED) [G5/FLAG-2]

D-LANES is itself a **door:one-way** organizing principle (decision-record:84 "treat everything as PARALLEL LANES … Fractal parallel/sequential structure (**not a global serial chain**). … Door: one-way (organizing principle)"). The selected Option-1 wave order is a **serial chain of waves** — it narrows the shape of a one-way founder ruling. The original plan resolved this silently; it is now surfaced as an explicit, signed tradeoff:

- **What is narrowed and why:** the waves themselves are serial (Wave-0 → Wave-1 → Wave-2 → Wave-3) to *buy* **D-2 (`[RULED:` map-before-sweep, no-dangling invariant, stable id-space, decision-record:42-43`]`)** and **the DERIVED D-1 (`[DERIVED:` rename-before-reauthor, no double-work on immutable docs; founder-ratifiable`]`)**. You cannot have both maximal cross-wave parallelism (D-LANES) and single-pass-over-clean-text (D-1) — they are in genuine tension. The plan chooses D-2 (RULED) + D-1 (DERIVED, recommended) over maximal D-LANES, knowingly. **Because D-1 is DERIVED not RULED (R2/SY1), the founder is signing the wave-serialization partly to buy a constraint they are *also* ratifying in the same signature** (the Option-1-vs-Option-2 wave-shape, now in the §F sign-off set) — not a pre-existing law.
- **What is preserved:** within-wave parallelism is full (lanes run concurrently inside each wave), and **S1 recovers most of the lost cross-wave parallelism** (below) so the *within-Wave-0* narrowing is minimal — **BUT the narrowing is NOT total-minimal: it has a SECOND, mandatory serialization point at the end (R3/Finding-3, below).**
- **TWO acknowledged serialization points (R3/Finding-3 — the narrowing is at TWO joints, not one):** the wave-serialization narrows D-LANES at **(1) the Wave-0 gate** (serial waves buy **D-2 map-before-sweep**, `[RULED:` D13 `:43` no-dangling invariant`]`) **and (2) the Wave-3 TERMINAL CROSS-REF BARRIER** — the masterplan backfill (Wave-3) HARD-BLOCKS on the LAST Wave-2 supersession resolving on the *delivered* ADR graph. This terminal re-resolution (SY2) is a **mandatory whole-corpus serialization**, NOT "minimal narrowing": build-first-cutover-later (D-META `:26`) keeps superseded bridges live so the graph GROWS through Waves 1–2, and no-dangling-ref (D13 `:43`) can only be proven of the *final* graph, so the terminal sweep CANNOT run until every Wave-1/2 supersession has landed — Wave-3 is therefore hard-blocked on all of Waves 1–2 completing. **Both serializations are RULED-driven (D13 `:43`), both are accepted, and S1 recovers within-Wave-0 parallelism but DOES NOT recover the terminal barrier.** Naming it here pre-empts a future reviewer re-discovering it as a surprise (which is exactly what round-3 did).
- **S1 split (recovers parallelism, makes the narrowing minimal):** split L1 into **L1-amend** (in-place de-foundry / de-stale of Accepted ADRs that are NOT being re-founded) and **L1-refound** (the ADR-0000+ consolidation of those that ARE). **L1-amend runs in Wave-0, concurrently with L2/L3/L6** — it is the same class of mechanical fix, and by definition it touches only files that L1-refound does *not* archive (the consolidation-set freeze A.0-2 is what makes that partition knowable). Only **L1-refound** must wait for the clean base in Wave-1. This honors D-LANES (more parallel) without violating D-1 (re-foundation still runs over clean text).
- **Founder sign-off:** because narrowing a one-way organizing principle is itself a one-way act, this tradeoff is added to the §F door:one-way sign-off set (signed together with the §A sequencing decision). The narrowing is justified, recorded, **and now honestly scoped: within-Wave-0 minimal (post-S1) but carrying the named Wave-3 terminal barrier (R3/Finding-3)** — not implicit, not optimistically "minimal" without the terminal serialization acknowledged.

### A.5 ORDERING-PROVENANCE EVIDENCE (R2/SY1 — what the SSOT actually rules vs what the planner derived)

> This section makes the §A.1-6 provenance discipline auditable: for each ordering constraint the plan relies on, the citable SSOT line (RULED) or the inference (DERIVED). Verified this round by grep + line-reads of `synthesis/decision-record-oyatie-canon.md`; the verifier lane re-runs this in §A.6.

| Ordering constraint | Tag | Evidence (verified this round) |
|---|---|---|
| sweep-before-masterplan-backfill | `[RULED]` | D11 `:52` "the FULL sweep (runs before masterplan backfill …)". The D11 (a)(b)(c)(d) are a **list of sweep contents under ONE batched door:one-way ruling, not a sequence** — "§c→§d" is NOT an ordering arrow (corrected this round). |
| **L2 foundry-rename ∈ the pre-backfill sweep (sweep-membership)** | **`[RULED]` (R3/Finding-1, RULED-deflation fix)** | **D11 `:52` content (d): "foundry→cloud-intelligence/governance bulk-rename (0347 Proposed→Accept; fix 0363's false 'eradicated' claim)" is a NAMED CONTENT of "the FULL sweep (runs before masterplan backfill …) Door: one-way (batch)". The same line that grounds sweep-before-backfill grounds L2's membership in that sweep. Round-2 cited D11`:52` for L3 only and silently amputated L2, re-deriving L2's Wave-0 placement through the weaker DERIVED D-1 driver. RE-TAGGED RULED this round (direction-2 of §A.6). This is sweep-MEMBERSHIP, distinct from the DERIVED rename-vs-reauthor *internal sequencing* below.** |
| map-before-sweep (D-2) | `[RULED]` | D13 `:43` "strict no-id-reuse + no-dangling-ref invariant" — a sweep against a moving id-space cannot honor a no-dangling invariant, so map-before-sweep is forced by the ruling. |
| renumber linux 0001–0026→0515+, dedup 0377 | `[RULED]` | D13 `:43`. |
| census-of-record = 831/43 | `[RULED]` | `:107` "total non-ADR = 831 (not 731), Palantir-Foundry carve-out = 43 (not 105)". |
| DROP 0325/0316/0349-half; KEEP 0134; AMEND 0352 | `[RULED]` | D14 (Proposed batch) — spot-confirmed genuine. |
| D-EVENT/Pulsar precedence, 0005 amended | `[RULED]` | `:107` CC-6 "Kafka→Pulsar(D-EVENT)". |
| build-first-cutover-later + `cutover_trigger:` (0510/0250) | `[RULED]` | D-META `:26`. |
| **rename-before-reauthor (D-1) — L2-vs-L1.1 INTERNAL SEQUENCING ONLY** | **`[DERIVED]`** | **NOT in the SSOT.** `grep double-work` → ZERO; `grep re-author` → only `:4` boilerplate + `:34` Cedar bullet (neither an ordering); the only "before.*rename"-class hit is D11`:52` sweep-before-backfill. D13 rules the re-founding with no ordering vs the rename. **Sound engineering driver, founder-ratifiable, NOT a ruling.** The old line-53 quoted "dependency note … to avoid double-work" was a **fabricated quotation — deleted this round.** **Scope clarified (R3/Finding-1): this DERIVED edge is ONLY the *internal sequencing* of the rename relative to re-authoring the same files. That the rename happens *inside the pre-backfill sweep at all* is RULED (D11(d)+`:52`, row above) — do NOT conflate the two.** |

### A.6 RULING-PROVENANCE VERIFIER PASS (R2, GATING before founder sign-off) [HIGH] — **BIDIRECTIONAL (R3/Finding-1)**

> The §A.5 table is the planner's self-report; per the founder rule the verifier must not trust it. A **one-time ruling-provenance pass** (separate verifier lane, opus — this gates an irreversible execution order + a door:one-way sign-off) runs **before §A.3/§A.4 founder sign-off**. **The pass is BIDIRECTIONAL (R3/Finding-1): a one-directional pass can only ever *remove* a RULED tag, never *add* a missing one — so it polices RULED-INFLATION but is structurally blind to RULED-DEFLATION (a genuinely-ruled SSOT constraint the plan left DERIVED/untagged). Round-2's pass was direction-(1) only, and that blindness let L2's sweep-membership (RULED by D11(d)+`:52`) be re-derived through the weaker DERIVED D-1 driver. Both directions are now mandated.**
>
> **Direction (1) — catch RULED-INFLATION (a DERIVED edge wearing a RULED tag):**
> 1. For **every** `[RULED]` / "RULED" / "the decision record rules" / "explicitly rules" assertion in this plan (not only D-1), confirm a **citable `decision-record` line exists** that states the ordering/ruling. Use the same discipline that caught D-1: grep the SSOT for the ruling string, don't infer it from "overlap exists."
> 2. Downgrade any tag with no citable line to `[DERIVED]` and surface it for founder sign-off (do not silently keep it RULED).
> 3. Confirm the fabricated-quotation deletion: `grep double-work` over the SSOT returns zero, and the plan no longer quotes a non-existent "dependency note."
> 4. Confirm the relabel is **surgical**: D-2 (map-before-sweep) and D11 (sweep-before-backfill) remain RULED (both grounded — D13 no-dangling invariant; D11`:52`); only D-1 dropped to DERIVED.
>
> **Direction (2) — catch RULED-DEFLATION (a genuinely-ruled SSOT action left DERIVED/untagged) [R3/Finding-1, GATING]:**
> 5. For **every D-decision in the SSOT that names a concrete amendment ACTION** (D11(a)-(d), D12, D13, D14, D15, D-EVENT, D-META, …), confirm the plan's corresponding lane carries the **matching `[RULED:<SSOT-line>]` tag**. Surface any ruled action the plan left `[DERIVED]`/untagged as a **re-review trigger**, not a silent fix. The discipline mirrors direction (1): walk the SSOT's ruled-action set forward into the plan, don't only walk the plan's tag-set back to the SSOT. The slip "I reasoned about a lane's *order* from an engineering driver and never re-read the SSOT to see the lane is already *inside* a ruled batch" is forbidden in this direction exactly as "overlap ⇒ ruled" is forbidden in direction (1).
> 6. Specifically confirm **L2's sweep-membership is tagged `[RULED: D11(d)+:52]`** (the foundry bulk-rename is a named content of the FULL sweep that runs before masterplan backfill, batched one-way) — NOT merely DERIVED. Keep **rename-before-reauthor (L2-vs-L1.1 *internal sequencing*)** as `[DERIVED: D-1]`: the rename happening *inside the pre-backfill sweep* is RULED; whether it lands *before or interleaved-with* the re-authoring of the same files is the DERIVED choice.
>
> **Exit:** every RULED tag in the plan resolves to an SSOT line (direction 1) AND every SSOT-ruled amendment action resolves to a matching RULED tag in the plan (direction 2); no DERIVED edge is presented as non-negotiable law, and no SSOT ruling is silently deflated to DERIVED. The round-2 lanes spot-confirmed D11/D13/D14/D-EVENT/census as genuine; this pass makes the check exhaustive **in both directions** so neither the "overlap ⇒ ruled" slip nor its mirror (a ruled lane detached from its ruling) can survive to canon. **If the pass surfaces additional false-RULED tags OR any RULED-deflated action, that is a re-review trigger (round-2 critic §5; round-3 critic §3), not a silent fix.**

---

## B. LANE DECOMPOSITION (L1–L7, parallel lanes; per-lane seq/par breakdown)

> Wave membership (from §A.3 Option 1, as narrowed-and-S1-recovered per §A.4): **Wave-0** = A.0-1 census-reconciliation (G1, FIRST) + A.0-2 consolidation-set freeze (G2, FIRST, door:one-way) + L3 + L2 + L6 + **L1-amend** (in-place de-foundry/de-stale of NON-re-founded Accepted ADRs) ∥ L1.0 renumber-MAP-freeze. **Wave-1** = **L1-refound** (ADR-0000+ re-foundation) + L1 Proposed batch + **L8** (oya-gate/oya-cli supersede + CaC/CaaS re-homing, Δ2/Δ3). **Wave-2** = L4 + L5 + L7 (new/reshaped/owed-depth). **Wave-3** = masterplan backfill + cohesion wiring (terminal). **Wave-0 cannot start until A.0-1 + A.0-2 are green (GATING).**

### L1 — ADR re-foundation (clean ADR-0000+ generative series), split into L1-amend (Wave-0) + L1-refound (Wave-1) per S1
Rulings: D13 (renumber + clean series), D14 (Proposed batch), D11 (sweep gates this). **L1-amend = Wave-0** (concurrent with L2/L3/L6); **L1-refound (L1.1–L1.3) = Wave-1**, after Wave-0 clean base + MAP-freeze.
- **L1.0-CONS (seq, FIRST of Wave-0, door:one-way) [G2/A.0-2] — PROVISIONAL DESIGN-FREEZE [R3/Finding-2]:** Freeze the **ADR-0000+ consolidation DESIGN-SET** as its own primary source — the *cluster-level* table `{old Accepted ADR → which 0000+ ADR it is INTENDED to fold into / be archived by}` at the design-intent granularity the SSOT rules (D3/D5/D6/D7, `:114-118`; D13 `:42-43`). This is lifted out of the general renumber MAP so it lands at the very start of Wave-0, establishing `consolidation-design-freeze → {L2.2, L3.3, L1-amend}`. It is the partition that tells every Wave-0 lane which files are *provisionally* throwaway-by-re-foundation (skip the rename) vs live-and-amended (rename in place once). **Door:one-way founder sign-off — but the founder signs the DESIGN-LEVEL classification, NOT a guaranteed-final file map (R3/Finding-2/PV-2): the file-level fold map is the OUTPUT of Wave-1 authoring (L1.1), so a Wave-0 freeze of it would be a one-way door over a guess. The provisional design-freeze is confirmed-or-amended in Wave-1 by L1.1-CONS-CONFIRM (below).**
- **L1-amend (par, Wave-0, after L1.0-CONS) [S1; safety-net per R3/Finding-2]:** in-place de-foundry/de-stale of the Accepted ADRs that are NOT in the PROVISIONAL consolidation design-set (i.e. not intended to be re-founded) — supersede-on-touch only where the ADR is frozen-immutable; same mechanical class as L2/L3/L6. Runs concurrently in Wave-0 under the *provisional* partition. **The no-throwaway guarantee is now CONDITIONAL on the Wave-1 L1.1-CONS-CONFIRM, not the Wave-0 provisional freeze (R3/Finding-2):** if Wave-1 authoring discovers a fold the provisional set missed, the L1-amend rename already landed on that now-archived file is REVERTED before archival (and the delta triggers re-review). The S1 parallelism is real but its safety net is the Wave-1 confirm + revert-on-delta, not an asserted-final freeze (closes the §4 carve via a recoverable checkpoint, not a guess).
- **L1.0 (seq, after L1.0-CONS):** Freeze the **renumber MAP** — a frozen table `{old-id → new-id}` covering: linux 0001–0026 → 0515+; the duplicate forge-board 0377 → next free id. (The consolidation map now lives in L1.0-CONS, referenced here.) This MAP is the primary source L3 and L2 reference. Door:one-way (anchors every cross-ref). *Runs in Wave-0 as the gate for L3/L2.*
- **L1.0-VERIFY (seq, AHEAD of MAP founder sign-off) [G3/S6 + R2/SY3 — MAP self-consistency + consolidation-completeness gate]:** before the founder signs L1.0/L1.0-CONS, a **separate verifier lane** proves the MAP is internally consistent — **renumber bijection** (no two old-ids → same new-id; no new-id reused; every linux 0001–0026 + 0377 mapped), **consolidation surjection** (every archived old-id is covered by exactly one 0000+ target; no old-id both consolidated-and-renumbered ambiguously), **no id-reuse** against the live 0000–0514 space, **and consolidation COMPLETENESS against a stated closure criterion [R2/SY3]** — every re-foundation-candidate is classified `consolidate` or `amend`, none unclassified (closure criterion: *every Accepted ADR carrying a foundry-sense term that is also a re-foundation candidate is classified*). This makes the S1 L1-amend partition **provably total AT THE DESIGN LEVEL**: the closure check proves *every candidate is classified at Wave-0*, but per R3/Finding-2 it **cannot** prove *the classification won't change when the re-foundation is actually authored in Wave-1* (completeness ≠ stability-under-authoring). File-level *stability* is the separate **L1.1-CONS-CONFIRM** Wave-1 check (confirm-or-amend the provisional design-freeze; delta = re-review trigger + revert-on-fold). Without BOTH, a Wave-1 re-foundation could discover a missed fold and an L1-amend rename would already have landed on a now-archived file — the exact wasted-work + dirtied-immutability-diff the freeze exists to prevent (round-2 FLAG-C / round-3 Finding-2); the Wave-1 confirm makes that recoverable (revert) rather than silent. The foundry-sense term set is small/enumerable, so the closure check is cheap. The founder signs the *governance* decision; the verifier proves the *bijection + completeness* — the founder is not asked to hand-verify a 372-row id map. A bad/incomplete MAP poisons L3.3 + L1.3 + L1-amend silently, so this gate precedes sign-off.
- **L1.1 (par across clusters, Wave-1, after L1.0 + L1.0-VERIFY + clean base) [L1-refound]:** Seed the ADR-0000+ ratifying ADRs per the decision record, one PR each:
  - oya-ci unified Run+graph (reshape 0513; supersede/relate 0511/0124; phase 0369/0367/0366) — D3.
  - identity (0476 `supersedes:[0187]`; Zitadel→bridge) — D5/CC-8.
  - policy engine (Cedar=contract + owned PARC engine; re-author phantom 0150-cedar anchor) — D6/CC-2.
  - isolation (framekernel+owned-VMM endpoint; bridges transitional; H2 committed-ratchet-gated) — D7/CC-7.
  - autonomy ceiling (0022 hard Cedar gate; 0007 demoted to design-time) — D16.
  - data tier (own-the-tier endpoint; bridges behind ports; Kafka→Pulsar) — D4/D-EVENT/CC-6.
  - masterplan-generated wiring meta-ADR (domain field on 0364 template + cohesion gate on 0365 lifecycle) — D1/D15.
- **L1.1-CONS-CONFIRM (seq, Wave-1, after each L1.1 ADR is authored, before L1.3 archival) [R3/Finding-2 — confirm-or-amend the provisional design-freeze]:** as each re-foundation ADR is actually authored, the **verifier lane** (separate from authoring) confirms the *actual file-level fold set produced by the authored ADR == the Wave-0 PROVISIONAL design-set* (L1.0-CONS). **Any delta (a fold the provisional set missed, or a file the provisional set wrongly marked archived) is a RE-REVIEW TRIGGER that re-opens the A.0-2 door:one-way — NOT a silent amend** (§A.1 principle-4: an unverified verdict never authorizes an irreversible act; door:one-way demands a finalized object). On a delta: (a) re-open the consolidation-set sign-off for the affected cluster; (b) **revert any L1-amend rename already landed on a newly-discovered fold before the file is archived** (so no wasted rename + no dirtied immutability diff survives); (c) re-run L1.0-VERIFY's completeness check against the corrected set. This is the file-level *stability-under-authoring* check that L1.0-VERIFY's Wave-0 *completeness* check structurally cannot prove (completeness ≠ stability). Two checks for two genuinely different claims: Wave-0 proves design-level classification is total; Wave-1 proves file-level fold is stable when authored.
- **L1.2 (batch, after L1.1 template proven):** The **Proposed batch (D14)** — apply ledger 03 verdicts: RATIFY ≈122, RENUMBER-then-ratify 0377-forge, KEEP-as-Proposed 0134, DROP 3 (0325/0316/0349-Jenkins-half), AMEND-MANDATORY 0352. Two-way Proposeds ratify-on-green; one-way Proposeds (the §201 cluster: fork/forge/data-tier/identity/scope/license/substrate-canon) require founder sign-off.
- **L1.2-DROP (gate, per dropped id) [G4/EXTEND-2 — DROP-reference-safety]:** L3.3 verifies dangling *outbound* edges against the frozen MAP in Wave-0, but a DROP creates *inbound* orphans authored in Wave-1 that L3.3 never re-checks. For **each** dropped id (0325/0316/0349-Jenkins-half, plus Bominal Train as a roadmap drop in L7.4), the verifier lane runs a **corpus-wide inbound-reference resolver** *after* the drop and proves **zero live citations** OR each surviving `supersedes:`/`amends:`/cite re-pointed to the superseding id (e.g. 0316→0329). D14 authorizes the drops; this gate proves each drop is reference-safe. A drop is not recorded done until its inbound set is clean.
- **L1.3 (seq, terminal of L1):** Apply the renumber MAP — linux 0001–0026 → 0515+, 0377 dedup — and archive the old frozen ADRs (consolidates-provenance). Re-resolve the `origin/dev` branch-locality edges (0476→0421, 0478→0457, 0479→0429, 0480→0443, 0481→0428) at merge.

### L2 — Foundry sense-routed rename + 4-term eradication (831 files — census-of-record per A.0-1) [Δ1]
> **[Δ1] Widened to a 4-term eradication.** `foundry` stays per-file *sense-routed* (intelligence vs governance, below); `forgejo`/`jenkins`/`oya-vcs` are *fixed replacements* (forgejo→GitHub-now/cloud-scm-dest; jenkins→GitHub-Actions/oya-ci; oya-vcs→cloud-scm). All four → the `oya-check-brand-residue` gate (L2.3). Carve-out: external "Palantir Foundry".

Rulings: CC-1, D11 (d) (foundry bulk-rename, a sweep content), WF2 mechanical sweep. **Wave-0.** **L2's membership in the pre-backfill sweep is `[RULED: D11(d)+:52`, foundry bulk-rename is a named sweep content, batched one-way before backfill`]` (R3/Finding-1 — re-tagged from DERIVED this round; see §A.5).** *Its position relative to L1-refound's re-authoring (run before vs interleaved) is `[DERIVED:` D-1, founder-ratifiable, NOT a ruled order; see §A.5`]` — recommended to precede L1-refound to avoid double-work. The sweep-membership is RULED; the internal sequencing is DERIVED — do not conflate.* Gated on A.0-1 census-reconciliation + A.0-2 consolidation-set freeze (both RULED/GATING). **Census-of-record: 831 non-ADR / 43 Palantir carve-out** (`decision-record:107`, NOT the register's stale 731/105 — see A.0-1).
- **L2.0 (seq, FIRST, after A.0-1):** Freeze the **routing rule** as primary source: `oya-foundry-* / axis-foundry / ai-foundry / agent-runtime / provider / capability / RAG / model / sandbox / supervisor` → **cloud-intelligence** (~274 files); `foundry-fitness / council-foundry / governance-foundry / amendment-foundry / Proof-Ladder / fitness-lane / CI-gate` → **governance** (~135 files). *(`274+135=409 ≠ 831`; the remainder = carve-outs + FP + journeys/personas residue, which is a sampled estimate until L2.0b — A.0-3.)*
- **L2.0b (seq, after L2.0, before L2.2) [R6/A.0-3 — complete the journeys/personas census]:** the routing split is a **sampled estimate** for the journeys/personas bulk (`00-REST-OF-DOCS-REGISTER.md:107`). Complete that census **template-first** — regenerate persona/journey boilerplate from its generator (O(1)), which collapses `intern-month-one.md`-class 138-hit fabricated-precision filler (`:164`) — so L2.2 runs against a real per-file census, not an estimate.
- **L2.1 (HARD carve-outs, par with L2.0):** Lock the do-NOT-sweep set: **Palantir Foundry** (**43 files** per A.0-1, third-party product), **Marlboro-Forge / forgery** (fictional co / security event), **`foundry-fitness-to-governance-transition-2026-05-21.md` + `transition-classification.json`** (the retirement RECORD itself). Verifier lane confirms zero carve-out hits in the sweep diff, against the 43-file census-of-record.
- **L2.2 (par, per-file after rule frozen + L2.0b census + A.0-2 consolidation-set):** Execute the sense-routed rename per file family. **Skip any file in the A.0-2 consolidation set** (it will be archived by L1-refound; the rename lands once on the new ADR-0000+ text instead — closes the §4 carve, no wasted rename, no dirtied immutability diff). Fix the GENERATOR/TEMPLATE first where the term arrives via boilerplate (personas, journeys, PRD template, runbook stubs) — O(1) not O(N).
- **L2.3 (seq, verification):** Wire `foundry` into the brand-residue token list; rename `oya-foundry-brand-residue-kernel` → `oya-governance-*`; fix the live name-lag bug (`oya-foundry-supervisor` binary builds `oya-intelligence-supervisor-app` package — verify binary/package/service names against `microservices/` before swapping doc commands). Reject mixed `oya-foundry-*` + `oya-intelligence-*` in one fitness seam.

### L3 — Integrity sweep (KCMVP/KISA, tautologies, dangling refs)
Rulings: D11 (full sweep, before backfill, each fix read-only-verified). **Wave-0.** *Before L1 re-author + before backfill.*
- **L3.1 (par):** Restore **KCMVP/KISA** from `*-shippable-tier` corruption (6+ files incl. GLOSSARY + 0043 + 0002) — URGENT, before anything else cites them.
- **L3.2 (par):** Fix self-referential renames + tautologies (0006 "Ontology→Ontology" ×2; "all all" typo 0038; 0025 tautology garble).
- **L3.3 (par, against the FROZEN L1.0 MAP):** Fix ALL dangling supersedes/amends edges: 0150-cedar phantom, 0476→0421, 0057→0055-v3, 0069→0088, 0377→0397/0436, Cedar-as-0083, 0140-as-Cedar on 0144, 0130-cite-on-0186→0139, 0150-cite-on-0182→0183. **[Δ5 — two NEW defects from the personal verification]: (i) PHANTOM ADR-0409** — `oya-vcs` cited by Accepted 0476/0481×2/0482×2 but no 0409 file exists → repoint to `cloud-scm`/ADR-0510 + drop the edge; **(ii) asymmetric 0509↔0512** — 0512 `supersedes:[…0509]` but 0509 is `status:Accepted`/`superseded_by:[]` → set 0509 `Superseded`+`superseded_by:[0512]` (auto-resolves its flat-`crates/` carve-out vs 0512). *Run after L1.0 MAP-freeze so renumber targets are stable (driver D-2).*
- **L3.4 (par):** Foundry→cloud-intelligence/governance disposition edges: 0347 Proposed→Accept; fix 0363's false "eradicated" claim. *Coordinates with L2.*

### L4 — Canon-contradiction fixes (CC-1..CC-13)
Rulings: WF2 register §1. **Wave-2** (CC fixes that are DECISION→ADR ride with the L1/L5 ADRs; GENERATED-REFERENCE re-frames ride post-amendment). Per-CC routing:
- CC-1 foundry (→L2). CC-2 Cedar→contract+PARC-engine, 24 genuine sites (→L1 policy ADR + doc re-frame). CC-3 masterplan-authority inversion: reclassify MASTERPLAN.md → GENERATED-REFERENCE in DOC-CATALOG; re-frame `authority_chain` to ADRs=SSOT; resolve 81 "Directive N" cites to originating ADRs. CC-4 Jenkins/ArgoCD→oya-ci bridges (build-first-cutover-later; delete "does NOT build replacements" sentence). CC-5 Forgejo→GitHub-now (re-scope 4 runbooks + 2 webhook-replay specs to `X-GitHub-Delivery` primary). CC-6 data-tier bridges + **[Δ4] Pulsar from day 0 (NO Kafka interim — drop the `Kafka→Pulsar` *migration* framing; Pulsar is stood up directly)** → owned-in-Rust diskless event log destination recorded on the L1.1 data-tier ADR (wording, not deletion, for the `cloud/PRD.md` managed-service catalog). CC-7 isolation framekernel-host + assume-breach (rename 2 `secure-by-default` phrasings). CC-8 identity oya-identity + Zitadel-bridge. CC-9 eventing 3-way (Pulsar canon; scope-or-drop NATS/Redpanda). **Precedence is explicit [R5/EXTEND-3; Δ4]:** D-EVENT (**Pulsar from day 0 → owned-in-Rust diskless destination**) is the door:one-way founder ruling and is the authority; **ADR-0005 is AMENDED to Pulsar-from-day-0, not "reconciled with" as a co-equal** — the verifier must not treat ADR-0005's substrate as competing canon. If ADR-0005 names a different substrate, D-EVENT wins and ADR-0005's framing is the stale side to fix. CC-10 DOWNGRADED (framing-clarity only — delete KR-as-launch from PRD.md North Star). CC-11 tenant-tier→tenant-class (fix persona template not 129 files; ~12 real plan-axis sites). CC-12 M0-M3/MVP→gate-waves (62 real, heavy FP carve). CC-13 linux pilot North-Star re-scope (Phase-0 port-provenance, demote under live mission).
- **(par by CC, but each gated on its upstream lane: CC-1→L2, CC-2/4/5/6/7/8→L1 ADRs, CC-3/12→template fixes, CC-9→L1 data/event ADR.)**

### L5 — New / reshaped ADRs
Rulings: D3 (reshape-0513), D-SAFETY (unified safety-gate), D-KR (KR employment enum), D-SEQ (infra-sovereignty schedule), D15 (domain-cohesion meta-ADR), D1 (masterplan-generated wiring). **Wave-2**, after L1 template + clean base.
- **L5.1 (par):** oya-ci reshape ratifier (also L1.1 — shared). Unified **safety-gate invariant ADR** (governance-owned: HITL / no-closed-loop-actuation / biometric-default-off+5-stage / no-autonomous-lethal). **[R1/§6 — safety runtime-hook-pending]:** the ADR ships now as a **design+invariant** doc, but its "hooked into D16's runtime Cedar gate" claim is marked **`runtime-hook-pending` (NOT asserted-live)** — the Cedar gate runs only in `.claude/worktrees`, so asserting live safety *enforcement* is the highest-consequence aspirational-vs-enforced gap and is the one safety/liability claim that must not be overstated. The runtime hook lands when the gate is on `dev`. **KR EmploymentClassification enum** ADR (정규직/계약직/단시간/파견/도급/프리랜서/인턴/임원 + per-class branching). **Infra-sovereignty ratchet** ADR (ordered substrate list + per-substrate M0 evidence-gate, NO calendar dates). **Domain-cohesion meta-ADR** (16-enum + splits/merges + `domain` field + cohesion gate). **Masterplan-generated wiring** meta-ADR (also L1.1 — shared); **[R1/S5/FLAG-3]** this meta-ADR carries the **[ASPIRATIONAL→port-to-dev]** generator status as a first-class deliverable, and a paired **supersede** of `decision-record D1` records the generator status — but **[R3/Finding-4/PV-3]** NOT as a static dated prose snapshot (a future reader would see a stale `@2026-06-06` date, not current truth). Instead the meta-ADR carries a **queryable `cutover_status:` front-matter field** (mirroring the mandated `cutover_trigger:` pattern, §E) for EACH aspirational-tooling item — `oya gen masterplan`, the cohesion gate, the drift gate, the safety runtime-Cedar hook — e.g. `cutover_status: { oya_gen_masterplan: worktree-only, cohesion_gate: worktree-only, drift_gate: worktree-only, safety_runtime_hook: worktree-only }`, **updated to `live-on-dev` when each lands on `dev`.** The canon thus carries CURRENT generator-status (a queryable field a reader can trust), not a dated snapshot; the `decision-record D1` supersede records the field's *existence + initial values @2026-06-06* (supersede-never-edit), and the **generator-to-`dev` follow-up stays visibly tracked** via the field flipping. So the "generator is not on `dev`" knowledge lives in the canon as live status, not only in this build plan (which gets archived).
- **L5.2 (seq, after L5.1):** Uniform vertical milestone-shape template ADR (`research(benchmark) → M0-gate → operating-contract ADR → EPIC`) — D-SEQ part 2.

### L6 — Vocabulary namespacing (`tier` → `*`)
Rulings: D12 (namespacing, not supersession), CC-11. **Wave-0** (mechanical, with L2).
- **L6.1 (par):** `tier` → `autonomy_tier` (T1–T4) / `eu_ai_act_risk_tier` (0–4) / `dr_tier` / `storage_tier` / `tenant_class` (retire tenant-tier per 0329). Rename ADR-0163 "environment tiers" → "environment stages". **Do NOT touch** already-namespaced `*_tier` identifiers. Glossary partition of the overloaded word "tier" (tenant-class vs namespaced-tier vs authority-tier vs edge/origin-tier vs IaC Tier-A/B/C).

### L7 — Owed-depth capture + restorations
Rulings: D-DEPTH, D-KR, D-RECOVER, .Trash recovery, D9 vertical-coverage. **Wave-2**, after L1/L5.
- **L7.1 (par, RECOVER-as-ADR):** KR HR/payroll 8-pack bundle (.Trash #1, hangs off D-KR enum); First-Proof-Slice + M3 first-paid-customer folded into ONE "first buildable+sellable slice" artifact (.Trash #2 + Theme 6); 4-lane source-closure model + anti-false-closure validators (.Trash #3).
- **L7.2 (par, FOLD-into-doc):** released-view record-boundary invariant → ADR-0332 (door:one-way); medical→emr confirm; Connect per-context DEK matrix → comms doc; ecosystem-as-a-service/no-internal-bypass invariant; trust-plane 9-KMS-key-class taxonomy + SCIM residual; economic-substrate deferral rationale; 8-stage delivery chain.
- **L7.3 (seq, owed-depth list):** Bind each TRACKED-PARTIAL / DEPTH-OWED item (CDSS→0332, Manufacturing-AI→0033, AMR→0027, Bid-Pricing→0115, NERC-CIP/IEC-62443/SCADA, SGP4/orbital, capital-markets ladder, MFDS/HITL, ISA-95, SLAM/ROS2/VDA-5050, 낙찰하한율) to its vertical's M0 operating-contract gate — recorded, NOT authored now.
- **L7.4 (seq, terminal):** **Vertical-coverage map** (task #18) — every vertical, compliance regime, in-source vs net-new (defense + power-grid net-new), feeding new ADRs + masterplan. **DROP: Bominal Train** (verified: no "Train" ADR exists — it was a lost-context backlog item, nothing in the ADR corpus to drop). Law/Finance survive as named-deferred tracks.

### L8 — oya-gate/oya-cli SUPERSEDE + CaC/CaaS governance-pipeline re-homing [Δ2/Δ3]
Rulings: D-CLOUD-NATIVE (oya-gate/oya-cli stale→SUPERSEDE) + D-GOVERNANCE-CENTRAL (governance→PaC/CaC/PaaS/CaaS) + D-LLM-DOMAIN. **Wave-1** (ADR re-authoring, with L1-refound — NOT a mechanical rename, so it is NOT in L2). Distinct from L2's *eradication*: here the CLI **mechanism** is retired and its sound **logic** is re-homed, not a word-scrub.
- **L8.0 (seq, FIRST, Wave-1):** Author the **governance-pipeline ratifier ADR** — PaC (Cedar policy-as-code) / CaC (compliance-as-code) / PaaS (owned PARC PDP) / CaaS (compliance pipeline) as the single central governance authority, run by GitHub-Actions (live) / oya-ci (shadow). This is the destination every superseded `oya gate`/`oya verify` check re-homes to. Converges with L4 CC-2 (Cedar→contract+PARC).
- **L8.1 (par, after L8.0):** Mark **Superseded** every ADR whose mechanism IS `oya gate`/`oya cli`/`oya check`/`oya verify`/`oya-dev-cli`/`oya git` (per the personal-verification register: e.g. 0108/0109, 0123/0128/0129/0135, 0167 tenant-cli, 0223 oya-git, 0362–0369, 0388, 0391). Re-author the preserved gate/governance/honest-claim LOGIC (0129 honest-claims, 0135 aspirational-enforcement, …) as CaC/CaaS pipeline gates in the L8.0 framework. Obey the coupled-invariant rule (both supersession edges + status flip in one act; G-XA).
- **L8.2 (seq):** Amend the foundational grammar/enum — **0056 `oya-check-*` BNF**, **0092/0105 `cli` layer + check-family** — to drop the CLI/check productions in favor of the pipeline-gate convention. (This is an *amend*, not supersede: the layer-enum + crate grammar survive; only the CLI/check productions change.)
- **L8.3 (seq, verification):** Re-home every `verified_by: "oya gate validate …"` field (live in Accepted **0481:25,:33**; sweep the corpus for others) to the CaC/CaaS pipeline. The brand-residue gate does NOT block `oya gate`/`oya cli` (they are supersede, not forbidden-vocab) — but the **masterplan drift-gate** must show zero live `oya gate` *gates* once L8 lands (the dashboard's pending-amendment badges go to zero).

---

## C. DEPENDENCY EDGES between lanes

```
A.0-1 census-reconcile (G1) ───> {L2.0, L2.1, L2 verifier} (single census-of-record 831/43)  [GATING, FIRST]
A.0-2 consolidation DESIGN-freeze (G2)> {L2.2 skip-provisional, L3.3, L1-amend}                [GATING, FIRST, 1-way, PROVISIONAL — R3/Finding-2]
L1.0-CONS provisional design-set ──> L1-amend (Wave-0 ∥ L2/L3/L6; touches only non-folded files) [S1; safety-net = Wave-1 confirm]
L1.1 re-foundation authored ───> L1.1-CONS-CONFIRM (file-fold == provisional? delta = re-review + revert) [R3/Finding-2; Wave-1]
L1.0 renumber-MAP-freeze ──┬──> L1.0-VERIFY MAP self-consistency gate (bijection/surjection)   [G3/S6]
                           ├──> L3.3 dangling-ref fixes (sweep against frozen id-space)         [driver D-2]
                           └──> L1.3 apply-renumber (terminal)
L1.0-VERIFY (MAP green) ───────> founder sign-off on L1.0/L1.0-CONS                              [G3]
L2 foundry-rename ∈ pre-backfill sweep ─────────────────────────────────────────────────────── [RULED: D11(d)+:52, batched one-way before backfill — §A.5]
L2 foundry-rename ─····─────────> L1-refound ADR-0000+ re-author (internal sequencing only)       [DERIVED: D-1, founder-ratifiable, NOT ruled — §A.5; sweep-membership above is RULED]
L2.0 routing-rule-freeze ──────> L2.0b journeys/personas census ──> L2.2 per-file rename        [R6]
L6 vocab-namespacing ──────────> L1-refound re-author (new ADRs inherit namespaced vocab)
L3 integrity sweep ────────────> L7 backfill/restorations (sweep before backfill)              [RULED D11]
L3 integrity sweep ────────────> Wave-3 masterplan backfill (each fix verified first)          [RULED D11]
L1.2 DROP (0325/0316/0349-half)─> L1.2-DROP inbound-reference-safety gate (Wave-1)              [G4/EXTEND-2]
L7.4 DROP Train ───────────────> L1.2-DROP inbound-reference-safety gate                        [G4/EXTEND-2]
L1.1 ADR-0000+ template ───────> L5 new/reshaped ADRs (inherit re-foundation template)
L1 + L5 (clean ADR front-matter)─> Wave-3 oya gen masterplan + drift gate                       [D1; ASPIRATIONAL]
all Wave-1/2 supersessions ─────> Wave-3 terminal cross-ref re-resolution (delivered graph clean) [R2/SY2; re-runs L3.3]
LAST Wave-2 supersession ═TERMINAL BARRIER═> Wave-3 masterplan backfill (hard-block; 2nd serialization) [R3/Finding-3; D13:43; NAMED]
L1 data/event ADR (D-EVENT) ───> L4 CC-6/CC-9 doc re-frames (D-EVENT precedence, 0005 amended)  [R5]
L1 policy ADR ─────────────────> L4 CC-2 (PARC engine named) doc re-frames
L2 ────────────────────────────> L4 CC-1 (foundry doc re-frames)
L5 safety-gate ADR ────────────> L7.3 owed-depth (each vertical references the gate)
D15 domain field (L1 meta-ADR) ─> Wave-3 cohesion-gate-by-construction                          [ASPIRATIONAL]
```

**Critical path:** A.0-1 census-reconcile + A.0-2 consolidation-freeze (GATING) → L1.0-CONS → L1.0 MAP-freeze → L1.0-VERIFY → (L2+L3+L6+**L1-amend** Wave-0) → L1-refound re-author → L1.2 Proposed batch (+L1.2-DROP gate) → L1.3 renumber → L5 → L7 → Wave-3 masterplan backfill + cross-ref re-resolution. **Hard ordering constraints (`[RULED]`, non-negotiable — each cites an SSOT line, see §A.5):** census-reconcile-before-rename (A.0-1/G1, `:107`), consolidation-freeze-before-rename (A.0-2/G2, door:one-way), map-before-sweep (D-2, D13`:43` no-dangling invariant), sweep-before-backfill (D11`:52`), **L2-foundry-rename-∈-the-pre-backfill-sweep (D11(d)+`:52` — R3/Finding-1, re-tagged RULED this round)**, MAP-verified-before-sign-off (G3). **`[DERIVED]` (recommended, founder-ratifiable, NOT in the hard set):** rename-before-reauthor *internal sequencing only* (D-1 — §A.5; engineering double-work, sufficient to recommend Option 1, but L2-vs-L1.1 ordering is a founder choice in §F — bounded by the RULED sweep-membership above). **D-LANES recovery:** L1-amend runs in Wave-0 (§A.4/S1) so the within-Wave-0 narrowing is minimal — but see §A.4 for the SECOND (Wave-3 terminal) serialization the recovery does NOT recover.

---

## D. VERIFICATION GATES per lane (separate verifier lane vs primary sources)

> Founder rule: the verifier lane is NEVER the authoring lane; verdicts are evidence-based against real files; no phantom findings; an unverified verdict never authorizes a delete/amend. Verifier sizing: small→haiku, standard→sonnet, large/security/one-way→opus.

- **A.0 gate (GATING, pre-Wave-0):** verifier confirms `00-REST-OF-DOCS-REGISTER.md` no longer carries the stale 731/105 figures (the correction PR landed, pointing at `decision-record:107`); confirms a single census-of-record (831/43) is referenced by every downstream L2 gate; confirms the A.0-2 consolidation **DESIGN-set** exists as a frozen *provisional* primary source covering every cluster-level intended fold (R3/Finding-2 — the founder signs the design intent, NOT a final file map; file-level stability is the Wave-1 L1.1-CONS-CONFIRM check, not this gate). **Wave-0 does not start until this gate is green.**
- **L1.1-CONS-CONFIRM gate (Wave-1, before L1.3 archival) [R3/Finding-2]:** for each authored re-foundation ADR, the verifier confirms its actual file-level fold set == the Wave-0 provisional design-set; **any delta is a re-review trigger (re-open A.0-2 door:one-way), NOT a silent amend**, and any L1-amend rename on a newly-discovered fold is reverted before archival. No file is archived by L1-refound until its fold is confirmed-or-re-reviewed. opus verifier (one-way re-open).
- **L1.0-MAP gate (GATING, before founder sign-off) [G3/S6 + R2/SY3]:** verifier proves MAP self-consistency — renumber **bijection** (every linux 0001–0026 + 0377 mapped; no two old-ids→same new-id; no new-id reused), consolidation **surjection** (every archived old-id covered by exactly one 0000+ target), **no id-reuse** vs live 0000–0514, **and consolidation COMPLETENESS against a stated closure criterion** [SY3] (every foundry-sense re-foundation-candidate classified consolidate-or-amend, none unclassified — so the S1 L1-amend partition is provably total, no Wave-1 fold retroactively wastes an L1-amend rename; FLAG-C). Founder sign-off only after this is green. opus verifier (one-way + downstream-poisoning).
- **L1 gate:** verifier diffs every ADR-0000+ ADR against (a) the decision-record ruling it implements and (b) the disposition-table row; confirms `supersedes`/`consolidates` edges resolve to real ids in the frozen MAP; confirms no in-place edit of a frozen ADR (immutability); confirms L1-amend touched only non-consolidation-set files (no wasted rename on a to-be-archived original). One-way ADRs: opus verifier + founder sign-off.
- **L1.2-DROP gate [G4/EXTEND-2]:** for each dropped id (0325/0316/0349-Jenkins-half, Train), verifier runs a corpus-wide inbound-reference resolver *after* the drop → zero live citations OR each re-pointed to its superseding id (e.g. 0316→0329). A drop is not done until inbound-clean.
- **L2 gate:** verifier greps the post-rename corpus for residual `foundry` outside carve-outs (expect 0 genuine); confirms all **43** Palantir (census-of-record per A.0-1, NOT 105) + Marlboro-Forge + retirement-record hits UNCHANGED; confirms the per-file count reconciles to the **post-L2.0b census** [R2/SY4] — i.e. the 831 figure *after* L2.0b hardens the journeys/personas tail (which is a sampled estimate at freeze time per A.0-3; "reconciles to 831" before L2.0b would be reconciling to a number with a soft tail); confirms no mixed-prefix seam; confirms binary/package names reconcile against `microservices/`.
- **L3 gate:** verifier confirms KCMVP/KISA strings restored in all 6+ files vs the pre-corruption reference; greps for remaining tautologies/`all all`; runs a cross-ref resolver over the whole ADR set → zero dangling supersedes/amends edges against the frozen id-space.
- **L4 gate:** per-CC, verifier confirms the canon claim now matches the ADR (DECISION→ADR) or the GENERATED-REFERENCE was regenerated (not hand-edited); CC-10 confirmed as framing-only; FP carve-outs (Cedar workflow-engine, M3-device, Tom-Jenkins) untouched.
- **L5 gate:** verifier confirms each new ADR carries the `domain` field and references the safety-gate where required. **[R3/Tension-C — adversarial cohesion stand-in]:** because the cohesion gate is [ASPIRATIONAL] (not on `dev`), the manual stand-in must NOT be a single lane both *producing* the no-contradiction verdict and *being* the only check on it (forbidden self-approval). The cohesion stand-in is run by a **second, adversarial lane whose mandate is to FIND a contradicting atom** (falsification mandate, not confirmation) against the sibling read-set, and the verdict is routed to a second opus lane or founder door:one-way. Two lanes reading the same set with the *same* confirmation mandate can share a blind spot; an adversarial mandate is the only manual approximation of the gate's by-construction guarantee. Also confirms the safety-gate runtime hook is marked `runtime-hook-pending`, not asserted-live.
- **L6 gate:** verifier confirms namespaced `*_tier` identifiers UNCHANGED; only bare `tier`/`tenant-tier` plan-axis sites + the persona template changed; glossary partition present.
- **L7 gate:** verifier confirms each owed-depth item is reachable from the vertical-coverage map + masterplan (worth-documenting⇒reachable); Train dropped; Law/Finance present as named-deferred; the 3 RECOVER-as-ADR artifacts exist as real ADRs.
- **Wave-3 gate:** verifier confirms the generated masterplan (or its manual stand-in, labelled "manually generated pending `oya gen masterplan` on dev") emits every Accepted ADR's planning_impact, the must-read session-context bundle is emitted/pointed-to. **[R2/SY2 — terminal cross-ref re-resolution]:** L3.3's whole-corpus cross-ref resolver is **re-run AFTER the last Wave-2 supersession lands**, asserting zero dangling supersedes/amends on the *delivered* graph — not only the Wave-0 frozen snapshot. This closes the build-first growing-graph gap (round-2 FLAG-B/Tension B): supersession edges authored in Waves 1–2 (a fresh `supersedes:[X]` whose X was renumbered/consolidated between MAP-freeze and authoring) live OUTSIDE the Wave-0 snapshot the original L3 gate proved; G4 closes inbound-orphan-on-drop but not inbound-dangling-on-new-supersession. Cheap — re-runs an existing resolver, no new mechanism. **[R4/EXTEND-1 — testable exit evidence]:** "zero contradicting ADRs survive the cohesion read-set" is NOT provable by a bare verifier-green when the gate is aspirational and the stand-in is the same lane that would find the atoms. The exit requires a **published evidence artifact**: (a) the cohesion read-set diff, and (b) the **adversarial second-lane's falsification log** showing zero contradicting atoms found *after a real search* (per the L5 adversarial mandate). An unfalsifiable assertion is not an acceptance criterion; this turns it into one.

---

## E. SOURCE-REGIMEN PR / sign-off points

- **One-doc-per-PR:** every ADR (re-authored, new, renumbered, Proposed-resolved) and every triggering doc (DOC-CATALOG/CHANGELOG/glossary rows ride with the doc that causes them) lands as its own PR to `dev`. The 831-file rename batches per file-family (template-first) but still as reviewable per-family PRs, not one mega-commit.
- **Door:one-way founder sign-off points (manual gate, founder credentials required):** the §A.3 sequencing decision itself; **the §A.3 Option-1-vs-Option-2 wave-shape as a GENUINE but BOUNDED founder choice [R2/SY1; R3/Finding-1]** — because the rename-before-reauthor *internal sequencing* edge that selects Option 1 is `[DERIVED]` not `[RULED]` (§A.5), the founder is asked to actually choose Option 1 (recommended, single-pass-over-clean-text) vs Option 2, NOT to rubber-stamp a closed ruling. **BUT Option 2's legal shape is BOUNDED (R3/Finding-1): it means "author the ADR-0000+ shells first, THEN run the still-pre-backfill sweep over them" — the foundry rename MUST remain INSIDE the pre-backfill sweep (D11(d)+`:52`, RULED). An Option-2 that runs the rename after re-foundation/backfill is NOT a legal alternative and is not on the menu. The founder chooses the sequencing within the RULED sweep-membership envelope, not a clean free choice.** **the §A.4 D-LANES-narrowing tradeoff** (serial-waves narrowing a one-way organizing principle is itself one-way — signed together with §A.3; its text now reads "buys D-2 (RULED) + DERIVED D-1 (founder-ratifiable)") [G5/FLAG-2]; **the A.0-2 ADR-0000+ consolidation DESIGN-set freeze** [G2] — *the founder signs the Wave-0 PROVISIONAL design-level classification; the file-level fold map is CONFIRMED-not-assumed by the Wave-1 L1.1-CONS-CONFIRM checkpoint, and any delta re-opens this door (R3/Finding-2 — no door:one-way is signed over a guaranteed-final file map that Wave-1 authoring produces)* — and **the L1.0 renumber MAP** (the latter only after its L1.0-VERIFY self-consistency + consolidation-completeness gate is green) [G3/SY3]; every one-way ADR in L1.1/L1.2 (the §201 cluster: fork/SSOT-fork, forge, data-tier, identity/self-mod, scope-breadth, license/residency, substrate-canon); the safety-gate ADR (D-SAFETY, shipped design+invariant with runtime hook `runtime-hook-pending`); the KR-home-market-first posture + employment-enum (D-KR); the infra-sovereignty build order (D-SEQ Q1.1); released-view record-boundary (L7.2); ADR-0247 self-mod ceiling.
- **[R2/SY1 + R3/Finding-1 — BIDIRECTIONAL ruling-provenance pass gates this whole section]:** before ANY §E door:one-way sign-off, the §A.6 ruling-provenance verifier pass MUST be green **in BOTH directions** — **(1)** every `[RULED]` tag in the plan resolves to a citable SSOT line (no `[DERIVED]` edge presented as non-negotiable law — no RULED-inflation); **AND (2)** every SSOT-ruled amendment ACTION carries its matching `[RULED:<line>]` tag in the plan (no ruled constraint left `[DERIVED]`/untagged — no RULED-deflation; specifically **L2's sweep-membership is RULED `D11(d)+:52`**, not merely DERIVED). The founder must sign an honest plan, not one that has promoted a planner's engineering instinct into a founder ruling **nor one that has silently deflated a ruling into a free choice**. A pass that surfaces additional false-RULED tags **OR any RULED-deflated action** is a re-review trigger, not a silent fix.
- **ASPIRATIONAL-vs-ENFORCED per step:** `oya gen masterplan`, the drift gate, the domain-cohesion gate, the safety-gate runtime hook are **[ASPIRATIONAL]** (worktree-only) → run **manually** with a verifier lane standing in, and file a follow-up to port them to `dev`. The supersede-never-edit immutability check, signed-commits, linear-history, brand-residue lane (once `foundry` is wired in) are **[ENFORCED]** today.
- **[R3/Finding-4/PV-3 — aspirational-tooling liveness is QUERYABLE, not a dated snapshot]:** each [ASPIRATIONAL] tooling item carries a **`cutover_status:` front-matter field** on the masterplan-wiring meta-ADR (L5.1) — `oya_gen_masterplan` / `cohesion_gate` / `drift_gate` / `safety_runtime_hook`, each valued `worktree-only` now and flipped to `live-on-dev` when ported — mirroring the `cutover_trigger:` pattern above. This makes the canon carry CURRENT generator-status (a reader queries the field, not a prose date that goes stale), and keeps the generator-to-`dev` follow-up visibly tracked (an item is not "done" until its `cutover_status:` reads `live-on-dev`). The verifier confirms the field exists and reflects reality at audit time.
- **Build-first-cutover-later PR discipline:** ADRs superseded by not-yet-built owned things land as `superseded-on-cutover (pending build+proof)`, keeping Jenkins/Argo/data-bridges operative; no "archive the bridge" PR until the owned replacement's proof-gate fires.
- **[R2/S4/§5 gap — cutover ownership]:** every `superseded-on-cutover` ADR MUST carry a required front-matter field **`cutover_trigger:`** pointing at its named trigger (ADR-0510 cutover-trigger / ADR-0250 build-ahead, per `decision-record D-META:26`). This makes the "pending-cutover" set **queryable with a named gate per entry** and gives each pending supersession an owner + evidence-pointer. Without it, build-first-cutover-later silently degrades to build-first-cutover-**never** and the corpus carries permanent dual-canon (old bridge ADR + new owned ADR both live). The verifier confirms no `superseded-on-cutover` ADR lacks `cutover_trigger:`.

---

## F. FOUNDER CREDENTIALS / sign-off / GitHub auth required

- **GitHub auth (founder):** push access + branch protection on `jason931225/oyatie dev`; **signed commits** (founder GPG/SSH signing key) for linear history; PR-to-`dev` then door:one-way founder sign-off before any merge. The amendment writes to `dev`, never to a protected default without sign-off.
- **Founder sign-off (decision, not auth):** all §E door:one-way points. The RALPLAN-DR sequencing decision (§A) must be signed before Wave-0 starts.
- **Where the founder is the blocker:** the §A.3 sequencing decision + **the Option-1-vs-Option-2 wave-shape choice (R2/SY1, a real BUT BOUNDED choice — D-1 internal-sequencing is DERIVED, but L2's sweep-membership is RULED D11(d)+`:52`, so Option 2 is bounded to "shells-first then still-pre-backfill sweep"; R3/Finding-1)** + §A.4 D-LANES-narrowing (signed together, gate Wave-0); the A.0-2 consolidation-set freeze + the renumber MAP (L1.0, after L1.0-VERIFY bijection+completeness green); the one-way Proposed cluster (L1.2); the safety/KR/infra-sequence ADRs (L5); the self-mod ceiling (0247); the vertical-coverage scope rulings (L7.4 — esp. net-new defense/power-grid). **Data predicates A.0-1 (census) + A.0-2 (consolidation-set) gate Wave-0 start — A.0-1 is a verifier+correction task, A.0-2 is founder-signed. The §A.6 ruling-provenance pass gates all sign-off (no §E door opens until every RULED tag is SSOT-backed).**

---

## G. RALPH LOOP structure

**The boulder:** "every ruled D-decision is applied to `jason931225/oyatie` as a clean, verified, reachable ADR/doc, with the masterplan generated from it." The ralph loop drives one **unit-of-amendment** per iteration (a single ADR/doc/rename-family = one-doc-per-PR), wave-ordered.

**Sign-off entry predicate (GATING, before ANY door:one-way founder signature):** the **§A.6 ruling-provenance pass** is green **in both directions** (direction 1: every `[RULED]` tag resolves to an SSOT line, no `[DERIVED]` edge presented as law, the fabricated quote deleted; **direction 2: every SSOT-ruled action carries its matching `[RULED]` tag — no RULED-deflation; L2-sweep-membership RULED `D11(d)+:52`**) AND the **§A.3 Option-1-vs-Option-2 wave-shape** is presented to the founder as a genuine **but BOUNDED** choice (Option 2 keeps the rename inside the pre-backfill sweep; not a closed ruling, not a free choice). The founder must sign an honest plan [R2/SY1; R3/Finding-1].

**Wave-0 entry predicate (GATING):** no unit is selectable until **A.0-1 census-reconciliation** (register corrected to 831/43) AND **A.0-2 consolidation-set freeze** (founder-signed) are green. These two are the first Wave-0 units and gate everything downstream (the irreversible lanes run against them).

**Each loop iteration:**
1. **Select** the next ready unit from the wave-ordered backlog (a unit is *ready* iff all its §C dependency edges are satisfied — e.g. no L2.2 unit is ready until A.0-1 census + A.0-2 consolidation-set + L2.0 routing-rule + L2.0b journeys census are frozen; no L1-refound unit is ready until L2 + L6 vocab + L1.0 MAP (L1.0-VERIFY green) are frozen; no DROP is done until its L1.2-DROP inbound gate is green).
2. **Author/amend** in the authoring lane: re-author into ADR-0000+ (never in-place on a frozen ADR), or apply the rename/sweep/namespace fix, carrying DOC-CATALOG/CHANGELOG/glossary rows.
3. **Verify** in the separate verifier lane against primary sources (§D gate for that lane). Fail → iterate (do not advance).
4. **Gate:** if door:two-way → ratify-on-green; if door:one-way → open PR to `dev` and PARK for founder sign-off (the loop continues with other ready units; the parked unit is not marked done).
5. **Record** the unit as done only when verified AND (two-way green OR one-way signed). Update the backlog + the disposition/ledger row status.

**Exit criteria (the boulder stops):**
- **§A.6 ruling-provenance pass green in BOTH directions** (direction 1: every RULED tag SSOT-backed, D-1 correctly DERIVED, fabricated quote deleted; **direction 2: every SSOT-ruled action carries a matching RULED tag — L2-sweep-membership RULED `D11(d)+:52`, no RULED-deflation**) and the Option-1-vs-Option-2 wave-shape founder-signed as a genuine but BOUNDED choice [R2/SY1; R3/Finding-1].
- A.0-1 + A.0-2 base-data predicates green (single census-of-record; consolidation-set frozen, **proven complete not just frozen** per L1.0-VERIFY SY3) — the precondition for everything else.
- Zero pending units across L1–L7; every Proposed in ledger 03 has a recorded RATIFY/DROP/KEEP outcome; every CC-1..CC-13 closed; renumber MAP fully applied (L1.0-VERIFY bijection+completeness green) with zero dangling edges; **every DROP passed its L1.2-DROP inbound-reference-safety gate (zero live orphans).**
- All door:one-way units founder-signed (none parked) — including the wave-shape choice, §A.4 D-LANES-narrowing, A.0-2 consolidation-set, L1.0 MAP.
- The verifier lane has evidence-passed every lane gate (§D) and the Wave-3 masterplan-backfill gate, **and the Wave-3 terminal cross-ref re-resolution proves zero dangling supersedes/amends on the DELIVERED graph (post-last-Wave-2-supersession), not just the Wave-0 snapshot** [R2/SY2].
- The masterplan is GENERATED (or its manual stand-in produced, labelled) and emits every Accepted ADR's planning_impact + the must-read session-context bundle. **"Zero surviving canon-contradictions" is proven by the published evidence artifact (cohesion read-set diff + adversarial falsification log showing zero atoms after a real search) — NOT a bare verifier-green** [R4/EXTEND-1].
- Every `superseded-on-cutover` ADR carries a `cutover_trigger:` field (no ownerless pending-cutover entries) [R2].
- **Standing caveat (R2/SY5): amendment-complete ≠ gates-live.** The boulder may stop with every ruling applied and verified, BUT the "manual stand-in → ported gate" debt (`oya gen masterplan`, cohesion gate, drift gate, safety runtime-Cedar hook — all [ASPIRATIONAL], worktree-only) remains OPEN and is tracked as a first-class deliverable in the masterplan-wiring meta-ADR (L5.1/L1.1) **via the queryable `cutover_status:` field (R3/Finding-4) — each item is not "done" until its `cutover_status:` reads `live-on-dev`, so the canon carries the CURRENT port-status, not a dated snapshot a reader must distrust** — until ported to `dev`. "Boulder stopped" must NOT be read as "tooling real" — a manual verifier-green is strictly weaker evidence than a gate-green-on-dev.
- Then, and only then, invoke cancel.

---

## H. DELIBERATE-MODE — pre-mortem + expanded verification

### H.1 Pre-mortem (failure scenarios + mitigations)

**Failure 0 (NEW, HIGHEST-PRIORITY) — The verifier inherits a self-contradictory census and either stalls or silently miscounts on the irreversible lane.**
The L2 verifier, obeying the founder rule (re-read primary sources each iteration, never trust a prior verdict), reads `00-REST-OF-DOCS-REGISTER.md` (731/105) and the SSOT (`decision-record:107`, 831/43) and cannot self-resolve which is canon. On the 831-file irreversible rename it then either (a) permanently stalls (reads 105, asserts 43, never reconciles), or (b) worse, reconciles to 105 and lets ~62 mis-counted governance-sense files through as intelligence-sense — a newly-authored CC-1-class contradiction, the amendment producing the exact defect it exists to kill.
- *Mitigation:* **A.0-1 (G1) reconciles the census BEFORE Wave-0** — `decision-record:107` is the single census-of-record (831/43); a one-doc-per-PR correction lands on the register as the first Wave-0 unit; every L2 verifier reference is re-pointed at the corrected number. The A.0 gate blocks Wave-0 start until the register no longer carries 731/105. This is a *data-correctness predicate, not a wording nit* — without it the irreversible lane has a built-in guaranteed-miscount path.

**Failure 1 — Blind foundry-swap breaks Palantir refs (and mis-routes the governance sense).**
The 831-file rename runs as a uniform `s/foundry/intelligence/`, corrupting the 43 Palantir-Foundry third-party refs, the Marlboro-Forge fiction, and the retirement record; and mis-routing the ~135 governance-sense `foundry-fitness` hits to intelligence.
- *Mitigation:* L2.0 freezes the sense-routing rule + L2.1 locks HARD carve-outs (43 Palantir per the census-of-record) as primary source BEFORE any rename; L2.0b completes the journeys/personas census so L2.2 runs against a real per-file census, not a sampled estimate; **L2.2 skips files in the A.0-2 consolidation set** (renamed once on the new ADR-0000+ text instead). The L2 verifier gate asserts the 43 Palantir + Marlboro + retirement-record hits are byte-unchanged and zero mixed-prefix seams exist. Template-first (fix generator, not 129 outputs) shrinks the blast radius. **This is why L2 is its own lane with a freeze-step, not a sed one-liner.**

**Failure 2 — ADR renumber breaks cross-refs (real dangling vs renumber-artifact indistinguishable).**
Renumbering linux 0001–0026→0515+ and dedup-0377 rewrites every cross-ref; if the integrity sweep runs against a moving id-space it cannot tell a genuine dangling ref from a renumber artifact, and the ADR-0000+ consolidation edges point at archived ids.
- *Mitigation:* L1.0 freezes the renumber MAP as the single primary source FIRST (Wave-0); L3.3 runs against the frozen post-map id-space; the L3 verifier gate runs a whole-corpus cross-ref resolver expecting zero unresolved edges; L1.3 applies the renumber only after L3 is green. Branch-locality edges to `origin/dev` (0421/0457/0429/0443/0428) are explicitly deferred to merge-time re-resolution, not faked now.

**Failure 2b (NEW) — A bad renumber/consolidation MAP poisons L3.3 + L1.3 silently.**
The MAP is door:one-way founder-signed, but a founder sign-off is a *governance* act, not a bijection check — the founder cannot hand-verify a 372-row id-map. If the MAP has a collision (two old-ids → same new-id), a reused new-id, or a consolidation gap (an archived id with no 0000+ target), L3.3's "zero dangling edges" passes against a wrong target-space and L1.3 applies a corrupt renumber — both downstream lanes fail silently.
- *Mitigation:* **L1.0-VERIFY (G3/S6)** — a separate opus verifier proves renumber-bijection + consolidation-surjection + no-id-reuse *ahead of* the founder sign-off. The founder signs the governance decision; the verifier proves the bijection. No downstream lane builds on an unverified MAP.

**Failure 2c (NEW) — A DROP leaves dangling inbound orphans.**
L3.3 verifies *outbound* dangling edges in Wave-0, but the L1.2 DROPs (0325/0316/0349-half) and the L7.4 Train drop are authored in Wave-1/2 and create *inbound* orphans L3.3 never re-checks. Dropping 0316 is safe only if nothing still cites it live.
- *Mitigation:* **L1.2-DROP gate (G4/EXTEND-2)** — for each dropped id, a corpus-wide inbound-reference resolver runs *after* the drop and proves zero live citations OR each re-pointed to its superseding id (0316→0329). A drop is not done until inbound-clean.

**Failure 3 — masterplan-generator not on `dev` blocks backfill (silent decision-drop) + the cohesion stand-in self-approves.**
`oya gen masterplan` + the cohesion/drift gates live only in `.claude/worktrees`; the Wave-3 backfill + the no-contradiction-by-construction guarantee (D15) cannot actually run on `dev`, so either the lane stalls, or someone hand-authors the masterplan (violating D1), or a single verifier lane both *produces* the manual no-contradiction verdict and *is* the only check on it (forbidden self-approval).
- *Mitigation:* every gate-dependent step is flagged **[ASPIRATIONAL]** and executed **manually**. **[R3/Tension-C]** the cohesion stand-in is run by a **second, adversarial lane whose mandate is to FIND a contradicting atom** (falsification, not confirmation), with the verdict routed to a second opus lane or founder door:one-way — never single-lane self-cleared; an adversarial mandate is the only manual approximation of the gate's by-construction guarantee (two confirmation-mandate lanes share a blind spot). A **follow-up to port the generator to `dev`** is filed as a first-class deliverable of the masterplan-wiring meta-ADR (L5.1/L1.1), and **[R1/S5/FLAG-3; R3/Finding-4]** `decision-record D1` is superseded to record the generator status as a **queryable `cutover_status:` field** (not a static dated snapshot — values `worktree-only`@2026-06-06, flipped to `live-on-dev` on port) so the aspirational status survives in the canon as CURRENT truth, not only as a stale date in this (archivable) plan. The masterplan is NEVER hand-authored as authority — the manual stand-in is a GENERATED-REFERENCE artifact labelled "manually generated pending `oya gen masterplan` on dev." D1's "backfill ≡ clean ADR front-matter" means the durable work (front-matter completeness) lands on `dev` regardless of generator availability.
- **[R1/§6 — safety sub-case, highest-consequence]** the safety-gate ADR (D-SAFETY, a liability/safety commitment) ships as a *design+invariant* doc, but its "hooked into D16 runtime Cedar gate" claim is marked **`runtime-hook-pending`, NOT asserted-live** — asserting live safety *enforcement* that runs only in a worktree is the single highest-consequence aspirational-vs-enforced gap, and a manual verifier stand-in for a safety gate is weaker than a real runtime gate; the claim must not be overstated.

**Failure 4 (NEW, HIGH — round-2 FLAG-A/SY1) — a planner-DERIVED ordering edge is asserted as a founder RULING and forecloses a real founder choice, becoming canon uncaught.**
The plan elevates "rename-before-reauthor (D-1)" to `RULED` in three places and (old line 53) quotes a non-existent SSOT "dependency note … to avoid double-work." The SSOT rules no such order (D11`:52` = sweep-before-backfill; D13`:42-43` = re-founding with no rename-ordering; `grep double-work` = ZERO). This propagated through THREE prior lanes uncaught — each trusted the prior lane's "RULED" tag instead of grepping the SSOT, the exact verdict-propagation the founder rule forbids. Consequence: the founder is asked to rubber-stamp an irreversible wave order as "non-negotiable law" when it is in fact a legitimate choice (Option 1 vs Option 2) the plan foreclosed by fabricated authority; if uncaught it becomes canon, and §A.1 principle-4 ("an unverified verdict never authorizes an amend") is violated by the plan's own justification.
- *Mitigation:* **[R2/SY1]** the fabricated quotation is **deleted**; D-1 is relabeled **`[DERIVED]`** (lines in §A.2/§A.5/§B-L2/§C — all marked this round); the **`[RULED:<line>]` vs `[DERIVED:<driver>]` provenance discipline** (§A.1 principle 6) is applied to every ordering constraint; the **§A.6 ruling-provenance verifier pass** (separate opus lane) checks EVERY RULED tag corpus-wide against a citable SSOT line BEFORE founder sign-off, using the same grep-the-SSOT discipline that caught D-1; the **Option-1-vs-Option-2 wave-shape enters the §F founder sign-off set as a genuine choice.** The order does NOT change (Option 1 is still the engineering-correct call); its *status* changes from foreclosed-law to founder-ratifiable. **A pass that surfaces further false-RULED tags is a re-review trigger, not a silent fix** (round-2 critic §5).

**Failure 5 (NEW, MEDIUM — round-2 FLAG-B/Tension B/SY2) — the cross-ref graph grows through Waves 1–2 under build-first, and "zero dangling" was only ever proven of the Wave-0 snapshot.**
Build-first-cutover-later (D-META`:26`) keeps `superseded-on-cutover` ADRs live (not archived), so the corpus *grows* as Wave-1/2 supersessions land. L3.3 + the L3 gate run only in Wave-0 against the frozen id-space; the G4 DROP gate closes inbound-orphan-on-drop but NOT inbound-dangling-on-new-supersession (a freshly authored `supersedes:[X]` whose X was renumbered/consolidated between MAP-freeze and authoring). The `zero dangling` exit was therefore true of the Wave-0 snapshot, not the *delivered* graph — a real integrity hole the MAP-freeze cannot close (you cannot both honor build-first/corpus-grows and prove the final graph clean with a single Wave-0 sweep).
- *Mitigation:* **[R2/SY2]** a **Wave-3 terminal whole-corpus cross-ref re-resolution gate** re-runs the L3.3 resolver AFTER the last Wave-2 supersession lands, asserting zero dangling supersedes/amends on the *delivered* graph. Cheap — re-runs an existing resolver, no new mechanism. Wired into §C edges, the §D Wave-3 gate, and the §G exit.

**Failure 6 (NEW, HIGH — round-3 Finding-2) — the Wave-0 consolidation-set freeze is signed door:one-way over a PROVISIONAL fold map that Wave-1 authoring revises.**
A.0-2/L1.0-CONS freezes "which old ADRs fold into which 0000+ doc" door:one-way at the very start of Wave-0, but the file-level fold map is the OUTPUT of authoring the re-foundation ADRs (Wave-1). A door:one-way over a map Wave-1 may revise is a one-way door over a guess (PV-2). If Wave-1 discovers a missed fold, an L1-amend rename already landed on a now-archived file → wasted rename + dirtied immutability diff — the exact defect A.0-2 exists to prevent. Completeness ≠ stability-under-authoring; L1.0-VERIFY's Wave-0 completeness check cannot close it.
- *Mitigation:* **[R3/Finding-2]** SPLIT the freeze — Wave-0 freezes only the *cluster-level design INTENT* the SSOT genuinely rules (D3/D5/D6/D7, `:114-118`; D13 `:42-43`) as a PROVISIONAL door:one-way; the Wave-1 **L1.1-CONS-CONFIRM** checkpoint confirms the actual file-level fold == the provisional set when each ADR is authored. Any delta = re-review trigger (re-open the door), NOT a silent amend; any L1-amend rename on a newly-discovered fold is reverted before archival. The founder signs the design (legitimately final at Wave-0); the file map is confirmed-not-assumed at Wave-1. S1's parallelism gets a real safety net (revert-on-delta) instead of an asserted-final freeze.

*(Secondary risks tracked: ledger 03 one-way Proposeds park indefinitely without founder sign-off → ralph keeps other units flowing, never blocks the whole loop on one parked door; the half-migration crate state — `oya-governance-*` 73% vs `oya-intelligence-*` 6% — completed in one L2 pass with the fitness lane rejecting mixed prefixes; verifier-lane drift — the verifier must re-read primary sources each iteration, never trust a prior verdict, per the founder no-phantom-findings rule; build-first-cutover-never — every `superseded-on-cutover` ADR carries `cutover_trigger:` (R2) so the pending set stays queryable with a named gate; CC-9 substrate precedence — D-EVENT/Pulsar is authority, ADR-0005 is amended to it not co-equal-reconciled (R5).)*

### H.2 Expanded verification plan — per-lane "what evidence proves done"

| Lane | Evidence that proves DONE (verifier vs primary sources) |
|---|---|
| **A.6 ruling-provenance (GATING, before sign-off) — BIDIRECTIONAL [R3/Finding-1]** | **Direction (1) RULED-inflation:** every `[RULED]`/"the decision record rules"/"explicitly rules" tag in the plan resolves to a citable `decision-record` line (corpus-wide, not only D-1); `grep double-work` over the SSOT = 0 and the plan no longer quotes a non-existent "dependency note"; relabel is surgical — D-2 (map-before-sweep) + D11 (sweep-before-backfill) stay RULED, only D-1 *internal-sequencing* → DERIVED; no `[DERIVED]` edge presented as non-negotiable law; the Option-1-vs-2 wave-shape is in the §F sign-off set as a BOUNDED choice. **Direction (2) RULED-deflation [R3/Finding-1]:** every SSOT D-decision that names a concrete amendment action (D11(a)-(d), D12, D13, D14, D15, D-EVENT, D-META) resolves to a matching RULED tag in the plan — specifically **L2's sweep-membership is tagged `[RULED: D11(d)+:52]`** (re-tagged this round from DERIVED), with rename-vs-reauthor internal sequencing kept `[DERIVED: D-1]`; no SSOT-ruled action left DERIVED/untagged. **Any further false-RULED tag (dir 1) OR any RULED-deflated action (dir 2) = re-review trigger.** [R2/SY1; R3/Finding-1] |
| **A.0 (GATING)** | `00-REST-OF-DOCS-REGISTER.md` no longer shows 731/105 (correction PR landed, cites `decision-record:107`); single census-of-record = **831/43** referenced by every L2 gate; A.0-2 consolidation set exists as a frozen door:one-way primary source covering every to-be-archived id. Wave-0 blocked until green. |
| **L1.0-MAP (GATING)** | renumber **bijection** proven (all linux 0001–0026 + 0377 mapped, no collisions, no new-id reuse); consolidation **surjection** proven (every archived id → exactly one 0000+ target); **consolidation COMPLETENESS proven** against the stated closure criterion (every foundry-sense re-foundation-candidate classified consolidate-or-amend, none unclassified — S1 partition provably total) [R2/SY3]; **no id-reuse** vs 0000–0514; all green *before* founder sign-off on the MAP. |
| **L1** | Every ruled D-decision maps to ≥1 ADR-0000+ ADR; each ADR's `supersedes`/`consolidates` edges resolve in the frozen MAP; zero frozen-ADR in-place edits (immutability diff); **L1-amend touched only non-consolidation-set files** (no wasted rename); ledger-03 row status updated for all 132 Proposed; one-way ADRs carry founder-sign-off record. |
| **L1.2-DROP** | for each dropped id (0325/0316/0349-half, Train): inbound-reference resolver = 0 live citations OR each re-pointed to its superseding id (0316→0329); no inbound orphan survives the drop. |
| **L2** | `grep -rliI foundry` over post-rename corpus = only the 3 carve-out families; **43** Palantir (census-of-record, not 105) + Marlboro + retirement-record byte-unchanged; per-file count reconciles to the **post-L2.0b census** [R2/SY4] (the 831 figure *after* L2.0b hardens the journeys/personas tail — a sampled estimate at freeze time per A.0-3, so not "831" before L2.0b); consolidation-set files skipped (renamed once on new 0000+ text); `oya-foundry-*` crate refs → 0 (or justified pending-binary-rename); brand-residue lane now flags `foundry`. |
| **L3** | KCMVP/KISA strings present in all 6+ files vs reference; cross-ref resolver = 0 dangling supersedes/amends; 0 `all all`/self-rename tautologies; 0347 status=Accept; 0363 "eradicated" claim corrected. |
| **L4** | Each CC-1..CC-13: the contradicting claim now matches canon (DECISION→ADR) OR the doc is regenerated GENERATED-REFERENCE; CC-10 = framing-only confirmed; all named FP carve-outs untouched; PARC named at the 24 genuine Cedar-engine sites; **CC-9: D-EVENT/Pulsar is precedence authority and ADR-0005 is AMENDED to it (not co-equal-reconciled).** |
| **L5** | safety-gate ADR exists under `governance` with 4 gates; **its D16 runtime-Cedar hook is marked `runtime-hook-pending`, NOT asserted-live**; KR enum ADR has all 8 classes + branching; infra-sovereignty ADR is ordered+M0-gated with NO dates; domain-cohesion meta-ADR adds `domain` to 0364 + gate to 0365; masterplan-wiring meta-ADR present **and carries the [ASPIRATIONAL→port-to-dev] generator follow-up + the paired `decision-record D1` supersede recording a queryable `cutover_status:` field (oya_gen_masterplan/cohesion_gate/drift_gate/safety_runtime_hook = worktree-only, flips to live-on-dev on port) — NOT a static dated snapshot [R3/Finding-4]**; manual cohesion stand-in run by an adversarial (find-an-atom) second lane, verdict routed to a second opus lane / founder. |
| **L6** | namespaced `*_tier` identifiers unchanged; bare-`tier`/`tenant-tier` plan-axis sites → namespaced/`tenant_class`; 0163 "tiers"→"stages"; glossary "tier" partition present. |
| **L7** | 3 RECOVER-as-ADR artifacts exist; 4 FOLD items folded into named docs; owed-depth list bound to per-vertical M0 gate + reachable from vertical-coverage map; Train dropped; Law/Finance named-deferred; defense + power-grid present as net-new in the coverage map. |
| **Wave-3** | masterplan (generated or labelled manual stand-in) emits every Accepted ADR's planning_impact + the must-read session-context bundle; every worth-documenting item reachable from masterplan/workflow; **terminal cross-ref re-resolution re-runs L3.3 AFTER the last Wave-2 supersession → zero dangling supersedes/amends on the DELIVERED graph, not just the Wave-0 snapshot** [R2/SY2]; **"zero contradicting atoms" proven by a PUBLISHED evidence artifact — the cohesion read-set diff + the adversarial second-lane falsification log showing zero atoms found after a real search — NOT a bare verifier-green**; every `superseded-on-cutover` ADR carries `cutover_trigger:`. **Standing caveat [R2/SY5]: amendment-complete ≠ gates-live — the manual-stand-in→ported-gate debt (generator/cohesion/drift/safety-hook) stays open + tracked in the masterplan-wiring meta-ADR.** |

---

## I. REVIEW DISPOSITION

### I.1 Round-1 (architect S1–S6 + critic G1–G5 / R1–R6 → where applied) — SETTLED, re-confirmed by both round-2 lanes

| Item | Severity | Where applied in this plan |
|---|---|---|
| **G1/S3** census 731/105 vs 831/43 reconciled | HIGH (critic), gating | §A.0-1, A.0 gate (§D), Failure-0 (§H.1), L2 row (§H.2), L2.0/L2.1 |
| **G2/S2** consolidation-set frozen first, edge to {L2.2,L3.3,L1-amend} | gating, 1-way | §A.0-2, L1.0-CONS (L1), §C edges, §F sign-off, L2.2 skip-archived |
| **G3/S6** MAP self-consistency verifier ahead of sign-off | gating | §A.0 D-4, L1.0-VERIFY (L1), L1.0-MAP gate (§D), Failure-2b, §H.2 |
| **G4/EXTEND-2** DROP inbound-reference-safety gate | gating | L1.2-DROP (L1), L1.2-DROP gate (§D), Failure-2c, §H.2, §G exit |
| **G5/FLAG-2/S1** D-LANES-narrowing named + L1-amend∥Wave-0 split | gating | §A.4 (new), §A.2 D-4, wave-membership, L1 split, §F sign-off |
| **R1/S5/FLAG-3/§6** aspirational→canon + safety `runtime-hook-pending` | recommended→safety-gating | L5.1, §F, Failure-3 + safety sub-case, §H.2 L5 |
| **R2/S4/§5** `cutover_trigger:` required front-matter | recommended | §E, §G exit, §H.2 Wave-3 |
| **R3/Tension-C/FLAG-4** adversarial (falsify) cohesion stand-in, 2nd lane | recommended | §D L5 gate, Failure-3, §H.2 L5 |
| **R4/EXTEND-1** testable exit = published cohesion diff + falsification log | recommended | §D Wave-3 gate, §G exit, §H.2 Wave-3 |
| **R5/EXTEND-3** CC-9: D-EVENT/Pulsar precedence, 0005 amended | recommended | L4 CC-9, §C edges, §H.2 L4 |
| **R6/FLAG-5/A.0-3** L2.0b journeys/personas census, template-first | recommended | §A.0-3, L2.0b (L2), L2 row (§H.2) |

Both round-1 reviewers confirmed the §A sequencing logic + the L2-before-L1 *order* as engineering-correct; round-2 re-confirmed the order while correcting its *provenance* (below). The round-1 additions are base-data predicates, a named ruling-narrowing, four correctness gates, and verification hardening — all pre-Wave-0 or per-lane, none altering the wave order.

### I.2 Round-2 (architect FLAG-A/B/C/D + SY1–SY5; critic ITERATE, HIGH re-derived → where applied)

| Item | Severity | Where applied in this plan |
|---|---|---|
| **FLAG-A/SY1** D-1 rename-before-reauthor is DERIVED not RULED; fabricated SSOT quote deleted; provenance discipline; wave-shape → founder choice | **HIGH, gating before sign-off** | §A.1 principle-6 (new), §A.2 D-1/D-2, **§A.5 provenance-evidence table (new)**, **§A.6 ruling-provenance pass (new)**, §A.3 Option-1/2 invalidation rationales (de-circularized), §A.4 sign-off text, §B-L2 header, §C edge + critical-path, §E + §F sign-off (wave-shape added), §G entry+exit, Failure-4 (§H.1), §H.2 A.6 row |
| **provenance pass** verify EVERY RULED tag corpus-wide (not just D-1) | **HIGH, gating** | §A.6, §E gate, §G entry, §H.2 A.6 row |
| **FLAG-B/Tension-B/SY2** terminal cross-ref re-resolution on the delivered (grown) graph | MEDIUM, before Wave-3 | §C edge, §D Wave-3 gate, §G exit, Failure-5 (§H.1), §H.2 Wave-3 |
| **FLAG-C/SY3** consolidation COMPLETENESS (not just surjection) → S1 partition provably total | LOW, before freeze is load-bearing | L1.0-VERIFY (L1), L1.0-MAP gate (§D), §H.2 L1.0-MAP |
| **FLAG-D/SY4** L2 reconciles to *post-L2.0b* census, not "831" (soft tail) | LOW/wording | §D L2 gate, §H.2 L2 |
| **SY5** standing caveat: amendment-complete ≠ gates-live | tracking | §G exit, §H.2 Wave-3 |

**What did NOT change (both round-2 lanes agree): the wave order.** Option 1 remains the recommended call; D-2 (map-before-sweep), D11 (sweep-before-backfill), the 831/43 census, D14 drops, D-EVENT precedence, and `cutover_trigger:` remain genuinely RULED (spot-confirmed vs SSOT lines 42-43, 52, 107, 26). The round-2 change is **provenance honesty + two verification holes**, not sequence: D-1's *status* drops from foreclosed-law to founder-ratifiable, the founder now signs the wave-shape as a real choice, and the delivered graph (not just the Wave-0 snapshot) is proven clean. **Open re-review trigger (round-2 critic §5/§8):** round-3 must confirm the fabricated quote is deleted, the relabel is surgical (only D-1 → DERIVED), and the §A.6 provenance pass surfaced no further false-RULED tags.

### I.3 Round-3 (architect APPROVE-WITH-CONDITIONS + critic ITERATE; both findings independently re-derived vs SSOT → where applied)

> Both round-3 lanes verified the two HIGH findings against the SSOT directly (D11`:52` content (d) names the foundry rename as sweep content; D3`:114-118` pins fold INTENT not a file map) and confirmed the wave order is unchanged. Round-3 findings are the **same directional/one-artifact blind spot round-2 fixed, found in the OPPOSITE direction (RULED-deflation) and a second location (the consolidation-set finalization model)** — closed STRUCTURALLY (bidirectional §A.6 pass + design/confirm split), not per-instance.

| Item | Severity | Where applied in this plan |
|---|---|---|
| **Finding-1/PV-1** §A.6 was RULED-inflation-only (blind to RULED-deflation); re-tag L2's sweep-membership `[RULED: D11(d)+:52]`; Option-2's bounded legal shape | **HIGH, gating before §A.6 complete + any §F sign-off** | **§A.6 made BIDIRECTIONAL (direction-2 mandate, new)**, §A.5 new L2-sweep-membership RULED row + D-1 scope-clarified to internal-sequencing-only, §A.2 (membership vs sequencing), §A.3 Option-2 legal shape restated (bounded), §B-L2 header re-tagged, §C edge split (sweep-membership RULED + sequencing DERIVED) + critical-path RULED set, §E + §F wave-shape (bounded choice), §H.2 A.6 row (both directions), **and (R4) the bidirectional contract propagated into the §E gating predicate (`:229`) + §G sign-off-entry predicate (`:249`) + §G exit criterion (`:261`) — closing the round-3-critic "4 loci" gate (A.6 prose + §H.2 + §E + §G), so the founder-facing sign-off contract itself advertises direction-2 (no RULED-deflation), not only the mechanism** |
| **Finding-2/PV-2** consolidation-set freeze is door:one-way over a Wave-1-produced (provisional) fold map; split into Wave-0 design-freeze + Wave-1 confirm | **HIGH, gating before A.0-2 signed door:one-way** | §A.0-2 split (provisional design-freeze + Wave-1 confirm), L1.0-CONS (provisional design-freeze), **L1.1-CONS-CONFIRM (new Wave-1 unit)**, L1-amend safety-net (revert-on-delta), L1.0-VERIFY (completeness = design-level, stability = Wave-1), §C edges, §D A.0 gate + new L1.1-CONS-CONFIRM gate, §E A.0-2 sign-off text, Failure-6 (§H.1) |
| **Finding-3/Tension** Wave-3 terminal barrier un-named as a 2nd serialization | MEDIUM, before Wave-3 | §A.4 (two acknowledged serializations named), §C edge (terminal barrier named), §A.4 sign-off scoping |
| **Finding-4/PV-3** aspirational-status was a dated prose snapshot, not queryable | LOW | §E (`cutover_status:` field mandate), L5.1 (`cutover_status:` on masterplan-wiring meta-ADR), Failure-3 mitigation, §G standing caveat, §H.2 L5 row |

**What did NOT change (both round-3 lanes agree): the wave order — confirmed correct a third time.** D-2, D11 sweep-before-backfill, census 831/43, D14, D-EVENT, `cutover_trigger:` stay RULED. Round-3 reshapes **provenance honesty in BOTH directions** (L2-sweep-membership is now correctly RULED, not deflated to DERIVED) and **the consolidation-set finalization model** (provisional-then-confirmed, so no door:one-way is signed over a guess) — not the sequence. **Open re-review trigger (round-3 critic §8):** round-4 must confirm (a) the §A.6 mandate is bidirectional in all loci and surfaced no further RULED-deflation, (b) L1.0-CONS is split so no door:one-way is signed over a provisional artifact, and (c) the same directional blind spot did not produce a THIRD instance (which would escalate toward REJECT).

---

*End AMENDMENT-PLAN. STATUS: pending approval (door:one-way founder sign-off).*
