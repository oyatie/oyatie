# Founder Decision Record — Oyatie canon consolidation (deep-dive consensus)

> **Source:** `/deep-dive` over the verified initial-sweep ADR audit (`00-MASTER-REGISTER.md`). The audit IS the trace.
> **Status:** IN PROGRESS — capturing founder rulings on the 16 door:one-way decisions. Each ruling here authorizes the corresponding amendment pass (supersede/re-author into the clean `ADR-0000+` generative series; never in-place). Nothing amends until a ruling is recorded.
> **Date:** 2026-06-06.

---

## D1 — Masterplan authority — RULED ✅
**Ruling: GENERATED FROM ADRs (confirmed).** ADRs are the immutable authored SSOT; the masterplan is a GENERATED projection (`oya gen masterplan` + drift gate), per the already-Accepted ADR-0364/0365.
**Implications / amendments authorized:**
- Retire `MASTERPLAN.md`'s `compatibility_projection` self-framing + the contradicting authored-authority design (`planning-ssot-drift-prevention.md` reconcile).
- "Backfill the masterplan" ≡ clean ADR front-matter (`planning_impact`/`milestone`/`deliverables`/`affected_surfaces`) so the generator emits it — NOT hand-authoring the masterplan.
- Generation must ALSO emit/point-to the **must-read session-context bundle** (operational instructions a fresh session needs, not just decisions) — the worth-documenting⇒worth-reading⇒reachable rule.
- Wire the **domain-cohesion** read-set + gate (enum-keyed now → vector later) into the lifecycle so no contradicting ADRs by construction.
**Door:** one-way. **Gates:** all backfill + D11/D12/D13/D15.

---

## D4 — Data tier — RULED ✅
**Ruling: OWN THE ENTIRE DATA TIER (endpoint) via the vendored→owned ratchet.** The owned-ideal = the whole data tier owned by the distributed multi-model engine (cloud-data / linux L-0001) — OLTP + vector + OLAP + FTS + streaming. The TRANSITION uses vendored best-of-breed behind ports (Pulsar 0377, Postgres+Citus 0179, Milvus 0192, ClickHouse 0193, TimescaleDB 0194, Iceberg 0337, Valkey 0336, SeaweedFS 0196), each ratcheted to the owned engine **when proven**. NOT permanent best-of-breed.
**Amendments authorized:** finish the L-0001 scrub as "Postgres+Citus is the vendored OLTP substrate **until** the owned engine proves parity" (retained-for-now, not retained-permanently); tag the best-of-breed ADRs as **transitional vendored substrates behind ports**, not endpoints; design every data port to the owned-engine ideal. **Door:** one-way.

## D-META — Universal ownership ratchet (default for all own-vs-buy) — RULED (pending confirm)
**Pattern:** own-the-endpoint / vendor-the-bridge / ratchet-when-proven (ADR-0019 + ADR-0482). **RULED ✅ — confirmed as the standing DEFAULT** for every own-vs-buy decision; per-decision we surface only the deltas (which vendored bridge, the ratchet trigger, any genuine contradiction). Open: the day-0 portfolio-capacity budget (audit #8 — which ONE substrate is crown-jewel built first vs DEFER_VENDORED) was NOT forced now; flagged for later so "own everything" ≠ "build everything at once".
**BUILD FIRST, CUTOVER LATER (RULED ✅ — the operational rule of the ratchet):** build the owned alternative **alongside** the vendored bridge → run **parallel/shadow** → **cutover only when the gate fires (proven)** → **THEN retire** the vendored. **Never retire/archive a bridge before its owned replacement is built and proven.** Applies to every substrate (CI, data tier, identity, policy, kernel, …). Consequence for amendments: ADRs superseded by a NOT-YET-BUILT owned thing are marked "superseded-on-cutover (pending build+proof)", not archived immediately. (Ties to ADR-0510 cutover-trigger, ADR-0250 build-ahead.)

---

## D5 — Identity — RULED ✅
**Ruling:** oya-identity (bespoke Rust, 0476 founder-locked) = owned endpoint; **Zitadel = the vendored OIDC bridge** (0187 demoted from canonical → Phase-1 bridge, superseded-as-endpoint by 0476). Crypto libs aws-lc-rs/webauthn-rs/opensk (0506/0507/0508) **reused behind ports** (foundational, not differentiators). **Amendments:** 0476 `supersedes:[0187]` (fix the 0421 mis-number); 0187 status → superseded-as-endpoint/bridge-retained; resolve the hard contradiction (C-4). **Door:** one-way.

## D6 — Policy engine — RULED ✅
**Ruling:** Cedar = **PERMANENT external-standard CONTRACT** (the policy language, like pg-wire/CRI — spoken forever); **own the EVALUATION ENGINE behind it** (compile-to-Rust PARC, linux 0021), vendored `cedar-policy` engine as the bridge, **differential-tested against the Lean-verified oracle** + Kani. **Amendments:** re-author the phantom Cedar-engine anchor (0150 is cursor-pagination — assign the cedar-engine decision a real id); keep 0007/0183/0243/0246 Cedar-contract refs, retarget engine refs to the owned PARC. **Door:** one-way.

## D7 — Isolation end-state — RULED ✅
**Ruling:** framekernel + owned-VMM = **COMMITTED owned endpoint** (incl. H2 bare-metal-host — ratchet-gated, NOT research-optional); Talos+Kata+Firecracker+wasmtime = vendored bridges; fleet default = **assume-breach microVM** (already ADR-0023, holds). **L7 container build engine = DEFER_VENDORED** (BuildKit now, own later). Ladder: Talos day-0 → owned node-OS → kernel-level ratchet (ADR-0025). **Amendments:** tag the isolation bridges transitional; affirm ADR-0023 default; mark H2 committed-but-ratchet-gated (reconcile ADR-0018 open-question). **Door:** one-way.

## D16 — Autonomy ceiling — RULED ✅
**Ruling:** **runtime-enforced hard gate, owned by `governance`.** ADR-0022 governs — T1–T4 is a hard Cedar gate checked at every agent invocation (matches assume-breach); ADR-0007's advisory framing demoted to design-time guidance. Mechanism owned by `governance` (policy authority); `intelligence` consumes. **Amendments:** reconcile 0007↔0022 semantics; set ownership home = governance (resolve the post-0335 ambiguity); gates 0139/0144. **Door:** one-way.

## D13 — ID discipline — RULED ✅ → **AMENDED 2026-06-06 (re-foundation DEFERRED)**
**Original ruling:** full plan — no-id-reuse + no-dangling-ref invariant; renumber linux 0001–0026 → **0515+**; renumber duplicate forge-board **0377**; clean **ADR-0000+ re-founding** (consolidates-provenance, ADR-0364-D7).
**AMENDMENT (founder, post-WIP-merge):** the full **ADR-0000+ renumber re-foundation is DEFERRED** to a dedicated later campaign — because the in-flight **WIP migration depends on STABLE ADR ids** (a full renumber invalidates every live citation + the migration's, during the authority-flip). **NOW: keep existing numbering as the stable base; AMEND-IN-PLACE + ADDITIVE only** (foundry rename, integrity sweep, vocab, supersede/amend, NEW ADRs into the live free block — like the migration's L10). Still enforce the no-id-reuse/no-dangling invariant; still renumber linux 0001–0026→0515+ and dedup 0377 (additive, non-disruptive). The clean-series re-foundation happens AFTER migration settles. **Door:** one-way.

## D-MERGE — Unified execution plan: WIP migration plan = execution authority + amendment rulings folded in — RULED ✅
**Ruling:** the **WIP `.omc/plans/monorepo-consolidation-migration.md`** (live-verified, ralplan-deliberate) is the **EXECUTION AUTHORITY** — its framework governs ALL execution: pre-lanes 0.4 (authority-snapshot+signing) / 0.5 (truthing+source/merge-surface manifests) / 0.6 (no_std inertness) / 0.7 (governance bootstrap); **BUILD-TO-BOTH-GATES** (live `github-lane-unlocker-required` Buck2-whole-graph + flip-ready `oya-ci-required`+signing); **authority-flip G0 HALT** (the dominant risk — `github-lane-unlocker-required`→`oya-ci-required` migration in flight, ADR-0513); **squash-merge**; **`github-mirror` remote** (`origin`=Forgejo, never push there); serial single-driver ralph loop; G0–G4 user gates. My **AMENDMENT-PLAN rulings (the WHAT)** fold in as source-internal **amendment lanes** under those same constraints. **This CORRECTS my AMENDMENT-PLAN's stale assumptions** (it had linear/one-doc-PR/signed-required → reality is squash/github-mirror/signatures-not-live-yet-but-flipping). Unified plan authored at `docs/audit/initial-sweep-2026-06-06/UNIFIED-EXECUTION-PLAN.md` (founder folds into the `.omc` plan on commit; the uncommitted WIP file left untouched). **Live blockers (WIP G0–G4):** authority-flip, signing (now provisioned), db-engine-source-missing (L8 droppable), cloud-k8s 6th surface, containerd 44-crate split, codename ratification. **Door:** one-way.

## D14 — Proposed-ledger sign-off — RULED ✅
**Ruling:** approve ledger 03 **as-is**. RATIFY ≈122 + 1 RENUMBER-then-ratify + 1 KEEP-as-Proposed-by-design; **DROP 3** (0325 prices a retired primitive · 0316 superseded by 0329 · 0349 Jenkins-half); **0352 AMEND-MANDATORY**. Zero unaccounted proposals. **Door:** one-way (batch).

## D15 — Domain enum + cohesion gate — RULED ✅
**Ruling:** approve the closed **16-domain enum** (taxonomy 04) as the domain-cohesion read-set key; **enum-keyed now → vector recall later**. Approve the splits (ci-cd-build, governance-process) + merges (hardware-firmware→node-os, comms-notify→compliance-residency). Meta-ADR adds the `domain` field to the ADR-0364 template + the `domain-cohesion` gate to the ADR-0365 lifecycle.

## D11 — Data-integrity sweep — RULED ✅
**Ruling: authorize the FULL sweep** (runs before masterplan backfill, each fix read-only-verified): (a) restore KCMVP/KISA from `*-shippable-tier` corruption (6+ files incl. GLOSSARY + 0043 + 0002); (b) fix self-referential renames (0006 "Ontology→Ontology", tautologies); (c) fix ALL dangling supersedes/amends edges (0150-cedar phantom, 0476→0421, 0057→0055-v3, 0069→0088, 0377→0397/0436, Cedar-as-0083); (d) foundry→cloud-intelligence/governance bulk-rename (0347 Proposed→Accept; fix 0363's false "eradicated" claim). **Door:** one-way (batch).

## D12 — Vocabulary namespacing — RULED ✅
**Ruling: namespace `tier`** → `autonomy_tier` (T1–T4) / `eu_ai_act_risk_tier` (0–4) / `dr_tier` / `storage_tier` / `tenant_class` (retired tenant-tier, 0329); rename ADR-0163 "environment tiers" → "environment stages". Namespacing, not supersession.

## D10 — Progressive delivery + chaos — RULED ✅
**Ruling: Argo Rollouts + Chaos Mesh** as vendored bridges behind ports (Argo-ecosystem-aligned), ratchet-to-owned later. Supersede Flagger (0160); reconcile 0040/0165. **Door:** two-way (reversible tool picks).

## D9 — Scope / breadth (verticals) — RULED ✅ (endpoint) + capture-needed
**Ruling: MAXIMAL VERTICAL SCOPE is the endpoint — sequenced, not cut.** oyatie covers **B2B, healthcare, consumer, defense, power-grid, and more** — every vertical is in-scope as the ambition; the **ratchet + the day-0 capacity budget (D8) sequence the build order** so "everything in scope" ≠ "build everything at once". The audit's "would a hyperscaler do all this?" concern is answered by *sequencing*, not *scope-cut*. Scope-OUT items stay VISIBLE in the roadmap (worth-documenting⇒reachable).
**Coverage vs source:** existing vertical ADRs — 0321 (B2B industry-leader), 0315 (ERP/SAP-parity), 0332 (healthcare), 0220 (consumer-intelligence), 0249 (marketplace); compliance/sovereignty — 0164 (sovereign/air-gapped), 0240 (sovereign-per-regional-pack), 0251 (compliance-pack cell-certification).
**NET-NEW / capture-needed (likely absent in source):** **defense** (classified, air-gapped, ITAR/IL-levels, cross-domain) and **power-grid / critical-infrastructure OT** (NERC-CIP, IEC 62443, SCADA/OT, safety-critical) — plus "many more" verticals to enumerate. → needs a **vertical-coverage map** (which verticals, compliance regime, in-source vs net-new) feeding new ADRs + the masterplan; raises requirements on CI (sovereign/air-gapped build+release), isolation (confidential/air-gapped Capsules), and compliance-pack certification. **Door:** one-way. **Sub-item D8 (capacity budget) still owed** to sequence this.

## D-LAYER — Cloud-as-dogfood-substrate / products-as-tenant-workloads — RULED ✅
**Doctrine:** **oyatie cloud (`cloud/` — the IaaS/PaaS substrate: cloud-compute, cloud-k8s/managed-k8s, cloud-data, cloud-iam/kms/secrets, cloud-network, tenancy/cells) is WHERE the oyatie products (`oya/`) dogfood** — products run on oyatie cloud as **tenant workloads**, not exceptions. Both are sold: **cloud as IaaS/PaaS** (a cloud provider) and **products as SaaS**; the products *prove* the cloud by running on it (self-hosting). oya-ci builds products and deploys them TO oyatie cloud → the full dogfood loop.
**Grounding (already in source — ratified from Proposed via D14):** ADR-0242 (oyatie-is-a-tenant), ADR-0245 (substrate-vs-product layering), ADR-0247 (self-hosting/self-modification), ADR-0244 (tenant universal scoping), ADR-0009 (cells). Legacy vision-thesis agrees ("B2C products are tenant workloads on the same platform, not exceptions").
**Sharpening to capture (the delta):** name the **`cloud/` (substrate) ↔ `oya/` (products) boundary** explicitly as *the* dogfooding seam — the existing ADRs frame substrate-vs-product at the microservice/tenant level but don't crisply state cloud-substrate-hosts-products. **Amendment:** add this layer statement to the substrate-vs-product canon (0245) + component-boundaries; thread it through the CI design (deploys to oyatie cloud) and the masterplan. **Door:** one-way.

## D-EXEC — Amendment execution authorized — RULED ✅ (founder sign-off, door:one-way)
**Ruling: APPROVE Option 1 (sweep-then-refound) + execute via PARALLEL TEAM.** This is the §A sequencing/wave-shape door:one-way sign-off. Per AMENDMENT-PLAN.md gates, source mutation (Wave-0) is still blocked until: §A.6 ruling-provenance pass green + A.0-1 census-reconciled + **A.0-2 consolidation-design-set FROZEN (separate door:one-way founder sign-off, pending the set's production)** + GitHub auth/signing-key handoff. Each door:one-way unit parks for per-unit founder sign-off. Plan: `AMENDMENT-PLAN.md`. **Pre-Wave-0 gating (read-only/safe) running now; Wave-0 source mutation gated on A.0-2 sign-off + credentials.**

**Pre-Wave-0 gates — COMPLETE (2026-06-06):** §A.6 ruling-provenance pass **GREEN both directions, zero re-review triggers** (L2-sweep-membership=RULED `D11:52` confirmed; 3 non-blocking citation-line drifts to fix in-plan during execution — census is `:110` not `:107`, D-LANES `:87` not `:84`). A.0-1 census **reconciled to 831/43** (raw mechanical floor 731; 831 adds sampled journeys/personas tail hardened at L2.0b; 43 = curated Palantir-product subset). **A.0-2 consolidation-design-set FROZEN (door:one-way founder-signed)** — archived-by-refoundation set is only ~4 files `{0513,0511,0124}`+phantom-0150; rest amend-in-place/cutover-bridge.
**A.0-2 ambiguity resolutions (founder, 2026-06-06):** (1) **0408/0514 = AMEND in-place** (standalone adopted-substrate ADRs, referenced by the oya-ci reshape, NOT archived). (2) **CI-cluster = DROP the Jenkins/Argo ADR debt** — ARCHIVE/DROP 0349+0361 (Proposed, never-ratified), 0359 (already Superseded), **0511 Argo** (conflicts with the D3 oya-ci ruling; its ideas already absorbed); **only oya-ci 0513 is ratified canon**; the **physical Jenkins scaffold (89 Jenkinsfiles + webhook jenkins-adapter) + 1 GH-Actions workflow stay OPERATIVE as an explicitly-UNRATIFIED de-facto bridge** under build-first-cutover-later, retired when oya-ci is built+proven; **resolve the `registry/foundation-bypasses/byp_adr_0349` bypass record.** (3) **phantom-0150 cedar real-id = assigned at Wave-1 L1.0 MAP** (mechanical, deferred). (4) **Data-tier = a NEW ADR-0000+ data-engine-endpoint ADR** (own-the-whole-tier endpoint; vendored bridges + linux engine ADRs 0515+ point up to it). (5) **TWO meta-ADRs** (domain-cohesion gate + masterplan-generated-wiring, separate single-concern).
**Remaining Wave-0 gate:** **commit-signing** — gh push auth present (jason931225); founder configuring SSH commit-signing (id_ed25519, already registered on GitHub as a signing key). Wave-0 team execution starts once signing is confirmed.

## D-INTEL — Two-layer AI: cloud-intelligence (Bedrock-analog framework, cloud-owned) + oya-intelligence (agentic workflows) — RULED ✅ (founder)
**Ruling (refines ADR-0255 two-layer + ADR-0335 foundry-absorption + ADR-0389/0390 cloud-intelligence-bedrock):**
- **`cloud-intelligence` = the Amazon-Bedrock analog — the CLOUD owns it.** It PROVIDES the **canonical AI/agent FRAMEWORK**: foundation models, model gateway, provider/account adapters, agent runtime + capability primitives, inference/request-pipeline, eval/proof layer. This is the substrate/platform.
- **`oya-intelligence` = the TENANT-FACING product layer** — **uses cloud-intelligence to SERVICE ITS TENANTS**: per-tenant agentic workflows / context / memory / isolation / quotas / usage-metering across the oya ecosystem (dovetails D-LAYER cloud-dogfood: cloud owns the substrate, products serve tenants on it). Shared-framework dimensions → cloud-intelligence; per-tenant-servicing dimensions → oya-intelligence.
- **Feature-maturity GOAL (founder):** the AI substrate must MATCH the feature-maturity bar of hyperscale AI substrates (AWS Bedrock/AgentCore, Google Vertex+Agent-Builder, Azure AI Foundry + the agent-framework layer) — beat-or-parity, gated by ADR-0123 maturity-claim-gate. Benchmark in flight (`hyperscale-ai-substrate-maturity`) → capability matrix + gap + maturity goal feeding the cloud-intelligence ADR.
- **Foundry routing CORRECTED (resolves D11(d) + the earlier cloud-intelligence-vs-intelligence catch):** foundry's **framework/platform primitives → `cloud-intelligence`** (it was a proto-Bedrock; folds UP into the cloud-owned canonical framework). Foundry's **agentic-workflow/application uses → `oya-intelligence`**. **foundry-fitness → `oya-governance`** (own service, pending validity check). **foundry agentic-VCS → RETIRED** (git+Forgejo+oya-ci, ADR-0363). So the L2 rename is sense-routed across cloud-intelligence (framework) / oya-intelligence (workflows) / oya-governance (gates) / retired-vcs — NOT a single "intelligence" target.
**VERIFIED REALITY (wk70a3f08, re-confirmed by me):** the framework engine is currently in **`oya/intelligence` (95,964 LOC, 128 crates, 7/7 primitives: model-routing 6, capability 4, MCP 1, eval 11, autonomy 3, guardrails 3, RAG 9; +`_legacy-foundry/` + 712 foundry-lineage files)**; **`cloud/cloud-intelligence` (12,603 LOC, 8 crates, 0/7)** is today only the egress broker and its PRD disclaims the framework; **`oya/governance` = spec-stage shell (0 `.rs`, 6 `.cedar`)**. The founder's intent and the code were INVERTED — resolved below.

### D-INTEL FINAL (founder-ruled 2026-06-06, post-verification + maturity benchmark)
- **oyatie cloud OWNS `cloud-intelligence` = the Bedrock-equivalent FRAMEWORK + RUNTIME.** **RE-HOME the 96k-LOC engine DOWN from `oya/intelligence` into `cloud/cloud-intelligence`** ("cloud owns it fully") — **overrides ADR-0389's port-not-relocation lean** (founder chose relocation). Large migration, ratchet-sequenced.
- **`oya/oya-intelligence` = the per-tenant SERVICING layer** (D23 + the per-tenant servicing halves) — a thin tenant-consumer of cloud-intelligence.
- **oya cloud IS A TENANT of oyatie cloud** (ADR-0242 recursion/dogfood) — uses the framework+runtime to serve its clients or itself.
- **DUAL CONSUMPTION (new requirement):** oyatie cloud / cloud-intelligence must serve **BOTH (a) metered API traffic AND (b) subscription/seat-based usage traffic** (the seat-pool side partly built — ADR-0384; maps to maturity D2 inference-economics + D16 cost/quota + oya-billing/oya-meter 0478/0479).
- **Governance: BUILD OUT into live Rust crates now** (promote from spec-stage; governance defines the gates, oya-ci runs them — authority/runner split preserved).
- **Wire chokepoint:** with the engine re-homed into cloud-intelligence, live provider calls converge there (no duplicate adapter stacks); retire oya/intelligence's mocks into the cloud-intelligence engine.
- **FEATURE-MATURITY GOAL (ADR-0123-gated):** 23-dimension matrix; **GA-parity on table-stakes** + **BEAT on 4 owned differentiators** — Automated-Reasoning guardrails · per-session microVM isolation + long-running agents (framekernel-native) · durable execution · eval-as-proof. MCP+A2A+AG-UI+OTEL-GenAI = vendored-as-wire/owned-as-runtime; batch/GraphRAG/fine-tuning = own-when-proven. **No parity claim without measured evidence** (never adopt vendor marketing figures as targets). Benchmark: `docs/research/ai-substrate-maturity-2026-06-06/`.
- **Minor:** amend cloud-intelligence service-local docs to cite ADR-0389/0390 (they exist centrally). **Door:** one-way (engine relocation + governance build-out are big, ratchet-sequenced under build-first-cutover-later).
- **PLAN FOLD (founder: "fold into plan + continue to Wave-0"):** add to AMENDMENT-PLAN L5 + D8/D-SEQ as ratchet-sequenced multi-quarter lanes — (i) **AI-engine re-home** oya/intelligence→cloud/cloud-intelligence; (ii) **governance build-out** (spec→live crates); (iii) **dual-consumption** (API + subscription) on cloud-intelligence; (iv) **AI-substrate maturity program** (23-dim GA-parity + 4 beat-bars, ADR-0123-gated). These are NOT Wave-0.
- **⚠️ Wave-0 foundry-rename targets CURRENT reality, NOT the re-home destination:** the 831 foundry-ref rename routes platform→`oya-intelligence` (where the crates physically are today — 712 files already carry the lineage), fitness→`oya-governance`, vcs→retired. The engine re-home (oya-intelligence→cloud-intelligence) is a SEPARATE later migration that renames oya-intelligence-*→cloud-intelligence-* — doing it in Wave-0 would drift docs ahead of code. So the earlier "foundry→cloud-intelligence" framing is the ENDPOINT; the Wave-0 rename uses the current home.

## D-CONFORM — Monorepo-conformance audit + migration-fit decisions — RULED ✅
**Audit verdict (verified):** the WIP migration plan owns the hard mechanical/process risks well (authority-drift, no_std exclusion, vendored stripping, db-engine, k8s/containerd entanglement, one-workspace collapse, brand rename, Buck2 whole-graph gate) BUT its per-lane acceptance is **build+brand-centric** — lanes could merge green-on-Buck2 while still VIOLATING ~12 architecture/governance policy items per repo (BNF layer-suffix enum, hexagonal layer-import-matrix/ports-in-kernel/api→kernel-only/app→app-ban, manifest hygiene [resolver-2, version.workspace, publish=false, license=Apache-2.0, [lints] workspace, [lib] doctest=false], dependency-rationale no-orphan, vendor A/B/C registry, microservice slot2 registration, per-service colocation + buildability-bar, deny.toml content-correctness, rebrand-arrow/retired-terms scan). **Fix = bounded + additive ralplan revision** (wire the missing arch+governance gates — which item 48's cross-ref/per-service-layout/aggregation packets already presume — + route the open decisions); KEEP the serial loop, pre-lanes 0.4–0.7, lane sequence, authority-drift machinery UNCHANGED. Register: `monorepo-conformance/00-CONFORMANCE-REGISTER.md`. Per-repo: office most policy-ready (clean hexagonal+Buck2), claude cleanest seams, linux/stack heaviest. Brand violations confirmed: linux talos-*×45 + Kuberos product-source leak, oyago×177, oyapy×181, oyaoffice×19; claude/codex brand-clean.
**Migration-fit DECISIONS (founder, verified vs real repos):**
1. **Transpiler names:** `oya/transpiler-go` + `oya/transpiler-py` (≤3-token; crates `oya-transpiler-go-*` / `oya-transpiler-py-*`).
2. **Import + license:** **fresh attributed import** all (oyago/codex have NO git history; claude's 78 commits squash-imported); **relicense claude MIT→Apache-2.0** to match the workspace one-license convention (MIT was ADR-0013-allowed but Apache unifies).
3. **Non-Rust artifacts:** Py/Go transpiler INPUT fixtures stay as **test-data** (legit non-Rust, like vendored); if `oyapy_analyzer.py` is a real build TOOL (not a fixture), port to Rust or home under the service's tooling with a documented exception (verify in-lane).
4. **Nested workspaces (REFINED, founder-directed):** **COLLAPSE the 2 STD service trees** (`kubernetes/` 95k8s+44ctrd, `operating-system/` node-os) into the one-version root; **EXCLUDE the ENTIRE `kernel/` subtree = 12 no_std workspaces** (framekernel + 9 `user-*-src` ELF test-target binaries + `fsbase-worker-src` + `tests-host`) via the `[workspace] exclude` key — they build on the kernel's own pinned nightly + custom no_std targets and CANNOT join the STD root; **EXCLUDE vendored** `third-party/rust/`. → the WIP plan's pre-lane 0.6 inertness proof + L11 must cover the **full 12-entry kernel exclude**, not 1.
**Next:** ralplan revision (add the ~12 gates + record these decisions) → consolidation. **Door:** one-way.
**Pre-lane 0.5 RATIFIED (founder, 2026-06-06; manifests verified vs real repos):** (G4-D2) **DROP L8** — db-engine has NO source (only the 33KB design spec); the owned distributed-DB engine is a FUTURE BUILD campaign (D4), not part of this migration → migration is **~10 lanes**. (G4-D1) **cloud/cloud-k8s = docs-only (0 crates), OUT as a merge target** — stays design-SSOT/cross-link. (L5) **codex SDK = NEW sibling crate** (complement to the 942-LOC proxy adapter, ~0 overlap; vendor `openai-codex-sdk`). (L6) **95 k8s apimachinery crates → `oya-cloud-k8s-*` under `managed-k8s-control-plane-host`** (ADR-0015/0016), beneath the 17 product crates; 44 `ctrd_*` → container-runtime CREATE. (G2) **22-target `tools/` standing-exception RATIFIED** (gate-load-bearing //tools/oya-* incl. doc-staleness-inventory + adr-index-regenerator + 16 governance fitness apps + tools/governance+hooks+buck; //services/=∅). Verified: 44 ctrd_ + 139 total = 95 k8s ✓; cloud-k8s 0 Cargo.toml ✓; 22 tools/ BUCK ✓; db-engine 0 crates ✓.
**Pre-lanes status:** 0.4 ✓ (G0 no-flip, G3 signing, github-mirror) · 0.5 ✓ (manifests + G2/G4 ratified) · **0.6 DEFERRED to consolidation-time** (founder: the exclude-state-inertness-vs-Buck2 proof needs the real merged root workspace + the 12-entry exclude key + founder toolchain/G4 sign-off — can't run under simulate-merged; the kernel-builds-isolated half is known-good on the pilot) · **0.7 ✓** (governance scaffolding bundle at `_execution/prelane-0.7/00-GOVERNANCE-BOOTSTRAP.md`). **Governance reality (verified):** ENFORCED = 96-lane `oya verify`/`gate run-all` + ~~multispectrum evidence (`/evidence/multispectrum/<id>-<ts>.json` v2.4.0 CC-1..7, via `oya-check-dependency-seam`)~~ **[RETIRED 2026-06-07 — D-MULTISPECTRUM-RETIRED: doctrine superseded; critique DROPPED, accounting→firewall; lane retires w/ Task #25/#26]** + ADR-shape (`validate-adr-shape.mjs`) + DOC-CATALOG row-coverage (with the `docs/CATALOG.md`↔`docs/DOC-CATALOG.md` path-bug). CONVENTION-ONLY (doctrine, NOT machine-gated): `## Code Review` (no `guard-pr-merge-review.mjs` on disk), 5-H2 traceability (`oya-governance-pr-traceability` checks only 3 booleans, not the H2s), CHANGELOG-row (D18, not in the 96-lane roster). `oya gate`/Jenkins = legacy bridge (ADR-0513/0363); live merge-dest (Prow-shaped oya-ci) not yet fully wired.
**ALL read-only/prep pre-lanes DONE (0.4/0.5/0.7 ✓; 0.6 deferred-to-consolidation).** Remaining before source mutation: **GATE-BEFORE-START** (kernel-workflow-done + independent kernel re-verify: check-tcb/diff-oracle/both-build/assert-talos) + the **CONSOLIDATION execution** (~10 M-lanes + A-lanes under §6 gates) — gated on founder go + (recommended) committing the WIP to a clean base first.

## D-LINEAGE — oyatie WAS bominal (renamed/migrated; context churned) — FACT
**Canonical lineage:** `oyatie`/source is the renamed + migrated successor of **`bominal`** (GitHub jason931225/bominal — "unified monorepo: train, health, finance"). The migration **churned/lost context.** Bominal holds the FULL product vision the rename fuzzed: 16 vertical modules (medical/patient/pharmacy/emergency/records/insurance/logistics/manufacturing/payments/hr/...), a 54-milestone multi-year roadmap (2026→2030: health/CDSS → logistics/transport/manufacturing/warehouse → marketplace → CCTV-vision/AMR → **infrastructure-sovereignty ratchet 2027-2028** → conglomerate → **2029-2030 far-future: facilities+data-center, agriculture+food, public-sector, civil-infra+UTILITIES/powergrid, public-safety+drones+DEFENSE**), 132 decisions, and the strategy/moat/sequencing rationale. → Reconciliation workflow `wguxmaorw` diffs bominal(past) vs oyatie(present) → lost-context register → **founder interview agenda** (NOT blind recovery; founder interviews on what to restore). The bominal far-future verticals are the source of the D9 "net-new defense/powergrid" — they were oyatie's OWN planned scope, churned in the rename.

## D8 — Capacity budget / crown-jewel — RULED ✅ (was deferred)
**Ruling:** **oya-ci is the priority day-0 crown-jewel** (its output is the ability to build the rest; D3 shape ratified; unblocks the dogfood loop). **BUT builds PARALLELIZE — not strictly sequential.** So "own everything, sequenced" = **M0-gated + capacity-bounded + oya-ci-prioritized + parallel where capacity allows** — NOT one-substrate-at-a-time. **Door:** one-way.

## D-SEQ — Build sequence + infra-sovereignty ratchet + milestone shape — RULED ✅ (bominal Theme 1)
**Ruling (3 parts):**
1. **Infra-sovereignty ratchet = ORDERED list + per-substrate M0 evidence-gate, NO calendar dates.** Restore bominal's substrate-by-substrate own-it order (IaC → gateway/KMS → mail/cache/stream → DB/storage) + the M0 gate (contract + incumbent benchmark + evidence before owning); drop the dates (fiction pre-revenue). Fills the masterplan's missing roadmap; reconciles with D8 + build-first-cutover-later. (Genuinely lost — not in source; recover as new ADR + masterplan input.)
2. **Uniform vertical milestone shape ADOPTED:** every vertical = `research(benchmark matrix) → M0 evidence-gate → operating-contract ADR → EPIC`. One "is this vertical proven enough to build?" gate (verify-at-each-step applied to roadmap scope). The DEPTH-OWED regulatory items (NERC/MFDS/IEC-62443/…) become the **M0 operating-contract deliverable per vertical** — authored when the vertical is sequenced, not before.
3. (D8 above names oya-ci the priority crown-jewel; parallel builds allowed.)
**Door:** one-way (anchors the roadmap).

## D-LANES — Parallel-lanes execution model — RULED ✅ (founder)
**Ruling:** **treat everything as PARALLEL LANES.** Within a lane there may be **sequential** tasks; even within a lane there may be **parallelizable** tasks. Fractal parallel/sequential structure (not a global serial chain). Generalizes D8 ("builds parallelize") to all work. Maps to source's wave/plane + governance-lanes framing (ADR-0016) — reconcile the canonical work-decomposition to this. **Door:** one-way (organizing principle).

## D-SAFETY — Unified safety-gate invariant set — RULED ✅ (bominal Theme 7)
**Ruling:** author ONE **`governance`-owned cross-vertical safety-gate invariant ADR** — **HITL** (clinical Dx) · **no-closed-loop-actuation** (OT: mfg/AMR/powergrid) · **biometric-default-off + 5-stage escalation** (vision/CCTV) · **no-autonomous-lethal** (defense) — referenced by each vertical spec, **hooked into D16's runtime Cedar gate**. Auditable in one place; a vertical cannot ship without its gate. **Door:** one-way (safety/liability commitment).

## D-KR — Global-canonical core + localization packs; KR = first pack to market — RULED ✅ (bominal Theme 5, corrected)
**Ruling:** the architecture is **GLOBAL-CANONICAL core + LOCALIZATION PACKS** (aligns ADR-0064 canonical-base + ADR-0010/0240 regional-packs) — **NOT** a KR-specific core. **KR is simply the FIRST localization pack to hit market** (first-to-market sequencing), not a special substrate. **Restore now:** (a) the typed **EmploymentClassification enum** (정규직/계약직/단시간/파견/도급/프리랜서/인턴/임원) **as a KR-localization-pack data model** (the spine the recovered KR payroll packs — .Trash #1 — hang off); (b) **"KR-first-to-market" as a sequencing posture** on the global-canonical core. Statute grounding (근로기준법/PIPA/통신비밀보호법/의료법/국가계약법/52시간/KCMVP) lives in the **KR pack**, re-threaded per vertical at M0. **Door:** one-way.

## D-DEPTH — Owed-depth capture discipline — RULED ✅ (bominal Theme 2 + Theme 3)
**Ruling:** for TRACKED-PARTIAL vertical specs (CDSS→ADR-0033/0332, Manufacturing-AI→0033, AMR→0027, Bid-Pricing→0115) AND the DEPTH-OWED regulatory items (NERC-CIP/IEC-62443/SCADA powergrid; SGP4/orbital; capital-markets license-ladder; MFDS/HITL; ISA-95/6-capability; SLAM/ROS2/VDA-5050; 낙찰하한율): **record each as an explicit OWED-DEPTH list bound to the vertical's M0 operating-contract gate (D-SEQ) — NOT authored now, but never silently lost.** Safety-gate slices route to D-SAFETY (authored now). The owed-list lives in the vertical-coverage map (#18) + masterplan, so it's reachable. **Door:** two-way (capture is reversible).

## D-RECOVER — Bominal restoration cleanup (Themes 4/6/8) — RULED ✅
**Theme 4 (lineage):** accept the FOLD/CONFIRM batch — (a) fold the healthcare **released-view record-boundary invariant** (patient = released-view projection, NOT 2nd system-of-record; per-surface write-authority) into ADR-0332 [door:one-way]; (b) confirm **medical→emr** mapping line explicitly; (c) fold the **Connect per-context DEK matrix** (Personal-E2EE-user-DEK vs Professional-tenant-DEK + four-eyes) into the comms-product doc.
**Theme 6 (first customer):** **restore the M3 first-paid-customer target** (≥1 paid KR group ~3000 wage employees closes real payroll; payroll-first public claim) and **FOLD with the .Trash First-Proof-Slice (recovery #2) into ONE "first buildable+sellable slice" artifact**; keep portfolio **block-edges** (trust ⊥ payments/healthcare-billing@L4) as a recovered invariant; Theme-1 sequence subsumes the full build-spine.
**Theme 8 (MED/LOW batch):** accept ALL recommendations **EXCEPT Train, which DROPS.** → fold CCTV 5-stage + biometric-default-off into vision op-contract (gate→D-SAFETY); fold email-mining + **person-pillar HARD exclusion-zone** (통신비밀보호법) as a security invariant; fold AI-surfaces catalog; confirm insurance/banking + ATS + security-as-product dispositions; ensure parked hospitality + **Bominal Law + Bominal Finance** survive into the masterplan roadmap as named-deferred tracks; fold PG-rail + 전자금융업 license-ladder. **DROP: Bominal Train** (brownfield hobby track — not carried). **Door:** mixed (4a one-way; rest two-way).

## .Trash legacy recovery (already verified) — FOR AMENDMENT
RECOVER-as-ADR: KR HR/payroll 8-pack bundle (#1, hangs off D-KR enum), First-Proof-Slice (#2, folded into Theme 6), 4-lane source-closure model + anti-false-closure validators (#3). FOLD: ecosystem-as-a-service/no-internal-bypass, trust-plane 9-KMS-key-class taxonomy, economic-substrate deferral rationale, 8-stage delivery artifact chain.

## D-EVENT — Eventing substrate (CC-9, surfaced by WF2) — RULED ✅
**Ruling (sharpened 2026-06-07): PULSAR from day 0 → OWNED-IN-RUST best-in-class destination (the data-tier ratchet, D-LAYER).** Pulsar is stood up **directly** as the interim best-in-class substrate — **NO Kafka interim; the prior `Kafka→Pulsar` migration framing is DROPPED** (Pulsar from day 0, not migrated-to). The DESTINATION is a **bespoke owned-in-Rust event substrate**, ratcheted in when proven, and it MUST be **best-in-class hyperscaler pattern**, specifically **object-storage-native / diskless**: the object store is the source of truth, brokers are stateless (storage/compute disaggregation — the streaming analog of the owned SQL engine's "the log is the database"; eliminates the inter-AZ replication tax, gives infinite cheap retention + cattle-not-pets brokers). It speaks an **ecosystem-compatible wire** (the streaming analog of the DB engine's pg-wire — maximize client/connector/stream-processor leverage). This is architecturally symmetric with the owned distributed-SQL engine + owned object store (ONE disaggregation pattern across DB + streaming + object storage). The NATS-JetStream + Redpanda framing in `architecture/keystone-bundle-idea-refine-deep-dive.md` (+ related) is STALE → **amend those docs to Pulsar.** One streaming substrate. **Door:** one-way.

## D-ARCH-DASHBOARD — Architecture dashboard (`product-graph.html`) — RULED ✅ (founder 2026-06-07)
**Ruling: ONE polished, AUTO-GENERATED, multi-view architecture dashboard at `source/docs/architecture/product-graph.html`.** Four views: (1) **product + cloud verticals** graph, (2) **tech-stack** graph (now / planned / ideal), (3) **masterplan**, (4) **development lanes + sequencing** DAG. **Maintenance is AUTOMATED** — the HTML is **GENERATED** from machine-readable SSOT (`docs/machine-readable/masterplan.generated.json` + a new `architecture-graph` SSOT derived from the verticals-map + tech-stack + the amendment-plan lanes/waves), regenerated + **drift-gated in the cloud-native pipeline (CaC/CaaS), NOT a CLI** — the file's current `Re-emit via oya doc graph emit` (M02-P20) note is **SUPERSEDED** (oya-CLI retired; D-CLOUD-NATIVE / Δ2). The current hand-authored file is **STALE/off-canon** and must be brought ON-CANON in the rebuild: **remove the `foundry` node** (→ cloud-intelligence / governance), **`Kafka KRaft` eventing** (→ Pulsar from day 0 → owned-Rust diskless, D-EVENT), and the **oya-CLI emit ref** (→ pipeline-generated). Designer-grade polish; source-backed data only (counts must match the verticals-map; no invented nodes). **Door:** one-way.

## WF2 rest-of-docs sweep — DONE + VERIFIED (feeds amendment)
13 ranked canon-contradictions (CC-1..CC-13) in `docs-sweep/00-REST-OF-DOCS-REGISTER.md`. **Verification corrections (spot-checked vs real files):** PARC = ~2 hits not 0 (Cedar-as-engine pervasive, PARC essentially unestablished — CC-2 holds); **foundry counts imprecise — total non-ADR = 831 (not 731), Palantir-Foundry carve-out = 43 files (not 105)** → the 800-file foundry rename MUST be per-file sense-routed (intelligence vs governance) with HARD carve-outs (Palantir Foundry, Marlboro-Forge/forgery, the foundry-fitness→governance retirement record) — never a blind swap; **CC-10 DOWNGRADED** (PRD "KR-first + no-locale-special-case" is CONSISTENT with D-KR global-canonical+KR-first, framing-clarity only, not a contradiction). Amendment targets: CC-1 foundry-rename (per-file), CC-2 Cedar→contract+PARC-engine (24 genuine sites), CC-3 masterplan-authority inversion (DOC-CATALOG/standards-INDEX omit ADRs as apex), CC-4 Jenkins/ArgoCD-as-canonical→oya-ci, CC-5 Forgejo→GitHub-now, CC-6 data-tier-bridges + Kafka→Pulsar(D-EVENT), CC-7 isolation framekernel-host+assume-breach, CC-8 identity oya-identity+Zitadel-bridge, CC-11 tenant-tier→tenant-class (fix persona template not 129 files), CC-12 M0-M3/MVP→gate-waves, CC-13 linux pilot North-Star re-scope. Mechanical-sweep totals + routing rules in the register. Coverage honest (journeys/personas residue = sampled estimate, large generated-index docs flagged-not-read).

## D2 — Forge — RULED ✅
**Ruling: GitHub NOW → bespoke VCS LATER.** GitHub `jason931225/oyatie` is the canonical forge for now (the transitional bridge); the owned-ideal destination is a **bespoke VCS** (substrate-ownership ratchet). Forgejo is **dropped** as the transitional.
**Amendments authorized:** supersede/amend the Forgejo ADRs (0363/0374/0377-forge/0387); reframe ADR-0510 cutover-trigger as GitHub→bespoke-VCS (not Forgejo→bespoke); CI automation substrate is forge-pluggable (GitHub adapter now, bespoke later — designed to the owned-ideal forge port, not GitHub's API shape). **Door:** one-way.

## D3 — CI/CD (unified oya-ci product) — RULED ✅
**Ruling: RATIFY the unified `Run`+graph reshape of ADR-0513.** oya-ci = ONE Rust-native CI/CD system = two nouns (`Run` + Buck2 build-graph over a CAS core) · four faces (Prow trustless gate+merge-queue · Tekton typed-task/provenance spine · Argo DAG engine · provenance) · the build-graph "brain" as the differentiator. Supersedes the "clone Prow's 8 components" framing; **phased ratchet** (ship the substrate first → grow the brain on its data) with a measured promotion gate on every OWN-when-proven claim. Dogfood loop (fan-out agents → PR → signed gate → zero-conflict merge); builds/tests/releases the ~90-service portfolio; deploys to oyatie cloud (D-LAYER).
**Cluster resolution:** ratifies 0513's bespoke instinct · resolves 0511 (keep Argo's DAG/event IDEAS, drop its etcd-CRD substrate; reject both "adopt-Argo-wholesale" and "clone-all-Prow") · adopts 0408/0514 as the core substrate · supersedes 0124 (file-overlap → graph-exact `conflicts(a,b)`) · phases 0369 speculation to Phase-3 (queue-depth-gated) · builds 0367 (trustless gate, signature-IS-the-check) early · sequences 0366 (agentic self-repair) last.
**Build engine:** **BuildKit (per-pool) + Buildah (hardened ~7-cap Job-per-build) behind the owned `BuildSpec` port**; **scratch/distroless default**; **SBOM (SPDX) + SLSA provenance HARD-required day-0** (accept per-build cost); Buildpacks opt-in only; **Kaniko archived**; owned build engine = OWN-when-proven endpoint later.
**MVP cuts:** accepted (serial Tide-invariant queue, single-Postgres `RunStore`, `tenant_id`-seam-only, internal `v0` API — nothing built day-0 that needs data first); promotion-gate STRUCTURE accepted, **exact threshold values set at implementation** (CAS-hit>60%, queue-p50, ≥N-weeks data, culprit≥98%, real-2nd-tenant are placeholders).
**Amendments:** seed ONE clean ratifying ADR (ADR-0000+ series) reshaping 0513; supersede/relate 0511/0124; mark 0369/0367/0366 phased. **Build-first-cutover-later:** **Jenkins (0349/0359/0361) + Argo stay OPERATIVE until oya-ci is built and proven → cutover → THEN retire** (mark "superseded-on-cutover", NOT archived now). Spec: `docs/research/bespoke-ci-design-2026-06-06/40-PRODUCT-SPEC.md`. **Door:** one-way.

---

## ═══ FOUNDER DOCTRINE + 2026-06-06 RULINGS — charter for the UNIFIED consolidation ═══
**Trigger:** discovered TWO execution-pending consensus bodies — this linux-side decision-record AND the source-side `.omx/backlog/platform-readiness-backlog.md` (pillars A–Q + decision register 1–21, "READY FOR /ralplan"). Founder: **the drift/contradiction between them is the symptom of faulty process + enforcement.** Re-scope: from "merge two plans" → **"establish the enforced, automated, fully-accounted, drift-proof, staleness-proof, hyperscaler-grade canon, and PROVE the enforcement is real."**

### D-CICD — CI/CD shape (resolves+extends D3 + backlog #16/#19; T1) — RULED ✅
oya-ci/oya-cd = **bespoke-Rust, cloud-native reimplementation that ADOPTS THE PATTERNS** of best-in-class cloud-native CI/CD: **Prow** (trustless gate + Tide merge-queue) · **Tekton** (typed Task/Pipeline + Chains provenance) · **Argo Workflows** (DAG/event engine) · **Argo CD** (declarative GitOps CD) · **Argo Rollouts** (progressive delivery — canary/blue-green/analysis) · **+ other best-in-class** — all reimplemented in Rust, cloud-native (kube-rs on Talos). Principle: **"do what Go does, in a cloud-native manner, but in Rust."** SUPERSEDES the backlog's "Prow-only / Argo-rejected" framing (the Argo/Tekton **patterns are adopted+reimplemented**, not the upstream Go binaries) AND extends linux D3 (adds the CD + progressive-delivery faces). Home = bespoke cloud toolchain services (cloud-scm/cloud-ci/cloud-cd) as **tenant-facing dogfood products**. Consolidate ADR-0349/0359/0361/0511/0513/0514 → ONE canonical CI/CD ADR (= **ADR-0515**, next free, additive on stable numbering per D13-AMENDED); **ADR-0408 (Buck2 build) stays a SEPARATE bounded-context ADR** — the build substrate oya-ci USES — new ADR `depends_on:[0408]`, amend 0408's refs only (founder 2026-06-06, minimal-blast-radius). Build-first-cutover-later. **Door:** one-way.

### D-PURESPLIT — repo structure (backlog STRUCTURAL RULING; T2) — RULED ✅
**Pure split: exactly two service trees — `oya/` (products) + `cloud/` (platform) — ERADICATE everything else** (services/, platforms/, microservices/, stray service-shaped trees). Amend ADR-0131/0512 (target is NOT a 3rd microservices/ tree). Enforced: a service dir exists ONLY under oya/ or cloud/, exactly once, flat-colocated; no oya→cloud internal dep (dogfood purity). Extends D-CONFORM. **Door:** one-way.

### D-SCOPE-UNIFY — scope (backlog T3) — RULED ✅
The backlog's **4-phase platform-readiness program is IN SCOPE** + expands the consolidation; the two bodies **merge into ONE canon** (one decision corpus, one masterplan, one amendment campaign). **Cloud services are dogfood products** consumed by oya/ + external tenants — **think like a hyperscaler** (cloud is sold; oya = tenant `oyatie-internal`). Ties D-CICD (pipeline is itself a dogfood cloud product) + D-LAYER + backlog #17/#19. **Door:** one-way.

### D-DOCTRINE — maintainable-by-enforcement (GOVERNING charter) — RULED ✅
- **Drift + contradiction = faulty process + enforcement.** Hand-reconciliation is not the fix; the enforcement that makes recurrence impossible IS the fix.
- **Maintainable BY ENFORCEMENT; automate anything automatable.** Without enforcement + automation, drift + staleness are guaranteed to return.
- **Total accounting — every file, doc, folder accounted-for AND justified:** owner + justification (→ decision/need) + reachability (→ masterplan) + staleness policy (TTL). Unaccounted/unjustified ⇒ blocks or auto-archives. Generated-not-hand-maintained.
- **Robust, not false:** no false promises; no thin/flaky enforcement or automation. Every gate proven by RED/GREEN fixtures that it actually BLOCKS; no advisory-shell-claiming-enforced (honest-claim §K + claim-ceiling #21). LIVE EVIDENCE the fear is real → `prd-axis-coverage` defined-not-active; `diataxis-doc-class` planned-not-blocking; 22 `oya-governance-*` crates NOT wired into CI; catalog `axes_count:6` stale vs 7.
- **Patterns: maintainable + hyperscaler (Google/Meta monorepo doctrine) + Linus-Torvalds taste** — simplicity, good data structures, no special cases, stable contracts, reject the bad.
- **Check ALL architecture-design invariants** corpus-wide: BNF 13-layer enum (0056/0105) · clean/hexagonal kernel-adapter-app · parallelizable builds+lanes (affected-targets, one-lane-one-path 0360/0366) · minimal shared blast radius (visibility fences, one-version, bounded contexts) · data_class · no_std-kernel/std-adapter.
**Door:** one-way (governing).

### D-DOCORG — documentation reorganization (use the EXISTING scheme, don't reinvent) — RULED ✅
Three existing schemes (verified present in source): **Diátaxis quadrants** (Tutorial/How-to/Reference/Explanation = doc-folder topology; `docs/standards/doc-style.md` + `docs/DOCUMENTATION.md`; agreed in `docs/ideas/planning-ssot-consolidation.md`; NOT yet executed) · **7-product-axis model** (`docs/DESIGN.md`; machine-readable in products/catalog/contracts.json) · **DOC-CATALOG tier** (0/1/2/3/cross-cutting = governance weight). Reorg = execute the unexecuted Diátaxis folder topology + reconcile drift (stale axes_count 6→7) + unify the three orthogonal attributes (quadrant + axis + tier) into ONE enforced machine-readable per-doc record, generated-validated. **Door:** one-way.

**SHARPENED (founder 2026-06-07) — organize EVERY doc to FIXED ENUMS; drift impossible by automation + enforcement.** The canonical doc-TYPE enum is **ADR-0388's SEVEN doc-axes** — `DECISIONS` (docs/decisions/ADR-NNNN) · `PLANS` (masterplan.generated.json, auto-gen) · `INDEX` (ADR-INDEX, auto-gen) · `SPECS-MS` (microservices/<ms>/manifest.json) · `SPECS-CRATE` (registry/catalog/*.yaml) · `RUNBOOKS` · `IPS` — plus transient `IDEAS` (14-day promote-or-archive); a CLOSED set, `no-shadow-docs` forbids anything off-axis (allowed `docs/` subdirs are fixed too). Every doc carries a unified **4-attribute fixed-enum record** `{doc-axis (ADR-0388) × Diátaxis quadrant × product-axis (DESIGN.md/contracts.json) × DOC-CATALOG tier 0/1/2/3/cross-cutting}`, machine-readable + generated-validated.
- **Human-readable docs = GENERATED or MINIMAL.** PLANS/INDEX (+ catalog.json/contracts.json) are AUTO-GENERATED from SSOT (never hand-edited); the rest are MINIMAL per-unit metadata. Prose is bounded to DECISIONS/RUNBOOKS/IPS.
- **Manual-maintenance is ACCOUNTED + has a PROTOCOL.** The no-auto-gen axes (DECISIONS/SPECS-MS/SPECS-CRATE/RUNBOOKS/IPS) follow the **ADR-0019 DOC-UPDATE-PROTOCOL** (5-stage pre-flight→author→validate→review→publish; per-doc owner_team + agent_authoring_allowed + dependent_docs cascade + EVT-DOC-* audit). Every manually-maintained doc MUST declare its owner + trigger + protocol binding; un-accounted ⇒ gate violation.
- **TWO-LAYER DRIFT DEFENSE (founder, defense-in-depth):** (1) **AUTOMATION** generates every generatable doc + the per-doc record from SSOT; (2) **ENFORCEMENT** — the doc-axis gate — BLOCKS shadow-docs / off-axis / stale-index / bad status-casing / catalog↔manifest drift / aged-ideas on every PR. **Even if the generator is never run (automation fails), the enforcement gate catches the drift** → staleness is impossible by construction, not by diligence.
- **De-CLI (Δ2/Δ3):** ADR-0388's gate `oya gate validate doc-axis` + auto-gen `oya gen masterplan`/`oya doc adr-index` + ADR-0019's `oya-governance-doc-catalog`/`oya-governance-glossary` are oya-CLI → **SUPERSEDE the CLI mechanism, re-home to the CaC/CaaS pipeline**; ADR-0019's Foundry `batches.json` → eradicate. **ADR-0388 + ADR-0019 = AMEND** (keep the enum + protocol LOGIC; replace the CLI delivery). The `organize-all-documentation` effort converges on THIS enum (not a fresh taxonomy).

### D-D1-TOPOLOGY — consistency topology (backlog T-D1; the defining architectural call) — RULED ✅
**Co-located consistency domain, federated execution.** Ontology = the SINGLE transactional system-of-record + write-path: typed entities + **effective-dating as a first-class kernel temporal type** (backlog A3 — VERIFIED ABSENT from the 1797-line ontology kernel, so this is a real net-new kernel build, not just a doc) + typed ACTIONS + audit. The workflow engine = federated durable-execution (Temporal-shaped) for orchestration ONLY; its domain mutations execute as **ontology typed-actions inside the ontology txn and return a consistency token**, so the next step observes read-your-writes; workflow's own DB holds orchestration state only, never domain entities. Intelligence = fully federated/stateless (read = typed tools, write = same typed actions). **Kafka outbox is REMOVED from the critical consistency path** (notification / eventual fan-out only) → SAP integration-tax eliminated by construction; engines still scale independently. **Acceptance = a passing payroll-close read-your-writes conformance test** (HR effective-dated change @T0 observed by payroll-close compute @T0, no stale read). **Author ONE net-new D1 meta-ADR** (the masterplan-wiring keystone for the backlog program; also needs the EntityMutated/Created/Deleted proto [A2a] + the workflow→AI-step gRPC contract [A2b], both verified absent); **resolve the D1/D01/D15 id naming clash** when numbering. **Door:** one-way.

### D-AEC-DECLINE — agent-execution-controller (backlog §I / T-AEC) — RULED ✅
**DECLINE + record.** The capture-only agent-execution-controller idea (work-item/evidence-bundle contract for agent-CLI K8s Jobs) is **declined**: ADR-0116 (retire external agent-coordination tooling) + ADR-0363 (retire agentic-VCS foundry) — both Accepted — deliberately killed the adjacent layer; the "missing area" is missing on purpose. Linus-taste: don't add what isn't load-bearing. **Amendment action (in the gated campaign):** record the decline in `source/docs/ideas/agent-execution-controller.md`; let the `oya-code` harness repo lapse; ADR-0116/0363 stand unamended. **Door:** one-way.

### D-SEQUENCE — FIREWALL-FIRST campaign order (verified by the justify/account/robustness audit) — RULED ✅
**Audit verdict (live-verified 2026-06-06):** enforcement is a FAÇADE — the sole required context `oya-ci-required` has NO live producer (both branch-protection files self-disclaim it as a "P0.0 target … not live-enforcement until the producer is live"), so **0 gates block a merge today**. This IS the mechanism of the drift. Therefore — **you cannot fix the canon on fake enforcement; make enforcement REAL first, then fix the canon THROUGH it.** Founder-ruled order:
- **Step-0:** commit the WIP to a clean base (source on `feat/oya-ci-tide`, 79 dirty, WIP plan untracked) — precondition to ALL mutation.
- **Phase-0 (false-green firewall):** author the ONE canonical CI/CD ADR (D-CICD — the producer spec) → stand up the live **`oya-ci-required` producer** (first real piece of oya-ci) → land the **4 keystone gates** over ONE generated accounting-registry (cross-artifact-agreement · total-accounting · staleness-reaper · automation-ratchet), each proven by **RED/GREEN** fixtures it actually blocks.
- **Phase-1 (amendments, each GATE-VERIFIED):** unified single-owner A-lanes (A-CI/A-STRUCT/A-FOUNDRY/A-INTEGRITY/A-TASTE/A-IDENTITY) — CI-cluster consolidation, foundry-completion + fix ADR-0363's false "eradicated", dup-0377 renumber, phantom-0150, status-enum, index-regen, pure-split eradication — never two sweeps over the 346-id space.
- **Phase-2:** doc-reorg (44→6 Diátaxis homes) + OWNERS/reachability closure + net-new ADRs (D1 meta-ADR + effective-dating kernel + sequenced platform-readiness program).
**Dependency spine:** producer → accounting-registry → gates → amendments → reorg. **door:one-way + founder sign-off** at: FE-1 producer go-live, the canonical CI/CD ADR, pure-split completeness, and every source mutation. Verified-state corrections folded in: foundry residue honest figure = `microservices/foundry/` 597-file shell + ~4110 mentions + Cedar `oyatie.foundry.*` (NOT 201 un-renamed crates — that raw count was worktree-inflated; crate-dir rename largely done). **Door:** one-way.

### D-FOUNDRY-CLARIFY — foundry is a dead context (founder 2026-06-06) — RULED ✅
**SOURCE-BACKED routing (ADR-0363:77–80, founder-authored):** "Foundry → Intelligence absorption; **Governance stays its own service**" — the foundry AI-agent platform + the `foundry/` name → Intelligence (`:78`); **Governance REMAINS its own service** (`:79`, layering: it validates Intelligence so can't be part of it); **no vcs service** (`:80`, retired). The source-backed routing is therefore **3-way** (intelligence / governance-own-service / vcs-retired), and foundry-fitness→governance is ~73% already landed in the codebase. **OPEN — NOT YET SOURCE-BACKED (founder 2026-06-06 verbally said "foundry all → intelligence", which CONTRADICTS ADR-0363:79):** founder to rule whether this OVERRIDES ADR-0363:79 (fold Governance into Intelligence too — note this breaks the layering rationale AND undoes the ~204 landed governance refs) OR was shorthand for "the foundry CONTEXT is dead; its platform is Intelligence" (= the source-backed 3-way, governance stays separate). **RESOLVED (founder 2026-06-06): the 3-way is CONFIRMED** — foundry-platform → Intelligence; foundry-fitness → **Governance (INTERIM service) → owned cedar-like PARC policy/compliance engine** (D6; governance is its own *interim* service while PARC cooks, NOT folded into intelligence); agentic-VCS → retired. A-FOUNDRY executes the 3-way; "all → intelligence" is NOT adopted. Amend **ADR-0363** to fix BOTH its false "The Foundry name was eradicated" claim AND its now-stale 3-way (intelligence/governance/vcs) + `microservices/foundry/`-shell text (that path no longer exists on `feat/oya-ci-tide`). Residue on this branch: `source/{docs/foundry, contracts/openapi/foundry, docs/products/foundry, docs/runbooks/foundry}` + Cedar `oyatie.foundry.*` (ADR-0247); ~4,746 tracked files mention `foundry` (re-verify). **Confirm, do NOT silently fold:** ADR-0363 kept Governance as its OWN service (it validates intelligence → cannot be part of it) + retired the agentic-VCS — confirm "all → intelligence" is the residue/name resolution, not a dissolution of the governance service / un-retirement of vcs. **VOCAB (founder 2026-06-06): "foundry" is FORBIDDEN VOCAB** — eradicate the word canon-wide: delete the ADR-0018 glossary "Foundry" row (C-2), add `foundry` to the brand-residue/forbidden-vocab gate enum (CI-blocked so it can never reappear as canon vocab), sweep the ~4,746 tracked mentions (foundry-named crates → intelligence/governance, Cedar `oyatie.foundry.*` principals → renamed). HARD carve-out: external **"Palantir Foundry"** (a different referent — keep). This is the gate-enforced form of the foundry→intelligence routing above. **Door:** one-way.

### D-FORGE-CLARIFY — Forgejo DROPPED; GitHub-interim only; bespoke SCM = Sapling-inspired (founder 2026-06-06; reaffirms D2) — RULED ✅
**Forgejo is DROPPED entirely** — NOT a bridge adapter. **GitHub is the ONLY interim forge** (now), until a **bespoke, Sapling-inspired SCM** (`cloud/cloud-scm`; the masterplan's own "Piper/Sapling-style monorepo change graph" at `masterplan.json:6134`). **CANON CONTRADICTION to fix (procedure failure — I propagated stale Forgejo framing into ADR-0515 without reconciling against D2):** the masterplan + ADRs still say "Forgejo/git as bridge adapters until cutover" (`masterplan.json:1227, :5960, :6106, :6109, :6121, :6132`; ADR-0510 SCM-cutover; ADR-0363; and ADR-0515's "Forgejo-native"/`ForgeAdapter` prose). FIX = a **systematic Forgejo-eradication sweep** (Phase-1, gate-verified — like foundry→intelligence): amend ADR-0510 (cutover = GitHub-interim → Sapling-bespoke; Forgejo removed) + ADR-0363 + ADR-0515 + the `oya-ci-controller-forgejo-adapter` framing + regenerate masterplan + sweep all 59 tracked Forgejo refs → GitHub-interim. The producer is ALREADY correct (GitHub poster). Do this in the gate-verified sweep, NOT reactively. **Door:** one-way.

### D-DISPOSITIONS-RATIFIED — Accepted-ADR audit dispositions ratified (founder 2026-06-06) — RULED ✅
The source-backed, adversarially-verified Accepted-ADR audit (SSOT = `00-MASTER-CONTRADICTION-REGISTER.md`, merging `accepted-adr-audit/` + `contradiction-audit/`) is **founder-ratified**: ~48 contradictions (21 CRITICAL + 24 HIGH) · ~16 hyperscaler-lens problems · 8 anti-patterns · **169-ADR disposition table** = KEEP ~108 / AMEND ~50 / **SUPERSEDE 9 clean** (0015, 0053, 0103, 0136, 0137, 0143, 0160, 0187, 0509) **+ 3 bridge** (0374, 0380, 0239 = `superseded-on-cutover`) / **ARCHIVE 5** (0057, 0097, 0101, 0102, 0138). **Hyperscaler AMENDs ratified as FIX-DON'T-DISCARD** (0006 effective-dating · 0028/0031 SPOFs · 0029/0030 add M0 gate · 0062 day-1-hyperscale · 0067 ops-mega-service · 0098 power-loss · 0173 own-the-stack picks · 0202 ArgoCD-as-engine→adopt · 0482/0508 bespoke-roadmap→D8 capacity gate). **KEYSTONE** (Phase-0 gate addition): add `superseded-on-cutover`/`bridge-unratified` status-enum value + a no-dangling-ref/supersession-completeness invariant to the cross-artifact-agreement gate. All execution is **gate-verified Phase-1** (firewall-first); these are the inputs to the A-lanes. **Door:** one-way.

### D-CICD-AUTHORITY — GitHub CI/CD is the live INTERIM authority; bespoke oya-ci/cd built in parallel/shadow (founder 2026-06-07) — RULED ✅
**GitHub Actions = the SOLE CI/CD authority until cutover** (GitHub Actions + branch-protection required checks are the ONLY thing that gates merges; nothing else has merge authority until the bespoke is proven and explicitly cut over). **The bespoke `oya-ci`/`oya-cd` is STILL built + maintained** — but it runs in **parallel/shadow (dogfood), NOT the live authority**: the producer posts `oya-ci-required` as a **non-blocking** status and exercises the same gate logic; cutover to oya-ci-as-authority happens later when proven (D-MASTER:26 build-first-cutover-later; ADR-0510 trigger). **Consequence for Phase-0:** "make enforcement real" is delivered NOW via **GitHub** (Actions running the gate logic + required checks); the earlier "flip the live ruleset to require `oya-ci-required`" founder-paired step is **NOT taken** (GitHub stays authority — no GitHub-admin blocker). **Shared gate LOGIC = Rust gate crates**, invoked by GitHub Actions (live) AND by oya-ci (shadow) — one logic, two runners. **Autonomy:** founder cleared full autonomous build of the bespoke. Reconciles with D-CICD/0515 (the bespoke remains the owned destination) + D-FORGE (GitHub-interim → Sapling-bespoke). **Door:** one-way.

**AMENDMENT — ONE CANONICAL CI (founder 2026-06-07, post-ralplan-APPROVE of CICD-DESIGN-PLAN.md):** Do NOT maintain two parallel CIs. Collapse to **ONE canonical CI** = one shared Rust gate-logic + one pipeline definition + one surface-all aggregator → **ONE canonical BLOCKING required context `oya-ci-required`** (name ratified; supersedes the interim `oya-ci-firewall-required` pick). REFINES "one logic, two runners": the two runners are the SAME canonical pipeline across TIME (GitHub Actions now → owned oya-ci later = a runner *migration*), NOT two simultaneous parallel runners. **SUPERSEDES the "`oya-ci-required` stays non-blocking shadow" clause** — `oya-ci-required` IS the one canonical blocking context, produced by GitHub Actions now (live authority/runner), by the owned runner after cutover. DROPS the parallel-shadow run + verdict-agreement apparatus (confidence-building, not enforcement; GitHub→owned cutover validated at runner-swap). KEEPS: GitHub Actions = live authority now; oya-ci = owned destination; cutover-later; gate-logic identical. **THROUGHPUT — the deciding factor (founder, hard):** keep caches WARM and ensure **only the part that CHANGED is cold**. Mechanism: content-addressed actions + a **remote/shared read-through cache** (Buck2 RE-API → BuildBuddy/EngFlow/BuildBarn/NativeLink) so an unchanged action's input-hash is a cache HIT (never re-executed, on any runner) + **affected-targeting** (owner→rdeps closure runs only the changed targets + reverse-deps) + content-addressed producer artifact (0 recompiles when unchanged) + `actions/cache` for the buck2 binary/toolchain/buck2-out so ephemeral runners don't bootstrap cold + hermetic actions (non-hermetic actions poison the cache) + precise (not coarse) cache keys (a 1-file change invalidates only its dependent actions, not the world) + merge-queue batching (whole-tree cost amortized per batch). Result: CI wall-clock scales with the SIZE OF THE CHANGE, not the repo (the hyperscaler property). And **NOT fail-fast — surface ALL errors in one sweep** (one aggregated report). Best-practices preserved, productivity up, enforcement NOT weakened (only confidence-building shadow removed, no enforcement seam). CICD-DESIGN-PLAN.md collapses Option-A-shadow + Stage-6-verdict-agreement + the two-context split accordingly. **Door:** one-way.

### D-SSOT-CURRENT-TRUTH — the SSOT holds ONLY current truth; hard-destroy retired/legacy/superseded (founder 2026-06-07) — RULED ✅
**Founder principle: "the SSOT contains only the truth and current truth."** The doc/decision stores + the live `source/docs/` tree hold ONLY current truth — no superseded entities, no history, no tombstones, no live `_archive/` dirs. Retired/legacy/superseded ADRs + docs are **HARD-DESTROYED** (`git rm`; **git history is the sole archive/audit-trail** — the "why" is recovered via git-archaeology, NOT a kept superseded doc), with **every reference to a deleted id SCRUBBED** so there is **zero dangling — no-dangling is satisfied by FULL EXCISION, not tombstone-entities**. **OVERRIDES** (deliberate, full-information founder project-doctrine override): supersede-don't-discard · archive-not-delete · the ratified ARCHIVE-5/SUPERSEDE-keep-9 dispositions · the generic `/documentation-and-adrs` "don't delete ADRs" best-practice. The lineage philosophy shifts from **ADRs-as-history → git-as-history**. **Only markdown kept:** `README.md` / `CLAUDE.md` / `AGENTS.md` + tool-mandated (GitHub special files, `SKILL.md`, `LICENSE`); everything else is data in the stores or a generated view. **ALLOW-LIST (the closed set of permitted source artifacts, doc-axis-gate-enforced):** carve-out markdown · the domain stores (instructions · design-docs · registry-catalog) · generated views · live infra/configs · build-graph (Cargo/BUCK) · **a consolidated `benchmarks` doc (1 md-or-json) and a consolidated `contracts` store (1 keyed JSON — machine-consumed, NOT markdown)** — both ADDED to the allow-list (founder 2026-06-07): scattered benchmarks(8)+contracts(87) → 1 each. CONSOLIDATE-with-dependency: benchmarks 8→1 updates the accounting-registry required-set (producer-regen + gate-baseline update or GATE-2 fails); contracts 87→1 re-points codegen/consumers. Anything NOT on the allow-list is CONSOLIDATE-into-a-store or DESTROY. **The no-dangling gate's invariant becomes "the SSOT references only current entities"** (held once the dead are excised + their refs scrubbed). **Execution discipline (override ≠ reckless):** `git rm` ONLY (history retained — NEVER `filter-branch`/BFG history-purge: breaks signed-commit integrity + is unrecoverable); a read-only **kill-list manifest** (KEEP=current vs DESTROY=superseded/legacy/retired + the reference-graph) precedes any deletion; per-batch kill-list shown for **door:one-way** founder review before commit; `grep`-verify **ZERO references to any deleted id** after each batch (the manual no-dangling safety net substituting for the not-yet-live cross-artifact gate); signed → github-mirror only. **Sequencing (founder: do not wait for the firewall):** the cleanup runs NOW to establish the clean current-only state; the firewall (accounting + staleness + cross-artifact + doc-axis gates) lands after to **ENFORCE stays-current-truth-only** (cleanup-now establishes; firewall-later prevents regression). Folds into D-DOCORG + the doc-as-data per-domain-store model (instructions · design · registry — three stores). **Door:** one-way.

**CLEANUP EXECUTION POSTURE (founder 2026-06-07 — THIS one-time whole-source-tree cleanup ONLY, explicitly NOT a standing rule):** bias HARD to DELETE. The asymmetry the founder ruled: a wrong DELETE is recoverable from git history; **leaving sprawl is not** ("deleting something you shouldn't is ok … leaving something out is not"). So for this sweep: **DEFAULT = DESTROY**; KEEP only with a CLEAR, STATED reason (live-referenced by running code/CI/gate · current-truth actively-used · gate-required-baseline · carve-out README/CLAUDE/AGENTS+tool-mandated); CONSOLIDATE current-scattered → its domain store; **AMBIGUOUS → DESTROY** (no default "review" pile). Be comprehensive/deliberate/thorough; end-state = a SELECT few docs + ZERO stale files/folders/scripts/code. **RECOVERABILITY PRECONDITION (load-bearing):** git-recovery only holds for COMMITTED content, and the source tree is currently DIRTY (~200 uncommitted consolidation files + the uncommitted firewall ratchet) — so a **CHECKPOINT-COMMIT of the whole source tree must land FIRST** (everything into history), THEN the aggressive `git rm` batches (never `filter-branch`/history-purge). Each delete still scrubs refs (no dangling) + producer-regens (gate stays consistent). The standing rule remains the rest of D-SSOT-CURRENT-TRUTH (verify-each-step, no-dangling, git-as-archive). **Door:** one-way.

### D-CLOUD-NATIVE — cloud-native (NOT CLI) is the universal posture; governance/evidence → pipeline; total forgejo/foundry eradication (founder 2026-06-07) — RULED ✅
- **POSTURE (universal):** oyatie is **cloud-native — NOT a collection of CLI tools.** "Do what Go does, cloud-native, in Rust" applies to the WHOLE stack: services / controllers / APIs / automated pipelines, not CLIs. We steer away from traditional Rust's CLI approach.
- **`oya` CLI:** RETIRED (ADR-0363) **— and it stays retired; do NOT record any revival in any ADR/doc/trace** (mentioning a future revival seeds later contradictions; keep it out of the canon entirely).
- **`oya gate` + `oya cli` (and `oya check` / `oya verify` / `oya-dev-cli` / `oya vcs` / `oya git`) are STALE → SUPERSEDE** (founder 2026-06-07; NOT amend-in-place): every ADR that establishes or relies on them as the mechanism is marked **Superseded**; the preserved gate / governance / check / honest-claim LOGIC (which is sound — e.g. 0129 honest-claims, 0135 aspirational-enforcement) is **re-authored as cloud-native CaC/CaaS pipeline ADRs** (run by GitHub Actions live / oya-ci shadow), never as CLI. The foundational grammar/enum (0056 `oya-check-*` BNF · 0092/0105 `cli` layer + check-family) is amended to drop the CLI/check productions in favor of the pipeline-gate convention.
- **GOVERNANCE + EVIDENCE → AUTOMATED PIPELINE:** the current `governance` service + `oya gate` / `oya-governance-*` lanes are **CLI-based = interim**; move ALL governance + evidence to a cloud-native **automated pipeline** (gates = Rust binaries run by GitHub Actions [live, D-CICD-AUTHORITY] + oya-ci [shadow]; evidence emitted by the pipeline, not a CLI). Cloud-native form of governance-interim→PARC (D-FOUNDRY-CLARIFY). **CAREFUL: governance is CLI today — migrate without breaking the live gates mid-transition (build-first-cutover-later).**
- **UNIVERSAL RATCHET (reaffirms D-META):** ALL stack postures toward **bespoke hyperscaler-pattern OWNED**, with **best-in-class VENDORED as INTERIM during transition.** SCM + CI + CD owned eventually = **best-in-class cloud-native products, DOGFOODED (oya = tenant `oyatie-internal`)** — part of oyatie's hyperscaler cloud (GCP/AWS/Azure-class). Interim: **GitHub Actions = live CI authority now**; GitHub = forge → bespoke cloud-scm/cloud-ci/cloud-cd later.
- **RUST CLOUD INFRA (self-hosting):** the entire **Kubernetes engine + OS + kernel are Rust**; the k8s engine serves the cloud ITSELF **and** tenants (self-hosting, like Rust self-hosts). (linux pilot = framekernel + Talos-class OS + k8s; [[cloud-native-rust-stack]].)
- **FORBIDDEN VOCAB — TOTAL eradication (founder-directed; OVERRIDES supersede-never-edit for these terms):** remove **`forgejo`/`forge`, `foundry`, `jenkins`, and `oya-vcs` / the agentic-VCS layer** from EVERYWHERE — code, infra, specs, registry, masterplan, docs, **AND superseded/historical ADR bodies** (scrub the word). The brand-residue gate (`registry/catalog/oya-check-brand-residue.yaml`) blocks them all going forward. **JENKINS = DROP NOW (founder 2026-06-07: "jenkins no need to keep — GitHub Actions is where we'll have our CI") — NO bridge retention:** delete the jenkins infra/lane/status-context definitions + scrub the word; GitHub Actions is the sole CI (D-CICD-AUTHORITY). The one founder-paired GitHub-admin step is the live branch-protection ruleset change that removes any jenkins-produced required contexts. **FORGE = SCRUB (founder 2026-06-07):** the forgejo brand-stem `forge` is eradicated; **`infra/forge` is CONSOLIDATED INTO `infra/gitops`** (the live gitops Application keeps running under the generic name — rename/merge + repoint `infra/gitops/values.yaml` + `bring-up.sh`; do NOT delete the live gitops). oya-vcs = the retired agentic-VCS, ADR-0363 — the GENERIC VCS/SCM concept survives only as `cloud-scm`. Carve-out: external product name "Palantir Foundry". **Door:** one-way.
- **NO SHELL SCRIPTS — everything Rust + cloud-native (founder 2026-06-07):** `.sh`/`.bash`/`.groovy` shell scripts are FORBIDDEN unless ABSOLUTELY necessary (a documented, justified, narrowly-scoped exception). ALL CI / governance / automation = **Rust binaries (gates) run by GitHub Actions** (D-CICD-AUTHORITY + D-CLOUD-NATIVE pipeline-not-CLI); ALL gitops = **declarative manifests (ArgoCD app-of-apps), NOT a shell `bring-up`**. Existing shell is a CLEANUP/MIGRATION TARGET: delete the dead/forbidden-coupled scripts (`*.groovy` jenkins, `scripts/check-oya-vcs-*.sh`, the pre-push/CI shell hooks), and for any LIVE behavior port it to a Rust gate or a declarative manifest — never re-author as more shell. Concretely for Task #25: the `infra/forge→infra/gitops` consolidation makes gitops declarative and **eliminates `bring-up.sh`** (replace with ArgoCD declarative sync; a Rust bootstrap tool only if a programmatic entrypoint is truly needed). **Door:** one-way.
- **AVOID CLI + EVERYTHING "JUST WORKS" (founder 2026-06-07):** CLI is avoided as much as possible — a justified fallback, NOT the interaction model. The platform is **AUTOMATIC + DECLARATIVE + SELF-OPERATING**: gates run themselves (GitHub Actions on push), gitops syncs itself (ArgoCD), config is declarative (convention-over-configuration), no manual bring-up, no human-invoked governance CLI. Target UX = **"everything just works"** — zero-touch, self-configuring. This sharpens **Task #26**: the `oya check`/`oya gate`/`oya verify`/`oya-dev-cli` governance-CLI family is RETIRED into the automated pipeline (Rust gates triggered by GitHub Actions; evidence emitted by the pipeline) — reinforces **D-GOVERNANCE-CENTRAL** (PaC/CaC *served*, not invoked) + **D-CLOUD-NATIVE** (pipeline-not-CLI). Any surviving CLI must justify why automation can't cover it. **Door:** one-way.

### D-GOVERNANCE-CENTRAL — consolidated, central governance authority via PaC/CaC + PaaS/CaaS (founder 2026-06-07) — RULED ✅
**All governance authority is CONSOLIDATED + CENTRAL** (one authority, not scattered `oya-governance-*` lanes / a `governance` service / a CLI), delivered as the cloud-native quartet:
- **PaC — Policy as Code:** policies authored as code (Cedar = the contract, D6), version-controlled + tested.
- **CaC — Compliance as Code:** compliance controls + evidence requirements as code (§K honest-claim controls; the keystone gates — cross-artifact-agreement / total-accounting / status-enum / no-dangling-ref — ARE CaC checks).
- **PaaS — Policy *as a Service*:** the OWNED policy engine (PARC, D6) served as the single CENTRAL PDP every service/tenant consults (replaces per-service + CLI policy).
- **CaaS — Compliance *as a Service*:** the compliance + evidence + attestation PIPELINE served centrally (the automated governance pipeline, D-CLOUD-NATIVE; runs the CaC checks, emits evidence — GitHub Actions live / oya-ci shadow).
This is the **hyperscaler governance pattern** (AWS Cedar+Config+Audit-Manager · GCP Policy-Controller/Org-Policy · Azure Policy class), **OWNED + DOGFOODED** (oya = tenant). It is the consolidation TARGET that retires the CLI governance (oya check/gate/verify + `oya-governance-*` lanes = interim → this central PaC/CaC/PaaS/CaaS). Reconciles D6 (Cedar+PARC) · D16 (autonomy gate, governance-owned) · §K (compliance-automation) · D-CLOUD-NATIVE (pipeline-not-CLI) · register #19 (cloud toolchain services) · D-FOUNDRY-CLARIFY (governance-interim→PARC). **Acronym note:** PaaS/CaaS here = Policy/Compliance *as-a-Service* (the served counterparts of PaC/CaC), NOT "Platform as a Service" — confirm if you meant otherwise. **Door:** one-way.

### D-LLM-DOMAIN — anything LLM = cloud-intelligence's domain (founder 2026-06-07) — RULED ✅
**Anything LLM-related is the domain of `cloud-intelligence`** (the cloud-owned Bedrock-analog framework + runtime, D-INTEL): inference, provider gateways/adapters, key-pools, model routing, prompts, agentic-LLM execution, eval — all live in/under cloud-intelligence and are served to ALL tenants (API + subscription). **`oya-intelligence` is the tenant-facing servicing layer that CONSUMES cloud-intelligence** (oya = dogfood tenant). Consequence for the foundry eradication/routing: foundry's LLM/AI-agent **framework → `cloud-intelligence`** (sharpens decision-record :86/:90 + D-FOUNDRY-CLARIFY — the framework/runtime is cloud-intelligence, the tenant-facing workflow servicing is oya-intelligence); any LLM functionality found elsewhere consolidates into cloud-intelligence's domain. Reconciles D-INTEL (AI two-layer) + the engine re-home (oya/intelligence → cloud-intelligence). **Door:** one-way.

### D-AUTHORITY-CONVERSATION — the conversation/decision-record is the APEX authority; un-surfaced = stale (founder 2026-06-07) — RULED ✅
**Anything NOT surfaced/decided in this consolidation conversation (= captured in THIS decision-record) is treated as STALE and is no longer authority.** The decision-record is the apex SSOT; every source ADR/spec/doc/masterplan claim is **stale-until-verified against this record**. Verification mandate (founder): **verify MYSELF (no rubber-stamping agent reports) — each Accepted ADR against this record, nothing missed; and verify that nothing dropped along the way is actually worth preserving** (no baby-with-bathwater; the verify-nothing-lost rule). Consequence: an Accepted ADR that contradicts a conversation ruling, OR relies on stale mechanisms (oya-CLI governance, forgejo, foundry, GitHub-Actions-not-sole, LLM-outside-cloud-intelligence), OR was never reconciled here = **STALE → amend/supersede**; a dropped/ARCHIVE/declined item that holds genuinely-worth-preserving substance = **flag for salvage before drop**. **Door:** one-way.

### D-MULTISPECTRUM-RETIRED — the 21-facet multispectrum-review doctrine is retired/superseded; critique DROPPED (founder 2026-06-07) — RULED ✅
**The multispectrum-review doctrine (21-facet F1–F13 + M1/M2 + A1–A7 consensus-debate review, v2.4.0) is RETIRED/SUPERSEDED** and joins the hard-destroy set under **D-SSOT-CURRENT-TRUTH** (git history is the sole archive; no tombstones). It was never ratified-forward: the canon filed it only as the LEGACY BRIDGE (:110; ADR-0513/0363), the only Accepted tie is **ADR-0092** (workspace dependency-seam policy), and the Proposed ADRs framing it as load-bearing (0327/0323/0322/0247) all cite a **PHANTOM `ADR-0091-multispectrum-review-doctrine.md` that does not exist** (on-disk ADR-0091 = "Foundry write-gate foundations" — an ID collision to resolve before scrubbing dangling refs). **Two halves, two fates (verified, not assumed):** (1) **Accounting/structural half → SUPERSEDED** by the ratified new substrate — **D-CICD-AUTHORITY** (one-canonical-CI, GitHub-Actions authority, `oya-ci-required`) + the **Phase-0 firewall** (accounting-registry-producer + GATE-1..4 + cloud-ci-firewall) + the doc-axis gate (`oya-check-doc-axis`). (2) **Per-changeset multi-lens CRITIQUE half → DROPPED (founder-ruled, NOT re-homed).** Verification proved the critique has NO replacement in the firewall (the 7 gates + doc-axis are purely structural/accounting — none evaluate facet/rigor/critique) AND that the go-forward design still demanded it (`bespoke-cloud-toolchain-services.json:174` + the GATE-4 `multispectrum/reviewer requirement` row feeding AC-0.12, with an open "non-501 reviewer status producer" gap). The founder chose to **DROP it rather than build a cloud-ci reviewer-status producer**, **consciously sacrificing ADR-0322's substance-density/anti-template-stamping bar + ADR-0247's SOC2 CC8.1 self-modification attestation** (accepted loss — recorded so it is not silent). **Consequence:** the GATE-4 reviewer/multispectrum row + the AC-0.12 reviewer/multispectrum input + `bespoke-cloud-toolchain-services.json:174` must be **DE-REQUIRED** (not re-backed) in lockstep with the lane retirement, or GATE-4 reds on an unbacked row. **Execution (verify-each-step, recoverable from checkpoint e38624dc4):** (a) **DONE 2026-06-07** — destroy-now slice committed + pushed github-mirror (`7a3455e5`): doctrine docs `docs/standards/multispectrum-review*.md` + templates `templates/checklists/pre-pr-multispectrum.*` + `evidence/debate/**` (324 facet-verdict files; sole consumer was the never-built `consensus-debate-evidence` subcheck) + 5 dangling-free residue, paired with **atomic** producer face-regen (accounting-registry 24161→23842 rows; gate-baseline debt buckets shrink-only); registry-drift + cloud-ci-firewall + total-accounting all GREEN. (b) **SEQUENCED with Task #25/#26** (load-bearing TODAY, gated): the lane `libs/oya-check-dependency-seam` multispectrum subchecks + fixtures (workspace member; remove the `multispectrum-evidence-attached` subcheck + fixtures as ONE atomic edit, keep the 3 mechanical seam subchecks for rehoming), the machine spec `specs/multispectrum-review.json` (runtime-read by the forbidden-vocab `oya-vcs-admission-gate-*` crates — destroy in the SAME sweep), the CI-consumed `evidence/multispectrum/backbone-*` + `backbone-microservices-ci.yml` path-filters/parse-args, the `oya-dev-cli`/`oya gate`/jenkins bridge, and the **ADR-0092 amend** (strip the 3 multispectrum subchecks before removing lane logic). The Proposed ADRs 0327/0323/0322/0247 drop; `specs/masterplan.json` `dependency-seam-discipline` milestone **regenerates** from the amended ADRs; the two `tenant-rbac` crates keep (rewrite dangling `evidence_refs` strings); known cosmetic dangling refs (glossary synthetic test string, pr-review doc-comment, foundry codex comment) scrub in the same pass. **Door:** one-way.

### D-EXCELLENCE-MANDATE — full restructure authority toward hyperscaler/cloud-native excellence + the standing findings-ledger (founder 2026-06-07) — RULED ✅
**Founder grants FULL authority to renumber, rekey, revamp, RESTRUCTURE anything — code, architecture, stack, infra, specs, registry, docs, naming, IDs, schemas, crate/service boundaries — toward ONE bar: hyperscaler pattern · architectural maturity + excellence · maintainability · trailblazing modernness · cutting-edge · research-backed · industry best-practices.** Applies to **anything and everything**, not just docs/files. The keep-only-current-truth + cloud-native + kubernetes-native + no-shell + avoid-CLI + everything-just-works posture is the FLOOR; this mandate is the CEILING — actively RAISE everything to excellence, do not merely clean. Extends D-SSOT-CURRENT-TRUTH's re-author-over-supersede freedom to code/arch/stack (break naming/numbering/structure stability where it serves the bar).
**STANDING FINDINGS-LEDGER (the "take a note" mechanism — load-bearing):** while reading ANY artifact, RECORD every (a) **worth-fixing**, (b) **stale**, (c) **cloud-native / kubernetes-native / hyperscaler ANTI-PATTERN** item into `linux/docs/audit/initial-sweep-2026-06-06/FINDINGS-LEDGER.md` (keyed F-NNNN: category · area · severity · evidence[file:line] · recommendation · status). EVERY reading/audit agent appends; nothing noticed is lost; the ledger is the backlog that drives the excellence pass. **verify-each-step still governs** — a finding is logged only with cited evidence (no phantom findings). **Door:** standing authority; each destructive restructure stays per-batch founder-reviewable + signed + git-recoverable. Reaffirms D-DOCTRINE + D-CLOUD-NATIVE + [[verify-each-step-evidence-based]].

