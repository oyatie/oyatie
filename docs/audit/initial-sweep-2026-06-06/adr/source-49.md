# ADR Audit — source-49

- **side:** SOURCE (`~/Developer/source`, GitHub `jason931225/oyatie`)
- **chunk:** source-49 (slice line 337–343 of `ls -1 docs/decisions/ADR-*.md | sort`)
- **range:** ADR-0482, ADR-0506, ADR-0507, ADR-0508, ADR-0509, ADR-0510, ADR-0511
- **ADRs reviewed:** 7
- **auditor posture:** masterplan is GENERATED from the ADR log; ADRs = immutable SSOT; a decision not represented as a LIVE ADR is "not needed." Trust the *superseding* ADR over stale front-matter (keystone map §6). Retired vocab enforced (foundry→intelligence/governance, tier-system→tenant-class, Redis→Valkey, Kafka→Pulsar, M0-M3/MVP).

> **Cross-cutting note for this chunk:** these 7 ADRs are the **bespoke-substrate spine**. ADR-0482 is the founder-locked doctrine root; 0506/0507/0508 are the three "Phase-1 OSS bridge → Tier-N bespoke" identity/crypto leaf ADRs that 0482 enumerates; 0509 is an internal-decomposition doctrine; 0510/0511 are the SCM + CI "transitory→bespoke/k8s-native destination" pair. The whole chunk is doctrinally coherent with itself — the audit tensions are almost entirely **cross-side (LINUX) and hyperscaler-realism**, not internal contradiction.

---

### ADR-0482 — Bespoke Substrate Roadmap: multi-decade kernel + OS ambition with phased timeline + bridges

- **decision_atom:** Every OSS substrate is a time-boxed *bridge*, not a destination; oyatie commits to a phased (Tier-1 NOW→12mo … Tier-4 60mo+/kernel-OS) bespoke-replacement roadmap where each bespoke component ships with a parallel OSS bridge, a feature-parity exit table, tenant opt-in, a rollback path, and **quality-gated (never hard-deadline) cutover**.
- **domain:** governance-process (primary; this is the meta-doctrine that sequences all bespoke work), cross-cut → kernel-frame (Tier-4 oya-kernel/oya-os).
- **current_status:** Accepted (founder authority, `door: one-way`, `doctrine_meta: true`, "Locked"), 2026-05-28; amended twice same day (kubers tier-remap; oya-authn-device Tier-3 add).
- **disposition:** KEEP. This is the governing doctrine the rest of the chunk instantiates. One AMEND-flavored caveat: the in-body amendment **RETRACTS its own Tier-4** ("kubers + Phase B kernel cover this; no separate Tier-4 needed") yet the Tier-4 table (oya-os/oya-kernel/oya-hypervisor/oya-runtime/oya-crypto) and §"Sequencing Principle" still speak of Tier-4 as live — an internal labeling drift the masterplan generator must normalize (is oya-crypto "Tier-4" or "kubers-Phase-B-gated"? both are asserted).
- **proposed_resolution:** NA (Accepted).
- **governing:** n/a (it is the governor).
- **truth_flag:** TRUE — but PARTIAL on the Tier-4 self-retraction (the doctrine is true; the tier *numbering* is internally inconsistent post-amendment).
- **in_masterplan:** YES — this is arguably the single most masterplan-load-bearing doctrine ADR in the chunk: "bridges not destinations" + the Tier table + "bespoke_replacement_planned" frontmatter rule are exactly the kind of generated-masterplan invariants the founder GOAL wants.
- **tensions:** (1) Self-tension: Tier-4 declared "RETRACTED" yet still tabled. (2) Cross-side LINUX fault-line #5 ("own everything" breadth): 0482 makes day-0-to-multi-decade *full* bespoke ambition explicit and founder-locked — this is the SOURCE end of the LINUX-vs-SOURCE "own when proven" ratchet, but 0482 leans much harder toward "own everything, cost is acceptable, only timeline constrains" than SOURCE's elsewhere-staged ratchet (ADR-0211/0173). 0482 actually *narrows* the LINUX/SOURCE gap (SOURCE now also wants own-kernel/own-DB eventually) — but raises a NEW tension: does 0482's "anything is acceptable cost for bespoke" survive the honest-cost test that 0510 itself applies (0510 declines to build bespoke VCS *because* it buys nothing today)? 0510 is the disciplined counter-example to 0482's maximalism.
- **hyperscaler_challenge:** ALIGNED in *direction*, QUESTIONABLE in *totality*. Google/AWS/Azure each own bespoke substrate — but each did so **selectively and revenue-justified**, not as a blanket "everything bespoke, cost-no-object" pre-commitment. No hyperscaler decided up front to own kernel+OS+VCS+CI+crypto+authn-silicon+events+search+graph+lakehouse simultaneously; they earned each replacement against a forcing function. Implication: KEEP the doctrine but the masterplan should encode 0482 *with* 0510's honest-cost discipline as a binding constraint (the "numeric trigger / forcing-function" gate), else 0482 reads as aspirational maximalism a hyperscaler architect would flag as unfocused.
- **ai_slop:** Low. Reads as genuine founder doctrine with a real self-correcting amendment (the kubers tier-remap). The FAANG bullet list (Borg/Spanner/Buck2/Nitro/Pingora) is accurate, not hallucinated.
- **refinement:** Normalize the Tier-4 retraction vs Tier-4 table; pick one canonical tier vocabulary. Reconcile "any cost acceptable" (0482) with "honest-cost forcing-function gate" (0510) — they are the same author, days apart, and the masterplan needs the *unified* rule.
- **consensus_needed:** Founder question — **"Is 0482's 'anything is acceptable cost for bespoke' a literal mandate, or is it implicitly bounded by 0510's numeric-forcing-function discipline? If bounded, that bound should be doctrine, not buried in one downstream SCM ADR."**

---

### ADR-0506 — aws-lc-rs canonical crypto provider (Phase-1) + oya-crypto Tier-4 bespoke destination

- **decision_atom:** `aws-lc-rs` (Apache-2.0/ISC, FIPS-140-3-validatable BoringSSL fork, ring-API-compatible) is the canonical workspace crypto provider replacing `ring`, held indefinitely as the bridge; bespoke **oya-crypto** is the Tier-4 destination, not admissible until kubers Phase-B kernel proofs land + FIPS 140-3 module validation + hyperscaler-readiness gate #10.
- **domain:** crypto-keymgmt (primary), security-supplychain (secondary — provider swap is a supply-chain/FIPS posture decision).
- **current_status:** Accepted, 2026-05-28, `planning_impact: false`, `door: two-way`.
- **disposition:** KEEP. Clean, well-scoped, hyperscaler-lens-passed, reversible (two-way door). The `ring`→`aws-lc-rs` swap is a textbook drop-in.
- **proposed_resolution:** NA (Accepted).
- **governing:** n/a.
- **truth_flag:** TRUE.
- **in_masterplan:** YES (as the crypto-provider canon) / PARTIAL on the oya-crypto Tier-4 entry, which is a *deferred-not-present* destination (record as planned, not as live capability).
- **tensions:** Mild internal: `ring` is retained in dev-deps with `TODO(ADR-0506)` — a known, tracked debt, not a contradiction. Cross-ref: this is the crypto floor under both 0507 (RP) and 0508 (authenticator); all three share the same Phase-1→Tier-N shape, so they must move as a family in the masterplan. No LINUX collision (LINUX has no crypto-provider ADR in 0001–0026).
- **hyperscaler_challenge:** ALIGNED. AWS literally runs AWS-LC internally; choosing its Rust binding is the *most* hyperscaler-defensible call in the chunk ("use the hyperscaler's own hardened primitive as the bridge"). The Tier-4 "own the crypto lib" ambition is where Google/AWS would raise an eyebrow — owning a FIPS-validated crypto library is enormous recurring validation cost for marginal differentiation; even hyperscalers mostly *fork/harden* (BoringSSL, s2n) rather than greenfield. Implication: keep aws-lc-rs as the effectively-permanent answer; treat oya-crypto Tier-4 as a very-long-tail aspiration, and the post-quantum parity rows (ML-KEM/ML-DSA) are better delivered *into aws-lc-rs's roadmap* than as a bespoke-crypto justification.
- **ai_slop:** Low. The parity table is concrete and credible (AEAD/KEX/sig/hash/KDF/MAC/PQ rows are real crypto categories). Benchmark claim "~5-15% TLS handshake improvement" is appropriately hedged ("expected", cites AWS published benchmarks).
- **refinement:** Complete the dev-dep `ring`→`aws-lc-rs` JWT-minting migration to close `TODO(ADR-0506)`. In the masterplan, mark oya-crypto Tier-4 as `bespoke_replacement_planned` with the lowest realistic priority.
- **consensus_needed:** Founder question — **"Is greenfield oya-crypto genuinely worth multi-year FIPS-revalidation cost, or is the honest destination 'fork+harden aws-lc-rs/AWS-LC' (the actual hyperscaler pattern) rather than 'replace it'?"**

---

### ADR-0507 — webauthn-rs canonical WebAuthn relying-party (Phase-1) + oya-webauthn Tier-2 bespoke destination

- **decision_atom:** `webauthn-rs` (Kanidm/Firstyear, MPL-2.0, W3C WebAuthn L3) is the canonical Phase-1 WebAuthn relying-party for oya-identity, consumed via `oya-identity-webauthn-*` use-case crates, bridging to bespoke **oya-webauthn** (Tier-2, 12–24mo) once the parity table is green and oya-identity Phase-2 promotion passes.
- **domain:** identity-authn (primary), cross-cut → authz-policy (parity rows include Cedar-policy-gated anomaly action + tenant policy override).
- **current_status:** Accepted, 2026-05-28, `planning_impact: false`, `door: two-way`.
- **disposition:** KEEP — with one AMEND flag: §2 mandates the `domain/usecase/api/adapter-postgres/rest/grpc/app` **PR #289 per-use-case clean-architecture pattern**, which **ADR-0509 (same day, 2026-05-28) explicitly supersedes** and declares "legacy." 0507's scaffold instruction is therefore born-stale and must be amended to the 0509 single-crate-per-service + subsystem-mod layout. (0509 §migration even names oya-identity as a Phase-1 collapse target.)
- **proposed_resolution:** NA (Accepted).
- **governing:** For the scaffold-pattern conflict: **ADR-0509** governs (single-crate-per-service supersedes the per-use-case slice 0507 prescribes).
- **truth_flag:** PARTIAL — the *decision* (webauthn-rs now, oya-webauthn later) is TRUE; the *embedded scaffold convention* is STALE (contradicted by 0509 same-day).
- **in_masterplan:** YES (webauthn-rs as RP canon) / PARTIAL (oya-webauthn Tier-2 = deferred destination) — but the masterplan must take the 0509 layout, not 0507's per-use-case crate list.
- **tensions:** (1) **0507 ↔ 0509 scaffold contradiction** (per-use-case 7-crate vs single-crate) — both Accepted, both 2026-05-28; 0509 is the later/governing word and explicitly retires the pattern 0507 cites. This is the sharpest in-chunk tension and a concrete generator hazard. (2) OpenSK history: 0507 records a same-day **founder reversal** (original OpenSK exclusion overturned → 0508) — clean, but means any cached "OpenSK excluded" state is WRONG.
- **hyperscaler_challenge:** ALIGNED. Google/Apple/Microsoft all run bespoke RP stacks; adopting the Rust-canonical RP as the bridge and owning it later is exactly right. The Tier-2 "own the RP" timing (12–24mo) is the most defensible bespoke-timing in the chunk — RP logic is small, spec-bounded, and genuinely differentiating for multi-tenancy. No amend implication beyond the scaffold fix.
- **ai_slop:** Low. Attestation-format list (packed/fido-u2f/tpm/android-key/android-safetynet/apple) and algorithm list (ES256/384/512, RS256, EdDSA) are accurate WebAuthn specifics, not filler.
- **refinement:** Amend §2 + Consequences to the 0509 single-crate `oya-identity` layout (subsystem mod `webauthn/`), removing the `oya-identity-webauthn-{domain,usecase,api,adapter-postgres,rest,grpc,app}` crate enumeration.
- **consensus_needed:** Not contested on the decision; only the scaffold convention needs the mechanical 0509 reconciliation.

---

### ADR-0508 — OpenSK canonical authenticator-side reference (Phase-1) + oya-authn-device Tier-3 bespoke hardware destination

- **decision_atom:** Google's **OpenSK** (Apache-2.0, Rust FIDO2/CTAP2.1 firmware, nRF52840 reference, OpenTitan port in progress) is the canonical Phase-1 authenticator-side reference, vendored at `tools/opensk-vendored/`, bridging to bespoke **oya-authn-device** (Tier-3, 24–60mo: USB-C/NFC/BLE oyatie hardware key with private-root-CA attestation, secure element, biometric UV) and ultimately an OpenTitan-SoC open-silicon authenticator at Tier-4 — closing the identity loop with 0507's RP side.
- **domain:** identity-authn (primary), cross-cut → hardware-firmware (Tier-3/4 silicon + manufacturing + secure-element vendor ADR family).
- **current_status:** Accepted, 2026-05-28, `planning_impact: false`, `door: two-way`.
- **disposition:** KEEP — but this is the chunk's **most speculative** Accepted ADR. The Phase-1 commitment is tiny and honest (vendor OpenSK, dev-dongle provisioning, "NOT vendored in this ADR — intent only"). The Tier-3/Tier-4 destination (own the hardware key, then own the silicon via OpenTitan) is a multi-decade aspiration recorded as decided. KEEP the doctrine; flag the destination as deeply deferred.
- **proposed_resolution:** NA (Accepted).
- **governing:** n/a.
- **truth_flag:** PARTIAL — Phase-1 (OpenSK reference) is TRUE; oya-authn-device Tier-3/Tier-4 silicon is a TRUE-as-recorded-intent but capability-absent destination (record as planned, never as present; consequences themselves say "deferred IP", "NOT vendored in this ADR").
- **in_masterplan:** PARTIAL — Phase-1 OpenSK reference belongs in the masterplan; the hardware/silicon destination belongs as a `bespoke_replacement_planned` long-tail entry with an explicit "no capability exists today" marker, plus the triggered manufacturing/secure-element/lifecycle ADR family it promises (none of which exist yet → "not needed until promotion" under the audit rule).
- **tensions:** (1) Cross-side with **0482's own retraction**: 0482 amendment says kubers Phase-B covers silicon and "no separate Tier-4 needed," yet 0508 routes its own silicon (OpenTitan SoC) through a Tier-4 phase — minor tier-vocabulary drift inherited from 0482. (2) Scope/breadth (LINUX fault-line #5): owning authenticator *silicon* is the furthest-reaching "own everything" claim in the chunk; no LINUX ADR touches this, so no direct collision, but it amplifies the SOURCE-side maximalism tension 0482 raises.
- **hyperscaler_challenge:** MIXED. Owning the *firmware fork* (OpenSK→oya-authn-device) is ALIGNED — Google Titan, Apple Secure Enclave, MS Pluton confirm hyperscalers own authenticator stacks. Owning the *silicon* (bespoke OpenTitan SoC + manufacturing line + per-device root-CA injection) is QUESTIONABLE for a company at oyatie's stage: that is Titan/Pluton-class capex that took Google/MS years and partner-silicon (AMD/Intel/Qualcomm for Pluton) — none did it greenfield-solo. Implication: keep the firmware-fork destination; treat the silicon/manufacturing tier as aspiration gated behind explicit hardware-budget + manufacturing-readiness ADRs (which 0508 itself defers) — do not let the masterplan present own-silicon as a near-or-mid-horizon commitment.
- **ai_slop:** Low–moderate. Technically accurate (CTAP2.1, nRF52840, ATECC608B, OpenTitan are real). The borderline-slop signal is the *breadth* of the destination table (DIY dongle → production line w/ per-device attestation injection → bespoke SoC) stated with parity-table confidence for hardware that does not exist and has no budget line — but 0508 is appropriately honest about this ("deferred IP", "budget line item required", "manufacturing ADR family triggered at Tier-3"), which keeps it on the right side of slop.
- **refinement:** In the masterplan, split 0508 into (a) live: OpenSK Phase-1 reference + dev-dongle provisioning, (b) deferred: oya-authn-device firmware Tier-3, (c) far-deferred/aspirational: OpenTitan silicon Tier-4. Only (a) is a present capability.
- **consensus_needed:** Founder question — **"Is bespoke authenticator *silicon* (OpenTitan SoC + own manufacturing line) a real roadmap commitment, or an aspiration we should mark explicitly non-committal until a hardware-budget ADR exists? The firmware fork is defensible; the silicon line is Pluton-class capex no startup-stage org has done solo."**

---

### ADR-0509 — Hyperscaler service decomposition pattern (single-crate-per-service + mod-based subsystems)

- **decision_atom:** **Single-crate-per-service** (one `Cargo.toml`, one binary, subsystems as `pub mod` directories, proto/OpenAPI-3.2.0/AsyncAPI-3.1.0 as interface SSOT, adapter indirection only when 2+ real backends ship today) is the canonical service decomposition — **superseding the PR #289 per-use-case 7-crate clean-architecture slice**, which is declared legacy because it has no FAANG-internal equivalent and projects to 1,500–3,000 workspace crates.
- **domain:** ci-cd-build (primary — build-graph/workspace-topology + Buck2 target mapping), cross-cut → governance-process (it is a binding repo-structure doctrine).
- **current_status:** Accepted (founder authority), 2026-05-28, `planning_impact: true`.
- **disposition:** KEEP — and this is a **governing/SUPERSEDE-issuing** ADR: it supersedes the PR #289 convention (a *convention*, not a numbered ADR, so it doesn't carry an ADR-id supersession edge, but it explicitly retires the `oya-intelligence-*` 7-crate pattern and *invalidates* the scaffold instructions embedded in 0476–0481 and 0507).
- **proposed_resolution:** NA (Accepted).
- **governing:** This ADR *is* the governor for the scaffold conflict against 0507 (and per its own §migration, against ADR-0476/0478/0479/0480/0481). Each of those is to be amended per 0509 Phase-1.
- **truth_flag:** TRUE — and it is a *truth-restoring* ADR (it corrects an over-engineered convention). Strong hyperscaler grounding.
- **in_masterplan:** YES — high-value generated-masterplan invariant: "one crate per service, subsystem mods, proto-SSOT, no speculative adapters." This is exactly the kind of structural rule the masterplan should bind and lint against.
- **tensions:** (1) **Ripple supersession:** invalidates the per-use-case scaffold language in 0507 (this chunk) and 0476–0481 (adjacent chunks) — the generator must propagate the amendment or those ADRs carry contradictory layout instructions. (2) Mild tension with clean-architecture/hexagonal doctrine elsewhere in the corpus (any ADR that *mandated* port/adapter isolation per use case is now narrowed to "only when 2+ backends ship"). (3) No LINUX collision (LINUX 0001–0026 has no service-decomposition ADR; LINUX ADR-0019 universal-port-ratchet is *compatible* — "vendor now, own when proven" ≈ 0509's "no speculative adapter").
- **hyperscaler_challenge:** STRONGLY ALIGNED — arguably the most hyperscaler-correct ADR in the chunk. Google (one Bazel binary target/service, subsystem libs, protos-as-SSOT), Meta (Buck2 single-target), Amazon (one service≈one binary, adapter only for genuine multi-backend), Stripe (monolithic payment surface by subsystem) all confirm it. The survey is accurate. No amend implication — this *is* the hyperscaler position, correcting an academic pattern.
- **ai_slop:** Very low. The FAANG survey is specific and correct (Borg/Spanner/Colossus, TAO/Scuba/Everstore, S3/DynamoDB cell services, Stripe Pay Server, Tailscale/Vector/Pulumi). The 1,500–3,000-crate projection math (5–10 use cases × ~50 services × 7–8 crates) is sound.
- **refinement:** Execute the Phase-1 amendments it mandates (collapse oya-identity/billing/meter/cost/flags; amend 0476–0481 + 0507 to the single-crate layout). Author the `IP-INTEL-MIGRATE-CANONICAL` it names. Until then, those ADRs carry stale scaffold instructions the masterplan must not emit.
- **consensus_needed:** Not contested — broadly correct. Only execution/propagation remains.

---

### ADR-0510 — SCM destination = bespoke hyperscaler monorepo-VCS; Forgejo transitory; cutover numerically triggered

- **decision_atom:** The long-horizon SCM destination is a **bespoke, Rust, hyperscaler-grade monorepo-VCS** (Piper/Sapling/Mononoke-class) — DECIDED; **Forgejo + plain git is the explicit transitory canonical host** (amends ADR-0363's unqualified "canonical"); the cutover is **deferred behind numeric triggers** (full-clone >10min, `.git` >20GB, >1M tracked files, or sustained >50 commit-status writes/s @ p99 >2s), with all near-term hyperscaler-SCM work (stacked diffs/trustless gateway/native sparse-partial checkout) layered host-portably on Forgejo now.
- **domain:** forge-vcs (primary), cross-cut → ci-cd-build (commit-status fan-out / merge-gate sink shared with 0511).
- **current_status:** **Proposed** — 2026-05-29, "DRAFT for founder review — do NOT auto-merge."
- **disposition:** AMEND (it is itself an amendment to 0363) + RATIFY-pending. The decision is sound and disciplined; it must clear founder review to become Accepted. As written it correctly amends 0363's "canonical" → "canonical-transitory."
- **proposed_resolution:** **RATIFY** — because the bespoke-SCM destination + numeric-trigger discipline is the *honest* reconciliation of 0482's maximalism with present scale; it is the model the masterplan should encode. Why: it converts an implicit, silently-droppable destination into a measured, deferred-not-absent one. (Ratify the *amend-0363 + record-deferred-destination*; the bespoke build itself stays gated, so ratification commits nothing premature.)
- **governing:** Amends **ADR-0363** (0363's Forgejo "canonical" is reframed transitory; 0363 index gets `amended_by: [ADR-0510]` on merge).
- **truth_flag:** TRUE — and notably the most *intellectually honest* ADR in the chunk: it ground-truths the repo (657 members / ~23,164 files / ~482M `.git`), concludes bespoke "buys nothing today," and **declines to build it now** — the disciplined counterweight to 0482's "any cost acceptable."
- **in_masterplan:** PARTIAL — the *transitory Forgejo host* + *numeric-trigger gate* belong in the masterplan now; the *bespoke-VCS destination* is recorded-deferred (no capability exists; `oya gate validate aspirational-enforcement` cited to ensure no binding claim asserts it exists).
- **tensions:** (1) **Forge three-way (keystone fault-line #4):** founder's migration directive = **GitHub** `jason931225/oyatie`; SOURCE canon (0363/0510) = Forgejo-transitory→bespoke-VCS; the GitHub directive conflicts with *even the transitory* Forgejo answer. Surface, do not resolve. (2) **Numbering:** `numbering_note` confirms `decisions.json next_adr` is STALE (records 0392) while dev carries through 0509; 0510 self-allocates above 0509 — consistent with keystone §6.3. (3) 0510 ↔ 0482: 0510's honest-cost "don't build bespoke yet" is the disciplined limit on 0482's "anything is acceptable cost" — same author, productive tension the masterplan must unify (see 0482 consensus question). (4) **LINUX ADR-0025** wants a Rust "Talos" / own-substrate; the own-VCS ambition here is in the same "own the substrate" family but no direct LINUX VCS ADR collides.
- **hyperscaler_challenge:** ALIGNED on *destination class* (Google Piper+CitC, Meta Mononoke+Sapling/EdenFS are real bespoke monorepo-VCS), **EXEMPLARY on method**: a Google/AWS architect would endorse "don't build it until a measured forcing-function fires" over "build it now." The one quibble: hyperscalers built these only at 10–100M-file / multi-PB scale; 0510's own triggers (>1M files, >20GB) correctly place oyatie far below the wall — so the *honest* hyperscaler verdict is "you may never cross these; keep Forgejo indefinitely," which 0510 essentially concedes. No archive implication; the discipline is the value.
- **ai_slop:** Very low. Ground-truth numbers are real and measured; rejected-alternatives are crisp; EdenFS/FUSE correctly rejected for interim scale. This is high-quality decision-writing.
- **refinement:** Founder review → flip Proposed→Accepted; set 0363 `amended_by`. Reconcile with the founder's GitHub directive (fault-line #4) — the masterplan must state which host is operationally true *now* (GitHub vs self-hosted Forgejo) independent of the bespoke destination.
- **consensus_needed:** Founder question — **"The active migration directive is GitHub `jason931225/oyatie`, but 0363/0510 make self-hosted Forgejo the (transitory) canonical host and bespoke-VCS the destination. Which is operationally canonical TODAY — GitHub-as-host or Forgejo-as-host — and is GitHub a permanent mirror, a temporary staging host, or a third path that 0510 must also account for?"**

---

### ADR-0511 — CI orchestration = Argo Workflows (k8s-native); Jenkins transitory; supersede Proposed ADR-0359

- **decision_atom:** The destination CI orchestrator is **Argo Workflows** (CNCF-Graduated, Apache-2.0, k8s-native DAG, self-hostable on Talos) + **Argo Events** (Forgejo-webhook trigger) + **ArgoCD/Argo Rollouts** (CD); **Jenkins is reframed as transitory bootstrap**; **ADR-0359 (Jenkins-sole-CI, only-ever-Proposed) is superseded** while its anti-GitHub-Actions-SPOF verdict is retained; the bespoke **oya gate engine stays the governance overlay** Argo invokes and **Forgejo Commit-Status remains the gate sink** until the SCM cutover.
- **domain:** ci-cd-build (primary), cross-cut → orchestration-scheduling (k8s-native CRD/GitOps coherence with Talos/ArgoCD substrate).
- **current_status:** **Proposed** — 2026-05-29, "DRAFT for founder review — do NOT auto-merge."
- **disposition:** SUPERSEDE-issuing (supersedes 0359) + RATIFY-pending. Sound; clears the long CI/CD churn chain to a coherent destination.
- **proposed_resolution:** **RATIFY** — supersession of 0359 is correct (0359 was never Accepted; Jenkins-as-destination contradicts the Talos/k8s/ArgoCD substrate) and Argo Workflows passes the hyperscaler-lens. Ratifying commits nothing premature (Jenkins stays the live surface until Argo lanes reach parity; "no binding claim asserts Argo is live today"). Drop nothing — the anti-GHA-SPOF half of 0359 is explicitly retained.
- **governing:** This ADR governs the CI chain; it **supersedes ADR-0359** (0359 flips Proposed→Superseded, `superseded_by: [ADR-0511]` on merge). Per keystone §1.3 it is the current net-truth terminus of the CI churn chain (0349→0359→0361→0408→**0511**→0513→0514) — note 0513/0514 (bespoke-Rust Prow oya-ci) sit *above* 0511 in that chain and further refine it; 0511 is the Argo-Workflows-destination node, not the absolute final word (oya-ci is). The masterplan must place 0511 as "destination orchestrator = Argo Workflows" with 0513/0514's bespoke-oya-ci as the longer-horizon successor.
- **truth_flag:** TRUE (as the current canonical CI-orchestration decision); PARTIAL only in that 0513/0514 extend it (Argo Workflows is destination *for the OSS-orchestrator layer*; bespoke oya-ci is the even-longer-horizon per the chain).
- **in_masterplan:** YES — Argo Workflows as destination orchestrator + Jenkins-transitory + oya-gate-as-overlay + Forgejo-status-sink are all masterplan-canonical. This ADR also kills the retired-vocab "Jenkins as destination CI" (keystone §2) — the masterplan must not emit Jenkins-as-destination.
- **tensions:** (1) **Stale front-matter to fix on merge:** 0359 still reads `Superseded`-pending; the masterplan FACTS note (keystone §4) flags `MASTERPLAN.md` still says "Jenkins required checks" — that line is now doubly stale (CI is Argo per 0511; Jenkins is transitory). (2) **Composition, not collision** with the in-flight Buck2 reversal (0392/0408): 0511 explicitly *defers to* the build decision rather than re-deciding it — clean. (3) Sibling 0510: shared Forgejo-Commit-Status sink that *moves at the SCM cutover* — the two ADRs are correctly cross-wired. (4) No LINUX collision (LINUX 0001–0026 has no CI-orchestration ADR).
- **hyperscaler_challenge:** ALIGNED. k8s-native DAG CI (Argo Workflows) is the cloud-native analog of Google TAP/Borg-driven and Amazon-internal pipeline orchestration; CNCF-Graduated + Apache-2.0 + self-hostable + no-managed-dep is textbook hyperscaler-lens-pass. Rejecting Tekton (avoid a second pipeline ecosystem alongside the already-adopted Argo family) is the correct focus call a hyperscaler architect would make. No amend/archive implication — keep, ratify.
- **ai_slop:** Very low. Accurate on Argo Workflows (CNCF Graduated, Apache-2.0), Argo Events, Argo Rollouts AnalysisTemplate; the Jenkins critique (stateful Groovy controller bolted onto k8s) is fair and specific; reconciliations with 0358/0111/0181/0366 are precise.
- **refinement:** Founder review → Proposed→Accepted; flip 0359 to Superseded with back-reference; update `MASTERPLAN.md`'s stale "Jenkins required checks" line to Argo Workflows. Ensure the generated masterplan threads 0511 → 0513/0514 (bespoke oya-ci) as the continuing chain, not a dead end.
- **consensus_needed:** Not contested on substance. One sequencing clarity for the founder: **"0511 names Argo Workflows as 'destination'; 0513/0514 name bespoke-Rust oya-ci as the destination beyond it. Confirm the canonical phrasing: Jenkins (transitory) → Argo Workflows (OSS destination) → oya-ci (bespoke destination) — so the masterplan states a 3-stage ladder, not two competing 'destinations.'"**

---

## Chunk notes

**Overall truth posture.** This is a high-quality, internally-coherent chunk — the **bespoke-substrate spine** of the corpus. ADR-0482 is the founder-locked doctrine root; 0506/0507/0508 are its identity/crypto leaves (all sharing the same Phase-1-OSS-bridge → Tier-N-bespoke shape with parity tables); 0509 is a strong, hyperscaler-correct decomposition doctrine; 0510/0511 are the disciplined SCM+CI "transitory→destination" pair. Six of seven are Accepted; 0510/0511 are Proposed drafts awaiting founder review and both should **RATIFY** (neither asserts a live capability; both correctly defer the bespoke build behind gates). No GARBAGE, no ARCHIVE candidates in this slice.

**The one real in-chunk contradiction — 0507 ↔ 0509 (and the 0476–0481 ripple).** Both Accepted, both dated 2026-05-28. ADR-0507 §2 prescribes the PR #289 per-use-case 7-crate scaffold (`oya-identity-webauthn-{domain,usecase,api,adapter-postgres,rest,grpc,app}`); ADR-0509 supersedes exactly that pattern the same day and names oya-identity as a Phase-1 *collapse* target. **0509 governs.** The masterplan generator MUST take the 0509 single-crate-per-service layout and treat 0507's (and 0476–0481's) embedded scaffold instructions as stale. This is the single most important mechanical reconciliation in the chunk and a concrete generator-hazard if propagated naively.

**The doctrinal tension worth a founder ruling — 0482 maximalism vs 0510 honest-cost.** Same author, one day apart. ADR-0482: "anything is acceptable cost for bespoke … timeline is the only constraint." ADR-0510: ground-truths the repo, finds bespoke-VCS "buys nothing today," and *declines to build it*. 0510's numeric-forcing-function discipline is the implicit bound on 0482's maximalism — but 0482 doesn't state that bound, and a generated masterplan reading 0482 literally would over-commit. **Recommend the masterplan encode 0482's roadmap *with* 0510's forcing-function gate as a binding cross-doctrine invariant.** (Founder question captured under 0482.)

**Tier-vocabulary drift (0482 self-retraction).** 0482's own amendment declares "Tier-4 RETRACTED — kubers + Phase B kernel cover this; no separate Tier-4 needed," yet 0482's Tier-4 table and 0506 ("oya-crypto is the Tier-4 destination") and 0508 (OpenTitan "Tier-4") all still reference Tier-4 as live. The generator must pick ONE tier vocabulary (kubers-Phase-gated vs Tier-N) and normalize the leaf ADRs to it.

**Hyperscaler-realism gradient (most→least defensible).**
- **0509** (single-crate-per-service) and **0506** (aws-lc-rs bridge) — strongly aligned; these are *exactly* what FAANG does.
- **0511** (Argo Workflows) and **0510 method** (numeric-trigger-gated bespoke-VCS) — aligned and, in 0510's case, exemplary discipline.
- **0507 RP-ownership** — defensible (small, spec-bounded, multi-tenant-differentiating).
- **0482 totality** and **0508 silicon** — most questionable: blanket "own everything, cost-no-object" (0482) and bespoke authenticator *silicon* + own manufacturing line (0508 Tier-3/4) are Titan/Pluton-class capex no hyperscaler did greenfield-solo or all-at-once. Keep as recorded aspiration; the masterplan must mark these capability-absent and budget-gated, not near-horizon commitments.

**Cross-side (LINUX) collisions:** none direct in this slice — LINUX 0001–0026 has no crypto-provider, WebAuthn, service-decomposition, SCM, or CI-orchestration ADR. The relevant cross-side pull is **scope/breadth (keystone fault-line #5 + #4)**: 0482/0508/0510 are the SOURCE-side articulation of "own the substrate eventually," which *narrows* the LINUX-vs-SOURCE gap (SOURCE now also wants own-kernel/own-VCS/own-crypto) while sharpening the *trigger-threshold* disagreement — LINUX leans OWN_DAY0, SOURCE (esp. 0510) leans own-when-a-measured-forcing-function-fires. The keystone's read ("both share the 'own when proven' ratchet; the dispute is the trigger") is confirmed and 0510 is the cleanest SOURCE-side statement of the "proven/forced" trigger.

**Forge three-way (keystone fault-line #4) reaffirmed:** founder's GitHub `jason931225/oyatie` directive conflicts with even 0510's *transitory* Forgejo canon and with the bespoke-VCS destination. Surfaced under 0510, not resolved. Needs the founder ruling on operationally-canonical-host-TODAY.

**Numbering:** both 0510 and 0511 carry explicit `numbering_note`s confirming keystone §6.3 — `decisions.json next_adr` (records 0392) is STALE; dev carries through 0509; these self-allocate 0510/0511 above it. Trust the on-disk corpus, never `next_adr`.

**On-merge mechanical actions this chunk implies:** (a) 0359 → Superseded, `superseded_by:[ADR-0511]`; (b) 0363 → `amended_by:[ADR-0510]`; (c) 0507 + 0476–0481 scaffold language → amend to 0509 single-crate layout; (d) `MASTERPLAN.md` "Jenkins required checks" line → Argo Workflows; (e) normalize Tier-4 vocabulary across 0482/0506/0508.
