# ADR Audit — SOURCE, Chunk 3

- **Side:** SOURCE (`~/Developer/source`, GitHub `jason931225/oyatie`)
- **Chunk:** 3
- **Slice range requested:** lines 15–21 of sorted `ADR-*.md` → **ADR-0016 … ADR-0022**
- **ADRs actually reviewed (7):** ADR-0016 (wave/plane framework), ADR-0017 (brand naming + repo layout), ADR-0018 (glossary canon), ADR-0019 (doc catalog + update protocol), ADR-0020 (foundry multi-provider adapter), ADR-0021 (foundry capability registry + MCP gateway), ADR-0022 (autonomy-ceiling runtime enforcement)
- **Baseline:** keystone map `_map/canonical-posture-and-supersession-map.md` (read first). Cross-checked against live `source/docs/GLOSSARY.md`, `MASTERPLAN.md`, ADR-0335/0347/0255/0056 on disk.

**Chunk-level headline:** This slice is the **Foundation-pack "doctrine/process" cluster** (0016–0019) plus the **original Foundry-runtime cluster** (0020–0022). Two systemic problems dominate:
1. **Retired-vocabulary leakage.** 0016 is built entirely on the retired `W-Foundry-Preview` wave name + still-anachronistic content; 0020/0021/0022 are entirely `oya-foundry-*` and "Foundry"-branded — and **foundry the brand is RETIRED** (ADR-0335/0347; GLOSSARY §1032). The *decisions* in 0020/0021/0022 mostly survive (they migrate to `intelligence`), but every crate name + the µservice owner are stale.
2. **Zero masterplan binding.** All 7 carry only `id/status/doc_status` front-matter — **no `planning_impact`, no `masterplan_ref`, no `deliverables/milestone`.** They are exactly the un-bound ADRs the planning-ssot-drift-prevention gate measures at 8.8%. For the founder's "masterplan = SSOT" goal, every one of these needs backfill front-matter regardless of authored-vs-generated resolution.

---

### ADR-0016 — Wave and plane integration framework (descriptive wave names; preview/stable/GA; no M0–M3)

- **decision_atom:** Replace date-bound `M0..M3 / minimum-shippable-tier` milestone vocabulary with descriptive, gate-defined **Wave names** (W-Foundation … W-Region-Fan-Out) and industry-standard surface-maturity labels **`preview / stable / GA`**, where each wave is an auditable gate (all in-scope ADRs Accepted + CI lanes green + evidence pack emitted + council sign-off), not a date.
- **current_status:** `proposed` (front-matter) / body says "Proposed". Dated 2026-05-09.
- **disposition:** AMEND. The *core* decision (kill M0–M3, adopt preview/stable/GA + gate-not-date) is TRUE and load-bearing and is itself cited by GLOSSARY as the authority retiring M0–M3 (keystone §2). But the wave-name table is drifted: **`W-Foundry-Preview` is a dead wave name** (the keystone explicitly flags "Foundry-Preview wave name itself now anachronistic post-0335"), and the W-Foundation scope list still references the retired `foundry` surface and lane names. Amend the wave table, not the principle.
- **governing:** Not superseded by any ADR. The retired-vocab dependency is governed by ADR-0335 (foundry retired) + ADR-0329 (tier-system retired — note this ADR's "no minimum-shippable-**tier**" predates and is orthogonal to 0329's tenant-tier retirement, but the word "tier" overload is a readability trap).
- **truth_flag:** PARTIAL. Principle TRUE; wave-name inventory STALE (Foundry-Preview, foundry lanes in W-Foundation).
- **in_masterplan:** PARTIAL/NO. MASTERPLAN.md does not reference ADR-0016 or the wave list by id; the masterplan uses its own `/specs/master-plan-sequencing.json`. ADR carries **no planning front-matter** → not bound. The "preview/stable/GA" labels ARE the live status vocabulary, so the *concept* is reflected, the *ADR* is not bound.
- **tensions:**
  - vs **ADR-0018** (same chunk): 0018's forbidden-terms table lists "M0/M1/M2/M3/minimum-shippable-tier → Wave names per ADR-0016" — consistent, mutually reinforcing (good).
  - vs **ADR-0335 / keystone §2:** `W-Foundry-Preview` is retired-vocab leakage; should be `W-Intelligence-Preview` (or equivalent) under cloud-intelligence naming.
  - vs **CI-churn chain (keystone §1.3):** gate criteria reference `oya-governance-*` lanes (correct post-0347) but the doc predates the Jenkins→Argo destination flip; "CI lanes hard-fail" is engine-agnostic so survives.
- **hyperscaler_challenge:** **Aligned.** Google (KEP "stages": alpha/beta/stable), AWS, Azure all use maturity tiers (preview/GA) and reject internal milestone vocab in external surfaces. Naming waves by substrate-delivered rather than by number is standard release-train practice. Verdict argues KEEP-the-principle / amend-the-table only.
- **ai_slop:** Low. The 25-row wave table is partly **fabricated-precision/over-specification** (W-Robotics-Vision-Speech, W-DataCenter-Operations as named "waves" this early reads aspirational), but it is labelled long-horizon. Minor hedging in open questions. No internal contradiction.
- **refinement:** (1) Rename `W-Foundry-Preview` → intelligence-named wave; purge `foundry` from W-Foundation scope. (2) Add `planning_impact: true` + masterplan binding front-matter. (3) Promote to Accepted or explicitly mark superseded-by a consolidated roadmap ADR — a `proposed` foundational-vocabulary ADR that is already enforced in CI is a status lie.
- **consensus_needed:** no (principle is settled); the only ruling is cosmetic wave-renaming, which folds under the foundry→intelligence rename already decided in ADR-0335.

---

### ADR-0017 — Brand naming and repo layout (Oyatie / oYa / oyatie.com; `oya-` Cargo prefix; GitHub slug `jason931225/oyatie` retained)

- **decision_atom:** Standardize the product brand as **Oyatie** (logo `oYa`, domain `oyatie.com`, npm `@oyatie/`, Cargo prefix `oya-<microservice>-(<bc>-)?<layer>` per BNF v4.1/ADR-0056), sweep all customer-visible surfaces via 17 bounded rename batches, while **retaining the repo path / GitHub slug `jason931225/oyatie`** because filesystem-migration cost exceeds brand purity.
- **current_status:** `accepted`. Dated 2026-05-09, rewritten 2026-05-13. ADR-0056 (BNF v4.1) confirmed to exist on disk.
- **disposition:** AMEND (sound but the forge claim is a load-bearing tension surface). The brand decision (Oyatie/oYa/oya-) is TRUE and current. The **`jason931225/oyatie` GitHub slug retention is the single most founder-relevant line in this chunk** — it is the *only* source ADR that hard-codes GitHub as the canonical repo home, and it directly matches the founder's migration directive (GitHub). But it sits in unresolved tension with source canon (ADR-0363/0510 say Forgejo is the canonical/transitory host, bespoke-VCS is the destination, GitHub is mirror). Amend to cross-reference the forge fault-line, not silently assert GitHub.
- **governing:** Not superseded. Forge posture governed by **ADR-0363 / ADR-0510 / ADR-0374 / ADR-0387** (keystone §5). This ADR's "GitHub slug" line is *unreconciled* with those.
- **truth_flag:** PARTIAL. Brand rules TRUE; "GitHub slug retained" is TRUE-as-founder-directive but **WRONG-as-source-canon** (source canon = Forgejo-canonical/GitHub-mirror). This is the crux forge contradiction, surfaced not resolved.
- **in_masterplan:** PARTIAL. "Oyatie" brand is pervasive in MASTERPLAN (it's the product name), so the brand decision is reflected de facto; but no ADR-id binding and **no planning front-matter**. The repo-slug decision is NOT reconciled with the masterplan's forge posture.
- **tensions:**
  - **FORGE (keystone §5, founder fault-line):** `jason931225/oyatie` GitHub-retained here vs **Forgejo-canonical** (ADR-0363/0374/0387) vs **bespoke-monorepo-VCS-as-destination** (ADR-0510). Three-way. This ADR is the GitHub vertex. Founder directive agrees with this ADR; source canon does not. **Highest-value cross-chunk tension in slice.**
  - vs **ADR-0018:** brand enforcement delegated to `oya-check-glossary` lane (0017 calls it `oya-check-glossary`; 0018/0019 call the lane `oya-governance-glossary`) — **naming drift between two Accepted ADRs in the same pack** (see ai_slop).
  - vs **ADR-0335/0347:** "Capability namespaces (e.g. `oya.foundry.capability.invoke`)" example uses retired foundry brand — should be intelligence (note: the Cedar principal namespace `oyatie.foundry.*` legitimately persists per ADR-0247, but a *capability* namespace example is leakage).
- **hyperscaler_challenge:** **Questionable** on the repo-slug specifically. Google/AWS/Azure would NOT hard-code a personal-account GitHub slug (`jason931225/...`) as the canonical home of a monorepo they intend to self-host/own — they run self-hosted SCM (Google Piper/Gerrit, Amazon internal, Azure DevOps) exactly as ADR-0363/0510 propose. The brand-sweep mechanics are aligned (bounded batches = standard large-repo rename hygiene). Verdict: argues AMEND to align the slug line with the forge ADRs / flag for founder.
- **ai_slop:** Low-moderate. **Internal contradiction:** lane named `oya-check-glossary` here vs `oya-governance-glossary` in ADR-0018/0019 — a real inconsistency, not filler. The "6,560-touchpoint mega-PR" figure is **fabricated precision** (an exact count with no cited derivation). Otherwise concrete.
- **refinement:** (1) Reconcile the GitHub-slug line with ADR-0363/0510 or explicitly mark it founder-overridden. (2) Unify lane name to `oya-governance-glossary`. (3) Replace `oya.foundry.capability.invoke` example with intelligence namespace. (4) Add planning front-matter.
- **consensus_needed:** **YES.** Crisp question: *"Is the canonical repo home `jason931225/oyatie` on GitHub (founder migration directive + ADR-0017) or Forgejo-self-hosted-now / bespoke-VCS-eventually (ADR-0363/0510)? Pick one as masterplan truth and mark the others mirror/transitory."*

---

### ADR-0018 — Glossary and terminology canon (industry-aligned vocab; KR-EN parity; `oya-check-glossary` CI lane)

- **decision_atom:** Establish a single enforceable glossary canon — five vocabulary rules (industry-term-wins; Oyatie-specific-needs-analog; KR/EN co-equal; renames carry `Replaces:`; stale terms removed not flagged) — plus a forbidden-token CI lane that hard-fails brand/architecture drift across all docs, ADRs, and source.
- **current_status:** `accepted`. Dated 2026-05-09, rewritten 2026-05-13.
- **disposition:** AMEND (the *mechanism* KEEP; the *canonical glossary table embedded in the ADR* is STALE and now contradicts the live GLOSSARY.md). The five-rules + CI-lane decision is TRUE, current, and exactly the kind of drift-prevention the founder wants. **But the in-ADR glossary table defines "Foundry" as a LIVE canonical term** ("Oyatie's AI agent runtime + control plane") — while the live `GLOSSARY.md §1032` now reads **"Foundry (RETIRED) 2026-05-21 per ADR-0335."** The glossary-canon ADR has itself drifted out of sync with the glossary it governs. Per this ADR's own Rule 5 ("stale terms removed from canonical usage"), the embedded table must be updated.
- **governing:** Not superseded as a process ADR. Its embedded "Foundry" entry is overridden by **ADR-0335 / ADR-0255** (foundry→intelligence) and **ADR-0347** (lane rename foundry→governance). The lane it names (`oya-check-glossary` / `oya-governance-glossary`) is governed by ADR-0347's `oya-governance-*` prefix mandate.
- **truth_flag:** PARTIAL → leaning STALE on the table. Process TRUE; embedded canonical glossary STALE (Foundry-as-live, and it predates Connect/social/intelligence consolidations). Self-referential failure: the anti-drift ADR is drifted.
- **in_masterplan:** PARTIAL. Glossary discipline is a masterplan-supporting invariant; not id-bound; **no planning front-matter.** The live authority is GLOSSARY.md, which has moved past this ADR's snapshot.
- **tensions:**
  - **vs live GLOSSARY.md §1032 (foundry RETIRED) — direct contradiction.** The ADR table says Foundry is canonical; the glossary it governs says Foundry is retired. Highest-severity internal tension in chunk.
  - vs **ADR-0017:** lane-name drift (`oya-check-glossary` vs the `oya-governance-glossary` used in 0018's own body line 86 / ADR-0019) — even *within* this ADR the CI lane is called `oya-check-glossary` in the title/related but `oya-governance-glossary` in the decision body. **Intra-ADR inconsistency.**
  - vs **ADR-0347:** any `oya-check-*` / `oya-foundry-*` lane prefix is retired-vocab; governance prefix is mandated.
- **hyperscaler_challenge:** **Aligned.** A CI-enforced terminology lane + KR/EN parity table is exactly the docs-as-code governance Google/Microsoft run (style/terminology linters gating merge). The decision is hyperscaler-grade; only the stale content is the problem. Argues AMEND (refresh table), not archive.
- **ai_slop:** Low. The one real issue is the **internal lane-name inconsistency** (`oya-check-glossary` vs `oya-governance-glossary` in the same file) and the **stale Foundry row** — both substantive, not filler.
- **refinement:** (1) Regenerate the embedded glossary table from live GLOSSARY.md or delete the embedded table and reference GLOSSARY.md as authority (avoids dual-source drift — strongly recommended given this ADR's whole thesis is single-source). (2) Unify lane name to `oya-governance-glossary`. (3) Add the now-retired terms (foundry, tier-system, Kafka, Redis, M0–M3) to the forbidden list. (4) Add planning front-matter.
- **consensus_needed:** no (the fix is mechanical: dedupe the glossary source). One sub-question worth confirming: should ADRs *embed* glossary snapshots at all, or always reference the live GLOSSARY (the consolidation-vs-authority design choice applies in miniature here).

---

### ADR-0019 — Doc catalog and update protocol (per-doc owner/trigger/cadence/deps/validation; 5-stage lifecycle; agent-authoring policy; machine-readable mirror)

- **decision_atom:** Treat every consolidated doc as a cataloged, governed artifact with an explicit owner / trigger-events / cadence / dependent-docs / validation-lane row, a five-stage lifecycle (pre-flight→authoring→validation→review→publish), a three-class agent-authoring policy (agent-direct-write for catalog-validated additions; propose-only for PRD-class; human-only for constitution-class), and a machine-readable JSON mirror per doc.
- **current_status:** `proposed`. Dated 2026-05-09.
- **disposition:** AMEND (and arguably the **most masterplan-relevant ADR in the chunk** — this IS the doc-as-SSOT machinery). The decision is sound and directly anticipates the planning-ssot-consolidation/drift-prevention designs (machine-readable mirrors, validation lanes, owner/trigger/cadence). It is stale only in surface detail: examples reference `foundry` as a secondary owner and `oya-governance-*` lanes (mostly correct post-0347). Should be AMENDED and promoted, then reconciled with planning-ssot-consolidation.md (which generalizes this same idea to ADR front-matter as the generative SSOT).
- **governing:** Not superseded. **Overlaps/should-merge with** `docs/ideas/planning-ssot-consolidation.md` + `planning-ssot-drift-prevention.md` (keystone §4) — this ADR is the doc-catalog precursor to the masterplan-as-generated/authored design. Foundry owner refs governed by ADR-0335.
- **truth_flag:** TRUE (with minor stale refs). The protocol is real and consistent with current direction.
- **in_masterplan:** PARTIAL. This ADR *describes the catalog mechanism* that would bind docs to the masterplan, but the ADR itself carries **no `masterplan_ref` / planning front-matter** — ironic, since it is the freshness-governance ADR. The `machine-readable/catalog.json` it mandates is conceptually the ancestor of `/specs/*.json`.
- **tensions:**
  - vs **planning-ssot-consolidation.md (OPEN founder question, keystone §4):** consolidation.md proposes ADR front-matter as the *generative* SSOT and masterplan as *generated*; this ADR proposes a *separately authored* DOC-CATALOG.md + per-doc mirrors. Both are "machine-readable mirror" designs but at different altitudes — needs reconciliation under the authored-vs-generated ruling. **Cross-chunk, founder-level.**
  - vs **ADR-0018:** consistent (0019 lists the glossary lane as a per-doc validation). Uses `oya-governance-glossary` (the correct post-0347 name) — confirming 0017/0018's `oya-check-glossary` is the drifted spelling.
  - "secondary_owners: [foundry, ...]" example = retired-vocab leakage (→ intelligence/governance).
- **hyperscaler_challenge:** **Aligned.** Docs-as-code with owners, CI validation, freshness SLAs, and machine-readable mirrors is precisely Google/AWS/Microsoft internal practice (e.g. Google's docs-as-code, OWNERS files, KEP metadata). The agent-authoring tiering (direct-write/propose/human-only) is a forward-looking, defensible governance pattern. Argues AMEND+promote, not archive.
- **ai_slop:** Low. Six open questions are reasonable (not hedging-as-filler). Minor redundancy with ADR-0018/0011 on the cohesion lane. No fabrication.
- **refinement:** (1) Reconcile with planning-ssot-consolidation.md — decide whether DOC-CATALOG.md is authored or generated from ADR front-matter (same open founder question). (2) Add planning front-matter to itself (eat-own-dogfood). (3) Replace `foundry` owner refs with intelligence/governance. (4) Promote from Proposed.
- **consensus_needed:** **YES** (this is one face of the masterplan open question). Crisp question: *"Is the per-doc catalog (ADR-0019) the authored SSOT for doc governance, or is it generated from ADR front-matter per planning-ssot-consolidation.md? Resolve before either is enforced, or they will fight at the gate."*

---

### ADR-0020 — Foundry multi-provider adapter model (`ProviderAdapter` trait, ProviderAuth, capability-level routing)

- **decision_atom:** Normalize every model-provider integration behind a single sealed `ProviderAdapter` trait (Anthropic/OpenAI/Gemini × {API, subscription}) with runtime-resolved per-tenant/per-capability routing, invocation-boundary failover, vault-backed secret/session resolution, and hard pre-flight per-tenant cost ceilings — so capability authors write provider-agnostic code and the runtime (not the author) chooses the route.
- **current_status:** `proposed`. Dated 2026-05-09. Owner `foundry` (RETIRED team).
- **disposition:** AMEND (decision survives; namespace + owner retired). The architecture (sealed adapter port + runtime router + failover + cost-ceiling pre-flight) is TRUE, sound, and consistent with current AI-substrate direction. But it is **wall-to-wall retired-vocab**: crates `oya-foundry-adapter-kernel/-router/-session-vault`, owner `foundry`, "Foundry is the force-multiplier axis." Per GLOSSARY §1032, these `oya-foundry-*` crates are explicitly **transition debt** (ADR-0335 D-37..D-50); new code lands under `oya-intelligence-*`. Amend: re-home to intelligence (Layer A: "model routing, providers, ... credential resolver").
- **governing:** Not formally `superseded_by` any ADR (stale front-matter — same drift class as ADR-0136 in keystone §1.3). De-facto governed by **ADR-0335** (foundry→intelligence absorption) + **ADR-0255** (intelligence Layer A explicitly owns "model routing, providers, ... credential resolver" = exactly this ADR's scope). Secret resolution it pins to ADR-0043 SecretProvider; live glossary says Cloud Secrets resolves provider credentials — consistent target.
- **truth_flag:** PARTIAL. Decision TRUE; branding/ownership/crate-paths STALE (foundry retired). Note **internal ref bug:** body cites "ADR-0043 secrets-management" for the SecretProvider but also says "OpenBao-backed per the platform secret contract" — verify ADR-0043 is the live secrets ADR (not re-checked this pass; flag).
- **in_masterplan:** NO. No planning front-matter; not id-referenced in MASTERPLAN.md. The *capability* (multi-provider routing) lives in the intelligence µservice manifest, but this ADR is unbound.
- **tensions:**
  - vs **ADR-0335 / ADR-0255 / GLOSSARY §1032:** entire foundry namespace retired → intelligence. Direct retired-vocab conflict (decision keeps, naming dies).
  - vs **ADR-0021/0022 (same chunk):** tightly coupled — 0020 router is consumed by 0021 registry and gated by 0022 ceiling. The three must be amended together (rename in lockstep) or cross-refs break.
  - vs **build-vs-buy (ADR-0014, keystone):** rejects LangChain/LangGraph as runtime ("in-house obligatory") — consistent with own-the-substrate posture; no conflict, but worth noting it's an own-vs-buy assertion that should carry build-vs-buy evidence.
- **hyperscaler_challenge:** **Aligned** on architecture. A provider-agnostic adapter + router + failover + pre-flight cost cap is exactly what Azure AI Foundry / AWS Bedrock / Google Vertex model-router do (Bedrock's unified API + intelligent prompt routing; Vertex model garden). The **subscription-auth (Claude Pro/ChatGPT Plus/Gemini Advanced) adapters are MISALIGNED/questionable** — hyperscalers never drive consumer subscription seats programmatically at production scale (the ADR itself admits "subscription terms forbid heavy programmatic use"). That sub-decision argues for scoping subscription-mode to dev-loop only or archiving it. Net: AMEND.
- **ai_slop:** Low-moderate. The Rust trait/router code blocks are illustrative-but-plausible (not fabricated). **Questionable real-world legitimacy:** building production adapters on consumer-subscription session-token vaults with "challenge detection" is operationally fragile and arguably ToS-violating — borders on fabricated-capability. The "Resolved (this revision)" section is substantive, not filler.
- **refinement:** (1) Re-home all crates `oya-foundry-adapter-*` → `oya-intelligence-adapter-*`; change owner `foundry`→`intelligence`. (2) Scope or drop subscription-auth adapters (ToS + fragility). (3) Verify ADR-0043 secrets ref. (4) Add `superseded_by`/`amended_by: ADR-0335` to front-matter to stop the drift. (5) Add planning front-matter.
- **consensus_needed:** **YES** (one ruling covers 0020/0021/0022). Crisp question: *"Confirm the Foundry-runtime ADRs (0020–0025) are absorbed into the `intelligence` µservice per ADR-0335 — do we (a) re-author them as `oya-intelligence-*` ADRs, or (b) mark them historical and let intelligence's manifest carry the live decision? And is consumer-subscription-auth (Claude Pro etc.) a sanctioned production path or dev-loop-only?"*

---

### ADR-0021 — Foundry capability registry and MCP gateway (`Capability` schema, MCP discovery, per-tenant endpoint)

- **decision_atom:** Define a single canonical typed `Capability` record (input/output schema, required autonomy tier, data classes, cost profile, sunset policy, audit topic) sourced from `registry/catalog/*.yaml`, and serve it through an **MCP-compatible per-tenant gateway** (OAuth device flow, per-tenant endpoint with structural cross-tenant refusal, audit-chain emission per invocation) so any MCP client (Claude Desktop/Cursor/Cline/etc.) can discover-and-invoke under the tenant trust envelope.
- **current_status:** `proposed`. Dated 2026-05-09. Owner `foundry` (RETIRED).
- **disposition:** AMEND (decision survives; namespace retired). The capability-registry-as-SSOT + MCP discovery + per-tenant isolation-at-protocol decision is TRUE, current, and strategically aligned (MCP is now the de-facto agent-tool standard). Stale only in branding: `oya-foundry-capability-kernel`, `oya-foundry-mcp-gateway`, `mcp.foundry.<region>.oyatie.<tld>`, owner `foundry`. Re-home to intelligence per ADR-0335.
- **governing:** Not formally superseded (stale front-matter). De-facto governed by **ADR-0335 / ADR-0255** (intelligence Layer A owns capability surface). The MCP-gateway domain endpoint should become `mcp.<tenant>.oyatie.com` per ADR-0017's brand table (note ADR-0017 says `mcp.<tenant>.oyatie.com` "Per Foundry MCP server" — itself retired-branded, another 0017↔0021 cross-ref to fix).
- **truth_flag:** PARTIAL. Decision TRUE; branding STALE. MCP-as-discovery is, if anything, *more* validated since 2026-05-09 (industry adoption), so the strategic bet has aged well.
- **in_masterplan:** NO. No planning front-matter; not id-bound. Capability/MCP surface lives in intelligence manifest, unbound at ADR level.
- **tensions:**
  - vs **ADR-0335:** foundry namespace retired (same as 0020).
  - vs **ADR-0017:** `mcp.<tenant>.oyatie.com` (0017) vs `mcp.foundry.<region>.oyatie.<tld>/tenants/<tenant-id>` (0021) — **two different canonical MCP endpoint shapes across the pack.** Must reconcile.
  - vs **ADR-0020/0022:** consumes 0020's route chain, gated by 0022's ceiling — lockstep rename dependency.
  - "Per-tenant endpoint multiplies DNS+TLS" — operational tension with the cost-ceiling thesis; consistent but worth a scaling note.
- **hyperscaler_challenge:** **Aligned.** A central typed capability/tool registry + standard discovery protocol + per-tenant isolated endpoint is exactly the tool-registry + per-tenant-endpoint pattern (Azure AI Foundry agent tool catalog, AWS Bedrock Agents action groups, Anthropic MCP). Betting on MCP rather than bespoke RPC is the choice a hyperscaler now makes. Strongly aligned; argues AMEND-not-archive.
- **ai_slop:** Low. Concrete, well-scoped. The dual-audience description / server-level-prompts detail is real MCP surface, not filler. Endpoint-shape inconsistency with 0017 is the one real defect.
- **refinement:** (1) Rename crates/owner foundry→intelligence; unify MCP endpoint shape with ADR-0017. (2) Add `amended_by: ADR-0335` to front-matter. (3) Add planning front-matter. (4) Confirm catalog path `registry/catalog/oya-foundry-capability-*.yaml` migrates to `oya-intelligence-capability-*`.
- **consensus_needed:** covered by ADR-0020's consensus question (foundry→intelligence re-authoring) + the MCP-endpoint-shape reconciliation (lower-stakes, mechanical).

---

### ADR-0022 — Autonomy ceiling: runtime enforcement via Cedar at every capability invocation

- **decision_atom:** Enforce a per-invocation **autonomy ceiling** = `min(tenant_configured, capability_min, vertical_pack_cap, subject_class_cap)` via a **Cedar** policy gate on the runtime hot path of *every* capability invocation, with deterministic allow/lowered/deny, mandatory audit-chain emission for all decisions, bounded M-of-N break-glass (2-of-3 standard / 3-of-5 catastrophic) logged in the foundation-bypass ledger, and structural tenant-class overrides (healthcare/fintech forced ≤T2; `ads.* bid/budget` default T1).
- **current_status:** `proposed`. Dated 2026-05-09. Owner `foundry` (RETIRED team).
- **disposition:** AMEND (decision is LIVE and load-bearing; only the foundry namespace is retired). **This is the strongest-surviving ADR in the chunk:** the live `GLOSSARY.md` lines 244–245 cite **ADR-0022 as the current authority** for "Autonomy ceiling" and "Persona tier (T1..T4)". The decision is canonical TODAY. Amend only the `oya-foundry-policy-*` crate names + owner → intelligence/governance, and align with Cedar canon (ADR-0243/0246 universal gate + ADR-0379 admission — keystone §3).
- **governing:** Not superseded. **Reinforced** by GLOSSARY (current authority) and by Cedar canon **ADR-0243 (Cedar universal gate) / ADR-0246 (policy substrate promotion) / ADR-0007 (Cedar authz + persona-tier origin, confirmed on disk)**. Namespace retirement governed by ADR-0335; the *policy decision* is explicitly NOT weakened by retirement (GLOSSARY: "self-modification execution remains under `oyatie.foundry.*` Cedar principal namespace per ADR-0247").
- **truth_flag:** TRUE (namespace STALE). The cleanest survive-in-substance ADR of the seven.
- **in_masterplan:** PARTIAL. The autonomy-ceiling concept IS reflected in live GLOSSARY canon (so it's effectively masterplan-adjacent truth), but the ADR carries **no planning front-matter** and is not id-bound to MASTERPLAN.md. Should be one of the first ADRs backfilled given it's already cited as live authority.
- **tensions:**
  - vs **keystone §2 retired tier-system (ADR-0329):** the word "tier" — autonomy T1–T4 (this ADR, LIVE) is a *different axis* from the retired tenant "tier-system." Must NOT be conflated; the keystone explicitly warns this. The ADR's own use of "tenant-class overrides (Healthcare/Fintech)" is consistent with ADR-0329's `tenant_class` (demo_trial|paid) replacement — good alignment, but the ADR uses class enums (Healthcare/Fintech/PublicSector) that are a *third* notion (regulatory class) — needs a glossary note distinguishing tenant-class (billing) from regulatory-class (compliance) from autonomy-tier (policy).
  - vs **Cedar canon (ADR-0243/0246/0379):** consistent — "Cedar is the policy substrate; we do not maintain a parallel engine" matches universal-gate canon. No conflict; rather a confirming instance.
  - vs **LINUX ADR-0021 (cross-side, keystone §5.2):** LINUX designs an owned compile-to-Rust *Cedar-compatible* policy language with autonomy-tier T1–T4 as first-class — this source ADR is the Cedar-reuse position LINUX positions itself as the owned successor to. Direct own-vs-reuse tension on the same autonomy-ceiling concept.
  - vs **ADR-0020/0021:** lockstep namespace rename.
- **hyperscaler_challenge:** **Aligned.** A centralized, outside-the-author, hot-path, fully-audited authorization gate with break-glass + structural overrides is textbook hyperscaler security (AWS IAM/SCP deny-by-default + CloudTrail; Google IAM + binary authorization; Azure Policy). Cedar is itself AWS-authored. M-of-N break-glass with auto-expiry is standard privileged-access management. Strongly aligned; argues KEEP/amend-naming-only.
- **ai_slop:** Very low. Concrete, defensible, the code is illustrative-not-fabricated. The 2-of-3 / 3-of-5 specifics are reasonable, not fabricated-precision. No internal contradiction.
- **refinement:** (1) Rename `oya-foundry-policy-kernel/-app/-domain` → `oya-intelligence-policy-*` or `oya-governance-policy-*` (decide which — autonomy enforcement straddles intelligence-runtime and governance-gates). (2) Add a glossary note disambiguating autonomy-tier vs tenant-class vs regulatory-class. (3) Cross-ref ADR-0243/0246/0379 explicitly. (4) **Backfill planning front-matter first among the chunk** — it's already live authority. (5) Promote Proposed→Accepted to match GLOSSARY's treatment of it as authority.
- **consensus_needed:** no on the decision (it's live canon). One sub-ruling: does autonomy-ceiling enforcement live under `intelligence` (runtime) or `governance` (gates) after the foundry split? — folds into ADR-0020's foundry→intelligence consensus question.

---

## Chunk notes for synthesis

**Cluster shape.** This slice splits cleanly into two clusters with opposite health:
- **0016–0019 (Foundation doctrine/process):** all *process/governance* ADRs (wave vocab, brand, glossary, doc-catalog). Decisions mostly TRUE; defects are stale embedded tables (0016 wave list, 0018 glossary table) and unbound front-matter. These are the **masterplan-machinery ADRs** — 0018/0019 in particular are the in-corpus ancestors of `planning-ssot-consolidation.md`. They should be the *first* to get planning front-matter.
- **0020–0022 (Foundry runtime):** all three are technically sound but **100% retired-brand** (`oya-foundry-*`, owner `foundry`). The *decisions* survive into `intelligence` (Layer A model-routing/capability/eval per ADR-0255) — they are AMEND (re-home), not ARCHIVE. **They must be renamed in lockstep** (0020 router → 0021 registry → 0022 ceiling form one call-graph). ADR-0022 is the strongest: live GLOSSARY cites it as current authority.

**Systemic patterns across the chunk:**
1. **Zero planning front-matter (7/7).** Confirms the keystone's 8.8%-binding finding empirically. For the founder's "masterplan = SSOT" goal, every ADR here needs `planning_impact` + `masterplan_ref` backfill — independent of the authored-vs-generated ruling. **Recommend ADR-0022 first (already live authority), then 0019/0018 (the SSOT-machinery), then the rest.**
2. **Retired-vocab leakage is pervasive, not isolated.** `W-Foundry-Preview` (0016), `oya.foundry.capability.invoke` example (0017), Foundry-as-live-glossary-term (0018), `foundry` secondary-owner (0019), full `oya-foundry-*` namespace (0020/0021/0022). The keystone's "hundreds of `oya-foundry-*` strings persist" is borne out in this single 7-ADR window. None of these are *wrong decisions* — they are **brand-residue** (MFL-0002/0003 class) and the fix is mechanical rename, EXCEPT the legitimately-persisting `oyatie.foundry.*` Cedar principal namespace (ADR-0247) which must NOT be renamed.
3. **Status-honesty gap.** 0016/0019/0020/0021/0022 are `Proposed` but are treated as live (0016's vocab is CI-enforced; 0022 is cited as GLOSSARY authority). "Proposed but enforced" is a status lie that should be reconciled — promote to Accepted or mark superseded/historical.
4. **Intra-pack naming drift.** The glossary CI lane is `oya-check-glossary` (0017 title, 0018 title/related) vs `oya-governance-glossary` (0018 body, 0019) — and the MCP endpoint is `mcp.<tenant>.oyatie.com` (0017) vs `mcp.foundry.<region>.oyatie.<tld>/...` (0021). These Foundation-pack ADRs were authored together but are internally inconsistent.

**Cross-chunk / cross-side tensions to escalate:**
- **FORGE (highest founder-relevance):** ADR-0017 is the GitHub vertex of the three-way forge fault-line (keystone §5). `jason931225/oyatie` GitHub-retained here vs Forgejo-canonical (0363/0374/0387) vs bespoke-VCS-destination (0510). Founder directive = GitHub = agrees with 0017; source canon disagrees. **Needs founder ruling.**
- **MASTERPLAN authored-vs-generated:** ADR-0019 (doc-catalog-as-authored-protocol) vs `planning-ssot-consolidation.md` (masterplan-generated-from-ADR-front-matter). Two designs for the same SSOT machinery; this chunk surfaces one of them as a Proposed ADR. **Folds into the open founder question (keystone §4).**
- **FOUNDRY→INTELLIGENCE re-authoring:** 0020/0021/0022 (+0023/0024/0025 in the adjacent chunk) need a single ruling on whether to re-author as `oya-intelligence-*` ADRs or mark historical-and-defer-to-manifest. The decisions are TRUE; only the authoring vehicle is in question.
- **AUTONOMY own-vs-reuse (cross-side):** SOURCE ADR-0022 (Cedar-reuse, live canon) vs LINUX ADR-0021 (owned Cedar-compatible compile-to-Rust policy). Same autonomy-ceiling concept; the pilot positions as owned successor (keystone §5.2). Surface, do not resolve.
- **Subscription-auth legitimacy (hyperscaler challenge):** ADR-0020's consumer-subscription adapters (Claude Pro/ChatGPT Plus/Gemini Advanced driven programmatically) are the one decision in the chunk a hyperscaler would *reject* on ToS/fragility grounds — flag for scope-down or archive of that sub-decision.
