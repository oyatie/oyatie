# ADR Audit — source-31

- **side:** SOURCE (`~/Developer/source`, GitHub `jason931225/oyatie`)
- **chunk:** source-31 (slice lines 211–217 of `ls -1 docs/decisions/ADR-*.md | sort`)
- **range:** ADR-0246 … ADR-0252
- **ADRs reviewed:** 7 (0246, 0247, 0248, 0249, 0250, 0251, 0252)
- **auditor:** ADR AUDITOR (coverage backfill)
- **date:** 2026-06-06
- **keystone context:** ALL seven are members of the `2026-05-20-foundational-doctrine` keystone bundle (ADR-0242 → ADR-0255, 14 ADRs landing as one multispectrum-reviewed PR). Bundle doctrine: "partial acceptance is rejected" — the keystones are mutually-reinforcing. Every one is `status: Proposed`, so every one carries a mandatory RATIFY/DROP disposition below. None are themselves superseded on disk (`superseded_by: []` on all seven).

---

### ADR-0246 — Policy-Engine Substrate Promotion

- **decision_atom:** Promote the Cedar `cedar-fragment-coverage` bounded context out of the `ontology` µservice into its own peer **substrate** µservice `microservices/policy-engine/` (per-cell evaluator pool, gRPC SDK, T1 DR tier, 47 crates across 8 BCs), because ADR-0243 elevated Cedar from app-authz to a universal hot-path gate that every state-changing action consults.
- **domain:** authz-policy (cross-cutting: isolation-runtime / orchestration-scheduling for the per-cell deployment topology)
- **current_status:** Proposed
- **disposition:** KEEP (as RATIFIED) — structurally correct and load-bearing for the whole keystone bundle (ADR-0247/0250/0251 all name `policy-engine` as a prerequisite). One AMEND-on-merge note: front-matter `amends: ADR-0150, ADR-0183` while the keystone map records ADR-0183 as **Superseded by ADR-0379** (Kubewarden default admission). The Cedar-app-authz separation principle survives 0379, so the amend reference is still coherent, but the citation should be refreshed to point at the live governing ADR-0379 rather than the superseded 0183.
- **proposed_resolution:** **RATIFY** — this is the substrate that the canonical posture (§3 "Policy/authz = Cedar universal gate via ADR-0243/0246") is already built on; dropping it would un-anchor ADR-0243's universal-gate doctrine.
- **governing:** n/a (not archived). On merge, refresh the ADR-0183 reference to ADR-0379.
- **truth_flag:** TRUE (PARTIAL on the ADR-0183 citation — stale-target risk, not wrong)
- **in_masterplan:** YES (`planning_impact: true`; enforced_by `oya gate validate policy-engine-substrate-promoted` + 4 lanes; advisory-until-substrate-lands)
- **tensions:** (1) Hard dependency on Citus/Postgres + Valkey + Kafka — collides with LINUX **ADR-0001** "eliminate PostgreSQL / own the DB engine" (keystone fault-line #1) and with the Valkey-not-Redis rename (this ADR correctly says Valkey). (2) Kafka topic `policy-engine.fragment-reload.v1` references standalone **Kafka**, which is RETIRED → Pulsar+Oxia per ADR-0377-kafka-to-pulsar (keystone §2). This is retired-vocab leakage. (3) LINUX **ADR-0021** owned typed compile-to-Rust policy language positions as the owned successor to Cedar — own-vs-reuse tension, not contradiction.
- **hyperscaler_challenge:** ALIGNED. AWS Verified Permissions (separate service since 2024-Q1), OPA-service-per-cell at Netflix/Pinterest, and Google Org Policy all centralize the policy engine as a peer service consulted per-call; the ADR cites these correctly and chose centralized-evaluator-per-cell over sidecar after the same tradeoff AWS measured. No argument for amend/archive on substance.
- **ai_slop:** Low. The 47-crate enumeration and full DDL are heavy but internally consistent; the BC table even mislabels `coverage-audit` mid-sentence (says "deferred to existing cedar-fragment-coverage crates … the BC is renamed fragment-registry") — minor authoring seam, not slop.
- **refinement:** Replace the Kafka hot-reload topic with the Pulsar/Oxia primitive (ADR-0377); repoint the ADR-0183 amend to ADR-0379.
- **consensus_needed:** "Does the policy-engine substrate's mandatory Postgres+Citus+Kafka stack get a waiver against LINUX ADR-0001 'own the DB' / the Kafka→Pulsar retirement, or does it adopt the source-canonical substrate set before ratification?"

### ADR-0247 — Self-Hosting / Self-Modification Doctrine

- **decision_atom:** Dissolve the Foundry-as-µservice framing and redistribute its six BCs into existing substrates (runtime+supervisor→workflow-engine, providers+guardrails+eval→intelligence, evidence→audit-chain, autonomy-gates→policy-engine); re-cast the internal CI/dev capability as a versioned `oyatie.foundry.*` **workflow library** that can self-modify the platform under Cedar-gated, HSM-rooted (Shamir 5-of-9) chain-of-trust, with a small explicit Tier-0 bootstrap minimum (hardware/DNS/git-host/registry) and a 5-stage bootstrap-to-steady-state sequence.
- **domain:** agentic-platform (cross-cutting: intelligence-ai; governance-process for the self-modification gate chain)
- **current_status:** Proposed
- **disposition:** KEEP (as RATIFIED), with AMEND-on-merge for vocabulary — this is the source-of-record for the Foundry dissolution and is consistent with the keystone map's Foundry-dissolution chain (ADR-0136 → 0239 → 0247 → ADR-0335 → 0347). It SUPERSEDES ADR-0136 + ADR-0136-amendment and AMENDS ADR-0239/0137/0138 per its own front-matter — all consistent with the map.
- **proposed_resolution:** **RATIFY** — without it the autonomous-masterplan goal (`feedback_autonomous_implementation_artifacts`) has no self-modification semantics, and the Foundry µservice's ~493 artefacts + 135 crates stay as live duplication.
- **governing:** n/a for archive. NOTE: ADR-0335 (later, Accepted) is the ADR that retires the **"foundry" brand** entirely → cloud-intelligence + governance. ADR-0247 predates that and still uses "Foundry"/`oyatie.foundry.*` heavily; on merge it must be read as superseded-in-vocabulary by ADR-0335/0347 even though it remains the governing decision for the *dissolution mechanics*.
- **truth_flag:** PARTIAL — TRUE on the dissolution architecture; STALE on brand vocabulary ("Foundry", `oya-foundry-*`, "retired external agent harness", "Foundry-Preview" wave) which is RETIRED downstream (keystone §2). The `microservices/foundry/` substrate-meta still appears in companion ADR-0248 §D-2 too.
- **in_masterplan:** YES (`planning_impact: true`; enforced_by `foundry-dissolution-complete`, `self-modification-permitted`, `bootstrap-tier-coherence`, etc.)
- **tensions:** (1) Tier-0 bootstrap lists "self-hosted Forgejo" + "GitHub Enterprise/Gitea" as the git host — collides with the THREE-way forge fault-line (founder directive = GitHub `jason931225/oyatie`; source canon = Forgejo-transitory → bespoke-VCS destination per ADR-0363/0510). (2) Self-modification "author new ADRs / publish workflow versions autonomously" sits directly against the founder's "ADRs = immutable append-only SSOT" doctrine — agents mutating the SSOT is a governance question. (3) Heavy reliance on `oyatie.foundry.*` retired brand. (4) `oya-ci`/Prow-shaped CI lineage (ADR-0513/0514) is downstream of this and re-frames the bootstrap CI — minor staleness.
- **hyperscaler_challenge:** ALIGNED in principle. The self-hosting precedent set (rustc stage0 bootstrap, kubeadm self-hosting, Borg-on-Borg, AWS-on-AWS, Palantir Apollo-deploys-Apollo) is real and correctly cited; mature platforms do self-host. QUESTIONABLE on *autonomous self-modification of governance artifacts* — no hyperscaler lets an automated agent author/ratify its own policy root unsupervised; the HSM M-of-N + 2-human-approval gate is the right mitigation and matches AWS KMS caller-separation. Argues for AMEND (tighten the human-in-the-loop gate language), not archive.
- **ai_slop:** Low-moderate. The 19-row workflow table + 5-stage bootstrap + Cedar fragment are dense but coherent and self-aware (explicitly resolves the chicken-and-egg). The long hyperscaler precedent list (rustc/Nix/Anduril/Cloudflare) is citation-padding but not fabrication.
- **refinement:** Sweep `Foundry`/`oya-foundry-*`/`retired external agent harness` → intelligence/governance vocabulary (ADR-0335/0347) before ratification; reconcile git-host Tier-0 with the forge decision.
- **consensus_needed:** "Can autonomous workflows author/publish ADRs and Cedar policy-root fragments at all, given ADRs are the immutable founder SSOT — or is self-modification capped below the governance-artifact layer (code/workflow versions only, never ADRs/policy-root)?"

### ADR-0248 — Amazon-shape Cellular Architecture

- **decision_atom:** Adopt AWS cell-based architecture as the canonical topology: a four-tier model (Tier-0 external deps, Tier-1 bootstrap cell, Tier-2 control-plane cells, Tier-3 data-plane cells) + dedicated peer service cells (marketplace/dev-tools/audit-aggregator/analytics/ops-console) + reserved Tier-4 for financial/fulfillment-grade, with mandatory shuffle-sharding (S=8 default), static stability (24h control-plane isolation tolerance), constant-work pull distribution, and an intra-cell hot-path bright line.
- **domain:** orchestration-scheduling (cross-cutting: dr-resilience / networking-mesh)
- **current_status:** Proposed
- **disposition:** KEEP (as RATIFIED) — this is the topology substrate the whole bundle binds to (ADR-0240/0241/0242/0243/0251 all assume a uniform cell topology); it AMENDS ADR-0009 cleanly. The keystone map already lists it as the canonical orchestration posture's cell primitive.
- **proposed_resolution:** **RATIFY** — it is the load-bearing topology; the keystone §3 orchestration row (Talos+CAPI+ArgoCD) and the cell-as-blast-radius primitive both presuppose it.
- **governing:** n/a (not archived).
- **truth_flag:** TRUE (PARTIAL: see vocabulary/stack tensions below).
- **in_masterplan:** YES (`planning_impact: true`; 7 enforced_by lanes; advisory-until-cell-substrate-lands; references ADR-0333 successor-owner condition).
- **tensions:** (1) Tier-1 substrate list includes a `foundry` substrate-meta (RETIRED brand per ADR-0335) and bootstrap via `kubeadm`/`containerd`/`istio` on-prem stack (ADR-0121) — but ADR-0121 is **Superseded by ADR-0375** (Talos+CAPI+ArgoCD) per the keystone map. So the bootstrap-cell provisioning path cites a retired orchestration stack. (2) "cell" as the universal primitive is now a **deployment pattern** per ADR-0333 (cell-µsvc retired), which this ADR's `microservices/cell/` registry references as a live µservice — surface the 0333 reframe. (3) Tier-4 reserved for payments collides with nothing in-bundle but presumes ADR-0252's TrueTime tier. (4) Edge/Cloudflare→Pingora deferred to ADR-0253 (out of chunk).
- **hyperscaler_challenge:** ALIGNED — this is the most defensible ADR in the chunk. Hamilton 2007 LISA, MacCárthaigh 2014 shuffle-sharding (math reproduced and correct: `1/C(100,8) ≈ 5.4×10⁻¹²`), Brooker constant-work, Weiss/Furr static-stability, Stripe Cells 2024 are all canonical and accurately applied. AWS/Stripe literally do this. No argument for amend/archive on substance.
- **ai_slop:** Low. Long but every section is load-bearing; the shuffle-sharding probability arithmetic checks out. Some citation density (ARC408/ARC405/ARC404 re:Invent sessions) is reference-padding.
- **refinement:** Repoint bootstrap-cell provisioning from ADR-0121 (kubeadm/containerd/istio, superseded) to ADR-0375 (Talos+CAPI); drop the `foundry` Tier-1 substrate name; reconcile `microservices/cell/` with the ADR-0333 pattern-not-service reframe.
- **consensus_needed:** "Does the bootstrap cell provision via Talos (ADR-0375 canon) rather than the kubeadm/containerd/istio stack (ADR-0121, superseded) this ADR currently assumes?"

### ADR-0249 — Multi-Category Marketplace Doctrine

- **decision_atom:** Establish oyatie Marketplace as one unified multi-category commerce brand (digital/physical/C2C/services/subscriptions) decomposed into **eight shared substrate µservices** (catalog, inventory, orders, fulfillment, reviews, discovery, pricing, trust-safety) built day-one, plus four category BCs rolled out per-category certification readiness (ADR-0250), and refactor plugin-app-store onto the shared substrates so digital plugins ship first.
- **domain:** marketplace-commerce (cross-cutting: api-contracts / product-ux)
- **current_status:** Proposed
- **disposition:** AMEND (as RATIFIED-with-conditions) — the doctrine is sound and the "one substrate, many surfaces" Amazon/Stripe/Apple pattern is correct, but this is the **broadest unbuilt scope-expansion in the chunk** (8 new µservices + full payments/orders/fulfillment DDL day-one) and the largest masterplan-impact-vs-LINUX-pilot mismatch. AMEND to defer the non-digital substrate DDL behind the ADR-0250 build-ahead gate rather than implying day-one build of all eight.
- **proposed_resolution:** **RATIFY** the *doctrine* (substrate-vs-surface, plugin-app-store-refactor, category sequencing). Founder should confirm whether the *day-one build of all 8 substrates* is in scope for the minimal-viable-kernel-first goal order (per MEMORY: kernel parity FIRST, cloud/k8s/marketplace later) — if not, the build trigger should be RATIFY-doctrine / DEFER-build.
- **governing:** n/a (not archived). It AMENDS ADR-0213 (ecosystem-as-a-service: activates the reserved `marketplace` name) and ADR-0132 (declares Marketplace a brand-layer concept, not a grouping µservice — consistent with ADR-0362 flat-only catalog).
- **truth_flag:** TRUE (the architecture is coherent), PARTIAL on timing (the "built day-one" framing is in tension with the build-ahead doctrine it cites and with the founder's kernel-first goal order).
- **in_masterplan:** YES (`planning_impact: true`; 5 enforced_by lanes; advisory-until-marketplace-substrates-land).
- **tensions:** (1) Scope/breadth vs founder goal order — marketplace is explicitly the LATER half of the cloud-native stack, not the minimal-viable-kernel-first work (MEMORY: cloud-native-rust-stack). (2) Kafka topic references for listing-search-projection (`per ADR-0145`) — standalone Kafka RETIRED → Pulsar (ADR-0377). (3) Massive Postgres+Citus DDL again collides with LINUX ADR-0001 own-DB posture. (4) `payment_intents` table is `reserved_pending_cert` — correctly gated by ADR-0250, no live conflict.
- **hyperscaler_challenge:** ALIGNED on pattern, QUESTIONABLE on timing. Amazon (ASIN-as-universal-id), Apple App Store, Stripe-powers-5-shapes, Salesforce AppExchange genuinely converge on shared-substrate/many-surfaces — the doctrine is what a mature platform does. But no hyperscaler built all five commerce shapes' substrates day-one before having a core platform; they evolved the substrate under the surfaces. Argues for AMEND (sequence the build), not archive.
- **ai_slop:** Moderate. The five `CREATE TABLE`/`CREATE TYPE` blocks (listings, inventory, orders, fulfillment, payment_intents, disputes) are extremely detailed for a Proposed doctrine ADR — this is design-doc content embedded in an ADR. Not fabricated, but over-specified relative to the decision; the DDL belongs in `/specs/` or the µservice PRDs, not frozen into an immutable ADR.
- **refinement:** Split the doctrine (KEEP) from the day-one DDL (move to specs/PRDs); gate non-digital substrate build behind ADR-0250; swap Kafka→Pulsar.
- **consensus_needed:** "Is the full 8-substrate marketplace in scope now, or RATIFY-doctrine-only / DEFER-build until after the minimal-viable-kernel-parity milestone the founder ordered first?"

### ADR-0250 — Build-Ahead-of-Certification Doctrine

- **decision_atom:** Every certification-gated capability (payments/healthcare/government/education/pharma/defense/marketplace/financial-services) is architected and built day-one and **launched per-market only after the regulator cert lands**, via a three-state lifecycle ARCHITECTED → BUILT → LAUNCHED-IN-MARKET that is Cedar-evaluated per (capability,market) tuple, with an anti-bypass rule and a LAUNCHED→DEGRADED→DARK rollback model.
- **domain:** compliance-residency (cross-cutting: governance-process)
- **current_status:** Proposed
- **disposition:** KEEP (as RATIFIED) — clean, no `amends`/`supersedes` (zero collision surface), and it is the doctrine that makes ADR-0249's "build day-one" coherent. The build-ahead pattern (Apple Pay per-country, Stripe geo-expansion, AWS region build-ahead) is exactly right.
- **proposed_resolution:** **RATIFY** — it is the enforceable primitive (3-state lifecycle + Cedar gate + cell.certifications) that prevents the build-vs-cert race; dropping it would make ADR-0249/0251 ambiguous about "built vs launched."
- **governing:** n/a (not archived).
- **truth_flag:** TRUE.
- **in_masterplan:** YES (`planning_impact: true`; 5 enforced_by lanes incl. `anti-bypass-built-only-tenant`; advisory-until-three-state-lifecycle-lands).
- **tensions:** (1) The §D-4 certification matrix is enormous (~40 capability×market rows with cost bands) — same over-specification concern as 0249's DDL: this is a living spec frozen into an immutable ADR; D-4 itself says "modifications require a follow-up ADR," which guarantees ADR churn. (2) Anti-bypass enforced "at admission time (Kyverno per ADR-0183)" — ADR-0183 is **Superseded by ADR-0379** (Kubewarden); the Kyverno reference is stale-target. (3) Depends on ADR-0252 + ADR-0254 (named as same-bundle) for substrate. No flat contradictions.
- **hyperscaler_challenge:** ALIGNED — strongest-fit doctrine in the chunk alongside 0248. Apple Pay (built-globally/launched-per-market, UK 8 months after US on a day-one-complete build), Stripe PSD2 (~18mo cert on a pre-built stack), AWS region build-ahead, Palantir Foundry one-codebase/per-environment-cert are all canonical and accurately cited. No argument for amend/archive on substance.
- **ai_slop:** Moderate — the certification matrix reads like a compliance research dump (regulator names, costs, renewal cadences across ~10 jurisdictions). Accurate-looking but is reference content, not decision content.
- **refinement:** Move the §D-4 matrix to `/specs/capability-certification-matrix.json` (which it already declares canonical) and keep only the 3-state lifecycle + gates in the ADR; repoint the Kyverno/ADR-0183 admission reference to ADR-0379/Kubewarden.
- **consensus_needed:** none material — doctrine is uncontested. (Minor: confirm the cert matrix lives in spec, not the ADR body, to avoid the self-declared follow-up-ADR churn.)

### ADR-0251 — Compliance Pack + Cell Certification Levels

- **decision_atom:** Make **Compliance Pack** a first-class versioned/signed (Ed25519 + Sigstore/cosign) bundle wrapping a single regulation; cells declare a SET of **certification levels** enumerating which packs they can host; tenants install packs onto themselves; the policy-engine aggregates installed packs at evaluation time (deny-wins) with a full pack lifecycle, NIS2 three-stage breach cadence, and a cell-certification-level matrix (general→pci/hipaa/fedramp/il5/il6/eu-sovereign/kr-csap/cn-pipl/…).
- **domain:** compliance-residency (cross-cutting: authz-policy / crypto-keymgmt)
- **current_status:** Proposed
- **disposition:** KEEP (as RATIFIED), with AMEND-on-merge — the pack-as-bundle primitive (regulation-as-product, matching AWS Audit Manager / Purview Compliance Manager / GCP Assured Workloads / Databricks compliance profiles) is correct and fills the half-specified gap ADR-0240/0241 left. AMEND its `amends:` citations on merge.
- **proposed_resolution:** **RATIFY** — it is the unifying primitive that makes the 38+ regulatory regimes tractable and is the cell-certification source-of-truth that ADR-0250's launch gates and ADR-0248's `cell.certifications[]` consume.
- **governing:** n/a (not archived). It AMENDS ADR-0099 (data-class extensions), ADR-0144 (EU AI Act tiers inside the pack), and ADR-0150 (`pack/<pack-id>/` Cedar scope).
- **truth_flag:** TRUE (PARTIAL on the ADR-0183/0150 reference set — same superseded-Kyverno/0379 drift as the rest of the bundle).
- **in_masterplan:** YES (`planning_impact: true`; 10 enforced_by lanes — the most gate-heavy ADR in the chunk; advisory-until-pack-registry-substrate-lands).
- **tensions:** (1) `bootstrap conditions` reference `microservices/governance/` as the fragment-bundle registry host — "governance" is the live name (ex-foundry-fitness per ADR-0347), so this is actually CORRECT/forward-compatible, good. (2) Hard dependency on the policy-engine substrate (ADR-0246) and audit-chain — bundle-coherent. (3) Cell-cert matrix lists `cn-pipl-eligible` (mainland-China residency, PRC-citizen ops staff) — a compliance-residency posture that may exceed the founder's declared market scope; surface but not resolve. (4) Like 0250, the cert-level matrix is large living content frozen into an ADR.
- **hyperscaler_challenge:** ALIGNED. "Regulation as a packaged bundle, activated per-tenant, evidence auto-emitted" is exactly AWS Audit Manager frameworks / MS Purview assessment templates / GCP Assured Workloads regimes / Databricks Compliance Security Profile. The cell-certification-level taxonomy mirrors GCP Assured Workloads regimes. No argument for amend/archive on substance.
- **ai_slop:** Moderate — the 38-regulation table + 20-row cert-level matrix + full JSON Schema (incl. exhaustive NIS2 Article 23 three-stage cadence) is compliance-encyclopedia content. Accurate and well-cited, but spec-grade detail inside an immutable ADR.
- **refinement:** Move the regulation table + cert-level matrix + JSON Schema into the declared `/specs/*.json` (already named canonical) and keep the pack-primitive + lifecycle in the ADR; refresh Cedar-admission references (0150/0183) toward 0243/0379.
- **consensus_needed:** "Is `cn-pipl-eligible` (mainland-China data-plane + PRC-citizen control-plane staff) within declared market scope, or reserved/dropped?"

### ADR-0252 — Time, Coordination, and Distributed Consistency

- **decision_atom:** Make **Hybrid Logical Clocks (HLC)** the default platform clock primitive (TrueTime/GPS+atomic reserved for Tier-4 financial + IL5+ cells); mandate Workflow Engine sagas (ADR-0222) over distributed locks for cross-µservice coordination; require caller-supplied Stripe-style idempotency keys (`idem_<32-char-base32>`, 160-bit); default to causal consistency (strict-total-order opt-in via raft); Google leap-smear; per-cell cron with jitter — no global scheduler.
- **domain:** data-engine-db (cross-cutting: dr-resilience; observability for the audit-chain HLC ordering)
- **current_status:** Proposed
- **disposition:** KEEP (as RATIFIED) — textbook-correct distributed-systems doctrine; it AMENDS ADR-0035/0145/0128 cleanly and formalizes the idempotency/HLC primitives the saga policy (ADR-0222) and DR policy (ADR-0241 T1 RTO) already assume.
- **proposed_resolution:** **RATIFY** — the T1 <5min RTO declaration in ADR-0241 is aspirational without this ADR's HLC-tagged replay + idempotency-key-safe semantics; this is foundational plumbing, not contested.
- **governing:** n/a (not archived).
- **truth_flag:** TRUE.
- **in_masterplan:** YES (`planning_impact: true`; 7 enforced_by lanes incl. `no-distributed-lock`, `no-wall-clock-for-ordering`; advisory-until-time-kernel-lands).
- **tensions:** (1) `related: ADR-0005-eventing-backbone-outbox` and §"What this is NOT" says "not a retirement of Kafka… Kafka topics carry HLC timestamps per ADR-0005" — but ADR-0005 is RETIRED (Kafka → Pulsar+Oxia per ADR-0377-kafka-to-pulsar; keystone §1.1/§2). The outbox pattern survives but the Kafka framing is stale. (2) `related: PR-159A 'oya git' rename` / "internal git operations carry HLC timestamps" — the agentic `oya git`/VCS is RETIRED per ADR-0363 (retire agentic-VCS → plain git + Forgejo); this citation is stale. (3) HLC in-house impl vs vendoring CockroachDB's `hlc` crate — consistent with the own-when-proven ratchet (ADR-0211 / LINUX ADR-0019), no conflict. (4) TrueTime Tier-4 presumes ADR-0248 Tier-4 + ADR-0251 cell certs — bundle-coherent.
- **hyperscaler_challenge:** ALIGNED — the single most industry-standard ADR in the chunk. HLC (CockroachDB/Yugabyte/MongoDB/TiDB), TrueTime (Spanner/Aurora DSQL), sagas + idempotency keys (Stripe/Temporal/Step Functions), Google leap-smear, per-cell cron (SRE Workbook ch.24), distributed-locks-are-anti-pattern (SRE Workbook) are all canonical and correctly cited with primary sources. Google/AWS/Azure would make exactly these calls. No argument for amend/archive.
- **ai_slop:** Low. The incident table (2012 leap-second, 2017 S3, 2022 Atlassian, 2024 CrowdStrike) is real and on-point; the HLC algorithm pseudo-code and Rust traits are correct. Citations are genuine primary sources (Demirbas+Kulkarni OPODIS 2014, Corbett OSDI 2012, Leach idempotency blog).
- **refinement:** Repoint the eventing references from ADR-0005/Kafka to ADR-0377 (Pulsar+Oxia); drop the retired `oya git`/PR-159A HLC-ledger framing (ADR-0363 retired agentic-VCS).
- **consensus_needed:** none material — uncontested foundational doctrine. (Minor: confirm in-house HLC vs CockroachDB-crate vendoring against the own-when-proven trigger threshold.)

---

## Chunk notes

**Bundle integrity.** All seven ADRs are one indivisible keystone (`2026-05-20-foundational-doctrine`, positions 5–11 of 14, ADR-0242→0255). They cross-reference each other as hard dependencies and declare "partial acceptance rejected." So the RATIFY/DROP question is effectively a single bundle-level decision: **either the founder ratifies the keystone bundle as the canonical doctrine layer (the masterplan's §3 canonical posture already treats Cedar-universal-gate + cell-architecture + tenant-as-tenant as TRUE, which presupposes this bundle), or the bundle is re-opened wholesale.** My net recommendation: **RATIFY the bundle's doctrines**, AMEND the shared stale-citation set, and DEFER the heaviest day-one *build* scope (0249 marketplace substrates) behind the founder's kernel-parity-first goal order.

**No archives, no garbage, no unaccounted-for proposals.** None of the seven is superseded on disk; none is obsolete/redundant/garbage. All seven are Proposed and every one is resolved RATIFY above (no DROPs warranted — each is load-bearing for the canonical posture or for a sibling keystone). The only ARCHIVE-adjacent items are *retired vocabulary/citations embedded inside otherwise-live ADRs* (AMEND, not ARCHIVE).

**Systemic stale-citation pattern (AMEND-on-merge, applies across the chunk).** Because the bundle was authored 2026-05-20, before the later retirement ADRs, it consistently cites primitives that are now retired/superseded:
- **ADR-0183 (Kyverno admission) → ADR-0379 (Kubewarden)** — referenced as live in 0246, 0250, 0251.
- **Kafka / ADR-0005 → Pulsar+Oxia / ADR-0377-kafka-to-pulsar** — referenced as live in 0246 (hot-reload topic), 0249 (search projection), 0252 (eventing backbone). Outbox pattern survives; Kafka does not.
- **"foundry" brand / `oya-foundry-*` / "retired external agent harness" → cloud-intelligence + governance (ADR-0335/0347)** — pervasive in 0247, present in 0248 (Tier-1 substrate-meta). The dissolution *mechanics* of 0247 are governing; the *brand vocabulary* is retired.
- **ADR-0121 (kubeadm/containerd/istio on-prem) → ADR-0375 (Talos+CAPI+ArgoCD)** — 0248 bootstrap-cell provisioning cites the superseded stack.
- **`oya git` / agentic-VCS → retired (ADR-0363)** — 0252 cites it for HLC-tagged git ledger.
- **"cell" as µservice → "cell" as deployment pattern (ADR-0333)** — 0248 `microservices/cell/` framing.
Trust the superseding ADR over the stale citation per keystone map §0/§6; none of these invalidate the seven decisions, but all must be refreshed before the ADRs are re-founded into the masterplan-generating log.

**Spec-vs-ADR boundary (refinement, repeated).** ADR-0246 (47-crate map + DDL), 0249 (six full DDL blocks), 0250 (~40-row cert matrix), 0251 (38-regulation table + 20-row cert-level matrix + full JSON Schema) embed large *living spec content* inside *immutable* ADRs. Each already declares a canonical `/specs/*.json` home for that content. Under the founder's "ADRs = immutable append-only SSOT; masterplan generated from ADR front-matter" design (consolidation reading), freezing mutable matrices into ADRs guarantees supersede-churn (ADR-0250 §D-4 literally says "modifications require a follow-up ADR"). Recommend: ADRs carry the *decision + invariants*; the matrices/DDL live in specs. **Flag under both masterplan readings** per keystone §4 (this affects masterplan generation regardless of which authority model wins).

**LINUX-pilot collision watch (for merge).** 0246 (Cedar substrate on Postgres/Citus) and 0249/0251 (heavy Postgres+Citus DDL) sharpen the keystone fault-line #1 (LINUX ADR-0001 "eliminate PostgreSQL / own the DB engine"); 0246's Cedar-as-substrate vs LINUX ADR-0021 owned-policy-language is fault-line #2 (own-vs-reuse, not contradiction). All seven carry no `renumber_note` (they are source-native 0246–0252) and do not collide numerically with the LINUX 0001–0026 series — but they DO presuppose the source best-of-breed substrate posture that the LINUX pilot deliberately diverges from. Surface, do not resolve.
