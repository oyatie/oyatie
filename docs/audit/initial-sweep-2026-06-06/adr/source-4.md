# ADR Audit — source-4

- **side:** SOURCE (`~/Developer/source`, `jason931225/oyatie`)
- **chunk:** source-4 (slice rows 22–28 of `ls ADR-*.md | sort`)
- **range:** ADR-0023 → ADR-0029
- **ADRs reviewed:** 7 (0023, 0024, 0025, 0026, 0027, 0028, 0029)
- **auditor posture:** READ-ONLY; keystone map is the binding baseline. This slice is the **Foundry cluster** (0023–0027, all `foundry`-owned, all `status: proposed`) plus two **Accepted** microservice ADRs (0028 Cloud, 0029 Connect). The single dominant finding: ADR-0335 RETIRED the "foundry" brand (foundry → **intelligence** for consumer AI + **governance** for CI/gates) and ADR-0347 renamed the `oya-foundry-fitness-*` CI prefix → `oya-governance-*`. **Every one of 0023–0027 is brand-stale and must AMEND its vocabulary** (the *decision atoms* mostly survive; the *naming* does not). Note: 0025 already self-amended its fitness-function crates to `oya-governance-*` mid-body while keeping `oya-foundry-*` everywhere else — proof the rename is real and in-flight, and proof these are pre-0335 docs frozen with residue.

---

### ADR-0023 — Foundry sandbox: Wasmtime + WASI Preview 2 for short-lived tools, Firecracker microVMs for full-kernel tools

- **decision_atom:** Agent-invoked tools run in a two-tier process sandbox — Wasmtime/WASI-Preview-2 for short-lived deterministic tools and Firecracker microVMs for full-kernel tools — with uniform per-spawn audit emission, deny-by-default egress allowlists, per-tool resource caps, and escape-attempt detection that auto-suspends the offending tool digest.
- **domain:** isolation-runtime (primary); intelligence-ai (secondary — it is the AI-agent tool sandbox)
- **current_status:** Proposed
- **disposition:** AMEND (sound decision; brand-stale `foundry` naming + must reconcile with SOURCE canonical isolation posture)
- **proposed_resolution:** **RATIFY** — the two-tier wasmtime+firecracker model is fully aligned with SOURCE canon (ADR-0147 runtime ladder native→sandbox→microvm→confidential; ADR-0200 wasmtime canonical WASM). It is a correct, hyperscaler-grade decision and should be Accepted, but only after the `oya-foundry-sandbox-*` crate/event/lane names are rehomed to `intelligence` (runtime) + `governance` (CI lanes) per ADR-0335/0347, and after it is positioned as the *intelligence-side instantiation* of the ADR-0147 ladder rather than a parallel sandbox.
- **governing:** AMEND-governed by ADR-0335 (brand) + ADR-0347 (lane prefix); reconcile with ADR-0147/0200 (runtime ladder/wasmtime canon).
- **truth_flag:** PARTIAL — decision TRUE; vocabulary STALE (`oya-foundry-*` crates, `EVT-FOUNDRY-SANDBOX-*` events, `foundry-sandbox-*` CI lanes, `Owner: foundry`).
- **in_masterplan:** PARTIAL — no `planning_impact`/`masterplan_ref` front-matter; carries real planning weight (sandbox substrate) but is not yet bound. Under *generated-from-ADRs* it must be re-authored to feed the masterplan; under *masterplan-as-authority* it needs a `masterplan_ref` binding. Flag both.
- **tensions:** Fault-line #3 (keystone §5): SOURCE Talos+containerd+firecracker+wasmtime vs LINUX framekernel/Capsule own-the-host. This ADR is the SOURCE firecracker+wasmtime instantiation; it competes with LINUX ADR-0018/0014 (own isolation backend). gVisor explicitly rejected here — consistent with SOURCE. Internal: references ADR-0025 "as the audit chain for emission" while elsewhere the audit chain is ADR-0003 — cross-ref drift to fix on amend.
- **hyperscaler_challenge:** ALIGNED. AWS literally authored Firecracker for exactly this (Lambda/Fargate); wasmtime for short-lived deterministic tools is the modern serverless-isolation consensus (Fastly/Cloudflare-class). Google/Azure would make the same native→sandbox→microvm laddering. Verdict argues **amend (keep the decision, fix the branding)**, not archive.
- **ai_slop / refinement / consensus_needed:** Not slop — concrete, threat-modeled, with named CI gates. Refinement: collapse into ADR-0147's ladder so there is one isolation doctrine, not a foundry-private one. **Founder question:** does the agent-tool sandbox live as a distinct `intelligence` substrate, or is it just the ADR-0147 ladder consumed by intelligence (no new ADR needed)?

---

### ADR-0024 — Foundry eval harness and replay: per-capability golden sets, A/B routing, adversarial cohorts, regional linguistic eval

- **decision_atom:** Every AI capability must ship a Cosign-signed golden eval set that gates publish, runs nightly, A/B-tests provider routing, and replays past production traces — with mandatory adversarial cohorts (prompt-injection, data-class-violation, autonomy-bypass, tool-exfiltration) and mandatory per-locale linguistic cohorts (KR/JP/EN minimum) — and this same harness is the cutover gate for in-house models.
- **domain:** intelligence-ai (primary); ci-cd-build (secondary — it is a publish/regression gate substrate)
- **current_status:** Proposed
- **disposition:** AMEND (decision sound and notably mature; brand-stale + needs masterplan binding)
- **proposed_resolution:** **RATIFY** — this is one of the strongest ADRs in the slice (it has a *Resolved* section that closed its four open questions: cryptographic-shredding for DSR vs replay retention, HumanJudged rubric scaling with Cohen's κ≥0.7, tenant-extensible adversarial cohorts, split eval token budget). Accept it; on accept, rehome `oya-foundry-eval-*` → `oya-intelligence-eval-*`, `foundry-eval-*` lanes → `governance-*`/`intelligence-*`, and `EVT-FOUNDRY-*` event names.
- **governing:** AMEND-governed by ADR-0335 (brand) + ADR-0347 (lane prefix). Cross-refs ADR-0043 (per-tenant KEK) and ADR-0003 (audit) are correct and survive.
- **truth_flag:** TRUE in substance; STALE only in `foundry` vocabulary. The Resolved-section discipline makes this the least-sloppy ADR in the chunk.
- **in_masterplan:** PARTIAL — carries strong planning weight (mandatory publish gate) but no masterplan front-matter binding. Flag under both readings.
- **tensions:** Tight coupling to ADR-0026 (eval = cutover gate) and ADR-0020/0022/0023 — internally coherent, no contradiction. The only watch-item: "Splitting eval per capability vs per model fragments authority" presumes the unified-Foundry axis that 0335 dissolved — on amend, confirm the harness is owned by `intelligence` (not split to governance).
- **hyperscaler_challenge:** ALIGNED. Eval-gated model promotion + adversarial/red-team cohorts + replay is exactly Google (Vertex eval), AWS (Bedrock evaluations), Azure (AI Foundry evals) practice; per-locale linguistic cohorts are a defensible regional-market differentiator. Verdict argues amend, not archive.
- **ai_slop / refinement / consensus_needed:** Not slop. Refinement: the duplicated runbook path (`capability-eval-regression.md` listed twice for two different purposes) is a copy-paste bug to fix on amend. No founder question — this one is decided.

---

### ADR-0025 — Foundry as the engineering platform: repoctl, catalog, gates, fitness functions, supply chain, customer-facing builder surfaces all under one axis

- **decision_atom:** Consolidate all engineering-platform surfaces (repoctl, catalog, claim-ceiling validator, foundation-bypass ledger, plane-gated CI lanes, scorecards, fitness functions, supply-chain attestation, plugin/marketplace authoring, customer-facing builder surfaces) into one axis with the agent runtime, on the thesis that the same runtime authors customer workflows and engineering PRs under one shared fitness-function gate substrate.
- **domain:** governance-process (primary); ci-cd-build + agentic-platform (cross-cutting)
- **current_status:** Proposed
- **disposition:** AMEND (the cohesion thesis is the precise thing ADR-0335 partially DISSOLVED — this ADR's "one axis" premise is now factually wrong; the surfaces survive but split across intelligence/governance)
- **proposed_resolution:** **RATIFY-with-major-amend** (accept the surviving decision, do NOT accept the "one Foundry axis" framing). ADR-0335 explicitly split the retired Foundry into **intelligence** (AI agent runtime) + **governance** (CI/gates/fitness functions), declaring Governance *stays separate*. This ADR's central claim ("splitting these into separate axes fractures cohesion → keep one axis") is the **opposite of the governing decision**. So: ratify the *substance* (catalog-as-ground-truth, claim-ceiling ratchet, bounded bypass-ledger, plane-gated lanes, fitness functions, recursion property) but amend the framing to "intelligence runtime + governance gates, sharing the catalog" not "one Foundry axis." Already half-done in-body: its fitness-function crates are written as `oya-governance-*` while the rest stays `oya-foundry-*`.
- **governing:** ADR-0335 (foundry → intelligence + governance, Governance stays separate) + ADR-0347 (fitness-lane rename) + ADR-0362 (flat-only, no axis/grouping). The "axis" noun itself is retired vocabulary per ADR-0058/0362.
- **truth_flag:** PARTIAL → leaning WRONG on the headline thesis. The decision atom's "all under one axis" is STALE/WRONG post-0335; the component mechanisms are TRUE. Highest brand-residue density in the slice (`oya-foundry-*` throughout + the literal line "The `oya-foundry-*` namespace is governed by the same team as `oya-foundry-*`" — a self-referential tautology that is almost certainly a botched `oya-governance-*` edit = a small GARBAGE artifact).
- **in_masterplan:** PARTIAL — heavy planning weight (it defines the gate substrate the whole repo runs on) but unbound and built on a retired axis premise. Must be reconciled before any masterplan binding.
- **tensions:** Direct tension with ADR-0335's intelligence/governance split (the governing retirement). Also tension with ADR-0394 (bespoke-Rust IDP supersedes Backstage) — this ADR rejects Backstage/Port/Cortex as SoR, which *agrees* with 0394's direction, so that part is fine. Recursion thesis (agent PRs == human PRs through one gate) is philosophically strong but couples failure modes (its own Negative section admits a fitness-function bug breaks both lanes at once).
- **hyperscaler_challenge:** QUESTIONABLE on org-shape, ALIGNED on substrate. Google/AWS/Azure absolutely build unified internal developer platforms with catalog-as-ground-truth, supply-chain attestation, and policy-as-code gates — but they do NOT fuse the consumer-AI product runtime with the internal CI-governance platform into "one axis/team"; they keep platform-engineering and AI-product as separate orgs sharing infra. That is exactly what ADR-0335 corrected. Verdict argues **amend** (split the axis, keep the mechanisms).
- **ai_slop / refinement / consensus_needed:** Borderline — the breathless "everything under one axis" maximalism is the kind of over-consolidation 0335 had to walk back; the tautological crate-namespace line is a concrete editing artifact. **Founder question (contested):** is the engineering-platform/governance substrate genuinely *one* surface with intelligence, or are governance (gates/CI) and intelligence (agent runtime) two surfaces sharing the catalog — i.e. should this ADR be re-authored as two ADRs (or archived in favor of a fresh governance-platform ADR + intelligence-runtime ADR)? This is the single biggest consensus item in the chunk.

---

### ADR-0026 — In-house AI model substrate: long-horizon W-AI-Model-Substrate; consume providers until per-vertical eval favors in-house

- **decision_atom:** Pursue a long-horizon in-house AI model substrate for Oyatie-specific tasks (KR-first foundation LLM, embeddings, STT/TTS, vision/OCR, vertical safety classifiers — explicitly NOT a frontier general LLM), consuming Anthropic/OpenAI/Gemini until a per-vertical eval set (ADR-0024) shows the in-house variant wins, at which point the `ProviderAdapter` trait makes cutover a single router-preference change; per-tenant fine-tuning is LoRA-on-in-house-base only, consent-recorded and DSR-eligible.
- **domain:** intelligence-ai
- **current_status:** Proposed
- **disposition:** AMEND (strategically sound, well-bounded, eval-gated; brand-stale + uses the retired W-name vocabulary)
- **proposed_resolution:** **RATIFY** — the "consume-providers-until-eval-favors-in-house, never a frontier lab" posture is correct and disciplined (anti-scope explicitly excludes frontier general chat + defense fine-tunes). Accept; on accept rehome `oya-foundry-model-*` → `oya-intelligence-model-*` and reconcile the **W-AI-Model-Substrate wave name** — wave names exist per GLOSSARY but the retired `M0–M3/MVP/Milestone` vocabulary and the now-anachronistic "Foundry-Preview" wave (keystone §2) mean the wave-naming needs a pass.
- **governing:** AMEND-governed by ADR-0335 (brand: this IS the intelligence substrate now) + ADR-0347. Wave-name reconciliation per GLOSSARY L250/L504 (M0–M3 retired).
- **truth_flag:** TRUE in substance; STALE in `foundry` brand + wave naming.
- **in_masterplan:** PARTIAL — long-horizon strategic decision with clear planning weight; unbound front-matter. The "What is the trigger to enter W-AI-Model-Substrate?" open question is genuinely a masterplan-sequencing question (owner: foundry+founder) — flag for the masterplan.
- **tensions:** No internal contradiction; couples cleanly to ADR-0024 (cutover gate) and ADR-0020 (`oya-internal-*` provider IDs) and ADR-0027 (sub-substrates on same kernel). Mild tension with SOURCE concentration on managed-OSS substrates elsewhere, but this is the AI-model layer (no SOURCE managed-model canon contradicts it). LoRA-only-on-in-house-base is a correct provider-TOS-aware constraint.
- **hyperscaler_challenge:** ALIGNED-with-caveat. "Don't build a frontier lab; build per-task specialized models gated by eval, fall back to providers" is exactly the rational mid-size-cloud posture (AWS Bedrock + Titan/Nova small models; Azure + Phi; Google + Gemma small models for narrow tasks). The caveat hyperscalers would raise: even *narrow* KR-first foundation-LLM training is a serious capex/talent bet — its own Negative section ("risk of building models that never beat the provider") concedes this. Verdict argues amend (keep, with the trigger-threshold left to the masterplan), not archive.
- **ai_slop / refinement / consensus_needed:** Not slop — unusually self-aware about the build-vs-buy risk. **Founder question:** the entry trigger for the wave (revenue threshold? volume threshold? provider price-shock?) is the open masterplan-sequencing decision; founder must set the threshold (the ratchet principle is shared with LINUX ADR-0019/0020 per keystone §5 — own-when-proven; only the trigger is unset).

---

### ADR-0027 — Robotics, vision, and speech sub-substrates: model crates, robotics control plane, deterministic latency, safety-critical anti-scope

- **decision_atom:** Define vision, speech, and robotics-control as three Foundry capability sub-substrates layered on the in-house model kernel (ADR-0026), with structurally-enforced lawful-basis gates (facial recognition, voice biometrics), microsecond deterministic-latency control loops in a dedicated runtime, a four-level SafetyClass with SC4-autonomous-actuation T4-disabled-by-default + simulation-first certification, and a structurally-enforced defense/weaponized-robotics anti-scope requiring founder+legal carve-out.
- **domain:** intelligence-ai (primary); hardware-firmware (secondary — robotics actuation/real-time control plane)
- **current_status:** Proposed
- **disposition:** AMEND (sound and admirably safety-conscious; brand-stale; scope-breadth is the real question)
- **proposed_resolution:** **RATIFY-with-scope-caveat** — accept the substrate framing and the safety/anti-scope mechanisms (these are excellent: structural anti-scope, lawful-basis gates, sim-first, T4-disabled actuation). On accept, rehome `oya-foundry-model-vision/speech/robotics-*` → `oya-intelligence-*` and `foundry-*` lanes → `governance-*`. The caveat: autonomous-vehicles + public-road + drones is an enormous regulatory surface for a company whose core is cloud/AI — its own open-question #3 flags this. Recommend RATIFY but explicitly **scope-gate AV/public-road to a future ADR** rather than admit it into the masterplan now.
- **governing:** AMEND-governed by ADR-0335 (brand) + ADR-0347 (lanes). Anti-scope binding referenced by ADR-0026 is consistent.
- **truth_flag:** TRUE in substance (safety design is genuinely strong); STALE in `foundry` brand.
- **in_masterplan:** PARTIAL — robotics/AV is a large, contested scope decision that should NOT silently enter the masterplan at full breadth; vision/speech sub-substrates are reasonable to bind. Split the masterplan binding.
- **tensions:** No internal contradiction. Cross-cuts ADR-0022 (T4 autonomy ceiling — correctly reused for SC4 actuation) and ADR-0024 (sim-suite as an eval cohort). The breadth tension is the "own everything" pattern (keystone §5 fault-line #5): adding a robotics/AV control plane is the most expansive scope claim in the slice.
- **hyperscaler_challenge:** QUESTIONABLE on breadth, ALIGNED on safety method. No hyperscaler bundles autonomous-vehicle/public-road robotics control into its core cloud+AI platform — AWS RoboMaker (now deprecated), Azure, GCP all treat robotics as a thin services/simulation layer, not an owned safety-critical control plane. Google/AWS/Azure would build the *vision/speech* model substrates (they all do) but would NOT take on SC4 autonomous-actuation liability in-house. Verdict argues **amend = narrow scope** (keep vision/speech + advisory/assistive robotics; defer SC4-autonomous + public-road AV to a separate gated ADR).
- **ai_slop / refinement / consensus_needed:** Not slop — the safety engineering (SafetyClass ladder, ISO bindings, structural anti-scope code) is concrete and credible, not hand-wavy. But the *scope* is aspirational. **Founder question (contested):** is owned robotics/AV control-plane in-scope for the company at all, or is robotics a consume-via-`ProviderAdapter` (vision/speech only) layer with actuation deferred? This decides whether 0027 is RATIFY-narrow or partial-archive.

---

### ADR-0028 — Cloud microservice: compute substrate with stable product surface across three infrastructure phases

- **decision_atom:** Cloud is a flat microservice (not an axis) that owns physical compute reality and exposes a phase-invariant product surface (fixed Compute SKUs / Storage / Network / IAM / Billing) across three trajectory phases — Phase 1 rented public cloud (OCI KR-Seoul primary, AWS ap-northeast-2 fail-open), Phase 2 hybrid KR colo (three-site quorum, per-cell HSM), Phase 3 greenfield 30–50MW mega-DC — so tenants buy capacity now and re-platform never.
- **domain:** node-os / orchestration-scheduling (primary — compute substrate, k8s namespaced cells); finops-cost (secondary — FinOps surface + KR 세금계산서 billing)
- **current_status:** Accepted
- **disposition:** KEEP (Accepted, current, non-conflicting; minor AMEND for stale cross-refs)
- **proposed_resolution:** n/a (not Proposed — it is Accepted).
- **governing:** none for archive. KEEP. (Minor amend only: cross-refs ADR-0042 observability is now SUPERSEDED by ADR-0383 per keystone §1.1 — the reference should point at the live observability ADR; ADR-0044 service-mesh and ADR-0045 database-tier references should be spot-checked against current canon.)
- **truth_flag:** TRUE — the phase-invariant-SKU + own-the-physical-substrate thesis is consistent with SOURCE's "best-of-breed now, own when proven" ratchet and with the Talos/CAPI orchestration canon (ADR-0375). No `foundry` brand contamination (it predates/sidesteps that cluster). One residual reference to "Foundry agent integration" exists only via its relationship to 0029, not in 0028 itself.
- **in_masterplan:** PARTIAL → leaning YES — Accepted compute-substrate ADR with concrete phased deliverables; strong masterplan candidate, but still lacks explicit `masterplan_ref` front-matter (8.8% binding problem, keystone §4). Bind it.
- **tensions:** Mild tension with the founder's apparent leaner posture (own-physical-DC by Phase 3 is the maximal "own everything" claim — keystone §5 #5), but it is staged (rent→colo→greenfield) and gated by the `oya-check-cloud-surface` lane that *forbids* hyperscaler-only SKUs, which is internally disciplined. Cross-ref to ADR-0042 is stale (superseded by 0383).
- **hyperscaler_challenge:** QUESTIONABLE (the greenfield-mega-DC ambition). The phase-invariant-SKU abstraction is exactly right and is what every hyperscaler does (stable API across changing fulfillment). But Google/AWS/Azure would say: a 30–50MW owned greenfield DC is a multi-billion-dollar bet that almost no company at this stage should make — they'd push to stay Phase 1/2 (rent + colo) far longer and treat Phase 3 as optionality, not commitment. KR-residency-from-day-1 via OCI Seoul is the genuinely smart, aligned part. Verdict: the SKU surface is KEEP; the Phase-3 commitment argues for an eventual amend to "optionality, not roadmap commitment" — but it is correctly time-distant and DR-gated, so KEEP now.
- **ai_slop / refinement / consensus_needed:** Not slop — concrete (named colos, KEPCO tariff risk, AZ ≥30km separation per KR FSC). Refinement: fix the superseded ADR-0042 cross-ref. No urgent founder question; Phase-3 commitment is a long-horizon capital question already self-flagged.

---

### ADR-0029 — Connect microservice: dual-context communications (Professional + Personal) replacing Google Workspace / M365 / Naver Works / Kakao Work

- **decision_atom:** Connect (formerly "Workspace," renamed; "axis" framing retired) is the communications/community microservice delivering twelve canonical collaboration apps (Mail/Calendar/Docs/Sheets/Slides/Drive/Meet/Chat/Forms/Sites/Tasks/Notes) on Yrs-CRDT collab + document-format kernels, with per-tenant cell isolation, dual-context (B2B Professional with legal-hold/eDiscovery + B2C Personal with a hard data boundary), KMS-shred-per-object, and migration tooling from incumbent suites — as a cohesion-bound replacement for Google Workspace / M365 / Naver Works / Kakao Work.
- **domain:** product-ux (primary — productivity-suite product); comms-notify (secondary — mail/chat/meet communications)
- **current_status:** Accepted
- **disposition:** AMEND (Accepted and current, but carries two retired-vocab leaks: residual "Foundry agent integration" references and a likely-stale BNF slot-2 naming `connector`/`connector`)
- **proposed_resolution:** n/a (Accepted, not Proposed).
- **governing:** none for archive. KEEP-with-AMEND. The "Foundry agent integration is native" / "Foundry transcription" / "Foundry scheduling agent" references must be retargeted to **intelligence** per ADR-0335 (foundry brand retired). Also the BNF naming block is internally inconsistent: it says slot2 = `connector` (registered µservice) but the title/owner is `oya-connect`, and several crate prefixes are bare `oya-mail-*`/`oya-calendar-*` (no µservice slot) while others are `oya-connect-chat-*` — a naming-discipline drift to reconcile (relates to the keystone's BNF/ADR-0056 canon).
- **truth_flag:** PARTIAL — product decision TRUE and Accepted; STALE in (a) `Foundry`→intelligence references and (b) inconsistent BNF µservice-slot naming (`connector` vs `connect`, bare-`oya-mail-*` vs `oya-connect-*`). Not garbage — these are fixable reference/naming leaks in an otherwise solid ADR.
- **in_masterplan:** PARTIAL → leaning YES — Accepted product ADR with concrete M03 scope (Professional dual-context at M03 GA). But the **M03 milestone label is itself retired vocabulary** (keystone §2: M0–M3/Milestone/MVP RETIRED → descriptive Wave names) — so even binding this to the masterplan requires translating "M03" to a Wave name. Flag.
- **tensions:** Inherits Bominal ADR-0208/0215 (dual-context, legal-hold) via ADR-0060 precedence — cross-repo inheritance to verify is still live. The `Foundry agent integration` claim ties Connect to the retired-brand intelligence substrate (consistent in substance, stale in name). Ontology routing (ADR-0055) reference is fine (object-graph→ontology rename is the *accepted* direction). No hard contradiction.
- **hyperscaler_challenge:** QUESTIONABLE on breadth, ALIGNED on regional wedge. Twelve from-scratch productivity apps to replace Google Workspace + M365 is an extraordinarily broad surface — Google/AWS/Azure would never greenfield a 12-app office suite; they'd partner/integrate. BUT the regional thesis (KR-residency, Hwpx export for KR public-sector tenders, Naver/Kakao migration, per-tenant-cell isolation so data can't be ad-sourced) is a genuine sovereign-suite wedge that no hyperscaler serves. Verdict: the *wedge* is aligned and defensible; the *12-app full-parity* breadth argues for staged scope (its own Negative section concedes "twelve apps is a large surface; each must reach feature-parity-enough"). Amend toward phased app delivery, not archive.
- **ai_slop / refinement / consensus_needed:** Not slop — concrete protocols (JMAP, CalDAV, Yrs CRDT, WebRTC SFU, Hwpx). Refinements: (1) retarget Foundry→intelligence references; (2) fix BNF `connector`/`connect`/bare-prefix naming inconsistency; (3) translate "M03" to a Wave name. **Founder question:** which subset of the twelve apps is true day-1 scope vs aspirational — the parity bar across all twelve is the breadth risk.

---

## Chunk notes

**The headline finding.** This slice is the pre-retirement **Foundry cluster** (0023–0027) frozen in time. The keystone map's single most consequential edge governs every one of them: **ADR-0335 retired the "foundry" brand** (→ `intelligence` for the consumer-AI agent runtime + `governance` for CI/gates/fitness functions, with Governance kept *separate*) and **ADR-0347 renamed `oya-foundry-fitness-*` → `oya-governance-*`**. Consequence: **0023, 0024, 0026, 0027 are AMEND/RATIFY** — their *decision atoms survive intact and are strong*, only the naming is dead. **0025 is the one that is partly WRONG, not merely stale**: its central thesis is "keep everything under ONE Foundry axis," which is the precise over-consolidation ADR-0335 had to walk back. 0025's smoking gun is the line `The oya-foundry-* namespace is governed by the same team as oya-foundry-*` (a self-referential tautology that is almost certainly a botched `oya-governance-*` edit) — plus the fact that 0025 *already* writes its fitness crates as `oya-governance-*` mid-body, proving the rename was landing while the doc was being written.

**Proposed-status accounting (no unaccounted proposals).** All five Foundry ADRs (0023–0027) are `status: proposed`; per the rubric each gets an explicit verdict: **0023 RATIFY**, **0024 RATIFY** (strongest — it closed its open questions in a Resolved section), **0025 RATIFY-with-major-amend** (accept mechanisms, reject the one-axis framing — or re-author as two ADRs), **0026 RATIFY**, **0027 RATIFY-with-scope-caveat** (narrow SC4-autonomous/public-road-AV out). Zero DROPs — none is garbage; all carry a true, hyperscaler-defensible decision atom under the brand fix. 0028 and 0029 are already `Accepted` (KEEP / KEEP-with-amend).

**Retired-vocabulary leak inventory for this chunk (for the rename pass):** `foundry`/`Foundry` brand (all of 0023–0027 + residual refs in 0029); `oya-foundry-*` crate prefix (→ `oya-intelligence-*` for runtime/model crates, `oya-governance-*` for gate/fitness crates); `EVT-FOUNDRY-*` event names; `foundry-*` CI lane prefix (→ `governance-*`); the **"axis"** noun (retired per flat-catalog ADR-0058/0362, explicit in 0028/0029 rewrite notes but still leaking in 0025); **W-/M03 wave-milestone names** (0026 "W-AI-Model-Substrate", 0029 "M03" — M0–M3/Milestone/MVP retired → descriptive Wave names per GLOSSARY L250/L504); and the **"Foundry-Preview" wave** is itself anachronistic post-0335.

**Cross-ref staleness to fix on amend:** 0028 references **ADR-0042 (observability)** which is `superseded` by **ADR-0383** (keystone §1.1) — retarget. 0024 has a duplicated runbook path (`capability-eval-regression.md` listed twice). 0023 cites "ADR-0025 audit chain for emission" where the audit chain is ADR-0003. 0029's BNF naming is internally inconsistent (`connector` slot vs `oya-connect`/bare `oya-mail-*`).

**Masterplan binding (the open founder question, keystone §4).** None of the seven carry `masterplan_ref`/`planning_impact` front-matter (consistent with the repo-wide 8.8%-binding finding). Under **generated-from-ADRs** (`planning-ssot-consolidation.md`): 0023–0027 must be re-authored (brand-corrected) before they can *generate* masterplan content, and 0025 likely splits into two. Under **masterplan-as-authority** (`planning-ssot-drift-prevention.md`): each needs an explicit `masterplan_ref` binding once accepted. I flag every one under both readings and do not assume the resolution — per the keystone directive.

**Hyperscaler verdict pattern across the chunk.** The *method/substrate* decisions are uniformly hyperscaler-ALIGNED (firecracker+wasmtime isolation, eval-gated model promotion, narrow-not-frontier in-house models, phase-invariant SKU abstraction, structural AI safety/anti-scope). The *breadth/ownership* claims are where a hyperscaler reviewer pushes back: one-Foundry-axis fusion (0025), owned SC4-autonomous-robotics + public-road AV (0027), greenfield mega-DC commitment (0028), and a from-scratch 12-app office suite at full parity (0029). The consistent recommendation is **amend toward staged scope**, never archive — the "own everything day-0" breadth (keystone §5 fault-line #5) is the recurring tension, and the disciplined move is to keep the decision while gating the most expansive scope behind future ADRs.
