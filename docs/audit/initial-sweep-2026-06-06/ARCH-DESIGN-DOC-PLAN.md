# ARCH-DESIGN-DOC-PLAN — hyperscaler-grade architecture design doc + live dashboard + 10k-doc reorg + enum enforcement (a PHASE-2 CONSUMER of the Phase-0 firewall)

> **STATUS: APPROVED as the Phase-2 reference (door:one-way, founder 2026-06-07).** PARKED behind the firewall-first campaign — execution resumes per-FE only after Phase-0 firewall + Phase-1 amendments settle (founder pivoted to firewall-first). Until then it mutates NOTHING in source. Original planning note follows: this is a PLAN. It mutates NOTHING in source. Every lane parks for per-unit founder sign-off before any source mutation. Source-backed, generated + drift-gated, honest (no false promises; dogfood rendered intended-vs-wired, never claimed wired).
>
> **Scope synthesized from 4 verified inputs + apex doctrine.** Consensus mode = DELIBERATE (high-risk: door:one-way, 10,069 docs, ADR space with the ADR-0377 collision, false-green firewall risk).
>
> **Apex SSOT:** `synthesis/decision-record-oyatie-canon.md` (D-AUTHORITY-CONVERSATION: this record is apex; un-surfaced ⇒ stale).

---

## (00) CAMPAIGN SUBORDINATION — this plan is a Phase-2 CONSUMER, not a parallel firewall (NEW; closes ARCHITECT/CRITIC must-fix #1 + #2 + the dual-keystone / phase-inversion principle-violations)

**This plan does NOT own a keystone, does NOT build a second registry, and does NOT run a second firewall — the second firewall was a SPECIAL CASE, and the special case is removed (Torvalds discipline: no special cases).** It is the **Phase-2 docs+graph CONSUMER** of the founder-ruled, door:one-way Phase-0 firewall owned by the sibling plans in this same directory. The apex sequencing doctrine (D-SEQUENCE; `synthesis/decision-record-oyatie-canon.md` **:207-210** — heading :204, Phase-0 "4 keystone gates over ONE generated accounting-registry" :207, "never two sweeps over the 346-id space" :208, dependency spine :210; `FULL-SCOPE-OF-WORK.md` spine line 19 + Track-2 line 136; `PHASE-0-FIREWALL-PLAN.md` line 52) is the spine THIS plan obeys, not one it re-shapes:

> **Campaign spine (verbatim, founder-ruled, decision-record :210):** `producer → accounting-registry → gates → amendments → reorg`. ONE generated `accounting-registry.generated.json`, FOUR keystone gates over it (cross-artifact-agreement · total-accounting · staleness-reaper · automation-ratchet, :207), "one registry, four predicates, no four parallel scanners" (`PHASE-0-FIREWALL-PLAN.md` Track-0 spine). **Data-structures-first (Torvalds): the ONE registry is the single load-bearing data structure; every gate is a predicate over it; this plan adds docs-domain predicates over the SAME structure — it adds no new structure, no new spine, no special case.**

**Sibling-plan relationship (NEW — was previously unstated; CRITIC gap #1):** this plan is downstream of all three Phase-0/Phase-1 owners and must not start before they settle:
- `PHASE-0-FIREWALL-PLAN.md` — OWNS the `oya-ci-required` producer (FE-1) + `accounting-registry.generated.json` + the **FOUR keystone gates** (`cloud-ci-cross-artifact-agreement` GATE-1 · `cloud-ci-total-accounting` GATE-2 · `cloud-ci-staleness-reaper` GATE-3 · `cloud-ci-automation-ratchet` GATE-4). **This plan CONSUMES these; it builds none of them as a peer.**
- `PHASE-1-AMENDMENT-LANES.md` — OWNS the gate-verified A-lanes (A-INTEGRITY status-enum/dup-0377/phantom-0150 · A-STRUCT · A-FOUNDRY forbidden-vocab · A-CI · A-DATA Kafka→Pulsar · A-IDENTITY · A-TASTE) over the ~346-id ADR space. **This plan's L3 doc-reorg is BLOCKED until these settle** ("never two sweeps over the ~346-id space").
- `FULL-SCOPE-OF-WORK.md` — the apex critical-path spine; places doc-reorg in **TRACK 2 — PHASE-2 (after gates + amendments land)**, line 136. **This plan's L3 IS that Track-2 work; it is sequenced there, not in a self-chosen Wave 2.**

**The four founder-ruled keystone gates and how THIS plan relates to each (NEW; closes must-fix #1 — disambiguation, Interpretation (b) chosen):**

| Phase-0 keystone gate (owner = PHASE-0-FIREWALL-PLAN) | This plan's relationship | What this plan does NOT do |
|---|---|---|
| **GATE-1 `cross-artifact-agreement`** (G-XA) — over `accounting-registry.generated.json` | **CONSUMES it.** This plan's docs+graph agreement check is a *predicate that runs against the SAME single registry* (the `_taxonomy.json ↔ architecture-graph.json` two-edge-model reconciliation is the docs-domain INPUT to GATE-1's crosswalk, not a second gate). **It is NOT a peer keystone; it does not read a second registry.** | does NOT define its own `cross-artifact-agreement` gate, does NOT name a second registry |
| **GATE-2 `total-accounting`** (G-TA) — Σ over `git ls-files × OWNERS × ADR-justification × masterplan-reachability` | This plan's `completeness-reconciliation` (Σhomed+Σexcluded==10,069) **IS the docs-corpus specialization of GATE-2** — explicitly so; it feeds GATE-2's total, it is not a rival total. | does NOT claim an independent total-accounting authority |
| **GATE-3 `staleness-reaper`** (G-SR) — report→`git mv`, never `rm`; per-class TTL | This plan's `staleness-reaper` is **BOUND to the Phase-0 GATE-3 keystone — it is NOT a free-standing Wave-2 crate** (the prior draft demoted it to an ordinary Wave-2 crate; that demotion is REVERSED here). The authored-prose drift seam (ADR-0019), IDEAS>14d, and past-TTL husks all report INTO GATE-3's single report→`git mv`-never-`rm` mechanism — this plan stands up no parallel reaper. | does NOT run a parallel staleness scanner; does NOT carry staleness as a Wave-2 crate |
| **GATE-4 `automation-ratchet`** (G-AR) — polices Gates 1-3; blocks advisory-claiming-enforced / oya-cli-authority / no-retirement | **INHERITED (was entirely ABSENT in the prior draft — ARCHITECT/CRITIC must-fix #1).** Every [ASPIRATIONAL]/[RUNS-NON-BLOCKING]/[ENFORCED] tag in this plan, the de-CLI mandate, and the "no advisory-shell" honesty ceiling are **policed by GATE-4** — the generator + every gate-crate this plan delivers is a ratchet subject, not a self-certifier. **GENERATOR-RATCHET EQUIVALENT (NEW; must-fix #1 + #4): the SSOT generator itself is a first-class GATE-4 subject — a generator that is never re-run, or re-run on too narrow a trigger, ships stale truth behind a green byte-compare (the freshness failure mode D-DOCTRINE "drift-impossible-by-construction" forbids). GATE-4 therefore requires the generator's regenerate-trigger to fire on EVERY input that mutates its output (the `**/Cargo.toml`+`**/BUCK`-across-`oya/` rescan for the dogfood/`wired` facet — must-fix #4 — plus the graph/template/html paths for the structural facet). An under-triggered generator is "advisory-claiming-enforced" and GATE-4 blocks it.** | does NOT let any gate in this plan self-certify; submits all of them — and the generator's trigger-scope — to GATE-4 |

**Net effect:** there is ONE keystone set and ONE registry (Phase-0's). This plan adds docs-domain PREDICATES that run against that single registry and submit to GATE-4 — it does not stand up a second, differently-shaped firewall. **Explicit relationship of this plan's `cross-artifact-agreement` work to the Phase-0 keystone: it is a CONSUMER / SPECIALIZATION of the single Phase-0 GATE-1 over the single Phase-0 registry — NEVER a peer keystone, NEVER a second registry (Torvalds: no special case for the docs domain — same data structure, one more predicate).** The keystone-set version reconciliation (CRITIC nuance): the current founder-ruled set is the **four GATE-1..4** of `PHASE-0-FIREWALL-PLAN`/`FULL-SCOPE-OF-WORK` (Task #23; decision-record :207); where a newer ruling (D-DISPOSITIONS-RATIFIED / D-GOVERNANCE-CENTRAL) names `status-enum`/`no-dangling-ref`, those are the **KEYSTONE-extension to GATE-1** (`PHASE-1-AMENDMENT-LANES` §0, lines 46-50), not a third set — this plan binds to GATE-1-extended, inventing no third keystone.

### (00.1) HARD EXECUTION PRECONDITIONS — no FE in this plan may fire until these are confirmed (NEW; closes the executability + Step-0 concern)

**Step-0 + signing gate (ARCHITECT's executability charge, answered with CRITIC-verified live evidence — the ARCHITECT's branch-state contradiction is REFUTED, but the precondition it asked for is adopted anyway):**

- **Branch reality RECONCILED on target (verified live for THIS revision, 2026-06-07):** `source` is on branch **`phase0/producer`** with exactly **219** dirty working-tree entries — matching §0 exactly. The ARCHITECT's `feat/oya-ci-tide @79 dirty` is the **STALE 2026-06-06 D-SEQUENCE Step-0 snapshot**, not the current tree. **DO NOT act on the ARCHITECT's branch-contradiction finding** — it reasoned from doctrine text, not the live tree (CRITIC concurs: this finding is REFUTED).
- **D-SEQUENCE Step-0 HAS FIRED (verified):** `git log` on `source` shows `e77f16eb2 chore(phase-0): commit the clean base — canon + WIP authority + Phase-0 seeds` — Step-0 (WIP committed to clean base) is DONE.
- **ADR-0515 RATIFIED (verified):** `869e48ca4 feat(adr-0515): ratify the unified Rust-native CI/CD canon` — the Phase-0 CI/CD producer spec is ratified.
- **Producer EXISTS in SHADOW (verified):** `30f4a0db9 feat(oya-ci): Phase-0 producer — forge-neutral seam + GitHub commit-status poster (SHADOW)` — the `oya-ci-required` producer-runner exists but is NOT yet GREEN/live (shadow), per D-CICD-AUTHORITY.

**Hard precondition (binds EVERY FE in this plan):** no FE-A…FE-F (or FE-D2) may proceed until (1) Step-0 (clean-base commit) is confirmed present on the target authority branch — **confirmed DONE above**; (2) commit-signing for the producer is confirmed (D-EXEC remaining Wave-0 gate — re-verify at FE-A sign-off, T5.5); and (3) **the consuming Phase-0/Phase-1 dependency for that specific FE is itself live** (FE-E binds the GATE-1 enum-extension; FE-F binds a live GREEN producer; **L3's FE-D binds Phase-1-amendments-settled** — see §00.2). The verifier lane (L5/T5.5) carries this precondition check before each FE.

### (00.2) PHASE ORDERING — L3 doc-reorg is gated behind Phase-1-amendments-settled (NEW; closes must-fix #2 + the phase-inversion principle-violation)

**The 10,069-doc reorg (L3) is PHASE-2 work and runs AFTER the Phase-1 ADR-amendment campaign settles — never concurrently.** D-SEQUENCE forbids "two sweeps over the ~346-id space" by name (decision-record **:208**; `FULL-SCOPE-OF-WORK.md` line 45; `PHASE-1-AMENDMENT-LANES.md` L107-111). Moving the `docs/decisions/ADR` tree while `PHASE-1-AMENDMENT-LANES` is amending ADR bodies, **renumbering dup-0377 (A-INTEGRITY)**, repointing **phantom-0150→0183**, flipping **status-enum** values, and running the **A-FOUNDRY forbidden-vocab sweep** in the SAME files would BE that forbidden double sweep. **This is the root-cause sequencing fix (cross-campaign), NOT an intra-plan link-check: the link-break Scenario (3a) is dissolved at the root because the ADR ids/bodies are STABLE before the move — there is no concurrent second sweep to break links against.** The intra-plan link-check survives only as defense-in-depth (§B Scenario 3a + L3/L4), never as the primary defense.

**Hard gate (binds L3 / FE-D):** the entire L3 move (T3.2) is blocked until **Phase-1 amendments are SETTLED** — specifically: A-INTEGRITY (status-enum + dup-0377 renumber + phantom-0150 repoint) merged, A-STRUCT FC-2 retargeted, A-FOUNDRY forbidden-vocab sweep complete, A-CI/A-DATA status sweeps done — each gate-verified per `PHASE-1-AMENDMENT-LANES`. This dissolves the prior draft's Scenario 3a/3b (link-break) **at the root** (the ADR ids/bodies are stable before the move), rather than patching a cross-campaign sequencing violation with an intra-plan link-check. The link-check remains as defense-in-depth, not as the primary fix. (Live confirmation: campaign Task #13 "Amend ADRs" is in_progress and Task #14 "stale-file/reorg-adjacent audit" is pending-AFTER-amendments — the live task list already sequences it this way.)

---

## (0) AUTHORITY LOCUS — read this BEFORE any [ENFORCED] claim (NEW; closes CRITIC/ARCHITECT BLOCKER #1)

**This plan PHYSICALLY RESIDES in `/Users/jasonlee/Developer/linux` but EXECUTES against the source monorepo.** Verified facts (re-verified live for this revision):

- **The `linux` repo (where this file lives) has NO git remotes, only branch `main`, and NO `.github/workflows`** — the only workflow yaml present are vendored third-party (talos/containerd/oci-spec/r-efi under `stack/kubernetes/third-party/`). **NONE** of the infra this plan references (`docs-graph-drift.yml`, `backbone-microservices-ci.yml`, `tools/oya-architecture-graph-generator-app`, `docs/architecture/product-graph.html`, `docs/machine-readable/architecture-graph.json`) exists anywhere in the `linux` tree. A reader/executor standing in `linux` cannot find a single referenced artifact.
- **The execution target is the source monorepo at `/Users/jasonlee/Developer/source`:** `origin = http://forgejo.local/oya-admin/oyatie.git`, `github-mirror = https://github.com/jason931225/oyatie`, **current branch `phase0/producer` with 219 dirty working-tree entries (mid-flight).** Every artifact this plan touches lives there.
- **AUTHORITY-LOCUS RECONCILIATION — the target branch/state vs the D-SEQUENCE Step-0 record (NEW; must-fix #5):** the doctrine TEXT (D-SEQUENCE Step-0, decision-record **:206**) records the Step-0 *snapshot* as "source on `feat/oya-ci-tide`, 79 dirty." The LIVE tree (re-verified 2026-06-07) is **branch `phase0/producer` with 219 dirty** — Step-0 has since FIRED and the campaign advanced one branch past the snapshot. **These are not in conflict: `:206` is a point-in-time Step-0 record, `phase0/producer @219` is its lineal successor authority branch (Step-0 commit `e77f16eb2` is present on it — §00.1).** The ARCHITECT's `feat/oya-ci-tide @79` "non-executability" charge reasoned from the doctrine snapshot text, not the live tree, and is REFUTED (CRITIC concurs). The authority branch for THIS plan's every FE is `phase0/producer` (or its named successor), NOT the `:206` snapshot branch.
- **Authority-flip risk inheritance:** the source monorepo is mid authority-flip (D-MERGE "authority-flip G0 HALT"; the 219 dirty entries are pre-existing Phase-0 producer work, unrelated to this plan). **Every [ENFORCED] / [RUNS-NON-BLOCKING] claim in this plan inherits that in-flight risk and is re-verify-on-target before its FE sign-off.**

**Mandate (HARD precondition to EVERY FE — must-fix #5):** no FE sign-off in this plan may proceed until (a) the named artifact is re-verified present + behaving as tagged ON `source @ phase0/producer` (or its successor authority branch) at sign-off time; AND (b) **D-SEQUENCE Step-0 (clean-base WIP commit — confirmed DONE, `e77f16eb2`) + producer commit-signing are confirmed live on that branch** (Step-0 + signed-commits are not a one-time check — they are re-confirmed before each FE, since the tree is mid-flight). The `linux` audit tree is the AUTHORING locus; `source` is the EXECUTION locus. The verifier lane (L5) carries this locus + Step-0 + signing check (T5.5).

**Enforcement-reality legend (three honest tiers — tightened per CRITIC BLOCKER #2):**
> - **[ENFORCED]** — a REAL gate exists AND is in the live authority's required-status-check set so a RED check BLOCKS merge. **Verified: NO check currently meets this bar.** `branch-protection.yaml` lists ONLY `oya-ci-required` in `required_status_checks`, and `oya-ci-required` has NO live producer (D-SEQUENCE "false-green firewall"); branch-protection.yaml's own header states it "is not Phase-0 exit authority until a trusted … required context is live." So **[ENFORCED] is currently an EMPTY set** and is a *destination tag*, reached only at FE-F go-live.
> - **[RUNS-NON-BLOCKING]** — a real gate-job exists and runs (it can go red on a PR / push) but is NOT in `required_status_checks`, so a PR can merge RED. **Verified: `docs-graph-drift.yml` is here** — it exists, byte-compares the generated dashboard vs committed HTML (`git diff --exit-code`, exit 1 on drift), and triggers on `pull_request` + `push:[dev]` for the graph/template/html paths — BUT it is not a required check and branch-protection disclaims itself as non-authoritative. It is OBSERVATIONAL today, not a firewall.
> - **[ASPIRATIONAL]** — the enforcement tool is NOT yet built or NOT wired into any workflow. Verified: `oya-check-brand-residue.yaml` exists as a catalog yaml referenced by ZERO workflows; the enum/doc-axis/cross-artifact/link-check/completeness gates are LOGIC-defined-but-not-built. These are the DELIVERABLES of L4.

**De-CLI mandate + build-first-cutover-later bridge (D-CLOUD-NATIVE / Δ2 / Δ3 / D-GOVERNANCE-CENTRAL / D-META; closes CRITIC must-fix #4 + ARCHITECT de-CLI principle-violation):** ALL enforcement re-homes to the CaC/CaaS pipeline (Rust gate crates invoked by GitHub Actions live / oya-ci shadow). NO `oya` CLI anywhere in this plan — `oya gate` / `oya gen` / `oya doc` / `oya check` / `oya verify` are SUPERSEDED.

**The live enforcement/promotion authority TODAY is Jenkins, not merely the `oya` CLI (CRITIC-verified):** `source @ phase0/producer` carries `infra/ci/jenkins/Jenkinsfile-oya-ci-gate` (ADR-0380 D3 oya-ci-gate pipeline body), `infra/ci/jenkins/shared-library/vars/oyaCiLane.groovy`, and `infra/ci/jenkins/parallel-lanes-seed.groovy`; `.github/branch-protection.yaml` + ADR-0361 ("Jenkins-native CI/CD") enshrine Jenkins as the staging→production promote pipeline. **The producer for the `oya-ci-required` status check IS the Jenkins `oya-ci-gate` pipeline** (it POSTs the GitHub statuses API authed by `github-ci-token`), but that producer is NOT YET GREEN/LIVE: the Jenkinsfile honestly documents its own failure mode — "the github-ci-token Secret may not exist before [ADR-0380] D4 ships; until then the withCredentials block will fail … Jenkins will mark the build FAILED." So `oya-ci-required` has a runner (Jenkins) but no passing producer until D4 mints the token. **Jenkins (the runner) is itself forbidden vocab per Follow-up Task #25** — it is an eradication TARGET, not the destination authority.

**Honest tension + the bridge (acknowledged, not hidden):** D-CONFORM's verified governance reality is that enforcement TODAY is the legacy `oya verify`/`gate run-all` CLI lanes PLUS the Jenkins `oya-ci-gate` pipeline; the cloud-native required check has no live (green) producer. So this plan's ~10 gate-crates have NO live required-check runner to wire into yet; wiring them collides with the in-flight authority-flip (G0 HALT). **Build-first-cutover-later applied to enforcement itself (the same ratchet doctrine mandates for every substrate, D-META / D-CICD-AUTHORITY "one logic, two runners"):** keep the existing CLI lanes AND the Jenkins `oya-ci-gate` shadow lane RUNNING (parallel/shadow, non-blocking) while the Rust gate-crates are built + RED/GREEN-proven; then cutover gate authority to the CaC pipeline; then retire the CLI + Jenkins lanes (Task #25/#26). Gates are built + RED/GREEN-proven + run-non-blocking FIRST (L4.2/L4.3), and only PROMOTED to required-check [ENFORCED] at FE-F once a live (green) `oya-ci-required` producer exists — never by re-homing onto the forbidden CLI and never by leaving a gate-coverage hole during the cutover. Until then they are [RUNS-NON-BLOCKING], tagged truthfully. **The bridge state (CLI + Jenkins live while the cloud-native pipeline cooks) is the NAMED, deliberate interim — destination = no CLI, no Jenkins; interim = both shadow-live so coverage never drops.**

---

## (A) RALPLAN-DR

### Principles (the non-negotiable constraints the design obeys)

1. **42010 meta-grammar is the spine.** Every architecture-doc facet is an instance of the verified `viewpoint → view → model → stakeholder concern` chain (deep-research finding 1, 3-0). No facet exists without naming its concern, its source framework, and its diagram primitive. Facet selection follows arc42/42010 "adapt-and-omit" — there is no canonical kernel-to-workload ordering (deep-research open-question 3), so we tailor; the dashboard-view-vs-doc-section-vs-overlay split (T1.3) is therefore an explicit FE-A judgment call, not a derivable fact.
2. **Generated where generatable, authored where not — both drift-controlled; honest, never false.** Source-backed data only; counts must match the DISK-VERIFIED body of `_taxonomy.json` (no invented nodes). The dashboard + structural/runtime doc-includes are GENERATED from machine-readable SSOT and drift-controlled; the prose facets (constraints G1, ADRs G2, risks G3, NALSD capacity R4) are NOT generatable (deep-research, 3-0) and are an AUTHORED layer with its own staleness protocol (ADR-0019). Honest-claim ceiling (D-DOCTRINE §K): no advisory-shell claiming-enforced, and no AUTHORED judgment dressed as "generated." Dogfood rendered **intended-vs-wired** (HONEST, disk-derived: 2/87 products IAM-wired, 0/87 products KMS-wired — never "wired"). The authored prose layer is the permanent seam where drift re-enters; the ADR-0019 protocol + staleness-reaper own it, generation does not "solve" it.
3. **Closed enums are the load-bearing backbone; an off-enum value is a gate violation — BUT enum-validity is NOT truth, AND an off-catalog value cannot itself be a gate target.** The ENUM-CATALOG `realm × vertical_stack_layer × clean_arch_layer × hyperscaler_category × microservice_status × edge_kind × doc-4-tuple × facet` closed sets are the single vocabulary all four artifacts draw from (00-ENUM-CATALOG §7 two-layer defense: generator writes enum-valid; gate blocks off-enum even if the generator never ran). **Enum-amendment prerequisite (CRITIC must-fix #2 + ARCHITECT 00-ENUM-CATALOG principle-violation):** the `edge_kind` discriminator this plan makes load-bearing for the dogfood honesty split requires a value the catalog does NOT yet define. VERIFIED: 00-ENUM-CATALOG §2 `edge_kind` closed set is exactly `orchestrates · serves · substrate-call · dogfood · port-adapter · depends` — `cloud-on-cloud` is ABSENT (0 occurrences in the catalog); in `_taxonomy.json` `cloud-on-cloud` exists ONLY as free-text inside the `note` field (5 occurrences), never as a structured field. By this plan's own two-layer defense, a `cloud-on-cloud` record would FAIL `enum-membership`. **Therefore `edge_kind: cloud-on-cloud` (substrate→substrate) MUST be ADDED to the catalog's closed `edge_kind` set BEFORE any gate references it** (owed at FE-E / T4.1; see §7-equivalent two-layer note; this enum-extension is the docs-domain feed to the Phase-0 GATE-1 KEYSTONE-extension per `PHASE-1-AMENDMENT-LANES` §0 — not a separate enum authority). **Explicit limit (CRITIC must-fix #6 / ARCHITECT tension):** enum-membership + byte-compare both pass GREEN on a well-formed-but-FALSE record (`wired:true` is enum-valid and can be a lie). The ONLY defense against a wrong-but-well-formed record is re-derivation from disk. Therefore for the dogfood + count facets, **DISK is the source of truth, not the stored SSOT field** (Principle 2's "generated" applies: the generator COMPUTES these, never trusts an authored value). **The dogfood-honesty derivation path is explicit (CRITIC must-fix #3 / ARCHITECT keystone-misread):** the product-overlay figure (2/87 IAM, 0/87 KMS) is re-derived from the `_taxonomy.json` verification block's DOGFOOD-IAM/DOGFOOD-KMS Cargo.toml/BUCK re-scan ("grep -rIl cloud[-_]kms over oya/ returns empty"), NOT by filtering or counting the `dogfood_edges` array — that array has 26 `wired:true` edges mixing product / cloud-on-cloud / inversion kinds, so counting it would over-report.
   **Disk-rescan trigger SCOPE — the freshness mechanism, not just the logic (NEW; closes must-fix #4 + the D-DOCTRINE automation-freshness principle-violation):** a disk-re-derived overlay is only as fresh as the trigger that re-runs it. The ONLY existing trigger (`docs-graph-drift.yml`, VERIFIED) is scoped to **graph/template/html paths** — if the dogfood rescan inherits that scope, the overlay can render a green `2/87` long after products get wired to `5/87`: a fresh lie behind a green gate, violating D-DOCTRINE "drift-impossible-by-construction." **Mandate:** the `dogfood-honesty` gate's rescan trigger MUST fire on **`**/Cargo.toml` and `**/BUCK` changes across `oya/`** (the path-dep surface that mutates `wired`), NOT only on doc/graph paths. This trigger-scope is a delivery requirement of T4.2b/T4.3 and a GATE-4 automation-ratchet subject (an under-triggered freshness gate is "advisory-claiming-enforced," which GATE-4 blocks). The overlay's truth is owned by the trigger SCOPE + the re-derivation LOGIC together.
4. **Enforcement re-homes to CaC/CaaS — never a CLI; shared LOGIC, two runners; and SUBORDINATE to the Phase-0 keystones (not a peer firewall).** Gate logic = Rust gate crates, invoked by GitHub Actions (live authority) AND oya-ci (shadow) — D-CLOUD-NATIVE / D-CICD-AUTHORITY / D-GOVERNANCE-CENTRAL (PaC/CaC/PaaS/CaaS quartet). **This plan's gate-crates are docs-domain PREDICATES that run against the ONE Phase-0 `accounting-registry.generated.json` and submit to the FOUR Phase-0 keystone gates (GATE-1..4) — they are NOT a second keystone set or a second registry (§00).** Every gate this plan delivers is policed by GATE-4 `automation-ratchet` (no self-certification, no advisory-claiming-enforced). Until a live `oya-ci-required` producer exists, gates ship [RUNS-NON-BLOCKING], not [ENFORCED] (see §0).
5. **Door:one-way → founder sign-off; verifier is a separate lane (no self-approval).** This is a PLAN; nothing mutates source until per-unit founder go. The verifier lane (INTEGRITY/HONESTY-CARRIES) is structurally separate from the authoring lanes (verify-at-each-step, evidence-based; no phantom findings). Irreversible operations (content RETIREMENT) carry their OWN door:one-way sign-off, distinct from reversible operations.

### Decision Drivers (top 3)

1. **Honesty under a vertically-integrated dogfood story that is mostly ASPIRATIONAL — and disk-derived from the VERIFICATION BLOCK, not from the edge array, not SSOT-trusted.** The headline claim ("products dogfood our own cloud substrates") is true as *intent* and false as *wiring*. **Verified from disk (`_taxonomy.json` `verification` block, DOGFOOD-IAM/DOGFOOD-KMS checks):** "DOGFOOD-IAM — FAIL: Only 2 of 87 products have ANY cloud-iam OR oya-identity-product edge in Cargo.toml/BUCK" and "DOGFOOD-KMS — FAIL HARD: ZERO of 87 products reference cloud-kms in ANY Cargo.toml or BUCK file (grep -rIl cloud[-_]kms over oya/ returns empty)." **Derivation discipline (CRITIC must-fix #3):** the 2/0 figure is re-derived from this Cargo.toml/BUCK re-scan, NOT from filtering `taxonomy.dogfood_edges` — that array holds 50 edges, of which **26 are `wired:true`** (NOT 4: the prior draft's "4 wired:true" was a misread a gate-author would inherit), comprising product→substrate edges + 5 `cloud-on-cloud` substrate→substrate edges (free-text `note` only) + 3 substrate→product inversions. Counting the array's `wired:true` edges would over-report dogfood; only the verification-block re-scan yields the true product overlay 2/87 IAM, 0/87 KMS. The doc/dashboard MUST render the dogfood facet as a two-state `intended | wired` overlay, with `wired` DERIVED from a live Cargo.toml/BUCK path-dep scan (never read from the stored edge), or it ships a lie. This is the dominant correctness driver.
2. **Drift is guaranteed without generation + a real gate (D-DOCTRINE: drift = faulty enforcement).** 10,069 docs, the 210-header-vs-208-body / 148-vs-146 count drift (verified: header is stale, body is correct — see Driver-note below), the ADR-0377 collision, dup trees, foundry/forgejo/jenkins/oya-vcs residue. Hand-reconciliation is not the fix; the only fix that survives is generation from SSOT + a gate that blocks the drift on every PR — once the gate is actually a required check.
3. **False-green firewall: NO enforcement is live-blocking yet (D-SEQUENCE).** Verified: `docs-graph-drift.yml` RUNS but is NOT a required check; the only required check (`oya-ci-required`) has no producer. So the plan must DELIVER enforcement (Rust gate crate + GitHub Actions wiring + RED/GREEN fixtures + promotion to required-check) before claiming any facet is [ENFORCED] — and tag every step [ENFORCED] / [RUNS-NON-BLOCKING] / [ASPIRATIONAL] truthfully.

> **Driver-note on the count (resolves ARCHITECT BLOCKER #3 as a PHANTOM; confirms CRITIC):** `_taxonomy.json` HEADER says `microservices:210, adapters:148` (lines 6-7), but the file's OWN verification block (line 4646: "208 microservices parsed, 208 unique slugs, zero collisions"; line 4648: "header counts do not match the body … the realms body enumerates 208 microservices and 146 adapters … This is a counts-vs-body inconsistency") proves **208/146 is the DISK-VERIFIED body count and 210/148 is the STALE HEADER.** Binding gates to 208/146 is therefore verify-at-each-step COMPLIANT. The ARCHITECT's proposed "fix" (gate on 210/148) would enforce the disproven header number — **DO NOT act on it.** The `count-reconciliation` gate must drive the HEADER toward the BODY (208/146), not the reverse. The two zero-microservice products (app-shell-frontend, social) do NOT close the 2-each gap; the gap is header-vs-body bookkeeping, not a real missing-count.

### Viable Options (≥2, bounded pros/cons + invalidation rationale)

**Option 1 — ONE generated artifact-family over a PRIMARY SSOT + a DISK-AUTHORITATIVE overlay + an AUTHORED prose layer (the design doc structural/runtime includes, the 4-view dashboard, the doc-indexes, and the enum-validation all read the SAME machine-readable record for the generatable facets; dogfood/count facets are disk-derived; prose is authored), drift-controlled on GitHub Actions. [CHOSEN]** — **re-titled from "single SSOT" per CRITIC/ARCHITECT: the architecture has THREE honest truth surfaces, not one (see Cons + ADR §E).**
- Pros: the PRIMARY SSOT eliminates cross-artifact drift *by construction for the generatable ~70%* (NOT for the dogfood/count facets, which are disk-authoritative — see Cons); REUSES the EXISTING owned-Rust generator + its already-running `docs-graph-drift.yml` job (lowest blast radius — but the gate is [RUNS-NON-BLOCKING] today, not the "live firewall" the prior draft over-marketed); honest dogfood overlay is a disk-derived two-state field consumed everywhere; satisfies D-DOCTRINE total-accounting + two-layer defense; matches Oxide docs-as-code exemplar + Backstage system-model.
- Cons (acknowledged honestly — THREE truth surfaces, three drift-entry points): **(a)** the generatable layer reads the PRIMARY SSOT, but T2.1 is NOT "add fields to one file" — it is a TWO-SOURCE MERGE (CRITIC must-fix #1): the structural graph lives in `architecture-graph.json` (edges `{source,target,kind}`, NO `wired`/`dogfood`/`intended`) while the dogfood `wired` data lives in a SEPARATE file/tree `_taxonomy.json` `taxonomy.dogfood_edges` (`{product,substrate,wired,note}`) — these two edge models must be reconciled, and the existing `kind:cloud` field must be reconciled against the new `edge_kind` discriminator. **(b)** the prose layer (G1/G2/G3/R4) is NOT generatable and carries ADR-0019 staleness protocol — generation does not eliminate this seam. **(c)** the dogfood + count facets are DISK-authoritative, so the SSOT is NOT the source of truth for those two facets (disk is) — a deliberate, honest concession, not a flaw to hide. The "by construction" drift-elimination claim is scoped to (a)'s generatable layer ONLY; surfaces (b) and (c) are first-class drift-entry points the ADR-0019 protocol + the disk-re-derivation gate own respectively.
- Delivery-risk mitigation (absorbs Option 2's one real virtue — CRITIC recommendation + ARCHITECT synthesis #5): the headline design doc is NOT 100% blocked behind the data contract. The un-generatable prose (G1 constraints, G2 ADRs, G3 risks, R4 NALSD) is authored CONCURRENTLY with FE-B schema design (L2.2 runs in Wave 2 parallel, bound to ADR-0019), so a readable doc slice ships without waiting for the SSOT freeze; the generated structural/runtime includes land when the SSOT freezes.

**Option 2 — Author the design doc as a standalone hand-written hyperscaler doc (arc42 12-section skeleton), keep the dashboard separate, run the doc-reorg as a one-time manual sweep, enforce enums by review checklist.**
- Pros: fastest to a *readable* doc; no schema-design dependency; arc42 skeleton is directly reusable (3-0 verified). **(This virtue is partially absorbed into Option 1's parallel-prose mitigation above.)**
- Cons (invalidating): violates Principle 2 + Driver 2 + D-DOCTRINE head-on for the GENERATABLE data — a hand-written graph/index over a 10k-doc drifting corpus is stale on commit; "enforce by review checklist" IS the advisory-shell-claiming-enforced anti-pattern D-DOCTRINE §K explicitly forbids (honest-claim ceiling). A manual reorg sweep over 10,069 docs is the exact hand-reconciliation D-DOCTRINE rejects. **INVALIDATED for the generatable layer; its fast-readable-prose instinct is absorbed, not discarded.**

**Option 3 — Defer the design doc + dashboard entirely; do only the enum-enforcement + doc-reorg first, then generate the doc later from the cleaned corpus.**
- Pros: enforcement-first aligns with D-SEQUENCE firewall-first instinct ("make enforcement REAL first, then fix the canon THROUGH it"; spine `producer → accounting-registry → gates → amendments → reorg`); cleaner inputs for the eventual doc.
- Cons (invalidating): the founder explicitly RULED ONE auto-generated multi-view dashboard NOW (D-ARCH-DASHBOARD) and the comprehensive design doc is the headline deliverable of THIS request — deferring both contradicts the explicit ask. Also the design doc's facet set is what *defines* the `facet` enum (00-ENUM-CATALOG §5 PENDING:B) and the dashboard views — you cannot finalize the enum without converging the facets, so "enums first, doc later" inverts a real dependency. **INVALIDATED as a wholesale deferral.**
- **Honest wave-order trade-off (CRITIC must-fix #5/#3 + ARCHITECT/D-SEQUENCE principle-violation — STOP claiming "absorbed," STATE the conflict + PRICE the dashboard-now half as PROVISIONAL):** D-SEQUENCE (firewall-first) and D-ARCH-DASHBOARD (dashboard-NOW) genuinely conflict. Option 1 does NOT fully invert to firewall-first; it makes a SCOPED choice, now stated plainly: (1) the **dashboard's 4 views ship NOW from the EXISTING generator** (it already runs; `docs-graph-drift.yml` already byte-compares) — this is the dashboard-now half. **HONEST PRICE (NEW; must-fix #3):** `docs-graph-drift.yml` is VERIFIED [RUNS-NON-BLOCKING] (it byte-compares but is NOT in `required_status_checks`; branch-protection self-disclaims) and the source tree is mid authority-flip (G0 HALT). So "dashboard-now" ships a refreshed artifact behind a gate that **BLOCKS NOTHING** during the single most fragile window. Spending an irreversible founder sign-off (FE-C is door:one-way) on an artifact nothing required-checks is precisely the false-green condition D-SEQUENCE Phase-0 exists to eliminate. **Therefore FE-C is re-priced as PROVISIONAL / REVOCABLE, not a clean door:one-way acceptance** — it is "accepting a refreshed artifact behind a NON-BLOCKING gate during the authority-flip," explicitly RE-VERIFIED once a live (green) `oya-ci-required` producer exists (it converts to firm acceptance only at/after FE-F). This preserves the founder's dashboard-NOW ruling without laundering it into a false-green claim. (2) the **NEW schema-extension (the `intended` field + the `edge_kind:cloud-on-cloud` split + the disk-derived `wired`) and the doc-family generation behind it land ONLY AFTER the docs-domain cross-artifact predicate (feeding Phase-0 GATE-1) is RED/GREEN-proven and run-non-blocking** — this is the firewall-before-NEW-canon half, honoring D-SEQUENCE for the parts that can lie. So the docs-domain cross-artifact predicate is BUILT early-Wave-1 (feeding the Phase-0 GATE-1 keystone, NOT a peer keystone — §00), NOT a Wave-3 afterthought (see L4 + §D). What is NOT done firewall-first: the *existing* dashboard refresh, because its generator+byte-compare already exist — and its acceptance is therefore PROVISIONAL until a live producer exists. This is the deliberate, named trade-off — dashboard-now (existing path, provisional acceptance) AND firewall-before-new-canon (D-SEQUENCE), not a claim that both doctrines were satisfied in full.

**Decision: Option 1.** (Full ADR in §E.)

---

## (B) DELIBERATE — Pre-mortem + Expanded test/verification plan

### Pre-mortem (3 failure scenarios + mitigations)

**Scenario 1 — "The dogfood lie shipped (via the substrate→substrate scoping trap)."** The dashboard/doc renders the products→cloud-substrate dogfood overlay as wired because (a) the generator trusted the stored `wired` field, OR (b) — the verified live trap — the dogfood overlay COUNTED the `wired:true` edges in `_taxonomy.json` `taxonomy.dogfood_edges` as "products dogfood IAM/KMS." **Verified live (CRITIC must-fix #3 — corrected from the prior draft's "4 wired:true" misread):** `dogfood_edges` has **50 edges, of which 26 are `wired:true`** (NOT 4). Counting that array is the trap: of the 26 `wired:true`, **5 are `cloud-on-cloud` substrate→substrate** edges (free-text `note` only: `cloud-compute→cloud-iam`, `cloud-compute→cloud-network`, `cloud-storage→cloud-kms`, `cloud-data→cloud-kms`, `cloud-network-dns→cloud-network`), **3 are substrate→product inversions**, and the rest are product→non-IAM/KMS-substrate edges. The `wired:true` edges TO cloud-iam are exactly **2** (`observability`=product, `cloud-compute`=cloud-on-cloud); TO cloud-kms exactly **2** (`cloud-storage`, `cloud-data`, BOTH cloud-on-cloud, ZERO product). The product-scope truth comes NOT from the array but from the verification block's Cargo.toml/BUCK re-scan: **2/87 IAM, 0/87 KMS**. If the overlay counts the array or conflates substrate→substrate with product→substrate, a reader concludes products run on cloud-iam/cloud-kms. Highest-severity failure (Driver 1 + Principle 2/3).
- **Mitigation (CRITIC must-fix #3 + #6 + #2 + #4):** **(i)** ADD `edge_kind: cloud-on-cloud` to the 00-ENUM-CATALOG closed `edge_kind` set FIRST (it is absent today — see Principle 3; an off-catalog value cannot be a gate target), so the SSOT schema can SEPARATE `edge_kind: dogfood` (product-pod → substrate) from `edge_kind: cloud-on-cloud` (substrate → substrate). The current `note` free-text "cloud-on-cloud" must be PROMOTED to this structured `edge_kind` so the gate can filter on it (it cannot filter free-text reliably). **(ii)** The `dogfood-honesty` gate RE-DERIVES the product overlay from the verification block's Cargo.toml/BUCK re-scan (`grep -rIl cloud[-_]kms over oya/`), NOT by filtering or counting the `dogfood_edges` array, and OVERWRITES any stored `wired` (generator computes, never trusts an authored value). **(iii)** The PRODUCT dogfood overlay renders **2/87 IAM, 0/87 KMS** per the verification block — NOT the 26-edge `wired:true` count and NOT the 2 IAM-targeted edges (one of which, `cloud-compute`, is substrate→substrate, not a product). `intended` edges render visually distinct (dashed/greyed + legend "intended, not wired") from `wired` (solid); the 5 cloud-on-cloud + 3 inversion edges render in a SEPARATE band, never in the product overlay. RED fixture: an edge with stored `wired:true` but no live path-dep MUST be force-corrected to false AND fail the gate; a substrate→substrate edge counted in the product overlay MUST fail. The completeness-reconciliation step cross-checks the rendered product count (2 IAM / 0 KMS) against the taxonomy verification block, NOT the edge array. **(iv) FRESHNESS TRIGGER SCOPE (NEW — must-fix #4):** the rescan MUST be triggered on `**/Cargo.toml` + `**/BUCK` changes across `oya/` (the surface that mutates `wired`), NOT only on the doc/graph paths that `docs-graph-drift.yml` watches today — otherwise the overlay renders a green `2/87` long after products wire to `5/87` (a fresh lie behind a green gate). This trigger-scope is itself a GATE-4 `automation-ratchet` subject (an under-triggered freshness gate is advisory-claiming-enforced). RED fixture: a new product→cloud-iam path-dep added with NO rescan-trigger on the Cargo.toml path MUST be caught (the trigger config is part of the gate's RED-provability).

**Scenario 2 — "Green on byte-compare, but the enums are violated."** The dashboard regenerates and byte-matches (`docs-graph-drift` passes), yet the underlying records carry off-enum values (`application` instead of `usecase`, `kafka`, a 6th `axes_count`, a `foundry`/`oya-vcs` token, a placeholder `(generic)` port). The byte-compare only proves "committed HTML == generated HTML" — it does NOT prove enum-validity. So byte-green gives false confidence (D-CONFORM "merge green while violating policy" generalized to docs).
- **Mitigation:** enum-validity is a SEPARATE gate (`enum-membership`) that runs against the SSOT records BEFORE generation, independent of the byte-compare. Two-layer defense (00-ENUM-CATALOG §7): generator writes enum-valid records AND the gate blocks any off-enum value even if the generator never ran. Each enum gets a RED fixture (an off-enum record MUST fail) and a GREEN fixture (the real corpus passes). The brand-residue gate (`forgejo|foundry|jenkins|oya-vcs|kafka`, Palantir-Foundry carve-out) is wired into a workflow (today [ASPIRATIONAL] — yaml exists, zero workflows reference it) as part of ENUM-ENFORCEMENT. **Honest limit:** this defends against off-enum and generator-failure, NOT against a well-formed lie (Scenario 1's job).

**Scenario 3a — "Doc-reorg MOVE collided with the concurrent Phase-1 ADR-amendment sweep (the 'two sweeps over the 346-id space' violation) and broke every cross-link."** The Diátaxis folder-topology execution rewrites paths WHILE `PHASE-1-AMENDMENT-LANES` is amending ADR bodies, renumbering dup-0377, repointing phantom-0150, and flipping status-enum values in the SAME files; internal links, ADR cross-refs, and the move-manifest either break, rebase-churn, or produce an unmergeable ADR space — violating verify-nothing-lost + total-accounting + reachability AND D-SEQUENCE "never two sweeps over the 346-id space."
- **Mitigation — ROOT FIX is sequencing, not a link-check (CRITIC must-fix #2 + #6 + ARCHITECT phase-inversion + total-accounting principle-violations):** **PRIMARY (NEW — dissolves the collision at the root):** L3's entire move (T3.2 / FE-D) is GATED behind **Phase-1-amendments-settled** (§00.2) — A-INTEGRITY (status-enum + dup-0377 renumber + phantom-0150 repoint), A-STRUCT FC-2, A-FOUNDRY forbidden-vocab sweep, A-CI/A-DATA all merged + gate-verified per `PHASE-1-AMENDMENT-LANES`. The ADR ids/bodies are STABLE before the move; there is no concurrent second sweep. This is the founder's literal Phase-1-before-Phase-2 order (`FULL-SCOPE-OF-WORK` Track-2 "after gates + amendments land"). **SECONDARY (defense-in-depth, not the primary fix):** the move is driven by an AUTHORED `(category,axis,doc_type)→folder` transform table (NOT "generated" — see Scenario 3b note), mechanized AFTER founder freeze. The seven inventory TSVs are the per-doc INPUT (they carry `{path,doc_type,category,axis,status,title}` only — NO target-home column, verified). **Totality proof BEFORE FE-A freeze (blocking):** the transform table MUST be proven a TOTAL FUNCTION over the actual **44 source folders × the doc-4-tuple actually present in the 7 TSVs** — every input maps to exactly one home OR an explicit excluded-with-reason bucket; an incomplete transform table mechanized at scale silently strands docs, so partiality is a freeze-blocker. **Definable GREEN condition (blocking):** `completeness-reconciliation` (= docs-domain GATE-2 `total-accounting`) asserts `Σ(homed) + Σ(excluded-with-reason) == census 10,069`, but that GREEN condition is UNDEFINABLE until the **214-survey-orphan fold-vs-exclude disposition** AND the **18 intentionally-double-listed docs** canonicalization are RESOLVED — both are currently Open Questions. Their disposition MUST be closed before FE-A so the gate has a definable passing state (a total-accounting gate whose pass-state is undefined is not enforceable). The path-rewrite is mechanical and **reversible**. A `link-check` gate (e2e-equivalent) must be GREEN (zero broken internal links) before the move-PR is eligible (hard intra-wave edge — see §D + L3/L4) — but it is now DEFENSE-IN-DEPTH behind the amendments-settled root fix, not the primary defense the prior draft made it. The 214 orphans are folded into an 8th "tools/libs/tests/benchmarks" survey or explicitly excluded-with-reason (no silent drop). dup-0377 + the 18 dupes resolve to a canonical home as part of A-INTEGRITY (Phase-1), BEFORE the move.

**Scenario 3b — "Irreversible RETIREMENT smuggled into the reversible move shipped a baby-with-bathwater."** (NEW — splits the prior bundled scenario per ARCHITECT/CRITIC must-fix.) The 1,184 "likely-stale" + 72 dup + 21 superseded docs are DELETED on the strength of a TSV heuristic `status` label (verified: "likely-stale" is a heuristic status-column value, NOT a verified per-doc disposition). A link-rewrite is reversible; content deletion is a ONE-WAY door. Genuinely-worth-preserving substance is dropped, violating verify-nothing-lost / total-accounting / no-baby-with-bathwater (D-AUTHORITY-CONVERSATION salvage-before-drop).
- **Mitigation:** RETIREMENT is split out of the move lane entirely (its own gate FE-D2). Each cohort (stale / dup / superseded) gets a VERIFIED per-cohort (or per-doc for ambiguous cases) disposition with salvage-before-drop, NOT a TSV label. The retirement is a separate door:one-way founder sign-off, distinct from the reversible-move sign-off (FE-D). Nothing is deleted until its disposition is independently verified by L5.

### Expanded test / verification plan (unit / integration / e2e / observability equivalents for docs + dashboard)

| Layer | Check | What it proves | RED fixture (must fail) | GREEN (must pass) | Tag |
|---|---|---|---|---|---|
| **unit** | `enum-membership` | every SSOT record field ∈ its closed set (realm, vertical_stack_layer, clean_arch_layer, hyperscaler_category, status, edge_kind, doc-4-tuple, facet). **PREREQ: `edge_kind` closed set must first be AMENDED to ADD `cloud-on-cloud` (T4.1/FE-E) — it is absent today, so the gate would otherwise reject the very value the dogfood split needs** | a record with `clean_arch_layer: application` / `kafka` / 6th axis / an `edge_kind` NOT in the amended set | the real corpus normalized to `usecase`, 7 axes, `edge_kind ∈ {orchestrates,serves,substrate-call,dogfood,port-adapter,depends,cloud-on-cloud}` | [ASPIRATIONAL]→build+wire (after enum amendment) |
| **unit** | `placeholder-ban` | no `(generic)` / `unspecified` port·tech in built services; flagged for spec-only | a built service with port `(generic)` | spec-only carve-out documented | [ASPIRATIONAL] |
| **unit** | `count-reconciliation` | HEADER counts driven TO match the DISK-VERIFIED BODY (208 ms / 146 adapters); fails if header≠body | header `microservices:210` ≠ body 208 | header == body == **208/146** (body is truth; header is the stale value to correct) | [ASPIRATIONAL] |
| **integration** | docs-domain feed to **GATE-1 `cross-artifact-agreement`** (KEYSTONE; CONSUMES the Phase-0 registry — NOT a peer gate, §00) | reciprocal edges + status/edge enums agree across `_taxonomy.json` ↔ masterplan ↔ `architecture-graph.json`, reconciled (the TWO edge models — must-fix #1), fed INTO the single `accounting-registry.generated.json` crosswalk that GATE-1 (G-XA) evaluates — NOT a second registry | a `serves` edge with no reciprocal; a dogfood `wired` value in one source disagreeing with the other | all edges reciprocal; the two edge models agree; GATE-1 crosswalk GREEN over the one registry | [ASPIRATIONAL] (**feeds the founder's Phase-0 GATE-1 keystone — built early-Wave-1 BEFORE the NEW honesty schema per D-SEQUENCE; subordinate, not peer — must-fix #1/#5**) |
| **integration** | **GATE-2 `total-accounting`** (KEYSTONE; this plan's `completeness-reconciliation` IS its docs-corpus specialization) | Σ(homed)+Σ(excluded-with-reason) over the doc corpus feeds the Phase-0 GATE-2 `git ls-files × OWNERS × ADR-justification × masterplan-reachability` total — one total, not a rival | a census doc in no home / unowned / unreachable | 10,069 fully accounted, feeding GATE-2 GREEN | [ASPIRATIONAL] (docs-domain specialization of Phase-0 GATE-2 — must-fix #1) |
| **observability** | **GATE-3 `staleness-reaper`** (KEYSTONE; BOUND to Phase-0 GATE-3, not a free Wave-2 crate) | the authored-prose ADR-0019 drift seam + IDEAS>14d + past-TTL docs report INTO Phase-0 GATE-3 (report→`git mv`, never `rm`; protected classes never reaped) | an IDEA doc >14d unpromoted; an over-TTL husk | TTLs current; all reachable; reported to GATE-3 | [ASPIRATIONAL] (bound to Phase-0 GATE-3 — must-fix #1) |
| **meta** | **GATE-4 `automation-ratchet`** (KEYSTONE; INHERITED — was ABSENT in prior draft; polices every gate this plan delivers) | no gate in this plan self-certifies; no advisory-claiming-enforced; no `oya`-CLI-authority; the disk-rescan trigger SCOPE is wide enough (Cargo.toml/BUCK); every [ENFORCED] tag has a wired buck2 target | a gate tagged [ENFORCED] with no wired target; an under-triggered freshness gate; an advisory-shell claiming enforced | every gate-crate + the generator + the trigger-scope pass GATE-4 ratchet | [ASPIRATIONAL] (INHERITED Phase-0 GATE-4 over this plan's deliverables — must-fix #1) |
| **integration** | `dogfood-honesty` | every `edge_kind:dogfood` (product→substrate) `wired` is RE-DERIVED from the verification-block Cargo.toml/BUCK re-scan (NOT from counting the 26-`wired:true` `dogfood_edges` array) and OVERWRITES any stored value; `edge_kind:cloud-on-cloud` (5 substrate→substrate) + the 3 inversions are SEPARATE, never counted in the product overlay; **rescan TRIGGER scoped to `**/Cargo.toml`+`**/BUCK` across `oya/` (must-fix #4), not doc/graph paths** | a `dogfood` edge stored `wired:true` with no path-dep (must force-false + fail); a `cloud-on-cloud` substrate→substrate edge counted in the product overlay (must fail); the overlay reporting the 26-edge array count or the 2-IAM-targeted edges (1 is substrate) instead of the 2/0 verification-block product figure; **a new Cargo.toml path-dep that does NOT re-trigger the rescan (must fail — proves the trigger scope)** | **2/87 IAM-wired / 0/87 KMS-wired** rendered in the product overlay (re-derived from the verification block); the 5 cloud-on-cloud + 3 inversion edges in a separate band; rescan fires on Cargo.toml/BUCK changes | [ASPIRATIONAL] (Driver 1; DISK-via-verification-block is truth, not the edge array, not the stored SSOT field; GATE-4-policed for trigger-scope) |
| **integration** | `doc-axis` / `no-shadow-docs` | every doc ∈ ADR-0388 7-axis closed set + allowed subdirs; manual docs bind ADR-0019 protocol | a doc off-axis / shadow-doc | corpus folded to 7 axes | [ASPIRATIONAL] |
| **integration** | `brand-residue` | zero LIVE `forgejo·foundry·jenkins·oya-vcs·kafka` NODES (Palantir-Foundry carve-out; flagged-as-eradication-target TEXT is allowed and badged, not a violation) | a record/doc with a LIVE `foundry` node | clean (carve-out + badged eradication-target text preserved) | [ASPIRATIONAL] (yaml exists, NOT wired) |
| **e2e** | `link-check` | zero broken internal links after reorg path-rewrites | a moved doc with a dangling rel-link | all links resolve | [ASPIRATIONAL] |
| **e2e** | `drift-gate` (byte-compare) | committed dashboard HTML == generator output from SSOT (`git diff --exit-code`) | hand-edit the committed HTML | regenerated == committed | **[RUNS-NON-BLOCKING]** (live `docs-graph-drift.yml` runs on PR/push:dev but is NOT a required check; promotes to [ENFORCED] only at FE-F) |
| **e2e** | `generator-golden / regeneration` | the existing `tests/golden.rs` + `tests/regeneration.rs` still pass after the schema EXTENSION (baked `const GRAPH` carries the 5 keys in order; render idempotent; deep-equals the audit golden `_graph-data.json`) | schema change that breaks the golden deep-equal or 5-key order | golden + regeneration green post-extension | [RUNS-NON-BLOCKING] (repo-local always-on tests; back-compat surface for the schema migration) |
| **e2e** | `completeness-reconciliation` (= docs-domain **GATE-2 `total-accounting`**, see keystone row above) | Σ(homed)+Σ(excluded-with-reason) == census 10,069; every node in dashboard ∈ taxonomy; feeds the single Phase-0 GATE-2 total. **GREEN condition is UNDEFINABLE until the 214-orphan fold-vs-exclude + 18-double-listed canonicalization disposition is CLOSED before FE-A (must-fix #6); and the transform table must be a proven TOTAL function over 44 folders × the 7-TSV 4-tuple** | a census doc in no home; a transform-table input with no target AND no excluded-with-reason | 10,069 fully accounted; 214 orphans folded/excluded-with-reason; 18 dupes canonicalized | [ASPIRATIONAL] (docs specialization of Phase-0 GATE-2; pass-state definable ONLY after orphan/dupe disposition closes) |
| **observability** | `visual-verdict` | dashboard renders legibly across the 4 views; intended-vs-wired visually distinct; product overlay shows 2 IAM / 0 KMS (not the 4-edge raw count) | screenshot shows dogfood solid (lie) OR product overlay counting cloud-on-cloud edges | screenshot: dashed intended + solid wired + product overlay 2/0 + separate cloud-on-cloud band | manual visual QA (separate verifier) |

> All "[ASPIRATIONAL]→build+wire" rows are the enforcement DELIVERABLES of the ENUM-ENFORCEMENT lane: each is a Rust gate crate (shared logic) invoked by a GitHub Actions workflow + oya-ci shadow, proven by its RED/GREEN fixture pair. A check is not allowed to be claimed [ENFORCED] until BOTH (a) its RED fixture is demonstrated to actually go red AND (b) it is promoted into `required_status_checks` behind a live `oya-ci-required` producer (FE-F). Until (b), the truthful tag is [RUNS-NON-BLOCKING].

---

## (C) LANE DECOMPOSITION

> Five lanes, fractal parallel/sequential (D-LANES). Each lane: wave · tasks (seq/parallel) · dependency edges · verification gate · founder sign-off points · honesty tag. The verifier lane (L5) is structurally separate — no authoring lane self-approves. **All tags are AS-OF source @ phase0/producer (see §0); re-verify on target at each FE.**

### LANE 1 — CONVERGE-FACETS  (Wave 0; mostly read-only/authoring; [ASPIRATIONAL] until the facet enum is gated)
**Purpose:** finalize the closed `facet` enum (00-ENUM-CATALOG §5 PENDING:B) by mapping each design-doc facet to its 42010 framework + diagram primitive (deep-research finding 9), and decide first-class dashboard-view vs doc-section vs overlay (an explicit judgment call per Principle 1, NOT derivable).
- **T1.1 (seq):** Take the deep-research RECOMMENDED FACET SET (S1–S5 structural, R1–R4 runtime, X1–X6 cross-cutting, G1–G5 governance — verified 1:1 onto the §5 candidate set, each facet 3-0 primary-sourced) and prune/validate the §5 candidate set against it. Resolve the candidate list (`system-context · stack-deployment · organization-hierarchy · interrelationship · runtime-flows · data-architecture · security-isolation · reliability-failure-domains · performance-scaling · cost-finops · operational-observability · sustainability · ports-adapters-ratchet · multitenancy-dogfooding · compliance-owed-depth · governance-policy · program-sequencing · decisions-adrs · risk-open-questions`) into a CLOSED set, each row = `{facet, 42010-viewpoint, source-framework, diagram-primitive, stakeholder-concern, dashboard-view|doc-section|overlay, generatable|authored}`. The `generatable|authored` column is load-bearing: it marks the prose facets (G1/G2/G3/R4) that L2.2 authors in parallel, NOT generated.
- **T1.2 (parallel with T1.1) — the HYPERSCALER LENS, made first-class (NEW framing; the directive lens):** Map the commonly-missed facets explicitly (the differentiators, all 3-0-verified), and make the hyperscaler facets FIRST-CLASS (not appendices): (a) **declarative spec-vs-status + reconciliation/choreography** as the control posture (Borg/Omega/k8s; `plane` control/data/analytics) — the whole architecture is read as desired-state + reconcilers, not imperative steps; (b) **cells + blast-radius as a first-class facet** — quantified blast-radius "survive any two failures" (X2) rendered as cell/failure-domain boundaries on the reliability view (reliability-failure-domains facet), so the doc states the blast-radius of every substrate, not just lists components; (c) **NALSD concrete capacity numbers (R4, AUTHORED) as a first-class facet** — per-substrate capacity math (QPS, storage, fan-out, headroom) is a named doc section, not an afterthought, owed at each vertical's M0 gate; (d) hardware-coupled deployment layering (Oxide; `vertical_stack_layer` L0–L9); (e) **the AWS Well-Architected pillars + Google/Meta-monorepo doctrine as the cross-cutting evaluation grid** — operational-excellence, security-isolation, reliability-failure-domains, performance-scaling, cost-finops, sustainability (X1–X6) are mapped 1:1 onto the pillars so each cross-cutting facet names its pillar; (f) **total-accounting as a first-class facet** (every node owned + justified + reachable + TTL'd — feeds Phase-0 GATE-2); (g) sustainability (X5); (h) the self-hosting/dogfooding dependency (G4 → the intended-vs-wired overlay). The Well-Architected/pillar mapping + the cell/blast-radius + NALSD facets are AUTHORED judgment rows (Principle 1, `generatable|authored`=authored where prose), not derivable from the graph.
- **T1.3 (seq, after T1.1/T1.2):** Decide the 4 dashboard views are the D-ARCH-DASHBOARD-ruled set (products+cloud verticals graph · tech-stack now/planned/ideal · masterplan · lanes+sequencing DAG) and bind the remaining facets as doc-sections or overlays (e.g. dogfood = overlay on view 1, with the product vs cloud-on-cloud bands of Scenario 1). This split is an FE-A judgment call (no canonical kernel ordering exists — deep-research open-q 3).
- **Dependency edges:** none inbound (Wave 0 root). Feeds → DESIGN-DOC+DASHBOARD (L2), ENUM-ENFORCEMENT (L4 facet enum).
- **Verification gate:** verifier (L5) confirms every facet row cites a 3-0-verified framework (no unsourced facet), every commonly-missed facet is present, and the `generatable|authored` column is correct. [ASPIRATIONAL] (the `facet` enum becomes a real gate only once L4 wires it).
- **Founder sign-off:** ⛳ FE-A — the FROZEN closed facet enum + the view/section/overlay split + the `(category,axis,doc_type)→folder` transform table (door:one-way; defines the doc + dashboard + §5 enum AND the L3 move; this transform table is an FE-A-class AUTHORED artifact). **FE-A freeze prerequisites (must-fix #6):** before the founder signs, (a) the transform table is PROVEN a TOTAL function over the actual 44 source folders × the doc-4-tuple present in the 7 TSVs (every input → exactly one home OR explicit excluded-with-reason); (b) the 214-survey-orphan fold-vs-exclude disposition AND the 18-double-listed canonicalization are RESOLVED so `completeness-reconciliation`'s Σ==10,069 GREEN condition is DEFINABLE. An incomplete table or open disposition blocks the freeze.

### LANE 2 — DESIGN-DOC + DASHBOARD  (Waves 1–2; the headline deliverables; mixed tags)
**Purpose:** the comprehensive hyperscaler-grade architecture design doc (arc42-skeleton, colocated docs-as-code) AND the live 4-view dashboard, with the GENERATABLE structural/runtime layer driven from one `architecture-graph` SSOT (drift-controlled) and the AUTHORED prose layer written in parallel (ADR-0019-bound).
- **T2.1 (seq, Wave 1) — MERGE TWO EDGE MODELS, not "add fields to one file" (CRITIC must-fix #1 — corrects the prior draft's file conflation):** **VERIFIED on `source @ phase0/producer`: `docs/machine-readable/architecture-graph.json` exists (81,325 bytes; top keys `_meta/verticals/techstack/lanes`) BUT its `verticals.edges` schema is `{source, target, kind}` (50 edges; e.g. `{"source":"cloud-compute","target":"cloud-k8s","kind":"cloud"}`) — it has ZERO `wired`, ZERO `dogfood`, ZERO `intended` edge fields** (the prior draft's "already HAS `wired` and `dogfood`" is FALSE; the 2 "dogfood" / "wired" substrings are description prose, confirmed against the audit golden mirror `_graph-data.json`). **The dogfood `wired` data lives in a DIFFERENT file in a DIFFERENT tree:** `_taxonomy.json` `taxonomy.dogfood_edges` (50 edges, schema `{product, substrate, wired, note}`). So T2.1 is a **TWO-SOURCE MERGE/RECONCILE**, not a single-file extension:
  - **PRECONDITION — PROVENANCE OF `_taxonomy.json` (closes the final close-out must-fix; bound to FE-B + T5.5):** `_taxonomy.json` is an **AUTHORED AUDIT artifact in the `linux/` authoring tree — it does NOT exist on `source`.** It is NOT blindly ported. Resolve per-facet BEFORE the merge runs on `source`: **(a) disk-derivable facets** — `wired`, the 208/146 counts, the microservice/layer/adapter enumeration — are **RE-GENERATED on `source` by the generator** from `oya/`+`cloud/` crates + `**/Cargo.toml`/`**/BUCK` (the §83 rescan trigger), never ported (disk is the source of truth — Principle 2/3); **(b) authored-judgment facets** — the hyperscaler-`category` assignment + the `intended` dogfood-edge set + group structure — are PORTED onto `source` as a **FROZEN FE-A-class artifact with founder sign-off** (same discipline as the doc transform table: an authored judgment mechanized after, not "generated"). FE-B must state, per facet, authored-ported vs disk-regenerated. The `linux/_taxonomy.json` is the authoring REFERENCE that PROVED both (incl. the 2/87-IAM, 0/87-KMS verification scan); the `source` SSOT is (re)built on `source` so the merge has BOTH inputs present on the execution locus. (Torvalds: compute the disk-authoritative facets, freeze+sign the authored ones — no cross-tree trust of an authored file.)
  - **reconcile the existing `kind` field (value `cloud`) on `architecture-graph.json` edges against the NEW `edge_kind` discriminator** — state which is renamed/superseded/dual-kept; an unreconciled `kind` vs `edge_kind` is itself a drift surface;
  - **PREREQ: amend 00-ENUM-CATALOG `edge_kind` to ADD `cloud-on-cloud` (T4.1/FE-E) BEFORE this field becomes a gate target** (it is absent today — Principle 3, must-fix #2);
  - MERGE the `_taxonomy.json` `dogfood_edges` (`{product,substrate,wired,note}`) into the unified SSOT as `edge_kind:dogfood` (product→substrate) vs the promoted `edge_kind:cloud-on-cloud` (substrate→substrate, currently only `note` free-text — must be promoted to a structured field);
  - add `intended:bool` + a `wired:bool` that the generator DERIVES from the disk Cargo.toml/BUCK re-scan (never trusts a stored value) — Pre-mortem S1;
  - add the FE-A facet keys + 00-ENUM-CATALOG §1–§4 enum keys;
  - bind counts to the DISK-VERIFIED body **208/146, NOT the stale header 210/148** (Driver-note);
  - update `tools/oya-architecture-graph-generator-app` + `tests/golden.rs` (deep-equals the audit golden `_graph-data.json`) + `tests/regeneration.rs` (baked `const GRAPH` 5-key order, idempotent render) IN LOCKSTEP, or the drift/regeneration proofs go red. This is a SCHEMA MIGRATION + a two-source merge with a real back-compat surface — acknowledged, not hidden behind "extend one file."
  - NO invented nodes.
- **T2.2 (parallel WITH T2.1+FE-B, Wave 1→2) — the AUTHORED prose layer, NOT blocked behind the schema (ARCHITECT synthesis #5 / CRITIC recommendation):** Author the design doc prose facets that deep-research (3-0) says are NOT generatable, CONCURRENTLY with the FE-B schema design, so the headline deliverable is not 100% gated on the data contract: §1 goals · §2 constraints (G1) · §4 solution strategy · §9 ADRs (G2) · §11 risks/open-questions (G3) · §10 NALSD capacity numbers (R4) · §12 glossary. Each prose facet binds the ADR-0019 staleness protocol (it is the permanent drift seam; the staleness-reaper owns it). The generated structural/runtime INCLUDES (§3 context S1, §5 building-block S2/S3, §6 runtime R1/R2, §7 deployment + hardware-coupled layering R3 `vertical_stack_layer`, §8 crosscutting X1–X6) land AFTER FE-B, as generated includes from the SSOT. Carry the dogfood-intended-vs-wired honesty in §7/§8 + G4.
- **T2.3 (parallel, Wave 2):** Rebuild the **dashboard** (`docs/architecture/product-graph.html`) ON-CANON via the existing generator. **VERIFIED current state (re-scopes the prior "remove foundry/Kafka nodes" claim):** the current source graph + HTML already carry NO live foundry/Kafka NODES — `_meta.on_canon` declares them out, eventing is already Pulsar-from-day0, and the off-canon tokens that remain (9 in JSON, 27 in HTML) are legitimate: the `on_canon` negation string, lane descriptions naming eradication TARGETS (badged `⚠ pending-amendment`, text never rewritten), a `jenkins-adapter` ADR reference, and the Palantir-Foundry carve-out. So T2.3 = (a) VERIFY no live off-canon node re-enters; (b) render the dogfood overlay with the product (2/0) vs cloud-on-cloud bands; (c) preserve the drift-badge honesty (eradication-target text is "plan-to-fix, not drift"); (d) confirm the `oya doc graph emit` ref is absent (verified already gone). Designer-grade polish; the 4 ruled views.
- **Dependency edges:** ← CONVERGE-FACETS (FE-A frozen facet enum + transform table). T2.3 + generated includes ← T2.1 (extended SSOT schema). T2.2 prose runs PARALLEL to T2.1. Shares the SSOT with DOC-REORG indexes (L3) + ENUM-ENFORCEMENT records (L4).
- **Verification gate:** **drift-gate (byte-compare) is [RUNS-NON-BLOCKING]** — `docs-graph-drift.yml` runs on PR/push:dev and goes red on drift, but is NOT a required check (it does NOT block merge today; promotes to [ENFORCED] at FE-F). PLUS the always-on `golden.rs`/`regeneration.rs` must stay green post-extension; PLUS `visual-verdict` (separate verifier: 4 views legible, intended-vs-wired distinct, product overlay 2/0 not 4-edge, on-screen counts == taxonomy body) + `dogfood-honesty` gate (L4) green.
- **Founder sign-off:** ⛳ FE-B — the EXTENDED SSOT schema + generator + golden/regeneration update (door:one-way; the apex data contract migration). ⛳ **FE-C — the generated dashboard + authored design doc accepted PROVISIONALLY / REVOCABLY (NOT a clean door:one-way — must-fix #3).** The dashboard-now half is "accepting a refreshed artifact behind a NON-BLOCKING gate (`docs-graph-drift.yml` — present, byte-compares via `git diff --exit-code`, but NOT in `required_status_checks`; branch-protection self-disclaims) during the authority-flip (G0 HALT)." It is REVOCABLE and RE-VERIFIED once a live GREEN `oya-ci-required` producer exists; it converts to firm acceptance only at/after FE-F (T5.6c rollback path applies). The founder's dashboard-NOW ruling (D-ARCH-DASHBOARD) is PRESERVED — it is not laundered into a false-green clean door:one-way. (The authored design-doc prose half of FE-C — being authored, not byte-gated — carries the ADR-0019 staleness protocol, not the non-blocking-gate caveat.)

### LANE 3 — DOC-REORG  (PHASE-2 — runs AFTER Phase-1 amendments settle, NOT in a self-chosen Wave 2; the 10,069-doc reorganization; [ASPIRATIONAL] gates; AUTHORED-transform-then-mechanized; reversible MOVE split from irreversible RETIRE)
**Purpose:** execute the unexecuted Diátaxis folder-topology + fold every doc into the ADR-0388 7-axis closed set + the unified 4-tuple per-doc record (D-DOCORG). **This is the campaign's Track-2 / PHASE-2 doc-reorg (`FULL-SCOPE-OF-WORK` line 136 "after gates + amendments land"); it is NOT a peer of the Phase-1 amendment campaign — it is sequenced strictly AFTER it (§00.2).**
- **T3.0 (HARD PRECONDITION — NEW; must-fix #2 + phase-inversion principle-violation):** L3 may not open until **Phase-1 amendments are SETTLED** per `PHASE-1-AMENDMENT-LANES`: A-INTEGRITY (status-enum value live in G-XA + dup-0377 renumber + phantom-0150→0183 repoint), A-STRUCT FC-2 retargeted, A-FOUNDRY forbidden-vocab sweep complete, A-CI/A-DATA status/Kafka→Pulsar sweeps merged — each gate-verified. Moving the `docs/decisions/ADR` tree while these are amending the SAME ADR bodies/ids is the "two sweeps over the ~346-id space" D-SEQUENCE forbids by name. The verifier (T5.5) confirms amendments-settled on target before FE-D.
- **T3.1 (seq, after T3.0) — pre-move blocker resolution (reversible setup):** Use the seven inventory TSVs as the per-doc INPUT (verified header `{path,doc_type,category,axis,status,title}` — NO target-home column). Resolve pre-move blockers FIRST: the 18 cross-area dupes → canonical home, the dup trees (`_legacy-foundry`, `release/`, `ARCH`/`ARCHITECTURE`, threat/dpia stubs) → canonical home, fold the 214 survey-orphans (tools/agent-skills 100, governance-lanes 65, libs/oya-governance-* 19, tests/cross-microservice 8, benchmarks 8, + loose top-level) into an 8th survey or exclude-with-reason (no silent drop). **(dup-0377 renumber is RESOLVED upstream in A-INTEGRITY / Phase-1, per T3.0 — it is NOT re-done here; that would be a second sweep.)** **RETIREMENT is NOT in this task — see T3.4.**
- **T3.2 (seq, after T3.1) — AUTHORED-transform-then-mechanized (CRITIC/ARCHITECT must-fix #4; NOT labeled "generated"):** Apply the FE-A-frozen `(category,axis,doc_type)→folder` transform table (an AUTHORED artifact, signed at FE-A) to MECHANIZE the 44→6/8 Diátaxis home assignment + the per-doc disposition of orphans/collision/dupes. The TSVs lack a destination column, so the move-manifest is AUTHORED judgment encoded in the transform table, then mechanically applied — it is NOT derived-from-TSV-as-SSOT. The path-rewrites are mechanical and REVERSIBLE. Emit the per-axis + per-subject `INDEX.md` (Diátaxis-grouped) + the master ADR-INDEX, regenerated from the TSVs (the INDEX generation IS generated-from-TSV; the FOLDER assignment is authored-then-mechanized — honest distinction).
- **T3.3 (seq, after T3.2):** Stamp every doc with the unified 4-tuple `{doc_axis × diataxis_quadrant × hyperscaler_category(subject) × doc_catalog_tier}` + owner_team + agent_authoring_allowed + generated|manual; manual docs declare ADR-0019 binding (un-accounted ⇒ gate violation).
- **T3.4 (seq, SEPARATE one-way door) — RETIREMENT (CRITIC/ARCHITECT must-fix #5; split out of the reversible lane):** The 1,184 likely-stale + 72 dup + 21 superseded set is a candidate-for-retirement list ONLY. "likely-stale" is a heuristic TSV `status` label (verified), NOT a verified disposition. Each cohort gets a VERIFIED per-cohort disposition (per-doc for ambiguous cases) with salvage-before-drop. NOTHING is deleted under the reversible-move sign-off; retirement waits for its OWN door:one-way founder go (FE-D2).
- **Dependency edges:** **← PHASE-1-AMENDMENTS-SETTLED (T3.0; the ROOT phase-ordering gate — must-fix #2).** ← FE-A (`(category,axis,doc_type)→folder` transform table, proven TOTAL over 44 folders × the 7-TSV 4-tuple; orphan/dupe disposition CLOSED — must-fix #6). ← shares the doc-4-tuple enum with ENUM-ENFORCEMENT (L4). Independent of L2's graph data. **HARD EDGE (must-fix #6 — the DAG shows this as a dependency, NOT parallel):** L4.2's `link-check` + `completeness-reconciliation`(=GATE-2) predicates must be BUILT + GREEN BEFORE the L3 move-PR (T3.2) is eligible. The move's reversibility/no-loss guarantee rests on these; sequencing the move parallel to its own safety gate would land the reorg before the firewall that proves it lossless. (Note: with T3.0 in place this is now defense-in-depth behind amendments-settled, not the sole defense.)
- **Verification gate:** **amendments-settled (T3.0)** + `link-check` (zero broken links) + `doc-axis`/`no-shadow-docs` + `completeness-reconciliation` (=docs GATE-2; Σhomed+Σexcluded == 10,069 — GREEN condition definable ONLY after the 214-orphan + 18-dupe disposition closes at FE-A) — all [ASPIRATIONAL] (built+wired by L4), required GREEN BEFORE the move-PR. The MOVE itself is reversible (mechanical manifest, reversible only IF the transform table is TOTAL); the RETIRE is irreversible and verified per-cohort by L5 before FE-D2.
- **Founder sign-off:** ⛳ FE-D — the move-manifest + orphan disposition (REVERSIBLE link-rewrite move only; door:one-way for the relocation; **may not be signed until Phase-1-amendments-settled per T3.0**). ⛳ FE-D2 — the RETIREMENT list with per-cohort verified disposition + salvage-before-drop (SEPARATE irreversible one-way door; nothing deleted before this).

### LANE 4 — ENUM-ENFORCEMENT  (Waves 1–3; the CaC/CaaS gate delivery; [ASPIRATIONAL]→[RUNS-NON-BLOCKING]→[ENFORCED] only at FE-F)
**Purpose:** turn every closed enum (00-ENUM-CATALOG) into a real **docs-domain PREDICATE that runs against the ONE Phase-0 `accounting-registry.generated.json` and submits to the FOUR Phase-0 keystone gates** — Rust gate crate (shared logic) + GitHub Actions workflow + oya-ci shadow, each proven by RED/GREEN fixtures. **SUBORDINATION (NEW; must-fix #1):** this lane builds NO peer keystone and NO second registry. Its `cross-artifact-agreement` work FEEDS Phase-0 GATE-1; its `completeness-reconciliation` IS the docs specialization of GATE-2 `total-accounting`; its `staleness-reaper` is BOUND to GATE-3; and every crate here is policed by GATE-4 `automation-ratchet` (no self-certification). Re-home ALL enforcement off the `oya` CLI to the CaC pipeline (Δ2/Δ3, D-GOVERNANCE-CENTRAL).
- **T4.1 (seq, Wave 1):** Finalize the PENDING enum value-sets the catalog flags: **ADD `edge_kind: cloud-on-cloud` (substrate→substrate) to the closed `edge_kind` set FIRST (CRITIC must-fix #2 — it is ABSENT today: catalog set is `orchestrates·serves·substrate-call·dogfood·port-adapter·depends`; load-bearing across S1/T2.1/T2.3/dogfood-honesty/ADR; an off-catalog value cannot be a gate target);** `hyperscaler_category` (PENDING:A close from real catalog), `maturity` (close), `microservice_status` confirm (built/spec-only/stub), `facet` (PENDING:B ← FE-A), plus the §4 governance PENDINGs (tenant_class, dr_tier, storage_tier, data_class ladder, eu_ai_act_risk GPAI question). Resolve the count drift by driving the HEADER to the DISK-VERIFIED BODY (208/146, NOT 210/148 — Driver-note), the `application→usecase` 22 stragglers, `cli` dropped. **PASS-STATE-CLOSURE before FE-E (NEW; closes the residual total-accounting principle-violation — an open carve-out IS an undefined gate pass-state by this plan's own Scenario-3a standard):** the **19 layerless spec-only services** carve-out-vs-fix decision AND the **placeholder `(generic)`/`unspecified` port·tech** disposition are total-accounting (GATE-2) pass-state inputs — they MUST be CLOSED (each → explicit fix OR explicit carve-out-with-reason) before FE-E, exactly as the 214-orphan/18-dupe disposition was promoted to a blocker. An open "carve-out OR fix" leaves the `placeholder-ban` + `count-reconciliation` + GATE-2 pass-state undefined.
- **T4.2a (seq, early-Wave-1 — docs-domain CROSS-ARTIFACT PREDICATE feeding Phase-0 GATE-1; SUBORDINATE not peer, must-fix #1 + #5 + D-SEQUENCE):** Implement + RED/GREEN-prove the **docs+graph cross-artifact-agreement PREDICATE that FEEDS the founder's Phase-0 GATE-1 `cloud-ci-cross-artifact-agreement` (G-XA), reading the SAME single `accounting-registry.generated.json` — NOT a second registry, NOT a peer keystone (§00).** It runs BEFORE the NEW honesty schema (T2.1's `intended`/`edge_kind:cloud-on-cloud`/disk-derived `wired`) and the doc-family generation behind it land. It reconciles the TWO edge models (`_taxonomy.json` ↔ `architecture-graph.json` — must-fix #1) as the docs-domain INPUT to GATE-1's crosswalk, making "generated + drift-gated" trustworthy for the lie-prone facets. The existing dashboard's 4 views still ship now from the existing generator (dashboard-now half, PROVISIONAL FE-C), but the NEW schema waits behind this predicate (firewall-before-new-canon half). **PREREQ (§00.1):** the Phase-0 GATE-1 KEYSTONE-extension (status-enum + no-dangling-ref per `PHASE-1-AMENDMENT-LANES` §0) is owned by Phase-0, not this plan; this predicate binds to it (FE-E). This is the explicit D-SEQUENCE honoring (see Option 3 trade-off + §D).
- **T4.2b (parallel, Wave 2):** Implement the rest of the docs-domain predicate family (shared Rust logic), each running against the ONE Phase-0 registry: `enum-membership` (after the `cloud-on-cloud` amendment), `placeholder-ban`, `count-reconciliation` (header→body=208/146), `dogfood-honesty` (RE-DERIVES `wired` from the verification-block live oya/ Cargo.toml/BUCK re-scan — NOT from counting the 26-`wired:true` `dogfood_edges` array — OVERWRITES stored value, separates `edge_kind:dogfood` product→substrate from `edge_kind:cloud-on-cloud` + the 3 inversions — must-fix #3/#6; **rescan TRIGGER scoped to `**/Cargo.toml`+`**/BUCK` across `oya/`, not doc/graph paths — must-fix #4**), `doc-axis`/`no-shadow-docs`, `brand-residue` (wire the existing yaml — zero workflows reference it today; carve-out + badged-text allowed), `link-check`, **`completeness-reconciliation` = the docs specialization of Phase-0 GATE-2 `total-accounting`** (GREEN condition definable only post-disposition; feeds the ONE GATE-2 total), **`staleness-reaper` BOUND to Phase-0 GATE-3** (reports into GATE-3 report→`git mv`-never-`rm`, not a free-standing scanner). Each ships a RED fixture (must go red) + GREEN fixture (real corpus passes).
- **T4.3 (seq, Wave 3) — wire + run the BRIDGE (must-fix #4):** Wire each predicate into a GitHub Actions workflow + the oya-ci shadow lane (non-blocking, mirrors `docs-graph-drift`'s authority model). At this point each is [RUNS-NON-BLOCKING] (runs, can go red, NOT a required check). **Build-first-cutover-later bridge for enforcement itself:** keep the EXISTING live CLI lanes AND the Jenkins `oya-ci-gate` pipeline (ADR-0380; the current `oya-ci-required` producer-runner) RUNNING in shadow while these Rust crates are RED/GREEN-proven, so coverage never drops during the migration; cutover gate authority to the CaC pipeline ONLY after the crates are proven; retire the CLI + Jenkins lanes afterward (Task #25/#26). Two-layer defense: generator writes enum-valid records AND the predicate blocks off-enum even if the generator never ran.
- **T4.4 (seq, Wave 3) — SUBMIT every predicate to Phase-0 GATE-4 `automation-ratchet` (NEW; must-fix #1):** No predicate this plan delivers self-certifies. GATE-4 (`cloud-ci-automation-ratchet`, owned by `PHASE-0-FIREWALL-PLAN`) polices each: blocks any [ENFORCED] tag with no wired buck2 target, any advisory-claiming-enforced, any `oya`-CLI-authority residue, AND (this plan's specific exposure) any under-scoped freshness trigger (the dogfood-honesty rescan must fire on Cargo.toml/BUCK). The generator itself is a ratchet subject. This is the inheritance of the 4th keystone that the prior draft omitted entirely.
- **Dependency edges:** ← CONVERGE-FACETS (facet enum). ← **Phase-0 firewall (PHASE-0-FIREWALL-PLAN): the `accounting-registry.generated.json` + GATE-1..4 must be live + RED/GREEN-proven first** (this lane CONSUMES them; §00). Provides docs-domain predicates consumed by L2 (dogfood-honesty, drift), L3 (link-check, doc-axis, completeness=GATE-2), L5 (the verifier runs them).
- **Verification gate:** the meta-gate — every gate is proven by demonstrating its RED fixture ACTUALLY goes red (no advisory-shell). A gate stays [ASPIRATIONAL] until built; [RUNS-NON-BLOCKING] once wired + RED-proven; [ENFORCED] ONLY when promoted into `required_status_checks` behind a live `oya-ci-required` producer (FE-F).
- **Founder sign-off:** ⛳ FE-E — the frozen enum value-sets, INCLUDING the `edge_kind: cloud-on-cloud` amendment (door:one-way; backbone vocabulary; the amendment must precede any gate that targets it). ⛳ FE-F — promotion of each gate to a REQUIRED check (door:one-way; gated on a live GREEN `oya-ci-required` producer existing — today the producer-runner is the Jenkins `oya-ci-gate` pipeline (ADR-0380) but it is NOT green until ADR-0380 D4 mints `github-ci-token`; until a green producer exists, gates stay [RUNS-NON-BLOCKING], NOT [ENFORCED]; per D-CICD-AUTHORITY GitHub stays authority, the CaC pipeline is the destination producer; the CLI + Jenkins shadow lanes stay RUNNING until cutover so coverage never drops — no ruleset flip onto the forbidden CLI, no gate-coverage hole during the bridge — must-fix #4).

### LANE 5 — INTEGRITY / HONESTY-CARRIES  (all waves; the SEPARATE verifier lane; no self-approval)
**Purpose:** the structurally-separate verification lane (D-DOCTRINE robust-not-false + verify-each-step). It does NOT author; it adversarially verifies the other four lanes against the verified inputs + real files, carrying the honesty invariants end-to-end.
- **T5.1 (continuous):** Carry the **dogfood-intended-vs-wired** honesty across ALL artifacts (doc §7/§8/G4, dashboard view-1 overlay, SSOT edge records, the dogfood-honesty gate) — re-verify the product overlay against the **verification-block Cargo.toml/BUCK re-scan** (`grep -rIl cloud[-_]kms over oya/` → empty): **2/87 IAM product-wired, 0/87 KMS** — DISK-via-verification-block is truth, NOT the stored edge AND NOT the 26-`wired:true` `dogfood_edges` array (counting the array over-reports — must-fix #3); block any "wired" claim without a path-dep; block any product overlay that counts the 5 cloud-on-cloud edges or the 3 inversions as product dogfood. No false promise survives this lane.
- **T5.2 (continuous):** Carry the **4-term + Kafka + Jenkins off-canon** discipline — verify NO live foundry/Kafka NODE re-enters the dashboard/graph (verified: none today; only badged eradication-target TEXT + Palantir-Foundry carve-out remain), the brand-residue gate is wired and its RED fixture goes red, the oya-vcs-* crates + `_legacy-foundry` + the Jenkins `oya-ci-gate` shadow lane are flagged as eradication TARGETS (actual source eradication is Task #25/#26, gated separately; Jenkins is the live bridge runner kept shadow-live until cutover, then retired — must-fix #4). This lane verifies the doc/dashboard/enum don't re-admit live forbidden nodes and that the de-CLI/de-Jenkins destination is named, not silently violated. Palantir-Foundry carve-out preserved.
- **T5.3 (per-lane gate):** For each lane's deliverable, produce evidence (no phantom findings): facet rows cite 3-0 frameworks + correct generatable|authored column (L1); SSOT counts == taxonomy BODY 208/146 + generator golden/regeneration green post-extension (L2); link-check + completeness == 10,069 + RETIRE dispositions verified per-cohort before any delete (L3); every gate's RED fixture demonstrably goes red + correct tier-tag (L4). Confirm the `Re-emit via oya doc graph emit` / any `oya` CLI ref is absent everywhere (de-CLI).
- **T5.4 (final):** Author the ADR (§E) + persist open questions; confirm STATUS remains "pending approval (door:one-way)" and no source mutation occurred.
- **T5.5 (continuous — authority-locus + CAMPAIGN-PRECONDITION carry, must-fix #1 + #2):** Before EVERY FE sign-off, re-verify on `source @ phase0/producer` (or successor authority branch): (a) the named artifact is present + behaving as tagged; (b) **the consuming Phase-0/Phase-1 dependency for that FE is itself live** — for FE-E, the GATE-1 KEYSTONE-extension; for FE-F, a live GREEN `oya-ci-required` producer; **for FE-D, Phase-1-amendments-settled (T3.0)**; and across all, Step-0 (clean-base commit — verified DONE) + producer commit-signing. Block any FE whose underlying claim or campaign-precondition cannot be re-verified live on target (inherits the D-MERGE G0 HALT risk). The `linux` audit tree is authoring-only; `source` is the execution authority.
- **T5.6 (per-FE rollback paths, NEW — closes CRITIC gap #2):** Carry the documented rollback for each partial-completion: (a) **FE-B lands but FE-C rejected** — the SSOT/schema migration is REVERTED to the pre-migration golden (the `golden.rs`/`regeneration.rs` back-compat surface is the revert anchor); the SSOT is NEVER left migrated with no accepted consumer. (b) **FE-D move lands but a downstream link-check regresses** — the mechanical path-rewrite is reverse-applied from the move-manifest (reversible by construction; T3.2). (c) **FE-C accepted PROVISIONALLY then a live producer reveals drift** — FE-C is REVOCABLE (re-verified at FE-F); the dashboard refresh can be rolled back to the last byte-compare-clean commit. No partial state is a dead-end.
- **Dependency edges:** observes ALL lanes; gates each founder sign-off (FE-A…FE-F + FE-D2) with independent evidence; confirms Phase-0/Phase-1 campaign preconditions live before each dependent FE.
- **Verification gate:** this IS the gate. Self-contained (no authoring lane approves its own work). Uses `verify`/`visual-verdict`/`code-reviewer`-class separate passes.
- **Founder sign-off:** ⛳ feeds every FE checkpoint; the founder is the door:one-way authority on each.

---

## (D) SEQUENCING — cross-lane wave order + one-screen DAG

**Wave order (fractal parallel; D-LANES) — anchored to the CAMPAIGN spine, this plan is the Phase-2 consumer:**
- **WAVE −1 (UPSTREAM, owned by PHASE-0-FIREWALL-PLAN + PHASE-1-AMENDMENT-LANES — NOT this plan, but a hard precondition):** Phase-0 firewall LIVE — `oya-ci-required` producer green + `accounting-registry.generated.json` + GATE-1..4 RED/GREEN-proven — THEN Phase-1 A-lane amendments settled (status-enum, dup-0377, phantom-0150, A-FOUNDRY, A-CI/A-DATA). **This plan CONSUMES the gates (its predicates feed them) and WAITS for the amendments before L3.** (§00)
- **Wave 0 — CONVERGE-FACETS (L1).** Read-only/authoring. Output: FROZEN facet enum + view/section/overlay split + `(category,axis,doc_type)→folder` transform table (FE-A). Root of THIS plan's DAG (downstream of WAVE −1).
- **Wave 1 — MERGE-SSOT + value-sets + GATE-1-FEED-FIRST + START-PROSE (L2.1 + L4.1 + L4.2a + L2.2-prose, parallel/ordered).** MERGE the two edge models into the unified `architecture-graph.json` + generator + golden/regeneration tests (FE-B; a two-source merge — must-fix #1) || finalize enum value-sets INCLUDING the `edge_kind: cloud-on-cloud` amendment + drive count drift header→body=208/146 + CLOSE the 19-layerless + placeholder-port pass-state (FE-E) || **build + RED/GREEN-prove the docs-domain cross-artifact PREDICATE that FEEDS Phase-0 GATE-1 (L4.2a) — SUBORDINATE to the keystone, NOT a peer (§00) — BEFORE the NEW honesty schema's `intended`/`cloud-on-cloud`/disk-`wired` and the doc-family generation behind it land — D-SEQUENCE firewall-before-new-canon (must-fix #1/#5)** || author the un-generatable prose layer concurrently (NOT blocked behind the schema). The SSOT + value-sets + GATE-1 feed are the contracts; prose is parallel.
- **Wave 2 — GENERATE + GATE-BUILD (L2.3 + L2.2-includes + L4.2b + L4.4, parallel).** Rebuild dashboard (4 views ship NOW from the existing generator — dashboard-now half, **PROVISIONAL FE-C** behind the non-blocking gate — must-fix #3) || land the NEW generated structural/runtime includes (only AFTER the GATE-1 feed is green — firewall-before-new-canon half) || implement the rest of the docs-domain predicate family with RED/GREEN fixtures (`completeness-reconciliation`=GATE-2 feed, `staleness-reaper`=GATE-3-bound, dogfood-honesty with Cargo.toml/BUCK trigger scope — must-fix #4) || submit all to GATE-4 `automation-ratchet` (L4.4 — the inherited 4th keystone, must-fix #1). All read the Wave-1 contracts. **(L3 doc-reorg is NOT in this wave — it is Phase-2, gated behind Phase-1-amendments-settled; see below.)**
- **Wave 2.5 / PHASE-2 — DOC-REORG (L3), GATED behind Phase-1-amendments-settled (must-fix #2).** Only after WAVE −1 Phase-1 amendments settle (T3.0) does L3 open: resolve dupes/orphans (T3.1, dup-0377 already done upstream) → AUTHORED-transform mechanized move (T3.2) → 4-tuple stamp (T3.3). **HARD EDGE: L4.2b's `link-check` + `completeness-reconciliation`(=GATE-2) GREEN BEFORE the L3 move-PR.** This is the campaign's Track-2; sequencing it here (not in Wave 2 concurrent with amendments) dissolves the phase-inversion at the root.
- **Wave 3 — WIRE + BRIDGE + VERIFY + SIGN (L4.3 + L5 final + founder FE-C/FE-D/FE-D2/FE-F).** Wire predicates [RUNS-NON-BLOCKING] while keeping the CLI + Jenkins shadow lanes live (build-first-cutover-later bridge — must-fix #4); verifier produces evidence + checks campaign preconditions (T5.5) + rollback paths (T5.6); founder signs the dashboard/doc PROVISIONALLY (FE-C, revocable until FE-F — must-fix #3), the REVERSIBLE doc move (FE-D, only post-amendments-settled), the IRREVERSIBLE retirement separately (FE-D2), and gate promotion-to-required IF a live GREEN producer exists (FE-F). Firewall-first is HONORED for the NEW honesty schema (GATE-1 feed in Wave 1, GATE-4 over all) and SCOPED-OUT-but-PROVISIONAL for the existing dashboard refresh — the named trade-off, not a blanket "absorbed."
- **L5 INTEGRITY/HONESTY-CARRIES runs across ALL waves** (continuous verifier; gates each FE; carries the authority-locus check T5.5).

**One-screen DAG:**

```
 ┌────────────────────────────────────────────────────────────────────────────────────────┐
 │  WAVE −1  UPSTREAM CAMPAIGN (owned by sibling plans — NOT this plan; HARD PRECONDITION)   │
 │  PHASE-0-FIREWALL-PLAN: producer(green) → accounting-registry.generated.json             │
 │     → GATE-1 cross-artifact-agreement · GATE-2 total-accounting                           │
 │     → GATE-3 staleness-reaper · GATE-4 automation-ratchet  (one registry, four predicates)│
 │  then PHASE-1-AMENDMENT-LANES: A-INTEGRITY(status-enum·dup-0377·phantom-0150)·A-STRUCT·   │
 │     A-FOUNDRY·A-CI·A-DATA  (gate-verified; "never two sweeps over 346 ids")               │
 └───────────────┬──────────────────────────────────────────────┬───────────────────────────┘
       this plan CONSUMES the 4 gates (its predicates feed them)  │  L3 reorg WAITS for amendments
                 ▼                                                ▼
                         ┌──────────────────────────────────────────────────────┐
                         │  L5 INTEGRITY / HONESTY-CARRIES (verifier)             │
                         │  spans all waves · gates every FE · no self-OK         │
                         │  + T5.5 authority-locus + CAMPAIGN-PRECONDITION carry  │
                         │  + T5.6 per-FE rollback paths                          │
                         └──────────────────────────────────────────────────────┘
                                   ▲        ▲        ▲        ▲
 WAVE 0        WAVE 1                       WAVE 2          WAVE 2.5/PHASE-2   WAVE 3
 ───────       ───────                      ───────         ──────────────    ───────
 L1 CONVERGE   ┌─ L4.2a DOCS-DOMAIN PRED.   ┌─ L2.3 dashboard rebuild  ─┐
 -FACETS  ──►  │   cross-artifact FEEDS ────┼─► (4 views NOW, existing   │
 (FE-A:        │   Phase-0 GATE-1 (SUBORD,  │   generator, byte-compare) │──► FE-C
  facet enum,  │   NOT peer; same registry) ├─ L2.2 NEW generated        │   (PROVISIONAL:
  view split,  │   RED/GREEN-proven FIRST   │   includes (ONLY after     │    revocable until
  transform    │   (D-SEQUENCE firewall-    │   GATE-1 feed green)        │    FE-F; non-block
  table proven │    before-NEW-canon)       └────────────────────────────┘    gate window)
  TOTAL +      ├─ L2.1 MERGE two edge models┌─ L2.2 AUTHORED prose (G1/G2/G3/R4)
  orphan/dupe  │   _taxonomy.json{prod,sub, │   parallel, ADR-0019, NOT schema-blocked
  disposition  │   wired,note} ⨝ arch-graph └─
  CLOSED) ──►  │   .json{source,target,kind}┌─ L4.2b predicate-family ─► L4.4 SUBMIT to ─► L4.3 wire+BRIDGE ─► FE-F
   │           │   +intended+disk-wired ─FE-B►  (=GATE-2 completeness,      GATE-4 ratchet   [RUNS-NON-BLOCK]  (promote iff
   │           ├─ L4.1 value-sets ─FE-E──►      GATE-3-bound staleness,    (inherited 4th   CLI+Jenkins      live GREEN
   │           │  (+edge_kind:cloud-on-cloud    dogfood w/ Cargo/BUCK       keystone)        shadow→cutover)  producer)
   │           │   AMEND; count→body 208/146;   trigger scope)
   │           │   close 19-layerless+placeholder)        ║ HARD EDGE: link-check+completeness(GATE-2) GREEN
   │           │                                          ▼ BEFORE the L3 move-PR
   │           │                          ┌── PHASE-1 AMENDMENTS SETTLED (T3.0, upstream) ──┐
   └───────────┴─────────────────────────►└─ L3 DOC-REORG (PHASE-2): T3.1 dupes/orphans ─► T3.2 AUTHORED-transform ─► T3.3 ─► FE-D
                                              (dup-0377 already done in A-INTEGRITY)  mechanized move (reversible)   stamp   (post-amend)
                                                                                                                    └─► FE-D2 (irreversible RETIRE)

 Dependency spine (THIS plan, anchored to the campaign):
   [UPSTREAM] Phase-0 producer→accounting-registry→GATE-1..4 LIVE ; Phase-1 amendments SETTLED
   → FACETS+TRANSFORM-TOTAL+DISPOSITION-CLOSED+pass-state-closed (FE-A/FE-E)
   → docs-domain cross-artifact PREDICATE feeding GATE-1 (firewall-first for NEW schema; SUBORDINATE)
   → {MERGE-SSOT(FE-B) ∥ PROSE , value-sets+cloud-on-cloud-amend(FE-E)}
   → {dashboard-NOW(existing gen, PROVISIONAL FE-C), NEW-includes(post-GATE-1-feed), predicates→GATE-4}
   → [Phase-1-amendments-settled ∧ link-check+completeness(GATE-2) GREEN] ─hard─► reorg-move (PHASE-2)
   → wire[RUNS-NON-BLOCKING] + CLI/Jenkins shadow bridge → verify (+rollback) 
   → founder FE-C(provisional) / FE-D(post-amend) / FE-D2 / FE-F(green producer) → (door:one-way source mutation, separate go)
```

---

## (E) ADR — Architecture Decision Record

**Title:** Generate the hyperscaler-grade architecture doc + dashboard + doc-reorg AS A PHASE-2 CONSUMER of the founder's Phase-0 firewall (one `accounting-registry.generated.json` + GATE-1..4) — over a PRIMARY enum-keyed `architecture-graph` SSOT + a DISK-AUTHORITATIVE overlay (dogfood/counts) + an AUTHORED prose layer (ADR-0019) — THREE honest truth surfaces, not one — built by MERGING two existing edge models, with docs-domain predicates that FEED the four keystone gates (not a second firewall), the reorg sequenced AFTER Phase-1 amendments settle, dashboard refresh accepted PROVISIONALLY behind the non-blocking gate, executed against the source monorepo. (Re-cast per CRITIC/ARCHITECT must-fix #1/#2/#3: subordinated to the campaign spine; "single SSOT" overstated a three-surface reality.)

**Status:** Proposed (pending founder approval; door:one-way).

**Decision:** Adopt **Option 1, re-cast as a PHASE-2 CONSUMER of the Phase-0 firewall** (not a parallel five-lane program with its own keystone — ARCHITECT synthesis / CRITIC must-fix #1). **(Subordination — the spine this plan obeys:)** there is ONE `accounting-registry.generated.json` and FOUR keystone gates (GATE-1 cross-artifact-agreement · GATE-2 total-accounting · GATE-3 staleness-reaper · GATE-4 automation-ratchet), all owned by `PHASE-0-FIREWALL-PLAN`. This plan's docs-domain predicates FEED those gates (GATE-1 crosswalk input; GATE-2 = this plan's `completeness-reconciliation`; GATE-3 binds this plan's staleness; GATE-4 polices every predicate + the generator) — it builds no peer keystone and no second registry (§00). **(Phase ordering:)** the L3 10,069-doc reorg is Track-2 / PHASE-2 and runs AFTER the Phase-1 ADR-amendment campaign (`PHASE-1-AMENDMENT-LANES`) settles — never concurrently ("never two sweeps over the ~346-id space"; §00.2). **(Surface 1 — PRIMARY SSOT, generatable ~70%:)** MERGE the EXISTING `docs/machine-readable/architecture-graph.json` (verified present on `source @ phase0/producer`, 81,325 bytes, keys `_meta/verticals/techstack/lanes`; **edges are `{source,target,kind}` — they do NOT have `wired`/`dogfood`/`intended`; corrects the prior draft's false "already has wired+dogfood"**) WITH the SEPARATE `_taxonomy.json` `dogfood_edges` (`{product,substrate,wired,note}`) into one unified SSOT plus its generator + golden/regeneration tests, keyed on the closed ENUM-CATALOG sets + the FE-A frozen facet enum. This is a TWO-SOURCE MERGE (reconciling the existing `kind:cloud` field against the new `edge_kind` discriminator), not a one-file extension. The comprehensive design doc (arc42 12-section, colocated docs-as-code) draws its STRUCTURAL/RUNTIME includes from this SSOT. **(Surface 2 — AUTHORED prose:)** the prose facets (G1/G2/G3/R4) are authored in PARALLEL (ADR-0019-bound, not schema-blocked), a permanent drift seam reported INTO the Phase-0 GATE-3 staleness-reaper. **(Surface 3 — DISK-AUTHORITATIVE overlay:)** the dogfood + count facets are DISK-derived, not SSOT-trusted: dogfood `wired` is RE-DERIVED from the `_taxonomy.json` verification-block Cargo.toml/BUCK re-scan (NOT from counting the 26-`wired:true` `dogfood_edges` array) and rendered as the PRODUCT overlay **2/87 IAM, 0/87 KMS** (the 5 `edge_kind:cloud-on-cloud` substrate→substrate edges + 3 inversions are a separate band, not counted in the product overlay); counts bind to the DISK-VERIFIED body **208/146** (the header 210/148 is stale and gets driven to the body). **The dogfood-honesty rescan TRIGGER is scoped to `**/Cargo.toml`+`**/BUCK` across `oya/` (must-fix #4), not doc/graph paths — so the overlay cannot render a green 2/87 after products wire to 5/87.** **The `edge_kind: cloud-on-cloud` value MUST be ADDED to 00-ENUM-CATALOG (FE-E) before any gate targets it — it is absent today; this enum-extension feeds the GATE-1 KEYSTONE-extension, not a separate enum authority.** Enforcement is delivered firewall-first for the new honesty schema: the docs-domain cross-artifact PREDICATE feeding Phase-0 GATE-1 is RED/GREEN-proven (Wave 1) BEFORE the new `intended`/`cloud-on-cloud`/disk-`wired` schema and the doc-family generation behind it land (D-SEQUENCE); the existing dashboard's 4 views still ship now from the existing generator, **but their acceptance (FE-C) is PROVISIONAL/REVOCABLE** because `docs-graph-drift.yml` is non-blocking — re-verified once a live GREEN producer exists (must-fix #3). All predicates are CaC/CaaS Rust crates invoked by GitHub Actions + oya-ci shadow, each proven by RED/GREEN fixtures, submitted to GATE-4, shipping [RUNS-NON-BLOCKING] and promoted to [ENFORCED] only when a live GREEN `oya-ci-required` producer exists (FE-F). **Build-first-cutover-later bridge:** the live CLI lanes AND the Jenkins `oya-ci-gate` pipeline (the current producer-runner) stay shadow-live until the Rust crates are proven, then cutover, then retire — no gate-coverage hole, no re-homing onto the forbidden CLI/Jenkins. NO `oya` CLI anywhere; Jenkins is an eradication target, not the destination. **The doc move (reversible, gated on Phase-1-amendments-settled + a TOTAL transform table + closed orphan/dupe disposition) and the content retirement (irreversible) carry SEPARATE one-way doors (FE-D vs FE-D2).**

**Drivers:** (1) honesty under a mostly-aspirational dogfood story, DISK-derived not SSOT-trusted; (2) drift is guaranteed without generation + a real gate (D-DOCTRINE); (3) the false-green firewall — NO enforcement is live-blocking today (the only required check `oya-ci-required` has no producer; `docs-graph-drift.yml` runs but does not block).

**Alternatives considered:** Option 2 (standalone hand-written arc42 doc + manual reorg + review-checklist enums) — INVALIDATED for the generatable layer: hand-maintenance over a 10k-doc drifting corpus is stale-on-commit and "enforce by checklist" is the advisory-shell anti-pattern D-DOCTRINE §K forbids. Its ONE real virtue (fast readable prose) is ABSORBED into Option 1 via the parallel authored-prose layer (T2.2), not discarded. Option 3 (defer doc+dashboard; enums+reorg first) — INVALIDATED: contradicts the explicit D-ARCH-DASHBOARD ruling + inverts the real dependency (the facet enum is DEFINED by converging the doc facets); its enforcement-first ordering instinct is absorbed into the wave sequencing. **Option 4 — the prior draft's OWN structure: a self-contained five-lane program with its OWN single keystone (`cross-artifact-agreement`) over its OWN spine (architecture-graph.json + _taxonomy.json), reorg in a self-chosen Wave 2 (NEW — the alternative the ARCHITECT/CRITIC identified and this revision REJECTS)** — INVALIDATED as a SECOND, differently-shaped firewall: it omitted GATE-4 `automation-ratchet` entirely, demoted GATE-2/GATE-3 to ordinary Wave-2 crates, built no `accounting-registry.generated.json`, and ran the 10k-doc reorg concurrent with the Phase-1 amendment campaign over the SAME 346-id ADR space — recreating the exact dual-consensus-body drift the consolidation exists to kill ("never two sweeps"). Its genuine strengths (three-truth-surface model, disk-re-derived dogfood overlay, FE-D/FE-D2 split, parallel prose) are PRESERVED; only its self-contained-firewall STRUCTURE is rejected, replaced by subordination to the Phase-0 spine.

**Why chosen:** It SUBORDINATES to the apex sequencing instead of running alongside it — one registry, four keystones, reorg-after-amendments — so it cannot recreate the dual-consensus-body drift (the entire reason the consolidation exists). The PRIMARY SSOT eliminates cross-artifact drift by construction **for the generatable ~70% ONLY** (Driver 2) — the "by construction" claim is SCOPED to that surface, NOT to the disk-authoritative dogfood/count overlay (disk is truth there) nor the authored prose (ADR-0019/GATE-3 owns it); these three surfaces are named honestly, not marketed as one. It REUSES the EXISTING owned-Rust generator + its already-running (but non-blocking-today) byte-compare job — lowest blast radius, with the back-compat surface (golden/regeneration tests) handled in lockstep through the two-source merge; the dogfood honesty is one DISK-DERIVED field (re-derived from the verification block, not the edge array, on a Cargo.toml/BUCK-scoped trigger) consumed everywhere (Driver 1); the docs-domain cross-artifact predicate feeding Phase-0 GATE-1 lands firewall-first before the new honesty schema (D-SEQUENCE), while the existing dashboard ships now under a PROVISIONAL/revocable FE-C (the named trade-off, honestly priced — not a blanket "absorbed" nor a false-green door:one-way); the authored prose ships in parallel so the headline deliverable is not schema-blocked; the CLI+Jenkins bridge keeps coverage during the cutover (build-first-cutover-later); the inherited GATE-4 ratchet polices every predicate + the generator so none self-certifies; satisfies D-DOCTRINE total-accounting + two-layer defense + D-CLOUD-NATIVE de-CLI + D-CICD-AUTHORITY (GitHub authority / oya-ci shadow). It is honest about its own authority locus (§0/§00.1), its own tags (no [ENFORCED] claimed where none is live), and its place in the campaign (a Phase-2 consumer, §00).

**Consequences:**
- Positive: drift-controlled + staleness-controlled for the generatable layer; honest by gate (dogfood disk-derived, brand, enum); the design doc maps each facet to a 3-0-verified 42010 framework + diagram primitive; one vocabulary across all four deliverables; the headline doc is not blocked behind the data contract.
- Negative / cost: T2.1 is a TWO-SOURCE MERGE (reconciling `architecture-graph.json` `{source,target,kind}` with `_taxonomy.json` `{product,substrate,wired,note}` AND the existing `kind` vs new `edge_kind`), not a one-file extension — a real migration with a golden/regeneration back-compat surface (FE-B); the generator/SSOT path bug-couples four deliverables (mitigated by byte-compare + golden + RED/GREEN); THREE truth surfaces, three drift-entry points — the prose layer (G1/G2/G3/R4) is authored-not-generated and carries the ADR-0019 maintenance protocol as a PERMANENT drift seam (reported into GATE-3), and the dogfood + count facets are DISK-authoritative, so the SSOT is NOT the source of truth for those two facets (deliberate, honest — the "single SSOT" framing is scoped to the generatable ~70% only); `edge_kind: cloud-on-cloud` must be ADDED to the catalog before it can be gated (it is absent today); ~10 docs-domain predicate-crates to build + wire + RED-prove + submit-to-GATE-4 before any [ENFORCED] claim is truthful, the GATE-1 feed must land BEFORE the new schema (firewall-first ordering cost), AND a live GREEN `oya-ci-required` producer (today the not-yet-green Jenkins `oya-ci-gate` pipeline) must exist before promotion — with the CLI+Jenkins bridge kept shadow-live until cutover so coverage never drops. **SUBORDINATION COST (NEW):** this plan cannot START its L3 reorg until the upstream Phase-0 firewall is live AND Phase-1 amendments settle (it is gated on sibling-plan completion — a real schedule dependency, not a flaw); and FE-C is accepted only PROVISIONALLY until a live producer exists (the dashboard refresh is revocable, not a clean door:one-way).
- Honest current state (verified live 2026-06-07): this plan AUTHORED in `linux` (no remotes, no workflows, none of the referenced infra) but EXECUTES against `source @ phase0/producer` (forgejo origin, jason931225/oyatie mirror, **219 dirty entries — confirmed live**, mid authority-flip / G0 HALT). **Step-0 clean-base commit (e77f16eb2) HAS fired; ADR-0515 ratified (869e48ca4); the Phase-0 producer exists in SHADOW (30f4a0db9) but is NOT yet green** — so the ARCHITECT's branch-state/non-executability charge (`feat/oya-ci-tide @79`) is REFUTED as a stale snapshot; §0/§00.1 are the current correct state. `docs-graph-drift.yml` RUNS but is NOT a required check (only `oya-ci-required` is required, and it has no green producer); branch-protection.yaml disclaims itself as non-authoritative. So **NO check is [ENFORCED] today** — the dashboard byte-compare is [RUNS-NON-BLOCKING] (hence FE-C provisional), and the enum/brand/doc-axis/cross-artifact/link-check/completeness predicates are [ASPIRATIONAL] until L4 builds+wires+RED-proves+submits-to-GATE-4. The existing source graph already carries NO live foundry/Kafka nodes (only badged eradication-target text + Palantir-Foundry carve-out).

**Follow-ups:**
- **SIBLING-PLAN DEPENDENCIES (NEW; closes CRITIC gap #1):** this plan is downstream of three siblings in this directory — `PHASE-0-FIREWALL-PLAN.md` (must deliver the producer + `accounting-registry.generated.json` + GATE-1..4 before this plan's predicates can feed anything), `PHASE-1-AMENDMENT-LANES.md` (must settle the A-lanes before L3 reorg opens — §00.2), and `FULL-SCOPE-OF-WORK.md` (the apex spine that places this plan's reorg in Track-2). An executor MUST read those three first; this plan PRECEDES none of them and FOLLOWS all of them on the campaign critical path.
- **GATE-4 inheritance (NEW; closes must-fix #1):** every predicate this plan delivers is submitted to the Phase-0 GATE-4 `automation-ratchet` (L4.4) — no self-certification, no advisory-claiming-enforced, no under-scoped freshness trigger. This is the 4th keystone the prior draft omitted entirely.
- A live GREEN `oya-ci-required` producer (so predicates can be PROMOTED from [RUNS-NON-BLOCKING] to [ENFORCED]) — Phase-0 firewall (Task #23). The current producer-runner is the Jenkins `oya-ci-gate` pipeline (ADR-0380 D3), but it is NOT green until ADR-0380 D4 mints the `github-ci-token` Secret (the Jenkinsfile documents this as its honest failure mode). Without a green producer FE-F cannot complete (and FE-C stays provisional); until then the CLI+Jenkins shadow bridge keeps coverage (build-first-cutover-later).
- Source eradication of forgejo/foundry/jenkins/oya-vcs vocab (oya-vcs-* crates, `_legacy-foundry`, the Jenkins `oya-ci-gate` lane itself) = Task #25 (gated separately; Jenkins is an eradication TARGET kept shadow-live only as the cutover bridge); this plan only ensures the doc/dashboard/enum do not re-admit LIVE nodes + the brand-residue gate is wired.
- Re-home the remaining CLI-governance surface (and the Jenkins promotion authority) to the cloud-native pipeline = Task #26 (the cutover step of the bridge).
- Close the §4 governance enum PENDINGs (tenant_class, dr_tier, storage_tier, data_class ladder, eu_ai_act_risk GPAI) — owed in L4.1.
- NALSD concrete capacity numbers (R4) per substrate are owed at each vertical's M0 gate (D-SEQ), authored not generated.
- Deep-research open questions persisted to `open-questions.md` (Meta/Netflix conventions, docs-as-code drift evidence beyond Oxide, canonical kernel-to-workload facet ordering, AWS-internal dogfood documentation).

---

### Open Questions (persisted)
See `.omc/plans/open-questions.md` (appended) and `synthesis/decision-record-oyatie-canon.md` for the door:one-way ruling lineage. Key un-decided items feeding this plan: the §4 governance enum value-sets, the GPAI/systemic distinct-value question, and the per-cohort RETIREMENT dispositions (FE-D2). **Now BLOCKING before FE-A (promoted out of deferrable per CRITIC/ARCHITECT must-fix #6):** the 214-orphan fold-vs-exclude disposition + the 18-double-listed canonicalization MUST close (so `completeness-reconciliation`=GATE-2's Σ==10,069 GREEN condition is definable and the transform table is provably TOTAL). **Now BLOCKING before FE-E (promoted per the residual total-accounting principle-violation):** the **19-layerless-services carve-out-vs-fix call** + the **placeholder `(generic)`/`unspecified` port·tech disposition** — each total-accounting (GATE-2) pass-state inputs; an open carve-out leaves the gate pass-state undefined. **Tracked items:** the `edge_kind: cloud-on-cloud` catalog amendment (FE-E) must precede any gate that targets it (feeds the GATE-1 KEYSTONE-extension); the dogfood-honesty rescan trigger must be scoped to Cargo.toml/BUCK (must-fix #4); whether a live GREEN `oya-ci-required` producer (the Jenkins `oya-ci-gate` pipeline post ADR-0380 D4 token) will exist in time to PROMOTE any predicate to [ENFORCED] (FE-F) and convert FE-C from provisional to firm remains open — resolved at the lane's founder sign-off. **Campaign-precondition tracked items (NEW):** Phase-0 firewall live (producer + registry + GATE-1..4) before any predicate feeds; Phase-1 amendments settled before L3 opens (§00.2); producer commit-signing confirmed before any FE (§00.1).

> **STATUS: pending approval (door:one-way).** No source mutation. Awaiting founder go per FE-A…FE-F (+ FE-D2). All [RUNS-NON-BLOCKING]/[ENFORCED] tags re-verify on `source @ phase0/producer` at sign-off (T5.5).
