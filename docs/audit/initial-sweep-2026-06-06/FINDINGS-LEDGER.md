# FINDINGS-LEDGER — the standing "take a note" backlog (per D-EXCELLENCE-MANDATE)

**Mandate (founder 2026-06-07, canon D-EXCELLENCE-MANDATE):** while reading ANY artifact — code, architecture, stack, infra, specs, registry, docs — record every **worth-fixing**, **stale**, or **cloud-native/kubernetes-native/hyperscaler ANTI-PATTERN** item here. Every reading/audit agent appends. Nothing noticed is lost. **verify-each-step governs:** a finding is logged only with cited evidence (file:line / reproduction) — no phantom findings.

**Schema (one row per finding):** `F-NNNN | category {worth-fix | stale | anti-pattern} | area | severity {CRITICAL|HIGH|MED|LOW} | evidence (file:line / repro) | recommendation | status {open | in-progress | fixed | wontfix}`

The bar everything is raised to: **hyperscaler pattern · architectural maturity + excellence · maintainability · trailblazing modernness · cutting-edge · research-backed · best-practices.**

---

## Open findings (seeded 2026-06-07 from this session's reading)

| ID | cat | area | sev | evidence | recommendation | status |
|---|---|---|---|---|---|---|
| F-0001 | worth-fix | oya-dev-cli | CRITICAL | `oya/developer-sdk/crates/oya-dev-cli/src/active_artifact_contract_gate.rs:687` reads `oya/developer-sdk/specs/artifact-profile-defaults.json` but the file is at repo-root `specs/`; 3 tests fail; reproduces on clean HEAD | fix the read path (or move the file); part of the restore-build-green pass | open |
| F-0002 | worth-fix | ci-webhook-gateway | HIGH | `oya-ci-webhook-gateway-authz-cedar-adapter` `include_str!("../policy/ci-webhook-gateway.cedar")` → `crates/<crate>/policy/` but file is at `oya/ci-webhook-gateway/policy/`; crate does not compile | fix the include_str path | open |
| F-0003 | worth-fix | source workspace | HIGH | ≥2 crates fail `cargo build` (F-0001/F-0002); the tree is not `cargo build --workspace`-green | **FIXED 2026-06-07** wave-2 `w2-build-green-f0003` (cedar/proto/AppState fixes); spine-verified `cargo build --workspace` 0 errors on the integrated tree `c43d38b20` | fixed |
| F-0004 | anti-pattern | oya/** + cloud/** docs | MED | ~945 oya + ~260 cloud ARCH/README/PRD carry identical jenkins boilerplate — generated from a canonical template (BATCH-2 finding) | fix the template source + regenerate; never hand-scrub 1200 files | in-progress |
| F-0005 | worth-fix | docs/decisions | MED | ADRs 0327/0323/0322 cite phantom `ADR-0091-multispectrum-review-doctrine.md`; on-disk ADR-0091 = "Foundry write-gate foundations" (ID collision) | resolve the collision before scrubbing dangling refs | open |
| F-0006 | anti-pattern | libs/oya-check-doc-axis | HIGH | unwired façade: scans `docs/` only, warning-not-error, in 0 workflows | Workstream C: whole-tree scan + strict/blocking + closed-enum allow-list + wire into the pipeline | open |
| F-0007 | worth-fix | firewall / CI | HIGH | `oya-ci-required` required-by-nobody; 0 gates block a merge (false-green); live `dev` required `github-lane-unlocker-required` (shadow) | go-live = 3 founder-paired branch-protection doors (ADR-0515 §go-live) | open |
| F-0008 | anti-pattern | docs/decisions (CI/CD) | MED | 7-ADR contradictory CI/CD cluster (0124/0349/0359/0361/0511/0513/0514), one pair directly contradictory with no supersede edge | resolved by ADR-0515 re-author (supersedes the cluster) | in-progress |
| F-0009 | maturity | stack/kernel ksync | LOW | S4b: `fork_current` cross-CPU bottom-push is safe only under the global `PROCS` lock; the lock-free Chase-Lev steal win is unused at runtime | revisit for S4c+ (a genuinely lock-free hot path) | open |
| F-0010 | worth-fix | linux repo (flagship) | HIGH | `~/Developer/linux` is mid **7,049-rename reorg** (stack/ · legacy-port/ · legacy-kernel/) UNCOMMITTED, AND **unsigned** (no `user.signingkey`, `commit.gpgsign` empty, HEAD `3786d41e` unsigned) | configure signing + decide the reorg-commit strategy before landing kernel S4b (founder decision pending) | open |
| F-0011 | worth-fix | source git topology | MED | two disjoint histories (`dev` vs `phase0/producer`, no merge-base) | go-live = fresh-root import (topology B) | open |
| F-0012 | anti-pattern | product-graph.html | MED | off-canon nodes: foundry / forge / oya-cli / Kafka-KRaft | rebuild auto-generated from SSOT, drift-gated; nodes → cloud-intelligence / cloud-scm / Pulsar | open |
| F-0013 | worth-fix | ADR-0378 / infra | MED | references LIVE K8s namespace `oya-ci-jenkins` + pod `oya-jenkins-0` (operational identifiers) | infra rename (namespace + pod + manifests) before the prose scrub | open |
| F-0014 | worth-fix | gate-catalog | MED | gate lane names `oya-governance-jenkins-*` are live identifiers in `cloud/cloud-data/contracts/openapi/cloud-data.yaml` + `specs/master-plan-sequencing.json` | coordinated gate-catalog + live-config rename | open |
| F-0015 | stale | source workspace | MED | #20 conformance drift: Cargo↔BUCK↔disk mismatches, naming/layer violations, orphan crates (full list in the consolidation-analysis output) | fold into the conformance + restore-build-green pass | open |
| F-0017 | stale | docs/decisions frontmatter | MED | B2 found ~14 ADRs the canon RULED superseded/archived (D-DISPOSITIONS) still read `status: Accepted` with empty `superseded_by` in source (0053/0057/0103/0160/0187/0509 + foundry-named 0097/0101/0102/0136/0137/0138/0143); the re-authoring never landed in source | **FIXED 2026-06-07** wave-2 `w2-adr-frontmatter-f0017` (9 supersede + 5 archive-as-Superseded), all pass validate-adr-shape; integrated `c43d38b20`. CAVEAT: the validator enum is {Proposed,Accepted,Superseded} — no `Archived`; 5 ADRs (0057,0097,0101,0102,0138) carry `status: Superseded` + a supersession_note recording archive-intent → if `Archived` is added to the validator, those 5 need a one-line second pass | fixed |
| F-0016 | anti-pattern | packs/ + regional-packs/ | MED | TWO parallel per-region pack dirs with conceptual overlap: `packs/{au,br,cn-pipl,eu,in,jp,kr,mx,us}-localization` (10, 61 files) vs `regional-packs/{eu,jp,kr,ksa,us-government}` (6, 7 files) — eu/jp/kr/us duplicated under different schemes; **33 live refs** in code/specs; governed by ADR-0251 (compliance-pack cell-certification) + ADR-0240 (sovereign-per-regional-pack) | consolidate to ONE canonical per-jurisdiction pack model unifying localization + compliance + sovereignty facets; LIVE → consolidate-with-repoint (not delete); sequence into Workstream A | open |

---

| F-0018 | worth-fix | registry/placeholder-debt | LOW | `adr-follow-ups.yaml:590` had a pre-existing invalid-YAML break (unquoted embedded colon); HEAD was already BAD | FIXED 2026-06-07 in BATCH-2 (quoted the scalar) — note other debt-ledger prose fields may have similar latent issues | fixed |
| F-0019 | worth-fix | linux repo (flagship) | MED | kernel S4b LANDED (commits `2adcbc2ef` reorg + `525e1e3ee` S4b, signed, 4 gates green) BUT (a) linux repo has **0 git remotes** → unpushed; (b) **89 pre-existing `kuberos-kernel→kernel` marker edits** remain uncommitted (build picks them up but they're not committed) | set up the linux-repo remote (github-mirror) + push; commit the 89 marker edits as a follow-up so the kernel builds under `kernel` consistently | open |

| F-0020 | worth-fix | stack/kernel (flagship) | MED | talos-init PID1 under `-smp 4` is a SEPARATE non-TLB SMP scheduler/state race (corrupted user-stack return; reproduces 0/4 with shootdown send on AND off — not caused/fixed by S4c). Not a required gate; smp-sched-demo -smp4 passes 6/6. | next kernel fix before talos multi-vCPU is land-ready; out of S4c scope | open |
| F-0021 | worth-fix | linux repo (flagship) | LOW | a 3rd in-flight rename `kuberos→operating-system` (stack/operating-system/talos-* + top-level docs) + WIP feature code (arch-*/src/user.rs libc-init syscalls) remain uncommitted after the F-0019 marker commit (left for founder, out of scope) | founder: commit the operating-system rename + the user.rs WIP as their own changes | open |

## Task #20 monorepo-conformance findings (2026-06-07; audit by `aacd0e3007fba6766`, load-bearing claims SPINE-VERIFIED before logging)

| ID | cat | area | sev | evidence | recommendation | status |
|---|---|---|---|---|---|---|
| F-0022 | worth-fix | source/ migration | CRITICAL | Task#20 + spine-verified on `cleanup/whole-tree-2026-06-07`: all 5 sibling repos still standalone (`~/Developer/{office,oyago,oyapy,claude,codex}`); **0** migration-product crates in the 731-member workspace (`oya-office-*`/`oya-transpiler-*`/`oya-cloud-k8s-*`/`ctrd*`/`oya-cloud-node-os-*`/`oya-cloud-kernel-*`); **0** `claude-agent-sdk`/`openai-codex-sdk` deps. Of L1–L11: only L8 legitimately dropped (canon); L5/L6 have PRE-EXISTING native same-named crates (NOT migration products); 8 lanes (office, transpiler-go/py, claude-adapter, k8s-95-body, containerd-44, node-os-51, framekernel) produced nothing | **not-yet-run, NOT regressed** — migration EXECUTION is Task #7 (PENDING) gated on Task #6 (finalize plan). Cleanup/hygiene operates on source's existing contents; the sibling pull-in is a SEPARATE pending body. Founder decision: schedule Task #6→#7 as its own program | open |
| F-0023 | anti-pattern | oya/observability | HIGH | spine-verified: `oya/observability/crates/oya-cloud-observability-{api,domain,kernel}` — cloud-substrate-named crates under `oya/` (products) with forbidden oya→cloud deps (api→domain→{cloud-iam,cloud-region,cloud-resource}-domain). Violates D-PURESPLIT (service dir exists ONLY under oya/ XOR cloud/; no oya→cloud internal dep) | **FIXED 2026-06-07** wave-3 `w3-observability` → integrated `b28dcc6ac`: 3 crates `git mv` to `cloud/cloud-observability/crates/` (mirrors minimal sibling `cloud/cloud-capacity/`), members+path-deps+BUCK repointed; native `oya-observability-*` left in place. Spine-verified: `cargo build --workspace` 0 errors, no dangling old-location refs, 6 gates re-run | fixed |
| F-0024 | worth-fix | source workspace | HIGH | spine-verified forbidden-vocab crate MEMBERS: `oya-vcs-{admission-gate,provider-execution-gate}-{app,kernel}` (4), `oya-ci-webhook-gateway-jenkins-adapter` (1), `Cargo.toml [workspace.metadata.oya.microservices.foundry]` + owners council-foundry/axis-foundry | **PARTIAL 2026-06-07**: wave-2 `w2-crate-del` DELETED+integrated the 4 oya-vcs crates + jenkins-adapter (spine-verified gone from tree+manifest, `c43d38b20`); REMAINING = the `foundry` workspace-metadata block + owners + the bulk vocab residue (foundry 4567 / jenkins 1626 / talos 256 / oya-vcs 253 / forgejo 18 tracked) → wave-3 vocab-eradication batch | in-progress |
| F-0025 | stale | crate naming (ADR-0056/0105) | MED | Task#20 + spine spot-check: 110 members carry a trailing token outside the 13-layer enum — largest class 35×`-usecase` — but **F-0029 REVERSES this: `-usecase` is CANONICAL per ADR-0106 (application→usecase), so those 35 are COMPLIANT, not violations**; the real non-compliant set is smaller (the `-contract`/`-evidence`/`-runtime`/`-service` classes, pending enum reconciliation). The 7 firewall gate crates (`accounting-registry-producer`/`automation-ratchet`/`cloud-ci-firewall`/`cross-artifact-agreement`/`registry-drift`/`staleness-reaper`/`total-accounting`) lack the `oya-` prefix entirely (spine-confirmed; **these are this-session's own crates**) | DO NOT rename `-usecase`. Reconcile the enum first (F-0029), recompute the rename set, THEN rename. The firewall-gate `oya-cloud-ci-*` prefix is the one clearly-valid sub-item (decoupled from the enum question) | open |
| F-0026 | stale | specs/crate-naming-audit.json | MED | Task#20 said "regenerate the audit". **SPINE CORRECTION 2026-06-07:** the file is NOT orphan (registered in `specs/root-hub-pointers.json` + referenced by DOC-CATALOG/README/ADR-0104/0105/0106/0107/0119/0122 + accounted in the producer faces) → NOT a stale-destroy. AND the gate `oya-governance-predictable-naming-app` computes live via `check(<paths>)` and never reads this JSON → the JSON is a catalogued snapshot, not the gate's SSOT. Regen is coupled to the ADR-0056/0105/0106 enum-version reconciliation (see F-0029) | **DEFER** — pair with the held BNF/naming work; reconcile the canonical enum first, then regen the snapshot to truth (no gate re-point needed; gate is live) | deferred |
| F-0027 | worth-fix | source stray dirs | LOW | Task#20 said "remove the stray dirs incl platforms/BUCK". **SPINE CORRECTION 2026-06-07 (audit mislabel):** `platforms/BUCK` is **LOAD-BEARING** — it defines the buck2 `execution_platform(name="default")` (macOS-aarch64 host) and `.buckconfig` wires platform detection via `prelude//platforms:default`. It is exactly the "live buck2 build-graph" keep-guard → **KEEP**. `services/` (0 tracked) + `crates/` (0 tracked) are untracked `.DS_Store`/`target/` cruft = no committable change + not git-recoverable → leave (or gitignore later) | KEEP platforms/BUCK; no source change. Nearly deleted live buck2 infra on the audit's word — verify-each-step caught it | wontfix |
| F-0028 | review | libs/oya-shared-*-kernel | LOW | Task#20: 17 `libs/oya-shared-*-kernel` reusable libs with zero `oya-*`-named Cargo dependents (partly a path-dep/BUCK-wiring measurement artifact) | review for keep/prune in a later pass; NOT actioned blindly | open |
| F-0029 | stale | crate-naming enum (ADR-0056/0105/0106) | HIGH | **SPINE-found 2026-06-07** while scoping F-0026: Task#20 flagged "35 `-usecase`→`-application` BNF violations", but **ADR-0106 = 'rename application→usecase'** — so `-usecase` is the *canonical* layer suffix, the 35 are COMPLIANT, and the audit's ~110 non-compliant count is OVERSTATED (it used a stale `application`-based enum). Enum-version drift exists across ADR-0056 ("12-layer"), ADR-0105 ("13-layer"), ADR-0106 (application→usecase) | RECONCILE the canonical layer enum to ONE current-truth version (honor ADR-0106) BEFORE any BNF rename (F-0025); the rename set + direction must be recomputed against the reconciled enum. Blocks/reshapes the held BNF work | open |
| F-0030 | anti-pattern | cloud-observability dep | MED | **surfaced 2026-06-07** by the F-0023 re-home (agent report, spine-noted): `oya-cloud-observability-domain` (now under `cloud/`) depends on the native products crate `oya-observability-domain` (under `oya/`) — a `cloud→oya` dependency (cloud reaching into products). Pre-existing (was a same-dir `../` sibling dep; the move made it a visible cross-tree path); builds fine | assess the D-PURESPLIT direction rule for `cloud→oya`: either the cloud crate should not depend on the products crate, or the shared piece belongs in `libs/`. NOT actioned in F-0023 (out of scope) | open |

## How to append
Add a row with the next `F-NNNN`, cite evidence (file:line or a reproduction), set status `open`. Flip to `in-progress`/`fixed` as the excellence pass addresses it. Promote clusters of related findings into a dedicated batch on the serial spine when ready.
