# Migration-only redirect

This frozen path exists only for legacy inbound references. It carries no
independent agent law, owner truth, workflow, fallback, or enforcement claim.

The complete session-loaded agent contract is root [`AGENTS.md`](../AGENTS.md).
Root [`CLAUDE.md`](../CLAUDE.md) is only its harness compatibility delta, and
root [`README.md`](../README.md) is human overview data.

All other non-root Markdown is frozen migration inventory, not current
authority or evidence that destination enforcement is live. This redirect's
retirement condition is explicit root-hub promotion evidence plus zero inbound
references; until then it remains a pointer only.
