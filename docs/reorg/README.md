# Reorg docs (human)

**Process law:** move plans live in `specs/reorg/` (see README there for singleton + PARKED/BLOCKED).  
**North star:** no net-new debt under `cloud/`, `oya/`, `infra/`, `libs/`, `tools/` except rehomes that remove dual-homes/legacy.  
**Brand (ADR-0619):** `gjc` / `omc` / `omx` / `hermes` are not live coordination authority; historical mentions in docs are provenance only. Delivery automation homes under `.grok/` when the kit is on trunk.
