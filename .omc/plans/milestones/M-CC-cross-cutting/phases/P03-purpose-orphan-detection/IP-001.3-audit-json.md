---
doc_class: ImplementationPlan
parent: ./INDEX.md
id: M-CC-P03-IP-001.3
title: Purpose-frontmatter audit (Part 3: JSON Registries/Specs)
status: complete
execution_unit: ChangeSet
changeset_contract: claimable-verifiable-bundleable-promotable
changeset_split_rule: split-before-execution-if-unrelated-lock-scope-or-deployable
final_shape_compliance: true
dependency_additions: []
purpose: Audit all JSON artifacts for purpose field; backfill where missing.
---

# M-CC-P03-IP-001.3 — Purpose-frontmatter audit (Part 3: JSON Registries/Specs)

## Purpose
Audit all JSON artifacts for purpose field; backfill where missing.

## Symbols-to-grit-claim
```
**/*.json::_meta.purpose
```

## Agent-prerequisites
Phase INDEX read; parent milestone INDEX read; MASTERPLAN §2 principles understood; M-CC-P01 ≥ P5 merged.

## Acceptance-test-commands
```
cargo run -p oya-foundry-fitness-purpose-audit -- **/*.json
```

## Done-criteria
- All JSON files in `registry/`, `specs/`, `contracts/`, and `.omc/` have `_meta.purpose` or top-level `purpose` field.
- No direct agent `git`/`gh` path.
- ICM completion row records count of JSON files backfilled.
