# Open Questions — Monorepo Consolidation Migration

## Pilot+5 Siblings → jason931225/oyatie Consolidation (ralplan deliberate) — REVISED 2026-06-05

### CRITICAL — authority flip (NEW, from Critic + Architect)
- [ ] **Authority migration `github-lane-unlocker-required` → `oya-ci-required` + mandatory signing is IN FLIGHT** — `.github/branch-protection.yaml@github-mirror/dev` (last changed 2026-06-04, one day before this plan, ADR-0513) declares the TARGET dev required context = `oya-ci-required`, plus `require_signed_commits: true` and `require_signed_tags: true`, and self-describes `github-lane-unlocker-required` as "shadow compatibility only, not merge authority." The LIVE `gh api .../branches/dev/protection` still returns required context = `[github-lane-unlocker-required]` (app_id 15368), `required_signatures.enabled=false`. In-flight producers exist: `github-mirror/feat/oya-ci-controller`, `oya/ci-controller/crates/oya-ci-controller-app/...` on dev, evidence `evidence/fd001/...apply...json`. A strict-serial ~11-lane campaign maximizes exposure to the flip landing mid-campaign. **Resolution in plan:** new gating pre-lane 0.4 (AUTHORITY-SNAPSHOT + SIGNING PRE-PROVISION) + per-iteration step 0 re-diff + signing promoted from optional to a HARD precondition. Needs USER decision: provision signing key NOW (recommended) vs accept campaign-halt risk if the flip lands.
- [ ] **Signing key / GitHub push auth (USER credential gate, PROMOTED to hard precondition)** — Local signing config is EMPTY (`commit.gpgsign`, `user.signingkey`, `gpg.format`, `tag.gpgsign` all exit 1 — NO key). `origin` is Forgejo (`forgejo.local/oya-admin/oyatie.git`); pushes/PRs MUST pin `github-mirror` (`github.com/jason931225/oyatie`) `--base dev`. USER must provide the signing key + push credentials before Lane 1. If `oya-ci-required` flips live, signed commits become mandatory for EVERY queued lane.

### MAJOR — surface modeling (NEW/corrected, from Critic)
- [ ] **db-engine has NO discoverable source tree** — Named in the LOCKED landing map (`db-engine→cloud/cloud-data`) but NO tree found in `~/Developer` (no `*db*`/`*data*`/`*engine*` dir) and none in the pilot. USER must confirm: does db-engine source exist, and where? If it does not exist yet, the `cloud/cloud-data` CREATE lane is DROPPED from the campaign (→ ~10 lanes, not 11). Hard blocker for that lane.
- [ ] **`cloud/cloud-k8s/` is a SIXTH k8s surface, un-modeled** — dev has a populated `cloud/cloud-k8s/` tree (ARCH.md, ARCHITECTURE.md, IP-001..IP-015, IP-CLUSTERAPI-*, AUDIT-FINDINGS json) ALONGSIDE the four `cloud/managed-k8s-*` services. The k8s MERGE lane must resolve: is `cloud/cloud-k8s` (a) a sixth merge target, (b) docs-only, or (c) out of scope? Gate the k8s lane on USER confirmation of the FULL merge surface.
- [ ] **containerd source is INTERMINGLED with k8s crates** — 44 first-party `ctrd_*` crates live INSIDE `linux/stack/kubernetes/crates/` (e.g. `ctrd_cap`, `name="ctrd_cap"`, edition 2024, our rewrite), NOT only in `_upstream_containerd/`. The container-runtime CREATE lane (`cloud/cloud-container-runtime`) and the k8s MERGE lane draw from the SAME physical `crates/` dir (139 crates = 44 ctrd_ + 95 k8s). Lane 0.5 must produce a crate-level split manifest assigning each of the 139 to k8s-MERGE vs container-runtime-CREATE vs vendored-exclude. USER confirms the split.

### MAJOR — physical source inventory (NEW, from Critic)
- [ ] **Per-lane SOURCE-INVENTORY allowlist not yet operational** — ONLY-OUR-CODE-MOVES needs a per-lane (source path, first-party allowlist, deny-globs) map. Resolved physical sources: framekernel=`linux/stack/kernel` (no_std, nightly-2026-02-28, build-std targets aarch64-unknown-none-softfloat + x86_64-unknown-none); node-os=`linux/stack/operating-system` (51 `talos-*` STD crates, channel 1.96.0 — NOTE: distinct from the EXCLUDED `linux/stack/talos-reference`); k8s+containerd=`linux/stack/kubernetes/crates` (entangled w/ `_upstream`, `_upstream_containerd`, `third-party`, `__pycache__`, `target`, `buck-out`, `prelude`, `toolchains`, `.omc`, `.omx`); claude SDK=`~/Developer/claude` (pkg `claude-agent-sdk`); codex SDK=`~/Developer/codex/sdk/rust` (pkg `openai-codex-sdk`); oyago=`~/Developer/oyago/crates` (oyago-cli/core/runtime); oyapy=`~/Developer/oyapy/crates`; office=`~/Developer/office/crates` (13 `oyaoffice-*` crates). Deny-globs differ PER tree. db-engine UNRESOLVED (above).

### MAJOR — brand-residue blast radius (NEW)
- [ ] **Heavy codename renaming required across most lanes** — office crates are `oyaoffice-*` (FORBIDDEN codename → `oya-office-*`); codex SDK pkg `openai-codex-sdk`; claude SDK pkg `claude-agent-sdk`; oyago/oyapy crates carry the `oyago`/`oyapy` codenames; node-os crates are `talos-*` (→ `oya-cloud-node-os-*`). The brand-residue gate FORBIDS `oyago/oyapy/oyaoffice/kuberos/foundry-*/oyatie-*`. Each lane carries a full crate-rename + `package.name==basename` + `oya-*` prefix pass as its first in-lane mutation. USER confirms canonical crate names per lane in Lane 0.5.

### Corrected citations (were fabricated/stale in prior plan)
- [ ] **First-party Buck generator** — prior `gen_first_party_buck.py:129` does NOT exist on dev. REAL generator = `scripts/ci/generate-first-party-buck.rs` (+ check `scripts/tests/generate_first_party_buck_check.rs`); it treats "Cargo metadata as an input format only" (members-based). Lane 0.6 no_std inertness proof must be EMPIRICAL (run `//:buck2-cargo-target-coverage-check` with excluded tree on disk), not argued from a citation.
- [ ] **ADR regen mechanism** — prior `oya doc adr-index` binary does NOT exist. REAL = `tools/oya-adr-index-regenerator-app` (a `//tools/...` Buck target — ANOTHER tools/ gate-load-bearing crate) driven by `.omc/automation/adr-index-pipeline.md`; index file is `docs/ADR-INDEX.md` (generated, `<!-- generated-by: adr-index-pipeline; do not edit -->`), ADRs live in `docs/decisions/ADR-NNNN-*.md`. Max committed on dev ≈ ADR-0370; in-flight branches reach adr-0505. Free block must be LIVE-recomputed via the regenerator immediately before docs-lane merge; "~0519+" stays a placeholder.

### Carried forward (still open)
- [ ] **`tools/` retirement vs live gate (LOCKED-decision collision — USER ratify)** — gate builds `//tools/oya-doc-staleness-inventory-app:...` AND the migration itself needs `//tools/oya-adr-index-regenerator-app`. tools/ holds 29 entries on dev; root Cargo.toml wires 723 members incl. many `tools/*`. Default = scope retirement to EXCLUDE gate-load-bearing targets and write tools/ as a STANDING exception in the canonical-homes invariant (NOT a deferrable cleanup lane). Any tools/ crate removed from disk must be removed from root members in the SAME atomic step or `//:buck2-cargo-target-coverage-check` reds. USER ratifies the locked-map deviation (gate G2).
- [ ] **Root vs docs/ governance-file location** — DOC-CATALOG.md / CHANGELOG.md exist under `docs/` on dev, ABSENT at root. Lane 0.5 determines which path the gate reads so Lane 0.7 writes the correct file (no duplicate).
- [ ] **Merge method = SQUASH only** — live repo settings: `allow_merge_commit=false`, `allow_rebase_merge=false`, `allow_squash_merge=true`, `delete_branch_on_merge=true`. The loop's "rebase/linear, no merge-commit" must be SQUASH-merge (which still yields linear history). Done-Definition reworded.
- [ ] **`agent-skills` sibling repo** — present in `~/Developer`, NOT in the landing map. Confirm out of scope vs missing landing zone.
- [ ] **no_std exclusion mechanism is itself a graph mutation** — root Cargo.toml has NO `exclude` key today (only `resolver="2"` at line 727, 723 members). Adding an `[workspace] exclude` entry (or keeping the no_std tree off the branch STD lanes rebase onto) is itself a root-manifest edit that perturbs `//:buck2-cargo-target-coverage-check`. Lane 0.6 must prove inertness of the EXCLUDED STATE INCLUDING the exclude-key edit, not merely the subtree's absence.
