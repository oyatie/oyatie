VERDICT: BLOCK

1. [HIGH, confidence: high] cloud/cloud-ci/gates/oya-cloud-ci-target-parity-app/tests/target_parity.rs:109 - The self-test now reads the PR worktree's committed `gate-baseline.generated.json` and uses it as the oracle, then asserts the live unwired-test set equals that same baseline at target_parity.rs:123. That is self-referential while FRIC-1781112000 is open: a future PR can add new target-parity debt, regenerate the baseline in the same change, and satisfy `unwired_tests == baseline_keys`. The old `unwired_tests.len() == 614` was crude and set-blind, but it was an independent tripwire against net count growth from a same-PR generated-baseline refresh; baseline-only laundering would have failed unless the Rust test oracle was also edited. Minimal fix: compare new debt against the merge-base baseline (`git show origin/dev:<face>` or true merge-base ref) and require an explicit baseline-grow exception record, or keep an independent non-baseline count guard until the merge-base ratchet fix lands.

2. [HIGH, confidence: high] cloud/cloud-ci/gates/oya-cloud-ci-target-parity-app/tests/target_parity.rs:128 - The expected verdict is now derived from the mutable baseline: empty baseline means expected `Verdict::Green`. Origin/dev instead unconditionally expected `Verdict::Red` while the corpus has known unwired-test debt. If the producer/evaluator drops `member_test_code_without_rust_test_target` findings and the generated baseline drops with it, this test accepts a false green. Minimal fix: keep the live-corpus verdict independent of the generated baseline until the zero-debt transition is explicitly reviewed and recorded; shrink/growth should be proven against merge-base, not against the current PR baseline.

Supporting evidence:
- Diff inspected with `git diff origin/dev...HEAD`: 3 files changed, namely `target_parity.rs` plus two generated accounting/scm faces.
- `origin/dev` and `HEAD` target-parity baseline key counts are both 614; extracted key-set diff is empty. This PR does not itself add target-parity baseline debt.
- FRIC-1781112000 in `/Users/jasonlee/Developer/oyatie/.omc/ultragoal/friction-ledger.jsonl:67` states the open gap directly: baseline-block-on-new gates compare against the baseline committed in the PR, so same-change regeneration launders new debt; the fix is merge-base baseline comparison. Line 70 records the second proven instance.
- The baseline producer freezes current keys from `evaluate_keyed` (`cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app/src/lib.rs:614`, `:673-681`), and the firewall contract only works when the committed baseline is a frozen reference (`cloud/cloud-ci/gates/oya-cloud-ci-firewall-app/src/lib.rs:21-27`, `:165-195`).
- Requested Buck validation passed:
  - `buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-target-parity-app:oya-cloud-ci-target-parity-app-unittest //cloud/cloud-ci/gates/oya-cloud-ci-target-parity-app:oya-cloud-ci-target-parity-app-gate`
  - Output included `test result: ok. 5 passed; 0 failed` for the unit target, `test result: ok. 1 passed; 0 failed` for the live corpus gate, and `Tests finished: Pass 2. Fail 0. Timeout 0. Fatal 0. Skip 0. Omit 0. Infra Failure 0. Build failure 0`.

Single most dangerous residual scenario: a future PR adds a workspace member with Rust test code but no Buck `rust_test`, runs the normal face regeneration/settle path, commits the grown target-parity baseline, and all checks stay green because both the firewall and this self-test trust the PR-local baseline instead of the merge-base frozen baseline. The result is a permanently uncompiled test surface admitted by the exact ratchet that was meant to block new debt.
