# AUTH-005 Wave-2b plan (corrected targets) — 2026-06-23

Wave-2 produced 5 PRs (#819-#823) of 9 planned instances. 4 instances produced no PR; 2 of those had codex-cited paths that had DRIFTED. Corrected, verified targets below. Launch Wave-2b STAGGERED (after the current merge train clears + agents free up — rate-limit discipline). Pre-assign ADR ids per lane to avoid collision.

1. **dsr-authz (ADR-0589, C16 — CRITICAL):** real path `oya/compliance/crates/oya-dsr-usecase/src/lib.rs` (codex cited a nonexistent `compliance/ports/dsr-usecase`). Antipattern CONFIRMED: `pub allowed_surfaces: Vec<String>` (line ~119) consumed as authorization (lines ~863, ~1114) — GDPR erasure-cascade authorizes caller-supplied principal/surfaces; fabricate principal → execute erasure. Kernel at `cloud/tenancy/crates/oya-tenancy-dsr-cascade-kernel/src/lib.rs`. Fix: derive principal from authenticated transport; remove caller-controlled allowed_surfaces; cloud-iam PDP decide for dsr.cascade.execute; default-deny on absent/invalid/stale evidence. RED: forged principal cannot trigger erasure.

2. **accounting-payroll-authz (ADR-0593, money — CRITICAL):** CONFIRMED — empty `MiddlewareChain::new()` on money-mutation routers: `oya/accounting/crates/oya-accounting-journal-infrastructure/src/lib.rs:145` and `oya/payroll/crates/oya-payroll-run-infrastructure/src/lib.rs:134`. Unauthenticated money mutation. Fix: wire verified-principal + PDP authz middleware fail-closed. RED: unauthenticated mutation → 401, cross-tenant → 403.

3. **accounting-idempotency-tenant (ADR-0592, money — CRITICAL):** codex framed as "billing" but real surface is accounting: `oya/accounting/crates/oya-accounting-journal-storage-adapter-inmemory/src/lib.rs:80` `records_by_idempotency_key: BTreeMap<String, _>` + `reserved_idempotency_keys: BTreeSet<String>` — RAW-STRING key. VERIFY whether the key includes tenant_id at construction; if not, tenant A can collide/suppress tenant B records. Fix: tenant-scoped + body-fingerprinted idempotency key. RED: same idem-key across tenants does NOT collide; changed body same key is rejected/distinguished. (Confirm the same defect in the durable adapter, not just inmemory.)

4. **crm-authz (ADR-0594, CRITICAL):** surface CONFIRMED — `oya/crm/crates/oya-crm-revenue-app/src/adapter/http.rs:7` `HttpRequest { tenant_id, principal_id, ... }` carries caller-supplied identity; gRPC at `adapter/grpc.rs`; proto `oya/crm/contracts/crm-v1.proto`. TRACE whether identity is server-verified before use. Fix: require verified principal + PDP decide; body-supplied identity never authorizes. RED: forged-identity mutation → 401/403.

Apply the 4 hard-won authz lessons to each: unforgeable VerifiedPrincipal (private fields + pub(crate) ctor) · true blast-radius (PDP resource = TARGET tenant from trusted source) · authn before body (route_layer/FromRequestParts + DefaultBodyLimit on any LIVE binary) · PDP-fault-denies (trait contract, not catch_unwind). Cross-model codex review every PR before merge.
