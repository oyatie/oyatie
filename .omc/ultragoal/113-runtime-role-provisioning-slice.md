# #113 — Provision NOBYPASSRLS runtime roles for tenancy + SCIM durable adapters + fix tenancy facade durability test — architect-scoped 2026-06-22 (dev 312382f3a)

Completes the prerequisite for the durable-store acceptance tier: the 0001 RLS policies are written `TO <runtime_role>` (so the role MUST pre-exist or the migration fails), and the new shared guard requires the serving role to be a USAGE-member of that role — but NEITHER tenancy nor SCIM adapter provisions the role (only the outbox adapter has `0000_runtime_role.sql`). ALSO fixes a real defect: the tenancy facade durability test reads the SUPERUSER url as its runtime DSN → `build_postgres_router` → `assert_rls_enforceable` rejects it → `.expect(...)` PANICS (the same #799 MAJOR, unfixed in tenancy). #101 (CI live-PG lane) is DEFERRED for founder input (doctrine-fraught); this slice is its prerequisite + standalone value.

## Mirror the proven precedent
`libs/oya-data-outbox-adapter-postgres/migrations/0000_runtime_role.sql` (role `oya_data_outbox_runtime`) + `0001_outbox_events.sql:73` (table GRANT) + `src/lib.rs:235-238` (LIVE_MIGRATIONS include_str!) + `:362-396` (harness: GRANT role TO app_login + SET ROLE). Read it; replicate the production-faithful model.

## The change
1. **`tenancy/adapters/tenant-lifecycle-store-postgres/migrations/0000_runtime_role.sql`** (NEW): idempotent `DO $$ ... IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname='tenancy_lifecycle_runtime') THEN CREATE ROLE tenancy_lifecycle_runtime NOLOGIN NOBYPASSRLS; END IF; END $$;` + `CREATE SCHEMA IF NOT EXISTS tenancy_lifecycle;` + `GRANT USAGE ON SCHEMA tenancy_lifecycle TO tenancy_lifecycle_runtime;`.
2. **`iam/adapters/identity-scim-store-postgres/migrations/0000_runtime_role.sql`** (NEW): same for role `identity_scim_runtime`, schema `identity_scim`.
3. **Table GRANTs** (`GRANT SELECT,INSERT,UPDATE,DELETE ON <each table> TO <runtime_role>;`) — tables exist only after 0001, so these go AFTER table creation. DECISION (executor): check for a migration-immutability gate (`git grep -n "immutab\|append-only" origin/dev -- cloud/cloud-ci`). If migrations are gated-immutable → put grants in a NEW `0002_runtime_role_grants.sql`; else append to `0001_*.sql` (the outbox put table GRANT in its 0001). Prefer the smallest born-accounting footprint that respects immutability. Tables: tenancy = `tenancy_lifecycle.{tenancy_lifecycle_tenants,applied_writes,operations}`; SCIM = `identity_scim.{identity_scim_users,identity_scim_groups}`.
4. **Live harness** (`tests/live_rls.rs` in both adapters): `include_str!` the new `0000` (+ any new `0002`) and prepend `0000` to the migration-apply loop (role BEFORE 0001's `TO <role>` policies). After applying, `GRANT <runtime_role> TO <app_login>` (membership, default INHERIT) so the app login is a USAGE-member → the `TO <role>` policies apply AND the shared guard's `pg_has_role(...,'USAGE')` passes. Remove now-redundant direct table-GRANT-to-app_login if present (or keep harmless). Mirror the outbox `SET ROLE`/membership model.
5. **Tenancy facade durability test** (`tenancy/facade/tenant-lifecycle-app/tests/acceptance.rs`): change `LIVE_URL_ENV` from `OYA_BACKBONE_POSTGRES_URL` to `OYA_BACKBONE_POSTGRES_APP_URL` (mirror SCIM #799), so the durability test boots `build_postgres_router` against the NON-superuser app role and passes `assert_rls_enforceable` instead of panicking.

## born-accounting (the #793 non-crate-file lesson)
The new `.sql` files are NEW tracked paths. Each adapter's existing `0001_*.sql` is reachability-covered by (a) the `rust_library` `srcs` glob `migrations/**/*.sql` AND (b) explicit listing in the `-live` `rust_test` `srcs`. The embedded-asset-hermeticity gate (cloud/cloud-ci/gates/oya-cloud-ci-embedded-asset-hermeticity-app) requires every `include_str!` literal to resolve in the buck `__srcs` tree → so `include_str!` the new files in the harness AND add `"migrations/0000_runtime_role.sql"` (+ any 0002) to each `-live` BUCK `srcs`. Mirror exactly how 0001 is covered. OWNERS + ADR governed-surfaces VERBATIM justification for each new path (find the owning persistence ADR the 0001 files use; reuse it).

## Done-bar / honest scope
- buck2 build + test the affected adapters + facades; DB-free lane green; live tests SKIP clean (no live PG locally). **LIVE end-to-end of the SQL is NOT verifiable locally (dark until #101)** — rely on mirroring the proven outbox precedent + adversarial SQL review; PR states the live-CI proof is pending #101. This is honest scope, not overclaim.
- Regen Cargo.lock; materialize faces; firewall GO-LIVE + freshness + affected-set + embedded-asset-hermeticity + face-settle --verify locally. git status clean.
- Fresh worktree off origin/dev; never touch canonical checkout; buck2 for build/test; trailer EXACTLY `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. After green: STOP, do NOT self-approve — orchestrator runs adversarial SQL/born-accounting review before merge (verify: idempotent role creation, NOBYPASSRLS present, grants minimal + correct, harness membership grant makes the guard pass, 0000-before-0001 ordering, no migration-immutability violation, born-accounting complete).

## #101 DEFERRED (founder-gated, do NOT build here)
Live-PG CI lane is doctrine-fraught (non-hermetic + shell-driven vs founder no-shell/cloud-native; touches required-CI). Founder must choose the shape (transitional GH-Actions `services: postgres` + cargo bridge lane — precedent: app-shell-codegen — vs operator-provisioned ephemeral PG as the cloud-native destination) AND whether it joins oya-ci-required (start NON-blocking regardless so a flaky live-DB lane can't wedge the merge queue). Proposed lane recorded in task #101.
