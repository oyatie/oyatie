# SESSION-HANDOFF 2026-07-10 — reorg + open-PR drain + pipeline productization program

Read FIRST on a fresh session. Durable-local (gitignored `.omc/`, survives across sessions). Companion durable artifacts in this dir; key decisions also in the memory store.

## 0. Durability map (where everything lives)
- **Decisions** → memory files: `reorg-boundary-rulings-2026-07-10.md`, `model-effort-routing.md`, `orchestration-scratch-harvest-and-reap.md`, `oci-talos-cluster-access.md` (+ MEMORY.md index).
- **The plan** → `.omc/ultragoal/reorg-openpr-pipeline-plan-2026-07-10.md` (the living master, all directives folded in).
- **Living Monorepo** → `.omc/ultragoal/living-monorepo-pm-2026-07-10.md` (PM) + `living-monorepo-graph-pattern.md` (design) + `living-monorepo-pm-seed.json`; graduating to ADR-0617.
- **Research findings (raw)** → `.omc/ultragoal/findings-*.json` (phase1-triage, d0-docs-exploration, documentation-adrs-skill-design, phase2a-rebase-drain).
- **Hypervisor seed** → `.omc/ultragoal/hypervisor-tier-research-seed.yaml`.
- **Repo/GitHub (durable):** ADR-0615 MERGED to dev (squash `76bb6ae1100a`, PR #1317, issue #1316).

## 1. LANDED this session
- **ADR-0615** ratified + merged (`76bb6ae1100a`): the 14 capability boundary rulings resolving ADR-0562's flagged_boundaries (substrate/product split). See `reorg-boundary-rulings-2026-07-10.md`.
- **Phase-2a open-PR drain COMPLETE — all 6 rebase PRs merged to dev:** #1300 (billing metering), #1305 (arch-graph golden retire), #1287 (iam tenant-rbac→PDP), #1209 (SECURITY-PROGRAM→security-program/ subtree+OWNERS), #1307 (shell contract + alloc-free grants), #1251 (firewall frozen-ref de-commit, ADR-0616). All independently Fable-reviewed (reviewer≠author) + Tide-auto-merged on `oya-ci-required` green.
- **PM "Living Monorepo Governance Graph"** generated (Ouroboros PM interview, ambiguity 0.10, 0 decide-later blockers) → harvested durable; ADR-0617 authoring in progress (worktree `oyatie-wt/living-monorepo-adr`, branch `design/adr-0617-living-monorepo-graph`).

## 2. Founder decisions (2026-07-10)
- 14 boundary rulings (Q1–Q14) — substrate/product split; drive/recordings=storage/facade (CONFIRMED, corrected from an initial mis-read); imaging/diagnostics/emergency=app/healthcare (0-crate parks); dev-cli→ci; governance NOT folded into compliance; compliance/audit stay separate.
- **CI contexts:** `oya-ci-required` → `ci-required` + NEW non-blocking `ci-advisory` (LOCKED; final de-brand step; ADR-0515 amendment; STOP-THE-WORLD parallel-add→bake→flip; D-7 ladder → advisory/required).
- **Gate de-brand:** `oya-check-*`→`check-*` (governance/); workflow display strings; firewall crate already de-branded (ci/facade/baseline-ratchet). Gate all on "survives the reorg".
- **#1294** flags → let Phase-1 QA classify (result: REWORK — unenforced JSON, no owned-Rust gate).
- **Batch-2** doc-drains run PARALLEL with the Phase-2 open-PR drain.
- **self-improve** DEFERRED until the program settles; first target = the P7 routing policy.
- **Hypervisor tier** → SEED A RESEARCH SPIKE (seed authored, see §4).
- **Living Monorepo Governance Graph** (PM ratified) — monorepo mgmt + lifecycle = ONE federated content-addressed graph (extend `governance/corpus/`); every artifact a governed node or a projection; ephemera (worktrees/branches/checkpoints/.omc/.omx/tmp) = TTL'd work-nodes harvested→reaped. Forks: federated per-cell; **fail-closed** after a one-time cleanup of the sprawl (369 worktrees / 550 local / 91 remote branches); git-artifacts-as-graph-nodes; console+gates together; local-set covered; ungoverned-creation allowed-but-auto-expired; **reap-by-default + harvest-on-durable-signal** (commits unreachable-from-origin/dev OR a harvest marker); per-kind TTL, last-touch-resets; harvest sink = **park-as-durable-node** (uniform). **Query substrate:** JSON = write-authority SSOT, DB = a derived, rebuildable, de-committed **materialized query index** (owned-Rust engine; embedded Rust DB — SQLite/DuckDB/CozoDB — behind an owned port; materialized-projection-of-a-source-of-truth, Palantir-Foundry-Ontology reference-only). All facets of the productized dev pipeline (Workstream P) on the corpus (Workstream D). Graduates to ADR-0617.
- **OCI/Talos = TEMPORARY dev-DATABASE only, NOT the deploy/ship target** (transient; local Postgres > SQLite for dev-DB because of RLS parity; see `oci-talos-cluster-access.md`).

## 3. The plan (see the plan doc for full detail)
- **Ordering doctrine:** land content before the move that relabels it; retarget content whose home already moved; never merge at a vacated location. Only the single move-slot + the 2 pending crate-move caps (ci, intelligence) serialize.
- **Phases:** 1 /ultraqa triage (DONE) → 2 open-PR drain (2a rebases IN PROGRESS, 2b reworks) → 3 reorg Batches 1–5 → 4 de-brand tail.
- **Agentic delivery fabric per-task lifecycle (15 stages):** research→design-doc→plan+spec→RED/GREEN→code→review→harden+sec→edge+perf+capacity→simplify→**full-test-suite (regression+integration+ladder)**→slop-clean→doubt-driven+verify→ci/cd→ship→self-improve. Each stage the right {model,effort,skills,MCP,context} bundle; each a fan-out point.
- **Workstream P (productize the ENTIRE dev pipeline as a portable product):** P0 neutralization audit (strip ADR-quotes/oyatie-nuance from engine code → policy-as-data; stranger's-repo test), P1 canonical, P2 automated+productized (auto-rebase queue = class-fix for "all PRs went stale"), P3 portable+universal, P4 ci-required/ci-advisory, P5 comprehensive+flaky-test, P6 security (CVE/RustSec/Trivy/Snyk/Veracode/Checkmarx/BlackDuck/AST/PrismaCloud/Wiz/Aqua/Cycode), P7 routing bundle, P8 operational-friction prevention. Method: root-cause (not symptom) → /idea-refine + /doubt-driven-development → canonical/universal capability; automation-where-possible + enforcement-everywhere.
- **Workstream D (docs-as-code):** PRD↔RFC↔ADR as a relational graph on governance/corpus/ (extend the EXISTING cross-artifact-agreement evaluator, ~70% built); docs become BUILD ARTIFACTS; deterministic invariants block, LLM advisory-only. D3 = the `/documentation-and-adrs` skill (see §4).
- **Benchmark battery:** security, architecture-conformance (Axivion/ArchUnit/FINOS-CALM), perf (K6), CD (Harness), chaos/resilience (Chaos Mesh/Litmus; continuous; multi-region DB failover within RTO/RPO), code-quality (SonarQube/CodeClimate), IDP/catalog (Backstage/Qovery/Cortex/OpsLevel), event-driven (KEDA), IaC-testing+policy-as-code, capacity/breaking-point, observability/telemetry (synthetics/RUM/high-cardinality/alert-fingerprinting/synthetic-trace), identity/access (IAM fuzzing/priv-esc/federated-token/secrets-audit), multi-cloud/cross-cloud (isolation/interop/drift/K8s-compliance), network (latency/jitter/MTU/egress-anomaly/unified-overlay), active-active/multi-region/DR.

## 4. Research findings (raw JSON in findings-*.json)
- **Phase-1 triage (12 open PRs, verified):** 6 REBASE (#1209,#1251,#1287,#1300,#1305,#1307), 6 REWORK-Python-slop (#1285,#1290,#1293,#1294,#1296,#1297). #1293→comms retarget; #1307 stays oya/application (app/ not landed); #1251 red = DUPLICATE ADR-0614 (renumber→0616).
- **D0 docs-exploration:** extend cross-artifact-agreement (don't rebuild); Docs-as-Code spine + Relational-Wiki substrate (Backstage reverse-edge trick + Foundry projection-of-reality) + AI advisory-only. Node/edge model + enforcing gates specified.
- **/documentation-and-adrs skill design:** HYBRID — seed thin `.omc/skills/documentation-and-adrs/SKILL.md` (9-step) + AMEND 7 owned-Rust surfaces (corpus core/doc-parser/extract, the evaluator +7 codes, fixtures, specs/ontology-packs/ DATA, Proposed ADRs). Do NOT amend deep-research (compiled-in) or ralplan (shared OMC) — ingest one-way. 7 open founder Qs listed in the plan (D3).
- **Hypervisor gap:** compute/ charters VM but no VMM/host-virt exists (kernel = guest-side virtio only; Firecracker only referenced for the ADR-0023 intel sandbox). Seed authored: research VMM (Cloud Hypervisor/Firecracker), KVM-class kernel gap (Asterinas vs transitional Linux-KVM), isolation spectrum, confidential computing (SEV-SNP/TDX), tier placement (compute/core/vmm vs hypervisor/ tier). Ready for `ooo run` / QA.

## 5. Execution state (IN FLIGHT — RESUME HERE)
- **✅ ADR-0617 (Living Monorepo Governance Graph) MERGED** — squash `d7568c731b5e` (2026-07-10 09:45), 2 review rounds (r1 REQUEST CHANGES → all 10 fixed → r2 APPROVE), CodeRabbit threads resolved → CLEAN → `gh pr merge --squash`. Worktree + branch cleaned. Issue #1318. **Merge-flow lesson (re-confirmed [[pr-merge-admission-mechanics]] pt 4):** green `oya-ci-required` is NOT enough — must **resolve addressed CodeRabbit review threads** (GraphQL `resolveReviewThread`) to reach mergeState CLEAN, THEN `gh pr merge --squash` (no passive auto-merger). Applies to every PR in this program.
- **✅ MERGED THIS SESSION (2026-07-10): ADR-0617 (`d7568c73`) · #1285 (`efc7256e`) · #1320 + ADR-0618 (`1e434763` — cross-model-hardened full-fidelity contract-slice DSL gate; opus + 4 gpt-5.6-sol rounds closed every fail-open). ▶ IN FLIGHT: staged 5-PR re-convert (`w732xcg0f` → #1290/1296/1297 byte-faithful data-only + #1293→`comms/` + #1294 casualty per ADR-0618), and CI-efficiency /ultraqa (`bj7ch9t7n`, codex gpt-5.6-sol). HEADLINE CI finding: the buck2 job builds+tests ALL `//cloud/cloud-ci/...` every run because a committed `.claire/worktrees/.../oya-payroll-run-usecase` STALE BUCK pkg breaks `rdeps` → affected-set stuck advisory; only 2/7 buck2 jobs cache buck-out. Fix = part of the `cloud-ci → ci/` reorg (destination is `ci/`, NOT cloud-ci). Pipeline-product FULL scope captured → [[pipeline-product-full-scope]] (portable gov+quality+compliance+security fabric; ci/=ONE slice; 5 bars: cloud-native/API-driven/hyperscaler/enterprise-prod/canonical-universal-portable).**
- **▶ NEW ACTIVE THREADS (session 2026-07-10 cont.):** (1) **Unified model-routing engine** — founder wants ONE quantitative/scored/mechanical router for claude+codex+gjc+hermes+agy (gemini retired→agy); DeepSWE-grounded ([[model-effort-routing]], [[current-frontier-model-catalog]], [[unified-model-routing-engine]]); design v1 at `.omc/ultragoal/unified-routing-engine-design-v1.md` (Hermes has NO scoring formula; owned `oya-intelligence-model-routing` + ADR-INT-001 already spec the numbers, unimplemented; destination = de-branded `intelligence/` capability). Source repos cloned to `scratchpad/routing-repos/`; hermes-agent source-verify + decomposition study still owed. (2) **Reconciliation audit register** at `.omc/ultragoal/reconciliation-audit-register.md` — 6 confirmed cross-dir dups + 3 same-dir, rename `messaging/`+`gateway/`, keep audit/compliance/governance triple; visibility=PUBLIC universal + `oya-data-boundary-kernel` fan-in 128; missing-invariant gate family (anti-dupe/fan-in/visibility/cross-cap-port/hermeticity). (3) **5-PR batch → PIVOTED to root-cause (founder chose "enrich the gate DSL to full fidelity" 2026-07-10).** The 4 contract-slice PRs (#1290/1294/1296/1297) were confirmed WHACK-A-MOLE: the JSON `contract-slice-conformance` gate DSL can't express the 287-line Python validators, so each conversion round drops assertions + CodeRabbit flags the next gap + `conversation_resolution:true` never clears. **Fix: ONE gate-enhancement PR lands the full primitive union first (`slice-dsl-build` opus executor, branch `ci/contract-slice-dsl-fidelity`), then the 4 conversions become DATA-ONLY.** Spec: `.omc/ultragoal/contract-slice-dsl-enrichment-spec.md` (Group A consolidate #1294 `scalar_str` + #1296's six primitives which CONFLICT on rebase + harden fail-closed; Group B add required_false_fields/positive-marker/`production-ready`≡`production ready` bug/regex-patterns/exact_projected_sequence/array_cardinality/value-set; Group C cross-doc/filesystem/YAML/full-schema = OUT → scope-boundary ADR + a separate cross-ref gate; **#1294/release is the honest casualty ~70% YAML+cross-doc**). Then re-convert #1290/1296/1297 data-only → resolve threads → merge; #1293+#1294 also need reorg redirect (oya/messenger→comms/, oya/feature-flags→flags/). (4) **#1285** through round-4 (bidi/Trojan-Source) APPROVED; blocker was CI churn (my rapid pushes cancelled runs — memory pt6) not a defect; `pr1285-finalize` executor rebased onto dev + merging. **Merge-flow: green oya-ci-required + resolve CodeRabbit threads (resolveReviewThread) → CLEAN → `gh pr merge --squash`; strict admission body = NO parenthetical before field colon.**
- (superseded resume detail below — ADR-0617 landed:)
  - **Round 1 (a38429d): REQUEST CHANGES** — 5 confirmed/plausible + 5 suggestions. ALL 10 addressed in `ec9848232`: §3 durable signal now covers dirty/untracked worktree state + reap archives-never-rm/second-verifier gated; §3 TTL gates eligibility / durable signal gates disposition (killed "reap on sight"); §5 restored owned-Rust/no-new-deps constraint + ADR-0520 embedded-DB exception; frontmatter `depends_on` += ADR-0520/0541/0580 (0541/0580 promoted from `related`); §1/§6 fixed ADR-0522 graph conflation + ADR-0517-doctrine-vs-0580-spike anchoring; §1 scoped "structurally impossible" + marked stage-list illustrative; §5 named the invariant battery; Open-Q scoped fail-closed timing. **Finding #11 (pre-existing ADR-0515 missing `amended_by:[ADR-0516]`) DEFERRED** as the first fixture for ADR-0617's new amends-reciprocity invariant — NOT a ratification-PR change.
  - **Round 2 (agent `adr0617-review-r2`, Fable, reviewer≠author): IN FLIGHT** — verifying each of the 10 fixes truly closes + no new inconsistency + gate-safety re-confirmed.
  - **IMMEDIATE NEXT:** when round-2 returns — if **APPROVE**, stamp PR #1319's `## Code Review` section (`- Reviewer: independent Fable code-reviewer (reviewer≠author, round 2)` / `- Verdict: APPROVE` / `- Resolved items: <the 10 round-1 findings, closed in ec9848232>` / `- Deferred items: ADR-0515 amended_by half-edge (follow-up fixture)`) via `gh pr edit --body-file`; the edit triggers a fresh `oya-ci-required` run and **Tide auto-merges on green**. If **REQUEST CHANGES**, fix in the worktree, re-push, re-review. (If a review verdict was lost to compaction, re-dispatch a Fable review of the branch vs origin/dev.) Admission literals: `## Issue`→`Refs #`; `## Traceability`→`Catalog records touched`/`Cross-axis contracts touched`/`ADRs cited`; `## Evidence`→`Audit-chain emission`/`Foundation-bypass referenced`/`Per-pack regulator-watch impact`.
- **Phase 2a: COMPLETE** — all 6 rebase PRs merged (#1300/#1305/#1287/#1209/#1307/#1251 [ADR-0616]). **Tide auto-merges on `oya-ci-required` green** (branch-protection requires only that context; CodeRabbit CHANGES_REQUESTED does NOT block).
- **Phase 2b (reworks, NOT started):** #1290, #1293, #1296, #1297 (+#1294, #1285) → owned-Rust contract slices via the merged `ci/facade/contract-slice-conformance` gate; #1293→comms retarget.
- **Reorg Batches 1–5, de-brand tail, Living Monorepo MVP:** NOT started (see the plan + reorg-plan; Living Monorepo MVP = harvest/reap reconciler + materialized query index + the one-time 369-worktree/550-branch cleanup, gated on ADR-0617 landing).

## 6. Standing directives (this session, durable)
- Autonomous drive at the hyperscaler bar, properly-the-first-time. Owned-Rust only; no new python/shell/CLI/deps. No de-brand/reorg regression. reviewer≠author; merge only on green oya-ci-required; never admin-override.
- **Routing** (`model-effort-routing.md`): {model(cross-model Claude/Codex/Gemini), effort, skills, MCP, context} per subagent by complexity+scope; keep orchestrator context lean (push detail down, only conclusions up).
- Neutral-engine + policy-as-data (portability anti-pattern = ADR-quotes/oyatie-nuance in engine code; stranger's-repo test). Entire dev pipeline is the product. Enforcement everywhere, automation where possible. Root-cause via idea-refine + doubt-driven.

## 7. Next actions
1. **IN PROGRESS:** author + merge **ADR-0617** (Living Monorepo Governance Graph; amends ADR-0516 fabric apex, depends_on ADR-0517/0522; Proposed) — the graduation of the PM. Then reciprocal `amended_by:[ADR-0617]` on ADR-0516, PR, independent review, merge.
2. **Phase 2b reworks** — #1290/#1293/#1296/#1297 (+#1294/#1285) → owned-Rust contract slices via the merged contract-slice gate; fan out cleanest-first (#1293→comms retarget).
3. **Reorg Batch-1/2** (parallel with 2b per founder ruling).
4. **Living Monorepo MVP** (post-DD): harvest/reap reconciler + materialized query index + the one-time git/ephemera cleanup; Workstream D (`/documentation-and-adrs` + 7 amend surfaces); the hypervisor seed run; then self-improve on the P7 routing policy.
5. **De-brand tail** (last, STOP-THE-WORLD): `oya-ci-required`→`ci-required` + `ci-advisory`.
