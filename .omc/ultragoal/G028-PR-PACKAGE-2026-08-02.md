# G028 PR package — serialize oya-arm64 runners via 22Gi ephemeral request

State: **MERGED — EXACT APPROVED HEAD — PROTECTED PR GREEN — LIVE GITOPS BLOCKED/INERT — NO MANUAL APPLY**

## Candidate

| Field | Value |
|---|---|
| Worktree | `/Users/jasonlee/Developer/oyatie/.worktrees/g028-runner-disk-request-20260802` |
| Branch | `g028-runner-disk-request-20260802` (deleted post-merge) |
| Base at open | `b651080374113aeb57500eecbd9d1326f0404e48` |
| Head | `051bc7ec603d49b838a400471a01778b966b2b8c` |
| PR | https://github.com/jason931225/oyatie/pull/1529 |
| Merge commit | `0c1014b87f0d881a821faa6a872b309deba0cfbf` |
| Merged at | 2026-08-02T21:06:55Z |
| Files | `infra/arc/runner-scale-set-arm64-values.yaml` only |

## Change (now on origin/dev)

- `resources.requests.ephemeral-storage`: `20Gi` → `22Gi` (verified on tip)
- `resources.limits.ephemeral-storage`: remains `60Gi`
- `maxRunners`: remains `3`
- `minRunners`: remains `0`

## Why

FULL-tier cold baseline pods were DiskPressure-evicted after using 29.49–30.93 GiB binary while two 20Gi-request runners could still co-schedule on a worker with 42.756 GiB allocatable. #1526 job 91524468086 annotation confirmed "self-hosted runner lost communication" during FULL baseline materialize.

Binary arithmetic:

- allocatable = 45,909,593,217 B = 42.7566405544 GiB
- 2 × 21 Gi still fits; 2 × 22 Gi excludes

## Admission evidence

1. Independent peer APPROVE on exact head `051bc7ec6` (g028-review-retry + g028-review-opus)
2. PR #1529 opened against `dev`
3. Protected `oya-ci-required` SUCCESS (12/12 constituent SUCCESS)
4. Squash-merged 2026-08-02T21:06:55Z → merge commit `0c1014b87`
5. `git show origin/dev:infra/arc/runner-scale-set-arm64-values.yaml` shows request `22Gi`

## Post-merge observation — fail-closed blocker

Read-only observation after merge proves the declared path has **not** converged:

- `AutoscalingRunnerSet/oya-arm64` request = `20Gi`, limit = `60Gi`
- `EphemeralRunnerSet/oya-arm64-lh4ch` request = `20Gi`, limit = `60Gi`
- all three live runner pods request `20Gi`
- Helm release `oya-arm64` revision 12 was deployed 2026-07-30T05:55:39 local, before #1529
- cluster has no `argocd` namespace, no Argo/Flux controller pods, no Argo CRDs, and no Applications
- `infra/gitops/values.yaml` is therefore declaration-only on this cell; nothing reconciles it

This is the existing G028/GitOps blocker class, not authorization for scratchpad `helm upgrade`. #1526 cold FULL and #1523 restack push remain blocked until an independently reviewed/admitted reconciler path applies the committed declaration and live read-only observation proves request=`22Gi`.

Promoted `origin/dev` push CI run 30767156146 is queued at observation time.

## Apply path

Declared GitOps only:

- `infra/gitops/values.yaml` release `oya-arm64`
- `valueFiles: [infra/arc/runner-scale-set-arm64-values.yaml]`
- namespace `arc-runners`

Never `helm upgrade` from a scratchpad copy.

## Non-actions remaining

- No live cluster mutation outside GitOps
- No #1526 cold FULL until live 22Gi observed or durable timeout + evidence that GitOps cannot apply
- No #1523 push until #1526 cold FULL path is unblocked
- #1524 remains draft DO-NOT-MERGE

## Post-merge tip CI (2026-08-02)

- Promoted tip run `30767156146` FAILED affected-set FULL on `//oya:corpus-yaml-facts` (`non-zero exit code <no exit code>`).
- Does **not** prove live 22Gi; ARS/ERS remain 20Gi until admitted reconciler converges.
- Related open repair: #1526 corpus YAML shard faces. Rerun blocked until live request 22Gi + owner GitOps class.

