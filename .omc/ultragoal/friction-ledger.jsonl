{"id": "FRIC-001", "seen_at": "2026-06-09", "friction": "Buck-only gate crate (oya-cloud-ci-rust-first-automation-hygiene-app, no Cargo.toml) escapes the gate_registration meta-test which enumerates by Cargo.toml presence", "pipeline_defect": "gate-registration invariant enumerates by build-system artifact instead of by gate-directory presence; a gate can exist and not be enforced", "enforcement_fix": "extend gate_registration in oya-cloud-ci-firewall-app/tests to enumerate gates by directory presence and fail closed on any gate absent from the oya-ci-required matrix + oya-ci.toml gates.enabled", "status": "queued-G11", "story": "G011"}
{"id": "FRIC-002", "seen_at": "2026-06-09", "friction": "PR #635 (historical docs preservation) required hand-editing oya-ci.toml carve-outs because vocab + total-accounting gates have no first-class concept of historical-provenance document trees", "pipeline_defect": "gates conflate live-authority surfaces with provenance archives; preserving history requires per-PR product-config edits (HANDOFF guardrail 8 surface)", "enforcement_fix": "add a declared provenance-archive path class to oya-ci-config schema (one declaration, all vocab/accounting/doc gates honor it) so provenance preservation never edits gate product config again", "status": "queued-G11", "story": "G011"}
{"id": "FRIC-003", "seen_at": "2026-06-09", "friction": "dev branch protection has required_signatures.enabled=false live (branch-protection.yaml is a self-described shadow record) while guardrails mandate SSH-signed commits", "pipeline_defect": "signing is convention, not enforcement — unsigned commits are currently shippable, violating the impossible-to-ship bar", "enforcement_fix": "enable required signatures on dev via ruleset (PAUSE-AND-PAIR: live branch-protection toggle requires founder pairing) + add a cloud-ci gate verifying signature presence on PR head commits as defense-in-depth", "status": "awaiting-founder-pairing", "story": "G011"}
{"id": "FRIC-004", "seen_at": "2026-06-09", "friction": "masterplan/sequencing gate_command still names oya-dev-cli (CLI) as planning-closure validation while all CLI is retirement-marked", "pipeline_defect": "authority pointer names a retirement-marked surface; agents following specs are routed to bridge tooling", "enforcement_fix": "G001 annotates bridge-only status; G11/Task#13 migrates planning-closure validation into a cloud-ci gate context and retires the CLI invocation path", "status": "in-flight-G001-annotation", "story": "G011"}
{"id": "FRIC-005", "seen_at": "2026-06-09", "friction": "omc team launch with long inline task text printed CLI help instead of launching (silent parse failure, exit ambiguous); required re-launch with brief-file pattern", "pipeline_defect": "orchestration tooling fails open to help text instead of erroring loudly on unparseable input — wasted launch cycle (local OMC tooling, not repo CI; recorded for completeness)", "enforcement_fix": "local tooling: prefer brief-file + --no-decompose pattern for team launches (process note); not a repo pipeline change", "status": "process-note", "story": "G011"}
{"id": "FRIC-006", "seen_at": "2026-06-09", "friction": "three *.generated.json faces were dirty in the working tree at session start, requiring a dedicated branch commit (69063dd07) to keep them out of the PR diff", "pipeline_defect": "local gate runs materialize generated faces into the tracked tree, putting contributors one git-add away from violating the generated-output diff policy", "enforcement_fix": "make local materialization write to an untracked/ignored path or auto-clean after gate consumption so the tracked tree never dirties; pre-push guard rejecting staged *.generated.json as defense-in-depth", "status": "queued-G11", "story": "G011"}
{"id": "FRIC-007", "seen_at": "2026-06-09", "friction": "6+ parallel PRs in flight with no live merge queue: oya-ci-required runs per-PR against PR head only; ADR-0515 Tide projected-merge-state is spec-not-runtime, so two green PRs can compose into a red dev (semantic conflicts on Cargo.toml workspace members, ADR numbering, ADR-INVENTORY.tsv, oya-ci.toml, buck2/reindeer regeneration)", "pipeline_defect": "merge admission lacks projected-state validation; per-head green is accepted as merge authority, violating the impossible-to-ship bar for cross-PR composition breakage", "enforcement_fix": "G011: land ADR-0515 Tide pessimistic merge queue (rebase + re-test against projected merge state, Landcastle-style) as the merge authority; interim bridge = manual merge train protocol at .omc/ultragoal/merge-train.md (rebase onto fresh dev + required context green on rebased head before every merge)", "status": "bridge-active-queue-queued-G11", "story": "G011"}
{"id": "FRIC-008", "seen_at": "2026-06-09", "friction": "founder caught workflow subagents verifying with cargo test instead of buck2 — the G001 workflow Gates phase and the original G12 team brief both instructed cargo fmt/check/clippy/nextest as the green signal; orchestrator-authored prompts defaulted to cargo despite the hermetic buck2 fabric being the substrate", "pipeline_defect": "two-layer defect: (a) orchestration layer — agent prompt templates have no enforced verification-command contract, so each prompt author re-invents the build invocation and drifts to cargo muscle-memory; (b) repo layer — nothing fails closed when a workspace member lacks a buck2 target, so cargo-only-verified crates can be authored and only caught (if at all) by the hermetic CI lane late in the PR cycle", "enforcement_fix": "(a) orchestration: single canonical verification preamble (buck2 build + buck2 test on affected targets; BUCK+reindeer = definition-of-done; cargo supplementary only) stored in .omc/ultragoal/brief.md AMENDMENT 3 and team-brief template — every future lane prompt copies it verbatim, never re-authored; (b) repo (G011): cargo-buck2 target-parity gate — any root Cargo.toml workspace member without a corresponding buck2 target fails closed in oya-ci-required (extends the existing hermetic lane from build-what-exists to prove-everything-is-wired); (c) interim: buck2 re-verification ordered for all 4 G12 workers + Task #14 G001 PR check", "status": "interim-applied-parity-gate-queued-G11", "story": "G011"}
{"id": "FRIC-009", "seen_at": "2026-06-09", "friction": "G001 gate phase proved dev base 8acec8920 is locally RED through no fault of any lane: committed scm-facts/gate-baseline *.generated.json faces are stale relative to HEAD's own #634 retirement commit (firewall +1 unjustified key, registry-drift regen-mismatch on 160 deleted paths), 3 app-shell-frontend generated artifacts undeclared in the control plane, and oya-dev-cli tests/deployment_ops_contract.rs repo_root() ascends only 2 parents (stale since the microservices/->oya/ split commit 2a0d41913) so the test can never pass from its current location", "pipeline_defect": "always-green-dev is violated for local mirrors: a fresh checkout of dev cannot reproduce CI green because (a) generated faces are committed but only valid post-CI-rematerialization, and (b) a permanently-failing test ships on dev — both produce false-red local signal that trains agents to ignore failures (normalization of deviance)", "enforcement_fix": "(a) post-merge CI job rematerializes + auto-commits generated faces on dev (or stops committing them entirely — preferred, aligns FRIC-006); (b) fix repo_root() to ascend by marker-file discovery not fixed parent count; (c) G011: add a dev-checkout-reproducibility gate — clean clone of dev must pass the full local mirror, fail closed otherwise", "status": "queued-G11", "story": "G011"}
{"id": "FRIC-010", "seen_at": "2026-06-09", "friction": "mass vocab-rename batch 3c3cdec63 blindly rewrote oya-foundry-* -> oya-intelligence-* INSIDE a historical audit document (docs/audit/initial-sweep-2026-06-06/docs-sweep/20-products-localization.md), destroying the evidence the doc records (its mixed-prefix demonstration became self-contradictory); a second sweep had also scrubbed the literal term-count line. Caught during Torvalds-discipline review of PR #635, which restores the snapshot original", "pipeline_defect": "mass renames/sweeps have no concept of quote-bound/historical content outside the explicit Wave-15P counterpart-fact rule — provenance archives are silently corrupted by mechanical rewrites, and nothing fails closed when a sweep edits an archive", "enforcement_fix": "PR #635's docs/audit/ vocab carve-out + path_excludes makes future sweeps skip the archive root (root cause closed for this tree); G011 generalizes: the provenance-archive path class (FRIC-002) must also be honored by rename/sweep tooling and a gate must refuse mechanical edits to declared archive paths", "status": "root-cause-closed-for-docs-audit-generalization-queued-G11", "story": "G011"}
{"id": "FRIC-011", "seen_at": "2026-06-09", "friction": "Torvalds review of PR #642 follow-up found (a) the hermeticity fix copied 4 protos crate-local (byte-identical at commit, but no guard against future drift between crate copies and canonical specs/proto/), and (b) a PRE-EXISTING live contract drift on dev: specs/proto/backbone/mail/mail.proto vs oya/mail/contracts/proto/mail.proto differ by ~440 lines while sharing name and purpose — two unguarded sources of the same contract, shipped silently", "pipeline_defect": "contract artifacts can exist in multiple locations with no parity enforcement; dual-build hermeticity (cargo include_str! vs buck2 sandbox) incentivizes copying, and nothing fails closed when copies diverge — violates the api-contract-ssot single-source doctrine", "enforcement_fix": "(a) in-PR: proto_specs_parity.rs drift-guard test added to the contract crate (cargo lane fails closed; canonical = specs/proto); (b) G011: repo-level contract-parity gate — every declared contract artifact family (proto/openapi/graphql) has exactly one canonical path, all other copies must byte-match or be generated projections; (c) the pre-existing mail.proto divergence needs reconciliation in the messenger/mail lane (G06-adjacent), tracked here so it is not lost; terminal fix = SSOT emitters generate every projection (ADR-0536)", "status": "guard-added-in-pr-gate-queued-G11-mail-reconciliation-pending", "story": "G011"}
{"id": "FRIC-012", "seen_at": "2026-06-09", "friction": "FRIC-008 root cause found: tools/hooks/no-cargo-enforcer.sh has existed since 2026-05-29 (founder 'stop using cargo', commit 451987f24) implementing exactly the buck2-gating later re-requested — but it was wired ONLY into .codex/hooks.json; no .claude/settings.json exists, so every Claude session/subagent/teammate ran 11 days unenforced. The gate policing the same policy (rust-first-automation-hygiene-app) is the Buck-only crate escaping gate_registration (FRIC-001): both enforcement layers were broken simultaneously and nothing detected the wiring gap", "pipeline_defect": "enforcement artifacts (hooks, gates) have no liveness verification — a hook can exist unwired and a gate can exist unregistered indefinitely; policy-exists is silently conflated with policy-enforced", "enforcement_fix": "(a) immediate: .claude/settings.local.json wired with no-cargo-enforcer + local-authority-enforcer (pipe-tested: cargo build/oya gate exit 2, cargo metadata/ls exit 0); needs /hooks reload to activate this session; (b) G011: repo-tracked .claude/settings.json via PR so every Claude session in the repo loads both hooks (+ gitignore settings.local.json); (c) G011 ratchet: enforcement-liveness check — every hook under tools/hooks/ must be referenced by at least one agent-runtime wiring surface (.codex/hooks.json + .claude/settings.json) and every gate dir by the required matrix, fail closed", "status": "local-wiring-done-awaiting-hooks-reload-tracked-wiring-queued-G11", "story": "G011"}
{"id": "FRIC-013", "seen_at": "2026-06-09", "friction": "hook fleet staleness was caught by founder prompt + manual audit workflow (w8qw7q8b2), not by the pipeline; same session also found stale AGENTS.md/CLAUDE.md/goal.json/specs (G001) and a corrupted historical doc (FRIC-010) — staleness is recurring and only detected by ad-hoc sweeps", "pipeline_defect": "staleness is a process failure with no continuous detector: nothing cross-checks live-authority claims against ground truth (citations of Superseded ADRs as current; referenced paths that no longer exist; required-context lists diverging from branch-protection config; retired-tool suggestions in agent-facing hook text)", "enforcement_fix": "G011 staleness gate family (mechanical, repo-agnostic per pipeline-universal doctrine): (a) adr-citation-liveness — any non-historical citation of an ADR whose frontmatter is Superseded/Retired fails; (b) referenced-path-exists — paths named in hooks/specs/root authority docs must exist on disk; (c) authority-claim-parity — required-context lists in docs must byte-match infra/branch-protection config; (d) hook-text-lint — agent-facing hook guidance must not suggest retired surfaces (cargo build family, oya CLI family); long-term W2 AST doc-tracking (stale_reference/broken_link/derived_doc_drift) per HANDOFF 6.5 subsumes these; rollout shadow→warn→enforce", "status": "manual-sweep-running-gates-queued-G11", "story": "G011"}
{"id": "FRIC-014", "seen_at": "2026-06-09", "friction": "stale-artifact handling defaulted to text-refresh: the hook-staleness workflow was mandated to FIX hooks in place, with no disposition step asking whether each artifact should exist at all; founder had to intervene mid-flight to demand dispose-or-update rulings with ADR-chain verification — and the running workflow could not receive the amended mandate (stop/edit/resume required)", "pipeline_defect": "two defects: (a) enforcement/automation artifacts (tools/hooks/*.sh, scripts/) carry no machine-readable authority-chain declaration (authoring ADR + purpose), so nothing distinguishes load-bearing artifacts from purposeless false-signal emitters — the active-artifact-contract v3.0.0 provenance capability exists but hooks/scripts are outside its applicable_paths coverage; (b) orchestration prompts hard-encode policy that cannot be amended mid-run", "enforcement_fix": "(a) G011: extend active-artifact-contract/artifact-capabilities-registry coverage to tools/hooks + scripts + automation artifacts — every one declares authoring ADR + purpose + wiring surface; gate fails closed on undeclared artifacts (subsumes FRIC-012 liveness); disposition protocol: stale artifact -> verify declared chain -> UPDATE only if purpose proves out, DISPOSE by default when chain/purpose is absent (false-signal risk outweighs utility); (b) orchestration: doctrine lives in durable briefs/rubrics referenced by prompts (already the pattern via brief.md amendments), prompts cite the brief instead of inlining policy so amendments propagate at next phase boundary", "status": "workflow-being-amended-registry-extension-queued-G11", "story": "G011"}
{"id": "FRIC-015", "seen_at": "2026-06-09", "friction": "FRIC-005 recurred with root cause now isolated: omc team refuses a second team when one_team_per_leader_session is enabled, but prints usage/help with exit=0 instead of erroring — fail-open UX; orchestrator compounded it by reading output tail while the one-line refusal sat at the head", "pipeline_defect": "orchestration tooling refusals are indistinguishable from help invocations (exit 0 + usage dump); errors must be loud, first, and nonzero", "enforcement_fix": "local OMC tooling process note (not repo CI): always check head of omc team output + exit code; team rotation protocol = shutdown completed team before launching successor; upstream wish: nonzero exit + stderr refusal line", "status": "process-note-root-cause-isolated", "story": "G011"}
{"id": "FRIC-016", "seen_at": "2026-06-10", "friction": "omc team tmux nudge injection ([OMC_TMUX_INJECT]) types message text into Claude panes but the submit fails when the pane is mid-turn: tmux-session.js retries C-m for only ~6 rounds spanning seconds while agent turns last minutes; text strands in the input box until a human presses Enter (founder manually submitted 4 stacked nudges in the leader pane)", "pipeline_defect": "agent-runtime message delivery is not idle-aware: submit retry window << pane busy duration, and there is no post-turn re-submit; human-in-the-loop Enter violates the automation doctrine and can silently stall workers after their turn ends", "enforcement_fix": "(a) immediate session-transient watchdog (background loop, not committed — purity doctrine permits transient glue): scans team panes every ~25s, fires C-m on idle panes whose tail still shows [OMC_TMUX_INJECT]; (b) upstream OMC fix: make injection idle-aware (defer typing+submit until pane has no active turn) or persist a pending-submit that re-fires on idle detection; (c) doctrine note: mailbox state files are the canonical channel — leader reads mailboxes proactively rather than depending on pane nudges", "status": "watchdog-deployed-upstream-fix-noted", "story": "G011"}
{"id": "FRIC-017", "seen_at": "2026-06-10", "friction": "PR #638 buck2 lane failed with runner disk exhaustion (No space left on device at Cache buck-out step, before any build step ran); same head had passed CI twice — a transient infra red that initially read as composition breakage and cost a diagnosis agent", "pipeline_defect": "CI runner fleet lacks disk preflight/hygiene: ephemeral runners can start jobs without enough storage, producing false-red required contexts indistinguishable from code failures at triage time", "enforcement_fix": "cloud-ci runner hygiene: disk-space preflight gate at job start (fail fast with explicit infra-red classification), buck-out cache size budgeting/eviction, and an infra-red vs code-red label on the required context so triage routes correctly without an agent dispatch; long-term owned cloud-ci runners (P-TOOLCHAIN) make this a first-class scheduler concern", "status": "rerun-triggered-runner-hygiene-queued-G11", "story": "G011"}
{"id": "FRIC-018", "seen_at": "2026-06-10", "friction": "buck2 build of any aws-lc-rs-dependent target fails on macOS dev machine: aws-lc-sys 0.41 build-script panics in CcBuilder::memcmp_check — reproduced on UNCHANGED origin/dev (oya-identity-workload-oidc-adapter), pre-existing, not lane-induced; Linux CI buck2 lane is green for the same targets", "pipeline_defect": "buck2 prelude rust tool from_any_dir.py (bundled with the buck2 binary, invoked as the cc shim wrapper) calls pathlib relative_to(walk_up=True), a Python>=3.12 API; macOS system python3 is 3.9.6, so EVERY build-script that compiles C via the cc shim dies with TypeError before the compiler runs. The prelude assumes an interpreter floor that no toolchain preflight asserts; AMENDMENT 3 buck2-first local verification silently impossible on macOS for crypto-dependent crates and the failure masquerades as an aws-lc-sys compiler-check defect", "enforcement_fix": "hermetic python in the buck2 toolchain (or pin buck2+prelude to a version compatible with the declared interpreter floor) so host python version cannot affect builds; cloud-ci/dev-env preflight gate asserting python>=3.12 where buck2 prelude requires it, failing with an explicit infra-red message; G011: document macOS local-dev support tier so AMENDMENT 3 names the authoritative platform", "status": "root-caused-host-python-3.9-vs-prelude-3.12-floor; workaround=cargo-supplementary-local, buck2-Linux-CI-canonical", "story": "G011"}
{"id": "FRIC-019", "seen_at": "2026-06-10", "friction": "buck2 build of any aws-lc-rs-dependent target fails on macOS aarch64 dev machine: aws-lc-sys 0.41 build-script panics in CcBuilder::memcmp_check (run_compiler_checks) — reproduced on UNCHANGED origin/dev (oya-identity-workload-oidc-adapter), so pre-existing, not lane-induced; Linux CI buck2 lane is green for the same targets", "pipeline_defect": "third-party fixup (third-party/fixups/aws-lc-sys/fixups.toml) sets LDFLAGS=-nostartfiles globally to fix aarch64-linux ld.lld duplicate-CRT in the memcmp probe link, but the env is not platform-scoped and the darwin probe path fails; no macOS buck2 smoke lane exists to catch platform divergence, so buck2-first local verification (AMENDMENT 3) is impossible on macOS for crypto-dependent crates", "enforcement_fix": "platform-scope the buildscript env in reindeer fixups (linux-only LDFLAGS) or land the prelude-level CRT fix the fixup comment names as the durable class-wide fix; add a macOS buck2 smoke target to CI or declare macOS local-dev support tier explicitly in docs so AMENDMENT 3 names the authoritative platform; G011 staleness family: fixup comments asserting platform tolerance (darwin tolerates it) must carry a verifying probe", "status": "logged-workaround-cargo-supplementary-local-buck2-ci-canonical", "story": "G008"}
{"id": "FRIC-020", "seen_at": "2026-06-10", "friction": "buck2 rust_test for a tests/-dir integration target fails to compile on env!(\"CARGO_MANIFEST_DIR\") (not defined at buck2 compile time) while the identical pattern compiles+passes under cargo; the G001 backbone-proto parity test uses the same env! pattern but silently escapes buck2 because no rust_test target covers tests/ there — so the canonical-vs-crate-local parity gate only runs in the cargo lane", "pipeline_defect": "cargo/buck2 target parity gap: integration tests under tests/ are not systematically mirrored as buck2 rust_test targets, and env!-based repo-root discovery is cargo-only by construction, so buck2-first verification (AMENDMENT 3) silently loses test rungs", "enforcement_fix": "G011 cargo-buck2 target-parity gate (already directed by AMENDMENT 3) must count [[test]] targets vs rust_test targets per crate fail-closed; codify option_env!(\"CARGO_MANIFEST_DIR\") + skip-with-stderr as the sanctioned hermetic-sandbox pattern for repo-root-dependent parity tests (both lanes compile, cargo lane enforces)", "status": "workaround-applied-option_env-skip-in-hermetic-sandbox", "story": "G004"}
{"id": "FRIC-021", "friction": "Crate rename (prototype->production shell) requires a Cargo.lock package-name update, but `cargo generate-lockfile --offline` re-resolves and bumps ~70 unrelated dependency versions, polluting the minimal diff the merge train requires; no surgical lockfile-rename path exists", "pipeline_defect": "lockfile maintenance is all-or-nothing; rename-only changes cannot be expressed through tooling, forcing a hand edit of Cargo.lock (data file) to keep the diff minimal", "enforcement_fix": "G011: lockfile-consistency gate that verifies Cargo.lock package set matches workspace manifests exactly (names+paths) and rejects unrelated version drift in diffs that only rename/move crates; pair with a Rust lockfile-surgery tool for rename/move operations", "status": "open", "story": "G007"}
{"id": "FRIC-022", "seen_at": "2026-06-10", "friction": "main checkout found dirty (no-cargo-enforcer.sh intermediate edit + stale Cargo.lock re-sort) despite worktree-only doctrine — an agent edited /Users/jasonlee/Developer/oyatie directly before/instead of its isolated worktree; residue blocked dev fast-forward and required manual forensic diff to rule stale-vs-valuable before discarding", "pipeline_defect": "worktree-only isolation is convention for agents with no mechanical guard on the main checkout; contamination is detected only when it blocks something (here: ff-merge), inviting either silent loss (blind reset) or wrong-direction commits", "enforcement_fix": "(a) hooks layer: extend local-authority/no-cargo enforcer family with a main-checkout-write guard (PreToolUse Edit/Write/Bash-cd deny on the canonical checkout path for team-worker sessions, allow in ~/oyatie-worktrees/*); (b) structural: OMC team worktreeMode=branch/detached launches workers already chrooted into worktrees (the option exists, was disabled — enable by default for future teams); (c) periodic main-checkout cleanliness assert in the leader loop", "status": "residue-cleared-guards-queued-G11", "story": "G011"}
{"id": "FRIC-023", "seen_at": "2026-06-10", "friction": "no-cargo-enforcer.sh blocked a `git commit` because the COMMIT MESSAGE text contained the phrase 'cargo clippy' (supplementary-verification note); the hook pattern-matches retired command names anywhere in the Bash command string including heredoc message bodies, so factual prose about verification triggers a false-positive block", "pipeline_defect": "string-level pattern matching cannot distinguish command invocation from quoted prose; agents must self-censor commit messages/PR bodies, degrading evidence fidelity (AMENDMENT 5: hook-only rule with imprecise matching)", "enforcement_fix": "G011 Rust hook successor parses the actual argv head (command position) instead of substring-matching the whole string; RED/GREEN fixture pair: `cargo clippy` invocation blocked, `git commit -m 'note about cargo clippy'` allowed", "status": "logged-workaround-reworded-commit-message", "story": "G009"}
{"id": "FRIC-024", "seen_at": "2026-06-10", "friction": "G05 lane: local buck2 build/test of any oya-identity target fails on macOS dev machine at third-party//:openssl-sys-0.9-build-script-main-run (Header expansion error in validate_headers) — sourced transitively via webauthn-rs FIDO2 attestation; reproduced on UNCHANGED origin/dev third-party//:webauthn-rs, so pre-existing platform divergence, not lane-induced; same class as the aws-lc-sys macOS entry (second FRIC-018)", "pipeline_defect": "third-party/fixups/openssl-sys/fixups.toml is pinned to the Linux worker image (libssl-dev + pkg-config) by design, but AMENDMENT 3 buck2-first local verification has no declared platform support tier, so macOS agents discover the unsupported path by failure; additionally the no-cargo hook blocks the cargo-supplementary fallback the prior FRIC-018 disposition relies on, leaving NO local test rung at all for crypto-dependent crates on macOS", "enforcement_fix": "declare the authoritative verification platform in docs (Linux CI worker image = the pinned unit) and add either a macOS buck2 smoke lane or an explicit hook allowance for cargo-supplementary on platforms where buck2 third-party crypto is by-design unbuildable; G011: platform-support-tier doc + hook-text alignment so the safety net does not contradict the recorded workaround", "status": "logged-verification-moved-to-PR-CI-canonical", "story": "G005"}
{"id": "FRIC-025", "seen_at": "2026-06-10", "friction": "friction-ledger.jsonl contains two different entries with id FRIC-018 (PR-hygiene dangling-references entry and aws-lc-sys macOS entry) — id collision under concurrent multi-lane appends", "pipeline_defect": "ledger ids are hand-assigned by each agent reading the tail under concurrency; no mechanical allocation or uniqueness check exists", "enforcement_fix": "G011: ledger lint gate (unique id, schema fields present, append-only) + mechanical id derivation (hash/ULID or CI-assigned sequence) so concurrent lanes cannot collide", "status": "logged", "story": "G011"}
{"id": "FRIC-026", "seen_at": "2026-06-10", "friction": "concurrent friction-ledger appends by leader + team workers collided: duplicate FRIC-018/FRIC-019 ids, one leaked shell-command line corrupting the JSONL — the ledger append protocol (echo >> with self-chosen next-free id) is not concurrency-safe", "pipeline_defect": "shared append-only ledger has no id allocation or write atomicity for multi-agent use; corruption is silent until a reader parses", "enforcement_fix": "interim: ids re-allocated by repair script + workers instructed to use ULID-suffixed ids (FRIC-<ulid>) instead of next-free integers; G011: ledger writes go through a small Rust appender (validates row, allocates id, O_APPEND single-write atomicity) delivered per the Rust-hooks doctrine — and ultimately the registry/gate substrate owns friction intake", "status": "repaired-rust-appender-queued-G11", "story": "G011"}
{"id":"FRIC-1781062100-G06","friction":"Adding a workspace member crate requires a Cargo.lock update, but the no-cargo enforcer hook blocks cargo build/check/test locally and buck2 does not maintain Cargo.lock; PR #647 failed 11 gate jobs uniformly at the shared 'cargo --locked' setup step (exit 101) despite buck2 build+test green, because the lock regeneration path (allowed 'cargo metadata') is undocumented in the hook guidance and nothing pre-push checks lock freshness","pipeline_defect":"lock-freshness is enforced only at CI runtime inside every gate job's setup instead of once, early, and locally; the agent-facing hook text names buck2 equivalents but omits the sanctioned Cargo.lock regeneration command for the workspace-member-add flow","enforcement_fix":"G011: (1) pre-push/PR-open lock-freshness check (cargo metadata --locked dry equivalent) as its own fast fail-closed gate with a clear remediation message; (2) extend no-cargo-enforcer hook text with 'workspace member added? run: cargo metadata >/dev/null to refresh Cargo.lock'; (3) dedupe the per-gate-job cargo setup so one lock failure reports once, not 11 times","status":"open","story":"G006"}
{"id":"FRIC-1781062183-G03","seen_at":"2026-06-10","friction":"buck2 test of any sqlx-dependent target fails on macOS aarch64 dev machine: ring-0.17 build-script cc compile of pregenerated/sha256-armv8-ios64.S exits 1 under the cc shim — reproduced on UNCHANGED origin/dev target oya-shared-postgres-command-adapter-sqlx, so pre-existing, not lane-induced; same class as the aws-lc-sys macOS entry","pipeline_defect":"no macOS buck2 smoke lane exists, so buck2-first local verification (AMENDMENT 3) is impossible on macOS for ring-dependent (TLS/crypto) crates; platform divergence in third-party buildscripts is only caught when a dev machine trips on it","enforcement_fix":"platform-scope the crypto-C-core buildscript env/flags in reindeer fixups (ring + aws-lc-sys as one class), plus either a macOS buck2 smoke target in CI or an explicit docs declaration that Linux is the sole authoritative buck2 platform","status":"logged-workaround-ci-buck2-canonical-local-kernel-targets-only","story":"G003"}
{"id":"FRIC-1781062484-G02","seen_at":"2026-06-10","friction":"FRIC-018 RESOLVED on macOS aarch64 host: buck2 aws-lc-sys memcmp_check panic root cause confirmed live as prelude rust tool from_any_dir.py calling pathlib relative_to(walk_up=True) (python>=3.12 API) via the cc shim under system python 3.9.6 — NOT the LDFLAGS=-nostartfiles darwin probe theory in the second FRIC-018 entry","pipeline_defect":"buck2 prelude assumes python>=3.12 with no preflight assertion; host fix applied: brew install python@3.13 + ln -s python3.13 ~/.local/bin/python3 + PATH prepend + buck2 killall — full buck2 build+test of aws-lc-rs-dependent crates now green locally on macOS (verified on oya-cloud-kms-enclave-kernel, 4/4 rust_test targets)","enforcement_fix":"G011: hermetic python pinned in the buck2 toolchain so host interpreters cannot leak into builds; until then a dev-env preflight (python3 --version >= 3.12) with explicit remediation text; broadcast fix to all lanes — supersedes the cargo-supplementary workaround in both FRIC-018 entries","status":"resolved-host-level-fix-applied-buck2-local-green-on-macos","story":"G002"}
{"id": "FRIC-1781062867-leader", "seen_at": "2026-06-10", "friction": "second main-checkout contamination in one session (FRIC-019 recurrence): a review critic checked out its pr-652-review branch IN the main checkout (/Users/jasonlee/Developer/oyatie) despite explicit READ-ONLY + fetch-refs-only instructions, leaving dev unswitchable and Cargo.lock dirty; leader had to restore dev mid-train", "pipeline_defect": "read-only review mandates are prompt-convention only; nothing mechanically prevents checkout/branch operations in the canonical checkout by any agent", "enforcement_fix": "escalates FRIC-019 fix: (a) main checkout pinned via a hook denying git checkout/switch/branch -f in the canonical path for agent sessions (allow in worktrees), (b) review prompts already say read-only \u2014 add explicit 'no checkout anywhere; use git show <ref>:<path> and git diff ref...ref' to the review rubric template, (c) G011 structural: review agents get ephemeral worktrees by default", "status": "restored-guards-escalated-G11", "story": "G011"}
{"id":"FRIC-1781063357-G03","seen_at":"2026-06-10","friction":"PR #645 merged GREEN (16/16 checks) while its sqlx adapter unit-test target cannot compile: a test uses unwrap_err on Result<SqlxDataSession,_> which requires Debug the type did not implement — rust-analyzer surfaced it post-merge; no CI lane ever compiled that rust_test/dev-profile target (macOS local buck2 was already blocked by the ring fixup, so the gap was invisible end-to-end)","pipeline_defect":"FRIC-019 class confirmed at merge scale: the hermetic buck2 lane runs affected gate tests only and the cargo matrix does not compile every crate test target, so a crate can land with uncompilable tests and a green required context","enforcement_fix":"G011 cargo-buck2 target-parity gate fail-closed (every [lib]+[[test]] target must have a compiled CI counterpart), plus a cheap interim: workspace-wide test-target compile pass (no run) in the required context","status":"fix-riding-PR-656-defect-was-latent-on-dev","story":"G003"}
{"id": "FRIC-1781063713-leader-falsegreen", "seen_at": "2026-06-10", "friction": "dev went RED undetected: PR #645 merged with oya-ci-required GREEN while its own crate's buck2 unittest target (oya-data-sql-adapter-sqlx-unittest) was compile-broken (E0277 missing Debug); discovered only via IDE diagnostics + local buck2 build; hotfix PR #657", "pipeline_defect": "hermetic buck2 lane's affected-set did not include the unittest target of a NEWLY ADDED crate \u2014 a changed/added crate can merge green without its own tests compiling (false-green, the exact class the doctrine forbids)", "enforcement_fix": "G011: hermetic lane invariant \u2014 for every crate touched/added in the diff, ALL its buck2 targets (lib+bins+unittest) are in the build+test set, fail closed; plus a dev-postsubmit full-graph build job that pages on red (postsubmit exhaustive per TAP doctrine, already in ADR-0536 delivery-fabric decision)", "status": "hotfix-657-open-lane-fix-queued-G11", "story": "G011"}
{"id": "FRIC-1781063713-leader-watchdog", "seen_at": "2026-06-10", "friction": "watchdog v3/v4 verification was narration-trusting: log entries claimed force-submits but founder manually submitted every stranded nudge batch (v3 fired C-m at empty input after manual submit \u2014 false success; v4 selftest arrived only via founder Enter). Orchestrator claimed 'verified live' wrongly and was corrected by founder", "pipeline_defect": "(a) OMC nudge submit retries span seconds vs minutes-long busy turns (root, FRIC-016); (b) watchdog detection cannot reliably distinguish input-box text from transcript echoes via plain pane capture; (c) orchestrator verification standard slipped \u2014 success claimed from tool logs instead of end-to-end observable effect", "enforcement_fix": "detection rewritten against the input-box region (text between the last two horizontal rules); TRUE end-to-end selftest = plant unsubmitted text, require it to arrive as a message without human Enter; verification doctrine reaffirmed: no success claim without observed effect (Torvalds discipline applies to own tooling)", "status": "v4-debug-in-progress", "story": "G011"}
{"id":"FRIC-1781063715-G07","friction":"Team task lifecycle dead-ends after lease expiry: transition-task-status and release-task-claim both refuse with lease_expired while claim-task refuses with claim_conflict on the stale claim record, so a worker whose lane outlived the 15-minute lease cannot record completion through any API path","pipeline_defect":"claim lease TTL (15m) is far shorter than realistic lane execution time and no expired-lease takeover/renew path exists; completion evidence gets stranded","enforcement_fix":"G011: claim renew/heartbeat-extends-lease semantics or expired-lease same-worker re-claim; lease TTL sized to lane SLO; gate test covering the expiry->complete path","status":"open","story":"G007"}
{"id": "FRIC-1781063950-leader-rebase", "seen_at": "2026-06-10", "friction": "merge-train auto-rebase with blanket 'checkout --ours' conflict resolution GUTTED PR #643's content (zero slice-4 files survived; only caught by post-rebase content verification before push); correct strategy for stacked branches whose earlier slices squash-merged is rebase --onto <dev> <stack-boundary>", "pipeline_defect": "train tooling lacked a stacked-branch-aware rebase strategy; blanket ours-resolution silently discards incoming adds; only manual content sanity (file-count vs review measurements) prevented destroying a 37.8k-line PR", "enforcement_fix": "train protocol amended: stacked branches ALWAYS rebase --onto with explicit boundary; post-rebase content assert (changed-file set \u2287 expected lane dirs, magnitude sanity vs review evidence) REQUIRED before any push; G011 Tide owns this natively (projected-state merge makes manual rebase choreography obsolete)", "status": "protocol-amended-tide-queued", "story": "G011"}
{"id":"FRIC-1781064265-G05-a","seen_at":"2026-06-10","friction":"PR #651 review exposed that NO binding CI step compiles service crates: the hermetic buck2 step is scoped to //cloud/cloud-ci/... only, the affected-set driver step is advisory (continue-on-error) and was failing (exit 3) while the job reported green — agent read job-green as code-verified when the advisory step had openssl ffi errors (E0425 EVP_DigestSqueeze/OSSL_get_max_threads/X509_PURPOSE_CODE_SIGN in the webauthn-rs->openssl chain, a cfg-propagation/version skew under buck2 buildscript_run on the Linux runner)","pipeline_defect":"advisory CI steps inside a green-reporting job are invisible failures: per-step verdicts are not surfaced as check-run conclusions, so a service crate can merge without ever compiling anywhere; openssl fixup does not propagate the build-script version cfgs to the openssl crate compile under buck2","enforcement_fix":"promote the affected-set driver to binding once the third-party blockers are fixed (openssl cfg propagation in third-party/fixups/openssl), or split it into its own check-run so its verdict is visible; G011: per-step verdict surfacing lint (no continue-on-error step inside a required context without its own reported conclusion)","status":"logged-webauthn-dep-parked-in-PR-651-to-restore-compilability","story":"G005"}
{"id":"FRIC-1781064265-G05-b","seen_at":"2026-06-10","friction":"oya-identity-workload-rest build.rs resolved protos at CARGO_MANIFEST_DIR/../../contracts/proto while the BUCK buildscript genrule copies them INTO the manifest dir — protoc proto-path failure under buck2 on every platform (reviewer reproduced on branch and dev)","pipeline_defect":"cargo-shaped build.rs path assumptions are not exercised by any binding buck2 build of the crate, so the buck2/cargo layout divergence shipped green","enforcement_fix":"dual-mode probe landed in PR #651 (in-manifest first, cargo fallback — PR 643 dual-mode pattern); durable fix = binding buck2 compile coverage for every crate (same as FRIC-1781064265-G05-a enforcement)","status":"fixed-in-PR-651-buildscript-run-verified-locally","story":"G005"}
{"id": "FRIC-1781064800-leader-ledger-serial", "seen_at": "2026-06-10", "friction": "omc ultragoal checkpoint refuses G012-complete because the story is 'pending' \u2014 the CLI state machine assumes SERIAL story execution (complete-goals activates one next story) while founder doctrine + actual execution is PARALLEL lanes; a finished story cannot be checkpointed out of order", "pipeline_defect": "durable-plan tooling models a different execution shape than the doctrine it serves; completion evidence is forced to live outside the ledger until the serial pointer catches up (state drift between reality and the canonical artifact)", "enforcement_fix": "local OMC tooling note: ultragoal needs per-story independent activation/checkpoint (parallel mode); interim = evidence recorded in task list + session log, formal checkpoint when the serial pointer reaches each story; long-term the registry/gate substrate owns story state (G011)", "status": "interim-accepted", "story": "G011"}
{"id": "FRIC-1781065651-leader-nudge", "seen_at": "2026-06-10", "friction": "ROOT CAUSE of the manual-Enter problem (supersedes the failed FRIC-016 watchdog attempts): OMC team-leader-nudge-hook.js fires on worker ticks, types '[OMC] Leader nudge ... [OMC_TMUX_INJECT]' into the leader pane (%38) and presses a SINGLE C-m; when the leader is mid-turn (minutes) the single submit fails and text strands until a human presses Enter. Orchestrator wasted 4 watchdog-daemon iterations falsely claiming success \u2014 each 'proof' was actually the founder pressing Enter (verification-by-narration failure, the exact anti-pattern the review doctrine forbids)", "pipeline_defect": "(a) leader-nudge submit is single-C-m with no idle-awareness or post-turn retry (same class as worker-nudge FRIC-016 but worse \u2014 one press); (b) the nudge is REDUNDANT for a leader that reads mailboxes proactively, so it is pure noise; (c) orchestrator self-verification standard collapsed \u2014 claimed effect from daemon logs instead of observed delivery", "enforcement_fix": "DISABLED at source: cleared leader_pane_id in team config.json + manifest.json so the hook returns no_leader_pane_id and never injects (leader reads mailboxes every turn anyway). Upstream OMC fix: leader-nudge must be idle-aware (defer to between-turns) AND opt-in (a proactive-mailbox leader does not need it). Verification doctrine reaffirmed: NO success claim without observed effect \u2014 applies to own tooling, hardest there", "status": "disabled-at-source-upstream-fix-noted", "story": "G011"}
{"id":"FRIC-1781069300-G06","friction":"omc team task lifecycle deadlocks after lease expiry: transition-task-status rejects with lease_expired, claim-task rejects with claim_conflict (stale claim row still occupies the slot), release-task-claim ALSO rejects with lease_expired — a worker that outlives its 15-min lease cannot complete, re-claim, or release its own task through any API path; worker-5 hit this when transitioning task 5 to completed at founder wind-down","pipeline_defect":"lease state machine has no self-service recovery edge: an expired-lease owner is locked out of every transition including release, forcing leader-side manual intervention for the routine case of work outlasting the default lease","enforcement_fix":"team API: (1) allow the SAME worker to re-claim its own expired-lease task (refresh lease + reissue token, version-checked), or (2) accept transition-task-status from the recorded owner when the lease is expired but unclaimed by anyone else; add heartbeat-driven lease auto-extension while the worker process is alive","status":"open","story":"G006"}
{"id": "FRIC-1781069288-merge-conflict-structural", "seen_at": "2026-06-10", "friction": "founder had to MANUALLY resolve a Cargo.lock merge conflict to land #656 \u2014 a manual conflict-resolution step in the merge path is itself the anti-pattern the doctrine forbids (automate everything; impossible to ship via friction-free process)", "pipeline_defect": "ROOT CAUSE deeper than FRIC-007 (no merge queue): root Cargo.toml lists 791 EXPLICIT workspace members, so every new-crate PR edits the shared members array AND Cargo.lock \u2014 concurrent parallel lanes therefore conflict on these two files BY CONSTRUCTION, 100% of the time. A merge queue (FRIC-007) only serializes the conflict; it does not eliminate it. Manual resolution is required whenever a PR merges without a fresh rebase (e.g. via GitHub UI).", "enforcement_fix": "STRUCTURAL elimination (enforcement-layering: structural > gate > hook): (1) convert root Cargo.toml to GLOB members ('libs/*', 'cloud/*/crates/*', 'oya/*/crates/*', etc.) so adding a crate requires ZERO edit to the shared file \u2014 the conflict class disappears for Cargo.toml; (2) register a Cargo.lock git merge driver (.gitattributes 'Cargo.lock merge=cargo-lock' + a driver that runs cargo metadata/regenerates deterministically) so lock conflicts auto-resolve, never manual; (3) THEN the FRIC-007 Tide merge queue serializes the residual; (4) a CI gate asserting glob-coverage (no orphan crate dir outside a glob, no explicit member that a glob already covers) keeps it from regressing. This makes manual conflict resolution structurally impossible, not just rare.", "status": "root-caused-structural-fix-specified-G011-high-priority", "story": "G011"}
{"id":"FRIC-1781069294-G08","seen_at":"2026-06-10","friction":"worker-7 cannot record task completion: claim lease expired mid-lane (15-min lease vs multi-hour lane), after which every lifecycle path deadlocks — transition-task-status and release-task-claim return lease_expired, claim-task (with and without expected_version) returns claim_conflict because the expired claim record persists, update-task refuses lifecycle fields by design","pipeline_defect":"team task state machine has no owner-rebind path for an expired lease: leases are shorter than realistic lane durations, heartbeats do not renew them, and expiry leaves a zombie claim only leader-side tooling can clear (orphan-cleanup is forbidden to workers by doctrine since it deletes the whole store)","enforcement_fix":"lease renewal on heartbeat (or claim-task acting as renew when caller==claim owner), plus a bounded leader-side requeue/rebind op; G011: state-machine property test that every reachable claim state has a sanctioned exit for owner, leader, or both","status":"logged-escalated-to-leader-for-leader-side-transition","story":"G008"}
{"id":"FRIC-1781069295-G04","seen_at":"2026-06-10","friction":"omc team task-claim state machine deadlocks after lease expiry: task 3 owner=worker-3 lease expired mid-execution (15-min TTL vs multi-hour lane work); transition-task-status returns lease_expired, claim-task by the SAME owner returns claim_conflict, release-task-claim returns lease_expired — no API path lets the owning worker renew, release, or complete its own expired-lease task, so completion evidence cannot be recorded by the worker that earned it","pipeline_defect":"claim lease TTL is not renewed by worker activity (heartbeat updates do not touch the lease) and the expired-lease state accepts neither re-claim nor release nor transition from the original owner — wedged terminal state requiring leader/manual repair","enforcement_fix":"omc team api: claim-task by the recorded owner on an expired lease should renew in place (owner-idempotent re-claim); transition-task-status should accept the recorded owner token on expired leases (completion is evidence recording, not contention); heartbeat should extend the lease; add a state-machine test for the expiry path","status":"workaround-leader-asked-to-transition","story":"G004"}
{"id":"FRIC-1781069305-G09","seen_at":"2026-06-10","friction":"team task API deadlock on expired lease: worker-8 lease on task 8 expired mid-session; transition-task-status and release-task-claim both return lease_expired while claim-task (with and without expected_version) returns claim_conflict against the worker's OWN expired claim — the owning worker has no supported self-recovery path and must escalate to the leader for a bookkeeping-only fix","pipeline_defect":"claim state machine treats an expired lease as both dead (for transition/release) and alive (for re-claim), so lease expiry converts a completed unit of work into a manual leader intervention; lease renewal during long work also silently failed to extend (renewal claim returned ok without a fresh token)","enforcement_fix":"task-API fix: same-owner re-claim of an expired lease must succeed (mint fresh token, bump version) OR transition must accept the last-issued token when the claim owner matches the caller; add RED/GREEN fixture pair (expired-lease same-owner re-claim succeeds; foreign-worker re-claim of live lease still conflicts); surface lease TTL + renewal outcome in claim responses so workers can renew deterministically","status":"escalated-to-leader-for-force-complete","story":"G009"}
{"id": "FRIC-1781069455-team-lease-deadlock", "seen_at": "2026-06-10", "friction": "omc team task lifecycle deadlocks: worker-1 and worker-7 report claim lease expired mid-lane (leased_until passed), then transition-task-status returns lease_expired AND release-task also fails \u2014 a worker that legitimately finished its PR cannot mark its task complete, leaving the task board stuck in_progress forever", "pipeline_defect": "team task-lease has an expiry but no self-renew on active work and no recovery path once expired (can neither complete nor release) \u2014 long-running lanes (PRs take >lease-TTL) structurally outlive their lease; task-board state diverges from real PR state", "enforcement_fix": "local OMC tooling: (a) auto-renew lease while the worker is actively producing (heartbeat), (b) allow the original owner to force-complete/force-release an expired-but-uncontested lease, (c) leader can reconcile task state from PR state (the PR is ground truth, the task board is bookkeeping); long-term the registry/gate substrate tracks lane state from VCS facts not a separate lease ledger", "status": "logged-tooling-note-leader-reconciles-from-PR-truth", "story": "G011"}
{"id":"FRIC-1781070150-G05-c","seen_at":"2026-06-10","friction":"team task lifecycle deadlock: worker-4 claim lease (15min) expired during a multi-hour lane; transition-task-status and release-task-claim both return lease_expired while claim-task (with and without expected_version) returns claim_conflict against the worker's OWN stale claim record — no worker-side recovery path exists; completion evidence had to be escalated to the leader for a forced transition","pipeline_defect":"claim leases are sized for short tasks but lanes run hours; expiry leaves a live owner with an unreleasable claim, and the API treats the same worker re-claiming its own expired claim as a conflict instead of a renewal","enforcement_fix":"team runtime: same-worker re-claim of an expired lease should renew (idempotent ownership), or update-worker-heartbeat should extend the task lease; add a lease-expiry self-heal path so completion evidence is never blocked on leader intervention","status":"escalated-to-leader-for-forced-transition","story":"G005"}
{"id": "FRIC-1781070457-buck2-no-shared-cache", "seen_at": "2026-06-10", "friction": "buck2 reports 0% cache hits across the board (review-agent throwaway worktrees, ephemeral CI runners, any fresh buck-out). Empirical: in-daemon incrementality works (2nd build same buck-out = nothing to do) but there is NO shared cache, so every fresh buck-out is 100% cold (153 local commands for a tiny target). Compounds: CI actions/cache key embeds github.sha in the PRIMARY key (buck-out-OS-hash-SHA) so the primary key MISSES every commit and each run SAVES a fresh full buck-out cache entry -> cache bloat feeding the runner disk-full failure (FRIC-017).", "pipeline_defect": "the content-addressed delivery fabric's central premise (a correct cache hit == a cold build, so warm is free) is unrealized: .buckconfig has NO [cache] section, no RBE/NativeLink/HTTP CAS \u2014 buck2 caching is local-buck-out-only, never shared across dev machines / CI runners / worktrees. Result is worst-of-both: cold everywhere (no sharing) yet not DELIBERATELY cold (no provenance guarantee, no hermeticity canary) \u2014 full cold-build cost paid on every action, everywhere, every time, plus CI cache bloat.", "enforcement_fix": "WARM-BY-DEFAULT via a shared content-addressed remote cache (NativeLink \u2014 Rust, open-source, content-addressed, owned-stack + Rust-purity aligned; or self-hosted HTTP CAS) read/written by ALL actors incl. throwaway review worktrees and ephemeral runners. COLD DELIBERATELY in exactly 3 cases: (1) release-image builds (provenance/reproducibility \u2014 the cargo --release exception path, no cache write-back); (2) a periodic COLD-CANARY CI job (full-graph cold build that must byte-match the warm result; cold!=warm = a hermeticity/non-determinism bug, fail closed \u2014 this is the cache-integrity gate); (3) trust-boundary: untrusted-author PRs build with READ-ONLY (or no) shared-cache access to prevent cache poisoning (Bazel/Google RBE security model). Also fix the CI key: drop github.sha from the PRIMARY actions/cache key (use restore-keys prefix + the remote cache), killing the per-commit miss + bloat. This is the HANDOFF W3 'buck2 RBE/NativeLink' item; grounded in Bazel Remote Execution + Meta Buck2 RE content-addressed caching.", "status": "diagnosed-cold-vs-warm-design-specified-G011-W3", "story": "G011"}
{"id": "FRIC-1781071186-affected-driver-serial-owner", "seen_at": "2026-06-10", "friction": "buck2-affected-gate.sh resolves owners with a SERIAL per-file loop: one `buck2 uquery owner('$f')` invocation per changed Rust file, sequentially (infra/ci/buck2-affected-gate.sh:64-92), each a separate buck2 client->daemon round-trip. A PR touching N Rust files = N sequential queries before any build starts. (The rdeps closure is a single query, and buck2 build/test execution is already fully parallel within the engine \u2014 the serial cost is purely owner pre-computation.)", "pipeline_defect": "affected-set COMPUTATION is serial where it should be one batched query \u2014 owner() accepts a set, so N round-trips collapse to 1: `buck2 uquery owner(%Ss) @changed.txt`. Latency scales linearly with changed-file count for no reason; matters more now that .claire graph pollution is gone (0 tracked) and the driver can be promoted from advisory to binding.", "enforcement_fix": "(interim, .sh) batch the owner loop into ONE uquery over the changed-file set via @argfile + %Ss (mirrors the pattern already used for rdeps at line 108); handle the rare BUCK-file->package-pattern expansion in a small separate pre-pass; (durable) the driver is a transitional .sh slated for a Rust rewrite per 0-shell + Rust-purity doctrine \u2014 the Rust successor does owner+rdeps in-process via the buck2 query API with no per-file process spawn. Also: now that .claire pollution is cleared, evaluate promoting the affected driver from advisory to the binding gate (replacing the scoped //cloud/cloud-ci/... fallback).", "status": "diagnosed-batch-fix-specified-G011", "story": "G011"}
{"id": "FRIC-1781071510-identity-architecture-open", "seen_at": "2026-06-10", "friction": "identity layering (cloud IdP vs oya product identity) is an OPEN architecture decision, surfaced when #651 was ready to merge: #651's oya/identity stands up its OWN OIDC issuer (oidc-issuer-kernel + IP-003 Zitadel adapter) with NO cloud-iam/cloud-kms dependency \u2014 a parallel IdP, not a consumer of the cloud IdP. Founder reopened the model: (a) cloud-owned only, (b) cloud IdP + ONE shared oya product identity consuming it (pure dogfood), (c) cloud IdP + PER-PRODUCT identity. Merging #651 would prematurely commit option (c)-ish.", "pipeline_defect": "a contested core-architecture decision had a PR about to land that commits one model without the decision being made \u2014 review APPROVE'd it on code quality but the layering question was not yet a directive. Identity model must be decided (hyperscaler-grounded) before identity PRs merge.", "enforcement_fix": "HOLD #651 pending the identity-architecture decision (exploration workflow launched: hyperscaler patterns for platform-vs-product-vs-service identity in multi-product suites + structured option analysis -> recommendation). Once decided: an ADR fixes the model; #651 either fits or is reworked to consume the cloud IdP via a port. Backlog: identity-architecture-decision (blocks G05 merge).", "status": "651-HELD-exploration-launched", "story": "G005"}
{"id": "FRIC-1781071664-ci-serial-async-audit", "seen_at": "2026-06-10", "friction": "CI async audit (founder-directed): oya-ci-required carries serial/redundant work. Top: gate-registry-drift has a COSMETIC `needs:[producer-regen]` (no data dependency \u2014 it re-materializes in-job) that serializes it behind producer-regen for zero benefit; the materialize step (scm-facts emitter + accounting producer via buck2) is cold-rebuilt independently by ~13 runners (every gate leg + firewall + buck2 + registry-drift); affected-driver owner loop serial (separate FRIC). Gate matrix + fan-in are correctly parallel (not defects).", "pipeline_defect": "CI topology has a cosmetic serializing needs-edge + massive redundant cold materialization across runners \u2014 wasted critical-path latency + cost; the owned full-Rust in-process materialization (vs cold buck2 bootstrap per lane) + shared remote cache are the structural fixes (founder async-efficiency thesis).", "enforcement_fix": "{\"ranked_safe_async\": [{\"rank\": 1, \"what\": \"Producer runs ~9 independent whole-corpus collectors strictly serially \\u2014 collect_repo_inputs/build_registry, collect_crosswalk_inputs, collect_enforcement_inputs, collect_brand_residue, collect_bnf_layer_suffix, collect_manifest_hygiene, collect_cargo_prefix, collect_slo_coverage (cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app/src/main.rs:200-241). Each re-walks the same immutable 18,144-path tracked universe / 359-ADR corpus.\", \"fix\": \"Run the independent collectors concurrently. The map phase (8 collect_* + build_registry/crosswalk/enforcement) has zero cross-deps and writes to separate local bindings; keep build_staleness_input (L211) and build_automation_matrix (L212) sequenced after registry/enforcement, then the single build_gate_baseline reduce at L241. In-process: std::thread::scope one thread per collector (zero new deps), or a rayon scope once rayon is added to Cargo.lock (not present yet \\u2014 adding it is the enabling owned-stack primitive). Verified: collectors are pure functions of the same &repo_root/&cfg/&scm_facts/&tracked_paths and the only fan-in is build_gate_baseline.\", \"payoff\": \"Highest-value producer change: collapses ~9 serial whole-corpus walks into ~1 wall-clock walk. Because the producer regenerates ~13x across the workflow (every gate leg, registry-drift, firewall, buck2, producer-regen), every second cut here multiplies across all lanes.\", \"effort\": \"medium\", \"risk\": \"low\"}, {\"rank\": 2, \"what\": \"collect_brand_residue reads EVERY non-carved-out tracked file serially via a single-threaded iterator chain: tracked_paths.iter().filter(...).map(|path| (path.clone(), read_text(repo_root.join(path)))).collect() (cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app/src/main.rs:404-408). With 18,144 tracked paths this is ~18k blocking std::fs::read_to_string calls on one thread \\u2014 the single largest I/O cost in the producer.\", \"fix\": \"Replace the serial .iter().map(read).collect() with rayon par_iter().map(read).collect(). Verified the downstream grouping (L414 onward) folds into a sorted BTreeMap<String, BTreeSet<String>>, so any read order yields a byte-identical face \\u2014 the committed==regenerated determinism invariant is preserved.\", \"payoff\": \"Parallelizes the dominant I/O fan-out across cores; canonical rayon use case. Largest single per-run reduction inside the producer after the collector-level parallelism.\", \"effort\": \"small\", \"risk\": \"low\"}, {\"rank\": 3, \"what\": \"infra/ci/buck2-affected-gate.sh:64-92 \\u2014 `for f in $RUST_REL` runs one `buck2 uquery owner('$f')` per changed file (L66), N serial daemon round-trips accumulating into OWNERS (L91), then sort -u at L93.\", \"fix\": \"Replace the per-file loop with one batched owner() query using the argfile+%Ss pattern the script ALREADY proves works at L107-108 for an arbitrary set (1689 owners cited): write the existence-filtered RUST_REL files to /tmp/gate-owner-inputs.txt and run `buck2 uquery 'owner(%Ss)' @/tmp/gate-owner-inputs.txt` once. Keep the BUCK-package expansion (L73-90) as a tiny second pass over only the */BUCK|BUCK entries the batched owner() did not resolve. Verified semantically identical: owner() over a set returns the union, and the final sort -u makes it order-independent.\", \"payoff\": \"Collapses N daemon round-trips to 1; payoff scales with PR diff size. Low cost since the argfile+%Ss mechanism is already in the script.\", \"effort\": \"small\", \"risk\": \"low\"}, {\"rank\": 4, \"what\": \"scm-facts emitter run() calls three git subprocesses strictly serially: tracked_paths (git ls-files), last_touch (git log --name-only over full history), revision_author_timestamps (git log %H %ct over full history) (cloud/cloud-ci/gates/oya-cloud-ci-scm-facts-emitter-app/src/main.rs:130-138). The two full-history git-log walks are the dominant cost.\", \"fix\": \"Spawn the three git Commands concurrently (std::thread::scope one thread per query, or Command::spawn all three then collect via wai", "status": "audited-quick-wins-identified-G011", "story": "G011"}
{"id": "FRIC-1781072059-corpus-decay-fundamental", "seen_at": "2026-06-10", "friction": "FUNDAMENTAL (founder genuinely has no answer): accepted-ADR/founder directives get missed/drifted/lost-across-sessions/stale; docs go stale; dead code, dead files, unmaintained docs+code \u2014 all decay with no fundamental mechanism. Surfaced when #651 missed the cloud-IdP-vs-oya-product-identity directive.", "pipeline_defect": "artifacts (code/docs/directives) lose binding to (a) purpose/authority, (b) their relationships, (c) liveness \u2014 and nothing mechanically detects/prevents decay. Root: directives are prose disconnected from the work they govern and from any enforcement.", "enforcement_fix": "CORPUS LIVENESS GRAPH (recommendation at .omc/ultragoal/RECOMMENDATION-corpus-liveness-graph.md): whole corpus = nodes in ONE content-addressed AST-derived graph; decay = fail-closed CI invariants (orphan=dead, broken-edge=stale, stale-node=unmaintained, missing-compliance-edge=drift, untraversed-edge=missed-directive). Docs/directives are BUILD ARTIFACTS with deps (buck2 dirty-tracking applied corpus-wide). Mechanizable directives->Cedar/gate; non-mechanizable->directive-coverage review axis. Built on W2 AST + registry + generated-faces. Sequence: research precedents -> ADR (self-referential, founder-ratified).", "status": "recommendation-delivered-research-then-ADR-G011-foundational", "story": "G011"}
{"id":"FRIC-1781069288","seen_at":"2026-06-10","status_update":"merge-driver-LANDED-glob-members-in-flight","evidence":"PR #661 squash-merged to dev @ 28154faa7 (tools/oya-cargo-lock-merge-driver-app: 3-way structural [[package]] merge, dep-array union sorted canonical + one-sided-removal-wins, lockfile-version fail-closed, 1:1 stem replacement; .gitattributes Cargo.lock merge=cargo-lock; 12 fixtures buck2-green). Two adversarial review rounds (BLOCK->fix->re-BLOCK->fix->APPROVE) before merge; CI-green-!= -review-clean held. Glob members + workspace-glob-coverage gate + ADR-0538 ride PR-1 (agent/g11-glob-members, in flight).","story":"G011"}
{"id":"FRIC-1781069288","seen_at":"2026-06-10","status_update":"RESOLVED-structurally","evidence":"PR #662 merged (dev @ 5aaa68ab4): root Cargo.toml = 6 narrowed globs (libs/oya-*, cloud/*/crates/oya-*, cloud/cloud-ci/gates/*, oya/*/crates/oya-*, oya/office/oya-*, tools/oya-*) + 2 excludes; libs/oya-workspace-members-kernel = canonical resolver (5 parsers migrated, zero textual members parsing left); oya-cloud-ci-workspace-glob-coverage-app gate born-blocking (explicit-path regression + orphan crate = impossible-to-ship); ADR-0538. Equivalence proven: 816 members, delta vs dev exactly the 2 new crates. Plus PR #661 merge driver. New-crate PRs now require ZERO shared Cargo.toml edits; lock conflicts merge structurally.","story":"G011"}
{"id":"FRIC-1781082000-G011","seen_at":"2026-06-10","friction":"PR #662 needed TWO sequential CI repair pushes after rebase: (1) stale Cargo.lock (FRIC-1781062100 signature, exit 101 across all cargo-based gate legs) because nothing pre-push checks lock freshness; (2) stale scm-facts.generated.json because a lock change invalidates committed producer faces and nothing rematerializes them pre-push — each cost a full ~15-job CI round-trip","pipeline_defect":"the two staleness classes (lock vs manifests, faces vs lock) are detected only at CI runtime, serially, instead of by one local pre-push composite check; the agent must know two undocumented repair invocations (cargo metadata; materialize-cloud-ci-generated-faces.sh)","enforcement_fix":"G011: single pre-push freshness gate (Rust, hook-layer + CI fast-fail first job): lock-freshness check + producer-face rematerialize-and-diff check in one pass with remediation commands in the failure message; candidates to fold into the existing workspace-glob-coverage or a new oya-cloud-ci-freshness gate per ADR-0132 single-concern","status":"open","story":"G011"}
{"id":"FRIC-1781082000-G011","seen_at":"2026-06-10","status_update":"RESOLVED","evidence":"PR #664 merged (dev @ 2705d1c96): oya-cloud-ci-freshness-app gate — lock-freshness via oya-workspace-members-kernel (lock_missing_member_package / lock_stale_member_version / lock_orphan_path_package) + face-freshness reusing the EXACT CI producer binaries (byte-parity, no false-green reimpl); remediation commands verbatim in findings; dev-cli gate run-all --ci-required lane (local bridge) + standalone buck2-binary CI job (canonical, no needs-edges, in rollup); ADR-0539. Also closes FRIC-1781062100 fixes 1-2 (pre-push lock check + documented regeneration path; per-job dedupe addressed by canonical first diagnosis). generated-output-diff-policy upgraded blunt-block -> precise byte-equality per ADR-0539. Adversarial review APPROVE first pass; CI 18/18.","story":"G011"}
{"id":"FRIC-1781063357-G03","seen_at":"2026-06-10","status_update":"RESOLVED-ratchet-active","evidence":"PR #665 merged (dev @ 2c097d181): oya-cloud-ci-target-parity-app gate + producer target-parity face (ADR-0540). member_missing_buck born-blocking frozen-empty (debt 0); member_test_code_without_rust_test_target baseline-block-on-new with mechanically-frozen 634-key baseline (reviewer re-derived byte-exact, zero padding/omission). New crates/test-code without a compiled buck2 rust_test counterpart are now impossible-to-ship; PR #645 false-green class closed for NEW debt. BURN-DOWN of the 634 frozen keys = open follow-on (mass BUCK rust_test wiring; many will surface latent uncompilable tests). Also: ADR-0539 freshness gate scored its FIRST real catch on this PR (2 stale faces, one-push fix vs #662's two blind round-trips). Review APPROVE first pass; CI 19/19.","story":"G011"}
{"id":"FRIC-1781100100-G011","seen_at":"2026-06-10","friction":"root CLAUDE.md line 25 cites 'ADR-0130 agentic SLO-gated promotion' but ADR-0130 is the knowledge-graph-registry deprecation; the SLO-promotion ADR is ADR-0139 — the stale citation propagated verbatim into a freshly authored governance doc (ADR-0541 draft) and was only caught by adversarial review","pipeline_defect":"the authoritative project-rules file carries an unchecked cross-reference; nothing validates CLAUDE.md ADR citations against the decision corpus, so its errors replicate into everything that trusts it","enforcement_fix":"correct CLAUDE.md:25 (ADR-0130 -> ADR-0139) in a small PR; durable: ADR-0541 D2 reference-integrity invariant covers CLAUDE.md as a doc node (doc->ADR edges hash-checked) — this friction is the live exhibit for that invariant class","status":"open","story":"G011"}
{"id":"FRIC-1781100200-G011","seen_at":"2026-06-10","friction":"ADR-0541 PR hit the freshness gate because the materialize script was run while the new ADR file was still UNTRACKED: the producer enumerates tracked paths, so regeneration before 'git add' bakes faces that exclude the new file; correct order (add -> materialize -> commit) is undocumented and the gate's remediation text does not mention it","pipeline_defect":"face regeneration has a hidden git-index ordering dependency; the remediation message names the command but not the precondition, so the gate's second catch of the day was a one-round-trip avoidable error","enforcement_fix":"extend the freshness gate remediation text with 'git add new files first; faces regenerate from TRACKED paths'; optionally the materialize script warns when untracked *.md/*.rs exist under governed roots","status":"open","story":"G011"}
{"id":"FRIC-1781100200-G011","seen_at":"2026-06-10","status_update":"root-cause-refined","evidence":"Second freshness catch on PR #666 head 35eb8c746 revealed the full protocol: scm-facts records per-path last_touch_commit, so ANY commit mixing content changes with regenerated faces is self-invalidating (the face cannot contain the hash of the commit that includes it). Terminating protocol = content commit FIRST, then materialize, then a FACES-ONLY settle commit (which changes no row inputs). The untracked-file precondition (first catch) and the faces-only-commit requirement (second catch) both belong in the gate remediation text: 'git add+commit content first; then materialize; then commit faces alone'.","story":"G011"}
{"id":"FRIC-CORPUS-LIVENESS","seen_at":"2026-06-10","status_update":"ADR-0541-PROPOSED-LANDED","evidence":"PR #666 merged (dev @ 0b03f15c0): ADR-0541 corpus-liveness-graph authored from the verified precedent corpus (.omc/research/corpus-liveness-precedents-20260610.json, 104-agent deep-research, 25/25 claims 3-0). Decision: one content-addressed corpus graph on W2 owned AST; six parameterized invariant classes subsume the gate fleet via verified-equivalence migration; per-class enforcement follows evidence (structural=fail-closed, deletion=SCARF quarantine+telemetry ratchet-only, doc/directive=unprecedented so staged report-only->baseline->born-blocking, index gaps fail-open-with-report). D4 spike IP = next executable step. FOUNDER RATIFICATION PENDING (door:one-way). Critic APPROVE after 1 MAJOR fix (stale ADR-0130->0139 cross-ref inherited from CLAUDE.md:25 = FRIC-1781100100, still open).","story":"G011"}
{"id":"FRIC-1781100200-G011","seen_at":"2026-06-10","status_update":"escalated-to-automation","evidence":"Third freshness-gate catch in one day; second from the identical settle-protocol trap (content + faces in one commit), committed by the LEADER an hour after documenting the protocol — proof that documentation does not fix process (automation-maximalism: manual-twice = write the automation).","enforcement_fix":"mechanical, not textual: (a) the face-freshness checker (oya-cloud-ci-freshness-app) learns the settle protocol and its finding says exactly 'commit content first, rerun materialize, commit faces alone'; (b) Rust successor of materialize-cloud-ci-generated-faces.sh (G011 zero-shell ledger) refuses to run with staged/dirty NON-face changes unless --settle is passed, and with --settle commits the faces-only diff itself; (c) optional pre-push hook mirror. Until then every face-touching PR pays one avoidable CI round-trip.","status":"open","story":"G011"}
{"id":"FRIC-1781100100-G011","seen_at":"2026-06-10","status_update":"RESOLVED","evidence":"PR #667 merged (dev @ ab732d87b): CLAUDE.md observability_substrate line now cites ADR-0139 (verified titles at head). Fresh-context verifier APPROVE; CI 19/19 after one settle-protocol round-trip (the leader hit FRIC-1781100200 himself — escalation to automation already logged).","story":"G011"}
{"id":"FRIC-1781100200-G011","seen_at":"2026-06-10","status_update":"RESOLVED","evidence":"PR #668 merged (dev @ a8797a4df): freshness-gate remediation now teaches the full settle protocol (FACE_SETTLE_PROTOCOL wired into every finding/message path); new oya-cloud-ci-face-settle bin — check mode refuses on dirty OR untracked non-face changes (git ls-files --others) with protocol text, --settle stages exactly the face paths (explicit pathspec + post-stage set-equality guard), --commit creates the faces-only settle commit; 17 buck2 fixtures incl. end-to-end commit-path coverage with local-only git config. Review cycle: BLOCK (untracked-file gap + untested commit path) -> fixed in code -> re-verify APPROVE. The trap that cost 3 CI round-trips on 2026-06-10 is now mechanically refused at the source.","story":"G011"}
{"id":"FRIC-012","seen_at":"2026-06-10","status_update":"RESOLVED-fully","evidence":"Part (c) landed: PR #669 merged (dev @ 16f2e3b54) — oya-cloud-ci-enforcement-liveness-app gate, born-blocking frozen-empty on hook_unwired_without_stub_marker / hook_wiring_mirror_drift / wired_hook_missing_file; producer parses actual JSON command values (structural, comment-evasion-proof); .claude/settings.local.json gitignored. Parts (a)/(b) were already live (tracked .claude/settings.json mirroring .codex/hooks.json). The 11-days-unenforced class is now impossible-to-ship: any hook in tools/hooks/ must be dual-wired or stub-marked, the two wiring files cannot drift, and dangling references fail closed. Review APPROVE first pass; CI 20/20.","story":"G011"}
{"id":"FRIC-1781110000","seen_at":"2026-06-10","friction":"Headless codex exec lane worker died silently at a context-compaction boundary (checkout-guard lane, 12:32 local), leaving a dirty worktree with an uncommitted hardening edit and no PR; nothing detected the death — discovery was manual leader inspection of worktree state hours later","pipeline_defect":"lane liveness is unmonitored: dispatch ledger rows are written at dispatch and PR-open only, with no heartbeat/exit-status capture; a dead worker is indistinguishable from a slow one","enforcement_fix":"lane supervisor: dispatch wrapper records worker PID + exit status + last-output timestamp into the dispatch ledger; a staleness detector (same class as ADR-0539 freshness) flags lanes with no ledger heartbeat and no PR after T minutes; workers commit WIP before any compaction-prone long phase (brief template amendment: commit-early discipline)","status":"open","goal":"G011"}
{"id":"FRIC-1781111000","seen_at":"2026-06-10","friction":"Two manually-dispatched codex exec lane workers blocked forever at Reading-additional-input-from-stdin because the background dispatch path handed them an open unix-socket stdin (review dispatch minutes earlier happened to get /dev/null and proceeded); cost two kill-and-relaunch cycles and ~10 min of silent zero progress","pipeline_defect":"lane dispatch is a hand-typed shell incantation with an unenforced invariant (stdin must be closed/dev-null); same manual-dispatch fragility class as FRIC-1781110000","enforcement_fix":"oya-lane-supervisor dispatch subcommand owns process spawning: stdin always null, stdout/stderr always to a ledger-recorded log, detached; manual codex exec dispatch retired for lanes once the tool lands (already-dispatched lane L-20260610-003)","status":"fix-in-flight","goal":"G011"}
{"id":"FRIC-1781112000","seen_at":"2026-06-10","friction":"PR #670 added a brand-new manifest-hygiene debt key (tool crate missing doctest=false) and the regenerated gate-baseline face committed in the same PR absorbed it; all 21 CI checks stayed green and only the adversarial reviewer caught it by diffing baseline keys against origin/dev","pipeline_defect":"baseline-block-on-new gates compare live state against the baseline file AS COMMITTED IN THE PR, so regenerating the baseline in the same change launders new debt past the ratchet — the gate is evadable by the exact mechanism (producer regeneration) the settle protocol mandates","enforcement_fix":"ratchet gates must evaluate new-debt against the MERGE-BASE baseline (git show origin/dev:<face> as the frozen reference), not the working-tree copy; a baseline key ADDED relative to merge-base fails closed unless carried by an explicit baseline-grow exception record; apply to manifest-hygiene, target-parity, and every baseline-block-on-new gate","status":"open","goal":"G011"}
{"id":"FRIC-1781110000-G011-lane-supervisor","seen_at":"2026-06-10","status_update":"fix-delivered","evidence":"Tool-layer fix delivered by tools/oya-lane-supervisor-app: dispatch records wrapper pid, start timestamp, log path, wait-file path, and stdin-null codex exec launch; reap appends pr-open/exited/stalled/dead rows from process liveness, wait-file exit status, log mtime, and gh PR presence; status emits human or JSON summaries. TEAMMATE-PREAMBLE now requires commit-early discipline before long build/test phases and after self-review fix rounds. Gate wiring remains follow-on once cloud-ci owns lane state.","story":"G011"}
{"id": "FRIC-1781124000", "seen_at": "2026-06-10", "friction": "Merging #684 (lane-supervisor) regressed .omc/ultragoal/TEAMMATE-PREAMBLE.md: the retrain reapplied the worker branch content tree, which carried a STALE copy of the preamble (pre-AMENDMENT-14, still says dispatched-via-codex-exec) over the leader Fable-dispatch edits already on dev \u2014 a doc-clobber by a content branch that touched a shared evolving doc", "pipeline_defect": "shared living docs (preamble/checkpoint/resume) edited by both leader and worker branches have no merge-aware protection; retrain/squash overwrites whichever side is older with no conflict surfaced (the doc is not in the HARD-collision intake set)", "enforcement_fix": "(a) add the ultragoal living-docs set (TEAMMATE-PREAMBLE/RESUME-PROMPT/CHECKPOINT/brief) to the dispatch-ledger HARD-collision domain so worker lanes never carry them; (b) workers edit ONLY their lane surfaces, never the shared preamble; (c) leader re-asserts the canonical preamble post-merge \u2014 durable fix is moving these into a single-writer governance surface", "status": "open", "goal": "G011"}
{"id": "FRIC-1781125000", "seen_at": "2026-06-10", "friction": "checkout-guard review r5: a parse-based PreToolUse hook cannot be made bypass-proof against arbitrary shell EXPANSION (command substitution producing git, eval $(...), $VAR/${x:-}/$'...' in command/subcommand position) without reimplementing the shell; 5 review rounds drove it from trivially-bypassable to exotic-only, but the residual is irreducible for a parser", "pipeline_defect": "the canonical-checkout protection was assigned to a shell-PARSING safety-net hook; per enforcement-layering doctrine (hooks=safety-net, structural impossibility preferred, audit Q: does the rule hold with hooks disabled?) the CANONICAL control must be structural, not parse-based", "enforcement_fix": "land the guard as the strong best-effort safety-net layer it now is (literal + wrapper + static-substitution coverage, default-closed, depth-fail-closed); make the canonical protection STRUCTURAL \u2014 candidates: filesystem/ownership perms on the canonical worktree for agent processes, a git pre-operation hook in the canonical .git, or agents only ever operating in worktrees by construction (the lane-supervisor enforces worktree dispatch). Founder decision required on the structural control.", "status": "escalated-founder-decision", "goal": "G011", "evidence": "PR #685 landed the guard as the strong best-effort safety-net layer (literal+wrapper+static-expansion coverage, default-closed, depth+left-fold fail-closed, r20 leader APPROVE); the parser-residual is runtime-unknowable dynamic content. Structural canonical control (worktree-only dispatch via merged lane-supervisor #684 + fs perms) is the accepted-risk mitigation pending founder decision; systemic follow-up is the differential-bash oracle in FRIC-1781128000."}
{"id": "FRIC-1781126000", "seen_at": "2026-06-10", "friction": "PR #685 went red on rust-first-automation-hygiene in CI: the new tools/hooks/main-checkout-guard.sh shim was registered in the ADR-0523 irreducible-glue ledger (prose, class 6) but NOT in the gate's machine-readable rust-first-automation-policy.json \u2014 two registration surfaces for the same fact drifted within a single PR", "pipeline_defect": "the irreducible-glue ledger is dual-homed (ADR prose + gate policy JSON) with no cross-artifact agreement check; any exception added in one surface can silently miss the other \u2014 staleness-by-construction, the exact class the cross-artifact-agreement gate (GATE-1) exists for but does not cover", "enforcement_fix": "make the gate policy JSON the single machine source for glue exceptions and have ADR-0523 reference it (docs-as-build-artifact per ADR-0541 corpus direction), OR extend cross-artifact-agreement (GATE-1) to assert ADR-0523 ledger rows == policy exceptions for tools/hooks paths; also: leader settle protocol gains a pre-push step running the affected //cloud/cloud-ci/... gate tests locally so registration gaps surface before CI", "status": "open", "goal": "G011"}
{"id": "FRIC-1781127000", "seen_at": "2026-06-10", "friction": "rust-first hygiene gate's exception status enum is closed over {temporary_legacy_bridge, portable_declarative_bridge}; an ADR-0523 class-6 authorized irreducible-glue shim has no honest status value and had to be filed as temporary_legacy_bridge", "pipeline_defect": "the gate's policy schema cannot express the difference between shrinkable legacy debt and authorized irreducible glue, so the ledger overstates debt and the ratchet cannot distinguish what must shrink from what is allowed to remain", "enforcement_fix": "add an irreducible_glue status to the gate schema requiring an ADR citation field (adr + class), counted separately from legacy-bridge debt in gate accounting; migrate the main-checkout-guard.sh row when the enum lands", "status": "open", "goal": "G011"}
{"id": "FRIC-1781128000", "seen_at": "2026-06-10", "friction": "checkout-guard r19: the r18 transitive-binding fix resolved `$ref` against the FINAL last-wins binding snapshot (8-pass fixpoint) instead of the value at the referencing assignment's execution point, diverging from bash; `A=reset; B=$A; A=log; git -C <canonical> $B --hard` modeled B=log and ALLOWed a canonical wipe \u2014 entire blocked-verb + -C-path surface bypassable", "pipeline_defect": "a security control's value model reimplemented bash scoping with the WRONG evaluation-order semantics, and the existing r18 test corpus did not include a capture-then-reassign ordering case, so the regression shipped green; the systemic gap is that the guard's bash-semantics fidelity has no differential oracle (no test that runs candidate payloads through real bash and asserts the guard's verb/target model matches)", "enforcement_fix": "(a) immediate: left-fold binding resolution \u2014 each RHS resolves against only the bindings to its left (source order), matching bash execution-point expansion; (b) systemic: a differential-fuzz test harness that generates assignment/expansion payloads, executes them under a sandboxed bash to capture the real argv, and asserts the guard's DENY/ALLOW matches whether that argv is a canonical-targeted mutation \u2014 converts 'reviewer finds Nth bypass' into a mechanical oracle. Tracks toward the structural control of FRIC-1781125000.", "status": "open", "goal": "G011"}
{"id": "FRIC-1781129000", "seen_at": "2026-06-10", "friction": "cloud-kms operator review M1: the kube-agnostic orchestration traits (ObservedStateProvider, KmsOperatorActuator, run_reconcile_cycle, ExponentialBackoff, ReconcileCycleReport) live in the ADR-0510-transient k8s-adapter crate, and the app imports its run loop from that transient crate \u2014 so at cloud-k8s cutover, pure orchestration code would be discarded with the kube adapter", "pipeline_defect": "the clean-arch port/adapter seam is mis-drawn: pure (cutover-stable) orchestration is colocated with transient (cutover-discarded) kube wiring; nothing mechanically enforces that zero-kube-dep code lives in a pure crate, so the 'would this interface change at cutover' litmus is left to reviewer eyes", "enforcement_fix": "(a) fast-follow: extract a pure oya-cloud-kms-operator-core crate holding the kube-agnostic traits + run loop; adapter depends on core, app depends on core; (b) systemic + universal: a cloud-ci gate that asserts crates named *-kernel/*-core carry zero transient-tech deps (kube/rustls/etc per a policy-pack denylist) \u2014 the same kernel-purity check every operator in the paved-road product needs; ties to the pipeline-as-product R0 seam-enforcement", "status": "open", "goal": "G002"}
{"id": "FRIC-1781129500", "seen_at": "2026-06-10", "friction": "cloud-kms operator review M2: the adapter's observed-state read uses ListParams::default() (single page) while declaring ReadConsistency::Complete \u2014 a large CRD set would be under-read, though fail-safe (desired derives from observed, so an unread object yields no action: delayed convergence, never key destruction)", "pipeline_defect": "a correctness gap (incomplete list pagination) shipped behind a Complete-consistency label; no gate asserts list-all-pages for reconcilers that claim complete reads", "enforcement_fix": "fast-follow: paginate via continue-token until exhausted before computing actions; consider a lint/gate for kube list calls that pair Complete consistency with a single ListParams::default() read", "status": "open", "goal": "G002"}
{"id": "FRIC-1781130000", "seen_at": "2026-06-10", "friction": "PR #644 retrain: the branch had silently re-encoded specs/root-hub-pointers.json from escaped-unicode (\\uXXXX, ensure_ascii) to literal UTF-8 (\u2192 \u00b5 \u00a7 \u2014 etc.), producing ~30 lines of churn on content unrelated to the lane's one intentional pointer addition; some tool/editor in the lane rewrote the JSON with different serialization settings", "pipeline_defect": "shared machine-readable JSON files have no canonical-serialization gate, so any agent/tool that round-trips them through a serializer with different ensure_ascii/indent/key-order settings introduces invisible churn that pollutes diffs, risks merge conflicts, and can defeat cross-artifact-agreement; this is a hermetic-output failure (same logical content, non-deterministic bytes)", "enforcement_fix": "a cloud-ci canonical-JSON gate (universal, pack-shaped): every tracked *.json under governed roots must equal its canonical re-serialization (stable key order, fixed indent, explicit ensure_ascii policy); the gate normalizes-and-compares and fails on byte drift, with a one-command fixer. This is a reusable paved-road capability \u2014 deterministic serialization is part of 'hermetic output for any repo'. Interim: lane fixed by restoring dev bytes + surgical re-add", "status": "open", "goal": "G011"}
{"id": "FRIC-1781131000", "seen_at": "2026-06-10", "friction": "PR #644 cedar-adapter never built hermetically: src/lib.rs include_str!(\"../policy/cloud-intelligence.cedar\") resolves (relative to the source file) to ADAPTER_ROOT/policy/..., but the BUCK mapped_srcs placed the export_file at the repo path cloud/cloud-intelligence/policy/... \u2014 so the hermetic sandbox lacked the file at the include-relative location and rustc failed 'couldn't read'. buck2 masked the real error as a downstream 'missing rmeta' and (under cold ordering) a ring-buildscript failure, sending diagnosis down a false path", "pipeline_defect": "(1) a crate with a broken include_str! BUCK mapping reached PR/merge-queue because the lane's pre-open filter did not run buck2 build on the affected target (green-on-head was asserted on a different scope); (2) buck2's failure_filter reports the downstream missing-rmeta artifact, not the upstream rustc 'couldn't read' \u2014 diagnosis cost is high without -v5", "enforcement_fix": "(a) universal hermeticity gate: statically parse include_str!/include_bytes! string-literal paths in each crate and assert the include-relative resolved path is present in that target's srcs/mapped_srcs destinations \u2014 catches unmapped embedded assets before build, repo-agnostic, pack-shaped; (b) merge-train + lane pre-open filter MUST run buck2 build+test on the exact affected target set (//<changed-crate>/...), not a curated subset; (c) document the 'missing rmeta == upstream rustc failure, rerun -v5' triage in the buck2 runbook", "status": "open", "goal": "G011"}
{"id": "FRIC-1781132000", "seen_at": "2026-06-10", "friction": "TEAMMATE-PREAMBLE \u00a72 instructs lanes to write a TRACKED review-verdict.txt (and premise.txt) and commit it; lanes do so at the repo ROOT, so squash-merges land premise*.txt / review-verdict*.txt as product-tree pollution (dev already carries root premise.txt + review-verdict.txt from earlier lanes)", "pipeline_defect": "the documented lane protocol mandates committing process scratch (premise, pre-open verdict) into tracked files at repo root, directly causing product-tree pollution; this is a protocol-induced hermeticity failure \u2014 the verdict's true durable home is the PR body + dispatch-ledger, not the source tree", "enforcement_fix": "(a) amend preamble \u00a72 to write lane scratch under a scoped, gitignored-or-swept path (.omc/ultragoal/lanes/<lane>/ or evidence/), never repo root; (b) a cloud-ci tree-hygiene gate (universal, pack-shaped) that fails any PR adding repo-root scratch matching premise*.txt/review-verdict*.txt/*.tmp outside allowed dirs; (c) sweep the pre-existing root premise.txt + review-verdict.txt off dev in a follow-up", "status": "open", "goal": "G013"}
{"id":"FRIC-1781133000","seen_at":"2026-06-11","friction":"G013 final-quality-gate slop scan found two raw TODO(codex-adapter) comments in production source (oya-cloud-intelligence-rest/src/lib.rs): a deferred CodexAdapter mirroring the implemented AnthropicAdapter, pending Codex OAuth refresh-flow documentation","pipeline_defect":"deferred work was recorded as a raw TODO comment rotting in source rather than as a governed, tracked-to-closure ledger entry; a TODO in code is an untracked friction that escapes the closed-loop accounting the friction-accounting gate enforces","enforcement_fix":"converted both comments to governed deferral prose referencing ADR-0384 §v1-scope + this friction id (so production grep for TODO/FIXME returns 0 in accepted scope); the CodexAdapter follow-up is now tracked here to closure. Systemic: the rust-first/anti-slop gate family should flag raw TODO/FIXME tokens in production src and require they be ledger/ADR references instead","status":"open","goal":"G013"}
{"id": "FRIC-1781130000", "seen_at": "2026-06-10", "status_update": "fixed-in-PR", "evidence": "gate-id cloud-ci-canonical-json (ADR-0546): oya-cloud-ci-canonical-json-app cloud-ci determinism gate on branch agent/g11-canonical-json-gate. Every tracked *.json under governed_roots (specs/, excl *.generated.json faces + specs/fixtures/) must equal its canonical re-serialization (literal UTF-8, 2-space indent, source key order, trailing newline) computed by a self-contained lexical canonicalizer (NOT serde_json::to_string_pretty, to stay independent of the reindeer-unioned preserve_order/arbitrary_precision features). Born-blocking at ZERO baseline: json_not_canonical / json_parse_error / json_duplicate_key are all empty on the live corpus, so a future lane re-encoding specs/root-hub-pointers.json (escaped<->literal, either direction) is impossible to ship. One-command fixer: buck2 run //cloud/cloud-ci/gates/oya-cloud-ci-canonical-json-app:oya-cloud-ci-canonical-json-bin -- --fix (local bridge feedback only; merge authority is the gate rust_test behind oya-ci-required). This PR fixed the 7 non-canonical spec files in place (incl the FRIC exemplar root-hub-pointers.json; parsed JSON byte-identical before/after, only serialization bytes changed). buck2 evidence: canonical-json unittest 47/47 + gate 4/4, firewall gate_registration 2/2, full //cloud/cloud-ci/... 40/40 GREEN after faces settle.", "story": "G011"}
{"id": "FRIC-1781180000", "seen_at": "2026-06-11", "friction": "independent review of PR #689 (canonical-json gate) found MED: parse_hex4 validated \\u escapes with u32::from_str_radix alone, which accepts a leading sign, so the strictly-invalid escape \\u+12f (rejected by RFC 8259 section 7, serde_json, Python) decoded as U+012F; the gate misclassified the file as fixable json_not_canonical drift and --fix silently rewrote invalid JSON into valid JSON instead of refusing - a refuse-defects contract violation (the fixer must never launder a parse defect into a rewrite)", "enforcement_fix": "parse_hex4 now requires exactly 4 ASCII hex digits (each byte in [0-9a-fA-F]) before from_str_radix; any signed/spaced/short escape is json_parse_error and the fixer refuses it byte-unchanged. RED-first regression fixtures added: signed_or_spaced_hex_escape_is_parse_error_not_drift (probes \\u+12f, \\u-123, \\u-000, \\u 123, \\u12) + fixer_refuses_signed_hex_escape_without_rewriting (classification + refusal + bytes-untouched). ADR-0546 sharpened in the same PR to state explicitly that key-order reordering (sort_keys=false) and number-lexeme respelling (1E9 vs 1e9) remain UNGATED residual drift classes", "status": "fixed-in-PR", "evidence": "gate-id cloud-ci-canonical-json (ADR-0546): strict-hex fix on branch agent/g11-canonical-json-hexfix; buck2 oya-cloud-ci-canonical-json-app-unittest 49/49 (47 prior + 2 new RED-then-GREEN) and -gate 4/4 GREEN; fixture proves a \\u+12f file classifies json_parse_error (never json_not_canonical) and --fix leaves it byte-unchanged with a refusal", "goal": "G011"}
{"id": "FRIC-1781131000", "seen_at": "2026-06-11", "status_update": "fixed-in-PR", "enforcement_fix": "Delivered enforcement-fix (a): the cloud-ci-embedded-asset-hermeticity gate (ADR-0545) statically asserts every include_str!/include_bytes! string-literal resolves within its Buck target's __srcs tree to a member of {srcs short-paths union mapped_srcs VALUES}; the unmapped-include class is now born-blocking. Ships an auto-remediator (--fix) per the automation-default directive. The gate itself caught a live SECOND instance (the ci-webhook-gateway cedar adapter), fixed as a prerequisite in the same PR.", "evidence": "gate-id cloud-ci-embedded-asset-hermeticity; ADR-0545 (docs/decisions/ADR-0545-embedded-asset-hermeticity-gate.md); crate cloud/cloud-ci/gates/oya-cloud-ci-embedded-asset-hermeticity-app; live-corpus gate test GREEN (embedded_asset_unmapped_include == empty); PR agent/g11-embedded-asset-gate vs dev. The closed loop: the friction that motivated the gate is now terminated BY the gate.", "goal": "G011"}
{"id": "FRIC-1781131000-buck-syntax-kernel", "seen_at": "2026-06-11", "friction": "The embedded-asset-hermeticity gate (ADR-0545) ships a minimal in-crate Buck srcs/mapped_srcs/crate_root destination parser — the THIRD text-heuristic Buck parser in the repo (tools/oya-buck-test-wiring-app, oya-cloud-ci-accounting-registry-app, and now this gate). Three independent BUCK parsers drift and each re-derives glob expansion, mapped_srcs comprehension handling, and call-block extraction.", "pipeline_defect": "duplicated BUCK-manifest parsing across gate/tool crates (SHARED-KERNEL-CANDIDATES.md #1) with no single oracle and no non-duplication lint; a parser fix in one consumer silently leaves the others wrong.", "enforcement_fix": "extract oya-buck-syntax-kernel (SHARED-KERNEL-CANDIDATES #1 / task #10) at the second consumer per the founder shared-kernel doctrine, migrate all three consumers, and add the workspace-glob-coverage-style non-duplication lint. Deferred from ADR-0545 (smallest-viable-change: the shared kernel does not yet exist).", "status": "queued-shared-kernel", "goal": "G011"}
{"id": "FRIC-1781131000-webhook-cedar-buck2", "seen_at": "2026-06-11", "friction": "oya-ci-webhook-gateway-authz-cedar-adapter was a live SECOND instance of FRIC-1781131000: its include_str!(\"../../../policy/ci-webhook-gateway.cedar\") was mapped to the crate-local sandbox path \"policy/...\" while a SHORT crate_root placed src/lib.rs at the package-relative short path, so the include escaped the __srcs tree. buck2 build FAILED ('couldn't read .../__srcs/src/../../../policy/...', masked as missing-rmeta). It had been 'fixed' cargo-only in an earlier commit and was never buck2-buildable nor in the oya-ci-required matrix. The ADR-0545 gate caught it during design.", "pipeline_defect": "a non-hermetic crate reached dev because (1) the earlier fix was validated under cargo (retired) not buck2, and (2) the target was absent from the buck2 build matrix — the exact gap FRIC-1781131000(b) names (pre-open filter / merge-train must buck2-build the affected target set).", "enforcement_fix": "rewrote the adapter BUCK to the cedar comprehension pattern (ROOT-prefixed crate_root + mapped_srcs comprehension + explicit deep mapped value for the cross-package policy) so the include resolves to its sandbox destination; buck2 build now SUCCEEDS. The ADR-0545 gate is now born-blocking on this class, and registered the adapter's hermeticity in its live-corpus self-test.", "evidence": "buck2 build //oya/ci-webhook-gateway/crates/oya-ci-webhook-gateway-authz-cedar-adapter:oya-ci-webhook-gateway-authz-cedar-adapter => BUILD SUCCEEDED (was failing); gate-id cloud-ci-embedded-asset-hermeticity; ADR-0545; PR agent/g11-embedded-asset-gate vs dev.", "status": "fixed-in-PR", "goal": "G011"}
{"id": "FRIC-1781190000", "seen_at": "2026-06-11", "friction": "auto-remediator (--fix) wrote unparseable BUCK output: apply_remediation emitted a new mapped_srcs block with no separating comma from the prior field when that field lacked a trailing comma (valid Starlark), producing a buck2 parse error on the written file", "pipeline_defect": "gate fixer produced corrupt output for a valid-but-no-trailing-comma input shape; the gate then reported GREEN while the patched BUCK was unreadable by buck2 — gate-GREEN/build-RED state", "enforcement_fix": "structural: (1) apply_remediation detects the no-trailing-comma case and inserts the separator comma before emitting mapped_srcs; (2) validate_remediation_output reparse guard — after generating any rewritten BUCK content, reparse it with the crates own parser before returning Ok; if the target block is not findable or the injected key is absent, return Err and leave the file untouched. This closes the whole corruption class structurally (enforcement-layering doctrine: structural impossibility preferred). Fixture-pinned: apply_remediation_no_trailing_comma_creates_valid_mapped_srcs + apply_remediation_self_validation_refuses_corrupt_output in lib.rs unit tests.", "status": "fixed-in-PR", "evidence": "gate-id cloud-ci-embedded-asset-hermeticity; ADR-0545; PR #690 agent/g11-embedded-asset-gate; buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-embedded-asset-hermeticity-app:...-unittest 34/34 GREEN", "goal": "G011"}
{"id": "FRIC-1781190000-guard-v2", "seen_at": "2026-06-11", "friction": "FRIC-1781190000 correction: the original guard v1 description overstated its scope. The v1 validate_remediation_output was findability-only (target string + key token present in output) — NOT a real parse round-trip. This was demonstrated false by the reviewer probe: a trailing-comment block produces a double-comma that v1 passed (target findable, key findable) while the BUCK was structurally corrupt.", "pipeline_defect": "guard v1 claimed to close the corruption class structurally but only checked string-token presence, not parse-round-trip success; the ledger row for FRIC-1781190000 incorrectly stated the class was closed", "enforcement_fix": "guard v2 (this PR): (a) comment-bearing blocks are refused (not edited) via block_has_out_of_string_comment — structurally impossible to corrupt; (b) validate_remediation_output now calls parse_buck_targets round-trip and asserts the injected mapped_srcs VALUE appears in the reparsed target mapped_dest list (not just as a string token). Scope stated precisely: the guard closes the corruption class for comment-free target blocks; comment-bearing blocks are reported manual. Destination: oya-buck-syntax-kernel shared parser (FRIC-1781131000-buck-syntax-kernel, task #10) will provide a full Starlark-aware parser that handles comments correctly, removing the comment-bearing manual exception entirely.", "status": "fixed-in-PR", "evidence": "gate-id cloud-ci-embedded-asset-hermeticity; ADR-0545; PR #690; unit tests: apply_remediation_trailing_comment_is_refused_not_corrupted, apply_remediation_self_validation_refuses_corrupt_output", "goal": "G011"}
{"id": "FRIC-1781129000", "seen_at": "2026-06-11", "status_update": "RESOLVED", "enforcement_fix": "ENFORCEMENT half (b) delivered: cloud-ci-kernel-purity gate (ADR-0547) born-blocking-asserts that *-kernel/*-core crates and their workspace-internal path-dep closure carry zero ADR-0510 transient-tech deps (kube/sqlx/rustls/aws-sdk/etcd per kernel-purity-policy.json), making the port/adapter seam-mis-draw class impossible to ship. EXTRACTION half (a) already landed in #686 (oya-cloud-kms-operator-kernel is serde-only; kube/k8s-openapi live in oya-cloud-kms-operator-k8s-adapter; app depends on kernel). The cutover litmus is now mechanical, not reviewer-eyes.", "evidence": "gate-id cloud-ci-kernel-purity; ADR-0547; gate at cloud/cloud-ci/gates/oya-cloud-ci-kernel-purity-app (buck2: 18 unit + 4 gate tests pass, live ~160-kernel corpus born-blocking green); registered in oya-ci-required.yml matrix + docs/oya-ci/gate-catalog.md; extraction half via PR #686 (b966cc312)", "goal": "G002"}
{"id": "FRIC-1781129000", "seen_at": "2026-06-11", "status_update": "RESOLVED", "evidence": "gate-id cloud-ci-kernel-purity; ADR-0547; cloud/cloud-ci/gates/oya-cloud-ci-kernel-purity-app (buck2: 27 unit + 9 gate tests pass, live 156-kernel/core corpus born-blocking green); + automation layer (--fix removes dead transient deps, design-action remediation for live deps; build-deps/dev-deps/features-safe, rust_library-bounded); registered in oya-ci-required.yml matrix + docs/oya-ci/gate-catalog.md; extraction half (a) via PR #686. Hostile RED proof: injecting kube into oya-cloud-kms-operator-kernel fires KP-TRANSIENT-DEP-CARGO; revert green.", "goal": "G002"}
{"id": "FRIC-1781200000", "seen_at": "2026-06-11", "friction": "kernel-purity --fix could write a cargo-rejected manifest: optional+feature-backed dep (kube={version,optional=true} + [features] k8s=[dep:kube]) was classified auto-fixable because the dep was unreferenced in src; removing the dep line left the dep:kube feature entry dangling; cargo rejected with error: feature includes dep:kube but kube is not listed as a dependency", "pipeline_defect": "the fixer did not check whether a dep is wired as dep:X in [features] before removing it; any optional dep that is a feature activator is structurally indivisible from its feature entry, making line-removal unsafe without a paired feature-rewrite", "enforcement_fix": "gate now collects feature_backed_optional_deps at manifest parse time; any dep in that set is classified not-auto-fixable (design-action) regardless of src-usage; the plan_fixes path therefore never schedules a removal for such a dep; the regression fixture fix_is_table_aware_and_does_not_corrupt_manifest was corrected to assert refusal rather than corrupt output; additionally: renamed deps (foo={package=kube}) are also demoted to design-action (HIGH-2); malformed policy deny rules now emit KP-POLICY-MALFORMED and short-circuit evaluation instead of silently dropping rules (HIGH-3); find_block_end uses paren-depth counting so indented closing parens cannot cause BUCK block spans to swallow following rust_test blocks (MED-4)", "status": "fixed-in-PR", "evidence": "buck2 test //cloud/cloud-ci/... -> Pass 42 Fail 0; 32 unit + 9 integration tests; optional_feature_backed_dep_is_not_auto_fixable (unit), renamed_dep_is_not_auto_fixable (unit), malformed_deny_match_value_fails_closed (unit), malformed_deny_missing_dep_key_fails_closed (unit), find_block_end_handles_indented_closing_paren (unit), fix_is_table_aware_and_does_not_corrupt_manifest (integration — corrected to assert refusal); live 156-kernel corpus born-blocking green; PR #691 branch agent/g11-kernel-purity-gate", "goal": "G002"}
{"id": "FRIC-1781200001", "seen_at": "2026-06-11", "friction": "BUCK --fix for kernel-purity gate cannot be made sound with the current paren-depth Starlark block parser: comments and string literals with stray parens can cause the block-boundary detector to mis-terminate, and multi-rule BUCK files with unusual indentation are not handled correctly; shipping a BUCK fixer with these gaps risks corrupting BUCK manifests", "pipeline_defect": "the oya-cloud-ci-kernel-purity gate --fix descoped BUCK writes to refusal-only (remove_buck_dep_line always returns Ok(false)); BUCK findings that would otherwise be auto-fixable are reported as design-actions with remediation pointing to this harness; the Cargo fixer is unaffected", "enforcement_fix": "queued to oya-buck-syntax-kernel fixer harness: a sound Starlark parser (rowan-style AST per W2 ADR) that can safely locate and remove a specific dep entry from a rust_library() block without corrupting surrounding rules; gate will re-enable BUCK --fix once the harness ships and the sound bound is validated", "status": "queued-shared-kernel", "evidence": "ADR-0547 D6 round-3 revision; PR #691 branch agent/g11-kernel-purity-gate; regression fixtures pin the bound: H1-H4 (every [features] reference syntax refuses auto-fix, unit + integration with manifest byte-identical), H5 (stray paren in a BUCK comment still detects the dep below), H6 (unterminated block -> None -> refusal, file byte-identical), fix_rolls_back_all_preimages_when_semantic_revalidation_fails (CRITICAL-A layer-2 rollback restores pre-images byte-identically with the cargo error in the message); evaluate_dep_list classifies every KP-TRANSIENT-DEP-BUCK finding as design-action citing oya-buck-syntax-kernel", "goal": "G002"}
{"id": "FRIC-1781210000", "seen_at": "2026-06-11", "friction": "kernel-purity --fix could remove an optional dep whose implicit feature a sibling member requests; both guard layers blind to cross-member feature resolution", "pipeline_defect": "an optional=true dep with zero src references and NO mention in its own [features] still exports an implicit cargo feature named after itself; a sibling workspace member can request it via features = [\"kube\"] on its path dep; layer 1 (collect_features_referenced_deps) scans only the OWNING manifest so it never sees the sibling request, and layer 2 (cargo metadata --no-deps) does no cross-member feature resolution — so --fix removed the dep, workspace-level cargo metadata then failed, and the gate printed passed on a corrupted tree", "enforcement_fix": "sound bound: optional deps never auto-fixable — the collector records optional_dep_names per manifest and evaluate_dep_list demotes every optional=true dep to design-action with remediation text explaining the implicit-feature export; ride-alongs: strip_starlark_comment_and_strings honors backslash escapes inside Starlark strings so an escaped quote cannot hide a following dep from the BUCK detect lane (LOW-X2), and apply_fixes_with_validator keeps the FIRST pre-image per path so a manifest edited twice rolls back to its ORIGINAL content (LOW-X3); ADR-0547 D6 adds bound (v) and now states precisely what cargo metadata --no-deps validates instead of claiming it closes any syntactic-checker gap", "status": "fixed-in-PR", "evidence": "RED->GREEN fixtures: optional_dep_without_own_features_mention_is_not_auto_fixable (unit), fix_refuses_optional_dep_whose_implicit_feature_a_sibling_requests (integration — reviewer vector reproduced: optional dep, no own-features mention, sibling requests the implicit feature; end state refusal, manifests byte-identical, verdict stays Red, gate never prints passed), backslash_escaped_quote_in_string_does_not_hide_following_dep (unit), rollback_restores_original_when_same_manifest_is_edited_twice (integration); buck2 test //cloud/cloud-ci/... green; branch agent/g11-kp-optional-dep-fastfollow", "goal": "G002"}
{"id": "FRIC-1781131000-buck-syntax-kernel", "seen_at": "2026-06-11", "status_update": "fixed-in-PR", "enforcement_fix": "oya-buck-syntax-kernel extracted under libs/ (ADR-0549): one sound std-only lexer/parser for the gate-consumed Starlark subset (comment-trivia, escape/continuation-exact strings, token-depth blocks, name-field target binding, byte-exact spans) + span-based safe-edit primitives + the write-through fixer harness (reparse + caller semantic hook + first-pre-image rollback). Both cloud-ci gate consumers migrated: oya-cloud-ci-embedded-asset-hermeticity-app dropped its call_block/field_value_expr/quoted_strings/var-scanner/glob_match private parser and its comment-bearing-block refusal guard (now edits soundly); oya-cloud-ci-kernel-purity-app dropped find_block_end/strip_starlark_comment_and_strings/raw-block token scan. Remaining private parsers (tools/oya-buck-test-wiring-app retirement-marked CLI, accounting-registry target-parity substring probe) audited + deferred in ADR-0549 D5; the non-duplication lint is the queued ratchet.", "evidence": "ADR-0549; libs/oya-buck-syntax-kernel (32 unit fixtures incl. every historical vector ported from both gates); identical-findings proof: differential probe 865/865 BUCK files parse + dep-set extraction identical to legacy on every file, hermeticity live-corpus baseline set-equality gate green with baseline untouched, gate binaries identical stdout+exit at base/head; buck2 test //cloud/cloud-ci/... + //libs/oya-buck-syntax-kernel: Pass 45 Fail 0", "goal": "G011"}
{"id": "FRIC-1781200001", "seen_at": "2026-06-11", "status_update": "fixed-in-PR", "enforcement_fix": "BUCK --fix re-enabled in oya-cloud-ci-kernel-purity-app on the oya-buck-syntax-kernel sound parser + fixer self-validation harness (ADR-0549, closing the ADR-0547 D6 round-3 refusal-only descope): a mechanically dead dep (all five D6 bounds hold) now also drops its third-party//:<dep> rust_library BUCK edge via span-exact remove_list_element guarded by guarded_rewrite (reparse + no-dep-left + no-collateral-removal semantic hook); unsound shapes (vars, select(), unterminated blocks, unmodeled elements) refuse with the file byte-identical; rollback rides the shared first-pre-image registry so a failed cargo-metadata revalidation restores Cargo.toml AND BUCK originals.", "evidence": "ADR-0549 D4/D6; unit fixtures buck_remover_removes_dead_edge_soundly_even_with_comments, buck_remover_refuses_unmodeled_deps_shapes, h6_unterminated_block_detect_fail_closed_removal_refused; integration fix_is_table_aware_and_does_not_corrupt_manifest (rust_library kube edge removed, rust_test edge survives) + fix_rolls_back_all_preimages_when_semantic_revalidation_fails (BUCK restored byte-identically); buck2 test //cloud/cloud-ci/... Pass 45 Fail 0", "goal": "G002"}
{"id": "FRIC-1781230000", "seen_at": "2026-06-11", "friction": "kernel-purity BUCK detect lane was blind to backslash-newline string continuation (#693 LOW-2): a Starlark string may continue across a backslash-newline, so \"third-party//:k\\<newline>ube\" cooks to the single value third-party//:kube, but the line-bounded stripper reset string state at the newline and the marker scan saw only the truncated token `k` — the denylisted dep `kube` was invisible to the detect lane (a dep edge hideable by formatting).", "pipeline_defect": "the pre-kernel paren-depth scanner modeled strings line-bounded; any lexical feature that joins content across lines (continuation) splits a dep token across the scanner's state reset and the substring detector under-reports — detect soundness depended on formatting, violating the born-blocking gate contract", "enforcement_fix": "closed by the shared oya-buck-syntax-kernel lexer (ADR-0549): strings are cooked with full escape state including the backslash-newline continuation, so the detect lane sees the JOINED value; RED fixture backslash_newline_continuation_does_not_hide_dep pins the vector at the gate level and backslash_newline_continuation_inside_string_cooks_to_joined_value pins it at the kernel level (asserting the truncated `k` token is NOT reported and `kube` IS)", "status": "fixed-in-PR", "evidence": "ADR-0549 D1/D4; libs/oya-buck-syntax-kernel unit backslash_newline_continuation_inside_string_cooks_to_joined_value; oya-cloud-ci-kernel-purity-app unit backslash_newline_continuation_does_not_hide_dep; buck2 test //cloud/cloud-ci/... Pass 45 Fail 0", "goal": "G002"}
{"id": "FRIC-1781220000", "seen_at": "2026-06-11", "friction": "pipeline-as-product doctrine lived only in gitignored session artifacts + memory: the founder decisions of 2026-06-09..11 (neutral ratchet engine + policy packs, R0 reusability requirement, K8s operator + CRD first surface, public-repo conformance, automation-default, soundness-defines-possible, paved-road promise) were recorded only in .omc/ultragoal/PRODUCT-pipeline-paved-road.md (gitignored), the dispatch ledger, and agent memory — none of which is a governed, citable artifact", "pipeline_defect": "binding product-direction decisions had no governed home, so gates and reviews could not cite them and future sessions could silently drift from them — the exact staleness/decay class the founder automation-maximalism doctrine names", "enforcement_fix": "ADR-0548 governed doctrine: docs/decisions/ADR-0548-pipeline-as-product-paved-road.md converts the one-pager + train evidence into Proposed doctrine (D1 engine/pack split, D2 R0, D3 operator+CRD surface, D4 conformance, D5 closed loop, D6 automation-default, D7 sound fixer bounds, D8 paved-road promise), with the honest 18/15/2 audit snapshot and Not-Doing scope carried into the governed record", "status": "fixed-in-PR", "evidence": "ADR-0548 (docs/decisions/ADR-0548-pipeline-as-product-paved-road.md); PR agent/pipeline-as-product-adr vs dev; decision-crosswalk face registers ADR-0548 after settle; buck2 test //cloud/cloud-ci/... green on the updated ledger", "goal": "G011"}
{"id": "FRIC-1781131000-buck-syntax-kernel", "seen_at": "2026-06-11", "status_update": "fixed-in-PR", "enforcement_fix": "evidence correction (review F4, event-sourced — prior row not rewritten): the kernel unit-fixture count cited in the prior closure row was stale; at the reviewed SHA 249c3c00d the oya-buck-syntax-kernel suite carried 34 unit fixtures (not 32), and the round-2 F1 escape-cooking fixtures grow it to 36. The buck2 test target owns the count; prose counts in evidence strings are point-in-time measurements.", "evidence": "buck2 test //libs/oya-buck-syntax-kernel:oya-buck-syntax-kernel-unittest -> 36 passed (round-2 head); reviewer-measured 34 at 249c3c00d; PR #695", "goal": "G011"}
{"id": "FRIC-1781240000", "seen_at": "2026-06-11", "friction": "Residual BUCK detect-lane class after the ADR-0549 F2 widening (review round 2): a macro defined in a .bzl file that wraps rust_library (e.g. `my_macro(deps = [...])` whose body forwards to rust_library) is invisible to the BUCK-file-local kernel-purity detector — the call site bears neither the rust_library name nor any opaque/load-alias trigger, and the macro body lives outside the scanned file.", "pipeline_defect": "the detect lane is file-local by design (no .bzl loading/evaluation); kwargs-splat and load-alias indirection are now widened to whole-file raw scans (NO SILENT MISS for in-file indirection), but cross-file macro indirection cannot be resolved without a load-graph evaluator", "enforcement_fix": "risk bounded: (a) the Cargo lane (KP-TRANSIENT-DEP-CARGO) independently detects the same transient dep from Cargo.toml, which buck2-built crates must also declare, so a macro-hidden BUCK edge alone cannot make a transient dep invisible to the gate; (b) the repo convention is direct rust_library/rust_binary/rust_test calls (no wrapper macros in crate BUCK files today — differential corpus 865/865 files modeled). Destination if macros ever land: extend oya-buck-syntax-kernel with load-graph awareness or trigger whole-file scans on any non-prelude rule name; revisit when a wrapper macro first appears.", "status": "logged", "goal": "G011"}
{"id": "FRIC-1781250000", "seen_at": "2026-06-11", "friction": "settle-not-final repeated 3x across PR #690 and PR #695, each caught only by the ADR-0539 freshness gate at the cost of a full CI round-trip: (1) PR #690 — leader pushed scratch-relocation commits AFTER the lane's settle commit, leaving scm-facts stale; (2) PR #695 — the lane's settle commit sat mid-stack with review-closure content commits after it; (3) PR #695 round 2 — the lane SKIPPED settle on a false local 'faces byte-identical' check, but scm-facts encodes per-path last_touch_commit so ANY new commit un-settles it and worker self-assessment provably fails", "pipeline_defect": "the settle protocol had no local mechanical verification step between 'settle committed' and 'push': staleness is commit-metadata-borne (scm-facts last_touch_commit), invisible to content-level human/agent inspection, so the first detector was the remote freshness gate — a full CI round-trip per occurrence (manual-twice doctrine: third occurrence mandates automation per ADR-0548 D6 automation-default)", "enforcement_fix": "face-settle --verify mode + protocol requirement before every push; freshness gate remains canonical backstop", "status": "fixed-in-PR", "evidence": "oya-cloud-ci-face-settle --verify (cloud/cloud-ci/gates/oya-cloud-ci-freshness-app): read-only HEAD-certifying mode reusing the gate's own check_repo_with_regenerated_faces full check (generated-face byte parity + Cargo.lock member parity, single owner); fixtures verify_passes_on_properly_settled_tree, verify_fails_after_docs_only_commit_following_settle (occurrence-3 shape), verify_fails_when_settle_commit_sits_mid_stack (occurrence-1/2 shape), verify_fails_on_stale_lock_even_when_faces_are_settled (pre-open review HIGH escape closed), verify_stays_green_after_lock_only_refresh_commit, verify_never_mutates_the_tree, verify_refuses_uncommitted_face_changes; protocol wired in docs/oya-ci/gate-catalog.md (canonical settle+verify paragraph) + .omc/ultragoal/TEAMMATE-PREAMBLE.md step (4); buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-freshness-app: green; PR agent/settle-verify-automation vs dev", "goal": "G011"}
{"id": "FRIC-1781260000", "seen_at": "2026-06-11", "friction": "layout doctrine scattered across CLAUDE.md prose, memory, and three ADRs without a single normative source: the canonical service shape (kernel/adapter/app seams), the libs/ charter, the owned-stack cutover litmus as a layout review question, and the flat-topology-vs-internal-seams reconciliation lived across ADR-0131/0132/0512, CLAUDE.md policy prose, and founder-directive agent memory (2026-06-09/10) — with ADR-0512's operative text (clean-arch layers as modules, no layer-per-crate) literally contradicting the shipped, gate-enforced kernel/adapter/app crate shape (PR #686, ADR-0543/0547/0548)", "pipeline_defect": "reviews and gates had no single citable layout authority, so doctrine was reconstructed from accumulated precedent each time and drift between the artifacts was undetectable — the staleness/decay class the founder automation-maximalism doctrine names; an un-amended ADR contradicting live enforcement is exactly the corpus-decay shape", "enforcement_fix": "ADR-0550 (docs/decisions/ADR-0550-repository-layout-doctrine.md): single normative layout source — D1 canonical service shape + seam direction matrix (amends ADR-0512 clauses 3/4 with amended_by backlink), D2 libs/ charter + rule of two, D3 cutover litmus as structural property, D4 flat-topology x internal-seams reconciliation, D5 honest enforcement audit with the gap row FRIC-1781270000 queued", "status": "fixed-in-PR", "evidence": "ADR-0550 (docs/decisions/ADR-0550-repository-layout-doctrine.md); ADR-0512 amended_by backlink + inline 2026-06-11 amendment note; PR agent/layout-doctrine-adr vs dev; decision-crosswalk face registers ADR-0550 after settle; buck2 test //cloud/cloud-ci/... green on the updated ledger", "goal": "G011"}
{"id": "FRIC-1781270000", "seen_at": "2026-06-11", "friction": "layout doctrine enforcement gap (ADR-0550 D5 audit at base d5d8be5d4): the topology/direction/colocation clauses have no merge authority — ADR-0512 R1-R7 workspace-topology, the architecture-boundaries role-edge matrix (incl. the app->app ban), and the ADR-0132 no-grouping check run only through the retirement-marked oya-dev-cli (local bridge feedback per cli_surface_policy, never merge authority), ADR-0131's per-service-layout and aggregation-index-generation gate packets were never implemented, and purity coverage is glob-bounded to *-kernel/*-core (kernel-class -domain crates unscanned); live proof the CLI tier does not hold: cloud/cloud-ci/gates/<crate> and oya/office/oya-* both ship today despite failing workspace-topology R6 as written", "pipeline_defect": "merge authority enforces only the naming/coverage/parity/kernel-purity subset (cargo-prefix ADR-0017, bnf-layer-suffix, workspace-glob-coverage ADR-0538, target-parity ADR-0540, kernel-purity ADR-0547); container shape, seam direction beyond the kernel half, no-grouping, and per-service colocation hold only by review — the enforcement-layering audit question (does the rule hold with hooks disabled?) answers no", "enforcement_fix": "pack-shaped cloud-ci service-layout gate family per ADR-0548 (policy as data: roots, container name, seam direction matrix, no-grouping patterns; detector + --fix where mechanically derivable; born-blocking with a reviewed shrink-only baseline carrying exactly the two live variances, whose migrate-vs-sanction disposition is decided in that gate-family review) + ADR-0547 policy extension of kernel globs to the remaining kernel-class suffixes (-domain first)", "status": "queued", "goal": "G011"}
{"id":"FRIC-1781112000","seen_at":"2026-06-11","status_update":"fixed-in-PR","enforcement_fix":"merge-base frozen baseline ratchet (ADR-0551): the cloud-ci-firewall frozen reference is the gate-baseline face at `git merge-base <base_ref> HEAD` (base_ref policy-as-data in cloud/cloud-ci/gates/oya-cloud-ci-firewall-app/ratchet-policy.json, default origin/dev), materialized out-of-graph by the scm-facts emitter --merge-base-baseline into the untracked gitignored gate-baseline.merge-base.generated.json snapshot; the firewall gate FAILS CLOSED on a missing/invalid snapshot; the sign-off door spans both predicates for one regen; ratchet growth is mode-aware with frozen-mode-wins (advisory dashboards grow freely; a same-PR disposition flip cannot disarm a code that was blocking at the merge-base)","evidence":"RED repro at d5d8be5d4: a synthetic born-unjustified file + the mandated settle regen left all 4 firewall tests GREEN while the key was absent at origin/dev (the PR #670 shape, structurally invisible because committed==proposed in every settled PR); fixed: live pin firewall_blocks_same_pr_baseline_regen_laundering keeps the hole as an executable FOIL (PR-local reference green on laundering) and proves the frozen merge-base reference RED on BOTH predicates (compare regression + ratchet growth); fixtures tc-FW-bad-same-pr-regen-launders-new-debt, tc-FW-good-merge-base-shields-concurrent-merge (concurrent-merge race shape pinned), tc-FW-good-signed-off-key-in-current, tc-FW-good-advisory-growth-allowed, tc-FW-bad-mode-flip-does-not-disarm; frozen_snapshot_provenance_matches_ratchet_policy audits the compared frozen point; buck2 test //cloud/cloud-ci/... Pass 44 Fail 0","goal":"G011"}
{"id": "FRIC-1781300000", "seen_at": "2026-06-12", "friction": "parallel lanes #644/#663 produced an evidence-artifact collision and a stranded capability subset: both lanes executed the same implementation plan under the same emission ID cloud-intelligence-xproxy-20260610 and the same evidence path with divergent content, and after #644 merged, #663's still-valuable agent-runtime/schedule/parity-canary status surfaces (~2,275 lines across contracts, kernel, REST, worker, tests) were stranded on a stale merge-base with retired crate names and 15-file conflicts", "enforcement_fix": "single-concern salvage PR re-deriving the unmerged status-surface subset from current dev under a fresh evidence emission ID (cloud-intelligence-canary-status-salvage-20260612); lane protocol already requires unique emission IDs per lane, so the collision class is closed by minting per-lane IDs instead of sharing the plan's ID", "status": "fixed-in-PR", "evidence": "PR agent/ci-canary-status-surfaces vs dev (salvage of #663 subset, citing the #663 disposition assessment and #644); evidence/multispectrum/cloud-intelligence-canary-status-salvage-20260612-1781239694.json + audit-chain row under the fresh change_id; RED contract-test run against dev contracts then buck2 test //cloud/cloud-ci/... //cloud/cloud-intelligence/... Pass 66 Fail 0", "goal": "G05"}
