PREMISE GATE — lane g11-embedded-asset (ADR-0545, gate-id cloud-ci-embedded-asset-hermeticity)

(1) LOAD-BEARING CLAIM: FRIC-1781131000 is a real, in-ledger embedded-asset hermeticity defect
    (cedar-adapter include_str! resolved to a sandbox location the BUCK mapped_srcs did not provide),
    now fixed on origin/dev, and that whole bug class is statically detectable from the Rust include
    string literal + the sibling BUCK srcs/mapped_srcs destinations — making a born-blocking,
    repo-agnostic, pack-shaped cloud-ci gate possible.

(2) VERIFIED AGAINST CURRENT BASE (origin/dev @ beec377a6):
    - FRIC-1781131000 present in .omc/ultragoal/friction-ledger.jsonl (grep -c = 1), status "open".
    - cedar-adapter is now hermetic on base: src/lib.rs line 36 include_str!("../policy/cloud-intelligence.cedar");
      BUCK crate_root = ADAPTER_ROOT/src/lib.rs, so the include resolves (relative to the including
      file) to ADAPTER_ROOT/policy/cloud-intelligence.cedar; BUCK line 8 maps
      "//cloud/cloud-intelligence/policy:cloud-intelligence.cedar" -> ADAPTER_ROOT/policy/cloud-intelligence.cedar,
      i.e. the mapped_srcs DESTINATION equals the include-relative sandbox path. The on-disk repo file
      lives at cloud/cloud-intelligence/policy/cloud-intelligence.cedar (NOT under the crate) — proving
      the gate must assert against mapped_srcs DESTINATIONS, not the repo filesystem layout.
    - ADR-0545 free (no docs/decisions/ADR-0545*), gate dir free
      (cloud/cloud-ci/gates/oya-cloud-ci-embedded-asset-hermeticity-app absent) — allocation uncontested.
    - friction-accounting gate (ADR-0544, PR #687) is merged and live on base — the pattern to mirror
      and the gate that will re-check my dogfood ledger edit.

(3) ONE FACT THAT WOULD INVALIDATE THE LANE: if include_str!/include_bytes! sandbox-path resolution
    were NOT statically determinable from (crate_root location + the literal + the target's effective
    srcs/mapped_srcs destinations) — e.g. if the destinations were computed by an opaque macro the
    static parser cannot evaluate for the real targets — the gate could not fail closed without a full
    buck2 evaluation, collapsing it into the build it is meant to pre-empt. Mitigation: the gate is a
    CONSERVATIVE static checker — it flags only literals it CAN resolve whose BUCK destinations it CAN
    parse; the cedar-adapter and the repo's ~220 include sites are simple string-literal + glob /
    dict-comprehension mapped_srcs the minimal parser handles; non-parseable BUCK is reported as a
    skip, never a silent pass.

RESULT: PASS — premise verified against origin/dev @ beec377a6.
