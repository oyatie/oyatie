apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - coraza-waf-config.yaml
  - rate-limit-config.yaml
  - geo-asn-block.yaml
  - ddos-xdp-policy.yaml

# Authority: ADR-0191 edge-authz tier boundaries.
# This component is included by every pack's identity overlay; per-pack
# overlay values (values-pack-<pack>.yaml) tighten or loosen rules.
