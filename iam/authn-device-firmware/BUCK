# TODO(ADR-0392): single rust_library target — Phase-2 wiring needed for nRF52840 cross-compile

load("//governance/corpus/extract:yaml_facts.bzl", "corpus_yaml_facts_shards")

corpus_yaml_facts_shards(
    srcs = glob(["**/*.yaml", "**/*.yml"]),
    shard_size = 256,
)
