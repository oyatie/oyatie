[package]
name = "iam-policy-cedar-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Transitional re-export of policy-cedar-domain, which now lives at policy/core/cedar-domain. Carries no behavior of its own. Each consumer drops this indirection and depends on the policy crate directly the next time it is opened for its own reasons; this crate is deleted when the last consumer has."

[dependencies]
policy-cedar-domain = { path = "../../../policy/core/cedar-domain" }

[lib]
doctest = false

[lints]
workspace = true
