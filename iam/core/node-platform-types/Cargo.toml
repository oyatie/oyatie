[package]
name = "os-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Foundational no_std crate for the operating-system Talos migration: shared errors, primitives, and traits."

[lib]
name = "os_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]


[lints]
workspace = true
