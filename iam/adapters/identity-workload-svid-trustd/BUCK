_DEPS = [
    "//iam/core/identity-workload-svid-kernel:iam-identity-workload-svid-kernel",
    "//iam/core/node-trust-domain:os-trustd-domain",
    # Real leaf-DER parsing + signature verification (G002 slice-1b-i; ADR-0561).
    # aws-lc-rs backend, NO ring (verify-aws only).
    "third-party//:x509-parser",
]

rust_library(
    name = "iam-identity-workload-svid-trustd",
    srcs = glob(["src/**/*.rs", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.txt"]),
    crate = "iam_identity_workload_svid_trustd",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = _DEPS,
)

rust_test(
    name = "iam-identity-workload-svid-trustd-unittest",
    srcs = glob(["src/**/*.rs", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.txt"]),
    crate = "iam_identity_workload_svid_trustd",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = _DEPS,
)
