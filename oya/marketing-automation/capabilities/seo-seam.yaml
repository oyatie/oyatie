apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: marketing-automation-seo-seam
  service: marketing-automation
spec:
  businessCapability: marketing-seo
  capability: seo-seam
  category: customer-engagement-substrate-seam
  big8Phase: "4A.5"
  primaryCounterpart: HubSpot SEO Recommendations
  flankingCounterparts:
    - Adobe Marketo Engage SEO Tools
    - "(Mailchimp limited SEO)"
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0263, ADR-0321, ADR-0328]
  boundedContext: seo-seam
  aggregateRoot: "(delegation only; sites owns aggregates)"
  invariants:
    - delegation to sites µservice for SEO recommendations + keyword tracking
    - landing-page SEO metadata composed here, validated by sites SEO seam
  commands: [request_seo_recommendation, ingest_seo_metric]
  events: [seo_recommendation_received, seo_metric_ingested]
  tenantScope:
    requiredFields: [tenant_id, principal_id, tenant_class, purpose, data_class]
  cedarGates:
    - principal: marketing.ops
      action: marketingAutomation::RequestSeoRecommendation
  observability:
    auditEvents:
      - EVT-MARKETING-SEO-RECOMMENDATION-RECEIVED
  crossMicroserviceHandoffs:
    - sites (SEO recommendations + keyword tracking)
    - audit-chain (sealing)
  implementationPlanRef: IP-049-seo-seam.md
  openQuestionResolution: "Q-019 settled — SEO recommendations delegated to sites µservice"
