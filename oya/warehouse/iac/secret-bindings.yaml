apiVersion: oyatie.dev/v1
kind: SecretBinding
metadata:
  name: oya-warehouse
spec:
  openbaoPaths:
    - secret/{{ tenant_id }}/warehouse/database
    - secret/{{ tenant_id }}/warehouse/signing
  maxTtlSeconds: 60
  auditEventClass: EVT-WAREHOUSE-INBOUND_DELIVERY-SECRET_LEASE
