# Preview planning inventory only. Not measured SLO, production readiness,
# runtime audit-chain, or cloud deployment evidence for treasury.
apiVersion: openslo/v1
kind: SLO
metadata:
  name: treasury-availability
  labels:
    service: treasury
    sap_code: "TRM"
    authority_scope: preview_planning_non_measured
  annotations:
    authority_ref: specs/microservices/treasury.json
    non_claim: "planning inventory only; no measured SLO, runtime service, cloud deployment, or production readiness"
spec:
  description: "Planning-only treasury-availability objective; benchmark inventory only, with no measured SLO, runtime service, cloud deployment, GA, or production readiness claim."
  service: treasury
  indicator:
    metadata:
      name: treasury-availability-sli
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: "sum(rate(http_requests_total{service=\"treasury\",status=~\"2..\"}[5m]))"
        total:
          metricSource:
            type: prometheus
            spec:
              query: "sum(rate(http_requests_total{service=\"treasury\"}[5m]))"
  objectives:
    - displayName: treasury-availability
      target: 0.999
