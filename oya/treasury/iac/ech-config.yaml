# Preview planning inventory only; not a live ECH runtime configuration.
apiVersion: oyatie.dev/v1
kind: EchConfig
metadata:
  name: oya-treasury-ech
  annotations:
    oyatie.com/authority-scope: preview-planning-non-authoritative
spec:
  rotationCadenceDays: 90
  httpsRecord: true
  gracefulFallback: true
  auditEventClass: PLANNING-ONLY-NO-RUNTIME-AUDIT-CHAIN-EMISSION
