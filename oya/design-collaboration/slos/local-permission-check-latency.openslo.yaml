apiVersion: openslo/v1
kind: SLO
metadata:
  name: design-collaboration-permission-check-latency
  labels:
    service: design-collaboration
    suite: local-operational-primitive
    adr: ADR-0130
    promotion_gate: ADR-0139
spec:
  service: design-collaboration
  description: Permission checks complete before file access for design file loading, version saving, comment synchronization, asset preview, permission checks, and handoff export operations.
  indicator:
    metadata:
      name: permission-check-latency
      labels:
        domain_object: DesignFile
        operator_audience: DESIGN_WORKSPACE_ADMIN
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(oya_design_collaboration_permission_check_latency_good_total[5m])) by (tenant_id, cell_tier)
        total:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(oya_design_collaboration_permission_check_latency_total[5m])) by (tenant_id, cell_tier)
  objectives:
    - displayName: Permission checks complete before file access
      target: 0.999
  timeWindow:
    - duration: 30d
      isRolling: true
