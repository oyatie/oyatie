apiVersion: v2
name: oya-intelligence-providers-postgres
description: |
  Postgres HA primary+replica for foundry-providers µservice provider-config
  persistence. Per-pack deployment per ADR-0117 residency posture.
type: application
version: 0.1.0
appVersion: "16.4"  # LTS line; verify-at-deploy
maintainers:
  - name: ops-sre-reliability
    email: ops-sre@oyatie.com
dependencies:
  - name: postgresql
    version: "^15.5.0"
    repository: https://charts.bitnami.com/bitnami
keywords: [foundry-providers, postgres]
annotations:
  oya.adr/references: "ADR-0117, ADR-0131"
  oya.lts-pin/postgres: "16.4 (LTS through 2028-11)"
