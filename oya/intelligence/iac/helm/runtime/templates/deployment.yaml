# Canonical-base: microservices/governance/iac/helm/_oya-helpers — helpers via {{ include "oya.X" $ }} (SWEEP-J Slice 2 per ADR-0064)
{{/*
Per-foundry CIS-compliant Deployment template (Sweep-E in-pass fix).

Substrate µservices share this baseline: non-root, read-only rootfs,
all capabilities dropped, no privilege escalation, seccompProfile RuntimeDefault,
explicit resource requests/limits, OpenBao SecretReference URIs (no literal secrets).

Per ADR-0131 per-µservice flat layout + ADR-0123 secret-reference URI policy.
*/}}
{{- $cfg := $.Values.runtime | default $.Values -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oya-intelligence-runtime
  namespace: foundry
  labels:
    microservice: foundry
    app.kubernetes.io/name: oya-intelligence-runtime
    app.kubernetes.io/version: {{ $.Chart.AppVersion | default "0.1.0" }}
spec:
  replicas: {{ $cfg.replicaCount | default 2 }}
  selector:
    matchLabels:
      microservice: foundry
      app.kubernetes.io/name: oya-intelligence-runtime
  template:
    metadata:
      labels:
        microservice: foundry
        app.kubernetes.io/name: oya-intelligence-runtime
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
    spec:
      serviceAccountName: oya-foundry
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: "{{ $cfg.image.repository | default "registry.oyatie.dev/oya-intelligence-runtime" }}:{{ $cfg.image.tag | default "0.1.0" }}"
          imagePullPolicy: IfNotPresent
          ports:
            - {name: app,     containerPort: 9000, protocol: TCP}
            - {name: metrics, containerPort: 9090, protocol: TCP}
          env:
            - {name: OYA_MICROSERVICE, value: foundry}
            - {name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "http://oya-observability-alloy.observability.svc.cluster.local:4317"}
            - {name: RUST_LOG, value: info}
          resources:
            {{- include "oya.resourceRequests" (dict "tier" ($cfg.resourceTier | default "M")) | nindent 12 }}
          readinessProbe:
            {{- include "oya.probes.healthzReadiness" $ | nindent 12 }}
          livenessProbe:
            {{- include "oya.probes.healthzLiveness" $ | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 64Mi
