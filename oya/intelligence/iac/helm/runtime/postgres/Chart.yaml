apiVersion: v2
name: oya-intelligence-runtime-postgres
description: Postgres 16 LTS primary + read-replica for session cold restore, capability mirror, invocation lifecycle + audit per policy/runtime-isolation.md + capacity-model.md
type: application
version: 0.1.0
appVersion: "16.4"   # Postgres LTS pin per docs/standards
maintainers:
  - name: axis-foundry-runtime
    email: axis-foundry-runtime@oyatie.com
dependencies:
  - name: postgresql
    version: "^15.0.0"  # bitnami/postgresql chart version pin
    repository: https://charts.bitnami.com/bitnami
