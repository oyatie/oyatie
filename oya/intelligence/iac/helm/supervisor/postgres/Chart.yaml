apiVersion: v2
name: oya-intelligence-supervisor-postgres
description: HA PostgreSQL (Patroni-managed primary + replica per pack region) for foundry-supervisor fleet-state, deployment history, autonomy entitlement store. Per policy/supervisor-isolation.md TI-P-*.
type: application
version: 0.1.0
appVersion: "16.2"  # Postgres 16 LTS line
maintainers:
  - name: ops-sre-reliability
    email: ops-sre-reliability@oyatie.com
dependencies:
  - name: postgresql-ha
    version: "^14.0.0"
    repository: https://charts.bitnami.com/bitnami
