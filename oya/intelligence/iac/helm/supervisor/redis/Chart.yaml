apiVersion: v2
name: oya-intelligence-supervisor-redis
description: Valkey Cluster (3 shards × 2 replicas per pack region) for kill-switch state cache + supervision-event-bus Valkey Streams (Redis wire-compat). Per policy/supervisor-isolation.md TI-R-*.
type: application
version: 0.1.0
appVersion: "7.2"  # Valkey 8.1 (Redis wire-compat)
maintainers:
  - name: ops-sre-reliability
    email: ops-sre-reliability@oyatie.com
dependencies:
  - name: redis-cluster
    version: "^10.0.0"
    repository: https://charts.bitnami.com/bitnami
