# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-object-type-registry-kernel
  microservice: ontology
  bounded_context: object-type-registry
  layer: kernel
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = object-type-registry; layer = kernel.
    Port-trait + sealed-trait + entity types; zero I/O; data_class annotations
    per Bominal ADR-0028 + oya-check-data-class lane.
  ports: [SchemaRegistry, PillarResolver]
  entities: [ObjectTypeSchema, PropertyDescriptor, PropertyTier, PillarKind, JurisdictionOverlay, SchemaRevision]
  data_classes: [INTERNAL_ONLY]
  test_coverage_threshold: {line: 90, branch: 80}
  related_ip: IP-002
  related_adrs: [ADR-0006, ADR-0056, ADR-0105, ADR-0106, ADR-0122, ADR-0131, ADR-0132]
