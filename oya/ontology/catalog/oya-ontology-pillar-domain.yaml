# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-pillar-domain
  microservice: ontology
  bounded_context: pillar
  layer: domain
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = pillar; layer = domain.
    Pure pillar-enforcement logic: PillarContext, PillarKind, CrossPillarGrant
    expiry + 2-person-rule check. Pure logic; no adapter layer needed (Cedar
    enforces via policy fragment).
  imports: [oya-ontology-pillar-kernel]
  test_coverage_threshold: {line: 95, branch: 90}
  related_ip: IP-013
  related_adrs: [ADR-0132, ADR-0140 (retired per ADR-0145)]
