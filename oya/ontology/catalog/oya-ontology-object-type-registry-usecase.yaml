# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-object-type-registry-usecase
  microservice: ontology
  bounded_context: object-type-registry
  layer: usecase
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = object-type-registry; layer = usecase
    (per ADR-0106; replaces legacy 'application'). Orchestrator reading
    schema-registry adapter + writing to registry store + emitting
    ObjectTypeRegistered events. Imports domain + kernel.
  imports: [oya-ontology-object-type-registry-domain, oya-ontology-object-type-registry-kernel]
  test_coverage_threshold: {line: 90, branch: 80}
  related_ip: IP-002
