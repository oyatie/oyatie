# NEUTRALIZED for the Buck2 cutover: placeholder microservice with no .rs source yet
# (implementation pending). The rust targets below are commented out so `buck2 build //...`
# is green. When the crate implementations land they bring their own Cargo.toml and a
# hand-written per-package BUCK using glob() — the buck2-native mechanism. There is no
# first-party BUCK generator: this directory holds no Cargo.toml, so no cargo-metadata-driven
# generator could ever have served it.
#
# microservices/oya-flags/BUCK
# Placeholder Buck2 targets for oya-flags (ADR-0481).
# Full target definitions ship when Buck2 CI integration lands (ADR-0408).

# rust_library(
#     name = "oya-flags-kernel",
#     srcs = glob(["crates/oya-flags-kernel/src/**/*.rs"]),
#     visibility = ["PUBLIC"],
# )

# rust_library(
#     name = "oya-flags-rest",
#     srcs = glob(["crates/oya-flags-rest/src/**/*.rs"]),
#     deps = [":oya-flags-kernel"],
#     visibility = ["PUBLIC"],
# )

# rust_binary(
#     name = "oya-flags",
#     srcs = glob(["crates/oya-flags-app/src/**/*.rs"]),
#     deps = [":oya-flags-kernel", ":oya-flags-rest"],
#     visibility = ["PUBLIC"],
# )
