openapi: 3.2.0
info:
  title: Marketplace API
  version: 1.0.0
  summary: Tenant-scoped DealSet API for universal deal-settlement substrate
  x-related-adrs:
    - ADR-0105
    - ADR-0131
    - ADR-0243
    - ADR-0244
    - ADR-0263
    - ADR-0249
    - ADR-0314
  x-naming-justifications:
    bnf_v4: "service.action.resource grammar uses marketplace.DealSet.<action>"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
servers:
  - url: https://api.oyatie.example/marketplace/v1
    description: Production tenant-scoped endpoint
paths:
  /marketplace/deal-sets:
    post:
      operationId: marketplace__marketplace_deal_sets_post
      summary: create DealSet envelopes
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
  /marketplace/deal-sets/{deal_set_id}/accept:
    post:
      operationId: marketplace__marketplace_deal_sets_deal_set_id_accept_post
      summary: accept priced commercial terms
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
  /marketplace/deal-sets/{deal_set_id}/settle:
    post:
      operationId: marketplace__marketplace_deal_sets_deal_set_id_settle_post
      summary: authorize settlement transition
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
  /marketplace/listings:
    post:
      operationId: marketplace__marketplace_listings_post
      summary: publish seller listings
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
  /marketplace/escrow/holds:
    post:
      operationId: marketplace__marketplace_escrow_holds_post
      summary: reserve escrow with payments
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
  /marketplace/disputes:
    post:
      operationId: marketplace__marketplace_disputes_post
      summary: open mediation case
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
  /marketplace/revenue-shares:
    post:
      operationId: marketplace__marketplace_revenue_shares_post
      summary: bind developer or partner share
      tags: [marketplace]
      x-cedar-action: marketplace.DealSet.mutate
      x-audit-event: MarketplaceDealOffered
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DealSetCommand'
      responses:
        '202':
          description: Accepted with audit-chain evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DealSetReceipt'
        '403':
          description: Cedar denied the tenant-scoped action
        '409':
          description: Idempotency conflict or state conflict
components:
  x-oyatie-sharding-automation-event-surface:
    microservice: marketplace
    wave: 15-ZF-13
    artifact_type: contracts
    scope: OpenAPI components metadata for the sharding_automation event surface.
    wire_shape: Extension-only annotation; no paths, operations, schemas, requestBodies, responses, headers, securitySchemes, or examples are added.
    doctrine_refs:
      ADR-0346:
        purpose: >-
          Establish that ./bin/oya verify --ci-required is the canonical local pre-push verifier and MUST locally mirror the full CI matrix and MUST block on exit-0 of EACH step before returning success to the caller.
        enforced_by:
          - oya-governance-oya-verify-ci-mirror-coverage
          - oya-governance-oya-verify-ci-step-exit-semantics
          - oya-governance-oya-verify-skip-flag-allowlist
          - oya-governance-oya-submit-calls-verify
          - oya-governance-oya-verify-exit-code-contract
      ADR-0347:
        purpose: >-
          Declare that every oya-governance-* CI lane prefix in the Oyatie corpus RENAMES to oya-governance-* in a single bulk-rename pull request.
        enforced_by:
          - oya-governance-no-foundry-fitness-residue
          - oya-governance-lane-prefix-vocabulary
          - oya-governance-rename-inventory-presence
      ADR-0348:
        purpose: >-
          Declare that cellular topology MUST support three control-plane-driven automation modes: AUTOSHARDING, AUTO-REBALANCE, and DYNAMIC SHARDING.
        enforced_by:
          - oya-governance-sharding-automation-coverage
          - oya-governance-autosharding-manual-mode-refusal
          - oya-governance-auto-rebalance-residency-honored
          - oya-governance-dynamic-sharding-threshold-coverage
          - oya-governance-audit-chain-emit-on-automation-events
          - oya-governance-tenant-migration-reversibility
        event_surface:
          autosharding: tenant_to_cell_shard_placement is computed by the control plane automatically.
          auto_rebalance: tenant migration honors residency and compliance pack constraints and is observable, reversible, and audit-chain-emit per ADR-0263.
          dynamic_sharding: HOT-SPLIT and COLD-MERGE shard-count operations are atomic and audit-emit.
          manifest_block: sharding_automation declares autosharding, auto_rebalance, and dynamic_sharding sub-blocks.
      ADR-0349:
        purpose: >-
          Declare Jenkins (LTS) and ArgoCD as the two canonical self-hostable CI/CD substrates for the Oyatie corpus.
        enforced_by:
          - oya-governance-jenkins-github-actions-parity
          - oya-governance-argocd-application-cosign-verified
          - oya-governance-argocd-tenant-namespace-isolation
          - oya-governance-jenkins-jcasc-only
          - oya-governance-deploy-audit-chain-emit
    consumer_guidance:
      generators: Ignore this x-extension for SDK and server wire generation.
      governance: Use this x-extension as doctrine metadata for sharding_automation event-surface review only.
  schemas:
    DealSetCommand:
      type: object
      required: [tenant_id, sub_scope_path, principal_id, idempotency_key, action, resource]
      properties:
        tenant_id:
          type: string
          pattern: '^tenant_[a-z0-9_]+$'
          examples: [tenant_acme]
        sub_scope_path:
          type: string
          examples: [workplace.hr.offers]
        principal_id:
          type: string
          examples: [principal_01h]
        idempotency_key:
          type: string
          examples: [idem_01h_marketplace]
        action:
          type: string
          examples: [marketplace.DealSet.mutate]
        resource:
          type: object
          additionalProperties: true
    DealSetReceipt:
      type: object
      required: [accepted, audit_chain_ref, state]
      properties:
        accepted:
          type: boolean
        audit_chain_ref:
          type: string
        state:
          type: string
          enum: [accepted, denied, deferred, replay_required, compensated]
