apiVersion: oyatie.dev/v1
kind: Kustomization
metadata:
  name: marketplace-kustomization
  labels:
    service: marketplace
    adr: ADR-0314
spec:
  tenantScope: required
  openBaoPath: secret/marketplace/kustomization
  naming_justifications:
    bnf_v4: "marketplace.iac.kustomization"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
