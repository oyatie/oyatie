apiVersion: oyatie.dev/v1
kind: SecretBindings
metadata:
  name: marketplace-secret-bindings
  labels:
    service: marketplace
    adr: ADR-0314
spec:
  tenantScope: required
  openBaoPath: secret/marketplace/secret-bindings
  naming_justifications:
    bnf_v4: "marketplace.iac.secret-bindings"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
