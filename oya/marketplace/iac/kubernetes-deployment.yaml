apiVersion: oyatie.dev/v1
kind: KubernetesDeployment
metadata:
  name: marketplace-kubernetes-deployment
  labels:
    service: marketplace
    adr: ADR-0314
spec:
  tenantScope: required
  openBaoPath: secret/marketplace/kubernetes-deployment
  naming_justifications:
    bnf_v4: "marketplace.iac.kubernetes-deployment"
    layer_enum: "13-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, graphql, worker, cli, sdk, api"
