apiVersion: oyatie.dev/v1
kind: NetworkPolicy
metadata:
  name: marketplace-network-policy
  labels:
    service: marketplace
    adr: ADR-0314
spec:
  tenantScope: required
  openBaoPath: secret/marketplace/network-policy
  naming_justifications:
    bnf_v4: "marketplace.iac.network-policy"
    layer_enum: "13-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, graphql, worker, cli, sdk, api"
