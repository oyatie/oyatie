apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-executive-rollup
  service: performance-management
spec:
  businessCapability: analytics-reporting
  capability: executive-rollup
  boundedContext: analytics-reporting
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328]
  benchmarks: [Lattice, Workday Performance]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    Executive view rollup across orgs: rating distribution, engagement, calibration outcomes,
    succession bench depth.
  prdRef: FR-032
