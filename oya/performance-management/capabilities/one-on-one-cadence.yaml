apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-one-on-one-cadence
  service: performance-management
spec:
  businessCapability: one-on-one-cadence
  capability: one-on-one-cadence
  boundedContext: one-on-one-cadence
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0314, ADR-0316, ADR-0321, ADR-0328, ADR-0331]
  benchmarks: [Lattice, 15Five, Workday Performance]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  marketplaceSettlement: DealSet
  policyEvaluationMode: caller_side_library_first
  ontologyReadMode: library_first
  description: |
    Manager-direct-report 1:1 cadence with persistent agenda, action items, and history.
    Owns the durable concern of 1:1 facilitation as a recurring meeting series.
  prdRef: FR-022
