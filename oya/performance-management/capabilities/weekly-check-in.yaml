apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-weekly-check-in
  service: performance-management
spec:
  businessCapability: weekly-check-in
  capability: weekly-check-in
  boundedContext: weekly-check-in
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0314, ADR-0321, ADR-0328, ADR-0331]
  benchmarks: [15Five, Lattice]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    Friday-AM weekly check-in: priorities, blockers, mood. Manager rollup published Mon AM.
    Sentiment keyword extraction runs locally to preserve anonymity guarantees.
  prdRef: FR-023
