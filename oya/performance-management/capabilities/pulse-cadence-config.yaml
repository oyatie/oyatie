apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-pulse-cadence-config
  service: performance-management
spec:
  businessCapability: engagement-survey
  capability: pulse-cadence-config
  boundedContext: engagement-survey
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328, ADR-0331]
  benchmarks: [Lattice, 15Five]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    Tenant-configurable pulse survey cadence: weekly, biweekly, monthly, quarterly.
    Pack overlays may override cadence.
  prdRef: FR-015
