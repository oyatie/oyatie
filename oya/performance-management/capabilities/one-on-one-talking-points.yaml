apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-one-on-one-talking-points
  service: performance-management
spec:
  businessCapability: one-on-one-cadence
  capability: one-on-one-talking-points
  boundedContext: one-on-one-cadence
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328]
  benchmarks: [Lattice]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    LLM-assisted talking-point suggestions for 1:1 agenda, drawing from recent feedback,
    goal progress, and recognition. Manager must explicitly accept or edit suggestions.
  prdRef: FR-024
