apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: oya-quality-management-default-deny
spec:
  podSelector:
    matchLabels:
      app: oya-quality-management
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              oya-ingress: "true"
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              oya-substrate: "true"
