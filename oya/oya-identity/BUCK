# NEUTRALIZED for the Buck2 cutover: placeholder microservice with no .rs source yet
# (implementation pending). The rust targets below are commented out so `buck2 build //...`
# is green; the Rust generator (scripts/ci/generate-first-party-buck.rs) emits real targets from cargo
# metadata once the crate implementations land.
#
# Buck2 build targets for oya-identity.
# Single-crate hyperscaler pattern per ADR-0509.
# TODO(ADR-0392): single rust_binary target

# rust_binary(
#     name = "oya-identity",
#     srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.graphql", "**/*.html", "**/*.css", "**/*.txt"]),
#     visibility = ["PUBLIC"],
# )

# alias(
#     name = "check",
#     actual = ":oya-identity",
# )

# alias(
#     name = "test",
#     actual = ":oya-identity",
# )

# alias(
#     name = "image",
#     actual = ":oya-identity",
# )
