apiVersion: oyatie.dev/v1
kind: KubernetesService
metadata:
  name: workplace-integration-kubernetes-service
  labels:
    service: workplace-integration
    adr: ADR-0320
spec:
  tenantScope: required
  openBaoPath: secret/workplace-integration/kubernetes-service
  naming_justifications:
    bnf_v4: "workplace-integration.iac.kubernetes-service"
    layer_enum: "13-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, graphql, worker, cli, sdk, api"
