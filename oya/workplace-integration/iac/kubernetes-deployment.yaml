apiVersion: oyatie.dev/v1
kind: KubernetesDeployment
metadata:
  name: workplace-integration-kubernetes-deployment
  labels:
    service: workplace-integration
    adr: ADR-0320
spec:
  tenantScope: required
  openBaoPath: secret/workplace-integration/kubernetes-deployment
  naming_justifications:
    bnf_v4: "workplace-integration.iac.kubernetes-deployment"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
