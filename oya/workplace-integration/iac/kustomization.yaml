apiVersion: oyatie.dev/v1
kind: Kustomization
metadata:
  name: workplace-integration-kustomization
  labels:
    service: workplace-integration
    adr: ADR-0320
spec:
  tenantScope: required
  openBaoPath: secret/workplace-integration/kustomization
  naming_justifications:
    bnf_v4: "workplace-integration.iac.kustomization"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
