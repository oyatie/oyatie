[package]
name = "audit-emission-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure-domain envelope validation: construction, canonical preimage
# encoding, fingerprint verification, and period bucketing. See the
# crate-level doc comment in src/lib.rs for the rules this crate enforces.

[dependencies]
audit-emission-kernel = { path = "../../ports/emission-kernel" }

[lib]
name = "audit_emission_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
