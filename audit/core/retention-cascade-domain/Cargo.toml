[package]
name = "audit-retention-cascade-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure retention-window, DSR-cascade and redaction-token rules for
# audit-chain. Rule authority: audit/policy/retention-matrix.yaml.

[dependencies]
audit-retention-cascade-api = { path = "../../ports/retention-cascade-api" }

[lib]
name = "audit_retention_cascade_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
