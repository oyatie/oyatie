[package]
name = "audit-retention-cascade-domain"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Wave 15-IMPL-truth-up scaffold (2026-05-21):
# Retention rules per IP-013.

[dependencies]
audit-retention-cascade-api = { path = "../../ports/retention-cascade-api" }

[lib]
name = "audit_retention_cascade_domain"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
