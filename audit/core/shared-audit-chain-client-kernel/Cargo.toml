[package]
name = "shared-audit-chain-client-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Skeleton authored 2026-05-18 per ADR-0145 Invariant 1.
# Each µservice integrates this trait to emit an audit-chain seal at the
# CALLING site of any state-changing inter-µservice call. Full impl tracked
# under registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-audit-client-impl.

[dependencies]

[lib]
name = "shared_audit_chain_client_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
