# Canonical-base: microservices/governance/iac/helm/_oyatie-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: audit-chain-hsm-operator
description: OCI Cloud-HSM operator + per-pack partition handle + PKCS#11 + KMIP client config for sealing-worker. Per Bominal ADR-0028 + policy/seal-integrity.md SI-06..SI-12.
type: application
version: 0.1.0
appVersion: "1.0.0"
maintainers:
  - name: cloud-secrets + axis-audit-chain
    email: cloud-secrets@oyatie.com
