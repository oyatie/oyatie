[package]
name = "audit-sealing-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Port-only crate: pure types and traits, no I/O.
# Rules live in audit/core/sealing-domain.

[dependencies]

[lib]
name = "audit_sealing_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
