[package]
name = "audit-sealing-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Wave 15-IMPL-truth-up scaffold (2026-05-21):
# Port-only crate per IP-006. Pure types/traits; no I/O.

[dependencies]

[lib]
name = "audit_sealing_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
