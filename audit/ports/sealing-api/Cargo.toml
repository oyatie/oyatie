[package]
name = "audit-sealing-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Command/result DTOs for the sealing worker cycle.
# Rules live in audit/core/sealing-domain.

[dependencies]
audit-sealing-kernel = { path = "../sealing-kernel" }

[lib]
name = "audit_sealing_api"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
