[package]
name = "audit-retention-cascade-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Port-only crate: pure traits, no I/O.
# Rules live in audit/core/retention-cascade-domain.

[dependencies]

[lib]
name = "audit_retention_cascade_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
