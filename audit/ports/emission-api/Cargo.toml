[package]
name = "audit-emission-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Wave 15-IMPL-truth-up scaffold (2026-05-21):
# DTO-only crate per IP-005. Implementation in IP-005.

[dependencies]
audit-emission-kernel = { path = "../emission-kernel" }

[lib]
name = "audit_emission_api"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
