[package]
name = "audit-emission-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# DTO-only crate for the audit-event-emit surface. No logic: the rules live in
# audit/core/emission-domain, the wire schema in contracts/openapi/audit-chain.yaml.

[dependencies]
audit-emission-kernel = { path = "../emission-kernel" }

[lib]
name = "audit_emission_api"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
