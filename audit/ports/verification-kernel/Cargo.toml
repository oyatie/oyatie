[package]
name = "audit-verification-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Port-only crate: pure types and traits, no I/O.
# The verifier lives in audit/core/verification-domain.

[dependencies]

[lib]
name = "audit_verification_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
