[package]
name = "audit-verification-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Verdict DTOs. The verifier lives in audit/core/verification-domain.

[dependencies]

[lib]
name = "audit_verification_api"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
