[package]
name = "audit-emission-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Port-only crate: pure types and traits, no I/O (ADR-0105).
# Rules over these types live in audit/core/emission-domain.

[dependencies]

[lib]
name = "audit_emission_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
