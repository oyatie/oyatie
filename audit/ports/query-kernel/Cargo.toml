[package]
name = "audit-query-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Port-only crate: pure traits, no I/O. Rules live in audit/core/query-domain.

[dependencies]

[lib]
name = "audit_query_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
