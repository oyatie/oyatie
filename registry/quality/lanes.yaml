# Oyatie quality lane registry. Source of truth mirrored by docs/standards/ci-lanes.md.
# Fields: id, stage, status, owner_team, purpose, source, runtime_budget_seconds, optional check_command.
# `check_command` is local/transitional bridge feedback and wired-command catalog data only.
# Merge authority is the single `oya-ci-required` fan-in plus constituent cloud-ci/Rust gate packets.
# Do not treat local bridge command rows as protected-branch authority.
lanes:
  - id: oya-governance-authority-cohesion
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: authority-chain declarations in AGENTS, README, and MASTERPLAN stay identical and avoid retired prescribed authorities
    source: root-hub-pointers.json / docs/AGENTS.md / ADR-0116
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate authority-cohesion
  - id: oya-governance-claim-ceiling
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: prevent unshipped stability, security, and supply-chain claims above foundation evidence
    source: ADR-0037 / registry/catalog
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate claim-ceiling
  - id: oya-governance-codeowners-mirror
    stage: foundation
    status: active
    owner_team: ops-sre-reliability
    purpose: RACI per-surface owner matches CODEOWNERS team ownership
    source: RACI-OWNERSHIP.md
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate codeowners-mirror
  - id: oya-governance-cohesion
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: cross-axis contract review-class label
    source: ADR-0011
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate cohesion
  - id: oya-governance-data-class
    stage: foundation
    status: active
    owner_team: council-privacy
    purpose: enforce ADR-0008 data-class annotation
    source: ADR-0008
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate data-class
  - id: oya-governance-doc-catalog
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: every consolidated doc has a DOC-CATALOG row
    source: DOC-CATALOG.md
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate doc-catalog
  - id: oya-governance-docs
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: documentation-system pipeline registry and wiki quickref stay grounded
    source: DOCUMENTATION.md / registry/docs/pipeline.tsv
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate documentation-system
  - id: oya-governance-quality-lanes
    stage: foundation
    status: active
    owner_team: axis-foundry
    purpose: registry/quality/lanes.yaml and this CI-lanes doc mirror stay in sync
    source: standards/ci-lanes.md
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate quality-lanes
  - id: oya-governance-honest-claims
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: scan authoritative docs/specs/ADRs for deferred active claims and validate ImplementationPlan ChangeSet graph integrity
    source: ADR-0129 / specs/plan-schema.json / specs/masterplan.json
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate honest-claims
  - id: oya-governance-aspirational-enforcement
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: block active required enforcement claims that reference missing check crates, workflows, or branch-protection contexts
    source: ADR-0135 / ADR-0133
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate aspirational-enforcement
  - id: oya-governance-banned-primitives
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: block banned primitive use inside fenced agent-instruction contracts and the sanitized tracked command-log corpus
    source: specs/master-plan-sequencing.json / F-FORBIDDEN-PRIMITIVES-CI-GUARD
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate banned-primitives --require-command-log-corpus --command-log-root registry/governance-corpora/banned-primitives
  - id: oya-governance-workspace-hygiene
    stage: foundation
    status: active
    owner_team: axis-foundry
    purpose: inventory temp, home, repo-root, build-artifact, and oyatie-worktrees residue before pipeline closeout, with explicit temp/build cleanup and owned-root exemptions
    source: specs/workspace-hygiene.json / ADR-0123
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate workspace-hygiene
  - id: oya-governance-hyperscaler-maturity-claims
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: block unsupported hyperscaler maturity claims unless product depth, pipeline, hygiene, UX, safety, guardrail, and competitor evidence are green
    source: specs/hyperscaler-gates.json / ADR-0123
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate hyperscaler-maturity-claims
  - id: oya-governance-design-spec-maturity-claims
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: allow only the bounded design/spec maturity claim when every microservice has the required implementation-ready design surfaces, while keeping operational maturity blocked
    source: specs/design-spec-maturity-claims.json / ADR-0123
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate design-spec-maturity-claims
  - id: oya-governance-foundation-bypass
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: foundation-bypass expiry monitor
    source: ADR-0040
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate foundation-bypass
  - id: oya-governance-glossary-cross-doc-coverage
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: every glossary term appears outside GLOSSARY when active
    source: GLOSSARY.md §11 / ADR-0018
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate glossary-cross-doc-coverage
  - id: oya-governance-glossary-vocabulary
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: retired-vocab hard-fail plus casing/acronym warning baseline ratchet
    source: GLOSSARY.md §11 / ADR-0018
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate glossary-vocabulary
  - id: oya-governance-placeholder-debt
    stage: foundation
    status: active
    owner_team: axis-foundry
    purpose: fail-closed `TODO` / `TBD` registry so placeholder cleanup is tracked outside glossary acronym warnings
    source: AGENTS.md Done-Definition / MISTAKES doctrine
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate placeholder-debt
  - id: oya-governance-dependency-seam
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: fail-closed ADR-0092 dependency rationale coverage, adapter-only imports, fixture-pair coverage, change_class declarations, and online cargo-audit vulnerability checks
    source: ADR-0092 / registry/dependency-rationales.json
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate dependency-seam --repo-root . --evidence evidence/multispectrum/cs-p13-dependency-seam-1779166052.json --online-audit --severity error
  - id: oya-governance-plane-class
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: catalog plane-class changes require explicit review
    source: ADR-0004
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate plane-class
  - id: oya-governance-raci-team-coverage
    stage: foundation
    status: active
    owner_team: ops-sre-reliability
    purpose: every team charter has RACI and CODEOWNERS coverage
    source: RACI-OWNERSHIP.md
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate raci-team-coverage
  - id: oya-governance-readme-doc-coverage
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: every root doc has catalog and README discoverability
    source: README.md / DOC-CATALOG.md
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate readme-doc-coverage
  - id: oya-governance-runbook-index-resolves
    stage: foundation
    status: active
    owner_team: ops-sre-reliability
    purpose: every RUNBOOKS-INDEX entry is a real file
    source: RUNBOOKS-INDEX.md
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate runbook-index-resolves
  - id: cloud-ci-slo-coverage
    stage: foundation
    status: active
    owner_team: ops-sre-reliability
    purpose: every catalog record carries SLO coverage
    source: SLO-CATALOG.md
    runtime_budget_seconds: 180
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate slo-coverage
  - id: oya-governance-catalog-records
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: every Cargo workspace member has a catalog record
    source: ADR-0015 / registry/catalog
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- catalog validate
  - id: oya-governance-product-index
    stage: foundation
    status: active
    owner_team: axis-foundry
    purpose: product README index and machine-readable product catalog stay in sync
    source: products/README.md
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate product-index
  - id: oya-governance-brand-residue
    stage: foundation
    status: active
    owner_team: council-architecture
    purpose: tautological brand transition check
    source: ADR-0017 / MFL-0011
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate brand-residue
  - id: cargo-fmt
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: "`cargo fmt --all -- --check`"
    source: TOOLCHAIN.md
    runtime_budget_seconds: 120
    check_command: cargo fmt --all -- --check
  - id: cargo-check
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: "`cargo check --workspace --all-targets --keep-going`"
    source: TOOLCHAIN.md
    runtime_budget_seconds: 600
    check_command: cargo check --workspace --all-targets --keep-going
  - id: cargo-clippy
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: "`cargo clippy --workspace --all-targets --keep-going -D warnings`"
    source: standards/testing.md
    runtime_budget_seconds: 900
    check_command: cargo clippy --workspace --all-targets --keep-going -- -D warnings
  - id: cargo-nextest
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: "`cargo nextest run --workspace --no-fail-fast`"
    source: standards/testing.md
    runtime_budget_seconds: 1200
    check_command: cargo nextest run --workspace --no-fail-fast
  - id: cargo-deny
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: per ADR-0013 license + advisory check
    source: ADR-0013
    runtime_budget_seconds: 300
    check_command: cargo deny check
  - id: oya-foundation-demo-smoke
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: "`oya demo` foundation smoke path exercises tenant, MCP, audit, run, step, outbox, and secret flows"
    source: PRD.md
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- demo
  - id: machine-readable-json-parse
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: every docs/machine-readable JSON file parses before merge
    source: DOC-CATALOG.md
    runtime_budget_seconds: 60
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate active-artifact-contract
  - id: cargo-machete
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: unused-deps
    source: TOOLCHAIN.md
    runtime_budget_seconds: 300
    check_command: cargo machete
  - id: pnpm-typecheck
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: TS workspace typecheck
    source: TOOLCHAIN.md
    runtime_budget_seconds: 600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate typescript-workspace --lane typecheck
  - id: pnpm-test
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: TS unit + integration
    source: TOOLCHAIN.md
    runtime_budget_seconds: 600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate typescript-workspace --lane test
  - id: oya-governance-api-semver
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: public-API stability tier per ADR-0037
    source: ADR-0037
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate api-semver
  - id: oya-governance-cargo-prefix
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: every workspace member starts with `oya-`
    source: ADR-0017
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate cargo-prefix
  - id: oya-governance-loop-recovery-patterns
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: pre-push repeat-mistake blocker joins deterministic score cards, loop-recovery patterns, and mistakes-ledger rows without shell hook expansion
    source: specs/agent-durable-goal.json score_cards + autonomous_foundry.meta_agent_anomaly_watch
    runtime_budget_seconds: 120
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate loop-recovery-patterns
  - id: oya-governance-master-plan-completion
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: status-honesty audit — no phase in specs/masterplan.json#live_implementation_index may be Complete while any child IP is stub/planned/pending/blocked/in-flight/probe-green; every complete IP must be referenced by at least one evidence JSON file
    source: NEXT-004 (evidence/goals/masterplan-actionability-durable-goal-2026-05-16.json) + scripts/audit-master-plan-completion.py port (audit B-7)
    runtime_budget_seconds: 60
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate master-plan-completion
  - id: oya-governance-retired-vocabulary
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: no live document mentions any retired CLI surface, retired crate, or retired script path (registry/vocabulary/retired.yaml is the canonical record)
    source: feedback_no_silent_regression + feedback_no_exceptions_canonical
    runtime_budget_seconds: 60
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate retired-vocabulary
  - id: oya-governance-protection-context-match
    stage: per-pr
    status: active
    owner_team: axis-foundry
    purpose: every required-status-check context in .github/branch-protection.yaml is the `name:` field of some workflow job (prevents silent-bypass where GitHub waits forever for a context no workflow posts)
    source: feedback_no_silent_regression + 2026-05-15 PR #3 merge-block incident
    runtime_budget_seconds: 30
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate protection-context-match
  - id: oya-governance-vacuous-green
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: ADR-0221 fixture-backed check that vacuous-green hook detection fails on intentionally empty gate evidence
    source: ADR-0221 M-06
    runtime_budget_seconds: 60
    check_command: bash tools/governance/adr-0221-governance-gates.sh vacuous-green
  - id: oya-governance-adr-orphan-citation
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: ADR-0221 fixture-backed check that orphan ADR citation detection catches missing decision records
    source: ADR-0221 M-13
    runtime_budget_seconds: 60
    check_command: bash tools/governance/adr-0221-governance-gates.sh orphan-citation
  - id: oya-governance-version-pin-source-citation
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: ADR-0221 fixture-backed check that OpenAPI and AsyncAPI version pins reject noncanonical versions
    source: ADR-0221 M-01
    runtime_budget_seconds: 60
    check_command: bash tools/governance/adr-0221-governance-gates.sh version-pin
  - id: oya-governance-buildability-line-count
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: ADR-0221 fixture-backed check that buildability line-count guidance distinguishes short docs from substantive docs
    source: ADR-0221 M-10
    runtime_budget_seconds: 60
    check_command: bash tools/governance/adr-0221-governance-gates.sh buildability-line-count
  - id: foundry-eval-nightly
    stage: nightly
    status: active
    owner_team: axis-foundry
    purpose: per-capability eval set per ADR-0024
    source: ADR-0024
    runtime_budget_seconds: 3600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate foundry-eval
  - id: chain-replay-drill
    stage: nightly
    status: active
    owner_team: ops-sre-reliability
    purpose: per-shard audit-chain integrity per ADR-0003
    source: ADR-0003
    runtime_budget_seconds: 3600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate audit-chain-replay
  - id: cross-tenant-access-fuzz
    stage: nightly
    status: active
    owner_team: council-privacy
    purpose: per-cell isolation per ADR-0009
    source: ADR-0009
    runtime_budget_seconds: 3600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate cross-tenant-access-fuzz
  - id: oya-governance-vendor-contract-recency
    stage: nightly
    status: active
    owner_team: council-architecture
    purpose: per VENDOR-PARTNER-LEDGER
    source: VENDOR-PARTNER-LEDGER.md
    runtime_budget_seconds: 900
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate vendor-contract-recency
  - id: oya-governance-mobile-native
    stage: nightly
    status: active
    owner_team: axis-foundry
    purpose: per ADR-0051
    source: ADR-0051
    runtime_budget_seconds: 1800
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate mobile-native
  - id: release-evidence-pack
    stage: per-release
    status: active
    owner_team: ops-sre-reliability
    purpose: per-regulator evidence regen per COMPLIANCE-MATRIX
    source: COMPLIANCE-MATRIX.md
    runtime_budget_seconds: 3600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate release-evidence-pack
  - id: release-runbook-freshness
    stage: per-release
    status: active
    owner_team: ops-sre-reliability
    purpose: freshness SLA per RUNBOOKS-INDEX §3
    source: RUNBOOKS-INDEX.md
    runtime_budget_seconds: 900
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate runbook-freshness
  # LEAN-A1..A4: flipped from report-only to BLOCKER at M01-P05 (2026-05-13)
  - id: lean-a1-architecture
    stage: per-pr
    status: active
    severity: BLOCKER
    owner_team: council-architecture
    purpose: layer-correctness — no dep-direction violations per ADR-0056 §2.2
    source: ADR-0056
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate architecture-boundaries
  - id: lean-a2-bounded-contexts
    stage: per-pr
    status: active
    severity: BLOCKER
    owner_team: council-architecture
    purpose: microservice-isolation — no cross-µservice deps except via workflow/ontology (v4.1 override)
    source: ADR-0056
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate cedar-fragment-coverage
  - id: lean-a4-semver
    stage: per-pr
    status: active
    severity: BLOCKER
    owner_team: council-architecture
    purpose: API semver stability tier per ADR-0037
    source: ADR-0037
    runtime_budget_seconds: 300
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate api-semver
  - id: lean-a5-documentation
    stage: per-pr
    # crate oya-shared-documentation-check-cli is report-only until M02-P22.
    status: active
    severity: report-only
    owner_team: council-architecture
    purpose: documentation set coverage — every µservice has full canonical + per-pack documentation set per ADR-0063; orphan-scan + masterplan↔workspace registry reconciliation; flips to BLOCKER at M02-P22
    source: ADR-0063
    runtime_budget_seconds: 600
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate documentation-system
  - id: lean-a10-regression
    stage: per-pr
    # crate oya-check-regression is planned in M02-P21 IP-005.
    status: planned
    severity: BLOCKER
    owner_team: council-architecture
    purpose: catch attempted silent regressions of public contracts (manifest.json schema bump without ADR; Cedar policy widening; Protobuf field reuse; fitness lane severity downgrade; µservice removal without retirement ADR; doc frontmatter schema downgrade; event topic schema regression); per feedback_no_silent_regression.md (Linus-style)
    source: ADR-0067 §5.5
    runtime_budget_seconds: 300
  # New quality lanes (report-only; flip to BLOCKER in M02)
  - id: quality-statelessness
    stage: per-pr
    status: planned
    severity: report-only
    owner_team: council-architecture
    purpose: no module-level mutable state in application/worker/presentation layers per ADR-0062
    source: ADR-0062
    runtime_budget_seconds: 300
  - id: quality-shardability
    stage: per-pr
    status: planned
    severity: report-only
    owner_team: council-architecture
    purpose: all DB designs declare tenant_id partition key + RLS per ADR-0062
    source: ADR-0062
    runtime_budget_seconds: 300
  - id: quality-perf-budget
    stage: per-pr
    status: planned
    severity: report-only
    owner_team: council-architecture
    purpose: impl plans include load-test results meeting declared perf targets per ADR-0062
    source: ADR-0062
    runtime_budget_seconds: 300
  - id: quality-benchmark
    stage: per-pr
    status: planned
    severity: report-only
    owner_team: council-architecture
    purpose: PRDs include competitive-benchmark section before L4→L5 per ADR-0062
    source: ADR-0062
    runtime_budget_seconds: 300
  - id: lean-a-active-artifact-contract
    stage: per-pr
    status: active
    severity: report-only
    owner_team: council-architecture
    purpose: every machine-readable artifact under applicable_paths_glob conforms to v3.0.0 9-capability contract per ADR-0089; invoked from `oya gate run-all`; emits evidence + graph-edges artifacts on every run
    source: ADR-0089
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate active-artifact-contract
    runtime_budget_seconds: 60
  - id: lean-a-cedar-fragment-coverage
    stage: per-pr
    status: active
    severity: report-only
    owner_team: council-architecture
    purpose: enforces invariants C01..C04 from /registry/cedar-fragments.json — no orphan .cedar files, no dangling cedar_fragments[] references in OpenAPI contracts or bounded-contexts.json, status↔path consistency; invoked from `oya gate run-all`
    source: cedar-fragments-registry (commit f00ec87)
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate cedar-fragment-coverage
    runtime_budget_seconds: 30
  - id: lean-a-openapi-rest-route-parity
    stage: per-pr
    status: active
    severity: report-only
    owner_team: council-architecture
    purpose: enforces 1:1 parity between `pub const *_ROUTE` constants in crates/oya-ops-*-rest/src/lib.rs and `paths:` keys in contracts/ops-*.openapi.yaml; default scope ops-only via --crate-prefix/--contract-prefix flags
    source: slice-J (commit pending)
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate openapi-rest-route-parity
    runtime_budget_seconds: 30
  - id: oya-governance-merge-queue-ref-hygiene
    stage: nightly
    status: active
    severity: report-only
    owner_team: axis-foundry
    purpose: GC merge-queue-staging-i refs older than 1 hour per ADR-0111 §"Consequences"; transient projected-merge-state staging refs must not accumulate after each scheduler tick. Lane is the post-tick hygiene gate referenced by ADR-0111 wave-C.
    source: ADR-0111
    check_command: cargo run -p oya-vcs-merge-queue-fix-loop-app -- --gc-staging-refs --max-age-seconds 3600
    runtime_budget_seconds: 60
  # PR #143 Fix-D — high-risk auto-decision refusal grounding.
  - id: oya-governance-high-risk-auto-decision-refusal
    stage: per-pr
    status: active
    severity: blocker
    owner_team: council-architecture
    purpose: enforces that every µservice declaring a high-risk "refused at Cedar layer" claim in `capabilities/T2-auto.yaml` has a matching `forbid` rule in `policy/tenant-scope.cedar` gating on employment-context (SEC-MAJ-02).
    source: ADR-0144
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate high-risk-auto-decision-refusal
    runtime_budget_seconds: 60
  # PR #143 Fix-D — SLSA L3 evidence grounding.
  - id: oya-governance-slsa-l3-evidence-grounded
    stage: per-pr
    status: active
    severity: blocker
    owner_team: ops-security
    purpose: "enforces that every scorecard claiming `slsa_l3: green` cites real `.github/workflows/<file>.yml` files that declare SLSA-relevant primitives (signed provenance, hermetic build, two-party review) (SEC-MAJ-01)."
    source: ADR-0133
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate slsa-l3-evidence-grounded
    runtime_budget_seconds: 60
  # PR #143 ADR-0145 enforcement lanes (advisory / deferred mode).
  # Skeleton crates ship; strict-mode promotion tracked under
  # registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-*.
  - id: oya-governance-otel-trace-propagation
    stage: per-pr
    status: active
    severity: report-only
    owner_team: ops-sre-reliability
    purpose: validates that per-µservice gRPC client adapters propagate W3C traceparent per ADR-0145 Invariant 2. Runs in DEFERRED (advisory) mode until the strict-mode parser lands per registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-otel-propagation-validator.
    source: ADR-0145
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate otel-trace-propagation
    runtime_budget_seconds: 30
  - id: oya-governance-ontology-projection-coverage
    stage: per-pr
    status: active
    severity: blocker
    owner_team: platform-eventing-og
    purpose: blocks canonical-entity-owning µservices that omit concrete `ontology_projections` in their manifest per ADR-0145 Invariant 3. Registry-authority cross-checking remains tracked under registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-ontology-projection-validator.
    source: ADR-0145
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate ontology-projection-coverage
    runtime_budget_seconds: 30
  - id: oya-governance-audit-chain-seal-coverage
    stage: per-pr
    status: active
    severity: report-only
    owner_team: platform-audit-evidence
    purpose: validates that µservices with `audit_chain.enabled=true` declare seal events per ADR-0145 Invariant 1. Runs in DEFERRED (advisory) mode until the strict-mode parser lands per registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-audit-chain-seal-validator.
    source: ADR-0145
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate audit-chain-seal-coverage
    runtime_budget_seconds: 30
  # ADR-0148 / ADR-0182 / ADR-0183 / ADR-0184 / ADR-0185 layered architecture
  # discipline lanes (authored 2026-05-18).
  - id: oya-governance-layered-architecture-discipline
    stage: per-pr
    status: active
    severity: blocker
    owner_team: council-architecture
    purpose: enforces zero feature overlap across the layered hyperscaler architecture — Cilium L3/L4 vs Istio Ambient L7 (ADR-0148); gateway vs mesh (ADR-0182); Cedar vs Kyverno (ADR-0183); Valkey vs Memcached (ADR-0184). Rejects any µservice manifest declaring conflicting layer ownerships or under-claimed mesh tiers.
    source: ADR-0148, ADR-0182, ADR-0183, ADR-0184
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate layered-architecture-discipline
    runtime_budget_seconds: 60
  - id: oya-governance-client-stack-discipline
    stage: per-pr
    status: active
    severity: blocker
    owner_team: axis-workspace
    purpose: enforces ADR-0185 native-per-platform client stack — SvelteKit (Phase 1) → Leptos (Phase 2) on web (sequential not parallel); Swift+SwiftUI on Apple (no KMP klib imports); Kotlin+Compose+KMP on Android (Android-scope only); WinUI 3 on Windows; gtk4-rs + libadwaita on Linux. Rejects React/Vue/Flutter/Electron/Cordova/Tauri/Avalonia/MAUI references.
    source: ADR-0185
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate client-stack-discipline
    runtime_budget_seconds: 60
  # PR #143 Fix-M/N/R/S/T/U batches — 1 strict + 11 advisory lanes (close-out 2026-05-18).
  - id: oya-governance-vendor-lockin-discipline
    stage: per-pr
    status: active
    severity: blocker
    owner_team: council-architecture
    purpose: enforces ADR-0173 tiered vendor classification — Tier I OWNED with license+steward; Tier II VENDOR-SEAMED with replacement_path, replacement_readiness_gate, seam_adapter_trait, and ≥1 seam_adapter_impls; Tier III FORBIDDEN with refusal rationale + replacement path.
    source: ADR-0173
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate vendor-lockin-discipline
    runtime_budget_seconds: 60
  - id: oya-governance-authz-tier-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: council-architecture
    purpose: enforces ADR-0191 tier boundary — Cedar policy MUST NOT reference edge concerns (ip/asn/geo/country/rate/waf/bot/ddos); Envoy filter config MUST NOT reference origin concerns (oidc principal claims/acr/tenant identity/residency/purpose/data_class). Advisory until per-microservice Cedar/Envoy assets exist.
    source: ADR-0191
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate authz-tier-discipline
    runtime_budget_seconds: 30
  - id: oya-governance-tenant-cost-labels-coverage
    stage: per-pr
    status: active
    severity: report-only
    owner_team: ops-finops
    purpose: advisory presence check for mandatory tenant cost labels per ADR-0199 D-1 (oya.io/tenant-id + oya.io/cost-center + oya.io/workload-class + oya.io/regulatory-pack) on every rendered Helm manifest under microservices/*/iac/helm/*/rendered/*.yaml.
    source: ADR-0199
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate tenant-cost-labels-coverage
    runtime_budget_seconds: 60
  - id: oya-governance-backup-retention-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: ops-sre-reliability
    purpose: advisory per-µservice backup retention validation per ADR-0197 D-5 (tier-driven RPO/RTO floor; storage.backup.tier ∈ {app, batch, gpu, regulatory}; storage.backup.retention_days set).
    source: ADR-0197
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate backup-retention-discipline
    runtime_budget_seconds: 60
  - id: oya-governance-vector-store-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: axis-foundry
    purpose: advisory enforcement of Milvus-canonical vector store tier per ADR-0192 (collection schema, index_type, metric, data_classes). Advisory until per-µservice manifests populate data.vector_store.* namespace.
    source: ADR-0192
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate vector-store-discipline
    runtime_budget_seconds: 60
  - id: oya-governance-olap-tier-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: axis-ads-analytics
    purpose: advisory enforcement of ClickHouse-canonical OLAP tier per ADR-0193 (no rogue analytics queries against OLTP postgres). Advisory until per-µservice manifests populate data.olap_client.* namespace.
    source: ADR-0193
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate olap-tier-discipline
    runtime_budget_seconds: 60
  - id: oya-governance-wasm-runtime-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: axis-foundry
    purpose: advisory enforcement of wasmtime-canonical WASM substrate per ADR-0200 (sandbox classes envoy-filter, workflow-studio-node, or foundry-tool). Promotion target T+60d after ADR-0200 land; flip to blocker.
    source: ADR-0200
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate wasm-runtime-discipline
    runtime_budget_seconds: 30
    promotion_target: T+60d after ADR-0200 land; flip to blocker
  - id: oya-governance-iac-tier-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: ops-sre-reliability
    purpose: advisory enforcement of OpenTofu-canonical IaC tier per ADR-0202; Terraform path supported only during 90-day migration window. Promotion target T+90d after ADR-0202 land; flip to blocker (forbids new .tf files).
    source: ADR-0202
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate iac-tier-discipline
    runtime_budget_seconds: 30
    promotion_target: T+90d after ADR-0202 land; flip to blocker
  - id: oya-governance-a11y-discipline
    stage: per-pr
    status: active
    severity: report-only
    owner_team: axis-workspace
    purpose: advisory WCAG 2.2 AA coverage per ADR-0207 (axe + pa11y runners declared per client surface in manifest.json#a11y).
    source: ADR-0207
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate a11y-discipline
    runtime_budget_seconds: 60
  - id: oya-governance-i18n-coverage
    stage: per-pr
    status: active
    severity: report-only
    owner_team: axis-workspace
    purpose: advisory Fluent ICU locale coverage per ADR-0206 (default_locale + required_locales + rtl_support + min_coverage_bps).
    source: ADR-0206
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate i18n-coverage
    runtime_budget_seconds: 60
  - id: oya-governance-compliance-evidence-coverage
    stage: per-pr
    status: active
    severity: report-only
    owner_team: ops-compliance
    purpose: advisory compliance evidence collector declaration per ADR-0209 (audit_chain_seal_required + tamper_evidence_algorithm + evidence_collectors[]).
    source: ADR-0209
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate compliance-evidence-coverage
    runtime_budget_seconds: 60
  - id: oya-governance-realtime-transport-tier
    stage: per-pr
    status: active
    severity: report-only
    owner_team: axis-workspace
    purpose: advisory real-time transport tier validation per ADR-0208 (sse vs websocket vs grpc-streaming; payload_budget_bytes per tier).
    source: ADR-0208
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate realtime-transport-tier
    runtime_budget_seconds: 30
  - id: oya-governance-adr-planning-completeness
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: ADR-0364 D2 — every planning_impact ADR that declares deliverables has id/description/exit_criteria/verified_by plus a milestone; ADRs without deliverables are advisory until D7 backfill
    source: ADR-0364
    runtime_budget_seconds: 60
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate adr-planning-completeness
  - id: oya-governance-masterplan-drift
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: ADR-0364 D4 — masterplan.generated.json (de-committed per ADR-0613) regenerates successfully from the planning_impact ADR log (regeneration-success, not committed-byte parity; wraps `gen masterplan --check`). Local-bridge feedback only; required-CI freshness directly regenerates masterplan and product-graph twice and compares each face independently in gate-generated-artifact-freshness.
    source: ADR-0364
    runtime_budget_seconds: 60
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate masterplan-drift
  - id: oya-governance-adr-supersession-consistency
    stage: per-pr
    status: active
    owner_team: council-architecture
    purpose: "#6b — every ADR supersedes/superseded_by pair is bidirectional; fails on any one-directional link (X supersedes Y but Y does not back-link X, or vice versa) over ADR-to-ADR edges"
    source: ADR-0083
    runtime_budget_seconds: 30
    check_command: buck2 run //marketplace/facade/dev-cli:oya -- gate validate adr-supersession-consistency
