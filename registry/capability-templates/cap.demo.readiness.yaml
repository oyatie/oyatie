# Bootstrap published capability template record for ADR-0024 eval gate validation.
id: cap.demo.readiness
namespace: foundry.demo
status: published
version: 0.1.0
description:
  agent_readable: Run the bootstrap demo readiness check and return structured readiness evidence for the Foundry publish gate.
  human_readable: Demo readiness capability exercised by `oya demo`; used as the checked-in bootstrap capability for eval publish-gate validation.
provider:
  preferred: foundation-local
  fallback: []
autonomy_tier_required: T1
data_classes_touched: [INTERNAL_ONLY]
evidence_emission_topic: foundry.capability.invoke:cap.demo.readiness
cost_profile:
  per_invocation_budget_usd: 0.01
  monthly_budget_usd: 1
input_schema:
  type: object
  properties:
    release_id:
      type: string
  required: [release_id]
output_schema:
  type: object
  properties:
    verdict:
      type: string
  required: [verdict]
eval_set: eval-sets/cap.demo.readiness.yaml
eval_run: eval-runs/cap.demo.readiness.yaml
owner_team: axis-foundry
