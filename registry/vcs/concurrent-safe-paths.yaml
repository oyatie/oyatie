# registry/vcs/concurrent-safe-paths.yaml
#
# Retired historical evidence only. This file records the former
# path-overlap seed for provenance and must not be used as active admission,
# lease, or merge authority.
#
# Current parallel-lane direction is native SCM service/control-plane lease
# records, Git/GitHub adapter publication, and Rust/Buck2/Prow checks. Shared
# surfaces should be pointer-thin or generated from lane-owned shards.
#
# Schema:
#   products:
#     <product-name>:
#       safe_paths:
#         - <repo-relative path string>
#
# No active loader is allowed for this retired seed.
schema_version: 1

products: {}
