context: os
role: test
capability: attestation-relying-party
plane: control
slo: cataloged-no-runtime-claim
data_classes_owned: [PUBLIC, INTERNAL_ONLY]
operational_classes_owned: []
api_stability: preview
security_review: unreviewed
supply_chain: source-only
# Excluded from root Cargo workspace until integ/build absorbs lock membership
# (Cargo.toml exclude). Catalog-liveness requires explicit non-live marker.
status: designed-ahead-row-no-crate
traceability:
  source_crate: os/harness/attestation-relying-party/Cargo.toml
non_claims:
  - crate lives on disk but is excluded from the root live Cargo workspace until integ/build lock absorb; row is inventory + SLO face only
  - relying-party type scaffold only; does not fetch AMD KDS / Intel PCS collateral
  - never claims live SNP/TDX/CCA hardware quotes; stub verdicts are UNKNOWN
  - does not amend ConfidentialPlatform sealed impls; extends the story via RP types
  - Cedar context key injection and SVID binding are future adapter work (blocked on F1/W0/K3)
