context: build
role: kernel
capability: corpus-yaml-extraction
plane: control
slo: cataloged-no-runtime-claim
data_classes_owned: [INTERNAL_ONLY]
operational_classes_owned: []
api_stability: preview
security_review: unreviewed
supply_chain: source-only
traceability:
  source_crate: governance/corpus/yaml-kernel/Cargo.toml
non_claims:
  - pure kernel only; the file I/O adapter is the corpus-yaml-facts binary in corpus-extract
  - indexes the YAML artifact class only; markdown, JSON, TOML and Cedar are not covered
  - emits source-level facts; no cross-file reference resolution is performed
