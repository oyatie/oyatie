context: managed-k8s-control-plane-host
role: app
capability: managed-k8s-control-plane-host-composition-root
plane: control
slo: preview-control-plane
data_classes_owned: [INTERNAL_ONLY]
operational_classes_owned: [AUDIT]
api_stability: preview
security_review: unreviewed
supply_chain: source-only
traceability:
  source_adrs: [ADR-0376, ADR-0105, ADR-0083, ADR-0092]
non_claims:
  - composition root wiring the ControlPlaneProvisioning port to an adapter + axum admin/status router + fail-closed [[bin]] main
  - in-memory adapter is the dev/test default; the kube-rs CAPI adapter's live reconcile is honest-deferred (HTTP 501 boundary)
  - no live tenant control-plane provisioning until kamaji-provider-live-integration lands; never runs tenant workloads (mgmt-cluster-only)
