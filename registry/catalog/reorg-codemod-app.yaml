context: tooling
role: app
capability: reorg-codemod
plane: control
slo: preview-control-plane
data_classes_owned: [INTERNAL_ONLY]
api_stability: preview
security_review: unreviewed
supply_chain: source-only
notes:
  - ADR-0562 Phase-0 reorg machinery; builds the deterministic, reversible capability-move codemod + pre-move green-snapshot oracle. Moves NO real capability.
  - local-bridge tool only; ships UNUSED until the per-capability strangler invokes it; merge authority stays in cloud-ci/oya-ci required contexts
  - transforms: git-mv directory move + Cargo.toml package/dep/path-recompute + root workspace members + Rust use/extern-crate kebab->snake + BUCK label/name/crate rewrite; recomputes every ../../../ move-fatal relative path-dep
  - fail-closed: refuses non-zero on non-resolving workspace, ambiguous path-dep recompute, or target-name collision; dry-run shadow-apply proves cargo metadata + buck2 targets //... resolve without landing
