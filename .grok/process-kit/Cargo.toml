[package]
name = "oya-process-kit"
version = "0.1.0"
edition = "2024"
description = "Rust-first swarm process-kit (git/env refuse + check-daemon stub). Not yet a root workspace member — wire via buck2 before freshness membership."
publish = false

[[bin]]
name = "oya-process-kit-check-daemon"
path = "src/bin/check_daemon.rs"

[lib]
name = "oya_process_kit"
path = "src/lib.rs"
