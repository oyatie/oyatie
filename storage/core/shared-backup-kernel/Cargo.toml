[package]
name = "shared-backup-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Backup substrate kernel per ADR-0197 (Velero + pgBackRest + Restic).
# Pure trait + reference adapter; no external network deps in kernel scope.
# Authored 2026-05-18.

[dependencies]

[lib]
name = "shared_backup_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
