apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-successor-bench
  service: performance-management
spec:
  businessCapability: succession-planning
  capability: successor-bench
  boundedContext: succession-planning
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328]
  benchmarks: [Workday Performance]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    Successor bench list per critical role with readiness tier (ready-now, ready-1-year,
    ready-2-year, not-ready). Bench reviewed at executive cadence.
  prdRef: FR-026
