apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-skip-level-review
  service: performance-management
spec:
  businessCapability: review-cycle
  capability: skip-level-review
  boundedContext: review-cycle
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328]
  benchmarks: [Workday Performance]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    Skip-level review: skip-level manager reviews report directly; outcome visible only to
    skip-level + HRBP per Cedar gate.
  prdRef: FR-007
