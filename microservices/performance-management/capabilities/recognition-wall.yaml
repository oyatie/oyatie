apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-recognition-wall
  service: performance-management
spec:
  businessCapability: recognition
  capability: recognition-wall
  boundedContext: recognition
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328, ADR-0331]
  benchmarks: [Lattice, Workday Performance]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    Public recognition wall surfacing kudos. Visibility configurable by tenant policy.
  prdRef: FR-028
