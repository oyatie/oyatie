apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: performance-management-goal-coaching-prompts
  service: performance-management
spec:
  businessCapability: manager-tooling
  capability: goal-coaching-prompts
  boundedContext: manager-tooling
  tier: product
  bindingAdrs: [ADR-0105, ADR-0244, ADR-0321, ADR-0328]
  benchmarks: [Lattice]
  counterpartCoverage: full
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEligibility:
    demo_trial: synthetic-only
    paid: full-surface
  description: |
    LLM-assisted coaching prompts for goal review; suggests questions manager should ask in
    1:1 about stalled goals.
  prdRef: FR-030
