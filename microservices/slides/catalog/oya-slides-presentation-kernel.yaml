# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
name: oya-slides-presentation-kernel
microservice: slides
bounded_context: presentation
layer: kernel
purpose: |
  Port-trait + entity declarations for the slides presentation BC.
  Zero I/O; zero business logic; zero external dependencies beyond serde + chrono.
  Per ADR-0105 13-layer enum + ADR-0131 per-microservice flat layout.
ports_declared:
  - DeckRepository
entities:
  - Deck
  - DeckMetadata
  - DeckAcl
data_classes:
  - BEHAVIORAL_TENANT_PRODUCT
  - INTERNAL_ONLY
  - AUDIT
ci_lanes:
  - lean-a1
  - port-location
  - layer-correctness
  - data-class
