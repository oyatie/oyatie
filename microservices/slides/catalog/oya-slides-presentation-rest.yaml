name: oya-slides-presentation-rest
microservice: slides
bounded_context: presentation
layer: rest
purpose: |
  HTTP surface for deck + ACL CRUD per contracts/openapi/slides.yaml.
  OIDC-bound at every entry; Cedar default-deny.
depends_on:
  - oya-slides-presentation-usecase
ci_lanes:
  - statelessness
  - cedar-preview-required
  - cargo-nextest
