apiVersion: oyatie.dev/v1
kind: SecretBinding
metadata:
  name: oya-real-estate
spec:
  openbaoPaths:
    - secret/{{ tenant_id }}/real-estate/database
    - secret/{{ tenant_id }}/real-estate/signing
  maxTtlSeconds: 60
  auditEventClass: EVT-REAL_ESTATE-LEASE_CONTRACT-SECRET_LEASE
