apiVersion: v2
name: oya-forms-form-cdn
description: |
  Forms OCI CDN edge configuration (per-pack edges). Serves form-renderer WASM +
  design-system primitives. Per-tenant cache key (tenant_id + form_id + locale + pack).
type: application
version: 0.1.0
appVersion: "1.0.0"
maintainers:
  - name: axis-forms + cloud-iac
    email: axis-forms@oyatie.dev
