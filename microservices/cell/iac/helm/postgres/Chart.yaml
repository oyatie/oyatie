# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: oya-cell-postgres
description: HA Postgres for cell-registry per pack (CloudNativePG operator + RLS-enabled schema). Per Bominal ADR-0019 + cell-boundary.md TI-01.
type: application
version: 0.1.0
appVersion: "16.2"  # Postgres LTS line
maintainers:
  - name: axis-cell-substrate
    email: axis-cell-substrate@oyatie.dev
dependencies:
  - name: cloudnative-pg
    version: "^0.22.0"  # LTS pin
    repository: https://cloudnative-pg.github.io/charts
