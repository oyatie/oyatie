//! Payments subscription-lifecycle usecase — `CreateSubscriptionUseCase`,
//! `RenewSubscriptionUseCase`, `DunningWorker`.
//!
//! Wave 15-IMPL-truth-up scaffold; full Cedar + COPPA + dunning ladder in
//! IP-012.
#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic))]

#[allow(dead_code)]
pub struct CreateSubscriptionUseCase {
    _placeholder: (),
}

#[allow(dead_code)]
pub struct RenewSubscriptionUseCase {
    _placeholder: (),
}

#[allow(dead_code)]
pub struct DunningWorker {
    _placeholder: (),
}
