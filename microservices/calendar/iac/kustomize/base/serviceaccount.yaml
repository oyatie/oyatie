apiVersion: v1
kind: ServiceAccount
metadata:
  name: oya-calendar
  namespace: calendar
  labels:
    microservice: calendar
  annotations:
    # OpenBao Kubernetes auth binding for secret reference resolution
    openbao.oyatie.dev/role: calendar
