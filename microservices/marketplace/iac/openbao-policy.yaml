apiVersion: oyatie.dev/v1
kind: OpenbaoPolicy
metadata:
  name: marketplace-openbao-policy
  labels:
    service: marketplace
    adr: ADR-0314
spec:
  tenantScope: required
  openBaoPath: secret/marketplace/openbao-policy
  naming_justifications:
    bnf_v4: "marketplace.iac.openbao-policy"
    layer_enum: "13-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, graphql, worker, cli, sdk, api"
