# Canonical-base: microservices/governance/iac/helm/_oya-helpers — helpers via {{ include "oya.X" $ }} (SWEEP-J Slice 2 per ADR-0064)
{{- range tuple "websocketGateway" "channelStore" "messageStreamRest" "messageStreamWorker" "presence" "fileAttachmentWorker" "mentionRouter" }}
{{- $c := index $.Values . }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: messenger-{{ . | kebabcase }}
  namespace: messenger
  labels:
    app.kubernetes.io/name: messenger
    app.kubernetes.io/component: {{ . | kebabcase }}
    app.kubernetes.io/part-of: oya-messenger
    oyatie/microservice: messenger
spec:
  replicas: {{ $c.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: messenger
      app.kubernetes.io/component: {{ . | kebabcase }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: messenger
        app.kubernetes.io/component: {{ . | kebabcase }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9091"
    spec:
      serviceAccountName: messenger-{{ . | kebabcase }}
      {{- include "oya.gracefulShutdown" $ | nindent 6 }}
      {{- include "oya.priorityClass.important" $ | nindent 6 }}
      securityContext:
        {{- include "oya.securityContext.podStandard65534" $ | nindent 8 }}
      containers:
        - name: messenger-{{ . | kebabcase }}
          image: "{{ $.Values.global.imageRegistry }}/{{ $.Values.image.repository }}:{{ $.Values.image.tag }}"
          imagePullPolicy: {{ $.Values.global.imagePullPolicy }}
          args: ["--component={{ . | kebabcase }}", "--pack={{ $.Values.global.pack }}"]
          ports:
            - name: http
              containerPort: 8080
            - name: metrics
              containerPort: 9091
            - name: ws
              containerPort: 8443
          resources:
            {{- include "oya.resourceRequests" (dict "tier" $c.resourceTier) | nindent 12 }}
          startupProbe:
            {{- include "oya.probes.startupProbe" $ | nindent 12 }}
          livenessProbe:
            {{- include "oya.probes.httpHealthLiveness8080" $ | nindent 12 }}
          readinessProbe:
            {{- include "oya.probes.httpHealthReadiness8080" $ | nindent 12 }}
          lifecycle:
            {{- include "oya.preStopHook.gracefulDelay" $ | nindent 12 }}
          envFrom:
            - secretRef:
                name: messenger-openbao-injected
          securityContext:
            {{- include "oya.securityContext.restrictedContainerInline" $ | nindent 12 }}
{{- end }}
