apiVersion: openslo/v1
kind: SLO
metadata:
  name: financial-planning-close-cycle-latency
  labels:
    service: financial-planning
    suite: local-operational-primitive
    adr: ADR-0130
    promotion_gate: ADR-0139
spec:
  service: financial-planning
  description: Close-cycle workflow advances inside finance calendar objective for budget locking, forecast recalculation, variance explanation, close-cycle workflow, FX rate, and board-report operations.
  indicator:
    metadata:
      name: close-cycle-latency
      labels:
        domain_object: PlanningCycle
        operator_audience: FPNA_CONTROLLER
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(oya_financial_planning_close_cycle_latency_good_total[30d])) by (tenant_id, cell_tier)
        total:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(oya_financial_planning_close_cycle_latency_total[30d])) by (tenant_id, cell_tier)
  objectives:
    - displayName: Close-cycle workflow advances inside finance calendar objective
      target: 0.99
  timeWindow:
    - duration: 30d
      isRolling: true
