[package]
name = "oya-managed-k8s-sla-observability-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Pure golden-signal SLA/error-budget model for managed-Kubernetes clusters. Computes deterministic availability, provisioning-latency, and error-budget summaries from control-plane status observations without Prometheus/Kubernetes I/O."

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lib]
name = "oya_managed_k8s_sla_observability_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
