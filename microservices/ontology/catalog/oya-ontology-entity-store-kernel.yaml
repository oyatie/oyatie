# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-entity-store-kernel
  microservice: ontology
  bounded_context: entity-store
  layer: kernel
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = entity-store; layer = kernel.
    ObjectTypeStore port + ObjectInstance/ObjectId/WriteReceipt entities;
    zero I/O; data_class annotations on every field.
  ports: [ObjectTypeStore]
  entities: [ObjectInstance, ObjectId, WriteReceipt]
  shard_key: tenant_id  # ADR-0019 + oya-check-shardability-cli
  related_ip: IP-004
  related_adrs: [ADR-0006, ADR-0018, ADR-0028, ADR-0106]
