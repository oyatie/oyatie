# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-entity-store-adapter-clickhouse
  microservice: ontology
  bounded_context: entity-store
  layer: adapter
  backend: clickhouse
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = entity-store; layer = adapter-clickhouse
    (per ADR-0105 Amendment 3). History-mirror via Kafka outbox consumer;
    ReplicatedMergeTree partitioned by (tenant_id, toYYYYMM(ts)); row-policies
    enforce per-tenant scope.
  imports: [oya-ontology-entity-store-usecase, oya-ontology-entity-store-domain, oya-ontology-entity-store-kernel]
  external_deps: [clickhouse-rs, kafka-rust]
  related_ip: IP-009
  related_adrs: [ADR-0050, ADR-0105]
