microservice:
  id: observability
  cargoPackage: oya-observability-domain
  tenantClass: contributor

replicaCount: 2

podRuntime:
  runtimeClassName: kata-cloud-hypervisor

serviceAccount:
  name: oya-observability

image:
  registry: registry.oyatie.dev
  repository: oya-observability
  tag: "0.1.0"
  # Release automation must inject a real signed image digest. Keep the
  # pre-release default empty so local checks do not carry fake digests.
  digest: ""
  pullPolicy: IfNotPresent
  cosign:
    # Dev-cell: cosign enforcement is off until BuildKit produces a signed image
    # (ADR-0181 + ADR-0381 D1). Setting true here causes `helm template` to fail
    # with a missing-digest error, which blocks ArgoCD manifest generation entirely
    # (observability App shows Unknown/Degraded). Production promotion flips this
    # to true via the release pipeline.
    required: false
    policy: ADR-0181

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

config:
  # This umbrella is the ArgoCD/dev-cell entrypoint. Production promotion is
  # fail-closed by templates/production-guard.yaml until object storage and
  # tenant-scoped datasource policy are explicitly wired.
  environment: development
  deploymentProfile: dev-single-node
  defaultTenant: oyatie-dev
  auditChainEnabled: true
  imagePromotionPolicy: cosign-required

cedar:
  enabled: true
  policyConfigMapName: oya-observability-cedar

otel:
  enabled: true
  endpoint: http://otel-collector.observability.svc.cluster.local:4317

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 1000m
    memory: 512Mi

namespaceCreate: true
namespaceName: observability

# Grafana OSS observability stack subcharts. Versions are pinned in Chart.yaml/Chart.lock.
loki:
  enabled: true
  deploymentMode: SingleBinary
  loki:
    storage:
      type: filesystem
    # NOTE: filesystem storage + useTestSchema=true is dev-only.
    # Production wires SeaweedFS-bucket-api per ADR-0349; tracked at
    # registry/placeholder-debt/adr-follow-ups.yaml#observability-loki-prod-storage.
    useTestSchema: true
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 10001
      runAsGroup: 10001
      fsGroup: 10001
      fsGroupChangePolicy: OnRootMismatch
      seccompProfile:
        type: RuntimeDefault
    containerSecurityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: [ALL]
      readOnlyRootFilesystem: true
      seccompProfile:
        type: RuntimeDefault
  singleBinary:
    replicas: 1
  read:
    replicas: 0
  write:
    replicas: 0
  backend:
    replicas: 0
  chunksCache:
    enabled: false
  resultsCache:
    enabled: false
  minio:
    enabled: false
  gateway:
    enabled: true
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 101
      runAsGroup: 101
      fsGroup: 101
      seccompProfile:
        type: RuntimeDefault
    containerSecurityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: [ALL]
      readOnlyRootFilesystem: true
      seccompProfile:
        type: RuntimeDefault
  sidecar:
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: [ALL]
      readOnlyRootFilesystem: true
      seccompProfile:
        type: RuntimeDefault
  test:
    enabled: false

tempo:
  enabled: true
  multitenancyEnabled: true
  tempo:
    podSecurityContext:
      fsGroup: 1000
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
      seccompProfile:
        type: RuntimeDefault
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: [ALL]
      readOnlyRootFilesystem: true
      runAsGroup: 1000
      runAsNonRoot: true
      runAsUser: 1000
      seccompProfile:
        type: RuntimeDefault
  storage:
    trace:
      backend: local
      # NOTE: local backend is dev-only.
      # Production wires SeaweedFS-bucket-api per ADR-0349; tracked at
      # registry/placeholder-debt/adr-follow-ups.yaml#observability-tempo-prod-storage.
  minio:
    enabled: false
  gateway:
    enabled: true

mimir:
  enabled: true
  minio:
    enabled: false   # SeaweedFS-on-Talos canonical per ADR-0349; do NOT ship MinIO transitive
  # mimir-distributed 6.x enables Kafka + ingest-storage by default. On this dev-cell
  # Kafka is Pending (no storage pool yet), so every stateful Mimir component crashes at
  # startup trying to create the mimir-ingest topic. Disable Kafka and revert to the
  # classic push path until the storage specialty pool (ADR-0381 D2) lands.
  kafka:
    enabled: false
  mimir:
    structuredConfig:
      multitenancy_enabled: true
      # Disable ingest-storage (Kafka-backed write path introduced in mimir-distributed 6.x).
      # Must be explicitly set to false when kafka.enabled=false to avoid the distributor
      # failing with "unable to create topic mimir-ingest: connection refused".
      ingest_storage:
        enabled: false
      # Dev-cell storage only. Production must override to SeaweedFS object
      # storage before config.environment=production can render. Tracked at
      # registry/placeholder-debt/adr-follow-ups.yaml#observability-mimir-prod-storage.
      blocks_storage:
        backend: filesystem
        filesystem:
          dir: /data/tsdb
      # Compactor data dir MUST NOT overlap with blocks_storage.filesystem.dir (/data/tsdb).
      # Default compactor data_dir is /data which is a parent of /data/tsdb — Mimir rejects
      # this at startup. Set an explicit non-overlapping path.
      compactor:
        data_dir: /data/compactor
      limits:
        max_global_series_per_user: 1000000
        ingestion_rate: 100000
        cardinality_analysis_enabled: true
  metaMonitoring:
    grafanaAgent:
      installOperator: false
  rollout_operator:
    enabled: false
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 10001
    fsGroup: 10001
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: [ALL]
    readOnlyRootFilesystem: true

externalSecrets:
  grafanaAdmin:
    enabled: true
    refreshInterval: 5m
    secretStoreRef:
      name: openbao
      kind: ClusterSecretStore
    targetName: grafana-admin
    remoteKey: oya/observability/grafana-admin
    userProperty: admin-user
    passwordProperty: admin-password

grafana:
  enabled: true
  admin:
    # MUST be projected via ExternalSecret from sref://openbao/oya/observability/grafana-admin
    # before bring-up; the ExternalSecret manifest is the operator's responsibility.
    existingSecret: "grafana-admin"
    userKey: admin-user
    passwordKey: admin-password
  # Dev-cell bootstrap Secret. Enabled only on dev-cell so Grafana can start
  # while OpenBao is sealed / ClusterSecretStore not yet registered.
  # The ExternalSecret adopts this Secret once OpenBao is unsealed (creationPolicy=Owner).
  # NEVER enable in production (production-guard.yaml enforces environment != production).
  devBootstrapSecret:
    enabled: true
    adminUser: "admin"
    adminPassword: "oya-dev-bootstrap-changeme"
  persistence:
    enabled: true
    type: pvc
    size: 5Gi
    storageClassName: local-path  # or appropriate per cell
  initChownData:
    # Restricted PSA forbids the chart default root+CHOWN init container.
    # fsGroup handles writable PVC ownership for the non-root Grafana pod.
    enabled: false
  testFramework:
    enabled: false
  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: Loki
          type: loki
          access: proxy
          url: http://observability-loki-gateway
          jsonData:
            httpHeaderName1: X-Scope-OrgID
          secureJsonData:
            # Dev-cell tenant marker. Production renders fail until this is
            # replaced by tenant-scoped datasource provisioning.
            httpHeaderValue1: oyatie-dev
        - name: Tempo
          type: tempo
          access: proxy
          url: http://observability-tempo-gateway
          jsonData:
            httpHeaderName1: X-Scope-OrgID
          secureJsonData:
            # Dev-cell tenant marker. Production renders fail until this is
            # replaced by tenant-scoped datasource provisioning.
            httpHeaderValue1: oyatie-dev
        - name: Mimir
          type: prometheus
          access: proxy
          url: http://observability-mimir-gateway/prometheus
          jsonData:
            httpHeaderName1: X-Scope-OrgID
          secureJsonData:
            # Dev-cell tenant marker. Production renders fail until this is
            # replaced by tenant-scoped datasource provisioning.
            httpHeaderValue1: oyatie-dev
  # Grafana official non-root UID is 472
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 472
    fsGroup: 472
    seccompProfile:
      type: RuntimeDefault
  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop: [ALL]
    readOnlyRootFilesystem: true

# DEFERRED per IP-001 (microservices/observability/IP-001-layer-a-grafana-stack-iac.md):
#   - alertmanager: deferred until first SLO alert is authored
#   - prometheus: deferred — Mimir is the long-retention layer, scrape via Alloy
#   - pyroscope: deferred — profile-based observability is post-MVP
#   - alloy: deferred — replace ad-hoc Prom scrape config when promoted
#   - oncall: deferred — Grafana OnCall is a separate microservice surface
# Each tracked at registry/placeholder-debt/adr-follow-ups.yaml#observability-<component>.
