# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
# Skeleton authored 2026-05-18 per ADR-0170 (Backstage developer portal).
# Spotify-Backstage internal developer portal at portal.oya.internal.
# Aggregates per-µservice catalog records (ADR-0131 manifest.json), TechDocs
# (per-µservice docs/), SLO dashboards (ADR-0139 Grafana), runbooks,
# team membership (.github/CODEOWNERS + registry/teams.json), and the
# ADR index (docs/decisions/*.md).
apiVersion: v2
name: oya-observability-backstage
description: Internal developer portal per ADR-0170; Backstage LTS aggregator over per-µservice manifests + TechDocs + SLO embeds + ADR index
type: application
version: 0.1.0
appVersion: "1.30.0"  # Backstage LTS pin per ADR-0170 §"Operational" #2
maintainers:
  - name: axis-developer-experience
dependencies:
  - name: backstage
    version: "^1.30.0"
    repository: https://backstage.github.io/charts
  - name: postgresql
    version: "^15.0.0"
    repository: https://charts.bitnami.com/bitnami
    alias: backstage-catalog-postgres
