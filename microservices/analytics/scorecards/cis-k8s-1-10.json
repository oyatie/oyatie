{
  "framework": "cis-kubernetes-benchmark-v1.10",
  "microservice": "analytics",
  "last_reviewed": "2026-05-18",
  "controls": {
    "network_policies_default_deny": "in-place via per-µservice NetworkPolicy (IP-001)",
    "pod_security_admission_restricted": "PSA restricted enforced cluster-wide",
    "secrets_via_external_secrets_operator": "OpenBao-resolved via ESO per ADR-0043",
    "image_pull_policy_pinned": "all images pinned to immutable digest in values.yaml",
    "container_runs_as_nonroot": "ClickHouse images run as `clickhouse` user; non-root",
    "read_only_root_filesystem": "where compatible; ClickHouse requires writable /var/lib/clickhouse",
    "service_account_token_not_auto_mounted": "automountServiceAccountToken: false where applicable",
    "audit_logging_enabled": "K8s API audit + ClickHouse query_log + audit-chain per ADR-0003"
  }
}
