# Per-data-class partition per ADR-0192 §"Multi-tenancy isolation" Tier 3
# (partition).
apiVersion: milvus.io/v1alpha1
kind: MilvusPartition
metadata:
  name: REPLACE_TENANT_DOMAIN__partition_REPLACE_DATA_CLASS
  annotations:
    oya.adr: "0192,0008"
    oya.data_class: REPLACE_DATA_CLASS
spec:
  collection: REPLACE_TENANT_DOMAIN
  partitionName: partition_REPLACE_DATA_CLASS
