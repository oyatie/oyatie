apiVersion: v2
name: gpu-runner-pool
description: |
  GPU runner pool for foundry-eval eval-case dispatch.
  Per ADR-0024 (eval harness) + ADR-0131 (per-microservice flat layout).
  gVisor / Kata sandbox enforced; per-pod ephemeral filesystem;
  per-pod NetworkPolicy restricts egress to provider model API allowlist.
type: application
version: 1.0.0
appVersion: "1.0.0"
home: https://oyatie.com
maintainers:
  - name: ops-sre-reliability
    email: ops-sre-reliability@oyatie.internal
  - name: axis-foundry
    email: axis-foundry@oyatie.internal
keywords: [foundry-eval, gpu, kubernetes, evaluation, sandbox]
icon: ""
