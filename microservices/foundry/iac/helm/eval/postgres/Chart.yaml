apiVersion: v2
name: postgres
description: |
  Postgres (eval-set metadata) for foundry-eval. CloudNativePG-backed HA.
  Per ADR-0024 (eval harness) + ADR-0131 (per-microservice flat layout).
type: application
version: 1.0.0
appVersion: "16.2"
dependencies:
  - name: cloudnative-pg
    version: ~0.21.0
    repository: https://cloudnative-pg.github.io/charts
home: https://oyatie.com
maintainers:
  - name: ops-sre-reliability
    email: ops-sre-reliability@oyatie.internal
keywords: [foundry-eval, postgres, eval-set-registry, ha]
