apiVersion: v2
name: oya-foundry-runtime-redis
description: Redis 7.4 OSS LTS cluster (6 shards × RF=2) for session-state hot tier per policy/runtime-isolation.md + capacity-model.md
type: application
version: 0.1.0
appVersion: "7.4.0"   # Redis LTS pin per docs/standards
maintainers:
  - name: axis-foundry-runtime
    email: axis-foundry-runtime@oyatie.dev
dependencies:
  - name: redis-cluster
    version: "^11.0.0"  # bitnami/redis-cluster chart version pin
    repository: https://charts.bitnami.com/bitnami
