apiVersion: oyatie.dev/v1
kind: SecretBinding
metadata:
  name: oya-crm
spec:
  openbaoPaths:
    - secret/{{ tenant_id }}/crm/database
    - secret/{{ tenant_id }}/crm/signing
  maxTtlSeconds: 60
  auditEventClass: EVT-CRM-ACCOUNT_MASTER-SECRET_LEASE
