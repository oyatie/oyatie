apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: consent-graph-base
namespace: consent-graph
commonLabels:
  app.kubernetes.io/part-of: consent-graph
helmCharts:
  - name: consent-graph
    repo: file://../../helm/consent-graph
    releaseName: consent-graph
    namespace: consent-graph
    valuesFile: ../../helm/consent-graph/values.yaml
