apiVersion: v2
name: oya-workflow-engine-postgres
description: Postgres + Citus distributed cluster for workflow-engine durable run state, outbox, spec versions, audit-chain seals per ADR-0131 + ADR-0103
type: application
version: 0.1.0
appVersion: "16.0"  # Postgres 16 LTS line per docs/standards/workflow-engine-substrate.md
maintainers:
  - name: axis-workflow
    email: axis-workflow@oyatie.dev
dependencies:
  - name: citus
    version: "12.1.x"
    repository: https://citusdata.github.io/charts
