apiVersion: v2
name: oya-workflow-studio-visual-canvas-rest
description: |
  Workflow Studio visual-canvas REST surface — Helm chart per ADR-0131
  per-microservice flat layout. Houses the editor REST endpoints
  (open/close session, save, load, jurisdiction switch, LLM-assist).
type: application
version: 0.1.0
appVersion: "1.0.0"  # workflow-studio µservice version
maintainers:
  - name: axis-workflow + council-design-system
    email: axis-workflow@oyatie.dev
