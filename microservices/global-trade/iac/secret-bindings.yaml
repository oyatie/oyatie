apiVersion: oyatie.dev/v1
kind: SecretBinding
metadata:
  name: oya-global-trade
spec:
  openbaoPaths:
    - secret/{{ tenant_id }}/global-trade/database
    - secret/{{ tenant_id }}/global-trade/signing
  maxTtlSeconds: 60
  auditEventClass: EVT-GLOBAL_TRADE-CUSTOMS_DECLARATION-SECRET_LEASE
