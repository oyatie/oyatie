apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: finops-portal-default-deny
  namespace: oya-finops-portal
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
  ingress: []
  egress: []
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: finops-portal-allow-substrate
  namespace: oya-finops-portal
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: finops-portal
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              oyatie.io/substrate-tier: "true"
      ports:
        - { protocol: TCP, port: 443 }
        - { protocol: UDP, port: 443 }
  egress:
    - to:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: oya-observability } }
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: oya-audit-chain } }
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: oya-cloud-secrets } }
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: oya-tenancy } }
      ports:
        - { protocol: TCP, port: 443 }
        - { protocol: UDP, port: 443 }
