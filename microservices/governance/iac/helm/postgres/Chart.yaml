# Canonical-base: microservices/governance/iac/helm/_oya-helpers (SWEEP-I Slice 2 per ADR-0064)
apiVersion: v2
name: postgres
description: |
  HA Postgres for governance µservice (lane-state + Finding metadata).
  Patroni-managed; 1 primary + 2 sync replicas; WAL-G PITR to S3.
  Per microservices/governance/capacity-model.md §"Worked example".
  Per microservices/governance/threat-model.md §T-T-03 (Finding tampering protections).
type: application
version: 0.1.0
appVersion: "16.2"
keywords:
  - oyatie
  - governance
  - postgres
  - patroni
home: https://github.com/oyatie/oyatie
maintainers:
  - name: ops-sre-reliability
    email: ops-sre-reliability@oyatie.example
  - name: axis-foundry
    email: axis-foundry@oyatie.example
dependencies:
  - name: postgres-operator
    version: "1.10.x"
    repository: "https://opensource.zalando.com/postgres-operator/charts/postgres-operator/"
annotations:
  category: database
  licenses: MIT
