[package]
name = "oya-authn-device-firmware"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
# Phase-1: NO direct deps. OpenSK is vendored separately (tools/opensk-vendored/).
# Phase-2 will add: opensk-vendored bridge crate + Nordic nRF52840 HAL + embedded-hal.
