apiVersion: v1
kind: ServiceAccount
metadata:
  name: oya-sites
  namespace: sites
  labels:
    microservice: sites
  annotations:
    # OpenBao Kubernetes auth binding for secret reference resolution
    openbao.oyatie.dev/role: sites
