apiVersion: v2
name: oya-audit-chain-postgres
description: Postgres HA primary + replica for audit-chain WAL + index. Per ADR-0117 + ADR-0028.
type: application
version: 0.1.0
appVersion: "16.2"  # Postgres LTS pin per docs/standards/audit-chain.md
maintainers:
  - name: axis-audit-chain
    email: axis-audit-chain@oyatie.dev
dependencies:
  - name: postgresql-ha
    version: "^14.0.0"
    repository: https://charts.bitnami.com/bitnami
