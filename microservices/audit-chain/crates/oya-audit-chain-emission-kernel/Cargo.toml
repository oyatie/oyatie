[package]
name = "oya-audit-chain-emission-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Wave 15-IMPL-truth-up scaffold (2026-05-21):
# Port-only crate per IP-003. Pure types and traits; no I/O.
# Full extraction from oya-audit-chain-domain tracked under IP-003.

[dependencies]

[lib]
name = "oya_audit_chain_emission_kernel"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
