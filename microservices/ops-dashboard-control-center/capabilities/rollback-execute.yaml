schema_version: "1.0"
name: rollback-execute
tier: T3
eu_ai_act_risk_class: high
description: Authorize rollback execution with target revision and rationale.
authz:
  cedar_policy: microservices/ops-dashboard-control-center/policy/cedar/operator-actions.cedar
  requires_mfa: true
  requires_idempotency_key: true
audit_chain:
  required: true
  seal_event: OpsRollbackExecute
