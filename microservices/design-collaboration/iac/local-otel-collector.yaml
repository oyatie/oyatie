apiVersion: opentelemetry.io/v1beta1
kind: OpenTelemetryCollector
metadata:
  name: design-collaboration-local-otel
  labels:
    service: design-collaboration
    suite: local-operational-primitive
    adr: ADR-0130
spec:
  mode: deployment
  config:
    receivers:
      otlp:
        protocols:
          grpc: {}
          http: {}
    processors:
      attributes/local-domain:
        actions:
          - key: service.name
            value: design-collaboration
            action: upsert
          - key: oyatie.domain_object
            value: DesignFile
            action: upsert
    exporters:
      otlphttp:
        endpoint: http://observability-ingest.observability.svc:4318
    service:
      pipelines:
        metrics:
          receivers: [otlp]
          processors: [attributes/local-domain]
          exporters: [otlphttp]
