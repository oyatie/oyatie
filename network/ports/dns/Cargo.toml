[package]
name = "network-dns"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
network-domain = { path = "../../core/domain" }
data-boundary-kernel = { path = "../../../base/data-boundary-kernel" }

[dev-dependencies]
network-residency = { path = "../../core/residency" }

[lib]
name = "network_dns"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
