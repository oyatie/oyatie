[package]
name = "network-transport-profile"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "network_transport_profile"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json = { workspace = true }
