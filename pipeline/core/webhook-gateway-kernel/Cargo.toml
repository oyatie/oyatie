[package]
name = "ci-webhook-gateway-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# CI webhook gateway kernel — pure-Rust contracts for the GitHub→Jenkins→GitHub
# commit-status bridge (ADR-0112, ADR-0359, ADR-0361, ADR-0363, ADR-0387).
# Stage-4 RED: data types + trait seams + failing tests. No I/O deps.

[lib]
name = "ci_webhook_gateway_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
