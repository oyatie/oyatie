[package]
name = "ci-planning-projection"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
bounded_context = "cloud-ci"
kind = "shared"
layer = "facade"
purpose = "Pure deterministic planning projections materialized by the cloud-ci controller."

[lib]
name = "ci_planning_projection"
path = "src/lib.rs"
doctest = false

[dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
