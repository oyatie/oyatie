rust_library(
    name = "ci-endpoint-authorization-coverage",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_endpoint_authorization_coverage",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

rust_binary(
    name = "cloud-ci-authz-coverage-app-bin",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate_root = "src/main.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-endpoint-authorization-coverage",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "ci-endpoint-authorization-coverage-unittest",
    srcs = glob(["src/**/*.rs", "**/*.json"]),
    crate = "ci_endpoint_authorization_coverage",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)

# issue #770 cloud-ci-authz-coverage: live-corpus self-test over the real axum Router surfaces plus
# RED/GREEN fixtures. The live leg walks the candidate tree, asserts the gate is born-blocking green
# today (every NEW control plane authz-covered; pre-existing unauthenticated surfaces frozen in the
# baseline), and confirms the real intelligence/adapters/rest + tenancy/facade/tenant-lifecycle-app
# authz patterns are recognized as COVERED. The RED fixture proves the gate genuinely FAILS on a new
# unauthenticated mutating router, and the health-endpoint case proves an allowlisted read is exempt.
rust_test(
    name = "ci-endpoint-authorization-coverage-gate",
    srcs = glob(["tests/**/*.rs", "**/*.json"]),
    crate = "ci_endpoint_authorization_coverage_gate",
    crate_root = "tests/authz_coverage.rs",
    visibility = ["PUBLIC"],
    deps = [
        ":ci-endpoint-authorization-coverage",
        "third-party//:serde_json",
    ],
)
