[package]
name = "ci-tide-kernel"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# ci tide kernel — pure-domain eligibility predicate + types for the
# tide merge-queue (ADR-0513 Phase 2). No I/O, no async, no network.
# Owns: TideConfig, PullRequest, CommitStatusState, Review, is_mergeable,
# ForgeClient trait seam.

[lib]
name = "ci_tide_kernel"
path = "src/lib.rs"
doctest = false

[dependencies]
serde = { workspace = true }

[lints]
workspace = true
