# ci/adapters: git + manifest implementations of the ci/ports move-aware path-resolution contract.
rust_library(
    name = "ci-path-resolver-adapters",
    srcs = glob(["src/**/*.rs"]),
    crate = "ci_path_resolver_adapters",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//pipeline/ports/path-resolver:ci-path-resolver-ports",
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "ci-path-resolver-adapters-unittest",
    srcs = glob(["src/**/*.rs"]),
    crate = "ci_path_resolver_adapters",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "//pipeline/ports/path-resolver:ci-path-resolver-ports",
        "third-party//:serde_json",
    ],
)
