[package]
name = "data-analytics-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Analytics composition-root binary: wires kernel + adapter + domain + usecase + api into a runnable µservice process. Layer 12 per ADR-0083. Static-musl binary; distroless image."

[package.metadata.oyatie]
microservice = "analytics"
bounded_context = "analytics"
layer = "app"
owner_team = "council-analytics"
adr_anchors = ["ADR-0083", "ADR-0090", "ADR-0039", "ADR-0193"]
ip_anchor = "IP-015"
container_image = "ghcr.io/oyatie/analytics:<sha> (non_claim: not yet built)"
data_classification = "INTERNAL_ONLY"

[lib]
name = "data_analytics_app"
path = "src/lib.rs"
doctest = false

[[bin]]
name = "data-analytics-app"
path = "src/main.rs"

[dependencies]
shared-olap-client-kernel = { path = "../../core/olap-client-kernel" }
data-analytics-api = { path = "../../ports/analytics-api" }
data-analytics-domain = { path = "../../core/analytics-domain" }
data-analytics-usecase = { path = "../../core/analytics-usecase" }


serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
clap = { workspace = true }

[lints]
workspace = true
