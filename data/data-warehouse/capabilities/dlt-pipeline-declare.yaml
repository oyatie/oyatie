apiVersion: oyatie.dev/v2
kind: CapabilityRecord
metadata:
  name: data-warehouse-dlt-pipeline-declare
  service: data-warehouse
  wave: Wave-15A
spec:
  businessCapability: lake-table
  capability: dlt-pipeline-declare
  tier: product
  bindingAdrs:
    [ADR-0105, ADR-0145, ADR-0254, ADR-0329, ADR-0331]
  benchmarks: ["Databricks Delta Live Tables", "Snowflake Dynamic Tables"]
  expectationFamily: [drop, fail, quarantine]
  tenantScope:
    requiredFields: [tenant_id, principal_id, audience_type, purpose, data_class]
  tenantClassEnvelope:
    demo_trial:
      allowed: false
      refusalCode: tenant_class_cap_exceeded
    paid:
      allowed: true
      billingComponents:
        - compute_credits
  policyEvaluationMode: caller_side_library_first
  cedarFragments: []
  sloBindings:
    - dlt-pipeline-freshness.openslo.yaml
  ipBindings: [IP-036]
