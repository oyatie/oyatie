# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-agent-gateway-rest
  microservice: ontology
  bounded_context: agent-gateway
  layer: rest
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = agent-gateway; layer = rest.
    LLM tool-call ingress; autonomy_tier ceiling enforced; tier-filtered
    tool-call result projection; per-session rate-limit; circuit breaker.
    Auto-generates OpenAI tool-spec from Function Type schemas.
  imports: [oya-ontology-agent-gateway-api, oya-ontology-agent-gateway-usecase,
            oya-ontology-cedar-fragment-coverage-usecase]
  external_deps: [tower, axum, jsonwebtoken]
  related_ip: IP-012
  related_adrs: [ADR-0107, ADR-0140 (retired per ADR-0145)]
