# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-object-type-registry-domain
  microservice: ontology
  bounded_context: object-type-registry
  layer: domain
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = object-type-registry; layer = domain.
    Pure schema-evolution logic, pillar/tier inference, jurisdiction overlay merge,
    property-tier monotonicity check. Imports kernel only.
  imports: [oya-ontology-object-type-registry-kernel]
  test_coverage_threshold: {line: 95, branch: 90}
  related_ip: IP-002
