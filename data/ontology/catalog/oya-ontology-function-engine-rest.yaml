# Canonical-base: specs/catalog/canonical-crate-record-schema.json (SWEEP-I Slice 6 per ADR-0064)
apiVersion: catalog.oya.dev/v1
kind: Crate
metadata:
  name: oya-ontology-function-engine-rest
  microservice: ontology
  bounded_context: function-engine
  layer: rest
  status: pending
spec:
  bnf_v4_1_justification: |
    microservice = ontology; bc-tokens = function-engine; layer = rest.
    HTTP handler/route layer for Function evaluation; Cedar middleware on every
    request; tier-filter projection enforced; per-tenant rate-limit; OpenAPI
    annotated via utoipa.
  imports: [oya-ontology-function-engine-api, oya-ontology-function-engine-usecase,
            oya-ontology-cedar-fragment-coverage-usecase]
  openapi_path: data/ontology/contracts/openapi/ontology.yaml
  related_ip: IP-014
  related_adrs: [ADR-0056, ADR-0105, ADR-0107]
