apiVersion: openslo/v1
kind: SLO
metadata:
  name: data-pipeline-deadletter-rate
  labels:
    service: data-pipeline
    suite: local-operational-primitive
    adr: ADR-0130
    promotion_gate: ADR-0139
spec:
  service: data-pipeline
  description: Dead-letter rate below pipeline threshold for source ingest, transform execution, data-quality measurement, null-rate controls, dead-letter replay, and lineage operations.
  indicator:
    metadata:
      name: deadletter-rate
      labels:
        domain_object: PipelineRun
        operator_audience: DATA_PIPELINE_OPERATOR
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(data_pipeline_deadletter_rate_good_total[30d])) by (tenant_id, cell_tier)
        total:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(data_pipeline_deadletter_rate_total[30d])) by (tenant_id, cell_tier)
  objectives:
    - displayName: Dead-letter rate below pipeline threshold
      target: 0.995
  timeWindow:
    - duration: 30d
      isRolling: true
