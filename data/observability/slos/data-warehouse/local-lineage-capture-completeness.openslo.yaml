apiVersion: openslo/v1
kind: SLO
metadata:
  name: data-warehouse-lineage-capture-completeness
  labels:
    service: data-warehouse
    suite: local-operational-primitive
    adr: ADR-0130
    promotion_gate: ADR-0139
spec:
  service: data-warehouse
  description: Lineage captured for refreshed partitions for warehouse pipeline scheduling, SLA-tier enforcement, dimensional completeness, query serving, and lineage operations.
  indicator:
    metadata:
      name: lineage-capture-completeness
      labels:
        domain_object: WarehouseDataset
        operator_audience: DATA_WAREHOUSE_OPERATOR
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(data_warehouse_lineage_capture_completeness_good_total[30d])) by (tenant_id, cell_tier)
        total:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(data_warehouse_lineage_capture_completeness_total[30d])) by (tenant_id, cell_tier)
  objectives:
    - displayName: Lineage captured for refreshed partitions
      target: 0.999
  timeWindow:
    - duration: 30d
      isRolling: true
