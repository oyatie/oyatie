apiVersion: openslo/v1
kind: SLO
metadata:
  name: data-warehouse-freshness
  labels:
    service: data-warehouse
    suite: local-operational-primitive
    adr: ADR-0130
    promotion_gate: ADR-0139
spec:
  service: data-warehouse
  description: Warehouse tables refreshed inside assigned SLA tier for warehouse pipeline scheduling, SLA-tier enforcement, dimensional completeness, query serving, and lineage operations.
  indicator:
    metadata:
      name: freshness
      labels:
        domain_object: WarehouseDataset
        operator_audience: DATA_WAREHOUSE_OPERATOR
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(data_warehouse_freshness_good_total[30d])) by (tenant_id, cell_tier)
        total:
          metricSource:
            type: prometheus
            spec:
              query: sum(rate(data_warehouse_freshness_total[30d])) by (tenant_id, cell_tier)
  objectives:
    - displayName: Warehouse tables refreshed inside assigned SLA tier
      target: 0.995
  timeWindow:
    - duration: 30d
      isRolling: true
