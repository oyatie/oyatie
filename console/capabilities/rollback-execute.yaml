schema_version: "1.0"
name: rollback-execute
availability: "paid tenant_class always-on; demo_trial cap-bounded with step-up authorization"
risk_class: high
description: Authorize rollback execution with target revision and rationale.
authz:
  cedar_policy: console/policy/cedar/operator-actions.cedar
  requires_mfa: true
  requires_idempotency_key: true
audit_chain:
  required: true
  seal_event: OpsRollbackExecute
