schema_version: "1.0"
name: deployment-approve
availability: "paid tenant_class always-on; demo_trial cap-bounded with step-up authorization"
risk_class: high
description: Approve, hold, or reject a deployment progression.
authz:
  cedar_policy: console/policy/cedar/operator-actions.cedar
  requires_mfa: true
  requires_idempotency_key: true
audit_chain:
  required: true
  seal_event: OpsDeploymentApprove
