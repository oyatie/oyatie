schema_version: "1.0"
name: tenant-isolation-posture-query
availability: "paid tenant_class always-on; demo_trial read-only"
risk_class: limited
description: Read tenant isolation and quota posture with evidence refs only.
authz:
  cedar_policy: console/policy/cedar/operator-actions.cedar
  requires_mfa: true
  requires_idempotency_key: true
audit_chain:
  required: true
  seal_event: OpsTenantIsolationPostureQuery
