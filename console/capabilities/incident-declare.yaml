schema_version: "1.0"
name: incident-declare
availability: "paid tenant_class always-on; demo_trial cap-bounded"
risk_class: limited
description: Declare an incident and emit a sealed incident event.
authz:
  cedar_policy: console/policy/cedar/operator-actions.cedar
  requires_mfa: true
  requires_idempotency_key: true
audit_chain:
  required: true
  seal_event: OpsIncidentDeclare
