schema_version: "1.0"
name: cluster-health-query
availability: "paid tenant_class always-on; demo_trial read-only"
risk_class: minimal
description: Query observed cluster, node, mesh, bootstrap, and restore health.
authz:
  cedar_policy: console/policy/cedar/operator-actions.cedar
  requires_mfa: true
  requires_idempotency_key: true
audit_chain:
  required: true
  seal_event: OpsClusterHealthQuery
