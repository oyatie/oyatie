[package]
name = "check-high-risk-auto-decision-refusal"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure-fn kernel (port-in-kernel, ADR-0056). Reads YAML capability
# declarations and Cedar policy fragments handed in by a runner; returns
# exact "claim-without-impl" findings.

[dependencies]

[lints]
workspace = true
