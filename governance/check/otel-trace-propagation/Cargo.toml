[package]
name = "check-otel-trace-propagation"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Skeleton kernel authored 2026-05-18 per ADR-0145 Invariant 2 enforcement.
# Validates that each µservice's inter-µservice call sites propagate the
# W3C traceparent header. Currently runs in DEFERRED (advisory) mode in
# the dispatcher; impl tracked under
# registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-otel-propagation-validator.

[dependencies]

[lib]
name = "check_otel_trace_propagation"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
