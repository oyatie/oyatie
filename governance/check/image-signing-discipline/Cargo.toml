[package]
name = "check-image-signing-discipline"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure-fn kernel (port-in-kernel, ADR-0056). Reads workflow yaml +
# µservice helm values; enforces cosign signing + Trivy scanning per
# ADR-0146 + ADR-0039.

[dependencies]

[lib]
name = "check_image_signing_discipline"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
