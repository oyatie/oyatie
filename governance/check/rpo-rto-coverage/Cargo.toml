[package]
name = "check-rpo-rto-coverage"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure-fn kernel (port-in-kernel, ADR-0056). Reads backfill-replay.md
# fronts-matter handed in by the runner; enforces ADR-0152 five-tier
# RTO model declaration.

[dependencies]

[lib]
name = "check_rpo_rto_coverage"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
