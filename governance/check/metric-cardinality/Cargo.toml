[package]
name = "check-metric-cardinality"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Pure-fn kernel (port-in-kernel, ADR-0056). Reads ServiceMonitor
# yaml + PrometheusRule yaml; flags high-cardinality labels not
# dropped via metricRelabelings per ADR-0151.

[dependencies]

[lib]
name = "check_metric_cardinality"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
