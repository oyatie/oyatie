[package]
name = "check-audit-chain-seal-coverage"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Skeleton kernel authored 2026-05-18 per ADR-0145 Invariant 1 enforcement.
# Verifies every state-changing capability declares an audit-chain seal
# event in its manifest's `audit_chain.seal_events`. Runs in DEFERRED
# (advisory) mode in the dispatcher; impl tracked under
# registry/placeholder-debt/adr-follow-ups.yaml#adr-0145-audit-chain-seal-validator.

[dependencies]

[lib]
name = "check_audit_chain_seal_coverage"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
