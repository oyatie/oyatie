[package]
name = "check-tenant-cost-labels-coverage"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

# Advisory gate per ADR-0199 D-2 — scans rendered Helm output for the
# canonical tenant cost labels and reports per-µservice coverage.
# Authored 2026-05-18.

[dependencies]

[lib]
name = "check_tenant_cost_labels_coverage"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
