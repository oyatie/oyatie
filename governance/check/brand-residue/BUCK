rust_library(
    name = "check-brand-residue",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "check_brand_residue",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        # forbidden_vocab.rs (the shrink-only ratchet census) is consumed by the
        # accounting-registry producer over the live corpus shape; serde_json carries it.
        "third-party//:serde_json",
    ],
)

rust_test(
    name = "check-brand-residue-unittest",
    srcs = glob(["src/**/*.rs", "migrations/**/*.sql", "**/*.cedar", "**/*.sql", "**/*.json", "**/*.toml", "**/*.yaml", "**/*.yml", "**/*.proto", "**/*.html", "**/*.css", "**/*.txt"]),
    crate = "check_brand_residue",
    crate_root = "src/lib.rs",
    visibility = ["PUBLIC"],
    deps = [
        "third-party//:serde_json",
    ],
)
