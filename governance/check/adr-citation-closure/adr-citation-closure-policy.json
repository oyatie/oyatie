{
  "_comment": "ADR citation closure gate (DATA, not code). A mechanical de-stale pass rewrote ADR path citations onto the ten live apex ADRs under docs/decisions/. Every rewritten path is a VALID PATH TO AN EXISTING FILE, so a link checker passes all of them; many nonetheless point at an apex that never claimed the ADR the surrounding sentence is about. Only checking MEANING against an independent supersession oracle catches that. The oracle is built from BOTH frontmatter sides — apex `supersedes:` and member `superseded_by:` — because supersession CHAINS through archived intermediates (ADR-0110 -> ADR-0363 -> ADR-0701; ADR-0349 -> ADR-0515 -> ADR-0700) and because members exist that declare a successor no apex acknowledges. ONLY those frontmatter keys are parsed: bodies mention ADR ids constantly, one archived ADR even carries a fenced `superseded_by:` block naming itself, and grepping whole files produces false matches. ALL repo-specifics are DATA — another repo adopts this gate by repointing these values.",
  "gate_id": "governance-check-adr-citation-closure",
  "schema_version": "1.0.0",
  "min_adr_records": 400,
  "min_live_apexes": 10,
  "min_archived_with_successor": 380,
  "min_citation_lines": 1000,
  "min_authority_surfaces": 3,
  "authority_surfaces": [
    "CLAUDE.md",
    "AGENTS.md",
    "docs/AGENTS.md"
  ],
  "exempt_path_prefixes": [
    "docs/adr-archive",
    "docs/decisions/_disposition",
    "governance/check/adr-citation-closure"
  ],
  "scan_extensions": [
    ".md",
    ".json",
    ".jsonl",
    ".yaml",
    ".yml",
    ".toml",
    ".rs",
    ".cedar",
    ".txt"
  ],
  "measured": {
    "_comment": "ENFORCED shrink-only ceilings, pinned by EQUALITY in live_tree_findings_equal_the_frozen_ceilings. Originally recorded as inert measurements with the gate deliberately red; that could not land, because affected-set test-health counts any failing test as a regression against a baseline of zero, so a permanently-red gate is correct and unmergeable. Equality rather than <= is deliberate and matches ci/facade/corpus-index-coverage: a NEW finding exceeds the ceiling and fails, and a REPAIRED finding falls below it and also fails, forcing the ceiling down in the same change. The census values are the anti-vacuity anchor — a repaired corpus and a collapsed walk both report fewer findings, and only the census distinguishes them. 2026-08-09 (ADR-0710), CORRECTING the provenance recorded here on 2026-08-08: the frozen VALUES below were right, but the trail written beside them was not reproducible. It said one file was added, citation_lines went to 8897, and files_scanned was unchanged at 16519; the branch adds TWO files and freezes 8899 / 16520, so the trail could not be replayed to distinguish a legitimate corpus change from an accidental re-freeze. Re-measured and re-attributed, term by term. files_scanned 16518 -> 16520 (the 16519 -> 16521 originally written here was against the pre-restack base; see _restack_remeasure_2026_08_09 for the base move): the branch adds exactly two tracked files, docs/decisions/ADR-0710-kubernetes-admission-substrate-is-the-api-server.md and docs/ideas/sovereign-confidential-kubernetes.md, both carrying a scanned extension and neither under an exempt prefix, so the TRACKED-file delta (+2, zero deletions) equals the SCANNED-file delta. citation_lines 8896 -> 8899: +3, and NONE of the three is in the two added files. A line is counted only when it writes a decisions/ADR-NNNN PATH, or names an ADR on one of the three authority_surfaces - the added ADR's own prose names ADR-0183/0338/0379/0701 as BARE ids on ordinary pages and therefore contributes ZERO. The three are MODIFIED generated/pointer faces, each writing the new ADR's path once: the docs/ADR-INDEX.md table row, the docs/machine-readable/decisions.json record path, and the specs/root-hub-pointers.json current_path. adr_records 458 -> 459 and live_apexes 10 -> 11: the single new file under docs/decisions/, which is the live-apex directory (live is a directory property here, not a frontmatter status - ADR-0710 is Proposed). THE ADD-VERSUS-NARROWING CROSS-CHECK, which is the only thing that separates corpus growth from a collapsed scan, since both shrink or move numbers: every OTHER pinned term held its value, all seven finding counts included - archived 448, archived_with_successor 425, rejected 16, mismatch 252, dangling_path 2002, rejected_authority 3, duplicate_id 0, asymmetry 29, unresolvable 8, ambiguous 2, scan_vacuous 0. A narrowed scan moves those too; an add does not. This is corpus growth, not a finding repair. The 2026-08-09 review-response commit on this branch is prose-only across the ADR, the idea page and the root-hub purpose string: it adds bare ADR mentions on non-authority pages and adds and removes zero decisions/ADR- path lines, so it moves no term here and needs no re-freeze - verified by re-running the gate, not by inspection.",
    "run_at": "2026-08-09T12:50Z",
    "adr_records": 460,
    "live_apexes": 12,
    "archived": 448,
    "archived_with_successor": 425,
    "rejected": 16,
    "citation_lines": 8996,
    "files_scanned": 16621,
    "adr_citation_closure_mismatch": 252,
    "adr_citation_dangling_path": 2295,
    "adr_citation_rejected_authority": 3,
    "adr_duplicate_id": 0,
    "adr_closure_asymmetry": 29,
    "adr_closure_unresolvable": 8,
    "adr_closure_ambiguous": 2,
    "spot_verified_by_hand": [
      "CLAUDE.md:45 cites ADR-0111 as merge-queue authority; docs/adr-archive/ADR-0111-*.md frontmatter says status: Rejected",
      "CLAUDE.md:53 and :59 cite ADR-0709 on lines naming ADR-0515; ADR-0515 declares superseded_by [ADR-0700], so the closure is ADR-0700",
      "audit/README.md:10 links ADR-0349 to ADR-0709; ADR-0349 declares superseded_by [ADR-0515] and ADR-0515 declares superseded_by [ADR-0700]",
      "docs/AGENTS.md:62 cites ADR-0347 as operating-contract doctrine; ADR-0347 frontmatter says status: Rejected"
    ],
    "_review_remeasure_2026_08_08": "Detection was widened by review repair, so these ceilings were re-frozen in the same change. rejected_authority 2 -> 3: the rule read only `context`, but scan_line moves an id out of `context` when the line ALSO writes it as a decisions/ path, so `[ADR-0347](decisions/ADR-0347-...)` on docs/AGENTS.md — the surface that MANDATES it as active doctrine while its frontmatter says Rejected — was invisible. It now reads both halves. adr_citation_dangling_path 1979 is a NEW code: a decisions/ path naming a non-apex used to be a silent `continue`, and docs/decisions/ holds only ADR-0700..0709, so every citation of an archived id by that path is a broken link. Composition, measured at FILE level rather than finding level: 51 .md files carry such citations against 6 .rs, 7 .json and a handful of others, so it is overwhelmingly real broken links in doctrine surfaces rather than test fixtures. An earlier draft gave a finding-level '161 vs 9' that review flagged as unreproduced; the file-level figures above are re-derivable with `git grep -l 'docs/decisions/ADR-0[0-6][0-9][0-9]'`; no blanket .rs exemption was added, because that would also blind the gate to genuine references in doc comments. archived_with_successor 423 -> 425 because the floor now counts BOTH oracle sides, which is what it claims to guard. files_scanned 16496 -> 16512: the census moved onto `git ls-files` (the boundary every other gate uses) and stopped skipping the 15 tracked symlinks that entry.file_type() never reported as files. citation_lines is unchanged at 8899, which is the cross-check that the boundary move did not widen the corpus. mismatch/asymmetry/unresolvable/ambiguous are all unchanged.",
    "_restack_remeasure_2026_08_09_pr1621": "Restacked again onto dev at 885794461 (PR #1621, the port-engine W0 skeleton and the first-ever top-level build/ tree), which moved this pin under the branch a second time. files_scanned 16520 -> 16526 is the ONLY term the restack touched, and it is again a sum of two independent moves attributed separately: the BASE moved 16518 -> 16524 and this BRANCH still adds its own +2. The base move is +6 and is an ADD, not a scan widening — PR #1621 adds NINE tracked files and deletes none, of which SIX carry a scan_extension (build/port-engine/core/port-engine-kernel/{Cargo.toml,src/lib.rs,tests/neutrality.rs,tests/seams.rs}, registry/catalog/port-engine-kernel.yaml and docs/programs/k8s-port/operations/W0-B-20260809-port-engine-kernel.md) and THREE do not (two OWNERS files and one BUCK file, all extensionless); none sits under an exempt_path_prefix. Scanned-add delta 6 equals the observed base delta of 6, so tracked and scanned agree. READ FROM THE GATE, not computed: the assert printed 'files_scanned: observed 16526, frozen 999999' against a deliberately wrong pin, and the same tree then passed 10/10 with 16526 frozen. THE CROSS-CHECK that rules out a narrowing: citation_lines 8899, adr_records 459, live_apexes 11 and every finding count (mismatch 252, dangling_path 2002, rejected_authority 3, duplicate_id 0, asymmetry 29, unresolvable 8, ambiguous 2, scan_vacuous 0) are UNCHANGED and were re-asserted by equality on the rebased tree — a narrowed walk moves those too, an add does not. citation_lines is deliberately NOT re-frozen: the six added scanned files name ADR ids as BARE ids in doc comments and none is one of the three authority_surfaces, so they write zero decisions/ADR-NNNN path lines and contribute zero citation lines.",
    "_dangling_vs_mismatch": "The two citation codes are disjoint by construction. adr_citation_dangling_path fires when the cited id is not a live apex — the path does not resolve, so there is nothing to compare meaning against. adr_citation_closure_mismatch fires only when the cited id IS a live apex and every resolvable ADR named on the line closes onto a different one. A bare id that is itself a live apex is no longer counted as evidence, because resolve() returns Live(itself) for it and that made every apex-to-apex cross reference an unsuppressable false accusation.",
    "_rebase_remeasure_2026_08_08": "Re-frozen against dev at 8097ce74c, which now carries PR #1617's 85 citation repairs. mismatch 340 -> 255 is exactly those 85, and the equality ratchet REFUSED the build until the ceiling followed — the ratchet working as designed on its first real occasion. rejected_authority 2 -> 3 is the review fix landing: the gate now sees docs/AGENTS.md:57, a Rejected ADR cited only inside a link path, which the old rule could not reach because scan_line moved the id out of `context` when the line also wrote it as a decisions/ path. That instance was findings 1+2 of the review and is the reason this gate needed fixing at all.",
    "_restack_remeasure_2026_08_09": "Re-frozen on rebase onto dev at 885794461 (PR #1621, the port-engine W0 skeleton that landed the first top-level build/ tree). files_scanned 16523 -> 16529; citation_lines stays 8898 and adr_records stays 458. The 16523 this branch carried was frozen against the PRE-#1621 dev of 16518 and was therefore wrong by the base's delta in the OPPOSITE direction from the one it was set for, which is the specific failure this re-derivation exists to prevent: a rebase that fixes only the text conflict leaves the pin red on a number and the redness reads as a new defect rather than as staleness. TWO INDEPENDENT DELTAS COMPOSE, and the pin refuses to land unless both are named. THE BASE contributes +6 files_scanned and 0 citation_lines: PR #1621 adds nine tracked files and deletes none, six of which carry a scan_extension (build/port-engine/core/port-engine-kernel/{Cargo.toml,src/lib.rs,tests/neutrality.rs,tests/seams.rs}, registry/catalog/port-engine-kernel.yaml, docs/programs/k8s-port/operations/W0-B-20260809-port-engine-kernel.md) and three of which do not (two OWNERS and one BUCK, all extensionless) — already measured and attributed in _port_engine_w0_add_2026_08_09 and _port_engine_w0b_journal_2026_08_09 below, and read here from dev's own frozen 16524 rather than re-derived from the merge diff. THIS PR contributes +5 files_scanned and +2 citation_lines: the five census records under docs/programs/k8s-port/census/ (concurrency, defer-panic-recover, interfaces, ownership-escape, reflect) are the only files it adds, all five carry .md, and exactly two of them write a `decisions/ADR-NNNN` PATH — defer-panic-recover.md:15 and interfaces.md:29, both citing the live apex ADR-0704. The other three name ADR ids only as bare `ADR-NNNN` in prose, and a census document is not one of the three authority_surfaces, so a bare id there is not a CitationLine (tests/adr_citation_closure.rs:228) and those three contribute zero. 16524 + 5 = 16529 and 8896 + 2 = 8898. CROSS-CHECK that this is an ADD and not a scan narrowing, which matters because a narrowed walk and a genuine add both move this one number and only one of them is legitimate: the tree delta against dev is exactly five additions and zero deletions or renames, reproducing the scan predicate (tracked files, minus dot/buck-out/target/node_modules/vendor directory components, minus exempt_path_prefixes, filtered to scan_extensions) over dev and over the rebased tree moves the scanned set by exactly +5, and the tracked-add delta, the scanned-add delta and the observed census delta therefore all agree at 5. THE SECOND CROSS-CHECK is that nothing else moved: adr_records 458, mismatch 252, dangling_path 2002, rejected_authority 3, duplicate_id 0, asymmetry 29, unresolvable 8, ambiguous 2 and scan_vacuous 0 are every one identical to the frozen value. A narrowing moves those too; a pure add of documents that carry two citations between them moves only the file count and those two citation lines. The frozen values were confirmed by RUNNING the gate on the rebased tree — under an equality pin a green run is exactly the assertion that observed == frozen, and a wrong value prints 'observed N, frozen M' before any finding count is compared — not by carrying the old freeze forward; the arithmetic is recorded only because the pin demands the move be attributed, and an unattributed re-freeze is precisely the false green it exists to catch.",
    "_corpus_remeasure_2026_08_09": "files_scanned 16519 -> 16518 on the wave25/26 residual consolidation. This is a GENUINE TRACKED-FILE DELETION, not a scan narrowing, and the distinction was established by measurement rather than asserted. Tracked files move +16 across the change (19 added, 3 deleted, 4227 renamed; renames are net-zero), while the SCANNED set moves -1, so the two disagree and the disagreement is the thing that needed explaining. Reproducing the scan predicate (excluded dot/buck-out/target/node_modules/vendor dirs, exempt_path_prefixes, scan_extensions) over both trees isolates it to exactly one file: oya/oya-flags/README.md is deleted and was in the scanned set, while all 19 added files (BUCK, OWNERS) carry no scanned extension and no rename crosses the scan boundary. The deletion is a name collision, not a content loss: oya/oya-flags (the ADR-0481 OFREP server) and oya/feature-flags (the microservice doc set) both rehome into flags/, only one README.md can hold that name, and feature-flags won it. The server code survives at flags/core/server/ with ADR-0481 cited across ten files there. The dropped README was stale on arrival anyway - it documented oya-flags-kernel/rest/app crates that no longer exist under those names and prescribed `cargo check -p` commands that repo policy forbids, buck2 being canonical. citation_lines stays 8896 and adr_records stays 458, which is the cross-check that the corpus was not narrowed: a narrowing moves both terms, a single deletion moves only the file count. Every finding count is likewise unchanged (mismatch 252, dangling_path 2002, rejected_authority 3, asymmetry 29, unresolvable 8, ambiguous 2).",
    "_corpus_pin_2026_08_08": "The corpus census (files_scanned, citation_lines, adr_records) is now PINNED BY EQUALITY in live_tree_findings_equal_the_frozen_ceilings, and is asserted BEFORE any finding count. A high-effort review proved the gate could be turned green without repairing one citation: append two entries to exempt_path_prefixes, lower the two ceilings in the same file, and all ten tests passed while 281 findings silently left the enforced set. The corpus definition and the ceilings share this file, so a policy-only edit satisfied a ratchet meant to require repair, and the census values this block already called 'the anti-vacuity anchor' were asserted by NO test. Pinning them closes it: the same exploit now fails with 'files_scanned: observed 16170, frozen 16519' before a finding count is compared. Repair moves finding counts and leaves the corpus alone; narrowing moves the corpus. The ratchet can now tell them apart. files_scanned 16512->16519 and citation_lines 8895->8896 are the seven completion packets PR #1618 merged, re-frozen here in the same change the pin was added.",
    "_port_engine_w0b_journal_2026_08_09": "files_scanned 16523 -> 16524, and NOTHING else moved. ONE tracked .md file added: docs/programs/k8s-port/operations/W0-B-20260809-port-engine-kernel.md, the operations-journal entry the k8s-port program's own INDEX obliges each run to record. Tracked-add delta 1, scanned-add delta 1, observed census delta +1 — they agree, so this is an ADD and not a scan narrowing. citation_lines is deliberately NOT re-frozen and stays 8896, which is the cross-check rather than a coincidence: a .md file becomes a CitationLine only when a line writes a `decisions/ADR-NNNN` PATH, or when it names an id on one of the three authority_surfaces (tests/adr_citation_closure.rs:228). The journal is not an authority surface, and it cites ADR-0637/0638/0704/0083/0597/0562/0615 as BARE ids with the archive location spelled `docs/adr-archive/` — deliberately, because a decisions/ path to an archived id is precisely the adr_citation_dangling_path defect this gate counts, and an owed record should not pay for itself with a new finding. Every finding count is likewise unchanged and was re-run after the re-freeze rather than assumed: the ceilings assertion compares the census BEFORE any finding count, so a green run after moving only files_scanned is the proof that mismatch 252, dangling_path 2002, rejected_authority 3, duplicate_id 0, asymmetry 29, unresolvable 8, ambiguous 2 and scan_vacuous 0 all held.",
    "_port_engine_w0_add_2026_08_09": "files_scanned 16518 -> 16523, and NOTHING else moved. RE-MEASURED after PR #1620 merged, not recomputed: this branch first froze 16524 against the pre-#1620 dev of 16519, #1620 then moved dev to 16518, and the new value was taken from the gate's own assertion on the rebased tree rather than by adding a delta to either number. ATTRIBUTION, because a narrowed scan and a genuine add report the same direction in this one number and only one of them is legitimate: PR #1621 adds 8 tracked files and deletes none; 5 of the 8 carry a scan_extension (build/port-engine/core/port-engine-kernel/{Cargo.toml,src/lib.rs,tests/neutrality.rs,tests/seams.rs} and registry/catalog/port-engine-kernel.yaml) and 3 do not (two OWNERS files and one BUCK file, both extensionless). Tracked-add delta 8, scanned-add delta 5, observed census delta +5 — the two agree, so this is an ADD and not a narrowing. THE CROSS-CHECK that rules out narrowing: a narrowed walk moves the other pinned terms and the finding counts too, and none of them moved. Measured on the rebased tree: citation_lines 8896, adr_records 458, mismatch 252, dangling_path 2002, rejected_authority 3, duplicate_id 0, asymmetry 29, unresolvable 8, ambiguous 2, scan_vacuous 0 — every one identical to the frozen value, and identical again to what the same walk reported before the rebase, which is the second cross-check that #1620's 4306-file reorg moved paths rather than the citation corpus. citation_lines is deliberately NOT re-frozen even though the five added files contain 23 lines naming an ADR id: a line becomes a CitationLine only when it writes a `decisions/ADR-NNNN` PATH, or when it names an id on one of the three authority surfaces (tests/adr_citation_closure.rs:228). The added files cite `ADR-0637`/`ADR-0704`/`ADR-0083` as bare ids in doc comments and are not authority surfaces, so they add zero citation lines — which is why 8896 holding is evidence rather than coincidence.",
    "_review_fixes_2026_08_08": "mismatch 255 -> 252: three FALSE POSITIVES removed, not three citations repaired. An `adr-archive/ADR-NNNN-...` link is the sentence stating that ADR is historical, but scan_line routed it to `context`, which evaluate reads as residual evidence of the sentence's subject. A correct line naming a historical ADR beside a live apex therefore fired a mismatch. Fixing it LOWERS the count, which is why the equality ratchet demanded a re-freeze in the same change. Also fixed here, latent rather than live: parse_supersession matched block-list items only at the exact prefix '  - ', so a supersedes/superseded_by written at four-space indent was silently dropped in a parser contracted to fail closed. No ADR uses that form today, so the counts are unchanged; three regression tests cover both fixes and the nested-key case that must stay ignored.",
    "_corpus_remeasure_2026_08_09_pr1623": "files_scanned 16524 -> 16525 and citation_lines 8896 -> 8897, attributed rather than assumed. RESTACKED onto dev at 885794461 (PR #1621, port-engine W0 skeleton), which added nine files — six of them carrying a scan_extension — and moved dev's own floor 16518 -> 16524 while leaving citation_lines at 8896. This branch's PREVIOUS freeze of 16519 was correct against the OLD base (1d3105277) and is simply wrong against this one, which is the whole reason a stale PR fails on a number rather than on its code. Both values below were RE-DERIVED from the rebased tree by running the gate and reading its assertion text, never by carrying a value forward or adding deltas; two runs were needed because files_scanned asserts before citation_lines and short-circuits, so citation_lines could not be observed until files_scanned was already green. ATTRIBUTION, dev's delta and this branch's delta kept separate: dev's +6 is fully absorbed into the 16524 baseline this branch rebased onto, and this branch contributes exactly +1 on top of it. This branch adds exactly one tracked file (ci/facade/affected-target-set/WALL-CLOCK-PROFILE.md, a .md outside all three exempt prefixes) and deletes none — tracked files count 18939 on dev against 18940 here, agreeing with the scanned delta of +1. The attribution is set-level rather than arithmetic: reproducing the scan predicate (excluded dot/buck-out/target/node_modules/vendor dir components, exempt_path_prefixes, scan_extensions) over both trees yields 16524 entries on origin/dev and 16525 here, and the set difference is exactly that one path — so the +1 is identified, not inferred from a subtraction. NOT A NARROWING: every other pinned term held at its frozen value once the two corpus terms were correct, and the ceilings assertion compares the census BEFORE any finding count, so the green run is the proof that adr_records 458, live_apexes 10, archived 448, archived_with_successor 425, rejected 16, mismatch 252, dangling_path 2002, rejected_authority 3, duplicate_id 0, asymmetry 29, unresolvable 8, ambiguous 2 and scan_vacuous 0 all still hold. A narrowed scan moves those too; an add moves only the corpus, which is what was observed. citation_lines +1 is SEPARATE from the file add and was located, not predicted: WALL-CLOCK-PROFILE.md line 104 is the ONLY line in the added file that writes a `decisions/ADR-NNNN` PATH, which is what makes a line a CitationLine outside the three authority surfaces — the bare `ADR-0554`/`ADR-0556` ids elsewhere in the same doc contribute zero, which is why 8896 -> 8897 rather than a larger move. NOTE FOR THE NEXT LANE: this branch's CI previously reported GREEN while carrying a red census — its affected-set derive resolved NO-GRAPH-TARGETS and never executed the census test, so the gate could only be re-frozen from a LOCAL buck2 run. That hole is CLOSED in this same PR, and closing it took TWO changes rather than the one first shipped. (1) `**/*.md` now seeds this gate in affected-set-policy.json instead of being declared inert. (2) That seed alone would still have missed the files that matter most here, because resolve() consulted a synthetic declaration ONLY when owner() was empty, and the ADR record set is buck2-OWNED — the corpus filegroup plus per-apex export_file entries mean 458 of this census's 463 owned `.md` inputs carry owners, and their rdeps cone reaches only ci-product-protocol-policy-gate, which asserts neither of these numbers. Synthetic declarations are therefore now ADDITIVE (they union with owner()), so an ADR-only diff selects this gate too. Without (2) the seed would have covered the unowned tail such as README.md while leaving every edit to the record set itself — the edits that move adr_records, archived, archived_with_successor and rejected — still able to skip the test that pins them.",
    "_corpus_remeasure_2026_08_09_pr1624_restack": "Restack onto origin/dev@550eae2e5 (#1623). files_scanned held at 16527 (dev 16525 + ADR-0710 adds). citation_lines observed 8900 vs prior freeze 8899: re-derived by running check-adr-citation-closure-gate live_tree_findings_equal_the_frozen_ceilings; files_scanned asserted first and passed, then citation_lines failed left=8900 right=8899. Re-freeze citation_lines 8899->8900 only; no finding counts moved.",
    "_integ_bundle_remeasure_2026_08_10_pr1642": "PR #1642 one-shot disjoint drain. Against the pre-catalog-row tip that observed files_scanned=16591 (frozen 16532), commit 2285a4df6 added four scanned registry/catalog/oya-cloud-os-*-domain.yaml rows (+4) and no other non-exempt tracked scanable adds, so freeze 16532 -> 16595. Finding ceilings held (mismatch 252, dangling_path 2002, rejected_authority 3, asymmetry 29, unresolvable 8, ambiguous 2); citation_lines/adr_records/live_apexes unchanged — corpus growth, not scan narrowing or finding repair. Re-frozen in the same change per gate contract.",
    "_integ_os_remeasure_2026_08_10_pr1643": "PR #1643 integ/os restack onto origin/dev@9755cf50e after #1642. Kept origin/dev policy tip (oyatie-o90 — never trust git merge of this file) and added +8 non-exempt scanned paths introduced by the kernel-ABI / os seam absorption (files_scanned 16595 -> 16603). THEN tip 0f6e5444d (absorb CI forever affected-set docs seeds) rewrote ci/facade/affected-target-set/tests/affected_set.rs and dropped exactly one `docs/decisions/ADR-NNNN` PATH string (path-count 5 -> 4 via `rg -c 'decisions/ADR-'`), so citation_lines 8902 -> 8901 with files_scanned held at 16603. Finding ceilings held (mismatch 252, dangling_path 2002, rejected_authority 3, asymmetry 29, unresolvable 8, ambiguous 2) — corpus citation-line loss from a test rewrite, not scan narrowing or finding repair. Re-derived by running root//governance/check/adr-citation-closure:check-adr-citation-closure-gate (observed citation_lines 8901, frozen 8902) and re-freezing in the same change.",
    "_a1_abi_matrix_remeasure_2026_08_11_pr1927": "PR #1927 A1 Asterinas ABI matrix scaffold. Against origin/dev tip that froze files_scanned=16603 (#1643), this branch adds 11 tracked files of which 9 carry a scan_extension (kernel/core/asterinas-abi-matrix/{Cargo.toml,src/lib.rs,matrix/abi-matrix-v0.1.0.json}; kernel/harness/asterinas-abi-probe/{Cargo.toml,src/lib.rs,src/main.rs,tests/probe_plan_integration.rs}; registry/catalog/kernel-asterinas-abi-{matrix,probe}.yaml) and 2 do not (two BUCK files). Scanned-add delta 9 equals observed 16603->16612. citation_lines held at 8901 and finding ceilings held (mismatch 252, dangling_path 2002, rejected_authority 3, asymmetry 29, unresolvable 8, ambiguous 2) — corpus growth from scaffold adds, not scan narrowing or finding repair. Re-frozen in the same change per gate contract; values attributed from the affected-set FULL-tier failure that printed files_scanned observed 16612 frozen 16603.",
    "_integ_specs_restack_remeasure_2026_08_11_pr1644_after_1659": "PR #1644 integ/specs restack onto origin/dev@4232ea2ed after #1659 land. Kept origin/dev policy tip including #1659/_a1_abi_matrix pin (oyatie-o90 — never trust git merge of this file; citation_lines 8901, files_scanned 16612, adr_records 459, live_apexes 11, dangling_path 2002) then re-applied #1644 tip. Live gate observed files_scanned 16616, citation_lines 8996, adr_records 460, live_apexes 12, adr_citation_dangling_path 2295 (friction-ledger + ADR-0711 corpus growth retained from tip). Other finding ceilings held (mismatch 252, rejected_authority 3, asymmetry 29, unresolvable 8, ambiguous 2). Re-frozen in the same change per gate contract.",
    "_integ_specs_remeasure_2026_08_10_pr1644": "PR #1644 integ/specs restack onto origin/dev@9a56538c7 after #1643 land. Kept origin/dev policy tip including #1643 census pin (oyatie-o90 — never trust git merge of this file; citation_lines 8901, files_scanned 16603 from _integ_os_remeasure_2026_08_10_pr1643) and re-applied the disjoint #1644 Swarm Delivery Law bootstrap + lifecycle-config absorption delta of +5/-6 non-exempt scanned paths (net -1): files_scanned 16603 -> 16602. citation_lines held at 8901. Finding ceilings held.",
    "_integ_specs_friction_retarget_2026_08_10_pr1644": "PR #1644 Codex P1: retarget ADR-0544 friction ledger off deleted .omc/ultragoal/friction-ledger.jsonl onto ci/facade/action-item-accounting/friction-ledger.jsonl (restore bytes + policy/baseline provenance). Tip already deleted the .omc scanned ledger in the prior bootstrap delta (files_scanned 16603->16602). This change adds exactly one non-exempt scanned path (the CI-colocated .jsonl), so files_scanned 16602->16603. citation_lines held at 8901; finding ceilings held. Envelopes adjunct_claims + _meta 1.16.15 same wave.",
    "_integ_specs_amendment_f_remeasure_2026_08_10_pr1644": "Amendment E at ec7e05ff553a added three citation-bearing corpus lines while the scan coverage stayed at 16607 files. The binding gate observed citation_lines 8993 versus the frozen 8990; all reported finding counts were unchanged. Re-freeze citation_lines 8990 -> 8993 in this same tip-owned Amendment F repair.",
    "_integ_specs_affected_set_remeasure_2026_08_10_pr1644": "At tip 572c9a253, the binding affected-set gate observed files_scanned 16607, citation_lines 8990, and adr_citation_dangling_path 2295. The retargeted CI-colocated ledger is now scanned rather than hidden under a dot directory, so its archived decisions/ paths enter the enforced corpus; the branch's added scanned governance/spec records and ADR-0711 account for the census growth. The scan configuration is unchanged. All other pinned finding ceilings were reported unchanged by the same gate.",
    "_integ_specs_codex_triage_remeasure_2026_08_11_pr1644": "PR #1644 Codex triage tip 74b8351e4: observed citation_lines 8996 vs frozen 8993 (+3) with files_scanned held at 16607. Attribution: root-hub entry_points rows for integ_branch_envelopes / naming_taxonomy / agentic_operating_patterns each write one decisions/ADR-0711 PATH (authority_adr), and Accepted promotion of the two policy specs adds no additional decisions/ path lines beyond those hub rows. Finding ceilings unchanged per equality ratchet; re-freeze citation_lines 8993 -> 8996 in the same change.",
    "_oci_oracle_harness_remeasure_2026_08_11_pr1931": "PR #1931 OCI executor differential-oracle harness scaffold. MERGE onto origin/dev@8aa043a03 after #1644 integ/specs restack (files_scanned=16616, citation_lines=8996, adr_records=460, live_apexes=12, dangling_path=2295) which already includes A1 (#1927/#1659) at 16612 plus #1644 corpus delta. This branch ADDS six tracked paths of which five carry a scan_extension (os/harness/oci-executor-oracle/{Cargo.toml,fixtures/cve-regression-obligations-v0.1.0.json,src/lib.rs,tests/oracle_scaffold.rs}; registry/catalog/os-oci-executor-oracle.yaml) and one does not (BUCK). Separately MODIFIED: Cargo.lock and this policy pin. OCI scanned-add delta +5 on the post-#1644 floor => observed freeze 16616->16621; citation_lines/finding ceilings inherited from #1644 confirmed by live gate (10/10). Keep _a1_abi_matrix and all _integ_specs_* notes. Re-frozen from live check-adr-citation-closure-gate observation in the same merge change per gate contract."
  },
  "product_contract": {
    "born_blocking": "Born BLOCKING and RED. Unlike a coverage ratchet there is no defensible ceiling here: a citation that resolves to the wrong apex is not debt to be burned down, it is a statement that is false today, and freezing a ceiling would licence the corpus to keep lying at its current rate. The gate reports every mismatch and fails until they are repaired.",
    "both_sides_oracle": "Successor edges are the UNION of member `superseded_by` and apex `supersedes` membership, walked transitively to a live apex. An apex-side-only map misses chained members; a member-side-only map misses the members no apex acknowledges. Either alone manufactures false accusations.",
    "false_positive_guard": "A citation line often names several ADRs — a range endpoint, a lineage, a see-also. A line is a defect only when at least one named ADR resolves AND NONE resolves to the cited apex. A line citing two apexes, or naming an ADR whose closure is ambiguous, is skipped rather than guessed.",
    "rejected_authority_scope": "The Rejected-ADR rule is scoped to `authority_surfaces` — the files that ARE the governance contract. Repo-wide it would fire on thousands of historical mentions, filename ranges and provenance notes, none of which claim authority. Scoped, it catches the real instances: root CLAUDE.md citing ADR-0111 (Rejected) as merge-queue authority.",
    "anti_vacuity": "Five floors. The first four guard the CENSUS (ADR records, live apexes, citation lines, authority surfaces). The third — min_archived_with_successor — guards the ORACLE, and is not redundant with the others: frontmatter parsing can silently return no edges while every ADR file and every citation is still found, leaving both censuses intact and the oracle empty. An empty oracle resolves nothing, so no citation can ever be called wrong, and the gate reports a flawless corpus. Only a floor on the edge count catches that.",
    "known_limitations": "Detection requires RESIDUAL evidence: a citation whose line no longer names any ADR carries nothing to check it against, and the de-stale pass rewrote the filename slug too. The measurable defect class is therefore a lower bound on the real one. Only supersession is modelled — `amends`/`amended_by` carry authority changes this gate does not evaluate. Attribution is LINE-SCOPED by deliberate refusal: the subject of a citation is read only from the line carrying it, so a subject stated on an adjacent line or in an enclosing heading is invisible and that citation is simply never judged. Widening attribution to the surrounding block was refused rather than deferred — a heuristic that guesses which nearby sentence owns a link manufactures accusations against correct citations, and this gate's whole value is that a finding is a statement that is false today. The consequence is under-detection, which is the safe direction and is already accounted for by the lower-bound clause above. The cases where attribution WOULD be ambiguous are skipped rather than guessed: a line citing two or more apex paths produces no mismatch finding at all, and a bare id that is itself a live apex is not counted as evidence, so the any-match rule is order-independent over the resolvable ids and never depends on which link came first."
  },
  "purpose": "Make a syntactically valid, semantically wrong ADR citation impossible to reintroduce, by resolving every cited apex against an independent both-sides supersession closure instead of checking that the path exists."
}
