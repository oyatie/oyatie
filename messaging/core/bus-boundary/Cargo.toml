[package]
name = "messaging-bus-boundary"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[dependencies]
messaging-substrate-kernel = { path = "../substrate-kernel" }

[lib]
name = "messaging_bus_boundary"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
