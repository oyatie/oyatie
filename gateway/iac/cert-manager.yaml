# api-gateway — cert-manager + cert issuance config
# Per ADR-0253 + ADR-0295.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-primary
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: ops-security@oyatie.com
    privateKeySecretRef:
      name: letsencrypt-account-key
    solvers:
    - dns01:
        cloudflare:
          email: ops-security@oyatie.com
          apiTokenSecretRef:
            name: cloudflare-api-token
            key: token
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-backup
spec:
  acme:
    server: https://acme.zerossl.com/v2/DV90
    email: ops-security@oyatie.com
    privateKeySecretRef:
      name: zerossl-account-key
    externalAccountBinding:
      keyID: ${ZEROSSL_KID}
      keySecretRef:
        name: zerossl-eab
        key: hmac
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-gateway-cert
  namespace: api-gateway
spec:
  secretName: api-gateway-cert
  duration: 2160h  # 90 days
  renewBefore: 720h  # 30 days
  issuerRef:
    name: letsencrypt-primary
    kind: ClusterIssuer
  dnsNames:
  - api.oyatie.com
  - "*.api.oyatie.com"
  - "*.oyatie.dev"
  usages:
  - digital signature
  - key encipherment
  - server auth
  privateKey:
    algorithm: ECDSA
    size: 256  # P-256
    rotationPolicy: Always
  # Must-staple
  additionalOutputFormats:
  - type: CombinedPEM
  - type: DER
  encodeUsagesInRequest: true
  # CT log: enforced by Let's Encrypt
