# Canonical-base: specs/openslo/canonical-envelope-schema.json (SWEEP-I Slice 4 per ADR-0064)
apiVersion: openslo/v1
kind: SLO
metadata:
  name: developer-sdk-payout-settlement-correctness
  displayName: "developer-sdk-payout-settlement-correctness"
  labels:
    microservice: developer-sdk
    bounded_context: payout
    tier: external-facing
    plane: data
spec:
  service: developer-sdk-payout-settlement-correctness
  description: |
    100% byte-equal payout settlement
  indicator:
    metadata:
      name: developer-sdk-payout-settlement-correctness-sli
    spec:
      ratioMetric:
        counter: true
        good:
          metricSource:
            type: prometheus
            spec:
              query: '1 - (sum(rate(developer_sdk_payout_settlement_mismatch_total[24h])) / sum(rate(developer_sdk_payout_settlement_total[24h])))'
        total:
          metricSource:
            type: prometheus
            spec:
              query: '1 - (sum(rate(developer_sdk_payout_settlement_mismatch_total[24h])) / sum(rate(developer_sdk_payout_settlement_total[24h])))'
  objectives:
    - target: 1.0
      displayName: "1.0 target"
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  alertPolicies:
    - kind: AlertPolicy
      metadata:
        name: developer-sdk-payout-settlement-correctness-fast-burn-1h
      spec:
        description: Fast-burn 1h burn-rate 14.4x → page on-call (Sev-2)
        notificationTargets:
          - target: grafana-oncall:axis-ecosystem-pri
        conditions:
          - kind: AlertCondition
            spec:
              op: gte
              threshold: 14.4
              lookbackWindow: 1h
              alertAfter: 5m
    - kind: AlertPolicy
      metadata:
        name: developer-sdk-payout-settlement-correctness-slow-burn-6h
      spec:
        description: Slow-burn 6h burn-rate 6x → ticket (Sev-3)
        notificationTargets:
          - target: grafana-oncall:axis-ecosystem-tickle
        conditions:
          - kind: AlertCondition
            spec:
              op: gte
              threshold: 6
              lookbackWindow: 6h
              alertAfter: 30m
