apiVersion: oyatie.dev/v1
kind: KubernetesService
metadata:
  name: marketplace-kubernetes-service
  labels:
    service: marketplace
    adr: ADR-0314
spec:
  tenantScope: required
  openBaoPath: secret/marketplace/kubernetes-service
  naming_justifications:
    bnf_v4: "marketplace.iac.kubernetes-service"
    layer_enum: "12-layer-enum: kernel, domain, usecase, app, adapter, infrastructure, rest, grpc, worker, cli, sdk, api"
