#!/usr/bin/env bash
# Compatibility stub only.
#
# This hook is no longer registered by project bootstrap. Keep the path as a
# silent no-op so already-running agent sessions with cached hook tables do not
# fail with command-not-found after the registration removal.

set -euo pipefail

exit 0
