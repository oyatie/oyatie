[package]
name = "oya-bot-autofix-app"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"

[package.metadata.oya]
vertical = "shared"
bounded_context = "autofix"
kind = "shared"
layer = "app"
purpose = "ADR-0531 PROPOSE-only bot skeleton for oya-ci remediation dry-run diff rendering."

[lib]
name = "oya_bot_autofix_app"
path = "src/lib.rs"
doctest = false

[[bin]]
name = "oya-bot-autofix"
path = "src/main.rs"

[dependencies]
oya-ci-gate-contract = { path = "../../libs/oya-ci-gate-contract" }

[lints]
workspace = true
