# G004 cloud-ci false-green hardening team brief — 2026-06-27T18:46Z

## Leader gate before any write
Do not start implementation until leader reports:
- `origin/dev` is `f9a61c3e1bf7e9c3e3520c298b516a0327c386a2` or newer explicitly selected by leader.
- `gh api repos/jason931225/oyatie/commits/$sha/check-runs?per_page=100` has `pending=0`, `failures=0`.
- No open PRs target `dev`.

## Universal lane rules
- One worker owns exactly one gate-app directory. Do not touch shared producer/accounting-registry code.
- No `*.generated.json` edits; do not add generated snapshots. If a command creates generated side effects, restore them and prove no tracked generated diff.
- Rust + Buck2 evidence only for final verification. Cargo is not merge authority.
- TDD sequence: add RED regression first, run targeted unit test to see failure, make minimal fix, run rustfmt + targeted Buck2 unittest + app build + gate test when local BUCK has a gate target.
- PR body headings: `## Issue`, `## Summary`, `## Verification`, `## Traceability`, `## Evidence`, `## Code Review`.
- Per-lane done: PR green, squash merged, post-merge `origin/dev` all check-runs green, clean worktree removed/branch cleaned.

## First write wave assignments after green baseline

### Worker lane 1 — cargo-prefix app
Owned path:
- `cloud/cloud-ci/gates/oya-cloud-ci-cargo-prefix-app/**`

Problem:
- `src/lib.rs` treats missing/non-array/empty `rows` and malformed row objects as an empty corpus.
- Current test `empty_corpus_is_green` should become fail-closed.

Minimal expected change:
- Reuse existing public code `cargo_prefix_unresolvable`; no new `VIOLATION_CODES` expected.
- Add tests for missing `rows`, empty `rows`, non-array `rows`, and malformed row `{}`.
- Emit stable sentinel keys such as `<missing-rows>`, `<empty-rows>`, `<malformed-row>` with `cargo_prefix_unresolvable`.
- Preserve neutral-profile behavior for valid rows.

Evidence targets:
- `buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-cargo-prefix-app:oya-cloud-ci-cargo-prefix-app-unittest`
- `buck2 build //cloud/cloud-ci/gates/oya-cloud-ci-cargo-prefix-app:oya-cloud-ci-cargo-prefix-app`
- `buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-cargo-prefix-app:oya-cloud-ci-cargo-prefix-app-gate`

### Worker lane 2 — bnf-layer-suffix app
Owned path:
- `cloud/cloud-ci/gates/oya-cloud-ci-bnf-layer-suffix-app/**`

Problem:
- `src/lib.rs` treats missing/non-array/empty `rows` and malformed row objects as an empty corpus.
- Current test `empty_corpus_is_green` should become fail-closed.

Minimal expected change:
- Prefer reusing existing `bnf_empty_after_prefix` for corpus/malformed sentinel findings if semantically defensible; otherwise add the smallest new stable corpus-health code with tests and mention it in PR review.
- Add tests for missing `rows`, empty `rows`, non-array `rows`, and malformed row `{}`.
- Emit stable sentinel keys such as `<missing-rows>`, `<empty-rows>`, `<malformed-row>`.
- Preserve neutral-profile behavior for valid rows.

Evidence targets:
- `buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-bnf-layer-suffix-app:oya-cloud-ci-bnf-layer-suffix-app-unittest`
- `buck2 build //cloud/cloud-ci/gates/oya-cloud-ci-bnf-layer-suffix-app:oya-cloud-ci-bnf-layer-suffix-app`
- `buck2 test //cloud/cloud-ci/gates/oya-cloud-ci-bnf-layer-suffix-app:oya-cloud-ci-bnf-layer-suffix-app-gate`

## Read-only reviewer/verifier lane
Review every implementation diff for:
- fail-open / malformed-input gaps;
- over-engineering or speculative abstraction;
- generated artifact policy violations;
- security and perf regressions;
- PR body completeness and reviewer verdict in `## Code Review`.

## Hard stops
- Baseline check failure.
- Shared file conflict.
- Need to edit generated JSON or shared producer code.
- One reproducible non-environmental verification failure after the fix.

## Amendment B — local gate target caveat (2026-06-27T18:51Z)
Local `*-app-gate` targets for cargo-prefix/BNF exec the accounting-registry producer and require `cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app/scm-facts.generated.json`. In a fresh local worktree this file may be absent. Therefore:
- Source-only final evidence is `rustfmt`, targeted `*-unittest`, app `buck2 build`, `git diff --check`, and no generated diff.
- Live-corpus `*-gate` evidence is required only after materializing faces in a disposable clean worktree or when CI provides the producer/freshness setup.
- If workers run the materializer locally, they must restore/clean generated side effects and prove no generated diff before committing.
