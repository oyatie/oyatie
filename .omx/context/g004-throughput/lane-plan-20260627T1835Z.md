# G004 throughput lane plan — 2026-06-27T18:35Z

## Objective
Advance `G004-m2-universal-cloud-ci-product-substr` toward universal, fail-closed, API/data-driven cloud-ci boundaries while preserving the active aggregate `G001` goal. Maximize throughput via disjoint lanes only after this plan exists.

## Hard constraints
- Launch write work only from fresh `origin/dev` isolated worktrees/branches.
- One writer per hot file/directory; no worker touches another worker's owned paths.
- Never hand-edit or commit `*.generated.json`; materializers may run, then generated side effects must be restored unless the lane is explicitly a generated-control lane.
- Rust + Buck2 are authoritative; Cargo is local feedback only and not final evidence.
- GitHub Actions is adapter evidence only; protected required context is `oya-ci-required`.
- Workers never mutate `.omx/ultragoal` or Codex goal state; leader checkpoints non-terminal evidence.

## Current evidence and gate
- PR #949 (`fix: fail closed on empty target parity rows`) merged at 2026-06-27T18:29:20Z as `f9a61c3e1bf7e9c3e3520c298b516a0327c386a2`.
- PR #949 PR-head checks were green; post-merge `origin/dev` checks are still pending during this plan. Write PRs must wait for post-merge green or use read-only/discovery only.
- Open PR collision map: no open PRs currently target `dev`.

## Acceptance criteria for the next wave
1. Every implementation lane starts from current `origin/dev` after post-merge checks are green.
2. Each worker owns exactly one disjoint gate app directory unless leader assigns a read-only lane.
3. Each source change is TDD-driven: add failing regression for malformed/missing/empty input corpus or another false-green mode, then minimal source fix, then targeted Buck2 test/build.
4. Each PR body includes `## Issue`, `## Summary`, `## Verification`, `## Traceability`, `## Evidence`, `## Code Review`.
5. PR checks green; merge via squash; verify post-merge `oya-ci-required`; cleanup worktree/branch.
6. Leader records non-terminal G004 ledger annotations only; no false milestone completion.

## Planned lanes before fan-out

### Lane A — read-only candidate map (explore)
Owned paths: read-only over `cloud/cloud-ci/gates/**`.
Task: identify gate apps still susceptible to missing/malformed/empty producer rows or similar false-green corpus handling; rank by minimal source-only hardening value and disjointness.
Output: candidate table: gate app, file, existing tests, suspected false-green, suggested regression, Buck2 target.

### Lane B — implementation lane template (executor workers, after Lane A and post-merge green)
Owned paths: one worker per selected app, e.g. `cloud/cloud-ci/gates/oya-cloud-ci-<gate>-app/**`.
Task: one gate app per worker; write RED regression; implement smallest fail-closed fix; run targeted unittest/gate/build; open PR; watch/fix CI.
Exclusions: root workflow files, `*.generated.json`, shared helper crates unless explicitly reassigned.

### Lane C — generated-control / materializer drift verifier (read-only unless separately assigned)
Owned paths: read-only over generated materializer outputs and `infra/ci/materialize-cloud-ci-generated-faces.sh`.
Task: verify whether current materialized drift is expected control-plane output; do not commit generated diffs. Produce evidence for whether a future controller-owned generated-settlement PR is needed.

### Lane D — code review / ponytail / security / perf / observability QA
Owned paths: no writes by default.
Task: review each implementation diff for over-engineering, fail-open behavior, secrets, unbounded telemetry/cardinality, performance regressions, and generated artifact policy compliance. Final approval requires independent code-reviewer + architect or an explicit unavailable-review blocker.

### Lane E — Team runtime orchestration
Owned paths: `.omx/state/team/<team>` and worker-owned worktrees only.
Task: launch OMX team only after this plan and a context snapshot exist. Team ledger is authoritative; workers report blockers/conflicts/PR terminal states; leader checkpoints Ultragoal from terminal evidence.

## Stop conditions
- Stop write fan-out if post-merge `origin/dev` checks for PR #949 fail.
- Stop a worker on `claim_conflict`, shared-file conflict, generated artifact hand-edit requirement, one reproducible verification failure without clear environmental cause, or inability to create a clean origin/dev worktree.
- Do not mark G004 complete from single-gate hardenings; annotate only until the milestone's universal API substrate acceptance is materially met.

## Amendment A — critic gate fixes before write fan-out (2026-06-27T18:47Z)

Write fan-out is rejected until all conditions below are true and re-checked by command immediately before launch.

### A1. Post-merge green definition
Baseline is green only when current `origin/dev` equals the expected merge commit and commit check-runs report `pending=0` and `failures=0`; branch protection still names `oya-ci-required` as the single required context, but this wave uses the stricter all-check-runs gate to avoid stacking on a failing advisory/check substrate.

Command shape:
```sh
sha=$(git ls-remote origin refs/heads/dev | cut -f1)
gh api "repos/jason931225/oyatie/commits/$sha/check-runs?per_page=100" --jq '... pending/failures summary ...'
```

### A2. Writer concurrency cap and priority
Initial write concurrency cap is 2 implementation PRs at once, plus at most 1 read-only verifier/reviewer lane. Raise to 3 writers only after one full PR cycle completes: PR green, squash merged, post-merge `origin/dev` green, worktree cleaned, and no generated drift.

Lane selection priority:
1. Highest false-green risk with smallest source-only diff.
2. Reuses an existing violation code/sentinel before adding a new public code.
3. Does not require shared producer/accounting-registry edits.
4. Has exact Buck2 unittest/build/gate targets in its local `BUCK`.

Shared producer/accounting-registry changes are a single-owner lane, never parallel per-app work.

### A3. Generated artifact rule tightened
No implementation worker may commit or intentionally modify `*.generated.json`. Running materializers is allowed only in disposable clean worktrees and must end with tracked generated diffs restored or zero-diff proven. A generated-control lane is read-only unless separately launched as a controller/face-settle generated-only PR; it still may not hand-merge generated JSON.

### A4. Team launch isolation
OMX team runtime must launch from a fresh clean leader worktree at current `origin/dev`, not from the dirty root checkout. The leader copies this plan into the clean worktree for context. Workers own only their assigned gate app directory plus a unique evidence path if required by the current gate/template.

### A5. Per-lane done definition
A lane is done only after: RED regression captured, minimal fix committed, `rustfmt`/targeted Buck2 unittest/build/gate run, `git diff --check`, no tracked generated JSON diff, PR body has canonical traceability sections and `## Code Review`, independent review verdict captured, PR checks green, squash merge complete, post-merge `origin/dev` green, and worktree/branch cleanup done.

### A6. Review and evidence gating
Each Rust gate bugfix PR needs a per-change-class reviewer verdict in `## Code Review`; for this wave use at minimum one independent `code-reviewer` verdict and, for new public violation codes or shared producer changes, an additional `architect`/`critic` verdict. PR body must preserve the canonical headings: `## Issue`, `## Summary`, `## Verification`, `## Traceability`, `## Evidence`, `## Code Review`. If current repo gates require a multispectrum/audit evidence file, each worker uses a unique path; workers never share or overwrite evidence files.

### A7. Candidate assignments after Lane A
Current read-only candidate ranking:
1. `oya-cloud-ci-cargo-prefix-app` — best first slice; reuses `cargo_prefix_unresolvable`, no new public code expected.
2. `oya-cloud-ci-bnf-layer-suffix-app` — likely source-only but new corpus sentinel; assign only with reviewer bandwidth.
3. `oya-cloud-ci-manifest-hygiene-app` — source-only but new corpus sentinel.
4. `oya-cloud-ci-cross-artifact-agreement-app` — higher semantic risk; do not include in first concurrent wave.
5. `oya-cloud-ci-staleness-reaper-app` — beware producer/shared-registry semantics; do not include in first concurrent wave unless Lane C confirms no shared-write coupling.

First write wave after green baseline: cargo-prefix + at most one of BNF or manifest-hygiene, disjoint app directories only.
