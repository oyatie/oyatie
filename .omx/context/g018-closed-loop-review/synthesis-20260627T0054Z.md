# G018 closed-loop review synthesis — after G015 Wave A M0-M4

## Verdict

G015 is complete, but the next write Team wave is **blocked until post-merge `dev` `oya-ci-required` run `28273027942` completes successfully**.

Read-only planning/preflight may continue. Write fanout must not start from the dirty main checkout or the preserved divergent G015 leader worktree.

## Evidence basis

- G015 final PR evidence: `.omx/context/g015-wave-a-m0-m4/final-pr-evidence-20260627T0035Z.json`.
- G015 checkpoint: `.omx/context/g015-wave-a-m0-m4/g015-checkpoint-complete-20260627T0040Z.json`.
- Active aggregate Codex goal snapshot: `.omx/ultragoal/checkpoints/get-goal-active-20260627T0037Z.json`.
- G018 input snapshot: `.omx/context/g018-closed-loop-review/input-20260627T0042Z.md`.
- G018 ledger annotation: `.omx/context/g015-wave-a-m0-m4/g018-next-lane-ledger-annotation-20260627T0042Z.json`.
- `origin/dev`: `a9c24f757880e2290281b6a674ff045a0cd8e62f`.
- Merged PRs: #914, #915, #916, #917, #918.
- Open PRs targeting `dev`: none at G018 start.
- Prior team status: `omx team status g015-wave-a-m0-m4-fou-1fb6d50c` returns `missing` after shutdown.

## Confirmed state

- `G015-wave-a-m0-m4-team-foundation`: complete.
- `G001-complete-oyatie-through-small-merge`: still the active umbrella, **not complete**.
- `aggregateComplete=false`; `artifactComplete=false`.
- #917/#918 status rollups: 42 checks each, no pending/failing checks in captured evidence.
- Verifier confirmed #914/#915/#916 also have 42-check rollups with no pending/non-success checks.
- Cleaned worktrees/branches: #917, #918, temp #915 removed; remote G015 follow-up branches absent.
- Preserved worktree: `/Users/jasonlee/oyatie-worktrees/g015-wave-a-leader-20260626T2328Z` is clean but divergent, retained as provenance and excluded from fanout.
- Main checkout is dirty and behind `origin/dev`; excluded from write lanes.

## Ranked process failures / guardrails

1. **Post-merge trunk CI not green yet** — blocker for write fanout.
   - Guardrail: next Team launch requires a captured success JSON for `gh run view 28273027942 --json status,conclusion,jobs` or an explicit classified failure/fix loop.

2. **G001 scheduler ambiguity** — high false-completion risk.
   - Guardrail: treat G001 as an umbrella only; choose executable pending leaf stories for work. Never call `update_goal` or checkpoint G001 complete until final aggregate quality gates pass.

3. **Reviewer freshness is body-text based** — high auditability risk.
   - Guardrail: next PRs must carry a reviewer artifact or comment/review tied to the final head SHA: `{reviewer, role, final_head_sha, verdict, timestamp, evidence}`.

4. **Final evidence drift** — G015 evidence initially had deeper check rollups for #917/#918 than #914-#916.
   - Guardrail: final wave evidence must include every PR number, head SHA, merge SHA, required-check rollup, reviewer artifact, and cleanup state.

5. **Dirty/divergent local state can poison fanout**.
   - Guardrail: fresh isolated worktrees from current `origin/dev` only; dirty main checkout and preserved leader worktree are read-only provenance.

6. **Team state disappears after shutdown**.
   - Guardrail: before shutdown, export team status, tasks, claims, worker reports, owner paths, and conflict map into an immutable evidence file.

7. **Production-readiness overclaim risk**.
   - Guardrail: next-wave reports must state local gate/evidence scope unless they prove CI, security, operability, performance, and launch readiness.

## Next-wave lane plan, after dev CI green

Launch from fresh `origin/dev` worktrees only. Keep lanes disjoint; any shared/hot file becomes a serial single-writer lane.

### Lane A — G003 repo hygiene/runtime-state boundaries
- Owns root scratch and runtime-state boundary mapping/fencing.
- Candidate focus: root scratch artifacts, `.claude/.codex/.omc/.omx` drift, SessionStart/runtime-state boundaries, GraphQL residue fencing.
- Stop if deletion is destructive or overlaps another lane.

### Lane B — G005 generated-artifact conflict elimination
- Owns generated-artifact merge-surface reduction by materializer/controller/drift-compare gates.
- No hand edits to `*.generated.json`; stop immediately on generated output edit pressure.

### Lane C — G006 Rust purity / shell-Python-MJS retirement
- Owns live authority inventory, fences, and tiny Rust/Buck2 replacement candidates where safe.
- No speculative rewrites of historical/vendor/dead code; no Cargo final evidence.

### Lane D — G004 universal cloud-ci product substrate
- Owns universal API/product-boundary improvements only if isolated from hot workflow files.
- If it must touch workflows/top-level CI hot paths, run it serially as the sole write lane.

## Sequential preflight gates before next Team write launch

1. `dev` post-merge run `28273027942` completed success, or failed and fixed/classified.
2. G018 checkpoint recorded with G001 ambiguity and dev-CI gate status.
3. Fresh `origin/dev` worktrees created; no write use of dirty main checkout or preserved G015 leader worktree.
4. No live prior team.
5. Lane ownership map shows no shared hot-file collisions.
6. Reviewer-artifact and final-evidence schemas prepared before PR creation.

## Stop conditions for next wave

- Any lane touches `*.generated.json` manually.
- Any lane overlaps a shared/hot file without being made serial.
- Any lane uses dirty main checkout or preserved divergent worktree for writes.
- Any report treats G001 as complete before aggregate/artifact completion.
- CI turns red or remains unresolved.
- A lane cannot produce a bounded evidence-backed change or no-op.

## Architect lane addendum

Architect review confirmed the next wave should be G003 + G004 + G005 + G006, with G018 as continuous closeout and G016/G017 deferred. It also sharpened lane ownership:

- Lane 0 leader-only: `.omx/ultragoal/**`, PR inventory, worktree map, no source edits.
- Lane 1 G003: live GraphQL residue in `oya/intelligence/crates/oya-intelligence-api-graphql-{kernel,adapter}/**` plus runtime-state drift classification; avoid cloud-ci and generated files.
- Lane 2 G004: `cloud/cloud-ci/gates/oya-cloud-ci-accounting-registry-app/**`; avoid workflows, `oya-ci.toml`, generated JSON, registry-drift, and generated-artifact control-plane.
- Lane 3 G005: `cloud/cloud-ci/gates/registry-drift/**` and `cloud/cloud-ci/gates/oya-cloud-ci-generated-artifact-control-plane-app/**`; avoid every `*.generated.json`, workflows, `oya-ci.toml`, root specs.
- Lane 4 G006: inventory-first over `infra/ci/**`, `tools/hooks/**`, `tools/buck*/**`, `scripts/**`, `oya/app-shell-frontend/scripts/**`, `oya/feature-flags/reference-implementations/**`, `bin/oya`; avoid cloud-ci, intelligence, generated JSON, root specs, archive-only surfaces.
- Lane 5 G018/G012 verification: evidence/review artifacts only.

Merge order: G003, then G005, then G004, then G006; if G004 and G005 complete together, merge G005 first. Branch pattern: `agent/g018-<goal>-<slice>-<YYYYMMDDTHHMMZ>` from fresh `origin/dev` worktrees.

## Post-merge dev gate final update — 2026-06-27T01:10Z

Run `28273027942` for `oya-ci-required` on `dev` at `a9c24f757880e2290281b6a674ff045a0cd8e62f` completed successfully. Final rollup: 41/41 jobs completed, failures `[]`, conclusion `success`, updated at `2026-06-27T01:10:15Z`. Evidence JSON: `.omx/context/g018-closed-loop-review/dev-run-28273027942-final-20260627T0110Z.json`.

Closed-loop consequence: the G015 post-merge queue caveat is cleared. G018 may be checkpointed complete while G001 remains active/in-progress, and the next write wave may launch from fresh `origin/dev` worktrees only.
