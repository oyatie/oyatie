[package]
name = "k8s-go-runtime"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Go concurrency semantics for ported Go source: channels with close-broadcast and rendezvous, WaitGroup, Once, and the wait.Until background-loop family. Hand-ported once so the rule corpus does not have to translate them per site."

[dependencies]

[lib]
name = "k8s_go_runtime"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
