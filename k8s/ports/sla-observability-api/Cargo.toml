[package]
name = "k8s-sla-observability-api"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish = false
license = "Apache-2.0"
description = "Transport-neutral SLA observability port and DTO seam for managed-Kubernetes clusters. Consumes the control-plane-host status DTOs and exposes deterministic golden-signal summaries."

[dependencies]
serde = { workspace = true }
k8s-sla-observability-kernel = { path = "../../core/sla-observability-kernel" }

[dev-dependencies]
serde_json = { workspace = true }

[lib]
name = "k8s_sla_observability_api"
path = "src/lib.rs"
doctest = false

[lints]
workspace = true
